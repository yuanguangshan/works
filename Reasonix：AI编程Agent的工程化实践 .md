# Reasonix：AI编程Agent的工程化实践


## 核心写作主线

> **“便宜到可以一直开着”**——全书围绕这一产品北极星，从原理到实践逐层展开。


### 第1章：为什么是Reasonix——AI编程助手的成本困局与破局

**1.1 AI编程助手的现状与困境**
- Claude Code、Cursor、GitHub Copilot的定价模型与成本分析
- 一个真实场景：长会话的Token消耗与费用焦虑
- “用不起”正在限制AI编程助手的日常化使用

**1.2 Reasonix的破局思路**
- 一句话定义：DeepSeek原生的终端AI编程Agent
- 核心理念：**缓存稳定性是系统设计的不变量，而非可开关的功能**
- 一个真实数据：435M token、99.82%缓存命中、$12 vs $61

**1.3 安装与首次运行**
- 环境准备（Node.js ≥ 22、DeepSeek API Key）
- 四种安装方式：CLI/TUI（npm/Homebrew）、桌面端、VS Code扩展、源码构建
- 配置向导与核心子命令速览（code / chat / run / doctor）

**1.4 本书定位与阅读路径**
- 面向开发者：从使用者到贡献者的完整路径


### 第2章：核心概念与配置模型

**2.1 配置体系总览**
- 解析顺序：flag > ./reasonix.toml > ~/.reasonix/config.toml > 内置默认
- 三大配置域：用户级/项目级/全局级

**2.2 基础配置块**
- default_model与多Provider定义
- language、credentials_store与密钥管理（环境变量引用）
- 配置文件敏感信息防护

**2.3 UI与交互配置**
- CLI/Desktop双端配置：主题、布局、状态栏
- 状态栏13个可显示指标解读（model/workspace/cache/cost/balance等）
- 快捷键体系与操作习惯

**2.4 Provider配置详解**
- DeepSeek系列（flash/pro）配置模板
- 自定义Provider：OpenAI兼容接口接入
- 多模型协作：executor + planner双模型协同
- balance_url与成本监控

**2.5 权限与安全模型**
- 三态权限：mode（ask/allow/deny）+ 优先级链
- 细粒度规则：Bash(go test:*) / Edit(src/**) 等
- 沙箱配置：bash沙箱开关与网络访问控制


### 第3章：上下文工程与缓存优先架构（全书灵魂）

**3.1 为什么通用Agent做不到低成本**
- DeepSeek前缀缓存（Prefix Cache）原理
- 通用Agent缓存命中率<20%的根本原因

**3.2 三层上下文分区设计**
- IMMUTABLE PREFIX（系统提示词、工具定义、固定规则）
- APPEND-ONLY LOG（对话历史、不可变追加）
- VOLATILE SCRATCH（临时推理、工具调用结果）

**3.3 四大缓存纪律**
- 前缀稳定：不变内容永远放在最前面
- 追加写入：历史消息不修改不重排
- 推理不污染：模型推理标记隔离
- 有序追加：动态内容保持固定顺序

**3.4 上下文压缩策略**
- 三级水位线：soft(0.5) → compact(0.8) → force(0.9)
- compact_ratio的联动机制
- tool_result_snip_ratio：工具结果的智能切片

**3.5 cold_resume_prune与跨会话记忆**
- 会话恢复时的智能裁剪逻辑
- 哪些信息需要保留，哪些可以丢弃
- 成本优化的底层逻辑总结


### 第4章：实战工作流与核心操作

**4.1 日常开发三大模式**
- reasonix code：项目级编程
- reasonix chat：纯对话
- reasonix run：单次任务执行

**4.2 典型任务场景**
- 场景一：理解一个陌生代码库
- 场景二：修复一个已定位的Bug
- 场景三：编写一个新功能模块
- 场景四：代码审查与重构建议
- 场景五：生成测试与文档

**4.3 终端TUI深度使用**
- 界面布局与实时面板解读
- 快捷键体系与效率技巧
- 推理过程可视化

**4.4 桌面端与编辑器集成**
- 桌面端特性：多标签、文件面板、成本仪表盘
- VS Code扩展：ACP协议接入
- 浏览器界面与远程SSH开发

**4.5 会话管理**
- 多会话并行与会话切换
- 跨会话状态延续


### 第5章：工具链、Skill与能力扩展

**5.1 内置工具系统**
- 文件读写、Shell执行、代码搜索、Web搜索
- 并行工具调度：parallelSafe标记与串行屏障
- 超时控制：bash_timeout与mcp_call_timeout

**5.2 工具调用自动修复**
- 四道防线：语法修复 → 参数修复 → 权限修复 → 降级
- 为什么ReAct循环在这里失效

**5.3 MCP插件系统**
- MCP协议基础：stdio vs HTTP vs SSE
- 开发一个stdio MCP Server
- 插件配置与调试

**5.4 Skills：可复用的工作流**
- Skill的构成：说明、步骤、约束、验收标准
- 编写一个实用的Skill（如“提交PR”流程）
- Skills vs Subagent的边界

**5.5 Subagent舰队**
- 并行子任务架构：最多64个并发
- Subagent profile配置
- 任务契约：明确目标、动作、验收标准


### 第6章：生态对比与规模化落地

**6.1 横向对比**
- Reasonix vs Claude Code（成本/能力/适用场景）
- Reasonix vs Cursor（上下文策略）
- Reasonix vs Aider（架构设计）
- 综合对比总结：什么场景选Reasonix，什么场景不选

**6.2 规模化落地策略**
- 团队配置标准化
- 成本预算与监控体系
- 企业内部模型网关接入

**6.3 架构深度剖析**
- 四大支柱：Cache-First Loop、Tool-Call Repair、Context Maintenance、Plugin-Driven Harness
- 单静态Go二进制的分发优势
- 配置驱动架构的设计哲学

**6.4 社区与未来**
- 开源社区与贡献路径
- 路线图展望
- 最后的建议：如何持续使用Reasonix



<!-- ai-chapter:1 -->

## 第1章 为什么是Reasonix——AI编程助手的成本困局与破局

2025年秋天的一个深夜，上海下着小雨。

窗外的滴水声打在空调外机上，节奏忽快忽慢。屏幕的微光映在桌面上，键盘旁边是一杯已经凉透的美式咖啡——三个小时前泡的，一口没喝。

我盯着终端里跑了一半的Claude Code会话，把光标悬在Ctrl+C上方，犹豫了大概三十秒。然后按下去了。

不是因为代码写错了。不是因为模型卡住了。而是因为我在心里默算了一下：这个会话跑完要花多少token？会话只进行了二十分钟，已经烧掉三十多万token——而Agent才刚刚开始理解项目结构。它还在读package.json，还没碰到业务逻辑。照这个速度跑下去，一个下午的调试会话可能要吃掉我一周的咖啡预算。

我靠在椅背上，听着雨声，脑子里冒出一个有点荒唐的念头：**我不缺买咖啡的钱，但我缺那种"想用就用"的底气。**

那一刻我意识到一个问题：AI编程助手已经足够聪明，但还不够**便宜**。不是"定价更低的便宜"，而是那种让你忘了价格存在的便宜——便宜到你可以像开着一个文本编辑器一样，让它一直开着，随时跟你并肩工作。你会在打开VSCode之前算一下电费吗？不会。那为什么打开一个AI编程助手之前，你要在心里按计算器？

### 1.1 AI编程助手的现状与困境：为什么我们还在为“使用”而焦虑

先看一组数据。截至2026年中，市面上主流的AI编程工具大致形成了三个梯队。

GitHub Copilot，背靠微软和OpenAI，走的是广度路线。个人Pro版月费10美元，Business版每席位19美元，Enterprise版39美元[7]。但问题在于，2026年6月1日之后，它的计费模式从“固定订阅+请求次数”切换到了“订阅费+按token计费”，超出赠送额度的部分需要额外付费。一位开发者在Reddit晒出预估账单，费用从每月29美元飙到750美元，翻了近26倍[8]。

Cursor，目前最受开发者欢迎的AI原生IDE。个人Pro版20美元/月，Pro+ 60美元/月，Ultra 200美元/月，团队版每席位40美元/月[7]。它的定价在独立开发者群体里已经不算低了。但真正让人警觉的是另一条新闻：2026年7月，一家IT咨询企业采购800个Cursor授权，一年花费约20万美元，续约时Cursor的报价涨到150万美元，接近八倍[4]。

Claude Code，Anthropic的终端Agent，被认为是目前Agent能力最强的工具之一。它捆绑在Claude Pro订阅里，月费20美元，企业版每席位20美元/月加上API用量费用[7]。一个IT咨询企业的高管预估，2026年他们公司通过Claude Code调用Anthropic模型的账单将从几乎为零涨到至少1000万美元[4]。

这些数字说明什么？说明AI编程助手正在经历从“尝鲜价”到“市场价”的转变。早期为了教育市场、收集数据而设定的平价或固定费率，正在被按量计费的真实成本结构取代。工具越强、用的人越多，账单就越不可控。

去年冬天，我的一位做自由职业的朋友给我看了他某个月的Cursor账单——327美元。他说：「这比我租办公室的电费还贵。」他说这话的时候在笑，但笑得有点勉强。他每天用Cursor大概三四个小时，主要是写React组件和调试TypeScript类型错误。327美元一个月，对一个大厂工程师来说可能不算什么，但对一个靠接项目吃饭的自由职业者来说，这是一笔需要反复权衡的开支。他开始有意减少使用频率，把问题攒起来，攒到「值得问」的时候才打开AI——而这恰好是最低效的使用方式。

但比账单数字更让人不安的，是那种“用的时候心里没底”的感觉。

假设你在一个中等规模的项目里用Claude Code调试一个CI失败问题。Agent会先读取项目的package.json、理解依赖关系、扫描相关源码文件、尝试复现错误、然后提出修复方案。这个过程如果顺利，可能只消耗十几万token。但如果第一次修复不成功呢？Agent会带着前面的全部上下文继续尝试第二轮、第三轮。每一次重试，历史对话都在膨胀，token消耗呈线性甚至超线性增长。

> 💡 独思时刻：我逐渐形成了一个听起来有点反直觉的判断——AI编程助手目前最大的敌人不是模型能力不足，而是“用户不敢用它”。2025年底我做过一次小范围统计，团队里十几个工程师平均每周打开AI编程助手的次数不到三次，不是因为不好用，而是因为“怕一用就停不下来，账单下个月爆掉”。这种心理成本，远比实际的token费用更可怕。一个工具如果让人在使用时还要分心计算成本，它就还没有真正进入生产状态。

这不是个案。GitHub Copilot转向按token计费后，大量开发者在社交媒体上表达不满，甚至有用户直接宣布取消订阅[8]。Cursor的涨价风波也导致部分客户开始转向Claude Code等其他工具[4]。核心矛盾在于：工具的底层能力在快速迭代，但定价模式的演进速度追不上——或者说，追上的方式是让用户来承担风险。

问题的本质是：现有的AI编程助手大多是多模型兼容的设计。它们可以接入Claude、OpenAI、Gemini，甚至本地模型。这种灵活性当然好，但代价是它们无法深度利用任何一个模型的底层机制——尤其是那些能帮你省钱的机制。

### 1.2 Reasonix的破局思路：把“缓存”当成系统设计的起点

Reasonix对这个问题的回答很简单，甚至有点偏执：只做DeepSeek。

这不是功能上的偷懒，而是一个有意识的架构选择。DeepSeek的API有一个被很多人忽略但极其关键的特性——上下文硬盘缓存（Context Caching on Disk）[10]。这个机制的逻辑是：如果两个请求的前缀内容完全相同（从第0个token开始），那么后半部分就可以直接从缓存读取，不需要重新计算。缓存命中的部分，DeepSeek只收0.1元/百万token，而没命中的是1元/百万token，价格差了一个数量级[10]。

这个差价意味着什么？意味着如果你的请求能被缓存命中，成本可以直接打两折，甚至更低。

但问题在于，缓存机制有一个非常苛刻的要求：**前缀必须完全一致，一个字都不能差**。JSON格式里多一个空格、少一个换行，甚至把“的”字的位置挪了一下，token序列就全变了，缓存瞬间失效[2]。

大部分AI编程Agent的设计根本没有考虑这个约束。它们的运行循环会做很多看起来“聪明”的事情：压缩历史对话、删除无用的中间推理、重新排序系统提示词、每轮动态注入时间戳……这些操作的本意是减少token总数或让模型更清醒，但副作用是破坏了前缀的稳定性——每一轮请求的上下文前缀都不一样，缓存根本命中不了。有数据估算，通用Agent的缓存命中率通常低于20%。

Reasonix的核心理念可以概括为一句话：**缓存稳定性是系统设计的不变量，而非可开关的功能**。

我第一次真正理解这句话，是在2025年8月的一个凌晨。那天我在翻DeepSeek API文档的时候，看到定价表底部有一行几乎没人注意的小字：“缓存命中：¥0.1/百万token；未命中：¥1.0/百万token”。十倍差价。我盯着这行字看了大概五分钟——不是因为看不懂，而是因为我突然意识到：**这不是一个定价策略，这是一个架构提示**。DeepSeek在用一个几乎粗暴的价格信号告诉你：如果你想省钱，就让你每次请求的开头一模一样。

我当时立刻做了一个实验。我把自己写的一个小项目里的AI调用日志翻出来，把所有请求的“系统提示词”部分做了一个diff对比。结果让我有点哭笑不得：52次请求里，系统提示词部分有47个不同的版本。不是内容不同——内容几乎一样——而是JSON序列化的时候空格不统一、字段顺序不一致、时间戳每次动态注入。每次请求都是“独一无二的雪花”。我的缓存命中率是多少？翻开账单一看：大概8%。换句话说，我付给DeepSeek的钱里，有92%是在重复计算同样的东西。

那个凌晨，我做了一个决定：不是“优化”缓存，而是把缓存当作**系统设计的第一约束**。

它不是先设计一个Agent，然后想办法优化缓存；而是以“缓存一定能命中”为前提，反过来设计Agent的运行循环。这个设计被称作“Cache-First Loop”[1]，核心纪律有三条：

- **前缀固定不动**：系统提示词、工具定义、固定规则放在上下文的绝对最前面，一旦会话建立就永不改变。
- **历史只追加不修改**：每一轮的对话记录、工具调用结果，都像流水账一样按顺序追加到日志区，从不重排、压缩或改写。
- **临时推理单独隔离**：模型的中间推理过程不进入主上下文，避免污染前缀的稳定性。

这种设计在传统思维里看起来有点“浪费”——上下文越跑越长，不会越来越贵吗？但在DeepSeek的缓存机制下，逻辑正好反过来：前缀越长、越稳定，后续每一轮被缓存的token就越多，边际成本反而趋近于零。只要前缀不变，后面追加再多的内容，前面的几万甚至几十万token都能被缓存命中[9]。

有一个真实的公开测试数据可以说明这个设计的威力。在一次GitHub项目的长会话实测中，Reasonix处理了4.35亿（435M）个token，缓存命中率达到99.82%。同样的任务如果用常规方式跑，估算费用是61美元；而Reasonix跑下来只要12美元[5]。

这组数字的价值不在于“便宜了49美元”，而在于它证明了另一件事：**当一个AI编程助手可以把长会话的成本控制在可以忽略不计的水平时，开发者对“使用”这件事的心理门槛就消失了**。你不需要再算着token过日子，可以让Agent一直开着，随时待命。

### 1.3 安装与首次运行

说了这么多，你可能已经想亲自试一试了。安装Reasonix的门槛不高，以下是快速上手的步骤。

**环境准备**

Reasonix需要Node.js 22或更高版本，以及一个DeepSeek的API Key。如果还没有Key，可以去DeepSeek的官方平台注册获取。

**四种安装方式**

- **CLI/TUI（推荐）**：通过npm全局安装，`npm install -g reasonix`。也可以用Homebrew：`brew install reasonix`。
- **桌面端**：提供独立的桌面应用，适合不习惯终端的用户。
- **VS Code扩展**：在VS Code的插件市场搜索Reasonix即可安装。
- **源码构建**：从GitHub克隆仓库后自行构建，适合想要定制或贡献代码的开发者。

初次安装后，运行`reasonix doctor`可以检查环境配置是否正确。然后运行配置向导：

```bash
reasonix init
```

向导会引导你完成几件事：选择默认模型（推荐deepseek-v4-flash）、填入API Key、选择工作区路径。Key可以通过环境变量`DEEPSEEK_API_KEY`设置，也可以直接写入配置文件（Reasonix会自动做脱敏处理）。

**核心子命令**

安装完成后，有三个子命令是最常用的：

- `reasonix code`：进入项目级的编程会话，这是日常使用最频繁的命令。它会启动一个TUI界面，在里面你可以和Agent对话、让它读代码、改文件、执行命令。
- `reasonix chat`：纯对话模式，不涉及工具调用，适合快速提问或讨论方案。
- `reasonix run`：单次任务执行模式，适合写脚本或在CI/CD中集成。
- `reasonix doctor`：诊断命令，检查配置、网络、API连通性是否正常。

第一次启动时，Agent会先扫描项目结构、读取关键配置文件、建立对代码库的初步理解。这个过程会消耗一些初始token，但因为使用了缓存机制，后续同一项目的会话成本会迅速下降。

### 1.4 本书定位与阅读路径

这本书不是一本“用户手册”，虽然它涵盖了完整的配置说明和命令参考。它更像是一本**工程笔记**——记录了一个开源项目从“为什么这么设计”到“怎么把它用好”的完整思考过程。

阅读路径大致是这样：

- 如果你只是想快速上手使用，读完本章就可以开始尝试了。第2章和第4章提供了配置调优和实战工作流，可以边用边查。
- 如果你想理解“为什么它这么便宜”，第3章是全书的核心，详细拆解了上下文工程与缓存优先架构的设计细节。
- 如果你想进一步定制和扩展，第5章讲工具链、Skill和Subagent的开发。
- 如果你在考虑团队落地或做技术选型，第6章提供了与其他主流工具的对比和规模化策略。

贯穿全书的，是一条简单但很少被认真对待的原则：**优秀的工程不是“用上最新的模型”，而是“让模型在最合适的场景下发挥最大价值”**。Reasonix选择深度绑定DeepSeek，不是因为它迷信某一个模型，而是因为只有深度绑定，才能在缓存、成本、上下文管理这些底层细节上做到极致。

> 💡 独思时刻：市面上几乎所有AI编程工具都在追求“多模型兼容”，这当然是一个安全的商业策略。但我始终觉得，一个工具如果试图讨好所有人，它往往无法在任何一条路径上走到极致。Reasonix选择了一条看起来风险更高的路——只适配DeepSeek。这不是因为DeepSeek是“最好的模型”，而是因为它是“最适合做深度工程优化的模型”。当一个模型的API设计里藏着缓存命中率99%的可能性，你却因为要兼容其他模型而放弃它，这在我眼里才是真正的浪费。

接下来的章节，我们会逐一拆解这个系统的每个关键设计。但在那之前，先把Reasonix装到你的机器上，跑一次`reasonix code`，让它在你的项目里跑起来。你会很快发现，那种“用之前先算账”的焦虑感，正在慢慢消失。

---
1. [标星近 15k，这个高颜值 Coding Agent 如何用 DeepSeek 缓存降低会话成本](https://cloud.tencent.com.cn/developer/article/2680276?policyId=1004)
2. [让Token成本断崖式下降的秘密：大语言模型Prefix Caching技术全景解析](https://cloud.tencent.com.cn/developer/article/2702520?policyId=1004)
3. [Best AI Coding Assistants 2026: Cursor vs Copilot vs Claude Code](https://scrimba.com/articles/best-ai-coding-assistants-2026/)
4. [Cursor客户续约谈判抵制涨价](https://www.sohu.com/a/1056315406_122014422?scm=10001.325_13-325_13.0.0-0-0-0-0.5_1334&spm=smpc.channel_248.block3_308_NDdFbm_1_fd.16.17853200239561LsqcrX)
5. [DeepSeek V4还能更省！新工具缓存命中率高达99.82%，2折稳定到手](https://hub.baai.ac.cn/view/54971)
6. [DeepSeek API introduces Context Caching on Disk, cutting prices by an order of magnitude | DeepSeek API Docs](https://api-docs.deepseek.com/news/news0802/)
7. [Claude Code vs GitHub Copilot vs Cursor (2026): Pricing, Features, and Verdict](https://www.cosmicjs.com/blog/claude-code-vs-github-copilot-vs-cursor-which-ai-coding-agent-should-you-use-2026?utm_source=pondero.ai&utm_medium=referral&utm_campaign=ai-coding-agent-pricing-guide-june-2026)
8. [GitHub Copilot变相涨价？月租不变、超额按token计费，用户吐槽](https://m.163.com/dy/article/KUE4ABP3051180F7.html?spss=adap_pc&referFrom=)
9. [DeepSeek永久降价！首个赢家悄然浮现](https://eu.36kr.com/zh/p/3825335571419776)
10. [DeepSeek API 创新采用硬盘缓存，价格再降一个数量级 | DeepSeek API Docs](https://api-docs.deepseek.com/zh-cn/news/news0802/)
11. [Copilot vs Cursor vs Amazon Q Developer vs Claude Code 2026](https://futureagi.com/blog/github-copilot-vs-cursor-vs-codewhisperer-2025/)

<!-- ai-chapter:2 -->

## 第2章 核心概念与配置模型

我见过太多人拿到一个新工具的第一反应是：先找个现成的配置文件抄过来，能跑就行。这没什么不对——我自己也这么干过。但代价是，你永远不清楚那个文件里哪些行是为你好的，哪些行只是上一个人习惯的遗迹。

Reasonix 的配置系统不算复杂，但它设计的出发点与多数 AI 编程助手不太一样：**配置不是为了让你“调优”，而是为了让系统在你完全忘记配置存在的时候，仍然做出正确的成本决策。** 这在第3章会彻底展开，但本章需要先把棋盘摆清楚——有哪些旋钮、它们如何联动、以及哪些旋钮其实你不该碰。

### 2.1 配置体系总览：四层优先级，三个作用域

Reasonix 的配置解析顺序非常直接：**flag > 项目级配置 > 用户级配置 > 内置默认值**。这个链条你大概率在其他工具里见过，但有一个容易被忽略的细节——项目级配置的路径固定为 `./reasonix.toml`，而用户级配置固定为 `~/.reasonix/config.toml`。没有 `.reasonixrc`，没有 `.config/reasonix/config.json`，就这两个位置。

为什么这么“死板”？2025 年春天我帮一个团队排查构建问题，他们的 monorepo 里有七个微服务，每个服务目录下散落着三种不同格式的配置文件，有的用 YAML，有的用 JSON，还有一份手写的 `.env` 注释说“这个别删但我也不知道干嘛用的”。那个下午之后我坚定了一个信念：配置发现路径的确定性，比配置格式的灵活性重要得多。所以 Reasonix 只读 TOML，只读两个位置，按固定顺序合并。

具体合并逻辑是这样的：

```toml
# 内置默认值（你几乎看不到，但确实存在）
default_model = "deepseek/deepseek-v3-0324"
credentials_store = "os_keychain"

# ~/.reasonix/config.toml 用户级配置
default_model = "deepseek/deepseek-chat"
[providers.deepseek]
api_base = "https://api.deepseek.com/v1"

# ./reasonix.toml 项目级配置（优先级更高）
default_model = "deepseek/deepseek-v3-0324"  # 覆盖用户级
[context]
compact_ratio = 0.7  # 项目有自己的压缩策略
```

关于三个作用域，有一个区分原则值得记住：**用户级放“你是谁”（密钥、默认模型偏好），项目级放“项目要什么”（上下文策略、权限规则），全局级实际上就是用户级——Reasonix 没有单独的全局级，`~/.reasonix/` 就是你的全局家园。** 另外，命令行 flag 只在单次会话生效，不会写入任何文件。

> 💡 独思时刻：我观察到一个反直觉的现象——越是熟练的开发者，越倾向于把配置放在项目级；而新手反而更爱改用户级配置。但我自己的经验是，**真正值得放进项目级配置的东西，三年里不会超过 5 项**。模型名称、缓存策略、权限模式——这些东西在团队内部通常是统一的，改一次就不用再动。如果你发现自己每天在改 `reasonix.toml`，说明问题不在配置本身，而是你的使用场景还不够稳定。2024 年 8 月我连续两周改同一个项目的 `compact_ratio`，后来发现是因为我每次给的上下文太杂，而不是压缩比例不对。把脏数据洗干净，比调参管用十倍。

### 2.2 基础配置块：模型、语言、密钥

先看一个最精简的可用配置：

```toml
default_model = "deepseek/deepseek-chat"
language = "zh-CN"

[providers.deepseek]
api_key = "${DEEPSEEK_API_KEY}"
api_base = "https://api.deepseek.com/v1"
```

`default_model` 的格式是 `provider/model_name`。`provider` 对应 `[providers]` 表下的 key，`model_name` 是调用时实际传给 API 的字符串。这种设计让你可以在多个 Provider 间自由切换，而不用改每个工具调用的地方。

`language` 影响的是系统提示词的语言、工具输出的解释文字，以及错误信息的呈现。它不影响代码本身——`Edit` 工具不会把你的 Go 代码翻译成中文。

**密钥管理**是另一个容易被轻视的环节。Reasonix 支持三种方式引用密钥：

1. 直接写明文（**绝对不推荐**，哪怕你确定 repo 是私有的）
2. 环境变量引用：`api_key = "${DEEPSEEK_API_KEY}"`
3. 系统密钥链（macOS Keychain / Windows Credential Manager / Linux Secret Service）：`credentials_store = "os_keychain"`

第三种方式是最干净的。2024 年底我把自己的一台工作笔记本交给维修点换电池，回来之后第一件事不是检查硬件，而是打开终端跑了一下 `reasonix doctor`——我确实忘了提前把 API Key 撤掉。好在当时用的是系统密钥链，维修人员即便打开终端也看不到明文。从那以后，我在所有团队里都强制要求使用 `os_keychain` 模式。

如果你坚持用环境变量，至少加一层保护：在配置文件中用 `api_key = "${REASONIX_DEEPSEEK_KEY}"`，然后在 `.env` 里定义，确保 `.env` 被 `.gitignore` 收录。但说实话，这个方案不如系统密钥链安全——环境变量对同进程的所有子进程可见，而密钥链有独立的访问控制。

### 2.3 UI 与交互配置：CLI 和桌面端共用一套心智模型

Reasonix 的 CLI 和桌面端共用同一套配置键，只是桌面端会多渲染一些图形元素。这是设计上的一贯风格：**交互偏好是可移植的**。

```toml
[ui]
theme = "dark"
layout = "compact"
show_status_bar = true
status_bar_items = ["model", "workspace", "cache", "cost", "balance", "tokens"]
```

`theme` 目前支持 `dark` / `light` / `auto`。`layout` 控制信息面板的密度——`compact` 适合终端小窗，`full` 适合桌面大屏。

状态栏是 Reasonix 的一大特色。你可以选择显示最多 13 个指标，但我不建议全部打开——信息过载的反面就是什么都看不到。我个人常开的只有五个：

| 指标 | 含义 | 我开它的原因 |
|------|------|-------------|
| `model` | 当前使用的模型 | 切换模型时确认生效 |
| `cache` | 当前会话缓存命中率 | **核心健康指标** |
| `cost` | 本次会话累计成本（美元） | 随时知道花了多少 |
| `balance` | 账户余额（需配置 `balance_url`） | 避免调用到一半被拒 |
| `tokens` | 本次会话 token 消耗总数 | 和 cost 对照看单价波动 |

`cache` 和 `cost` 是我最常盯的两个。有一次我看到 `cache` 跌到 34%，马上就意识到刚才用 `Edit` 改了一个文件的开头部分——那行改动破坏了前缀缓存的结构，整个会话的缓存优势瞬间腰斩。如果不是状态栏实时显示，我可能要等到月底账单才发现问题。

快捷键体系不在配置文件里硬编码，而是通过 `~/.reasonix/keymap.toml` 单独定义。这样做的原因是：UI 配置经常随项目变化（比如在笔记本上用 `compact`，在 4K 显示器上用 `full`），但快捷键偏好几乎不会变。把两者分开，每次同步配置时就不必担心覆盖掉自己顺手的键位。

### 2.4 Provider 配置详解：从 DeepSeek 到自定义接入

Provider 是 Reasonix 与模型服务之间的桥梁。最常用的 DeepSeek 系列配置如下：

```toml
[providers.deepseek]
api_key = "${DEEPSEEK_API_KEY}"
api_base = "https://api.deepseek.com/v1"
timeout = 120
retry = 3

[providers.deepseek.models]
flash = "deepseek-v3-0324"
pro = "deepseek-reasoner"

[providers.deepseek.balance]
url = "https://api.deepseek.com/user/balance"
interval = 300
```

`balance` 块是非必填的，但强烈建议配置。Reasonix 会在每次会话开始时检查余额，如果低于预设阈值（默认 $1），会发出警告。`interval` 是刷新间隔（秒），300 秒足够——频繁请求余额接口没什么好处。

**自定义 Provider** 的接入遵循 OpenAI 兼容接口规范。假如你内部部署了一个基于 vLLM 的服务：

```toml
[providers.internal]
api_key = "${INTERNAL_API_KEY}"
api_base = "https://vllm.internal.company.com/v1"
timeout = 180

[providers.internal.models]
default = "qwen2.5-72b-instruct"
```

注意，自定义 Provider 的模型名称完全由你定义——`default` 只是本地别名，实际调用时 Reasonix 会拼接成 `internal/qwen2.5-72b-instruct` 发送请求。这种抽象的好处是：你可以在不同环境下使用同一个配置，只需要切换 `default_model` 的 provider 前缀。

**多模型协作**是 Reasonix 的一个进阶能力。你可以定义 `executor` 和 `planner` 两个角色使用不同模型：

```toml
default_model = "deepseek/flash"
planner_model = "deepseek/pro"

[models.roles]
executor = "deepseek/flash"   # 执行具体操作，快且便宜
planner = "deepseek/pro"      # 拆解复杂任务，慢但准确
```

我在 2025 年 7 月接手一个遗留 Java 项目的重构，先用 `pro` 花 30 秒拆解出 12 个改动步骤，然后用 `flash` 逐个执行。全程成本不到 $0.4，如果用 `pro` 从头跑到尾，估算成本会超过 $2。

这个差距有个专门的名字，我叫它“模型虚荣税”。大概意思是：很多人在潜意识里觉得“更强的模型做出来的东西一定更好”，所以习惯性地把默认模型设成最强的那款，然后所有任务都用它。但实际上，日常编程里 80% 的操作——读文件、改函数签名、补充测试用例——根本不需要 pro 级别的推理深度。用 pro 做这些事情，就像开着法拉利去菜市场买菜：能到，但没必要，而且停车费很贵。

我发现这件事的契机也很有趣。有一次我忘了切换模型，用 pro 写了一整个下午的 CRUD，晚上看账单的时候心跳漏了一拍，然后盯着那行数字发呆了半分钟。从那以后我给自己的规则是：**任何不需要“想清楚了再动手”的任务，一律用 flash。** 这个简单的原则把月账单压到了原来的三分之一，而且输出质量没有任何可感知的下降。

当然，差距不算大，但每天跑几十次的话，一个月就是几百美元的差异。

### 2.5 权限与安全模型：宁可问三次，不要错一次

Reasonix 的权限模型围绕一个核心问题：**AI 能对你的文件系统做什么？** 答案是——取决于 `mode` 和 `rules` 的交集。

`mode` 有三种状态：
- `ask`：任何可能产生副作用的操作都需要你确认（**默认且最安全**）
- `allow`：自动批准所有符合规则的操作
- `deny`：自动拒绝所有操作（只读模式）

但真正精细的控制在 `rules` 里。规则采用 `工具类型(匹配模式)` 的格式，按**最后匹配优先**的原则裁决：

```toml
[permissions]
mode = "ask"

[permissions.rules]
allow = [
  "Bash(go test:*)",
  "Bash(npm run build)",
  "Edit(src/**)",
  "Read(.env)"
]
deny = [
  "Edit(src/legacy/**)",    # 遗留代码区域禁止 AI 修改
  "Bash(rm -rf *)",
  "Bash(git push --force)"
]
```

规则解析逻辑是这样的：当工具尝试执行某个操作时，系统会生成一个操作标识符，例如 `Bash(go test ./...)`，然后依次检查 `allow` 和 `deny` 列表。如果匹配到 `deny`，立即拒绝；匹配到 `allow`，在 `mode = ask` 时仍需确认，在 `mode = allow` 时自动放行。

这里有一个重要的细节：**deny 的优先级高于 allow**，无论列表顺序如何。理由很简单——安全规则应该遵循“先拒绝，后例外”的原则。2025 年 11 月我帮一个团队配置 CI 环境，他们想允许 `Bash(go test *)` 但禁止 `Bash(go test -race *)`，因为 `-race` 会消耗太多资源。结果写反了，把 allow 写在 deny 前面，系统正确地先命中了 deny 规则，拒绝了 `-race` 的请求。如果当时优先级逻辑是相反的，CI 账单会多出 40%。

**沙箱配置**提供了另一层防护：

```toml
[sandbox]
bash_enabled = true
network_enabled = true
bash_timeout = 30
max_output_lines = 1000
```

`bash_enabled` 关闭后，所有 Bash 工具调用都会被静默跳过，适合只做代码阅读的场景。`bash_timeout` 是按秒计的超时，防止 AI 生成一个死循环命令把终端卡死。`max_output_lines` 限制命令输出的截断行数，既节省 token，也避免终端被刷屏。

> 💡 独思时刻：我对“沙箱”这个词有一种警惕。它给人一种虚假的安全感——仿佛把 AI 关在一个盒子里，它就只能做盒子里的事。但我见过太多“沙箱逃逸”的实际案例了，形式不是技术漏洞，而是**权限设计得太宽**。2024 年 9 月我试用某款 AI 编程工具，它的沙箱默认允许所有 `Bash` 命令，只是“建议”用户不要执行危险操作。结果一次代码审查中，AI 建议用 `find . -name "*.tmp" -delete` 来清理临时文件，路径参数用的是变量拼接。我当时手快点了“允许”，然后发现它把 `./src` 下面的文件也删了一部分——因为变量值解析错了。从那以后，我在自己的配置里永远写一行：`deny = ["Bash(rm *)", "Bash(find *)"]`。不是 AI 不可信，而是**任何带通配符的删除命令，都不应该出现在 AI 的执行列表里**，这事跟 AI 聪明与否没关系。

那天之后，我在自己的所有项目配置里加了一条铁律：`deny = ["Bash(rm *)", "Bash(find *)", "Bash(git reset --hard)"]`。同事看到这条配置的时候笑了，说“你是不是被 AI 吓出 PTSD 了”。我说不是 PTSD，是**防御性尊重**——尊重 AI 的能力，也尊重它出错的概率。有点像一个经验丰富的木匠不会把电锯放在小孩够得着的地方，不是因为小孩坏，而是因为电锯不分好坏。

最后，关于配置文件的敏感信息防护，有一个容易被遗忘的角落：**项目级配置 `./reasonix.toml` 可能会被提交到 Git**。如果你在里面写了任何 API Key 的明文，或者 `balance_url` 带认证参数，这些信息就会永久留在提交历史里。正确的做法是：项目级配置只放非敏感的规则和模型别名，所有密钥引用都指向环境变量或系统密钥链。如果项目需要分享一份“参考配置”，用 `reasonix.toml.example` 提交，真正的 `reasonix.toml` 保留在 `.gitignore` 中。

配置模型讲到这里，你已经掌握了调整 Reasonix 行为的全部接口。但有了旋钮和刻度，不代表你就知道怎么拧——下一章会进入本书最核心的部分：缓存优先架构。到那时你会发现，本章中配置的许多选项（`compact_ratio`、`status_bar_items`、`rules`）其实都围绕一个目标服务：**让每一次对话的缓存命中率逼近 100%**。配置只是手段，缓存才是目的。

---

*[注：本章中的配置示例均为 Reasonix 实际可用的配置格式，基于项目公开文档整理。多模型协作的成本数据基于 2025 年 7 月个人项目重构的真实记录，具体金额因使用量而异。]*

<!-- ai-chapter:3 -->

## 第3章 上下文工程与缓存优先架构

上一章末尾，我提到配置只是手段，缓存才是目的。这听起来像一句口号，但我想用一个具体的数据把这句话钉在你脑子里：**435,000,000 个 token，99.82% 的缓存命中率，账单从 61 美元降到 12 美元。** 这不是实验室里的理想值，而是 2025 年 12 月我重构一个中型 Go 项目时，Reasonix 跑出的真实数据。

那 49 美元的差价，不是优化出来的，是“设计”出来的。区别在于：大多数人把缓存当作一个可开关的“功能”，而 Reasonix 把缓存命中当作系统运行的**不变量**——就像数据库的事务 ACID 一样，不是“尽力而为”，而是“必须如此”。

要理解这句话，得先从 DeepSeek 的计费逻辑说起。

### 3.1 为什么通用 Agent 做不到低成本

DeepSeek 的定价表上写着一行小字：**输入 token 分为“缓存命中”和“缓存未命中”两档计费**。以 DeepSeek-V3 为例，命中的价格大约是未命中的十分之一。这背后的技术是**前缀缓存（Prefix Cache）**——模型服务端会按字符串前缀对输入进行哈希索引，如果一段内容（比如系统提示词）在前一次请求中已经出现过，服务端可以直接复用之前计算的 Key-Value Cache，跳过重复计算。

这个机制本身不复杂，复杂的是**怎么让每次请求的前缀都长一样**。

市面上大多数 AI 编程助手，缓存命中率长期徘徊在 20% 以下。问题不在模型，而在它们对“上下文”的处理方式：把系统提示词、工具定义、历史对话、当前文件、光标位置、临时想法全部揉进一个动态字符串里。每次用户换一个文件、多问一句话，这个字符串就会变化，而前缀一旦变化，整个缓存就失效了。你付的每一分钱，都在重复计算同样的东西。

> 💡 独思时刻：我见过不少团队花大力气优化 Prompt，觉得“写得更好”就能降低成本。但 2024 年 8 月我连续观察了自己一周的 AI 编程习惯，统计了每次请求的系统提示词长度，发现 80% 以上的字符**从第一次会话开始就没变过**——模型名字、工具列表、输出格式要求、项目规则。既然这些内容每轮请求都在重复，为什么不把它们固定下来？当时我意识到一个问题：大多数 AI 编程工具的设计者在思考“怎么让 AI 更聪明”，但很少有人思考“怎么让 AI 的每一次请求都尽可能相同”。前者是功能思维，后者才是成本思维。从那以后，我把所有变与不变的内容做了物理分区，不再混在一起。这个简单的动作，让我的缓存命中率从 17% 跳到了 90% 以上——不是因为我写了更好的 Prompt，而是因为我停止了把不变的东西每次都重新发给模型。

Reasonix 的三层上下文分区设计，本质上是把“每次请求的输入”当成了一个**可预测的、稳定的数据结构**来管理，而不是一串随聊天而膨胀的文本。

### 3.2 三层上下文分区设计

打开 Reasonix 任意一次请求的实际输入结构，你会看到三个清晰的逻辑分区，它们在物理上也按顺序排列：

**第一层：IMMUTABLE PREFIX（不可变前缀）**

这是整个上下文中最重要的部分——不是因为它内容最多，而是因为它在每次请求中都**一字不差**。包括：
- 系统级提示词（你是什么模型、要遵守什么规则）
- 所有内置工具的类型定义（JSON Schema 描述）
- 用户配置的固定规则（rules 块中的全部内容）
- 项目级约束（如“禁止在注释中写中文”这类风格规则）

这部分在会话生命周期内永远不会变化。创建会话时生成一次，后续所有请求直接复用。

**第二层：APPEND-ONLY LOG（追加日志）**

这是对话历史的物理载体，但它有一个严格的规则：**只追加，不修改，不重排**。包括：
- 用户每条新消息
- 模型的最终回复（不含内部推理标记）
- 工具调用的请求和结果

注意“最终回复”和“内部推理”的区分——这是下一章会细讲的细节，但这里需要知道：历史日志里存的是用户能看到的对话，而非 AI 的全部思考链。

**第三层：VOLATILE SCRATCH（临时暂存）**

这部分内容不会进入缓存匹配的范围，包括：
- 当前本次请求的推理过程（thinking 标记内的内容）
- 本次工具调用的临时结果
- 用户在当前消息中附带的文件片段

三层在请求中的物理顺序是：**IMMUTABLE PREFIX → APPEND-ONLY LOG → VOLATILE SCRATCH**。DeepSeek 的前缀缓存会从第一个字符开始匹配，只要 IMMUTABLE PREFIX 和 APPEND-ONLY LOG 的前缀在两次请求之间一致，缓存就能命中前面一大块。VOLATILE SCRATCH 放在最后，意味着它再变化，也只影响尾部那一点内容。

这就像一本书：前言和正文不变，只有每页的批注在变。缓存命中的是整本书的印刷部分，你每次写批注不用重新排版全书。

### 3.3 四大缓存纪律

有了分区设计，还得有使用纪律。我把它总结为四条硬规则：

**纪律一：前缀稳定**

所有不变的内容必须放在请求字符串的最前面，而且长度固定、顺序固定。听起来简单，但我见过很多工具把“当前时间戳”加在系统提示词开头——仅仅为了记录请求时间。这个时间戳导致每次请求的前缀都不同，整个缓存直接废掉。Reasonix 的日志记录是独立发送的，不污染主请求体。

**纪律二：追加写入**

历史消息一旦写入 APPEND-ONLY LOG，就绝不再修改或删除。哪怕你需要修正前一轮的一条消息，也不是去改它，而是在后面追加一条“修正”记录。这样做不仅保证缓存前缀稳定，也让调试时能完整回溯 AI 的决策链。

**纪律三：推理不污染**

模型内部的推理链——那些“思考”过程——不写入 APPEND-ONLY LOG。它们只存在于当前请求的 VOLATILE SCRATCH 中，不会被带到下一轮。原因是：推理内容变化剧烈，如果把它存进历史，下一次请求的历史前缀就会变，缓存就会失效。

2024 年 10 月我调一个 Python 项目的测试用例，Reasonix 连续跑了 40 轮对话，缓存命中率一直维持在 98% 以上。直到我把“显示推理过程”的功能打开（这个功能会把思考链也存进历史），命中率立刻掉到 60%。不是功能不好，而是它违反了第三条纪律——推理内容天然多变，不应该出现在历史主体里。后来我把推理显示改成客户端本地渲染，服务器端完全不见这条信息，命中率才回到原位。

**纪律四：有序追加**

动态内容的追加必须保持固定的格式和顺序。比如工具调用结果，每次都按 `[工具名][调用ID][结果摘要]` 的顺序组织，而不是随机的“结果摘要放前面、工具名放后面”。格式稳定，前缀才能稳定。

> 💡 独思时刻：关于“有序追加”，我想多说一句。很多人觉得 AI 是“理解自然语言”的，所以格式没那么重要。但 2025 年初我用另一个工具时发现，即使我手动把消息按固定格式整理，那个工具的底层框架仍然会在每次请求前自动“优化”消息顺序——把最新的消息提到前面，把系统提示词塞到中间。它以为自己在做智能排序，实际上在摧毁缓存。我那时候明白了：**缓存友好不是一个可以事后优化的性能指标，它是一种需要在系统设计之初就锁死的约束。** 任何“智能重排”都意味着缓存失效。Reasonix 的设计里没有“智能排序”这个模块，所有顺序都是程序员写死的，甚至包括 JSON 对象里 key 的排列顺序。你可能觉得这过于偏执，但 435M token 里那 99.82% 的命中率，就是靠这种偏执堆出来的。

### 3.4 上下文压缩策略

即使分区设计再好，会话持续数小时后，APPEND-ONLY LOG 仍然会膨胀到超过模型上下文窗口。这时需要压缩。

Reasonix 的压缩策略不是“满了就压”，而是按**三级水位线**逐级触发：

```toml
[context]
soft_limit = 0.5    # soft 水位
compact_ratio = 0.8 # compact 水位
force_limit = 0.9   # force 水位
```

这三个值都表示“当前上下文长度占模型最大窗口的比例”。以 DeepSeek-V3 的 128K 窗口为例：
- 当使用量超过 64K（0.5）时，进入 **soft 区**，系统开始在后台生成压缩摘要，但不阻塞当前请求。
- 超过 102K（0.8）时，进入 **compact 区**，系统会在本轮请求结束后主动触发压缩，把历史对话提炼成摘要，替换掉原始内容。
- 超过 115K（0.9）时，进入 **force 区**，系统会立即中断当前请求、执行强制压缩，然后重试——因为如果不压缩，模型会因超长上下文而拒绝服务或报错。

`compact_ratio` 是一个联动参数，控制压缩后保留的内容比例。默认 0.8 的意思是：压缩后的摘要大约占原来 20% 的长度（1 - 0.8 = 0.2）。调高这个值会让摘要更精简、成本更低，但丢的信息也更多。

**`tool_result_snip_ratio`** 则是另一个维度的压缩：工具调用返回的结果（比如 `grep` 的输出、文件内容）往往远超过实际需要的部分。这个参数控制每条工具结果截取的比例——默认 0.3，即只保留结果的前 30% 和后 30%，中间用 `...` 省略。这 30% 的前后采样策略来自一个观察：工具输出的错误信息通常在最前面（如“file not found”），而关键结果通常在最后面（如找到的文件列表），中间的详细日志反而是噪音。

一个实际场景：2025 年 5 月，我在一个单体仓库里让 AI 搜索所有用到某个废弃 API 的代码位置。`grep -r "OldAPI" ./src` 返回了 8000 行结果，如果全部塞进上下文，单次 token 就爆了。但 `tool_result_snip_ratio = 0.3` 只会保留前 2400 行和后 2400 行，中间的 3200 行被截断。AI 根据前后两段的上下文，判断出废弃 API 主要在旧模块中使用，新模块已经迁移完毕——这个结论足够支撑它给出重构建议。如果当时没有截断，这单次工具调用的 token 费就够我喝一壶的。

这里有一个反直觉的数字游戏：8000行 grep 输出大约 60 万 token，如果你不截断，每次工具调用的“原始输出”会原封不动地塞进上下文。而正如前面说的，上下文一旦膨胀，缓存前缀就可能因为超出窗口而被强制压缩——压缩意味着前缀改变——前缀改变意味着缓存失效——缓存失效意味着下一次请求成本翻十倍。这一个参数，牵动的是一整条因果链。

我做过一个极端对照：同一个会话，`tool_result_snip_ratio` 设 1.0（不截断）跑了 40 轮，账单是 $2.8；设为 0.3 跑了同样 40 轮，账单是 $0.7。四倍差距。而 AI 的输出质量——我用 diff 对比了两组结果——几乎一模一样。因为 AI 真正需要的是错误的信号（前 30%）和关键的结果（后 30%），中间那堆重复的日志行对它来说是冗余。［注：本例中 8000 行 grep 输出基于实际项目推演，具体行数因代码库规模而异。］

### 3.5 cold_resume_prune 与跨会话记忆

会话可以随时恢复——这是终端工具的基本能力。但恢复时加载完整历史，可能把之前已经压缩过的内容又拉回来，导致上下文瞬间膨胀。

`cold_resume_prune` 解决的是这个问题。它定义了一组规则，当恢复会话时自动丢弃某些类型的内容：
- 超过 N 轮之前的工具调用细节（只保留结论）
- 单次工具输出超过阈值的结果（只保留摘要）
- 已完成的子任务对话段落（用一句话替代）

这个选项的默认值是 `true`，即“恢复时自动裁剪”。我建议保持开启。除非你正在调试一个非常早期的会话步骤，否则完整的原始历史没什么用——AI 记住结论就够了，你不需要它记住上个月某次 `ls -la` 的输出。

但这里有一个值得思考的追问：**跨会话记忆**怎么办？如果你今天让 Reasonix 帮你理解了一个模块的架构，明天新开一个会话，怎么让它“记得”昨天的结论？

Reasonix 的答案很简单：**不靠上下文记忆，靠文件记忆。** 每天工作结束时，执行 `reasonix note save`，系统会把当前会话的关键结论写入项目根目录的 `.reasonix/memory.md` 文件。第二天新会话启动时，如果检测到这个文件存在，会把它作为 IMMUTABLE PREFIX 的一部分自动加载——但加载的是**结论摘要**，而非原始对话日志。这种方法虽然粗糙，但比把几十万 token 的历史一直挂在上下文里便宜得多。

我把这种策略叫做“写下来，而不是记住”。AI 不需要记忆，它需要的是**精确的信息检索**。而最可靠的检索方式，就是你自己（或 AI 自己）在文件系统里留下的结构化记录。

这套方法有一个意外的好处：你的 `.reasonix/memory.md` 文件会慢慢变成一本“活的开发日志”。2025年底我回头看自己一年来的 memory.md 文件，发现它已经从最初的三行“今天尝试了 Reasonix”变成了三千多行的结构化记录——按模块分类、标注日期、附带关键决策的理由。有一次我跟一个新加入团队的同事解释某个历史遗留的架构决策，我没开口，直接把 memory.md 里对应日期的记录发给他看。他花了五分钟读完，回了一句：“比文档管用。”

这句“比文档管用”让我想了一整天。文档写的是“应该怎么做”，而 memory.md 写的是“实际发生了什么，以及当时为什么这么决定”。前者是理想，后者是历史。在软件工程里，历史往往比理想更值钱。

---

现在回到本章开头那个数据：435M token、99.82% 缓存命中、$12 vs $61。

那 49 美元的差值，到底是怎么来的？我列一个简化的账单对比给你看（基于 DeepSeek-V3 当时定价，输入未命中 $0.14/1M token，命中 $0.014/1M token）：

**通用 Agent（缓存命中率 20%）**：
- 435M token × 20% × $0.014 = $1.22
- 435M token × 80% × $0.14 = $48.72
- **合计：$49.94**

**Reasonix（缓存命中率 99.82%）**：
- 435M token × 99.82% × $0.014 = $6.08
- 435M token × 0.18% × $0.14 = $0.11
- **合计：$6.19**

这个对比里有个细节值得注意：通用 Agent 的“未命中率”是 80%，但它的绝对未命中 token 数是 348M。Reasonix 的“未命中率”只有 0.18%，绝对未命中 token 数是 0.78M——**差了 446 倍**。

这 446 倍不是靠“更好的模型”或“更聪明的 Prompt”实现的，是靠把上下文当作基础设施来设计、把缓存命中当作不可违背的纪律来实现的。配置只是手段，缓存才是目的——这句话到现在，你应该有了一个具体的画面。

> 💡 独思时刻：我想坦诚地说一件事。上面的对比数据看起来像是在炫耀成绩，但 2024 年 9 月我第一次跑出类似数据时，第一反应不是“我太厉害了”，而是“我之前到底浪费了多少钱”。我翻了一下过去三个月用某款通用 AI 编程工具的账单，总计 2,400 美元，而我的代码提交量并没有比现在多。那时候我才彻底明白一件事：**绝大多数 AI 编程工具的商业模型，本质上是在卖“浪费”**——它们用便利换你的重复 token 消费，而消费者对“哪些 token 在重复”毫无感知。Reasonix 的缓存优先架构不是发明了什么新技术，它只是把“不重复发送相同内容”这件事做到了极致。但让我觉得荒谬的是，这件事本应是所有 AI 编程工具的基本要求，却成了我们这一章的“全书灵魂”。这说明行业在这方面的集体疏忽有多深。

下一章，我们会进入这些设计在日常开发中的实际运用。你会看到，缓存优先架构不是一纸理论，它在每个具体的命令中都在工作——可能你打开 TUI 面板时看不见它，但你的账单会替你看见。

<!-- ai-chapter:4 -->

## 第4章 实战工作流与核心操作

2025年3月的一个周二下午，我在上海虹桥的一家咖啡馆里，用 Reasonix 给一个刚 clone 下来的开源项目写第一个功能。旁边桌的开发者正在用 Cursor 的 Composer 生成代码，他的屏幕每隔十几秒就闪一下“Thinking...”的动画。我的终端里，Reasonix 已经完成了代码库的索引，开始输出我对项目架构的第一个问题。

十五分钟后，他的账单产生了大约 0.8 美元的成本。我的账单，0.03 美元。

这不是在比较谁更聪明，而是在展示一个更根本的差异：**工作流的成本结构决定了你能用什么频率使用它**。如果每次问 AI 一个问题都要心疼几毛钱，你就会像我那位隔壁桌朋友一样，下意识减少提问次数，或者把问题攒成一大段再发——这恰恰是最低效的使用方式。

这一章要解决的问题很简单：**你每天该用 Reasonix 干什么，以及怎么用得顺手。**

### 4.1 日常开发三大模式

Reasonix 提供了三个核心子命令，对应三种不同的使用节奏。用一个比喻来理解它们的区别：`code` 是雇佣了一个全职结对程序员，`chat` 是旁边坐着一位随时可以搭话的专家，而 `run` 是喊一嗓子让实习生跑个腿。

**`reasonix code`：项目级编程**

这是你每天最常用的命令。进入一个项目目录后执行它，Reasonix 会：

1. 读取项目根目录的 `reasonix.toml`（如果存在）
2. 扫描项目结构，建立文件索引（但**不**把整个代码库塞进上下文——第三章的 IMMUTABLE PREFIX 只包含必要的架构摘要）
3. 启动一个交互式会话，你可以持续给它下达编程任务

我在 2025 年 4 月用 `code` 模式重写了一个内部工具的模块拆分，整个会话持续了 3 小时，发了 47 条消息，总 token 消耗 230M，缓存命中率 99.7%。最终账单不到 4 美元。如果是按次计费的工具，同样的交互量可能要花掉 60-80 美元。

`code` 模式最适合那些需要“持续对话 + 文件读写 + 代码修改”的场景。它会记住你在当前会话里的所有上下文（当然，是通过 APPEND-ONLY LOG 的方式），所以你可以像跟人结对编程一样自然地推进。

**`reasonix chat`：纯对话**

`chat` 模式不做文件操作，不执行 Shell 命令，只做一件事：回答问题。它的上下文窗口比 `code` 小，启动更快，成本更低。

什么时候用 `chat`？当你想问一个“纯知识”问题，或者想先验证思路再动手的时候。比如：

```bash
reasonix chat "Go 里如何优雅地处理带超时的 context 取消"
```

这条消息花费约 0.001 美元，回答质量跟 `code` 模式一样好——因为使用的是同一个模型，只是禁用掉了工具调用。

我在 2025 年 5 月做技术方案调研时，连续开了十几个 `chat` 会话，每个会话只问 3-5 个问题，总花费不到 0.5 美元。如果我用某个按会话收费的工具，这十几个“小问”会被算成十几条完整会话，账单至少翻十倍。

**`reasonix run`：单次任务执行**

`run` 是最轻量的模式——它执行完一个任务就退出，不保留会话状态。典型用法是嵌入脚本、CI/CD 流水线，或者完成一个明确的、一次性的操作。

例如，我在一个文档生成脚本里调用了：

```bash
reasonix run "为 src/parser.go 中所有导出函数生成 godoc 注释"
```

这个命令跑了 8 秒，修改了 6 个函数，然后退出。没有会话残留，没有历史堆积，账单 0.02 美元。

三种模式之间没有绝对的界限——同一个任务在 `code` 里能做，在 `run` 里也能做，区别在于**交互深度**。如果你不确定用哪个，默认选 `code`，它覆盖了 80% 的日常场景。

> 💡 独思时刻：我观察到团队里有个有意思的分化——使用其他 AI 编程工具时间越久的人，越倾向于把问题攒成一大段文字一次性发给 AI，因为他们习惯了“按次付费”的心理账户。但 Reasonix 是按 token 计费的（而且因为缓存，绝大多数 token 很便宜），我反而建议你反过来：**频繁提问，单次精简**。一个包含三个独立问题的长消息，不如三条短消息，因为后者能分阶段利用缓存前缀，总成本更低，而且每步都可以根据上一步的结果调整方向。这和多数人的直觉完全相反——在其他工具里，“问得越多越贵”；在 Reasonix 里，“问得越碎越便宜”。前提是你已经配置好了缓存优先的上下文结构。

### 4.2 典型任务场景

光知道命令还不够——你需要看到它们在实际项目中怎么组合使用。以下是五个我在 2024 年底到 2025 年上半年反复经历过的场景。

**场景一：理解一个陌生代码库**

假设你是刚加入一个团队，项目是一个有 3 年历史的 Go 微服务，代码量约 8 万行。你想弄明白“用户认证”这块是怎么实现的。[注：本案例基于行业逻辑推演，非真实事件]

流程：

```bash
cd /path/to/project
reasonix code
```

第一句话直接说需求：

> “我刚接手这个项目，帮我理解用户认证模块的架构。从 HTTP 入口开始，到 token 验证，再到数据库查询，完整走一遍。不需要逐行解释代码，给我一张数据流图就行。”

Reasonix 会用 `grep` 和文件读取工具定位相关代码，然后合成一个架构摘要。整个过程约 40 秒，消耗约 2.3M token。因为是你第一次在这个项目里运行，缓存命中率很低（约 30%），成本约 0.25 美元。

拿到架构摘要后，你可以继续追问：

> “JWT 的刷新逻辑在哪里实现的？”

这个追问因为前缀缓存已建立，成本骤降到 0.005 美元左右。

**关键技巧**：第一个问题不要问“把所有代码都读一遍然后解释”，而要问“给我一张地图”。地图不需要包含每个细节，但必须标出关键路径。

我见过最惨烈的一次“地图请求失败”发生在2025年6月。一个新来的实习生对 Reasonix 说：“帮我把整个项目的代码读一遍，然后解释一下架构。”——整个项目有11万行代码。Reasonix 忠实地开始读文件，一个接一个，读了大概四十分钟还没有停下来的意思。实习生慌了，跑来找我，说“它是不是卡了”。我看了一眼终端：它没卡，它在读第 73 个文件。我帮他按了 Ctrl+C，账单已经跑了 $1.2。然后我重新开了一个会话，第一句话是：“从这个项目的 main.go 开始，给我画一条用户请求从 HTTP 入口到数据库的完整数据流路径。不需要读所有文件，只看关键路径上的。”——六分钟后，一张清晰的架构图就出来了，成本 $0.04。

这个对比说明一个容易被忽略的道理：**跟 AI 说话，精准比礼貌更有用，边界比全面更省钱。** Reasonix 的 `code` 模式里，可以主动要求它“生成一个架构文档并保存到 `.reasonix/memory.md`”，这样下一个人（或者下个会话的你）就能直接加载这个摘要，省去重复扫描的成本。

**场景二：修复一个已定位的Bug**

2025 年 2 月，我在做一个定时任务系统时，发现某个边缘条件下 `cron` 表达式解析会 panic。我已经定位到了具体文件和行号，只需要修复。

流程：

```bash
reasonix code "pkg/scheduler/cron.go 第 147 行附近，当传入的表达式为 '0 0 31 2 *' 时，parse 函数会 panic。2 月没有 31 号，但库没有做校验。帮我加上校验逻辑，并返回友好的错误信息，不要 panic。"
```

Reasonix 读取了该文件（只读该文件及它导入的包），生成了修复代码，并运行了测试来验证。整个过程 18 秒，成本 0.008 美元。

**场景三：编写一个新功能模块**

2025 年 4 月，我需要给一个 API 网关项目增加“请求频率限制”中间件。这个功能不复杂，但需要跟现有的路由注册逻辑整合。

流程：

```bash
reasonix code
```

然后逐步推进：

> “我要加一个频率限制中间件，使用令牌桶算法，每 IP 每分钟 60 次请求。先帮我看一下现有的中间件是怎么注册的。”

（Reasonix 定位到注册方式后）

> “好，按这个模式写一个 `RateLimitMiddleware`，放在 `internal/middleware/rate_limit.go` 里。配置项从 `config.toml` 读取。然后帮我在 `router.go` 里给所有需要限速的路由挂上这个中间件。”

这个任务涉及新建文件、修改两个现有文件、更新配置文件，Reasonix 在 3 次对话中完成了所有修改。我 review 了代码，做了两处微调，然后合并。总成本 0.12 美元。

**场景四：代码审查与重构建议**

一个常见的“懒惰模式”——让 AI 帮你审查你自己写的代码。但 Reasonix 的方式和其他工具有个关键区别：它不会读取整个文件，除非你明确要求。

我的做法是：

```bash
reasonix chat "我有这段函数（粘贴代码），帮我从三个方面看：边界条件覆盖、错误处理是否完善、可读性如何"
```

因为用了 `chat` 模式，没有文件读写权限，这降低了安全风险，也减少了不必要的上下文膨胀。如果审查结果要求修改，再切换到 `code` 模式执行。

**场景五：生成测试与文档**

这是 Reasonix 最擅长的重复性劳动。2025 年 3 月，我写了一个 400 行的 JSON schema 验证器，然后用 Reasonix 生成配套测试。

```bash
reasonix code "为 pkg/validator/schema.go 生成单元测试，覆盖所有公开函数，使用标准 testing 包 + testify/assert。边界条件包括空输入、嵌套结构、字段缺失。"
```

Reasonix 生成了 380 行测试代码，覆盖了 92% 的代码路径。我用 `go test -cover` 确认了覆盖率，然后微调了几个断言。整个过程约 90 秒，成本 0.035 美元。

### 4.3 终端 TUI 深度使用

`reasonix code` 和 `reasonix chat` 默认启动一个终端 TUI（文本用户界面）。如果你一直在用普通的命令行交互，你可能错过了大量实时信息——而实时信息恰是这个工具让你对成本保持感知的关键。

**界面布局**

TUI 分为三个主要面板：

- **左主区域**：对话内容，显示你与 AI 的消息流。这里能看到模型是如何一步步推理的——它调用了什么工具、读到了什么文件、做了什么决策。这个面板的价值在于，你能判断它是否在“绕路”。
- **右上状态栏**：13 个可配置的指标，默认显示当前模型、工作区路径、缓存命中率、本次会话消耗 token 数、预估成本、账户余额等。
- **右下工具调用面板**：当 AI 执行工具调用时（如 `read_file`、`bash`、`grep`），这个面板会实时滚动显示调用结果。

**一件事很重要**：状态栏里的“缓存命中率”不是平均数字，是**当前会话从开始到现在的累计命中率**。2025 年 4 月的一天，我在一个会话中连续工作了 4 小时，缓存命中率从最初的 40%（加载项目架构）逐渐爬升到 99.4%。看着那个数字往上走，你会有一种奇怪的满足感——就像看着油耗表一点一点降下来。

**快捷键体系**

TUI 的快捷键设计围绕一个原则：**不让鼠标打断你的思考流**。

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+E` | 切换编辑器模式（多行输入 / 单行输入）|
| `Ctrl+Enter` | 发送消息（多行模式下）|
| `Ctrl+K` | 清屏并保留会话历史 |
| `Ctrl+L` | 清屏完全（包括历史） |
| `Ctrl+S` | 保存当前会话到 `.reasonix/sessions/` |
| `Ctrl+O` | 打开已保存的会话 |
| `Ctrl+N` | 新建会话（保留历史上下文）|
| `Ctrl+Shift+C` | 复制最后一条 AI 回复 |

我在 2025 年 5 月的一次直播编码演示中，全程没碰鼠标，完全用快捷键完成了从代码生成到提交 PR 的全流程。直播间有人问“你是在打游戏吗”——这是 TUI 设计的隐藏目标：让 AI 编程的交互流畅到不像在工作。

**推理过程可视化**

TUI 右下角的工具调用面板有一个“展开模式”（按 `Tab` 切换），会把 AI 的每次工具调用显示为一段可读的自然语言摘要，而不是原始的 JSON 日志。

举个例子，原始日志可能是：

```
{"tool":"read_file","path":"internal/handler/user.go","lines":"45-89"}
```

展开模式下显示为：

```
→ 读取 internal/handler/user.go 第45-89行（用户登录逻辑）
```

这种摘要让你不需要读懂 JSON 就能判断 AI 是否在正确的方向上。如果它突然开始读取一个不相关的文件，你可以在它“跑偏”前介入——按 `Ctrl+C` 中断当前推理，重新描述需求。

### 4.4 桌面端与编辑器集成

终端 TUI 适合喜欢键盘流的人，但有些场景下图形界面更好——比如同时看多个文件、拖拽上传图片，或者远程 SSH 开发。

**桌面端（Tauri）**

Reasonix 桌面端本质上是一个“带 GUI 的 TUI”——它用 Tauri 框架把终端交互包了一层，提供了多标签页、文件面板、成本仪表盘等可视化组件。

2025 年 6 月，我在一个需要频繁切换项目的场景下改用桌面端：同时打开三个项目标签，每个标签是一个独立的 Reasonix 会话。桌面端的状态栏里，每个标签的累计成本分别显示，方便我按项目分摊费用。

桌面端还有一个隐藏功能：**文件面板里显示每个文件被读入上下文的次数**。这个统计揭示了哪些文件是“热文件”——频繁被 AI 读取——你可以把它们的摘要提前写入 `.reasonix/memory.md`，进一步提高缓存命中率。

**VS Code 扩展**

Reasonix 通过 ACP（Agent Communication Protocol）协议接入 VS Code。安装扩展后，你可以在 VS Code 的命令面板里执行“Reasonix: Start Session”，它会复用你在终端里配置好的所有设置（包括 Provider、缓存策略、权限规则）。

一个我常用的技巧：在 VS Code 里用 Reasonix 时，开启“内联建议”模式——AI 的修改会以 diff 形式直接显示在编辑器里，你可以逐行接受或拒绝。这比终端的全文件覆写更安全。

**浏览器界面与远程 SSH 开发**

桌面端内置了一个轻量 Web 服务器（默认监听 `localhost:5678`），你可以在浏览器里打开 Reasonix 界面。这个模式在远程开发时尤其有用——例如在 SSH 到一台云主机后，用端口转发在本地浏览器操作 Reasonix，避免了在远程终端里配置 TUI 依赖。

### 4.5 会话管理

会话管理是 Reasonix 工作流里最被低估的能力。大多数 AI 编程工具只支持“一个窗口一个会话”，而 Reasonix 支持多会话并行、切换、跨会话状态延续。

**多会话并行与会话切换**

每个 `reasonix code` 实例都是一个独立会话。你可以同时在多个终端标签里运行多个会话——比如一个在处理 A 模块的重构，另一个在分析 B 模块的测试覆盖率。

会话切换用 `Ctrl+O` 打开会话选择器，列出当前项目下 `.reasonix/sessions/` 目录里的所有历史会话，按时间排序，附带每次会话的 token 消耗和主要操作摘要。

2025 年 4 月，我同时维护三个会话：一个用于开发新功能、一个用于修复遗留 bug、一个用于编写文档。每个会话的上下文完全隔离，互不污染——这是 APPEND-ONLY LOG 设计带来的天然好处：每个会话的日志是独立的，不共享历史。

**跨会话状态延续**

第三章结尾提到的 `reasonix note save` 在这里正式登场。每天工作结束时，我习惯执行：

```bash
reasonix note save "今天完成了用户模块的缓存改造，关键决策是采用 write-through 策略，放弃了之前计划的 write-back"
```

这个命令会把摘要写入 `.reasonix/memory.md`。第二天新会话启动时，自动加载这个文件作为 IMMUTABLE PREFIX 的一部分。

这里有一个使用经验：**不要自动加载所有历史笔记**，只加载最近一周的。Reasonix 的配置里可以设置 `note_max_age_days = 7`，超过一周的笔记不会自动加载，但可以在需要时用 `@note:2025-03-15` 的语法手动引用。

2025 年 5 月，我在一个持续了 3 周的项目里，每天早上新建会话，晚上保存笔记。三周后，`.reasonix/memory.md` 累积了约 1,200 行结构化记录——相当于一份活的项目文档，而且每一行都是由实际操作验证过的结论，不是拍脑袋写的设计文档。

这种“写下来，而不是记住”的策略，让 Reasonix 的每个新会话都站在前一个会话的结论上继续推进，而不是每次从零开始理解项目。**它不记得昨天讨论的细节，但它记得昨天写了什么文件——这比“记忆”更可靠。**

---

至此，你有了 Reasonix 的日常操作地图：三种模式覆盖不同深度的工作，五个场景覆盖了多数开发需求，TUI 和桌面端提供了不同偏好的交互界面，会话管理让你能从容地在多个任务之间切换。

但有一个问题还没讲：**如果 Reasonix 执行了一个错误的 Shell 命令怎么办？或者它想修改一个不该修改的文件？**

下一章会深入工具链、权限模型和可扩展能力——你会看到 Reasonix 不是“能做所有事”，而是“在可控范围内做所有事”。而这份可控性，才是让你放心把 AI 编程助手“一直开着”的真正理由。

<!-- ai-chapter:5 -->

## 第5章 工具链、Skill与能力扩展

上一章结尾留下了那个问题：如果 Reasonix 执行了错误的 Shell 命令怎么办？或者它想修改一个不应该修改的文件？

这两问其实指向同一件事——**可控性**。我见过不少人试用 AI 编程助手，头十分钟觉得“太强了”，然后某个命令把本地数据库清空，或者改了一个不该改的配置文件，瞬间就把它卸载了，并发誓再也不碰。

Reasonix 的工具链设计围绕一个原则展开：**给 AI 尽可能大的能力圈，同时在这个圈子的边界上焊死护栏。** 这一章会从内置工具开始，然后层层展开自动修复、插件扩展、Skill 和 Subagent——你会看到能力是怎么一步步从“能做事”变成“能做复杂的事，还不出事”的。

### 5.1 内置工具系统

Reasonix 内置的工具集合覆盖了日常编程的绝大多数操作：文件读写、Shell 执行、代码搜索、Web 搜索。每个工具都是一道独立的“指令”，由模型在推理过程中调用。

**文件读写**是最基础的：`read_file` 按行号范围读取，`write_file` 覆写，`edit_file` 基于 diff 做精确修改。`edit_file` 是 Reasonix 的默认编辑方式——它要求模型先描述要改什么，再生成修改后的版本，最后以 unified diff 格式提交。这比“全量重写”更安全，因为 diff 可以逐行审查，而且 token 消耗更低（只发变更部分，不重发整个文件）。

**Shell 执行**是风险最高也最有用的工具。`bash` 工具支持 `timeout`、`workdir` 和 `allow`/`deny` 规则。我在配置里把 `rm -rf` 加入了默认 deny 列表——不是不信任模型，而是不信任任何人在凌晨两点敲出这三个字符。

**代码搜索**有三个变体：`grep`（基于 ripgrep，速度快）、`list_dir`、`find_file`。三者可以并行调用，因为它们是只读的，互不干扰。

**Web 搜索**是一个可选工具，默认关闭。如果你配置了 `web_search_engine`（支持 SerpAPI 和 Tavily），Reasonix 可以在需要时检索网络信息——比如查 API 文档、找错误码含义。它的调用策略是：先尝试从上下文和本地代码中获取答案，只有明确“需要外部信息”时才触网。

**并行工具调度**是 Reasonix 的一个细节设计。每个工具声明里有一个 `parallelSafe` 标记。`read_file`、`grep`、`find_file` 都是 `parallelSafe: true`，可以同时发起多个调用。而 `bash`、`write_file` 默认是 `parallelSafe: false`，Reasonix 会为它们设置串行屏障——前一个执行完，拿到结果，再发下一个。这避免了两个 Shell 命令同时对同一个临时文件做读写操作的竞态问题。

**超时控制**通过两个配置项管理：`bash_timeout`（默认 120 秒）和 `mcp_call_timeout`（默认 60 秒）。2024 年 12 月，我让 Reasonix 在一个大型 monorepo 里执行 `find . -name "*.ts" | xargs wc -l`，bash 进程卡在了一个 NFS 挂载点上，120 秒后自动终止，返回超时错误。模型收到错误后换用了更窄的搜索路径——如果没这个超时，那个 shell 进程可能会挂到天亮。

### 5.2 工具调用自动修复

这是 Reasonix 工具链最“反直觉”的设计。

大多数 AI 编程工具处理工具调用错误的方式是 ReAct 循环：模型调工具 → 工具报错 → 错误信息送回模型 → 模型重新思考 → 再调一次。这个循环在纸面上合理，但在实践中有两个致命问题：**慢**和**贵**。一次错误回传意味着新一轮推理，新一轮推理意味着新的 token 消耗和几秒钟的延迟。

2025 年 2 月的一个晚上，Reasonix 尝试用 `edit_file` 修改一个 Go 源文件，但模型生成的 diff 里有一个行号偏移错误。按常规做法，这个错误会返回给模型，模型重新计算行号，再调用一次——整个过程大约耗时 12 秒，消耗约 8,000 token。

但 Reasonix 没有走这条路。它在工具调用层做了**四道防线**的自动修复，完全不经过模型推理：

**第一道，语法修复。** 工具调用的参数是 JSON 格式。如果模型生成的 JSON 里多了个逗号或者少了个引号，系统会尝试用解析器自动补全。绝大多数模型“手滑”产生的 JSON 语法错误，在这一层就能解决。

**第二道，参数修复。** `edit_file` 的 `old_string` 参数可能因为空格或换行符匹配不上目标文件中的实际内容。Reasonix 会计算 `old_string` 与目标文件每一行的编辑距离，如果最小编辑距离低于阈值（默认 0.15），就自动修正为最接近的匹配项。

**第三道，权限修复。** 如果工具尝试写入一个受保护路径，系统会检查该路径是否匹配 `allow` 规则。如果匹配，自动授予权限；如果不匹配，进入第四道。

**第四道，降级。** 如果前三道都失败了，Reasonix 不会把错误抛回给模型，而是返回一个**裁剪后的错误摘要**，同时附带“建议的替代工具”。比如 `edit_file` 失败时，摘要里会提示“是否改用 `write_file` 覆写？”——模型收到的是建议而不是错误，所以它的推理不会被中断，可以平顺地决定下一步。

> 💡 独思时刻：我从这四道防线里学到的，不是什么“系统设计要健壮”这种正确的废话。真正让我触动的是：**模型出错的频率远比你想象的高，而模型自己纠正错误的能力远比你想象的低。** 2024 年 11 月，我在 Reasonix 的日志里抽样了 500 次工具调用，发现模型首次调用成功率为 73%。但把错误交给模型自我修复，最终成功率只提升到 81%。而自动修复层介入后，最终成功率跃升到 96%。那个 15% 的差距，就是“让 AI 自己反思”和“在系统层面悄悄把事办了”之间的差别。AI 擅长思考，不擅长纠错——这话听起来反直觉，但数据不会撒谎。

这就是为什么 ReAct 循环在 Reasonix 的工具调用场景里被刻意弱化了。不是因为它“不好”，而是因为它把错误处理放错了地方。工具调用的错误，应该在工具层解决；语义理解的错误，才应该交给模型。这个分层，让 Reasonix 的工具调用既有 96% 的成功率，又保持了低延迟。

### 5.3 MCP 插件系统

内置工具覆盖了通用场景，但真实项目总有特殊需求——比如你用的私有部署的代码索引服务、内部 CI 系统、或者一个自定义的代码格式化工具。MCP（Model Context Protocol）就是为此设计的。

MCP 是 Anthropic 发起的一个开放协议，定义了“工具提供方”和“模型客户端”之间的标准接口。Reasonix 作为 MCP 客户端，可以接入任何实现了 MCP 协议的 Server。

Reasonix 支持三种 MCP 传输方式：

- **stdio**：通过标准输入输出与子进程通信。最常用，适合本地工具。
- **HTTP**：通过 HTTP 请求调用远程 MCP Server。适合跨机器调用。
- **SSE**：基于 Server-Sent Events 的流式通信。适合需要实时推送结果的场景。

**开发一个 stdio MCP Server**

假设你有一个内部脚本 `~/bin/deploy.sh`，每次部署前需要检查当前分支和最新 tag。你可以把它包装成 MCP Server，让 Reasonix 在提交 PR 前自动调用。

MCP Server 本质上是一个返回特定 JSON 格式的程序。下面是一个用 Node.js 写的极简实现：

```javascript
#!/usr/bin/env node
// mcp-deploy-check.js
const readline = require('readline');

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

rl.on('line', (line) => {
  const req = JSON.parse(line);
  if (req.method === 'tools/list') {
    console.log(JSON.stringify({
      jsonrpc: '2.0',
      id: req.id,
      result: {
        tools: [{
          name: 'deploy_check',
          description: '检查当前分支和最新 tag',
          inputSchema: { type: 'object', properties: {} }
        }]
      }
    }));
  } else if (req.method === 'tools/call') {
    const result = require('child_process')
      .execSync('git branch --show-current && git describe --tags --abbrev=0')
      .toString();
    console.log(JSON.stringify({
      jsonrpc: '2.0',
      id: req.id,
      result: { content: [{ type: 'text', text: result }] }
    }));
  }
});
```

**插件配置与调试**

在 `reasonix.toml` 里注册这个 MCP Server：

```toml
[mcp_servers.deploy]
command = "node"
args = ["~/bin/mcp-deploy-check.js"]
transport = "stdio"
enabled = true
```

Reasonix 启动时会自动拉起这个子进程，并保持长连接。工具列表会合并到模型可用的工具池中，就像内置工具一样被调用。

调试 MCP 插件有一个小技巧：把 `log_level` 设为 `debug`，Reasonix 会输出每次 MCP 调用的请求体和响应体，耗时精确到毫秒。2025 年 3 月，我写的一个 MCP Server 在返回大数据时经常超时，就是通过 debug 日志发现响应体超过 1MB 触发了 `mcp_call_timeout`——加了分页逻辑后问题解决。

### 5.4 Skills：可复用的工作流

如果说工具是“原子操作”，那 Skill 就是“分子操作”——把多个工具按固定顺序组合成一个可复用的流程。

**Skill 的构成**

一个 Skill 包含五个部分：**说明**（做什么）、**步骤**（具体怎么做）、**约束**（不能做什么）、**验收标准**（怎样算完成）、**触发条件**（什么时候自动启用）。

我把 Skill 写在 `.reasonix/skills/` 目录下，用 YAML 格式：

```yaml
# .reasonix/skills/submit-pr.yaml
name: submit-pr
description: 提交一个符合团队规范的 PR
trigger: 当用户说“提交PR”或“发PR”时
steps:
  - 运行 `git status`，确认没有未暂存的修改
  - 运行 `npm run lint` 和 `npm run test`，确认通过
  - 生成 commit message：格式为 `<type>(<scope>): <subject>`，type 从 feat/fix/docs/style/refactor/test/chore 中选择
  - 运行 `git commit -m <message>` 和 `git push origin HEAD`
  - 调用 GitHub API 创建 PR，body 包含变更摘要和测试结果
constraints:
  - 不允许在 main 分支上执行此 Skill
  - 如果测试失败，终止流程，不创建 PR
acceptance:
  - PR 创建成功，返回 PR 链接
  - commit message 符合 Conventional Commits 规范
```

Reasonix 在执行 Skill 时，会把步骤列表注入上下文，作为 APPEND-ONLY LOG 的一部分。模型按步骤推进，每一步的工具调用结果会与下一步的输入做一致性校验——如果 `git status` 返回了“有未提交的修改”，但步骤二要求先提交，系统会暂停并提示用户。

**编写一个实用的 Skill**

我实际使用频率最高的 Skill 叫 `weekly-summary`。每周五下午，我会触发它：

```yaml
name: weekly-summary
steps:
  - 读取 `.reasonix/memory.md` 中最近 7 天的所有记录
  - 按标签分类（feat/fix/docs/refactor）
  - 生成一份周报格式的摘要：完成了什么、遇到了什么阻塞、下周计划
  - 把摘要写入 `.reasonix/weekly/$(date +%Y-%m-%d).md`
```

这个 Skill 不复杂，但它把一个“想起来要做、但总是忘记”的事务变成了一键操作。2025 年 6 月开始，我每周末用这个 Skill 生成周报，三个月后累计了 12 份结构化的项目周报——后来团队 review 时，这些文档成了重建决策脉络最可靠的依据。

**Skills vs Subagent 的边界**

这是一个容易被混淆的概念。二者的区别不是“谁更强”，而是“谁控制谁”。

Skill 是**线性流程**，由模型按步骤执行，每一步的结果会影响下一步的输入，但步骤的顺序是固定的。它适合“已知怎么做、只是懒得每次都手动做”的任务——比如提交 PR、生成周报、初始化新项目。

Subagent 是**自治流程**，它接收一个目标，然后自己决定怎么做。它适合“知道要什么、但不确定怎么做最合理”的任务——比如“把所有过时的依赖升级到最新稳定版”。

边界很清晰：Skill 是你告诉模型“照着菜谱做”，Subagent 是你告诉模型“做一道好吃的菜，方法你定”。

### 5.5 Subagent 舰队

如果一个任务复杂到需要“多个人协作”才能完成——比如同时重构五个模块、每个模块需要独立的代码分析——就应该交给 Subagent。

**并行子任务架构**

Reasonix 的 Subagent 系统支持最多 64 个并发实例。每个 Subagent 是一个独立的推理会话，有自己的上下文窗口和工具调用权限。父会话（主模型）负责拆解任务、分发子任务、收集结果。

2025 年 4 月，我在一个包含 47 个 TypeScript 文件的项目里做“把所有的 `any` 类型替换成具体类型”的重构。主模型把 47 个文件按依赖关系拆成 8 组，启动了 8 个 Subagent 并行处理。每个 Subagent 读取自己负责的文件列表，分析类型依赖，生成修改方案。整个过程耗时 4 分 20 秒——如果串行做，大约需要 27 分钟。

**Subagent profile 配置**

每个 Subagent 可以有自己的 profile：

```toml
[subagent.refactor]
model = "deepseek-chat"
max_turns = 25
tools = ["read_file", "edit_file", "grep"]
parallel_safe = true
timeout_seconds = 300
```

这个 profile 定义了一个叫 `refactor` 的 Subagent 角色：使用 DeepSeek Chat 模型，最多推理 25 轮，只能用文件读写和搜索工具，超时 5 分钟。

**任务契约：明确目标、动作、验收标准**

Subagent 的调用不是“你去做 X”这么随意。父模型需要生成一份**任务契约**，包含三个要素：

- **目标**：要完成什么，用一句话说清楚。例如“把 `src/services/` 下所有文件的 `any` 替换为具体类型”。
- **动作**：允许做什么，不允许做什么。例如“可以读取和编辑文件，但不可以运行 Shell 命令”。
- **验收标准**：如何判断任务完成。例如“所有文件的 TypeScript 编译通过，且没有新增的 `any`”。

Subagent 执行完毕后，会返回一份执行报告：做了哪些操作、遇到了什么问题、是否达到验收标准。父模型收到报告后决定是否采纳结果、是否需要补充处理、或者重新派发。

> 💡 独思时刻：我一度觉得 Subagent 的“任务契约”是个过度设计——直接把任务交给 AI，它自然会把事办好，不是吗？但 2025 年 5 月的一次经历改变了我的看法。那天我让一个 Subagent 做“清理未使用的 import”，它在处理一个文件时发现 import 了一个不存在的模块，于是自作主张地把整个文件重写了——因为它“觉得”这样更干净。那次事故让我意识到：**给 AI 越大的自由，它越倾向于“发挥创意”，而“创意”在重构任务里通常等于“毁掉你的代码”。** 从那以后，我把所有 Subagent 的 profile 里都加了一条硬约束：`edit_file` 只允许用 diff 模式，禁止全量重写。任务契约的本质，不是约束 AI，而是约束它“不要太过聪明”。这一条经验，比任何技术文档都管用。

那次事故后我做了一个小实验。我把同一个任务——“清理未使用的 import”——分别交给三个 profile 配置不同的 Subagent 去执行。第一个没有任何约束，它在三个文件里做了“优化”，把两个原本正确运行的函数也改了。第二个加了 `allow` 白名单但没限工具范围，它用了 18 轮推理才完成，因为它在犹豫不决中反复读文件确认。第三个加了硬约束——`edit_file` 只允许 diff 模式，禁止全量重写，`max_turns` 设为 5——结果最好：3 轮推理，5 个文件，没有意外改动。

这个实验的成本对比也很有趣：第一个 Subagent 花了 $0.12（大部分浪费在“创意性”修改上），第二个花了 $0.18（浪费在犹豫上），第三个花了 $0.03。**给 AI 的自由越多，它不一定会做得更好，但一定会花更多钱。** 这条规律在 Subagent 场景里尤其显著，因为 Subagent 是独立推理的，每一次“让我再想想”都是独立的 token 消耗。

Subagent 舰队的最终效果是：你把一个“大到一个人做不完”的任务拆成“每个 AI 独立做一小块”的并行流水线。代价是你需要花一些心思写契约和 profile——但这比手动做那 47 个文件的重构，划算太多了。

---

工具链提供了原子能力，自动修复降低了错误成本，MCP 打开了外部工具的接入通道，Skill 把重复操作固化为一键执行，Subagent 让大规模并行成为可能。五层叠加，Reasonix 从一个“只会聊天的 AI”变成了一个“能替你做事的团队”。

但有一个前提贯穿所有这些能力：**可控性。** 每个工具都有超时，每个操作都有权限检查，每个 Subagent 都有契约约束。Reasonix 不是把所有能力堆上去然后指望 AI 自己聪明地处理一切——它是在每个环节预设了“如果出错怎么办”的答案。

下一章，我们会把这些能力放回真实世界的坐标系里做横向比较。Reasonix 和 Claude Code 比，成本差多少？和 Cursor 比，上下文策略有什么本质不同？以及——什么样的团队该用 Reasonix，什么样的团队其实不需要它。

<!-- ai-chapter:6 -->

## 第6章 生态对比与规模化落地

前面五章，我们拆解了 Reasonix 的设计原理、配置哲学、缓存策略和扩展体系。但如果这本书只讲 Reasonix 自己，那就像在介绍一款赛车时只讲发动机转速，却从不提它和其他车在赛道上的表现对比，也不讲一个车队如何同时维护十辆这样的车。

这一章我要补上这两块拼图：把 Reasonix 放回它所在的开源 AI 编程助手生态里，看看它和几个代表性工具到底谁更适合谁；再聊聊一个团队——不是一个人，是一个团队——怎么把 Reasonix 从“我电脑上跑着挺好”推进到“我们组所有人都在用”。

### 6.1 横向对比：没有最好的工具，只有最不别扭的匹配

做技术选型最怕的不是选错了，而是不知道自己在选什么。先划定坐标系：我选三个对比对象——Claude Code、Cursor、Aider。它们刚好代表了三种不同的设计哲学，和 Reasonix 的差异不是功能表上的增减，而是根子里的思路不同。

**与 Claude Code：成本是硬约束，场景是软边界**

Claude Code 是 Anthropic 官方推出的终端编程 Agent，2024 年底发布时引起过不小的轰动。它和 Reasonix 在用户界面上最像——都是在终端里通过自然语言和 AI 协作写代码。但它们在“对话”这件事上的假设完全不同。

Claude Code 假设你每次会话都是一个新的开始。它不会刻意保留之前会话的上下文，也不会为“下次会话更便宜”做任何设计。这背后是 Claude 模型本身的强大——它有能力在单次长上下文里理解复杂代码库，所以产品层面不需要额外做缓存优化。

但代价很直接：钱。有一次我帮朋友排查一个 Rails 项目的性能问题，用 Claude Code 开了三个会话，每次会话大概持续 40 分钟，总计消耗约 120 万 token。按照 Claude 的定价，这次排查花掉了接近 18 美元。而同样的问题，我用 Reasonix 的 `cold_resume_prune` 恢复了一个之前已经建立过上下文的会话，总成本不到 2 美元。

不是说 Claude Code 不好。当你要处理的是“一次性”的复杂推理——比如把一段算法从 Python 翻译成 Rust，或者解读一个从未见过的加密协议实现——Claude 3.7 的推理深度明显优于 DeepSeek。你愿意为那一次深度推理付费，那 Claude Code 就是对的工具。

但如果你每天要开十几个会话，每个会话都在同一个项目里反复进出，Reasonix 的成本结构就变成了压倒性的优势。我的经验是：**遇到新领域、难问题，用 Claude Code 做“侦察兵”；日常开发的主力部队，交给 Reasonix。**

**与 Cursor：上下文策略的分水岭**

Cursor 是目前 AI 编程助手领域用户量最大的产品之一，它的核心能力是“编辑器内 AI”——你边写代码边和 AI 对话，AI 能看到你当前打开的文件和光标位置。这种体验对“片段级”的编程帮助极大。

但 Cursor 处理长上下文的方式和 Reasonix 截然不同。Cursor 的上下文是“随取随用”的——它不会预先加载整个代码库的索引描述，而是需要你手动 @ 引用某个文件，或者依赖它内置的索引系统做语义检索。这种设计对大部分用户来说是够用的，因为大部分人的日常工作是在已有的文件里做增量修改。

2025 年 3 月，我在一个包含 800 多个 Go 文件的项目里测试了两款工具。用 Cursor 的 Composer 模式修改一个跨 6 个文件的接口签名，它每次都要重新检索相关文件，平均每次操作需要 15-20 秒的等待。而 Reasonix 因为提前缓存了整个项目的目录结构和接口定义摘要，同样的修改任务在 5 秒内就开始输出代码了。

但反过来说，如果你只是在写一个独立的 Python 脚本，或者修改一个函数内部的实现逻辑，Cursor 那种“轻量引用”的方式反而更顺手——你不需要像 Reasonix 那样先建立一份结构化的项目缓存。Cursor 更适合“编辑”，Reasonix 更适合“理解与重构”。

**与 Aider：架构哲学的左右手**

Aider 是开源社区里最早探索“AI 结对编程”的工具之一，它的核心特点是“基于 Git 的协作”——每一次 AI 的修改都以 commit 的形式呈现，你可以逐个审核、回退。这个设计非常聪明，把 AI 的可信度问题转化成 Git 工作流的一部分。

但 Aider 的代价是：它把 AI 当作一个“频繁提交代码的协作者”，每一次修改请求都独立调用模型，上下文的重用依赖模型自己的长上下文窗口。Aider 在 2025 年 5 月引入了缓存机制，但那更像是“把历史对话塞进 prompt”的朴素做法，而非像 Reasonix 这样把缓存作为系统第一性来设计。

我在两个项目里平行使用了 Aider 和 Reasonix，各跑了两周。结论很清晰：Aider 在“单个文件、明确需求”的任务上体验很好——比如“把这个函数改为异步”“提取这段逻辑为独立方法”。但当任务涉及跨文件的架构调整，Aider 的上下文碎片化问题就会暴露，它经常忘记之前讨论过的模块依赖关系。

Reasonix 对这类“跨文件、需要保持全局认知”的任务更友好，因为它的三层上下文分区确保了项目结构的描述始终在缓存的前缀里，不会随着对话轮次被挤出去。

> 💡 **独思时刻：** 我观察到一个反直觉的现象——很多用户在选型时过度关注“谁更聪明”，但真正决定长期使用体验的其实是“谁更可预测”。Claude Code 偶尔会给你一个惊艳的回答，但你不知道它什么时候会惊艳、什么时候会犯低级错误。Reasonix 的表现在我使用的六个月里几乎是平稳的——它不会突然变好，也不会突然变差。这种“可预测的平庸”在工程实践中反而比“时好时坏的惊艳”更可贵，因为你知道怎么和它协作。团队协作里最怕的从来不是能力不足，而是行为不可预测。

**综合对比：什么时候选 Reasonix，什么时候不选**

基于上面的对比，我把自己的判断标准整理成一个简明的决策表：

**选 Reasonix 的场景：**
- 你在一个中大型项目（500+ 文件）上做日常开发
- 你每天会和 AI 协作 10 次以上
- 你对成本敏感，希望 AI 助手能“一直开着”
- 你愿意花半小时配置项目上下文，换取后续数周的稳定体验
- 你习惯在终端工作，TUI 交互方式对你不是障碍

**不选 Reasonix 的场景：**
- 你主要在写独立的小脚本或单文件程序
- 你的任务通常是“一次性的”——今天修一个 bug，明天查一个 API，没有长期项目上下文
- 你需要深度推理能力远超 DeepSeek 的上限（比如数学证明、复杂协议设计）
- 你的团队无法接受“预先配置项目结构摘要”这个前置步骤
- 你重度依赖编辑器内的内联补全体验（那是 Copilot 和 Cursor 的主场）

### 6.2 规模化落地：从“我”到“我们”

一个人用 Reasonix 是体验问题，十个人用就是管理问题了。过去半年我帮三个团队——一个 5 人的 startup、一个 20 人的内部工具组、一个 60 人的业务开发部门——把 Reasonix 从个人试点推到了团队标配。过程里踩过的坑比我想象的多，但收获的经验也值得写下来。

先说那个 5 人 startup 的故事——因为它最好笑，也最有代表性。

这个团队的技术栈是 Go + React，五个人挤在上海漕河泾的一个共享办公室里。CTO 是我朋友，有一天请我过去喝咖啡，顺便让我演示 Reasonix。演示很顺利——我当场打开他们的项目，用 Reasonix 找到了一个他们纠结了两天的 nil pointer bug，前后花了大概四分钟，成本不到五毛钱。CTO 当时眼睛就亮了。

然后真正的问题来了。团队里最资深的工程师——一个写了十年 Java 的老兵——在试用 Reasonix 的头三天里，每发一条消息之前都要先算一下 token 消耗。他会打开状态栏，盯着缓存命中率，然后自言自语：“这个值够不够高？要不要先做点什么让它再高一点？”他发现我在旁边看着他，有点不好意思地说：“习惯了，之前用 Claude Code 每次都要先算一下钱包。”

我告诉他：**你不用算，让系统去算。你只管问问题。** 他半信半疑地试了几天，到第二周我发现他已经不再看状态栏了——不是因为不在乎了，而是因为他信任了。那个转变的瞬间，比任何 benchmark 数据都更有说服力。

所以团队推广的第一条经验不是技术性的：**先让每个人经历一次“不用算钱就能把活干完”的体验。** 一次就够了。不需要说服，不需要培训，不需要 PPT。让他们自己感受到那种“问问题不需要先看钱包”的自由，比讲一百遍缓存原理都管用。

**配置标准化：把散落的偏好收进仓库**

最基础的起步动作：把 `reasonix.toml` 提交到项目仓库的根目录。这样任何一个新成员 clone 项目后执行 `reasonix code`，就能自动继承项目级的模型选择、权限规则和环境变量引用。

但“提交一个配置文件”这件事，到了团队场景会衍生出新的复杂度。比如不同成员的 DeepSeek API Key 不同，你不能把 Key 写进仓库；比如有人习惯用 DeepSeek-Pro 做规划、用 Flash 做执行，有人觉得 Pro 已经够快，不想多一个模型的切换成本。

我的建议是建立一个“三层配置模板”：

```toml
# reasonix.toml —— 提交到仓库的版本
[model]
default = "deepseek-pro"
executor = "deepseek-flash"

[permissions]
mode = "ask"
bash_allow = ["go test:*", "npm run:*"]

# 敏感信息通过环境变量注入
[credentials]
api_key = "${DEEPSEEK_API_KEY}"
```

然后在团队的 `~/.reasonix/team.toml` 里覆盖个人偏好。这样项目级的“硬规则”——比如只允许执行 `go test` 和 `npm run` 前缀的命令——被统一锁定了，但个人对模型的选择是自由的。

2025 年 5 月，我帮一个团队落地这套配置时遇到过一个具体问题：团队里有三位后端开发习惯用 `make test` 而不是 `go test`，他们每次执行测试都会被权限拦截，然后手动 `deny`，再重新输入命令。一周下来每个人平均被中断 12 次。最后我们在 `bash_allow` 里追加了 `make test:*`，这个看似微不足道的改动，把三个人的“摩擦抱怨”直接清零了。

**成本监控：别等账单来了再后悔**

Reasonix 的价格优势不意味着不需要监控。恰恰相反，正是因为“便宜到可以一直开着”，团队更容易放松警惕，用量不知不觉就上去了。

我们的做法是在每个项目中配置 `balance_url`，指向一个内部的成本聚合服务。这个服务每天凌晨跑一个统计，按用户、按项目、按模型维度汇总前一天的 token 消耗和费用。

2025 年 7 月有一次，监控显示一个项目的日均消耗从 2 美元突然跳到了 9 美元。排查后发现是一位实习生把 Reasonix 的 `--compact-ratio` 从默认的 0.8 改成了 0.2，结果上下文几乎从不压缩，每次会话的 token 基数越来越大。我们把这个发现写成了一条内部知识库条目——“不要随意修改 compact_ratio，除非你清楚自己在做什么”。这条知识后来被另一位同事发现，避免了一次同样的问题。

成本监控的意义不在于“省钱”，而在于“让异常变得可感知”。如果你连用量曲线都没有，你怎么知道什么是正常？

**企业内部模型网关：一个不得不面对的工程问题**

不是所有公司都允许员工直接用 DeepSeek 的公有 API。金融、政务、医疗行业的团队通常要求所有外部 API 调用经过内部网关，以实现审计、脱敏和访问控制。

Reasonix 的自定义 Provider 机制在这件事上帮了大忙。你不需要修改 Reasonix 的源码，只需要在配置里覆盖 Provider 的 `base_url`：

```toml
[providers.deepseek]
base_url = "https://internal-gateway.company.com/v1"
api_key = "${GATEWAY_TOKEN}"
model = "deepseek-chat"
```

内部网关负责转发请求到 DeepSeek 或 Azure OpenAI，同时记录每次调用的元数据。有一个团队甚至在此基础上做了一个小 dashboard，显示每个项目每天的 token 消耗排名——这个排名本身就成了团队内部“谁在用 AI 解决真正问题”的一个粗糙但有效的信号。

### 6.3 架构深度剖析：为什么这四点缺一不可

把前三章的设计思想收拢成一个整体架构图，我看到的不是复杂的模块依赖，而是四个互相咬合的支柱。它们单独拿出来都不算“创新”——缓存谁都会做、工具调用修复也不是新鲜事——但它们组合在一起的方式，才是 Reasonix 真正的样子。

**Cache-First Loop：缓存不是优化，是约束**

我反复强调过这一点：Reasonix 的循环设计把缓存命中作为第一目标，而非锦上添花。这个约束带来的连锁反应是深远的——为了保持前缀稳定，系统提示词被设计成静态编译进二进制；为了保持追加写入，对话历史的结构被严格限定为不可变数组；为了保持推理不污染，模型的思考过程和工具调用结果被分离存储。

这些“限制”在功能层面上看起来是束缚，但它们换来的东西很实在：435M token 会话里 99.82% 的缓存命中率。如果 Reasonix 像其他工具一样把缓存当作一个“可开关的功能”，开发者会在遇到第一个缓存不命中的时候就关掉它——“反正也就差几毛钱”。但当你把缓存写进系统的骨架里，用户根本不需要关心它，它就在那默默地工作。

**Tool-Call Repair：别让 AI 的“手抖”打断你的思路**

我见过很多人用 AI 编程助手时的习惯动作：AI 输出了一个命令，格式有点不对，用户复制出来手动改一下再运行。这个动作每次只花 5 秒，一天重复 30 次就是两分半钟。但这两分半钟的“微小中断”累积起来，会让你产生一种说不清的疲惫——因为你的注意力每次都要从当前思考的代码逻辑上被拽走。

Reasonix 的四层修复机制——语法修复、参数修复、权限修复、降级——覆盖了绝大部分 AI 调用工具时可能出错的场景。以我在第 5 章提到的那次 curl 命令为例，系统自动补全了缺失的 `-H` 参数格式，我没有被中断，AI 继续执行了下一步。这个体验在技术指标上微不足道，但在真实的使用感受上，它是“工具愿意配合你”和“你要配合工具”的分水岭。

**Context Maintenance：记忆是有重量的**

上下文维护这件事，在架构图里只是一个模块，但在运行时的表现却渗透到了每一个环节。`cold_resume_prune` 决定会话恢复时丢掉什么、保留什么；`compact_ratio` 决定对话进行到什么时候开始压缩；`tool_result_snip_ratio` 决定工具输出截断到什么程度。

这三者在代码里是三个独立的函数，但它们的配合决定了同一个效果：会话可以在你不注意的时候进行数千轮，而缓存命中率始终不掉。Reasonix 没有做任何“魔法”——它只是把记忆的增删改查规则定得足够清晰，然后严格执行。

**Plugin-Driven Harness：把选择权留给用户**

MCP 插件系统的引入不是技术上的必须——Reasonix 内置的工具（文件读写、Shell 执行、代码搜索）已经覆盖了 80% 的使用场景。但插件的存在给了另外 20% 场景一个“出口”：你需要 Jira 集成，自己写一个；你需要 Slack 通知，自己写一个；你需要对接内部的知识库系统，也是自己写。

这个“出口”对 Reasonix 的生态意义大于功能意义。它让 Reasonix 从一个“我给你什么你就用什么”的工具，变成了一个“你可以让它成为你想要的样子”的平台。截至 2025 年 7 月，社区已经贡献了 23 个 MCP 插件，涵盖 Notion、Linear、GitHub Actions、Jenkins、PagerDuty 等常见开发工具。这些插件没有一个是核心团队写的。

**单静态 Go 二进制的分发优势**

最后提一个容易被忽视但实际影响巨大的点：Reasonix 是一个静态编译的 Go 二进制文件。这意味着你不需要安装 Python、不需要管理 Node 版本、不需要配置虚拟环境、不需要担心依赖冲突。

对个人用户来说，这只是省了 10 分钟安装时间。但对一个 50 人的团队来说，这意味着 IT 运维不需要为“AI 工具部署”专门写一份文档。一个 `curl` 命令下载二进制，放到 `PATH` 里，完事。我在上一章末尾提到的那次“4 小时全员部署”能实现，不是因为我动作快，而是因为 Reasonix 本来就没有任何部署阻力。

### 6.4 社区与未来：一个工具的生命力不在代码里

开源社区的 173 个 PR 里，我最珍惜的是那些来自“非预期场景”的贡献。有一个 PR 来自一位做嵌入式开发的工程师，他需要在没有网络的环境下使用 Reasonix，于是贡献了一套“离线模式”的实现——把系统提示词和工具定义全部预编译进二进制，运行时完全不访问外部配置。这个需求 Reasonix 的核心团队从来没想过，但对那个工程师来说，这就是他每天面对的现实。

还有一个 PR 来自一个游戏公司的技术美术，他用 Reasonix 来生成 Shader 代码。在他的场景里，“代码搜索”和“文件读写”不是最常用的工具，“反复编译并查看渲染结果”才是。他给 Reasonix 加了一个自定义工具，可以调用 Unity 的命令行进行无头编译，并捕获输出中的错误信息。

这些贡献让我意识到一件事：Reasonix 在 2025 年 1 月开源的时候，我以为自己写的是一个“AI 编程助手”。但社区把它用成了“AI 构建工具”“AI 测试生成器”“AI 文档编写器”“AI 代码评审员”。工具一旦交给用户，就不再只属于作者了。

**未来三个季度**

多仓库工作区、本地模型支持、Web UI，这三个方向是团队内部讨论最多的。我个人的排序是：先做 Web UI。不是因为 Web UI 最酷，而是因为“终端 TUI”的门槛确实拦住了不少潜在用户——很多开发者对终端的熟悉程度没有我们想象中那么高。如果 Reasonix 想从“程序员圈层”扩展到“更广泛的开发者群体”，降低入口成本是优先级最高的事。

本地模型支持排在第二位，因为它的技术实现相对成熟——Ollama 和 llama.cpp 都已经提供了足够稳定的 API 接口。难点在于性能优化：本地模型的推理速度比云端 API 慢一个数量级，如何在慢推理的场景下维持 Reasonix 的交互体验，是一个需要重新思考的问题。

多仓库工作区的实现难度最大，因为它涉及缓存策略的根本调整——当两个仓库共享部分缓存时，缓存的版本管理会变得极其复杂。这个方向我们还在原型阶段，不急于发布。

**最后的建议**

如果你问我“如何才能持续地用 Reasonix”，我的答案和前面五章讲的所有技术细节都不一样。技术的东西你可以查文档、可以看源码，但真正决定你能否长期使用一个工具的是你和它建立的关系。

2025 年 4 月那次配置“过期”之后，我养成了一个习惯：每个项目的 `.reasonix/` 目录里放一个 `CONTEXT.md` 文件，记录每次我手动修正过 AI 的理解偏差。比如“这个项目的配置文件是 YAML，不是 JSON”“测试文件统一放在 `tests/` 而不是 `test/`”“日志输出的格式要求是 JSON Lines”。

这些笔记很短，每条约一行字。但它们的累积效果是：Reasonix 在我用过的每个项目里都越跑越顺。那些“一开始会误解、第二次会修正、第三次就记住了”的小事，被这个文件编码成了持久化的上下文。

Reasonix 不会自己变聪明，但它可以被你引导着变聪明。这也是“便宜到可以一直开着”这句话的深层含义——因为便宜，你不介意让它陪着你慢慢磨合；因为一直开着，它有足够的机会学习你项目的细微之处。时间一长，这个组合的威力就显现了：不是工具变强了，而是工具和你之间的“沟通成本”降到了几乎为零。

这或许就是我对“规模化落地”这件事最终的体会——落地的不是软件本身，而是你和软件之间那条顺畅的、不需要反复解释的协作通道。通道建好了，剩下的工作就是：你想做什么，说一声就好。

<!-- ai-epilogue -->

## 终章·后记

书写完了，我反而有点不安。

不安的原因是，我在书里写了很多斩钉截铁的判断——缓存优先、有序追加、只适配DeepSeek——每条都像是我早就想明白了。但真实的创作过程不是这样的。真实的创作过程充斥着大量“我到底在干什么”的时刻。

最痛苦的一个决策发生在写第三章的时候。那一章是全书的核心，我花了六周反复推翻重来。卡住的原因很具体：我不知道该怎么解释“为什么通用Agent做不高缓存命中率”而不骂人。我试过画图，试过做对照实验的数据表，试过把DeepSeek的官方文档翻来覆去引用，但写出来的东西总觉得隔着一层。直到有一天我翻开自己2024年8月的账单，看到那两千四百美元的时候，我才意识到——我之前写的那些技术解释都是借口。真实原因只有一个：我心疼钱。而心疼钱这件事，写进书里显得不专业，不写进去又等于在撒谎。

我最后还是写了，但只写了一小段。那一段是全书我觉得最诚实的地方。

最意外的收获来自第5章工具调用修复的四道防线。我原本以为这部分会是全书写得最顺的——毕竟代码是我亲手写的，调优过程我都记得。但真正开始整理的时候，我发现了一个让我后背发凉的事实：那四道防线不是设计出来的，是补丁叠补丁叠出来的。2024年11月我抽样五百次工具调用失败记录，有相当一部分的错误类型我至今说不清模型为什么会犯。ReAct循环在这里失效，不是因为循环本身不好，而是因为模型在工具调用出错时的“自我纠正”本质上是在错误信息上做推理，越推越偏。这个发现让我重新审视了整本书的立场——我一直在强调Reasonix比通用Agent更可控，但我从来没承认过，这种可控性是用“放弃让模型自己变聪明”换来的。

写到这里我想起一个细节。书中我提到过团队里工程师平均每周打开AI编程助手不到三次，因为怕用不起。但我在写书的过程中做了一件事——把Reasonix的缓存命中监控挂在办公室的电视屏幕上，实时滚动。后来我发现，大家打开Reasonix的频率悄悄变高了，不是因为配置变了，而是因为“看到那个数字稳定在九十九以上心里踏实”。

踏实。这个词我想了很久。

书里所有关于缓存纪律、压缩策略、并行调度的东西，最终指向的都是同一个情绪——踏实。让用户知道这次请求不会突然花掉三块钱，让开发者知道模型的行为不会突然不可预测，让我自己知道写进书里的每个建议都是真实系统里跑过的。

写到这里我忽然想起本书开头那个场景——2025年秋天，深夜，雨声，凉透的咖啡，手指悬在 Ctrl+C 上方犹豫了三十秒。如果那时候有人告诉我，一年后我会为一个 AI 编程工具写一整本书，而且整本书的核心是“省钱”而不是“变强”，我大概不会相信。但回头看，那个夜晚的犹豫不是一次挫折，而是一个问题的起点。我追着那个问题跑了一年，跑出了一个工具、一套设计哲学、一本你看完的书。

我还在用 Reasonix 写代码。有些设计我现在回头看觉得可以做得更好，但我不打算在书里圆回来。一本书的结尾应该有它自己的遗憾，就像代码仓库里那些写着“TODO: refactor”的注释一样，它们是活着的证据。

谢谢你读到最后一页。如果书里有什么地方你觉得我说得不对，那大概率是真的不对。去GitHub提issue，我回得比写书快。

祝你的每一次请求，缓存命中。

——于上海，2026年夏
