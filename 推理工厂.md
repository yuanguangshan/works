---
sync_time: 2026-09-02 12:56:38
source: clipboard
content_hash: 4f4d6cda934bd8704b98a14e83c4beea1cbb569c051f989b053693cd6f478c0e
---

# 《推理工厂》

> 作者:广山哥  
> 完成时间:2026-09-02

---

## 序言

# 序言

先报一笔账：过去一年，我在大模型 API 上花的钱，从每月几十块涨到了每月大几百块。涨的原因不是单价变贵——恰恰相反，同样的模型，价格每年都在跳水——而是我调用得越来越多，越来越离不开。

于是我产生了一个工程师的好奇心：这钱到底花在哪了？一个 token 从我按下回车，到变成屏幕上的字，中间经历了什么？它为什么值这个价？凭什么输出比输入贵三四倍？凭什么加一个“缓存读”就能打一折，而“缓存写”反而要加价？凭什么 H100 的时租是三美元，而某些云上要卖到十二美元？

带着这些问题，我把家底翻了一遍：我自己写的 LLM 网关 poeapi_go 有六千多行 Go 代码，配置文件里躺着七八个模型渠道；为了搞清楚 mimo-v2.5 为什么报四百，我在一个深夜顺着接口报错挖到了“接口方言”这个问题；为了压账单，我把 TokenPolicy 的四级预算门钉进了我的 CLI。这些个人的、微小的实践，成了我理解大模型 Infra 的解剖标本。

这本书，就是那场解剖的报告。

## 一座工厂

我给这本书起了个名字，叫推理工厂。这不是修辞，而是一种观察方式：GPU 集群就是一座工厂——显存是厂房，权重是固定资产，KV Cache 是在制品，调度器是排班系统，token 是一件件出厂的产品，而账单，是这座工厂唯一不会说谎的年度报告。

用工厂的眼光看，你会发现大模型推理的所有问题都是同一个问题的变体：显存太贵了，而绝大多数显存正在睡觉。业内公开的秘密是，推理 GPU 的利用率常年徘徊在百分之五到十，而训练集群能干到一半以上——这道鸿沟，就是过去三年所有推理框架（vLLM、TensorRT-LLM、SGLang）拼死要填平的洼地，也是这本书十章内容的总纲。

所以这本书的每一章，本质上都在回答同一个问题：怎么让显存别睡觉？第二章算清显存的账（KV Cache 的几何学），第三章讲怎么少扔（PagedAttention 的分页），第四章讲怎么排班（连续批处理），第五章讲怎么减重（量化），第六章讲怎么分工（Prefill 与 Decode 的分离），第八章讲怎么复用（Prompt Cache），第九章看引擎们各自的取舍，第十章把 token 一路追到电费，看清这座工厂的物理地板。

## 我的立场：摩尔定律在调度算法里

硬件圈有一句老话：摩尔定律给了我们晶体管，而软件把它们都吃掉了。在大模型推理这里，事情更有趣：过去三年，同样的硬件，推理吞吐翻了十几倍——靠的不是新芯片，而是调度算法：分页、连续批处理、前缀复用、推测解码。一张 H100 的算力三年间几乎没变，它服务请求的能力却完全不同了。

这是我全书最重要的一个判断：大模型的摩尔定律不在制程工艺里，在调度算法里。制程的进步按年计，而调度算法的进步，每一篇论文、每一个版本都在发生。对个人开发者和小公司来说，这是好消息——你买不起最新的卡，但你读得起最新的论文；推理效率的红利，一直在往会用软件的人手里流。

## 几句方法论的交代

这本书有两个写作上的坚持。

其一，所有关键数字都做成数字卡片，并且能算的我都亲手算过：一个 token 的 KV 占用、一张卡的满产地板价、缓存命中率对账单的影响。数字是这本书的主角，类比只是它的翻译。我要坦白：云上的价格比天气变得还快，书里的报价是写作当时的市价，请把它们当作“量级”而非“标价”来读。

其二，凡涉及我的实践，第一人称，不装：包括我如何为四百错误连夜建独立渠道、如何在渠道集体失效的那天靠熔断器救场、如何用预算门管住自己的手。这些故事的价值不在技术本身，而在它们证明了一件事——个人开发者与大厂，在推理这件事上面对的是同一条物理定律，差的只是规模。

## 三部曲的收尾

这本书是基建三部曲的最后一部。《边缘王国》写了网络的大地：一个人如何在全球边缘网络上领地、耕作、缴税；《命令之下》挖开了交互的地层：终端五十年的九层考古，以及 AI 如何在最上面加盖；这一本走进了计算的厂房：算力如何被调度、被定价、被压榨出每一分利用率。

三部曲合起来，是一个个人开发者对其生产资料的完整盘点：网络、交互、算力。它们都曾是巨头专属的重资产，如今都以按量付费的方式向个人开放——而看清它们的底层逻辑，是从“租客”变成“明白的租客”、再变成“有谈判地位的租客”的唯一路径。

现在，进厂。戴上安全帽，我们去看显存里的在制品。

广山哥
二〇二六年九月，于听雨轩

---

## 目录

- 序言
- 第1章 推理的本质:生成一个字，为什么要跑一遍网络
- 第2章 KV Cache 的物理约束:显存几何学
- 第3章 PagedAttention:把操作系统的分页搬进 GPU
- 第4章 连续批处理:随时插队的调度革命
- 第5章 量化:用精度换显存的交易
- 第6章 Prefill 与 Decode:两段式调度的秘密
- 第7章 模型路由:从我的网关到集群调度
- 第8章 Prompt Cache 的物理实现:前缀复用的经济学
- 第9章 引擎战场:vLLM、TensorRT-LLM 与群雄的取舍
- 第10章 推理的物理极限:把 token 一路追到电费

---

## 第1章 推理的本质:生成一个字，为什么要跑一遍网络

# 第 1 章 推理的本质：生成一个字，为什么要跑一遍网络

## 1.1 工厂开工：一次 API 调用的全景旅程

深夜调过大模型 API 的人，多半注意过一件小事：第一个字总是来得最慢，掐着半秒到两秒的钟点才露面，然后余下的字才哗哗地流出来。最常见的解释是“服务器正在加载模型”。这个解释很直觉，也很错——模型早就在显存里住着了，没有哪张 GPU 会在你按下回车之后才去硬盘上搬 140 GB 的权重——那是一个 70B 模型在 FP16 精度下的身量。第一个字的迟到另有原因，而那个原因恰好藏在这本书要讲的一切的起点处。在揭开它之前，我们先把一次 API 调用从头到尾走一遍，因为本书本质上是一部参观记：GPU 集群是一座工厂，显存是厂房面积，权重是常驻的机床，token 是产品，KV Cache 是流水线边的在制品，调度器是排班系统，而你我的账单，是这座工厂的营业额。

作为 poeapi_go 的作者，我每天替自己转发几百次这样的调用，对这段旅程的每一站都有体感。我们从终端开始。你在命令行敲下一条 curl 命令，指向某个 OpenAI 兼容端点的 /v1/chat/completions 路径，正文是一段 JSON：模型名、一段系统提示词、你的问题、一个流式开关。这个请求离开你的机器之后，第一站往往不是模型，而是网关——鉴权、脱敏、限流、预算检查、选择渠道。我自己写的那个网关有六千多行 Go 代码，干的全部事情翻译成工厂语言就是：登记来客、搜身、发工牌、决定送去哪个车间。生产级推理服务在这一层做的事大同小异，只是规模大几个数量级：负载均衡器把请求派往某个数据中心、某台装着八张 GPU 的服务器。

第二站在推理节点内部。HTTP 服务进程收下请求，先做一件朴素的事：把你的文字切成 token。token 是模型世界的最小计件单位，大致相当于半个到一个汉字、四分之三个英文单词（工程常识口径，不同分词器略有出入）。你写的一段八百字 prompt，进厂时已经是上千个 token 的编号序列——从这一刻起，工厂里流动的不再是文字，而是整数。

第三站是调度器，工厂的排班系统。请求不会立刻开工，它先被放进 waiting 队列排队（materials/raw_vllm.md）；排班员会在合适的时机把它接纳进正在运转的批次。接纳的时机与方式是第 4 章的主角，这里只需记住：一座工厂同时服务成百上千个像你一样的来客，谁先谁后、一批装多少，直接决定这 80 GB 的显存厂房当晚是满负荷出货，还是大面积空转。顺带看一眼排班员的桌面：vLLM 这类引擎维护着三个队列——waiting 里是排队的新客，running 里是正在逐 token 解码的，swapped 里是缓存被暂时挪到主存、请出去等空位的（materials/raw_vllm.md）。排班系统调度的对象并不是请求这个抽象概念，而是显存里具体到字节的位置。

第四站，开工。推理引擎先执行一个叫 prefill（预填充）的阶段：把你整段 prompt 一次性并行送进网络，几十层 Transformer 逐层计算，为每个 token 算出两组后面马上要讲到的向量，存进 KV Cache。这一步是整趟旅程里 GPU 最开心的时刻，算力被喂得饱和，利用率可以冲到九成上下（materials/raw_cost.md）。prefill 做完，第一个 token 被选中，经流式通道推回你的终端——这就是你看到的第一个字。它来得慢，是因为它必须等整段 prompt 走完全部几十层；后面的字快，是因为它们每步只走一遍轻量的增量流程。业界称首 token 延迟为 TTFT、逐 token 延迟为 ITL，量的就是这两段体感。

然后是 decode（解码）阶段：一个循环，每个迭代过一遍网络、吐出一个 token、追加进缓存、推送给用户，直到撞上终止符或长度上限。你看到的“打字机效果”，物理上就是一个每秒重复几十次的工业循环。循环结束，请求出场，计费系统记下两个数：输入多少 token，输出多少 token。计费单上输出单价是输入的三到五倍（materials/raw_cost.md），这不是平台贪心，本章稍后给出它的物理依据。

这条流水线的每一站都在花钱。一台 H100 的云上时租约 2.85 到 3.50 美元，做预留能压到 1.85 美元上下，放到超大规模云上则可能贵到 6.88 美元开外（materials/raw_cost.md）。换算一下：这台机器一小时的租金约一杯咖啡，而它一小时能产出多少 token，取决于流水线排得多满——静态批处理时代，利用率常年只有两到四成（materials/raw_vllm.md），相当于租下一整间厂房、灯和空调全开，机床却有超过一半的时间在空转。我在 poeapi_go 里看到的每一次超时与限流，追到物理层，都是这座工厂在用某种方式告诉你：床位满了，或者机床闲着。理解这本书，就是理解你的账单为什么会是现在这个形状。

值得把 HTTP 响应也拆开看一眼。你把 stream 参数设为真时，服务器推回的不是一整段 JSON，而是一连串小包裹——每个包裹装着一个 token，术语叫服务器推送事件。你在屏幕上看到的打字机节奏，就是包裹到达的节奏；包裹的间隔就是逐 token 延迟，第一个包裹之前的等待就是首 token 延迟。体感层的两件事——打字机效果与首字慢——在这一刻都有了物理对应物：前者是 decode 循环的节奏，后者是 prefill 的长度。

这张旅程图在全书反复出现。第 2 章量的是在制品区的面积，第 3 章讲厂房怎么重新隔间，第 4 章讲排班表怎么改，第 6 章把 prefill 与 decode 拆成两条生产线，第 10 章走到结算处把 token 一路追到电费。而现在，我们先回到那个总根源：为什么产品必须一个一个生？

## 1.2 自回归的宿命：token 为什么要一个一个生

训练和推理用的是同一个网络，走的却是两种完全不同的节奏。训练时，GPU 一次前向传播就能同时算出整句话每个位置的结果；推理时，token 却必须一个一个往外挤。这不是工程上的偷懒，而是语言模型的数学定义使然。所谓语言模型，本质是一条条件概率链：第 t 个 token 的分布，以输入 x 和此前已生成的全部 token 为条件——

```
P(y_t | x, y_1, y_2, ..., y_{t-1})
```

推理时的串行就从这条链里长出来：第 t 步的输入包含第 t-1 步的输出，而那个输出在你亲手采样出来之前根本不存在。分布没定，输入就缺一位；输入缺一位，网络就没法开工。串行不是选择，是定义。

先看训练为什么可以并行。工程师手里有完整的目标序列，于是把整句话一次性送入网络，让每个位置同时预测“下一个 token”，再与标准答案比对出损失，这套手法叫 teacher forcing。麻烦在于，位置 3 的预测本来不该看到位置 4 以后的内容，直接并行会让答案从后门漏进前门。解决办法就是因果掩码：把注意力分数矩阵的上三角置成负无穷，softmax 之后那些位置的权重精确归零——

```
掩码矩阵（行=当前位，列=被看位，上三角为 -inf）：
[  0  -inf -inf -inf ]
[  0    0  -inf -inf ]
[  0    0    0  -inf ]
[  0    0    0    0  ]
```

（materials/ch01_补充.md）有了这块遮眼布，一次前向传播同时算出 T 个位置的损失，训练的算力得以铺满整张卡，这也是训练 GPU 利用率能稳定在五成以上的原因之一（materials/raw_cost.md）。有个细节值得玩味：推理时不需要掩码，因为未来压根还不存在——掩码是训练时为了并行而模拟因果的装置，推理时因果天然成立。但因果天然成立的代价，是并行性彻底丧失：每一步只有一个新 token 进入网络。

不对称还有一个更隐蔽的维度：算力的形状。训练时一次前向是一大批 token 一起过网络，矩阵乘法又大又规整，GPU 的上万个核心都有活干；decode 时一次只有一个 token，矩阵瘦成一条线，绝大多数核心无事可做。同样是过一遍网络，训练像整船集装箱进港，decode 像独轮车过秤——后者慢，不是因为路更陡，而是因为货太少，养不起整座码头（第 6 章把这笔账算细）。

这一步有多重？经验法则是，每生成一个 token，大约要执行两倍于参数量的浮点运算（工程常识口径，精确数字随架构与实现浮动）。以一个 70B 参数的模型计，就是约 1400 亿次 FLOPs；生成一段五百 token 的回答，等于把这套几百亿参数的网络完整跑过五百遍。更反直觉的是，这五百遍里每一遍都要重读全部历史——历史每长一个 token，下一遍的重读就多一份。串行的长度本身在给每一步加码，这是自回归最隐蔽的一层复利。

有人会问：既然每一步都是一次确定的前向计算，为什么不能把整段回答一次算完？答案是算不出来。第 t 步的输出是一份概率分布，你要从里面采样——采出哪个 token 带着随机性；你在 API 参数里调的温度、top-p，调的正是这份随机性的形状。下一步的输入是上一步的采样结果，是随机变量，而随机变量没法提前进网络。这就是为什么推理引擎可以并行处理一百个请求，却无法并行处理一个请求的一百个 token。工程师当然试过折中：beam search 同时维护几条候选序列并行打分，可候选之间的比较仍要一步一步走，而且很快撞上显存的天花板——候选序列能共享前缀这件事，第 3 章会回来收账（materials/raw_vllm.md）。

再把数字翻译成体感。H100 的 FP16 稠密算力标称近千 TFLOPS，70B 模型每个 token 约需 1400 亿次 FLOPs，粗算之下这台机器理论上每秒能吐出几千个 token；实际部署里，单个请求的生成速度通常只有每秒几十个 token，落差在一个数量级以上（粗算取工程常识口径，严格解释见第 6 章：decode 是搬运问题，不是计算问题）。账面上的算力与落到字上的算力之间，隔着一整座库房——本书要讲的就是这座库房。

训练与推理的不对称，还有一条直达价目表的通道。输入的 prompt 是现成的，可以像训练那样一次性并行处理；输出必须逐个采样，每一个都依赖它前面的所有。所以同样一个 token，“读进来”与“写出去”的成本天然不同——各大平台对输出 token 收三到五倍的价（materials/raw_cost.md），定价差的底层就是这条物理事实，不是营销策略。同一块 GPU，处理 prompt 的 prefill 阶段利用率能到九成，逐 token 的 decode 阶段只有两成半到三成（materials/raw_cost.md），差距是数量级的。落到价目表上就是一件你今天就能验证的事：让模型读一篇三千字的文章，和让模型写一篇三千字的文章，后者的账单是前者的三到五倍——你现在可以在物理层向财务解释这件事了。

到这里可以收拢本章的第一个断言：自回归不是实现细节，而是推理一切物理性质的总开关。串行、复利、读写不对称、算力形状——四样东西都从同一个定义里长出来。后面所有章节的数字，追到底都是这四样的组合。

面对这份宿命，工程界能做的只有两件事：把每一步串行做得更便宜，或者让更多条串行流共享同一块硬件。前者通向第 6 章的带宽账本，后者通向第 4 章的调度革命；再往深处走，所有路线都汇入同一条河——让显存被填得更满。但在讨论任何一条之前，必须先回答一个更基本的问题：为什么每生成一个 token，都要把全部历史重读一遍？答案藏在注意力的计算结构里。

## 1.3 注意力的账本：Q、K、V 与历史重读

自回归规定了节奏，真正让“重读历史”成为物理事实的，是注意力的计算结构。每一层 Transformer 的自注意力，都在重复同一个动作：

```
Attention(Q, K, V) = softmax(Q · K^T / sqrt(d_k)) · V
```

三个角色的分工，可以用一本账本来记。Q（Query，查询）是当前 token 递上来的问题：“我在找什么？”；K（Key，键）是账本里每条记录的索引标签：“我是什么、能被谁匹配”；V（Value，值）是每条记录的内容：“匹配上之后，我贡献什么信息”（materials/ch01_补充2.md）。注意力计算，就是拿新来的 Q 与账本里每一行的 K 比对相似度，再按匹配度把所有行的 V 加权汇总成一份摘要——一份专为当前这个 token 定制的摘要。

两个补充让账本更接近实物。其一，Q、K、V 不是天生就有的，而是各自由一组可学习的投影矩阵从同一份隐藏状态变换而来——W_Q、W_K、W_V 三台机床，把同一批原料加工成三种零件；每过一层，就换一套新的三台。其二，现代模型用的是多头注意力：几十个头并排开账，每个头有自己的一套 Q、K、V，各自记各自的账，最后把几十份摘要拼接融合。所以账本不是一本，是一整面书架；等第 2 章算显存账的时候，这面书架会被精确到字节。

现在把自回归的节奏叠上来。生成第 t 个 token 时，进网络的是第 t-1 步的产出，它递上一份崭新的 Q；而它要比对的，是账本里前 t-1 个 token 的全部 K，要汇总的是全部 V。注意，这不是某一层的特殊行为：几十层 Transformer，每一层都有自己独立的 Q、K、V 投影，每一层都要把全部历史重读一遍。在长度 4096 的上下文里，每生成一个字，就有几十份新查询与几千把旧钥匙逐一比对，再把几千条旧内容加权汇总，一层不落。“每生成一个 token 都要重读全部历史”不是修辞，是字面意义的机械动作清单。

把我账单里的一件日常翻译过来：每次调用 API，客户端都要把整段对话历史重新发送一遍——因为服务方的账本默认按请求结算，历史 token 的 K、V 不留到下一次调用。你为重读历史付的钱，就藏在每一笔输入计费里；哪些情况下可以不重读，第 8 章的 Prompt Cache 会给出答案。同一个机制还解释了一个体感现象：对话越长，回复越慢——历史每多一段，prefill 要算的 K、V 就多一批，decode 每步要重读的账本也更厚。长对话的迟滞感，是历史长度在物理时间上的投影。

故事到这里似乎只剩一条路：既然每一步都要用全部历史的 K 和 V，那就每一步都把它们重新算一遍。毕竟这一步的历史比上一步多了一个 token，账本变厚了，重抄一遍似乎天经地义。因果掩码在这里递来了第二份礼物，也是本章最重要的转折：由于掩码保证每个位置只看得到自己和过去，每个 token 的 K 和 V 只由它自己以及它之前的 token 决定，与未来无关（materials/ch01_补充2.md）。未来生成出什么内容，都不会反过来修改历史 token 的键与值。换句话说，账本里已经写下的每一行，一旦写就永远是那一行——历史 K 和 V 是不可变数据。

不可变，就意味着可以缓存。这就是 KV Cache 成立的全部物理学基础。而 Q 的待遇完全不同，它没有缓存价值的原因有三条（materials/ch01_补充2.md）：其一，每一步的 Q 都来自刚生成的新 token，是这一轮才出生、此前根本不存在的向量，无从缓存；其二，旧 token 的注意力输出只用来推进它自己那一步的预测，算完即弃，未来任何一步都不会回头再用；其三，若强行缓存旧 Q，它只会占着显存，一分收益都没有。一句话：K 和 V 是历史，会被未来的 Q 反复查询，所以要存；Q 是现在，只用一次就作废，所以不存。这三条理由值得慢读，因为它们反过来定义了缓存的形状：我们缓存的不是 token 本身，而是 token 被未来查询时需要的那部分投影。判断一个东西该不该缓存，标准从来不是它重不重要，而是它会不会被再次原样使用——工程里的缓存（cache）一词，翻译过来其实是复用。

省下的到底是多少？不做缓存时，每一步都要把全部历史的 K、V 重算一遍，序列长度为 T 时总成本按三次方增长；缓存后每步只算新增的那一份，总成本降到平方级（materials/ch01_补充2.md）：

```
无 KV Cache：每步重算全部历史 K/V，总成本 O(T^3)
有 KV Cache：每步只算新增 K/V，    总成本 O(T^2)
```

但必须认清省的是什么：每一步线性重读全部缓存的 K、V，这部分省不掉，因为新的 Q 必须查询所有历史；被消除的只是重复的“重写”。重读是注意力的本性，重写才是纯粹的浪费。分清这两个词，就抓住了推理优化的第一条原则——不可变的存起来，会变的算下去。

平方级依然是不可忽视的数字：上下文翻一倍，每步要重读的账本也翻一倍，重读的总功跟着翻番。工程界为此造过一整套“把重读做得更省”的内核——FlashAttention 一族把注意力算得更巧，vLLM 让被重读的对象可以散落在显存各处；它们都没有改写注意力的账本结构，只是把每一页翻得更快、放得更妙（materials/raw_vllm.md；内核细节在第 3 章展开）。下一节，我们看工程师怎么在显存里兑现“存起来”这条原则。

## 1.4 KV Cache 的诞生：用显存买时间

用空间换时间，是计算机科学里最古老的交易。KV Cache 是它在推理车间的现代版本：把算过的历史 K、V 存进显存，换掉每一步的重复计算。机制拆开是三步（materials/ch01_补充2.md）：prefill 阶段，整段 prompt 并行算出全部 K、V，一次性写入缓存；decode 的每一步，只为新 token 算一份新的 K、V，追加进缓存；然后用新 Q 查询全部缓存，完成本步注意力。只追加，不重算——增量推理的全部秘密就是这六个字。

值得停半秒，体会这笔交易在各层的分量。对用户，它意味着流式输出在物理上是逐 token 的真实计算，不是假打字机；对引擎，它意味着显存管理从配角变成主角——账本比产品本身更占地方；对商业，它意味着定价必须区分读与写，因为两者的物理成本根本不同。一条缓存机制，同时改写了体验、架构与价目表，是教科书级的杠杆。

但天下没有免费的缓存。这笔交易省下的是算力账，付出的是显存账，而后者的规模需要一条公式来算清（materials/raw_vllm.md）：

```
KV bytes = 2 × num_layers × num_kv_heads × head_dim × seq_len × batch × dtype_bytes
```

逐项认一遍：因子 2 是 K 与 V 两份张量；num_layers 是层数，每层都有自己的账本页；num_kv_heads 是 KV 头数——它不一定等于全部注意力头数，采用 GQA 分组查询的模型会刻意压小这一项，比如 Llama-3-70B 只有 8 个 KV 头（materials/raw_vllm.md）；head_dim 是每头维度，常见取 128；seq_len 是序列长度；batch 是并发请求数；dtype_bytes 是每元素字节数，FP16 与 BF16 为 2，FP8 与 INT8 为 1（materials/raw_vllm.md）。请对这条公式保持敬畏——第 2 章整章都在为它做注脚，显存几何学的全部内容都从这一串因子里生长出来。

先代几个数字感受量级。Llama-3-70B 有 80 层、8 个 KV 头、每头 128 维，FP16 精度下单个 token 的 KV 占用是：

```
2 × 80 × 8 × 128 × 2 = 327680 字节 ≈ 320 KB/token
```

一个 4096 token 上下文的请求，缓存就是 327680 × 4096 ≈ 1.25 GB——还没开始生成正文，账本先吃掉一块小硬盘的零头。层数与维度取自该模型的公开结构参数，公式与 KV 头数出自素材，乘法是我替你算的。再往后想一步：并发 32 个这样的请求，KV 显存就奔着 40 GB 去，逼近半张 A100；上下文若拉到 128K，单个请求的缓存就要 40 GB 上下，抵得上两台 8B 模型的 FP16 权重还有余。这就是为什么概念词典里说，KV Cache 是推理显存的主要住客、流水线边的在制品库存——它随并发与上下文无限增长，而厂房面积是死的。如果你替更大的模型心算一遍，会发现规律不变、基数惊人：405B 级的模型层数更多，单 token 的 KV 水涨船高，128K 上下文的一个请求就能吃掉大半张 80 GB 显卡——第 2 章会把这笔账逐项算给你看。现在只需建立一个直觉：KV Cache 的增长曲线，天然比厂房的扩建速度快。

更糟的是传统实现的姿势：为每个请求按最大序列长度预分配一整块连续显存。请求实际只生成了五百个 token，也按 4096 预留，这叫内部碎片；长度不一的请求把显存切成参差的空隙，新来的大请求谁的空间都放不进去，这叫外部碎片；运营者不知道你会写多长，只能按最坏情况超发床位，这叫过度预留。三宗罪叠满，厂房有六到八成的面积从未真正生产过一个字节（materials/raw_vllm.md）。直到 vLLM 的 PagedAttention 借用操作系统分页的智慧，把浪费压到约 4%（materials/raw_vllm.md）——那是第 3 章的故事，这里先立此存照：账本本身差点掏空厂房。

显存的账本要分两页记。第一页是权重，固定成本：70B 模型 FP16 要占 140 GB，压到 FP8 砍半、压到 INT4 再砍到四分之一，而且不管有没有流量，它都夜夜睡在厂房里（materials/raw_cost.md；量化的细账在第 5 章）。第二页是 KV Cache，边际成本：随并发数与上下文长度相乘增长，流量越大、对话越长，这一页越厚。固定的一页决定你能不能开机，边际的一页决定你能接多少单——推理服务的全部容量规划，归根结底是这两页账的加法。

【数字卡片】本章的关键数字：

- 传统按最大长度预分配的 KV 显存浪费：60%-80%（materials/raw_vllm.md）
- PagedAttention 分页管理之后的浪费：约 4%（materials/raw_vllm.md）
- 静态批处理下的 GPU 利用率：约 20%-40%（materials/raw_vllm.md）
- Llama-3-70B 单 token 的 KV 占用（FP16、8 个 KV 头）：约 320 KB（公式出自 materials/raw_vllm.md，乘法为本章计算）
- 4096 上下文单请求的 KV 总量：约 1.25 GB（本章计算）
- decode 阶段 GPU 利用率 25%-30%，prefill 阶段可达约 90%（materials/raw_cost.md）
- 输出 token 相对输入 token 的定价倍数：3-5 倍（materials/raw_cost.md）

这笔交易还有后半程的利息。缓存一旦放进显存，decode 的每一步都要把全部 K、V 从显存读进计算单元——账本越厚，每步搬运的字节越多。这就是为什么 decode 的瓶颈是显存带宽而不是算力、GPU 利用率只有两成半到三成（materials/raw_cost.md）：算一下只要一眨眼，把整本账搬上台面却要大半天。第 6 章我们会看到，这道带宽天花板如何催生了 prefill 与 decode 分家的 PD 分离，甚至催生了用 RDMA 远程搬运缓存的重型工程。KV Cache 用显存买回了时间，又把新的瓶颈写在带宽上——工程就是这样一环扣一环的债。

也有人会想：显存不够，加卡不就行了？账没这么好算。显存带宽的增长常年追不上容量与算力的增长，多卡之间还要靠通信带宽粘合，每跨一次卡，延迟与功耗都加一层；何况 KV Cache 是随请求伸缩的弹性负载，而卡是整块采购的刚性供给——弹性需求配刚性供给，中间必然长出调度与复用的学问。这正是本书把大部分篇幅给了算法与调度、而不是买更多卡的原因。

回望这一节，KV Cache 的诞生把推理的主战场从算术搬进了库房。算力的问题尚有硬件红利帮忙，库房的问题没有——显存容量的增长慢得多，缓存的增长却快得多。当显存利用率成为生死线，工程师们最终意识到，他们需要的不是一个更聪明的乘法内核，而是一整套显存上的操作系统。这个意识的完整展开，就是本书剩下的全部内容。

## 1.5 推理工厂的平面图

参观完第一条流水线，我们登上工厂的天桥看全景。脚下这座厂房可以划成四个区。权重区摆着常驻机床——模型权重，一次装配、长期在岗，FP16 的 70B 模型要占 140 GB，无论有没有流量都赖在显存里（materials/raw_cost.md）；在制品区是流水线边的库房——KV Cache，随每一单增长，是显存里最活跃的住客；排产区是调度中枢——队列、排班表与派单逻辑，决定厂房的每一平方米在同一时刻为谁工作；结算处把物理动作翻译成账单——token 单价、利用率、电费。本书十章，就是这四个区的勘察报告，而第 1 章是这张平面图本身。图上还有一条隐藏的动线：token 从厂门进（路由与排队），到在制品区上架（建 KV Cache），到排产区组批出活（循环解码），经流式水线出货到你屏幕上，最后在结算处计价。后面每一章，都是在给这条动线上的某一段提速、扩容或者省钱；而所有段落共享同一个物理约束——厂房就那么大。

逛厂的方法也一并交代：每一章我都会先摆出一个反直觉的现象，再把它拆到公式与数字，然后看引擎与论文怎么接招，最后回到我自己的网关实践里对照一遍。你可以把每一章当成一次入厂流程：现象是门票，公式是工牌，实践是回执。

权重区有两章。第 5 章讲量化：机床太大，就按比例做小——用更低的比特表示权重，从 FP16 到 INT4 显存省四倍而质量保住 98% 上下，这笔精度换面积的生意能做成，靠的是大模型自身的冗余度；但合同有条款，模型越小越要慎签。第 9 章讲引擎战场：vLLM、TensorRT-LLM、TGI、SGLang，相当于四家整厂方案商，各自交付一整条流水线；比的不是谁的机床更快，而是谁家的流水线排得更满、谁的安装调试成本更高——70B 级模型上，vLLM 的吞吐约每秒 1000 到 2000 个 token，TensorRT-LLM 能再快一到三成，代价是仅支持 NVIDIA 与以天到周计的编译运维；还有一家老牌方案商 TGI，在 2025 年 12 月交出了退场通知书（materials/raw_cost.md）。

在制品区有三章，是全书的主舱。第 2 章量面积：一条 KV 公式统治显存，8B、70B、405B 各有一本显存账，长上下文与并发一相乘，账本立刻见底——你会第一次看清“为什么 70B 是分水岭”。第 3 章讲货架革命：PagedAttention 把 1970 年代操作系统分页的智慧搬进显存，浪费从六到八成压到 4%，一张 A100 因此能同时多服务三到五倍的请求（materials/raw_vllm.md）——推理经济学由此奠基。第 8 章讲复用：既然前缀的 K、V 不可变，不同请求共享同一段前缀时，账本可以只记一份；Prompt Cache 把缓存读的价格压到约十分之一，物理本质就是前缀复用的经济学。

排产区有三章。第 4 章讲连续批处理：把排班表从火车时刻表改成网约车派单，调度粒度降到每个前向迭代，请求完成即出场、空位即插队；调度员手里永远攥着三个队列——waiting、running、swapped，显存吃紧时旧请求会被抢占，要么重算、要么换出（materials/raw_vllm.md）。混合负载下，吞吐比静态批提升 2-4 倍（materials/raw_vllm.md）。第 6 章讲两条生产线：prefill 与 decode 性情相反，一个吃算力、一个吃带宽，混在一起互相拖累——混部时 decode 的逐 token 延迟会出现 150 毫秒量级的尖峰（materials/raw_cost.md）；拆开各自调优就是 PD 分离，综述口径下最高 6 倍吞吐、15%-40% 的成本节省，激进的方案报告过 23 倍（materials/raw_cost.md）。第 7 章讲厂门与派单：模型路由、熔断、降级、预算门——这一章的主角是我的网关 poeapi_go，它在个人尺度上重演了集群调度器的全部逻辑，包括一次让我一夜白头的接口方言事故。

结算处是第 10 章。我们把 token 的价格一路追到底：硬件折旧、电费、带宽。GPT-4 同等性能每百万 token 的价格从 2022 年底的约 20 美元跌到 2025 年底的约 0.40 美元（materials/raw_cost.md），而推理 GPU 的利用率仍在 5%-10% 徘徊、训练却能过五成（materials/raw_cost.md）——这道鸿沟就是全书所有优化的总战场。价格塌方的每一级台阶，都踩在前九章的某项优化上：量化四倍、连续批处理两倍、推测解码两倍，复合成 16 倍的成本坍缩（materials/raw_cost.md）。这个战场也不止有降价：2025 年 DeepSeek R1 以每百万输入 0.55 美元、输出 2.19 美元的定价搅动全球价目表（materials/raw_cost.md），而低价的底气，同样是利用率与调度挣出来的。大模型的摩尔定律不在制程工艺里，在调度算法里。

最后交代我的位置。在这座工厂里我有两重身份：付账单的 API 用户，和传达室的值班员。poeapi_go 那五十四个文件、六千六百多行 Go 代码，分成 handlers 与 internal 下的 auth、logger、metrics、sanitizer、trace 几层，翻译成工厂语言就是登记、脱敏、派单、记账：23 条脱敏规则把来客身上的敏感词搜干净，5 条高危内容直接以 400 拒之门外，多渠道路由与熔断负责派单，TokenPolicy 四级预算门负责记账。个人尺度的微缩模型，但物理规律一视同仁：我的预算门和数据中心里的利用率指标，受同一条 KV 公式管辖。读过《终端觉醒》的朋友会认出 Prompt Cache 的用户侧体验，读过《一个人的军团》的朋友会认出 TokenPolicy——这两条旧线会在第 8 章与第 7 章，和这座工厂正式接驳。接下来的九章，我们按平面图逐区开工。先去在制品区，带上计算器——第 2 章，我们把那条公式逐项拆开，算一算显存的几何学。

---

## 第2章 KV Cache 的物理约束:显存几何学

# 第 2 章 KV Cache 的物理约束：显存几何学

在真正走进 GPU 机房之前，我以为大模型推理的瓶颈是某种说不清的东西——算力、集群、玄学调度。直到我把一条公式抄进笔记本，亲手算出“一个 token 到底占多少显存”，这座工厂才第一次向我交出平面图。这一章我们只做一件事：把这条公式逐项掰开，算清 8B、70B、405B 三个量级的显存账本，看模型架构师如何用 GQA 给 KV 减重，看长上下文如何把账单沿着一条直线推高，最后回到我自己的网关，讲讲一个个人开发者为什么要像 Infra 工程师一样，一个一个地数 token。

## 2.1 一条公式统治显存

我是先当 API 用户，后当网关作者的。poeapi_go 的日志里，token 数每天滚过我的屏幕，可很长一段时间，我答不上来一个最简单的问题：一个 token，在 GPU 的显存里到底值多少字节？后来我把 vLLM 文档里那条 KV Cache 公式抄在笔记本第一页，盯着它看了很久——推理服务的一切容量规划、一切“这张卡能接多少用户”、你账单上每一个数字的物理来源，都从这行乘法开始：

```
KV bytes = 2 × num_layers × num_kv_heads × head_dim × seq_len × batch_size × dtype_bytes
简写:    KV bytes = 2 × L × H_kv × D_head × S × B × dtype_bytes
```

逐项拆开。第一个因子 2，是 K 与 V：注意力层里，查询向量 Q 要和历史每个 token 的 K 做匹配，再按匹配权重去取 V 里的信息。这里有个初学者常问的问题：为什么只缓存 K 和 V，不缓存 Q？因为 Q 属于“当下”——它由当前这一步的输入算出，用完即弃；而 K 与 V 属于“历史”——过去每一个 token 的 K、V，在未来任何一步都可能被读到。缓存 Q 毫无意义，丢失 K、V 则等于失忆。所以推理引擎给每个历史 token 发两张永久工牌，一张 K，一张 V，公式里的因子 2 就是这么来的。

第二个因子 L 是层数。Llama-3-70B 有 80 层，每一层都有自己的注意力、自己的一套 K、V，层与层之间互不通用——第 12 层的键值帮不上第 13 层的忙，所以 KV 的账要按层翻 80 倍，模型越深，KV 越沉。第三个因子 H_kv 是 KV 头数，注意它不等于 Q 头数——这是 2.3 节的主角，GQA 模型里 KV 头远少于 Q 头，70B 只有 8 个。第四个因子 D_head 是每头的维度，当代旗舰几乎清一色取 128，它和头数相乘，大致就是这一层留给 K 或 V 的宽度。

然后是三个运行时因子。S 是序列长度，prompt 加上已经生成的部分——它是唯一会自己长大的因子：对话每进行一轮，S 就悄悄加一截，KV 池的水位就无声地涨一截。B 是并发数，同一时刻有多少请求把各自的 KV 摊在显存里；它是公式里最贵的一个乘数，因为它把前面所有因子整体翻倍。dtype_bytes 是每个数值占的字节，FP16 与 BF16 是 2，FP8 与 INT8 是 1——整个公式里唯一能事后打折的一项。

> **数字卡片 1|Llama-3-70B：一个 token 的 KV 体重（FP16）**
>
> ```
> 每 token KV  = 2 × 80 层 × 8 KV 头 × 128 head_dim × 2 字节
>              = 327,680 字节 = 320 KB
> 8K 上下文    = 320 KB × 8192 token ≈ 2.5 GB
> 100 路并发   = 2.5 GB × 100 = 250 GB
> ```

320 KB 是什么概念？手机随手拍一张 JPEG，大约 300 到 500 KB。也就是说，Llama-3-70B 每生成一个 token，都要先在显存里为它圈下一块相当于一张照片的地皮。你跟模型聊满 8000 个 token，就是八千多张照片铺开的厂房——2.5 GB。这不是修辞，是字节数；把这块地皮乘以你此刻的并发，就是你必须整租下来的厂房总面积。

250 GB 还可以再翻译一次：它意味着一个 70B 的服务商，哪怕模型权重免费送，光是为 100 个用户保留对话记忆，就要常备三张多的 H100；这些卡的租金按小时跳动，而用户要的不过是让对话记得上一句。所有关于推理的生意，都是在为记忆付房租——这个理解，比任何图表都接近本质。

还有一个维度常被忽略：时间。KV 的地皮是按对话生命周期租的——一个请求从建立到结束，它的 KV 一寸都不会释放；对话持续半小时，这块地就租半小时，租期内不管 GPU 有多忙，都不能转租给别的请求。所以调度器才那么在意请求何时结束：每一次提前的完成，都是一次提前的退租。

用这座工厂的隐喻说：权重是常驻的机床，原料与成品来来去去，激活值是流水线上的半成品，唯独 KV Cache 是不退货的在制品库存——每一个字都要一个工位，而且直到对话结束才肯走。更麻烦的是，在制品还在增殖：对话越长，工位越多；并发越高，同时占工位的订单越多。机床的占地是常数，在制品的占地是变量——推理工厂的容量规划，难就难在这里。

这条公式的七个因子，身份完全不同。L、H_kv、D_head 是出厂参数，烙在 config.json 里，我们一行也改不了，它们决定每个 token 的 KV 单价；S 与 B 是排产参数，由负载决定，却是服务方唯一真正在运营的东西，它们决定总价；dtype_bytes 是唯一能事后打折的项——给 KV 降精度，正是第 5 章量化的入口。而乘法结构本身宣判了一条铁律：并发与长上下文是相乘的关系，不是相加，你没法把两个旋钮同时拧到最大，显存会先一步见底。

这条公式还有一层面纱：它不只是容量公式，也是带宽公式。解码阶段每吐一个 token，都要把当前序列的全部 KV 完整读一遍——读到 4K 上下文时，每吐一个字就要搬运约 1.25 GB 的 KV；一个会话从零聊到 8K，累计流过显存总线的 KV 流量要以 TB 计。容量决定显存里能同时停多少请求，带宽决定每个请求每秒能吐多少字，两笔账写在同一条乘法里。这也是为什么 GPU 厂商每一代都在卷显存带宽：HBM 的每一次换代，首先都是给 KV 这条传送带换轨。

顺带把第 1 章的一处伏笔接上：为什么解码阶段 GPU 利用率上不去？因为每一步的算术强度太低——为了算出一个 token，要把全部权重读一遍、把全部 KV 读一遍，访存的时间远长于计算的时间。KV 越长，搬运越重，计算在搬运的影子里越显单薄。第 6 章我们会专门解剖这件事，这里只需记住：KV 公式不仅管显存的仓容，还管显存的门口。

我后来把这条公式称作全书的宪法，因为后面每一章都在改它的某一个因子：第 3 章的 PagedAttention 不动乘积本身，动的是乘积之外被白白扔掉的部分——同一张卡，分页让这个乘积真正住满；第 4 章的连续批处理调度的是 B，让并发因子时刻贴着显存水位运行；第 5 章的量化打的是 dtype_bytes，从 2 打到 1 再打到 0.5；第 8 章的前缀缓存最直接，它让重复的前缀不重复计算，等于把 S 里人人相同的部分合并同类项。一条公式，十章故事——推理工厂里没有一章绕得开它。

我们习惯说“算力决定一切”。但对自回归解码来说，每生成一个 token，都要把此前全部 KV 从显存里完整读一遍——显存既是仓库，又是流水线的传送带，仓库塞满的那一天，传送带再快也无货可运。一个 token 的 KV 占用是一个可以精确计算的数字，这个数字乘上层、乘头、乘序列、乘并发，就决定了你能跑多大的模型、服务多少并发。推理的一切经济学，从这条公式开始。

## 2.2 8B、70B、405B 的显存账本

拿到一张 80 GB 的卡，老工程师做的第一件事不是跑分，是记账。推理显存这本账有四个科目：权重、KV Cache、激活值、运行时开销。权重是机床，一次性落地、常年不动；KV 是在制品，随并发与上下文起伏；激活值是流水线上的瞬时占用，prompt 越长、批越大，峰值越高；运行时开销是厂房杂项——CUDA 上下文、框架缓冲、通信缓冲，零零碎碎但赖着不走。工程上的经验法则是：做预算时给整卡留出 1.2 到 1.5 倍的余量，而 vLLM 这类引擎默认也只敢收 90% 的显存当配额——账算错一格，第一波真实流量就会把卡打爆。

为什么只敢收九成？因为显存一旦打满，轻则分配失败，重则整卡进程崩溃，引擎必须给碎片与峰值留出缓冲带；九成是工程上反复校准出来的安全线，不是保守的借口。这个细节值得每个做容量规划的人记一辈子：任何资源池，都要先给系统自己留一口呼吸的气，剩下的才能拿去做生意。

第三、第四个科目也要记账，只是量级小得多。激活值的峰值随批大小与预填充分块的长度走，通常在个位数 GB 一档；运行时开销的大头是 CUDA 上下文与框架缓冲，一两 GB 起步。这两项加起来，就是预算里那 1.2 到 1.5 倍余量的实体——账本上最容易被忘掉、又总在流量峰值时爆雷的两行。

这四个科目里，只有 KV 随时间只增不减：权重开机即定，激活算完即散，唯独 KV 从对话的第一个 token 起持续累积。所以做容量规划，权重给常数，激活给峰值，KV 给曲线——三者时间尺度完全不同，混在一个数字里做预算，必然出错。

先看账本第一页，权重。规则极简：显存约等于参数量乘以每参数字节。

> **数字卡片 2|显存账本第一页：权重占用（单位 GB）**
>
> | 精度 | 7B | 13B | 70B |
> |---|---|---|---|
> | FP32 | 28 | 52 | 280 |
> | FP16/BF16 | 14 | 26 | 140 |
> | INT8 | 7 | 13 | 70 |
> | INT4 | 3.5 | 6.5 | 35 |

405B 在这张表之外：FP16 权重约 810 GB，80 GB 的卡十张起步，任何“单机浪漫”都与它无关。

但权重只是第一页。第二页是 KV，由 2.1 节那条公式决定：8B 是 32 层、8 个 KV 头、128 维，每 token 128 KB，8K 上下文的单请求不多不少正好 1 GB；70B 每 token 320 KB，单请求 2.5 GB；405B 有 126 层，每 token 504 KB，单请求约 3.9 GB。把两页账并在一起读，才能明白为什么业界总说 70B 是分水岭——给 70B 摆三种装法，同一张 80 GB 的 H100，命运完全不同。

装法一，FP16 原生：140 GB 权重，一张卡根本装不下，只能两张卡做张量并行，每卡摊 70 GB。按九成配额算，每卡留给 KV 池的只剩约 2 GB——满血的智商，干涸的池子，几乎接不住几个满上下文的请求。

装法二，FP8 或 INT8：70 GB 权重恰好塞进一张卡，听上去很美，可九成配额之后余量同样约 2 GB，连一路满 8K 的 KV 都驻不下，只能 batch 为 1、中短上下文地跑。素材里那组单卡 70B 量化实测 52 tok/s 的数字，说的就是这种“能跑、跑不富”的处境：权重与 KV 在同一张卡上贴身肉搏，谁多占一格，另一方就少活一路。

装法三，INT4：权重降到 35 GB，九成配额后余下约 37 GB，按每路满 8K 花 2.5 GB 计，能驻 14 路——这才是“服务”两个字的起点，也是量化真正的动机：不是让模型变快，是给在制品腾厂房。

> **数字卡片 2b|同一张 80 GB 卡上的 70B：三种装法，三种命运**
>
> | 装法 | 权重占地 | 约余给 KV | 满 8K 并发 | 判词 |
> |---|---|---|---|---|
> | FP16 双卡 | 每卡 70 GB | 约 2 GB | 接近 0 路 | 智商满格，池子干涸 |
> | FP8 或 INT8 单卡 | 70 GB | 约 2 GB | 0 路 | 能跑，跑不富 |
> | INT4 单卡 | 35 GB | 约 37 GB | 约 14 路 | 服务由此起步 |

这本账有个阅读技巧：第一页决定单价，第二页决定总价。经 GQA 拉平之后，小模型与大模型的每 token KV 差距，远小于它们参数量的差距——层数成了主要差项，四十几层与八十层不过一倍之差；可一旦算总账，大模型接待的并发与上下文也大得多，总价差距远比单价差距吓人。单价思维是选模型时的直觉，总量思维才是做服务的入门课。

生产环境里 70B 的真实答案是多卡：把 FP8 权重切到八张 H100 上做张量并行，每卡只摊约 9 GB 权重，九成配额后每卡余出 60 GB 以上，整机能驻一两百路满 8K 的并发——这才是服务商把 70B 卖得起的物理原因。要注意，张量并行切的是权重，KV 池并不合并：每张卡只持有所负责那一部分头的 KV 分片，并发天花板是八张卡各自余量的加总。回头再看前面三种装法的窘迫，本质是同一件事：不肯为在制品多租厂房，在制品就用离场替你做决定。

对照组是 8B：FP16 权重才 16 GB，同一张卡轻松驻下 50 路以上满 8K 的并发；换成 INT4 权重 4 GB，余量大到可以让工程师奢侈地去谈延迟，而不是天天数容量。再看 405B，它是另一个物种：权重 810 GB 起，KV 每 token 504 KB，100 路并发、8K 上下文就是约 390 GB 的 KV，差不多等于它 FP16 权重的一半；就算把权重压到 FP8 的约 405 GB，八张 80 GB 的卡也只刚够安放权重，KV 依旧要精打细算——任何单卡账本对它都失效，只剩多机切分一条路。

8B 这一档还有个身份：个人开发者的甜点。一张 24 GB 的消费级显卡，装 INT4 权重只花 4 GB，余下近 20 GB 全给 KV，足够十来路 8K 并发——一个人的一台机器，就能复现本章讲的全部账本逻辑。这也是为什么本地部署文化围绕 7B、8B、13B 生长：不是这些模型最聪明，而是只有在这个量级，个人才当得起自己的 Infra 工程师。

所以“分水岭”不是玄学，是加法。模型大小决定的从来不是“能不能放下”，而是“放下之后还剩多少厂房给在制品”：7B 是宽敞的单间，70B 是塞满的仓库，405B 是一片必须整体规划的工业园区。跑得起来与服务得了之间，隔着完整的第二页账——KV 显存；而第 5 章我们会看到，量化之所以被称作交易，正因为它是唯一能在模型出厂之后重写这两页账的手段。

## 2.3 GQA 与 MQA：给 KV 减重的模型设计

公式里有个因子最值得单独拎出来讲：H_kv。因为在模型设计的层面，它是唯一被大刀阔斧砍过的旋钮——而且砍它的不是 Infra 工程师，是训练模型的人。这是本章最有趣的一段合谋：推理侧的显存约束，一路倒灌回了模型的基因。

标准的多头注意力（MHA）里，有多少个 Q 头，就有多少套 K、V。Llama-3-70B 有 64 个 Q 头，若 64 套 K、V 全都保留，代入公式是 2 × 80 × 64 × 128 × 2 = 每 token 2560 KB，是后来的整整 8 倍；8K 上下文的单请求从 2.5 GB 变成 20 GB，100 路并发就是 2 TB——同样的智商，KV 先胖 8 倍。第一批把模型做大的人很快撞上这堵墙，于是有了两个减重方案。

多查询注意力（MQA）最激进：所有 Q 头共享唯一一套 K、V，KV 头数压到 1，每 token 只剩 40 KB，减重 64 倍。代价是所有头挤在同一份 K、V 空间里做检索，注意力本该有的多样性被抽走，质量掉得肉眼可见。分组查询注意力（GQA）是折中：把 Q 头分组，每组共享一套 K、V，像宿舍里的公用书架——一层楼的人共用一套，而不是每人盖一间藏书楼。Llama-3-70B 的答案是 64 个 Q 头分 8 组、每组 8 个 Q 头共用一套 KV——这就是它 config 里 num_key_value_heads 等于 8 的全部含义。

> **数字卡片 3|同一个 70B，三种注意力，三种命运（80 层，FP16）**
>
> | 方案 | KV 头数 | 每 token KV | 8K 单请求 | 100 路并发 |
> |---|---|---|---|---|
> | MHA | 64 | 2560 KB | 20 GB | 约 2000 GB |
> | GQA（Llama-3 实配） | 8 | 320 KB | 2.5 GB | 250 GB |
> | MQA | 1 | 40 KB | 约 0.31 GB | 约 31 GB |

读到这里可以算一笔合账：若 64 个 KV 头全保留，100 路并发、8K 上下文要约 2 TB 的 KV；GQA 之后，250 GB。这 8 倍的差额，乘以每 GB 显存的租金，就是 GQA 每天替整个行业省下的钱——一笔从不出现在发布会上的隐形收入。

换个主角再看一眼这张卡片：8B 每 token 128 KB，128K 上下文的单请求 16 GB，100 路并发 1.6 TB——小模型并不是长上下文的避风港，只是把同一条曲线整体下移了一格。曲线面前，模型只有大小之分，没有豁免之名。

顺手做一次交叉验证，把 320 KB 拆回单层：每层每 token 的 KV = 8 头 × 128 维 × 2 个张量 × 2 字节 = 4 KB，乘以 80 层，恰好回到 320 KB。Llama 3 的 8B 小模型也是同一副骨架——32 层、8 个 KV 头，只是每层宽度减半，每 token 128 KB。工程数字的好处就在这里：怎么拆都能对上，大模型小模型共用同一条公式。

为什么旗舰几乎全选中间那行？因为 GQA 的实测表现接近无损——64 个 Q 头分 8 组，每组的 8 个 Q 头依然可以从共享的 K、V 里问出各自的问题，多样性的损失被证明很小。于是在省 8 倍与不掉点之间，GQA 成了甜点；MQA 则退到对成本极度敏感的角落，比如端侧小模型与极限吞吐场景。近几年开源旗舰几乎签了同一份合同：Llama 3 全系 8 个 KV 头，同代主流开源模型也普遍把 KV 头数压在个位数到十几之间。

顺带一提，组数取 8 也有工程讲究：8 是 64 的因子，分组天然整齐；8 个 KV 头乘 128 维，每层 K 或 V 的宽度正好 1024 个数、2 KB，对显存分块与访问粒度都友好。头数一旦取了 5 或 13 这类不好切的数，分块与对齐都会变扭——公式的每个因子背后，都站着一群 kernel 工程师的对齐强迫症。

两个方案都诞生于产业界对并发的渴望：先有 MQA 证明 KV 原来可以共享，再有 GQA 找到质量与容量的平衡点。研究者给出结论，Infra 工程师拿到弹药——从此服务容量的军备竞赛，一半在数据中心里打，另一半在模型的注意力设计里打。

还有一个常被忽略的要点：这种减重无法在推理时补做。K 与 V 一旦生成，就成了历史的一部分，注意力机制必须能读到它们——想让两套 K、V 合并成一套，只能在训练之前就把参数共享设计好。换句话说，显存的减重要趁早：Infra 能省的是管理上的浪费，架构省的是物理上的生成。config.json 里 num_key_value_heads 这一行，是模型架构师替 Infra 工程师签下的第一笔显存合同；Infra 自己挣到的那次减重——把 60% 到 80% 的浪费压到约 4%——要到下一章才登场。两者性质截然不同：GQA 是少生，分页是少扔；前者改基因，后者改管理。

自那以后，我读一个模型卡的顺序变了：第一眼不再看参数量，而是找 KV 头数。参数量决定一个模型有多聪明，KV 头数决定它有多好养；在推理工厂的账本上，后者往往更是生死线。

## 2.4 长上下文的代价曲线

公式里最诚实的因子是 S：KV 严格随序列长度线性增长，聊多长、占多少，一寸不多，一寸也不少。它没有缓存命中的惊喜，没有压缩算法的奇迹，一条直线从 4K 一直铺到 128K。模型发布会喜欢用窗口这个词——窗口听起来是采光，是体验；在机房的账本上，窗口的单位是 GB。

> **数字卡片 4|70B 单请求的 KV 阶梯（FP16，GQA 8 头）**
>
> | 上下文 | 单请求 KV | 直观对照 |
> |---|---|---|
> | 4K | 1.25 GB | 一篇长报告 |
> | 8K | 2.5 GB | 一次深度技术对话 |
> | 32K | 10 GB | 一本小书 |
> | 128K | 40 GB | 半张 H100 |

128K 这一行值得停下来看一眼：320 KB 乘以 131,072，正好 40 GB——一个漂亮得不像巧合的整数。线性从不拐弯：上下文翻 16 倍，KV 就翻 16 倍。40 GB 是什么概念？只为一个用户的聊天记录站岗，就吃掉半张旗舰卡；而这次对话里真正新鲜的内容，可能只有最后那几百个 token——历史越厚，每一步都要背着全部历史走路。

这条曲线在多轮对话里最折磨人。一个日常的技术助手会话，聊到二三十轮，历史轻松越过 2 万 token，KV 就要 6 GB 上下；所谓支持 128K，真正的含义不是让你一次塞进 128K，而是提醒你：驻留一个重度用户的整个会话，要按 40 GB 做预算。工程师的对策在后面几章——KV 量化、分块预填充、预填充与解码分离；而许多产品的对策很委婉：超长会话悄悄做摘要压缩，本质上是在替用户手动回收 S。

长上下文的另一个驱动力是 agent：工具调用把系统的输出喂回上下文，几十轮下来，窗口里堆满中间结果；检索增强则一次性注入整段资料的摘录。这两类负载的共同点是，S 的增长不再由人聊天的速度决定，而由机器自动堆叠——代价曲线因此从爬坡变成俯冲。给 agent 做上下文管理，会是未来几年 Infra 与应用共同的功课。

再乘上 B，曲线立刻露出獠牙。

> **数字卡片 5|70B 的并发总账（FP16）：上下文越长，厂房越贵**
>
> | 上下文 | 100 路并发的 KV 总量 | 约合 80 GB 卡（只放 KV） |
> |---|---|---|
> | 4K | 125 GB | 2 张 |
> | 8K | 250 GB | 4 张 |
> | 32K | 1000 GB | 13 张 |
> | 128K | 约 3.9 TB | 约 50 张 |

反过来看同一张卡更直观：假设 KV 池 64 GB，8K 上下文能驻 25 路，128K 上下文只能驻 1 路半；上下文乘 16，并发就得除 16。这就是长上下文的代价曲线：上下文窗口是模型的卖点，同时也是 Infra 的成本中心，两个身份共用同一个数字。厂商宣传页上支持 128K 上下文的下一行小字，永远是显存预算另议。

工程界对这条曲线的第一次反击，是给 dtype_bytes 动刀：KV 用 FP8 存储，每 token 从 320 KB 减到 160 KB，128K 单请求从 40 GB 砍到 20 GB，同卡并发直接翻倍——数字卡片 5 里那一列，整列减半。但 KV 的量化比权重凶险：K 与 V 直接参与注意力的打分与加权求和，精度损失会一路渗进生成质量，所以业界的普遍分寸是权重敢上 4 bit，KV 从 8 bit 起步，再低就要拿生成质量抵债。

还有一条隐性曲线藏在读带宽里：KV 不只占地方，还要被反复读。解码每一步都把全部 KV 过一遍手，128K 的会话每吐一个字就要搬 40 GB——先用 FP8 把它减半，再靠前缀缓存省掉重复段，最后靠访存优化把每一步压进几十毫秒，长上下文才算真正可服务。容量决定能不能开张，带宽决定开张的速度，长上下文把两笔账同时放大。

这条曲线也解释了价目表上一个司空见惯的现象：为什么长上下文档位的千 token 单价更贵。这不是商业歧视，是物理——更长的上下文意味着更多 KV 长期驻留、更少可接待的并发、每个 token 分摊更高的固定成本；再叠加超长 prompt 预填充对算力的一次性冲击，那是第 6 章的主角。而用户侧的解药叫前缀缓存：把一成不变的系统提示词固定在开头，厂商只算一次、反复复用，重复的 KV 不再重复收费——第 8 章我们会回到它的物理实现，那是全书里最优雅的一笔复利。

下次翻价目表，可以做个小实验：对比同代模型短上下文档位与长上下文档位的单价，再看看它们支持的窗口倍数，你会发现价格差与窗口差之间有肉眼可见的呼应——定价不是拍脑袋，是这条曲线经过商业部门的转译。看不懂的定价，往往只是没有换算成字节。

所以，上下文窗口的军备竞赛每翻一倍，账单就沿这条直线翻一倍。发布会的讲台上说的是我能读多长，机房的账本上记的是我要为此租多大的厂房——曲线的另一头，连着我们这些按 token 付费的人。

## 2.5 我的网关对照：上下文预算的个人实践

以上都是工厂的账。回到我的书桌：一个按量付费的 API 用户，需要懂显存几何学吗？我的答案是，正因为买不起工厂，才更要会算账。厂商的 KV 池满不满，我看不见；但我的上下文窗口超没超，报错看得见——而且每一格窗口都按 token 计费，没有人替我兜底。

我在 npm_yuangs 里写过一个 TokenPolicy，一个四级预算门。它做的事很朴素：请求出门之前，先估算这次要发送的上下文 token 数，除以目标模型的 contextWindow，按占用率分四级处置——

```
占用率 ≤ 70%    安全区：直接放行
70% ~ 80%       预警区：放行，但记录警告
80% ~ 100%      警告区：拦截确认，提供多种解决方案
> 100%          阻断区：强制阻断,必须修改内容或切换模型
```

警告区里那句提供多种解决方案，展开是四个动作：确认继续，明知贵也要发；抽样砍内容，按首尾保留、中间抽稀的 head_tail 策略，把一篇长文档压到能塞下；建议换模型，切到长上下文档位；以及最后的放弃，直接止损。四个动作对应四种付费姿势——硬发是全价，抽样是打折，换模型是换档，放弃是拒付。

四级门里最不受欢迎的是阻断区，但我坚持保留它，并让阻断信息尽量可执行——报错里写明估算占用、阈值与三条出路，而不是一句冷冰冰的超限。后来读厂商的限流文档，发现人家也是这么做的：限流响应带着建议重试时间，预算接口带着余量数字。错误信息的质量，就是产品对用户的诚意；在 Infra 的世界里，连拒绝都要讲清楚数。

70%、80%、100%，三条阈值线画出来，其实就是 2.4 节那条代价曲线的个人版。代码里那个 ModelSpec 结构体，三个字段——contextWindow、costTier、longContextCapable——就是我个人的硬件规格表：我没有 80 GB 的卡，但我有一个按 token 计费的上下文窗口，它的每一格都是租来的显存。厂商的 KV 池按字节计价，我的窗口按 token 计价，单位不同，曲线是同一条。

ModelSpec 里的 costTier 是我自己加的私货：把常用模型按每百万 token 的价格分成低中高三档，预算门在警告区给出的换模型建议，只会指向低档或长上下文档位。这个小设计让预算门从一道闸门变成了半个路由器——它不只会说不行，还会说怎么才行。后来我在云厂商的企业控制台里看到按 key 设预算、超限自动降级的界面，会心一笑：个人在网关里手搓的那点逻辑，和厂商产品经理想的是同一件事。

对照工厂，真正的差别在时机。vLLM 的 KV 池真满了，调度器只能抢占，而抢占的两条路都收费：recompute 模式把已经算过的 prefill 从头重算，拿算力换显存；swap 模式把 KV 序列化搬去 CPU 内存，拿 PCIe 带宽换显存。预算门则把算账挪到请求出门之前，代价约等于零。这是我写 TokenPolicy 时想明白的一件事：先算账再上车，永远比上了车再把人赶下来便宜。集群如此，个人更是如此——因为我们连 swap 到 CPU 内存的资格都没有，超限就是一记报错，连协商的余地都没有。

实现上还有一处让我得意的对应。TokenPolicy 对上下文条目是延迟加载的：先只拿文件的元数据估算体积，估算完决定这条要不要进上下文，授权之后才真正去读内容。这不就是工厂的逻辑吗——先看货物清单，再决定租多少厂房，而不是先把货全搬进厂房再开始扔。估算是廉价的，读取是昂贵的；把昂贵的动作放在便宜的判断之后，是 Infra 的常识，也该是个人工作流的常识。

落到日常，我有三条个人守则。其一，给网关喂大文件前先估算量级：一眼能看到头的文件直接整读，一眼看不到头的走抽样，让占用率停在安全区，而不是撞进警告区再补救。其二，长对话主动开新会话，等于手动回收 KV：上下文每多一句，S 就多一格，那条直线上没有往事自动清零的好事。其三，按任务的上下文画像分流模型：只有真正吃长上下文的任务才配 longContextCapable 的贵档，其余都留在便宜档——这和机房里把不同服务等级的流量调进不同的池子，是同一个动作在个人尺度上的微缩版。

这三条守则没有一条需要新代码，需要的只是把 2.1 节那条公式带在身上：每次粘贴长文之前，心里默算一遍 320 KB 乘以 token 数，再看看窗口还剩几成。Infra 的素养，一半来自工具，另一半来自这种随时随地可以做的口算。

最近的一次真实场景：我让网关帮我审一份几十页的规格文档，预算门在出门前拦下它——估算占用了目标模型窗口的 86%，警告区。我选了 head_tail 抽样，保留开头的方法论部分与结尾的变更清单，砍掉中间大段重复的接口表格，占用率落到 54%，安全区，放行。审阅质量没有受损，账单少了一半多。预算门存在的意义就在这一刻：它把省 token 从一种美德，变成一个按钮。

如果这一章只能带走一个数字，请带走 320 KB：它是每个 token 的 KV 单价，是所有并发与上下文故事的原子。看得见这个数字的人，看账单的眼神会不一样——那不再是服务商拍出来的价格，而是一张显存地租的分期账单，每一期都清清楚楚。

这一章从一条公式出发，算到了三个量级的账本、三种注意力的取舍和一条上下文的代价曲线，最后停在我自己的预算门上。全书的论点在这里第一次完整显形：大模型 Infra 的核心，不是让模型跑得更快，而是让显存利用率接近 100%。这一章算清了分母——一张卡到底有多少可用的显存；下一章讲分子怎么被扔掉——按最大长度预分配连续显存的老办法，会白白浪费 60% 到 80% 的厂房，而 PagedAttention 要用 1970 年代操作系统的老智慧，把浪费压到 4%。从 320 KB 到 4%，推理工厂的经济学，才刚刚开场。

---

## 第3章 PagedAttention:把操作系统的分页搬进 GPU

# 第 3 章 PagedAttention：把操作系统的分页搬进 GPU

上一章末尾，我们算出一条干净的单价：70B 模型每生成一个 token，要在显存里圈下 320 KB 的地皮。这一章要揭开的是一张更难看的账：在 PagedAttention 出现之前，推理系统按最大长度预分配显存，这张地皮的六到八成从头到尾没长出过一个 token。模型没有浪费，浪费的是我们管理显存的方式。2023 年 9 月，几位 UC Berkeley 的研究者把论文挂上 arXiv，10 月在操作系统顶会 SOSP 上宣读——他们把上世纪六七十年代虚拟内存的分页智慧原样搬进 GPU，把 KV 显存浪费从 60%-80% 压到约 4%，同一张卡能同时服务的请求数多出几倍。这一章我们解剖这次移植：浪费从哪来，分页怎么搬，共享怎么产生复利，内核怎么消化非连续的块，最后回到我自己的账单。

## 3.1 浪费的字节数不清：预分配的三宗罪

先回答一个反直觉的问题：一座把厂房按小时出租的工厂，怎么会有六成厂房常年空着？答案藏在推理服务分配显存的老规矩里——预分配。

请求进门时，调度器问的第一个问题不是“你要多长”，而是“你最长能有多长”。上下文上限是出厂就定好的，4096 也好、8192 也罢，老式引擎直接按这个上限，给每个请求划出一整块连续显存，把未来可能长到的 KV 空间一次性圈到位。为什么这么设计？因为那一代实现有两个隐含假设：KV Cache 在显存里必须连续存放；生成过程中只增不减，不能搬家。连续加不可移动，意味着分配只有一次机会，只好按最坏情况圈地。逻辑上无懈可击，工程上是灾难。

用第 2 章的账本把这笔浪费具体化。70B 模型、8K 上限，单请求按上限预留 2.5 GB；100 路并发，预分配时代必须一次性圈下 250 GB——第 2 章数字卡片里那笔著名的总账。可真实流量里的会话，多数几百个 token 就结束了，实际用掉的 KV 只有 16 GB 上下。250 GB 的租金，16 GB 的产出，中间那 234 GB 不是抽象的百分比，是机房里实实在在趴着的一排卡，整夜亮着灯，没存过一个有用的 token。

第一宗罪，内部碎片。预留按上限，生长看天意。你给每个请求留了 8192 个 token 的格子，而真实负载的输出是一条长尾分布：绝大多数回答几百个 token 就撞上句号，能跑满 8K 的是极少数。于是每块地皮里，大半的格子从开盘到退租始终空着。这不是估算偏差，是结构性偏差——预分配天然按最坏情况说话，真实世界按平均情况运转，两者的差额全部躺在显存里晒太阳。

第二宗罪，外部碎片。连续存放意味着地皮必须整块整块地划。显存用得久了，剩下的是东一块西一块的空隙：加起来够再住十个请求，却没有一处连续的空间住得下一个。这像停车场只剩零散车位，总共空着三十个位子，来了一辆要连排十二个车位的房车，管理员只能摆手。于是推理服务最荒诞的一幕出现了：显存明明没满，新请求却在排队；服务商眼睁睁看着空位，接不了客。

第三宗罪，过度预留与重复。解码算法还会火上浇油。并行采样要一个 prompt 同时长出四条回答，束搜索要同时养六条候选，候选的前缀一模一样，老式实现却给每条各配一套完整的 KV，前缀被原样复制了四遍六遍；更早的 Orca 为了防止溢出，甚至按两倍长度预留。圈地按上限，上限之外再加保险，厂房就这样一层层空了下去。

耐人寻味的是，那个年代的工程师并非不努力——注意力内核越写越快，算子融合一个接一个，可显存管理停在 malloc 时代的思维里：整块划出去，整块还回来。聚光灯都打在计算上，管理被当成不值得写论文的杂务。PagedAttention 的作者们反其道而行，把论文投到操作系统顶会而不是机器学习会议，这个选择本身就是宣言：瓶颈搬家了，答案也得跟着搬家。

三宗罪叠加，浪费有多重？PagedAttention 论文的实测是：当时最先进的系统里，真正用来存 token 的 KV 显存只占 20.4%-38.2%。

> **数字卡片 1|预分配时代，一张卡的显存账本**
>
> ```
> KV 池          60 GB（以 80 GB 的卡扣掉权重与杂项为例）
> 有效利用       20.4%-38.2%，真正存 token 的只有 12-23 GB
> 陪跑显存       37-48 GB，从未存过一个有用的 token
> 折成租金       H100 时租 2.85-3.50 美元，一半以上买的是空气
> 连带后果       并发上不去，批次装不满，decode 利用率再打两三折
> ```

最后一行是三宗罪的复合利息。显存浪费直接压低并发上限，批次装不满，本就只有两三成利用率的 decode 阶段雪上加霜；服务商的对策只能是限流、排队、拒客，用客户体验给显存管理的失败垫背。更让人无处发火的是，这一切与模型无关，与 GPU 无关：换成再新的卡，浪费的比例纹丝不动。这是管理问题，不是硬件问题。把浪费翻译成钱更直接：一家驻留 64 张 H100 的服务，六成显存在陪跑，按时租 2.85-3.50 美元计，每月约有 8 万美元的租金花在从未存过 token 的显存上——折算成卡，等于六十四张卡里三十八张整月空转。

这套老办法为什么能活这么久？路径依赖。GPT-2 时代上下文上限 1024，浪费的绝对值小到没人心疼；上下文窗口从 2K、8K 一路涨到 128K，预分配的浪费跟着上限线性膨胀，矛盾才被彻底引爆——第 2 章那条长上下文的代价曲线，每一格增长都在给预分配的账本加码。技术的欠账不会消失，只会等一个更大的数字来清算。

管理问题需要管理的答案。同样的故事，计算机工业在六十年前演过一遍：内存珍贵、程序各自圈地、碎片越积越多，直到 1962 年曼彻斯特大学的 Atlas 机器把内存切成固定大小的页，按需装入，用一张页表在连续的虚拟地址与零散的物理页帧之间做翻译。分页从此成为一切操作系统的常识。2023 年，Berkeley 的研究者只是把这份常识又发明了一遍——换了一个地方，叫显存。

## 3.2 分页的移植：Block、块表与按需分配

PagedAttention 的移植近乎直译。把 KV Cache 切成固定大小的块，默认 16 个 token 一块，想调可以用 block_size 参数改；所有物理块集中放在一个池子里；每个请求看到自己的逻辑块从零开始连续编号，至于每个逻辑块落在哪个物理块上，由一张块表记录。操作系统与 PagedAttention 的概念对照，几乎逐项成立：

| 操作系统 | PagedAttention |
|---|---|
| 进程 | 请求（一条序列） |
| 虚拟地址空间 | 逻辑 KV 块 |
| 页表 | 块表（block table） |
| 物理内存页帧 | 物理 KV 块 |
| 按需调页 | token 写到才领块 |
| 写时复制 | 分叉时复印一块（3.3 节） |
| 换出到磁盘 | 换出到 CPU 内存（3.4 节） |

块大小是这套设计的第一个工程权衡。理论上块越小浪费越少：4 个 token 一块，最后一块的期望浪费从 8 个格子压到 2 个；可块越小，块表越长，内核取数越碎，翻译开销越贵。16 是工程上摸出来的甜点：单层一块 64 KB，与 GPU 的访存粒度对得整齐，块表条目又不至于爆炸；真有特殊负载，block_size 参数随时可调。一切资源管理的终极问题都是粒度——粒度即自由，粒度即开销，分页把这对矛盾从“以请求为单位”下放到“以块为单位”，腾挪的空间就此打开。

工作流程值得完整走一遍。一个请求带着 1000 个 token 的 prompt 进门：引擎从池子里领出第 1 块，装下前 16 个 token；写到第 17 个 token，再领第 2 块——这一块落在显存的哪个角落完全无所谓，块表添一条“逻辑块 1 指向物理块 47”即可。以 70B 模型为例，块表里的一个逻辑块对应 80 个物理块，每层一个；单层物理块装 16 个 token 的 K 与 V，形状是 16 × 8 × 128 的两个张量，FP16 下约 64 KB，80 层合计约 5 MB——正是第 2 章那条公式算出的 320 KB 乘以 16。请求结束时，块整体归还池子，立刻可以转租。地址翻译发生在每一次注意力计算里：内核拿着块表问“逻辑第 3 块在物理哪里”，把散落的块取回来拼装。连续性从物理世界的硬约束，降级为逻辑世界的幻觉——而制造这种幻觉，恰好是虚拟内存的全部精髓。

浪费的几何学随之改写。预分配时代，浪费的是“上限减实际”的整段空间；分页之后，每个序列只有最后一块可能不满，期望空半块、8 个格子，前面所有块都严丝合缝。算笔账：100 路并发、平均每路 500 个 token，总占用 50000 个 token 的 KV；分页后的浪费是 100 个“最后半块”，约 800 个格子，占比 1.6%。序列越短，块内浪费的占比越高，但无论怎么糟蹋都压在个位数；论文口径是 4% 以内。最坏情况也好算：假如每条序列只长 1 个 token，浪费是 16 分之 15，可这个上界由块尺寸钉死；预分配的浪费却没有上界，它由上限与实际的差决定，差多大，浪费多大——有界对无界，是分页在数学上第一次赢下的地方。外部碎片则干脆归零——池子里只要还有一个空块，就能接待一个新请求，连排车位从此不必存在。

移植不是复印，有三处关键的不同，反而值得单独看一眼。其一，页是死的，块是活的：操作系统里页框一旦分给进程，通常驻留到进程结束，而 KV 块跟着对话生长、随请求来去，周转率高得多。其二，缺页是异常，领块是日常：传统系统里缺页中断意味着罕见的磁盘往返，分页引擎里每次注意力都在查块表，翻译路径必须整体塞进 kernel 内部，慢一拍都承受不起。其三，换页到磁盘是灾难，换出到主存是日常：显存与 CPU 内存之间的 PCIe 固然比 HBM 慢几十倍，却远好过磁盘的量级——这给 3.4 节的 swap 留了活路。同一套思想，换一个物理底座，细节全部重排，这正是系统设计的趣味所在。

> **数字卡片 2|从 60% 到 4%：浪费的几何学**
>
> ```
> 块大小          16 token（默认，可用 block_size 调整）
> 每块浪费上限    最后一块，期望空半块 = 8 token
> 例：100 路 × 平均 500 token
>   总占用        50,000 token
>   浪费          约 800 token ≈ 1.6%
>   外部碎片      0，有空块即可分配
> 论文口径        KV 显存浪费 < 4%，有效利用率约 96%
> ```

利用率从约三成跳到约九成六，等效于同一块硬件的 KV 池凭空放大两三倍。连锁反应逐级传导：池子驻得下更多请求，批次装得更满，吞吐跟着抬头。论文的正式结论是：同等延迟水平下，vLLM 把主流模型的服务吞吐比当时最先进的 FasterTransformer 与 Orca 抬高 2-4 倍，序列越长、模型越大、解码算法越复杂，优势越大。后来官网宣传里那句“比 HuggingFace 高 24 倍”，对比的是不做任何 KV 管理优化的朴素基线；工程上更该记住 2-4 倍这个诚实的口径，因为对手已经是当时的世界纪录保持者。本书把结论压成一句论断——一张 A100 能同时服务多 3-5 倍的请求——它落在 2-4 倍吞吐与共享场景更高倍数之间的稳健区间。

还有一个容易被忽略的美德：这一切对上层完全透明。模型不用改，训练不用重来，用户看到的上下文依旧是一条完整的序列——分页是纯 Infra 侧的手术，切口只在引擎内部。它因此能在几个月内被整个行业接受：不需要任何人配合的改变，才是传播最快的改变。另外，vLLM 默认只把显存的九成当配额，留一成给激活值与运行时杂项——第 2 章那条九成安全线，在引擎里是一行叫 gpu_memory_utilization 的配置。

代价当然有，一共两笔。一笔是翻译开销：每次注意力都要查块表，多一层间接寻址；另一笔是访存模式：数据不再连续，硬件预取帮不上忙，取数要多绕几步。这两笔账怎么还，是 3.4 节内核工程师的活。这里只需记住这笔交易的汇率：用百分之几的翻译开销，换回六成以上的显存——人类历史上没有几笔买卖比这更划算。

## 3.3 共享与写时复制：前缀的复用经济学

第 1 章埋过一笔账：束搜索让几条候选序列并行打分，候选们共享前缀，这件事“第 3 章会回来收账”。现在兑现。预分配时代，共享前缀是无解的——两块连续显存不能重叠，谁也没法把自己的一段空间借给别人住；分页之后，共享成了世界上最自然的事：两个请求的块表里，各有一条记录指向同一个物理块。块不会因为被多张表指着而变形，它只是被多引用了几次。

机制是操作系统递过来的旧工具：引用计数加写时复制。并行采样的分叉瞬间——一个 prompt 要长出四条回答——引擎不复制任何字节，只把前缀各块的引用计数加一，四条序列的块表都指向同一批物理块，此后各写各的增量。危险只出现在分叉块上：两条序列要往同一个块里写下一个 token，谁先动笔，谁就把这一块复印一份、改自己的副本——写时复制。引用计数归零，块才真正归还池子。整套机制的灵魂是“延迟到最后一刻”：共享是默认，复制是例外，而且只复印一块，不复印一段。

画一张块表的简图，共享的几何一目了然——

```
逻辑块    请求甲的块表     请求乙的块表     物理块池
  0   ───►   P7    ◄───    P7          P7（引用计数 2，共享）
  1   ───►   P12   ◄───    P12         P12（引用计数 2，共享）
  2   ───►   P33   ◄───    P41         P33（甲私有） P41（乙私有）
```

前两块是共同前缀，两张表指过去；第三块起各自分叉。所谓写时复制，就是把“分叉”从复制字节的时刻，推迟到写入字节的时刻——中间隔着的每一纳秒，都是省下来的带宽。

收益立竿见影。拿一次机器翻译感受一下：六个束共用同一段 2000 个 token 的原文，分叉之后，每一步只有最新的那一小块属于各束自己；没有共享时，六束就是六份完整的 KV，其中五份是纯粹的重复。论文测算这类负载下束间共享比例过半——省下的不是零头，是 KV 账本的一半以上。束搜索这类吃显存的解码算法，也是到这时才第一次变得经济起来。

落到吞吐上，同等显存下候选养得更多更肥，vLLM 对 Orca 的优势扩大到两倍多；并行采样场景，对比按两倍预留的 Orca，优势在数倍到八倍之间。共享率每抬高一寸，显存就多省一尺——这些收益在预分配时代一个字节都拿不到。

更大的复利在前缀缓存。不同请求经常带着同一段开头：同一个系统提示词、同一套角色设定、同一份被反复引用的文档。分页之后，这段前缀的物理块算一次，就能被后来的所有请求用块表直接指过去；判别依据是块的内容哈希，内容相同即命中，请求之间互相不认识都不要紧。效果是把重复的 prefill 整段免掉：一千个用户共享同一段系统提示词时，工厂只需要为它施工一次。

前缀缓存的实现也值得多看一眼。索引按块建立，且是链式的：一个物理块的哈希值，由块内 token 与前一个块的哈希共同算出。想命中第 N 块，必须从第 1 块起逐段对上，一个 token 都造不了假——顺带回答了安全性问题：没人能用拼接的假前缀骗到你的 KV。空闲池的淘汰交给 LRU：没人引用的前缀块先降级为可回收，池子紧张时按最久未用让路。缓存、引用计数、空闲池三本账记在同一张块表上——分页把原本各自为政的内存管理，捏合成了一本账。

> **数字卡片 3|前缀共享的经济学：2000 token 的系统提示词 × 100 路并发**
>
> ```
> 无共享          2000 × 100 = 200,000 token 的 KV ≈ 64 GB
>                 （70B，每 token 320 KB）
> 有共享          前缀 1 份 0.64 GB + 各自增量 100 × 500 token ≈ 16 GB
>                 合计约 16.6 GB，省下约 74%
> 账单侧对应      缓存读约十分之一价——《终端觉醒》第 3 章的演算
> ```

这三种共享是同一条经济学的三个版本。引用计数是公共基建的成本分摊，路灯不必每家自装一盏；写时复制是先用后付的复印机，图纸大家先一起看，谁动工谁复印；前缀缓存是一次施工反复收租，样板间盖一次，一百个买家排队参观。工厂的隐喻到这里升级了：上一节分页解决的是“厂房怎么隔”，这一节共享回答的是“哪些墙根本不必砌两遍”。

当然，共享率的上限不由引擎决定，由负载决定。客服机器人、带固定系统提示词的助手、同一份代码库上的成百次问答——这类前缀同质的负载，共享率轻松过半；而每个请求都天马行空的探索型负载，共享无从谈起。引擎提供的是兑现共享的能力，负载提供的是共享的原材料。这也是评估一家推理服务时值得问一句的问题：它管不管前缀缓存？答案会直接写进你的账单。

边界也要说清。共享只对只读的段安全，谁写谁复印，规则冷酷但公平；内容哈希以块为单位，前缀哪怕只差一个 token，哈希就变，后面整段全部重算——这也是为什么“把不变的东西放在开头”成了提示词写作里的经济行为，第 8 章我们专门算这笔账。至于多租户场景下谁能蹭谁的缓存，那里横着一条隐私的边界，同样留给第 8 章。

## 3.4 内核细节：paged_attention_kernel 与 FlashAttention

分页不是免费的，账单寄给了内核。注意力计算的老代码有一个心照不宣的假设：一个序列的 K 与 V 在显存里连续排列，给个基地址，线性往后读就行。分页之后这个假设碎了——序列的第 3 个逻辑块可能躺在显存任何角落。于是 vLLM 写了自己的 CUDA 内核 paged_attention_kernel：每次解码迭代，先读块表，把逻辑块号翻译成物理地址，再把散落的块一批批取回来，与当前 token 的 Q 做点积、算 softmax、对 V 加权求和。翻译与拼装的开销被压进 kernel 内部，换来的是调度器在显存管理上的完全自由。

这条翻译路径还背着一层设计压力：解码阶段的注意力天然是窄计算——一个 Q 对整串 K、V，并行度低。分页内核把块当成了并行的抓手：K 与 V 按块分给不同的线程组，块内再按头与维度摊开，块与块之间没有依赖，各块的贡献最后按在线 softmax 的算法合并。同一张块表，在调度器眼里是索引，在内核眼里是任务清单——一份账本，两种读法。

它与 FlashAttention 的分工，是这一节最值得带走的知识。FlashAttention 解决的是另一个问题：标准注意力算得太笨——要把序列两两之间的分数矩阵整个写进显存再读回来，访存量随长度平方增长。FlashAttention 用分块计算加在线 softmax，让分数矩阵从不落地，显存读写从平方压回线性，又快又省。两相对照：PagedAttention 回答“KV 放在哪里”，是存储布局问题；FlashAttention 回答“注意力怎么算得快”，是访存算法问题。两者正交，天然可叠加。今天的 vLLM 正是这么分工：预填充阶段整段并行，交给 FlashAttention-2/3 去冲算力；解码阶段逐 token 小步走，交给分页内核按块表取数。

合作之前有一段磨合。FlashAttention 最初同样假设 K 与 V 连续——它是为普通布局写的加速器，并不认识块表。两边要合作，总得有一方让步：要么把散块先拷成连续，白花一次搬运；要么让注意力内核原生接受分页布局。工程界选了后者，于是有了认块表的 FlashDecoding 与 FlashInfer，分页内核也吸收了 FlashAttention 的在线 softmax——两套内核在社区里持续合流，边界慢慢变成同一份分页布局上的两种调度策略。这座工厂里的每一件新工具，最后都得学会读同一本账。

> **数字卡片 4|一层注意力的三层分工**
>
> | 组件 | 回答的问题 | 手段 |
> |---|---|---|
> | PagedAttention | KV 放在哪里 | 分块、块表、按需分配 |
> | paged_attention_kernel | 散落的块怎么取 | 查块表、按块取数、核内拼装 |
> | FlashAttention-2/3 | 注意力怎么算得快 | 分块、在线 softmax、少读写 HBM |

KV 池再大，也有见底的一刻。池子满了而新请求还在排队，调度器只剩两条路——又都是操作系统的老路。第一条叫 recompute，如今的默认：把被选中序列的 KV 直接丢弃，腾出块来，等它们再次轮到时，从 prompt 头部把 prefill 重算一遍，用算力换显存，PCIe 一字节不动。第二条叫 swap：把被抢占序列的 KV 块序列化，寄存到 CPU 的锁页内存里——搬运与计算可以重叠——轮到时再经 PCIe 搬回来，用带宽换显存，算力一次不费。被换下的请求住进 swapped 队列——第 1 章参观调度器时，我们见过这个工位。两条路的定价不同：PCIe 4.0 一条 x16 大约 32 GB/s，把一路 8K 上下文的 2.5 GB 搬出去再搬回来，约一百多毫秒；重算 2048 个 token 的 prefill，在八十层的大模型上是几百毫秒到秒级。prefill 越长，重算越贵，swap 越划算；prompt 短，重算反而便宜。vLLM 把 recompute 设为默认，赌的正是大多数 prefill 没那么长。没有免费的抢占，只有不同的账单。

至此可以把一次解码迭代的完整账单摆出来了：调度器按块表与 KV 池水位决定这一轮哪些请求上场；分页内核逐序列查表、按块取数，FlashAttention 的分块手法规避多余的读写；批次算完，新 token 落进各自序列的最后一块，哪块写满，调度器就为哪条序列再领一块；哪个请求撞上终止符，它的块立即归还池子，排队的新客当轮就能进场。整个循环里，显存的每一字节都有账可查——这正是“推理引擎是显存上的操作系统”的字面含义。

> **数字卡片 5|论文成绩单（SOSP 2023）**
>
> ```
> 有效 KV 利用    20.4%-38.2% → 约 96%
> 吞吐            对 FasterTransformer、Orca，同延迟下 2-4 倍
> 束搜索          束间共享过半，对 Orca 约 2.3 倍
> 并行采样        对两倍预留的 Orca，2.7-8 倍
> 宣传口径        对朴素基线 24 倍，看个热闹就好
> ```

顺带交代论文的下落：SOSP 2023，德国科布伦茨，题目直译是“用 PagedAttention 实现大模型服务的高效内存管理”，拿到全部三项工件评估徽章；当年的最佳论文奖另有其主，它却在随后几年成了被引数千次、被整个行业照抄的标准答案——历史给论文的评分比评委慢，但更准。这篇论文后来长成了行业的事实标准：TensorRT-LLM、SGLang、TGI、DeepSpeed-FastGen，连 llama.cpp 都各自实现了分页式 KV 管理，SGLang 的 RadixAttention 更像是它的前缀共享扩展。vLLM 项目 2024 年捐入 Linux 基金会，2025 年被 PyTorch 基金会收编为托管项目。一次成功的思想移植养活了整个生态——而它借来的所有零件：分页、页表、按需调页、写时复制、换入换出，没有一件是 2023 年的新发明，最新的一件也写了四十多年。

## 3.5 个人尺度：显存浪费就在我的账单里

我没有 A100，机房里那 60%-80% 的浪费看上去与我无关。但把传导链走完就会发现，那笔浪费从来都在我的账单里，只是换了个名字，叫单价。

服务商的成本结构极简：显存租金按小时固定，token 产量按实际产出变化。当 KV 显存的六到八成在陪跑，同样的租金只产出三成的 token，单价就必须乘回去才能保本。换句话说，PagedAttention 之前，你付的每一美元里，可能有六到八毛买到的是从未存过 token 的显存时间。2023 年之后，同一张卡多养活几倍的请求，单价的分母直接放大——此后推理价格的塌方，GPT-4 同等能力从每百万 token 约 20 美元跌到 2025 年底的约 0.40 美元，拆开看是量化、批处理、缓存与分页的合力，而分页是地基：没有它，其余几项的收益挤不进被浪费的六成空间里。这条 50 倍的降价曲线，第 10 章我们完整走一遍。

还有一个体感层面的对应：高峰期的 429 与排队。过去我以为限流是服务商小气，写完这一章再看，那是 KV 池的水位在说话——池子被预分配的浪费占着，能接待的并发就少，高峰一来只能拒客；分页把浪费挤掉之后，同样的池子多养活几倍的会话，你被拒之门外的概率随之下降。利用率每提高一分，最先感受到的往往不是工程师，是我们这些在门外排队的人。

我的网关里没有 KV，但同构的浪费一笔不少。最贵的一笔是系统提示词：我在 poeapi_go 里给每个转发请求都注入一段固定的系统说明，几千 token，过去每一次调用都全价重算——等于我一个人把同一间样板间反复施工了上千次。直到我把提示词缓存用起来，读价约十分之一，账单里最粗的那条线才瘦下去，这笔演算我记在《终端觉醒》第 3 章。第二笔是过度预留：TokenPolicy 的预算门坚持请求出门前先估 token、再看窗口占用，把“按上限圈地”的冲动拦在门口——个人版的反预分配。第三笔最隐蔽：长对话从不主动开新会话，上下文只增不减，我在厂商块表里占着的那串逻辑块越排越长，其中大半是早已无人问津的旧轮次。分页治得了碎片，治不了我不肯松手的执念。

这些教训最后凝成三条个人守则。其一，把系统提示词当固定资产：开头固定、内容稳定，让每一分钱的前缀都只施工一次、长期收租。其二，把会话当租约：任务闭环就开新会话，主动退租，别让旧轮次一直占着块表里的格子吃租。其三，把估算放在发送前面：预算门多按一次的成本是零，发出去之后再想省，就只能指望厂商的缓存了。个人没有 KV 池，但个人的每一次调用，都在替某个 KV 池付租金。

> **数字卡片 6|我自己的浪费账：一段 2000 token 的系统提示词**
>
> ```
> 调用频次        每天 100 次，每次都带着它
> 无缓存          每天为它全价 prefill 200,000 token
> 前缀缓存命中    读价约十分之一，这一行账单直接打一折
> 等价节省        每天约 180,000 token 的输入费，一年约 6,500 万 token
> ```

本地跑模型的人对这套机制的体感更直接。24 GB 的消费卡跑 8B，FP16 权重占 16 GB，留给 KV 的只有 8 GB，这时引擎会不会分页，直接决定你能同时开几路会话、每路能聊多长。今天连 llama.cpp 都吸收了分页思想，分页 KV 已是引擎界的默认常识；你在本地感受到的“多开几个会话就开始排队”，本质就是 KV 池的水位管理，和云端调度器喝的是同一口水。

诚实地说，4% 不是零。块内最后半块的碎片永远在；块越小浪费越少，但块表更长、翻译更勤；块越大访存越顺，但碎片越多——16 个 token 是工程上摸出来的甜点。更要紧的是，分页把利用率推上去之后，瓶颈立刻转向下一层：批次怎么排，prefill 怎么不挡 decode，带宽怎么喂饱 decode，那是第 4 章和第 6 章的故事。利用率的每一次跃升，都把瓶颈推向更深的一层；这是 Infra 工程的宿命，也是它的魅力。

写这一章时，我反复想起自己在 poeapi_go 里写的第一版限流器：按渠道粗粗划几个槽位，一个渠道同时只许跑固定数量的请求，槽位占着哪怕闲置也不放。那正是我亲手造的预分配——一样按最坏情况圈地，一样把浪费留给自己。后来改成按实际负载动态伸缩，代码只多了几十行，同一台小机器硬是多扛了一半流量。操作系统教过我们的，显存又教了一遍，我的小网关再教一遍：管理方式的改进，永远是最便宜的扩容。

全章收拢。PagedAttention 没有让 GPU 多出一个晶体管，没有让一瓦特的电变成两瓦特的算力，它只是换了一种管理显存的方式，同一张卡就多养活了三到五倍的请求。全书总纲的第二个论点在这里第一次被完整证明：容量先于速度，而容量的一半藏在管理里。1962 年，分页让昂贵的内存第一次被穷尽使用；2023 年，同样的智慧让昂贵的显存第一次被穷尽使用。大模型的摩尔定律不在制程里，在调度算法里——这句论断的第一个铁证，刻在一张 80 GB 的 A100 上。厂房还是那间厂房，隔间方式变了，产能翻了几倍。而隔好间的厂房里，排班表还是旧的：请求还在等整批跑完才能出场。下一章，连续批处理。

---

## 第4章 连续批处理:随时插队的调度革命

# 第 4 章 连续批处理：随时插队的调度革命

深夜十一点，我盯着自己网关的监控面板，看到一件当时解释不了的事：同一个渠道、同一张卡，高峰时段的整体吞吐量不降反升，而单个用户的体感却在变慢。后来我顺着 vLLM 的调度器文档一路读到 2022 年那篇 OSDI 论文，才明白“API 该多快”这个问题底下，压着一整场还没讲完的调度革命。这一章从一个朴素的问题出发：为什么一句长话能拖垮全场的短请求？答案分四步展开——先看清静态批的集体等待，再看迭代级调度怎么在每个迭代重新组队，然后处理长 prompt 笨重的进场动作，最后解决车满之后谁让座的难题。全部讲完，我们回到我自己的网关，把每一次卡顿翻译成排班系统的术语。

## 4.1 静态批的集体等待：一句长话拖垮全场

先看一段我网关日志里的真实构图。晚上九点零七分，三个请求在一秒之内进了厂：一个来自我自己的小脚本，问的是“这个正则匹不匹配”，预期答案不超过五个字；一个来自我的阅读流水线，要给一篇六千字的文章做摘要；还有一个是某场长对话的第十轮，上下文已经滚到八千 token。它们几乎同时到达，于是被装进同一个批次。按静态批处理的规矩，这辆车要等车上最后一名乘客下车才算到站——五个字的正则问答、六千字的摘要、十轮的长谈，从此同生共死。这个画面就是本章的起点：调度粒度错了，再快的 GPU 也救不回来。

静态批处理是推理服务最朴素的排班方式：请求到达后攒一批，批次成员在发车那一刻冻结，直到批内所有请求全部生成完毕、整批一起释放，下一批才能上车。它背后的调度视角叫请求级调度——调度器眼里的最小单位是“一个请求”，一次决策、管到底，中途不再过问。这像极了传统铁路的运营哲学：时刻表提前排定，旅客凑满发车，中途不设站，谁也不许提前下车，想上车的人请等下一班。倘若所有请求的长度都彼此接近，这套哲学毫无问题；麻烦在于，语言模型的生成长度是天然的长尾分布。

值得说明，静态批并不是愚蠢的方案，它是深度学习服务化的第一代正统答案。批处理的思想来自训练：GPU 的天性是吃大矩阵，批越大，矩阵乘法越规整，权重搬运摊得越薄——这套直觉从训练场原样搬进了服务车间。在请求长度可预测的场景里，比如固定长度的分类、批量打标，静态批至今仍是正解。它真正失灵的地方，恰在对话式生成这种长度不可预测的负载上：你无法在发车前知道谁坐几站，却必须按“全员同乘到底”来排班。问题的根源不是工程师不会排班，而是负载的性质变了，旧的排班学没有跟上。

长尾有多长？给一个直观的对比：同一个批次里，一个请求可能只等着一句“是”或“否”，另一个请求可能在写千字长文，两者的生成长度可以差出两个数量级（materials/ch04_补充.md）。两个数量级意味着什么？相当于一趟列车上，有人坐一站就下，有人要坐到线路终点，而列车规定：必须等最后一名乘客抵达终点，才允许任何人下车。于是车厢里反复上演同一种荒诞——短途乘客到站后被按在座位上，列车载着他继续空跑几十站；而站台上挤满的乘客，眼睁睁看着一列明明还有空位的火车，就是不肯发车。

系统研究给这两种浪费起了学名。前者叫早完成问题：先算完的请求不能退场，只能留在批次里陪跑——更准确地说，是 GPU 继续为它们计算填充位，把早已没有信息量的位置一遍遍喂进网络，烧的是纯度百分之百的无效算力。后者叫晚加入问题：批次启动之后才到达的请求，哪怕眼前的 GPU 明明闲着，也只能在队列里干等，等整批结束才能进场。一侧在烧没有产出的算力，一侧在拒绝立即可用的产能，浪费同时发生在两侧——这不是工程师偷懒，是请求级调度的结构性缺陷（materials/ch04_补充.md）。

填充位还藏着一笔更隐蔽的浪费。为了让批内所有序列拼成一个规整的张量一起前向，静态批会把短序列补齐到批内最长的长度——明明只需要算五个 token，却要按三百个 token 的矩阵去铺，补出来的位置不携带任何信息，却要实打实占用算力与显存带宽。批内长度越参差，补齐的比例越高，这种“为整齐而支付”的开销就越大。它与第 3 章讲的显存碎片是同一种病：为了用规整的矩形去装不规则的货物，宁肯把箱子填满废纸。

这两种浪费落到运营报表上，就是那个被引用最多的数字：静态批处理下，GPU 利用率常常只有 20%-40%（materials/raw_vllm.md）。用工厂的语言翻译一遍：租下了一整间厂房，灯全开、空调全开、机床折旧照计，但机床有超过一半的节拍在空转——要么在为已经完工的订单打磨废料，要么在拒绝车间门口排队的新订单。运维工程师看到的是利用率低，财务看到的是每 token 成本高，排班员看到的是同一件事：时刻表的粒度太粗了。

> **数字卡片 1|一班静态批火车的账**
>
> ```
> 批内生成长度差距：可达两个数量级（“是/否” 对 千字长文）
> 早完成请求：留批陪跑，GPU 反复计算填充位
> 晚到请求：GPU 空转也须等整批结束
> 静态批 GPU 利用率：约 20%-40%（materials/raw_vllm.md）
> 批大小的两难：车大则陪绑，车小则空转
> ```

有人会想：把车开小一点，陪绑不就轻了吗？账没这么好算。批次是这个系统里摊薄成本的基本手段——权重是固定的，一次前向搬运一次权重，批内序列越多，每个 token 分摊到的权重搬运就越少，吞吐越高。车开大，吞吐好看，但组批要等、陪绑加深，延迟被车上最长寿的那位乘客劫持；车开小，延迟体面，利用率与吞吐立刻塌方。静态批逼着运营者在两个都不想要的东西里二选一，而且选完之后听天由命——这一批恰好全是短请求，效率惊人；混进一个长请求，全员遭殃。同一家服务商，白天忙时与深夜闲时的表现可以差出很远，批次的运气是分不掉的。

用全书总纲的语法说：批处理本身就是一笔延迟换吞吐的交易，而静态批的问题，是它把这笔交易的定价权交给了运气。交易条款在最粗的粒度上一次性签死，此后几十个迭代的每一个瞬间，批次成员都无权再谈。想要更细的条款，就得换一种签法——不是按请求签一份长期合同，而是按迭代签一份可以随时改签的短约。所以问题从来不在车，在时刻表：车还是那辆车，引擎还是那个引擎，需要改的是“谁在车上、坐多久、什么时候能上”的决策粒度。2022 年，一支系统研究团队把调度粒度从请求砍到迭代，给这种新排班法起了一个后来传遍行业的名字——下一节，我们看它如何跳舞。

## 4.2 迭代级调度：三个队列的舞步

2022 年，操作系统顶会 OSDI 上发表了一篇题为《Orca: A Distributed Serving System for Transformer-Based Generative Models》的论文，出自首尔国立大学与 FriendliAI 的团队。它做的事说来只有一句话：把调度粒度从请求降到迭代——调度器每次只命令执行引擎跑一个迭代，也就是一次 forward pass，迭代一结束就重新决定下一轮谁在批次里。术语“迭代级调度”由这篇论文确立，后来传遍行业的“连续批处理”一词也从它流行开来，另一些文献叫它 iteration batching（materials/ch04_补充.md）。名目繁多，内核只有一条：批次不是一份合同，是一个每个迭代都可以重新洗的牌局。

用伪代码看，调度器的全部生活就是这样一个循环：

```
loop forever:
    evict(finished)           # 撞上终止符或长度上限的序列立即出场，释放 KV
    while KV 池有空位 且 批未满:
        admit(waiting.pop())  # 新请求进场，先做 prefill，再并入批次
    step(running)             # 在册的每条序列各生成一个 token
    emit(tokens)              # 各自的 token 流式推回各自的用户
```

（伪代码为示意，综合自 materials/ch04_补充.md）

循环里只有两个关键动作，恰好对应静态批的两种浪费。完成即出场：某条序列撞上终止符的那个迭代，当场退出批次，KV 显存立刻释放——早完成的乘客终于可以下车，陪跑的填充位从此消失。空位即插队：waiting 里的新请求最多等一个迭代就能被接纳进场做 prefill——站台上的乘客不用再等一整班车跑完全程。批次成员逐迭代流动，时刻表变成了派单：司机送完当前这一单，立刻接下一单，不空驶，也不等整队人齐。

把镜头对准四个迭代，舞步就看得见了。迭代一，批次里是 A、B、C 三条序列，各生成一个 token；迭代二，B 撞上终止符，当场出场，占用的 KV 块归还池子，队首的新客 D 立刻被接纳进场，补做 prefill 后与 A、C 同台；迭代三，C 的上下文长到需要新块，池子刚好腾得出；迭代四，又有新客 E 进场。批次的花名册每个迭代都在改写，而场外的 waiting 队列始终有人盯着空位——没有一发车仪式，也没有一收到站广播，派单，接单，送达，下一单。

工程上有一道看似过不去的坎：批次里的序列长短不一、进度各异，注意力计算没法像矩阵乘法那样跨序列拼成规整的大张量——每条序列手里的 K、V 长度都不同。Orca 的解法叫选择性批处理：注意力之外的算子——MLP、LayerNorm、GeLU——照常成批执行，吃满算力；注意力则逐序列单独计算，论文论证这部分损失很小（materials/ch04_补充.md）。而真正的终解要等另一位主角登场：第 3 章的 PagedAttention 让注意力内核能拿着块表到显存各处收集不连续的 KV，变长批次从此不再是问题。调度革命与内存革命是互相成全的一对——没有分页的显存，迭代级调度装不下那么多变长的乘客；没有迭代级调度，分页省下来的显存也只能继续空着。

vLLM 把这套思想工程化成三个队列的舞步（materials/raw_vllm.md）：

```
waiting   排队的新客，还没开始 prefill
running   正在逐 token 解码的在册序列
swapped   被抢占、KV 暂时挪去 CPU 的序列
```

每个迭代，调度器在三条硬约束下重新排班：批内序列数不超过 max_num_seqs，默认 256；单步调度的 token 总数不超过 max_num_batched_tokens；KV 池里必须腾得出足够的空闲块。排班的先后顺序大致是：先让 running 里的老乘客完成本步解码，再把 swapped 里的换回来，最后才从 waiting 接纳新客做 prefill（materials/ch04_补充.md）。三个队列就这样循环转位：waiting 转入 running，running 偶尔被降入 swapped，再转回 running，完成后离场——一场永不停歇的圆舞，舞步的节拍就是 forward pass 的节拍。

这套舞步还有一个常被忽略的动力学细节：每个迭代的时长不是常数，而是批次成员数的函数。批里有十条序列，一次前向就要为十条各算一步；新客不断插队，批越来越肥，迭代就越走越慢，批内所有人的逐 token 节奏随之放缓。接纳新客是吞吐的收益，拉长迭代是全场的延迟成本，而 max_num_seqs 这类闸门就是这笔交易的定价器——连续批处理没有取消延迟与吞吐的守恒律，它只是把结算周期从“一整批”缩短到“一个迭代”，让调度器在每个节拍上都有机会重新权衡（materials/ch04_补充.md）。

效果有多直接？混合长度的负载下，连续批处理的吞吐比静态批高出 2-4 倍（materials/raw_vllm.md）；Orca 论文自己的口径更激进：在 GPT-3 175B 上对比当时的 FasterTransformer，相同延迟下吞吐提升最高达 36.9 倍（materials/ch04_补充.md）。两组数字的实验口径不同，不必互相换算，但方向完全一致：光是把调度粒度切细，就能从现有硬件里凭空拧出成倍的产能——没有换卡，没有改权重，没有动模型的一根毫毛。

还有一桩隐性的收益：公平。静态批的晚加入问题让先来后到形同虚设——你比下一位乘客早到十分钟，却因为他凑巧赶上了上一班车而多等一整班。迭代级调度把上车权还给队列：每个迭代都是一次新的发车机会，等待时长与到达时刻挂钩，而不是与某一批人的运气挂钩。对上游的网关而言，这直接改善超时率的分布——尾部延迟收敛，超时与重试的雪崩就少了；第 7 章讲熔断与退避时，我们会回到这条因果链。

> **数字卡片 2|调度革命的价签**
>
> ```
> 调度粒度：整个请求 → 单次 forward pass
> 新请求的等待上限：一个迭代，而非一整批
> 混合负载吞吐：比静态批高 2-4 倍（materials/raw_vllm.md）
> Orca 论文同延迟吞吐：最高 36.9 倍（GPT-3 175B，对 FasterTransformer）
> 批内序列上限：max_num_seqs 默认 256（materials/ch04_补充.md）
> ```

用一句比喻收拢本节：静态批是火车时刻表——车次固定、中途不上下客、全员绑定到终点站；连续批处理是网约车派单——每一单结束立刻派下一单，乘客随到随走，司机永不空驶。排班表的粒度从“一班车”细化到“一次起步”，厂房的每一平方米在同一时刻都有活干。但两个新问题随之浮出水面：上车动作太重怎么办——长 prompt 的 prefill 会压住全车；车太满怎么办——KV 池见底时谁让座。这是后两节的主角。

## 4.3 Chunked Prefill：把长 prompt 切成不阻塞的小段

连续批处理解决了“谁在车上”，却没解决“上车动作”本身有多重。请求被接纳的第一件事是 prefill：把整段 prompt 一次性并行送进网络，为每个 token 算出 K、V 写入缓存。第 1 章交代过，prefill 是计算密集的活，一旦启动就要把 GPU 的算力吃干吃净；而 prompt 越长，这个动作持续得越久。迭代级调度能随时换人，却换不掉这个笨重的进场动作——在调度器眼里，它本身就是一个不可分割的大迭代。

于是出现了新时代的队头阻塞。设想一个三万 token 的 prompt 进场——在今天的负载里这毫不稀奇，整本书、整个代码仓库都会被整个塞进来。一次 prefill 要独占 GPU 几百毫秒，这几百毫秒里，同批所有 decode 序列一步都走不动，所有正在流式输出的用户同时看到自己的打字机冻结。祸端在队头，账单却均摊给全场：一个人的行李太大，全车厢的人陪他停站。英语世界给这类现象起了个形象的名字，Head-of-line blocking，翻译过来就是队头阻塞。

把它翻译成体感：深夜流式聊天，屏幕上的字突然顿了半秒又继续奔涌，多数人的第一反应是网络抖动，其实是厂房里有人进了一节超长车厢——你的每一步解码，都在给别人的 prefill 让路。反过来，那个发长 prompt 的人也不舒服：首字迟迟不来，因为他的 prefill 被排进了迭代序列，要按节拍慢慢磨。

vLLM 的解法是 chunked prefill，分块预填充：把超长 prompt 切成固定大小的段——默认 2048 token——每个迭代只调度一段，与 decode 序列交错执行（materials/raw_vllm.md）。类比仓库作业：大件货物拆成标准托盘，叉车一次只搬一盘，流水线不必为单件大货停线；托盘的尺寸按流水线的节拍设计，2048 这个默认值，就是当代 GPU 一个迭代消得化的口粮刻度。

切段之后还有一层红利：prefill 的段与 decode 的序列可以混进同一个批次，V1 引擎默认支持（materials/raw_vllm.md）。于是每个迭代里，算力负载在重计算与轻搬运之间摊匀——这一步有重型 prefill 段压阵，下一步全是轻巧的 decode，GPU 的节拍从“重一拍、轻一拍”变成“每拍都匀”。对调度器而言，批次第一次在两个维度上同时流动：成员在换，成员内部的活也在切。

2048 不是一个魔法数字，是一个可调的旋钮。块切得越小，decode 的节奏越平滑，但长 prompt 的 prefill 被拉得越碎、首字来得越晚；块切得越大，长请求的 prefill 越快收敛，但每个迭代里的重型段落越重，decode 又开始跟着颠簸。调度器手里还有一道配套的预算 max_num_batched_tokens——单步调度的 token 总额度，prefill 段与 decode 序列在这份额度里分账。调优的乐趣与烦恼都在这里：没有普适参数，只有你这个负载的形状（materials/ch04_补充.md）。

代价照例要标价。长 prompt 的首 token 延迟变差了——原本一口气算完，现在分段排队，排在每个迭代的重活末尾。这是一笔明码标价的交易：用一个请求几百毫秒的首字延迟，换全场用户平稳的逐 token 节奏。总纲第四条说没有免费的加速，chunked prefill 是它的又一处注脚——调度的每一次细化，本质上都是把延迟从大多数人身上挪到少数人身上，挪得越精确，整体就越接近无人受苦的边界。

这笔交易还有更深的背景，第 6 章会整章展开：prefill 与 decode 是性情相反的两段工作，一个吃算力、一个吃带宽，混在同一张卡上天然互相拖累。实测里，当 prefill 挤进共享引擎，decode 的逐 token 延迟会出现 150 毫秒量级的尖峰，远超 50 毫秒的服务目标线（materials/raw_cost.md）。chunked prefill 是把尖峰锯碎的第一步，先降低问题的烈度；更彻底的答案是把两个阶段彻底分家、各开各的厂——那是 PD 分离的故事，第 6 章再讲。

> **数字卡片 3|一段长 prompt 的两种走法**
>
> ```
> chunk 默认大小：2048 token（materials/raw_vllm.md）
> 不切块：一次 prefill 独占 GPU，同批 decode 全体停摆，流式集体冻结
> 切块后：每迭代一段，与 decode 交错，算力节拍摊匀
> 代价：长 prompt 自身 TTFT 变差
> 混部实测：ITL 尖峰 150 毫秒量级，对照 50 毫秒 SLO（materials/raw_cost.md）
> ```

我在自己的网关里见过这两副面孔。给阅读流水线发长文摘要时，首字明显慢于短问答，但流式节奏反而更稳——当初只当是玄学，后来才对上号：慢，是 prefill 的长度在给首字定价；稳，是调度器把大 prefill 拆成小段之后发给全场的补偿。体感层面的“慢”与“稳”各有各的物理来源，账不能混着算。

至此，调度粒度完成了两级细化：从请求降到迭代，再从迭代内部把重型进场动作切成轻段。刀法已经足够细，最后一个问题却更加物理：显存是有限的，车开得越满，KV 池越容易见底——满载的时刻，必须有人下车。

## 4.4 抢占的艺术：recompute 与 swap

连续批处理把车开满之后，最尴尬的时刻迟早到来：KV 池见底。running 里的每条序列每个迭代都在生长，KV 占用只涨不跌；waiting 里还排着新客；此时物理显存里挤不出下一个空闲块。调度器只剩两个选项：把新客继续晾在门外——利用率应声回落，前面所有努力打折；或者，让在册的某条序列下车，把它的显存腾出来。后一条路需要一种机器时代的老手艺：抢占。

这是第 3 章埋下伏笔的兑现处，也是总纲第三条“操作系统的胜利”的又一页：1970 年代虚拟内存的换页、缺页与内存吃紧处置，在 GPU 显存上原样重演（materials/raw_vllm.md）。区别只在名词：被换出的不是进程的内存页，而是序列的 KV 块；被挂起的不是进程，而是一段写到一半的对话。当年操作系统课上的换页算法，五十年后在显卡上重新上岗，连队名都懒得改——swapped，换出。

vLLM 给了调度器两条下客路。第一条叫 recompute，重算：把牺牲序列的 KV 全部丢弃，块当场归还池子；等它被恢复时，拿着当初的 prompt 从头再做一遍 prefill。KV 一字节都不占，恢复的代价是重算的算力。这是 V1 引擎的默认模式（materials/raw_vllm.md；materials/ch04_补充.md）。听起来奢侈——把已经算过的东西再算一遍——但账要算全：prefill 本来就快，而 GPU 相对富余的恰恰是算力；被请出去的多是进场不久、上下文尚短的新序列，重算的基数并不大。

第二条叫 swap，换出：把牺牲序列的 KV 块序列化，搬进 CPU 的主存；恢复时再经 PCIe 总线原样拷回。KV 完好保存，一个 token 都不用重算，代价是来回两趟 PCIe 的搬运时间与带宽。swapped 队列的名字就从这条路上来——那里面排的不是抽象的“被暂停”，而是一批寄存在主存里的账本，等池子有了空位再搬回来（materials/raw_vllm.md）。

两条路的成本都可以粗算。recompute 的账单是重做一次 prefill，代价与被牺牲序列的上下文长度成正比——几百 token 的序列眨眼算完，几万 token 的序列就是一笔重款。swap 的账单是两趟 PCIe 搬运：KV 字节数乘以二、除以总线带宽。按第 2 章的公式，一条八千 token 上下文的 70B 序列，KV 就有约 2.5 GB，普通 PCIe 总线来回一趟要以百毫秒计。账算到这里，“什么时候选哪条路”就不再是口味问题，而是上下文长度与总线带宽两个变量的函数。

两条路是一笔带宽换算力的交易。重算花 GPU 的算力——算力是近二十年涨得最凶的东西；swap 花 PCIe 的带宽——而 PCIe 的带宽增长常年追不上 GPU 算力的增长。天平因此倒向 recompute：重算一遍往往比把账本搬回来还便宜。但交易没有普适解：上下文越长，重算越贵，swap 的相对身价就慢慢涨回来。工程里没有永远正确的选项，只有约束变化之后需要重新签订的条款——量化是精度换显存，批处理是延迟换吞吐，抢占是带宽换算力，全书翻来覆去用的都是这一条语法。

那么谁当牺牲者？调度器按优先级挑人：恢复成本最低、业务权重最轻的序列先请出去，刚接纳的新客与高优先级的会话尽量不动（materials/raw_vllm.md）。再往下追问——优先级怎么定、租户之间怎么公平、服务质量分层怎么落地——就走进了生产集群的排班学，那是第 7 章的领地。这里只需记住：抢占不是失败的兜底，而是满载运行的常规动作；一个从不需要抢占的调度器，多半是把利用率压得太低，才换来那份清闲。

抢占还有一层不太体面的政治学：它把延迟的苦果精准地砸在被牺牲者头上。池子紧张时，谁的低优先级会话被请下车、谁的高优先级会话稳坐钓鱼台，取决于运营者给请求标的价码——个人开发者的免费额度与企业的年度合同，在同一个 KV 池里排队，待遇并不相同。这不是阴谋，是任何稀缺资源分配系统的必然形状：调度器必须在公平与服务承诺之间选一个倾向，而选择的代价由队列末端支付。看懂这一层，你就明白为什么同样的 prompt，换一个套餐，速度判若两卡——第 10 章算成本账时，这笔差价会再次出现。

> **数字卡片 4|两条下客路的成本结构**
>
> ```
> recompute：丢弃 KV，恢复时重做 prefill；不占 PCIe 带宽，花 GPU 算力；V1 默认
> swap：KV 序列化搬入 CPU 主存，恢复时经 PCIe 拷回；不重算，占带宽与主存
> 天平倾向：GPU 算力增速长期高于 PCIe 带宽增速，故 recompute 为默认
> 例外条款：上下文越长，重算越贵，swap 的相对身价回升
> 挑人原则：按优先级，先请恢复成本低、业务权重轻的序列下车
> ```

我在自己的网关里做的是同一道题的小号版本。poeapi_go 的多渠道路由与熔断，本质也是在资源不够时决定牺牲谁、保住谁——渠道连坏几次就熔断降级，把流量让给健康的渠道；TokenPolicy 的四级预算门则更进一步，在抢占被迫发生之前，就按预算把超额请求挡在门外。写过这些代码之后再读 vLLM 的抢占逻辑，像在照镜子：个人尺度的熔断与集群尺度的抢占，共享同一个决策内核——用局部的牺牲换全局的存活，差别只在牺牲的计量单位，一个是请求，一个是 KV 块。

把本节接回主线：抢占是连续批处理的执行机构。正因为每个迭代都有权反悔、有权请人下车，调度器才敢把车开到满员；敢承诺高利用率，是因为敢反悔。第 3 章省下来的显存、本章排满的批次，到 KV 池见底的那一刻汇合成同一个问题——而答案，操作系统在半个世纪前就写好了。

## 4.5 个人尺度：排队论就在每次卡顿里

回到每个 API 用户都有的体感：同一家服务商，凌晨三点又快又稳，晚上九点首字要等两秒、流式还会突然冻一下。模型没换，GPU 没换，机房没搬——变的只有排班表上的拥挤程度。本章前四节的所有机制，都藏在这句朴素的抱怨里；现在我们把它们一一指认出来。

先补一笔排队论的常识，它解释“为什么偏偏是高峰变慢”。利用率逼近满载时，等待时间的上涨不是线性的，是爆炸式的——到达率每逼近服务能力一分，队列长度就翻着跟头往上涨。连续批处理把利用率顶到接近满载，这正是它 2-4 倍吞吐的来源；硬币的另一面是，高峰时段的队列天然比静态批时代更满、更敏感，一次小的到达率波动就能传遍整个批次。吞吐与延迟之间有一条守恒律，调度器的一切技艺，都是把这笔交易标好价、排好序，而不是取消它——总纲第四条，一切优化都是交易，在排班表上同样生效。

我在自己的网关里看这些机制落成体感。poeapi_go 的 internal/metrics 记下每次调用的首字延迟与逐 token 节奏，internal/trace 把每段等待归因到具体的渠道路径。日志看得久了，四种最常见的体感各自找到了物理对应物：

```
首字越来越慢        →  waiting 队列积压，接纳被推迟
流式突然冻半秒      →  超长 prompt 的 prefill 段插队，或 swap 拷回占用 PCIe
生成节奏忽快忽慢    →  每迭代重新组队，批内同伴在换，摊薄随之波动
偶发超时、重试即成  →  你的请求被抢占过，或网关熔断切换了渠道
```

这张对照表是我写网关两年最实用的收获：体感不再是玄学，每一次卡顿都能在排班系统里找到自己的工位。更重要的是它划清了责任边界——队列积压与抢占归服务商的调度器管，上下文长度与重试姿势归我自己管；归自己管的部分，才是个人尺度上真正的优化空间。

几条我每天都在用的守则。其一，流式必开：首字延迟由 prefill 与排队共同定价，流式至少让第一个字尽早抵达，体感改善远大于账面数字。其二，给上下文瘦身：历史每多一段，prefill 要重算的 KV 就多一批，decode 每步重读的账本更厚，你的请求在 running 里待得更久，也更可能在抢占中受伤——第 2 章的公式与第 8 章的前缀缓存，都是给这条守则供给弹药的。其三，错峰是免费的加速：深夜与高峰的差距是队列长度的差距，不是机器的差距，能挪的批处理任务挪到低峰，等于白捡一倍吞吐。其四，把超时与重试当调度参数调：超时设得太短，你会亲手把一条只是被抢占的请求判成失败；重试不设退避，你会在爆炸的队列里再塞进一票。其五，选服务商时看并发配额与排队透明度，不只看单价：单价是每个 token 的价，配额决定你的请求在 waiting 里站多久——个人开发者真正的天花板常常是后者。

再往深处想一层，个人用户的排队管理，本质上是在给自己建一座微型调度器。我的 TokenPolicy 四级预算门是入场控制——预算见底的请求连门都进不了，根本不去挤人家的 waiting 队列；重试的指数退避是流量整形——被拒了就退避着再来，而不是把失败变成新的洪峰；错峰的定时任务是主动的负载迁移——把自己的波谷对齐电网的波谷。网关写到第三年我才想明白：我在个人尺度上写的每一行限流代码，都是 vLLM 调度器某个抽象方法的拙劣实现——区别只是它管理的是显存块，我管理的是自己每月的 token 预算。

> **数字卡片 5|体感的数字对照**
>
> ```
> 高峰与深夜的首字差距：队列长度之差，非机器之差
> 混合负载吞吐：连续批比静态批高 2-4 倍（materials/raw_vllm.md）
> ITL 尖峰：150 毫秒量级，对照 50 毫秒 SLO（materials/raw_cost.md）
> chunk 默认 2048 token，批内序列上限默认 256
> 抢占两式：recompute 为默认，swap 为备援
> ```

回望全章，我们没有换一张卡、没有改一行权重，只把排班表从火车时刻表改成网约车派单，混合负载的吞吐就多出 2-4 倍；把长 prompt 切成 2048 的托盘，全场的流式就不再集体冻结；给满载的车配好抢占的规矩，利用率才敢常年在高位运行。这是全书论点迄今最锋利的一次显形：大模型 Infra 的核心不是让模型跑得更快，而是让显存利用率接近 100%——调度粒度每细化一层，厂房就多挤出一分产能，而制程工艺一个纳米都没有前进。第 10 章我们会看到，这 2-4 倍与量化的 4 倍、推测解码的 2 倍相乘，正是 token 价格塌方的底层密码。

座位排满了，下一章我们换个方向：不再研究怎么让更多人挤进同一间厂房，而是把每个座位本身做小——用更低的比特表示权重，让 70B 的模型住进 35GB 的显存。精度换面积的交易怎么签、条款里藏着哪些陷阱，是第 5 章的全部内容。

---

## 第5章 量化:用精度换显存的交易

# 第 5 章 量化：用精度换显存的交易

在真正动手算这笔账之前，我一直不太信量化这件事。把每个参数从 16 位砍到 4 位，等于逼一座工厂把每台机床的加工精度砍掉四分之三——产品还能合格？直到我把一个 70B 的模型压进 35 GB 的文件，在一张卡上跑通，又看到评测里那条曲线：质量保留 98%。那一刻我才明白，大模型是一座冗余到奢侈的工厂，而量化是一门把机床做小的手艺：用精度换显存，用每位数的损失，换厂房里多住下的几路请求。这一章我们把这笔交易从头算起：条款怎么写，四条技术路线各自怎么签，精度与显存的实价是多少，条款里藏着哪些例外，最后回到我这张显存预算以个位数 GB 计的书桌，看一个个人开发者怎么把合同签得聪明。

## 5.1 一笔交易的条款：仿射量化与分组量化

先回到第 2 章那份显存账本。账本第一页是权重，规则只有一行：显存约等于参数量乘以每参数字节。这行乘法里，参数量是模型出厂时焊死的，我们一行也改不了；唯一能动刀的因子是每参数字节——FP32 四字节，FP16 两字节，INT8 一字节，INT4 半字节。量化不减少机床的台数，只把每台机床做小：一个 70B 的模型，FP16 权重占 140 GB，INT4 只要 35 GB，同样的智商，占地缩到四分之一。多出来的厂房干什么用？第 2 章已经算过——给 KV Cache 这个在制品腾地方，同一张卡多接几路并发。这就是量化这笔交易的全部动机：牺牲一点精度，换取多住几个人。

> **数字卡片 1|常见数值格式：每位参数的体重**
>
> | 格式 | 位宽 | 每参数字节 | 说明 |
> |---|---|---|---|
> | FP32 | 32 bit | 4 B | 训练基准 |
> | FP16 | 16 bit | 2 B | 推理常用，范围受限需 loss scaling |
> | BF16 | 16 bit | 2 B | 与 FP32 同范围，训练首选 |
> | INT8 | 8 bit | 1 B | FP16 的一半 |
> | INT4 | 4 bit | 0.5 B | FP16 的四分之一，部署甜点 |
> | NF4 | 4 bit | 0.5 B | 档位按正态分布排布，QLoRA 专用 |
> | FP8 | 8 bit | 1 B | E4M3/E5M2，H100/H200 原生支持 |

条款的数学措辞只有一行，叫仿射量化：

```
weight_float ≈ scale × weight_int + zero_point
```

scale 是缩放因子，约等于浮点范围除以整数范围；zero_point 是零点偏移，负责把两边零点的位置对齐。量化的全部动作，就是拿这把尺子去量每一个浮点权重，把读数吸附到最近的整数档位上；反量化时再用尺子把档位换算回浮点数，误差就是吸附那一刻损失的那一点。INT8 有 256 档，INT4 只有 16 档。16 档怎么够？一条数轴上只有 16 个可以落脚的点，要承载几十亿个千差万别的浮点数，听上去像天方夜谭——这是每个初学量化的人心里最大的一道坎，答案叫分组量化。

权重的分布有个天然属性：局部相似。相邻的一小段权重，数值范围往往很接近。于是量化不给整条数轴只配一把尺子，而是把权重按 32 个或 128 个分成一组，每组各配自己的 scale 与 zero_point。INT4 的档位仍然只有 16 个，但 A 组的刻度 7 可能代表 0.03，B 组的刻度 7 可能代表 -1.2：刻度是稀疏的，尺子是海量的。把档位少的损失，用分组密的方式摊薄，实际有效精度远高于 16 档——这是 INT4 敢上生产线的第一根支柱。当然天下没有白配的尺子：分组越细，精度越高，尺子本身占的字节也越多；分组越粗，省得越狠，误差越大。group size 这个超参数，就是这笔交易里第一个可以谈的条款。

举个具体的例子，把尺子摸一遍。设某一组 128 个权重落在 -0.08 到 0.06 之间，INT4 的 16 个档位要对齐这段区间，尺度约为 0.0093；量化就是拿每个权重除以尺度、四舍五入到最近的整数档，误差上限约半个档位，0.005 上下——对大多数权重足够体面。麻烦出在离群值：假如这组里混进一个 0.4 的极端权重，按最大值定尺子，档位间距会被拉到 0.032，普通权重的误差瞬间放大六倍。所以每个量化实现都会先做裁剪，把千分之一二的极端值牺牲掉，换其余权重的安宁。尺子怎么定、极值怎么裁，是各家量化实现的第一处分歧，也是它们质量差异的源头之一。

第二根支柱更根本：大模型的冗余度。几十亿参数的集体行为像一场规模空前的表决，单个权重差之毫厘，结论纹丝不动。冗余度高到什么程度？经验数据是，70B 模型压到 INT4，效果常常比 13B 模型的 FP16 还好——参数量的优势，碾压了位宽的损失。这句话是本章的地基：量化交易的定价权，不在量化算法手里，在大模型自身的冗余度手里。模型越大，冗余越多，谈判筹码越足；模型越小，冗余越薄，越经不起砍。这句话先记下，5.4 节它会变成一条硬规律。

> **数字卡片 2|两条签约路径：PTQ 与 QAT**
>
> | 维度 | PTQ（训练后量化） | QAT（量化感知训练） |
> |---|---|---|
> | 是否需要训练 | 否 | 是，额外微调 |
> | 耗时 | 分钟级 | 数小时到数天 |
> | INT4 精度 | 可接受 | 更好 |
> | 适用场景 | 默认选择 | 精度要求极高或激进量化 |

绝大多数部署签的是 PTQ 这一条：模型练完之后拿过来直接量化，几分钟出结果，再用 AWQ、GPTQ 这类算法把精度损失补回来。QAT 则把量化动作提前写进训练过程，让模型在学习的阶段就适应档位，效果更好，但代价是一次额外的训练——大模型时代，训练一次的成本足以让所有人对 QAT 望而却步。于是 PTQ 配上聪明的补偿算法，成了事实上的行业标准；本章后面四条技术路线，全是 PTQ 阵营里的手艺。

还有一层认知帮我安下心来：训练与推理对精度的需求，本来就不对称。训练要反向传播、要累加梯度，误差会被优化器一路放大，所以 FP16、BF16 是底线；推理只是一次前向，读的是几十亿参数的集体表决，天然容错。把权重想成一张灌好的母带，量化不是重录，而是降低比特率的有损压缩——母带的动态范围还在，只是多数听众听不出那些被舍入的细节。CD 到 MP3 没有杀死音乐，FP16 到 INT4 也没有杀死智能——前提是压缩器的算法要够好，这正是下一节四位主角的用武之地。

条款至此谈妥：用仿射量化把浮点压成整数，用分组量化把档位的损失摊薄，用模型自身的冗余度兜底。但同一份合同可以有不同的签法——GGUF、AWQ、GPTQ、bitsandbytes，四条路线各有各的措辞，也各有各的追债手法。

## 5.2 四条技术路线：GGUF、AWQ、GPTQ、bitsandbytes

同一场交易，四种签法。

先说最平民的 GGUF。它首先是 llama.cpp 生态的文件容器格式——把权重与元数据打包成单个文件，mmap 零拷贝加载，CPU、Apple Silicon、消费级 GPU 通吃，是本地部署世界的通用语。但 GGUF 的灵魂是它的 k-quant 量化方法族。先学会读文件名：Q4_K_M 三个字段，Q4 是名义位宽，4 bit；K 是 k-quant 家族的姓氏，由 ikawrakow 在 2023 年 6 月的一笔 PR 里引入；M 是 Medium，混合精度的中档预设——后面还有 S 与 L 两档，控制的是同一档位内体积与质量的取舍。这三个字母背后，是三层工程智慧。

第一层，超块。老式块量化（Q4_0、Q8_0 这类）每 32 个权重共享一个 scale，一个块一把尺子，遇上离群值整组遭殃。k-quant 引入两级层次：8 个 32 权重的子块组成一个 256 权重的超块，超块配一把 FP16 的主尺子管全局动态范围，每个子块再配自己的小尺子管局部细节。以最常用的 Q4_K 为例，256 个权重的超块总共 144 字节，实际位宽 4.5 bit——比名义的 4 bit 略胖，换来的是重建误差显著小于老式方案；反量化时每个权重按主尺子乘子尺子换算回去，两级系数相乘，16 个档位就这样撑起一张连续覆盖的网。第二层，双重量化：尺子本身也要占地方，参数一多，量化常数能吃掉数 GB；k-quant 把这些常数再压一层，16B 模型的常数开销从约 2 GB 降到约 1 GB。第三层，混合精度，也是 M 后缀的精髓：不同张量对量化的敏感度不同，Q4_K_M 把大部分张量留在 Q4_K，把注意力 V 投影与 FFN 下投影这两类经验上最敏感的张量升到 Q6_K，输出层恒定 6 bit。敏感张量占比小，整体位宽只从 4.5 涨到约 4.8 bit，质量却逼近 Q5_K——用极小的体积，换显著的质量。这一手自动的精度分配，让 Q4_K_M 成了社区事实上的默认档，HuggingFace 上量化模型下载量的三到四成都是它。

> **数字卡片 3|GGUF 家族价目：位宽与质量的阶梯**
>
> | 类型 | 实际位宽（bit/权重） | 质量判词 |
> |---|---|---|
> | Q8_0 | 8.5 | 极佳，几乎无损 |
> | Q6_K | 6.6 | 很好 |
> | Q5_K_M | 5.7 | 好，质量敏感任务推荐 |
> | Q4_K_M | 4.8 | 最流行的默认推荐 |
> | Q4_0 | 4.5 | 可接受，快于 Q4_K_M 但质量更低 |
> | Q3_K_M | 3.9 | 明显损失，显存受限才用 |
> | Q2_K | 3.4 | 显著损失，最后手段 |

再看 AWQ，激活感知权重量化。它的出发点是一条观察：并非所有权重同等重要。用校准数据把模型跑一遍，按激活值的幅值去找显著权重——注意，判据是激活的幅值，不是权重自身的数值大小。为什么看激活不看权重？因为权重的数值大小不等于重要性——有些通道的权重平平无奇，激活一到它这里就爆表，它的贡献会被下游层层放大；激活的幅值，才是权重重要性的信号弹——这样的显著通道只占 0.1% 到 1%，却对模型质量影响巨大。找到之后并不给它们开小灶存储，而是做一次数学等价的变换：量化前把重要通道的权重放大，同时把对应的激活反向缩小，乘积不变；然后统一量化。被放大的通道在同样的 16 个档位里占据更宽的范围，相对精度自然更高——好钢用在了刀刃上。AWQ 不需要重新训练，精度通常略优于 GPTQ，内核干净、推理更快，是 vLLM 在 A100、RTX 4090 这类没有 FP8 硬件的服务端卡上的主流格式。

GPTQ 走的是另一条补偿路线。它的哲学是：误差不可避免，但可以补救。逐层逐权重地量化，每压掉一个权重，就立刻用 Hessian——损失函数的二阶导数——算出最优方向，调整其余还没量化的权重，把这一步的误差分摊掉，像一位记账极勤的会计：每花掉一分精度，马上在别处补一笔分录。代价是需要校准数据（标准配置 128 条），7B 模型单卡量化约 5 分钟；group size 128 的 4-bit 量化，质量接近 FP16。128 条校准数据是个耐人寻味的量级：用如此少的数据就能算出足够好的补偿方向，靠的是损失面在局部足够平滑——大模型又一次用自己的平滑性，替工程买了单。它的量化过程比 AWQ 慢，推理内核也不如 AWQ 干净，但 Hessian 补偿这条思路，是量化从格式工程走向误差最优化的分水岭。

第四条路线 bitsandbytes 是实用主义的极致。LLM.int8() 干脆不做全量 8 bit：识别激活中的离群特征通道，这些通道保留 FP16，其余压到 INT8。NF4 则把 4 bit 的 16 个档位按正态分布排布——权重天然近似正态分布，中间密、两头稀，档位照这个密度摆，比均匀分布的 INT4 略好。双重量化再省约 0.4 bit。这套格式的巅峰之作是 QLoRA：NF4 冻住底座模型，上面挂一对 LoRA 适配器做微调——一张消费级显卡，就这样拿到了微调大模型的资格。

> **数字卡片 4|四条路线对照：同一笔交易的四份合同**
>
> | 路线 | 一句话原理 | 需要校准数据 | 强项 | 弱项 |
> |---|---|---|---|---|
> | GGUF k-quant | 超块、双重量化、混合精度 | 不需要 | 全平台通用，开箱即用 | GPU 服务吞吐一般 |
> | AWQ | 激活感知，保护 0.1%-1% 显著权重 | 少量 | 精度好，内核快 | 需离线校准 |
> | GPTQ | Hessian 二阶误差补偿 | 128 条左右 | 4bit 质量接近 FP16 | 量化慢，内核不如 AWQ |
> | bitsandbytes/NF4 | 离群通道保留 FP16，档位按正态分布 | 不需要 | 即插即用，QLoRA 标配 | 服务吞吐垫底 |

四条路线摆在一起，规律清晰可见：GGUF 是格式工程，不动模型，只改表示，不需要任何校准；AWQ 与 GPTQ 是误差最优化，用少量数据与数学，为每一次压缩买单；bitsandbytes 是端侧与微调场景的即插即用。它们不是竞争者，而是同一份条款在四种场景下的措辞：追求通用选 GGUF，追求服务吞吐选 AWQ，追求极限压缩选 GPTQ，追求微调选 NF4。

## 5.3 数字卡片：精度损失与显存节省的实价

这一节是全章的价目表。前面谈条款、谈路线，都是为了最后能读懂这三张卡片——每一行都是一个明码标价的档位。

第一张，质量价：压到不同位宽，模型还剩几成智商？

> **数字卡片 5|精度 → 质量保留（相对 FP16）**
>
> | 量化方案 | 典型质量保留 | 一句话判词 |
> |---|---|---|
> | INT8 / W8A8 | 99%-99.5% | 近无损 |
> | FP8 | 约 99.7% | 旗舰 GPU 的默认生产格式 |
> | GGUF Q8_0 | 约 99.7% | 本地世界的近无损 |
> | AWQ 4-bit | 98%-99% | 服务端 4bit 首选 |
> | GPTQ 4-bit | 97%-99% | 与 AWQ 缠斗的另一种签法 |
> | GGUF Q5_K_M | 98%-99% | 质量敏感任务的本地档 |
> | GGUF Q4_K_M | 96%-98% | 社区默认档 |
> | NF4 | 95%-97% | 为微调而生 |
> | 朴素 INT4 | 最差 | 不配算法的裸量化，不可用 |

读这张表的正确姿势，是先看压缩比再看保留率：从 FP16 到 INT4，显存压到四分之一，质量保住 96% 到 99%——这就是本章开头那笔交易的市场行情。98% 的保留率是什么体感？在常规基准上，它意味着一两个百分点的波动，多数任务肉眼难辨；但这份损失的分布并不均匀——常识问答几乎无感，多步数学与长链推理的末端偶尔掉链子。这也给评测量化模型立了一条铁律：单点问答测不出量化的真实损失，要让模型跑长链条任务，误差是沿着链路复利的。裸 INT4 那一行是给所有人的警示：没有 AWQ 的显著权重保护、没有 GPTQ 的 Hessian 补偿、没有 k-quant 的超块，4 bit 直接管不住场。位宽是标的，算法是中介，两者的报价要合在一起看。

第二张，显存价：参数量乘精度，就是权重的地皮。

> **数字卡片 6|参数量 × 精度 → 权重显存（理论值，GB）**
>
> | 精度 | 7B | 13B | 70B |
> |---|---|---|---|
> | FP32 | 28 | 52 | 280 |
> | FP16/BF16 | 14 | 26 | 140 |
> | INT8 | 7 | 13 | 70 |
> | INT4 | 3.5 | 6.5 | 35 |

公式仍然只有一行：显存约等于参数量乘以每参数字节。7B 从 FP16 的 14 GB 压到 INT4 的 3.5 GB，省 75%；70B 从 140 GB 压到 35 GB——原来两张 80 GB 的卡才装得下的模型，一张卡装下还有富余。这行数字解释了本地部署文化为什么围绕 7B 生长：INT4 之后，一个 7B 模型只值一部手机相册的几段视频。但务必记住这是权重的理论值：实际推理还要加上 KV Cache、激活值与运行时开销，工程上按 1.2 到 1.5 倍做预算——第 2 章的余量法则在这里原样生效，3.5 GB 的模型真实住下来要按 5 GB 报预算。

把显存翻译成钱，交易的分量更清楚。70B 的 FP16 要 140 GB，至少两张 80 GB 的卡；INT4 只要 35 GB，一张卡装下还剩一半厂房。省下的一张 H100，时租 2.85 到 3.50 美元，一年就是两万五到三万美元——这是量化在每一张推理卡上默默签出的租金差。服务商价目表敢一年降几次价，物理源头之一就在这张表里：地租省下来的钱，最终变成价格战里的子弹。

第三张，速度价：同样的显存，吞吐各有各的命。

> **数字卡片 7|Llama-3.1-70B 单张 H100 实测（batch=1）**
>
> | 格式 | 权重显存 | 实测吞吐 |
> |---|---|---|
> | FP16 | 约 140 GB，单卡放不下 | — |
> | FP8 | 约 70 GB | 约 52 tok/s |
> | AWQ 4-bit | 约 35 GB | 约 38 tok/s |
> | GPTQ 4-bit | 约 35 GB | 约 32 tok/s |
> | GGUF Q4_K_M | 约 38 GB | 约 28 tok/s |
> | NF4 | 约 35 GB | 约 24 tok/s |

同样是 4 bit，速度却排出了座次：AWQ 38，GPTQ 32，GGUF 28，NF4 垫底。位宽相同，差的是内核——AWQ 的推理内核干净利落，GPTQ 的内核优化不如它，GGUF 的超块结构为通用性而非极致吞吐设计，NF4 则压根是为微调而非服务而生。质量价与速度价要合读：AWQ 用 98% 到 99% 的质量保留，拿下 4 bit 阵营最快的实测吞吐，这是它在服务端流行的全部理由；GGUF 慢三分之一，却握着 CPU、Mac、消费卡的全平台通行证——在本地世界的账本上，通用性比 10 tok/s 值钱。

这张表里还藏着一个容易误读的事实：显存省 4 倍，速度并没有快 4 倍。FP8 的 52 tok/s 到 AWQ 的 38 tok/s，位宽再砍一半，吞吐只涨三成。原因在 5.4 节展开，这里先点破：weight-only 量化省的是权重搬运的带宽，省不了计算本身；decode 是访存密集的活，带宽红利真实存在，但反量化要耗算力，调度与采样也在分时。量化省得最狠的是账单上的地租，不是墙上的时钟。

这三张卡片在 API 市场上也有影子。同一家厂商的同一款模型，常常分出标准档与高速档、旗舰价与低价，两档之间动辄一倍以上的差价——低价档很多时候就是量化过的部署：FP8 或 INT8 服务把省下的地租让渡成低价。作为网关作者，我在配置多渠道时会把这一点计入决策：便宜的档位可能就是量化档，省的是钱，押的是精度——聊天无感，代码要慎。看懂了本章的三张卡片，再看各家价目表，那些奇怪的价差忽然都有了物理解释。

把三张卡片串起来读，还能算出每个 bit 的行情。从 FP8 的 8 bit 到 AWQ 的 4 bit，位宽砍半，质量从约 99.7% 落到 98% 到 99%——一个 bit 的价钱大约是半个百分点的智商；从 Q4_K_M 的 4.8 bit 再压到 Q2_K 的 3.4 bit，只省 1.4 bit，质量却从 96% 到 98% 一路跌进“明显损失”的区间。头几个 bit 最值钱，最后几个 bit 最便宜——压缩的痛苦不是线性的，4 bit 就是行情的分水岭，再往下每一档都贵得惊人。社区把 3 bit 以下标注为最后手段，翻译过来就是：这一段的智商，打折也无人接盘。

最后把任务的脾气也计入报价。同一份 4 bit 权重，做情感分类、文本摘要几乎无感；做数学推理与代码生成就可能露怯——推理链条越长，每一步的小误差越有机会被放大成结论的偏差。MoE 架构另有一套算法：鲁棒性由总参数量决定，总参 200B 以上的 MoE，即使每 token 激活的参数很少，也照样耐受 4 bit。三张卡片合起来，就是量化的全部实价：位宽价（显存省几倍）、质量价（保留几成）、速度价（快慢几何）。签约之前，三个价签都要翻过来看。

## 5.4 条款与例外：weight-only 不省计算

合同正文读完，现在读小字。量化的条款书里有一条最容易被略过的小字：weight-only 量化，不省计算。

把 70B 压成 4 bit，35 GB 权重安安静静躺在显存里——但矩阵乘法并不会在 4 bit 上进行。推理时，每个权重块被读进芯片，先反量化回 FP16，再老老实实地做 FP16 的乘加。省下的是从显存搬运权重的带宽，不是计算本身。decode 阶段是访存密集的活，权重搬运时间占大头，所以 4 bit 的 decode 确实快——数字卡片 7 里 AWQ 的 38 tok/s 就是带宽红利；但 prefill 阶段是计算密集，瓶颈在乘加本身，反量化反而是额外开销，收益接近于零。用户侧的体感对应物是首 token 延迟：weight-only 量化几乎救不了 TTFT，能救它的只有连计算一起提速的方案。这就是那条小字的完整含义：省带宽，不省 FLOPs。拿着 4 bit 的 decode 吞吐去宣称推理全面提速，是量化营销里最常见的偷换。

想连计算一起省，就得让权重与激活同时量化，也就是 W8A8：矩阵乘法直接跑在 INT8 上，吃 INT8 Tensor Core 的硬件红利，带宽与算力双赢。难点在激活这一侧：权重的分布是静态的，训练一结束就焊死；激活的分布却随输入漂移，还常带离群值，直接量化很容易翻车。SmoothQuant 的解法堪称精巧——把激活里难量化的那部分量尺，用数学变换迁移到权重那边去，让难缠的一方变乖，让好商量的一方多担。另一条路是 FP8：E4M3 与 E5M2 两种编码，H100、H200 原生支持，约 99.7% 的质量保留近乎无损，vLLM 已把它做成自动校准的默认格式——第 9 章讲引擎时我们再细看。

激活离群值值得单独讲一段，它是低精度路上最著名的拦路虎。大模型的激活分布里存在极少数特征通道，幅值是其余通道的成百上千倍，且位置相对固定——LLM.int8() 给它们保留 FP16，是承认这类通道惹不起；SmoothQuant 把量尺搬去权重那一侧，绕的也是它们。激活的脾气，决定了 W8A8 的难度上限：权重的分布是印在文件里的常量，激活的分布却是每次请求的变量——量化静态的东西容易，量化流动的东西难，这是我一再向同行复述的分工。

> **数字卡片 8|省显存与省计算：条款的两页**
>
> | 方案 | 显存节省（对 FP16） | 计算加速 | 质量保留 | 适用 |
> |---|---|---|---|---|
> | weight-only 4bit | 4 倍 | decode 快，prefill 不快 | 96%-99% | 容量优先 |
> | weight-only 8bit | 2 倍 | 有限 | 99% 以上 | 过渡方案 |
> | W8A8（SmoothQuant） | 2 倍 | 有，INT8 Tensor Core | 99%-99.5% | 吞吐优先 |
> | FP8 | 2 倍 | 有，硬件原生 | 约 99.7% | 旗舰默认 |

落到决策上，两页条款对应两种完全不同的动机。如果你的瓶颈是“装不下”——一张卡想多住几路并发、一台笔记本想跑 70B——签第一页，weight-only 足矣；如果你的瓶颈是“算不快”——prefill 太慢、首 token 迟迟不出、批量吞吐上不去——就得翻到第二页，W8A8 或 FP8，还得问一句硬件答不答应：INT8 Tensor Core 与 FP8 单元都是硬件能力，不是软件开关。这也是我选型时先问自己的问题：缺的是厂房，还是流水线？缺厂房，量化补得齐；缺流水线，量化只能补一半。

条款书还有一条例外条款，印在最不起眼的位置：模型越大越耐量化。30B 以上的模型压到 4 bit，损失通常小于 2%；7B 以下的小模型压 4 bit，损失可能达到 5% 到 10%，生产环境建议老实退到 5 到 6 bit。道理要回到 5.1 节的第二根支柱：冗余度。大模型的冗余是几十亿参数的集体保险，单个权重的失误有一整个议会的缓冲；小模型的冗余本就单薄，砍到 4 bit 等于拆承重墙。MoE 例外地遵循总量法则：总参数够大，激活再少也耐砍——鲁棒性认总参，不认激活。

> **数字卡片 9|越大越耐量化：规模与 4bit 的容忍度**
>
> | 模型规模 | 4bit 表现 | 签约建议 |
> |---|---|---|
> | 30B 以上 | 损失通常小于 2% | 放心签 |
> | 13B-30B | 损失约 2%-3% | 常规签 |
> | 7B 上下 | 损失可能 5%-10% | 慎签，或升到 5-6 bit |
> | 7B 以下 | 风险陡增 | 建议 FP16 或 Q5 起步 |

把两条小字并排读，会看到全书的第四条叙事再次显形：一切优化都是交易。量化的合同其实有两页——第一页省显存，weight-only 就能签，人人可签；第二页省计算，需要 W8A8 或 FP8 的硬件与算法配合，有门槛。看清自己签的是哪一页，才不至于拿着 decode 的加速去吹 prefill 的性能，也不至于把省下的地租误当成省下的时间。

把两条分水岭并排放，还有一层呼应。第 2 章说 70B 是显存的分水岭——FP16 装不进单卡；本章说 30B 是量化的舒适区下限——再小就伤质量。两条线一夹，生产级 4bit 的安全区就画出来了：30B 以上、INT4 上下的量化档，恰好是当代开源主力模型的聚集地。这不太像巧合：模型架构师知道量化会来，Infra 工程师知道冗余在哪儿，两条战线在 30B 到 70B 的区间里会师。顺带接上第 2 章埋的那条线：权重敢上 4 bit，KV Cache 从 8 bit 起步——同一门生意，在制品的仓位比机床的仓位金贵，因为 KV 直接参与注意力的打分与求和，精度损失会一路渗进生成质量。

还有一笔复合账值得预告：量化的 4 倍显存节省，与连续批处理的 2 到 4 倍吞吐、推测解码的约 2 倍，是可以相乘的——第 10 章我们会把这三笔乘出一个 16 倍的成本优化。单看每一项都是条款，合起来才是推理工厂的经济学。

## 5.5 我的实践：本地小模型的量化日常

以上都是工厂里的谈判。回到我的书桌：没有 80 GB 的 H100，只有一台 Mac 的统一内存和一两张消费级显卡——我的显存预算以个位数 GB 计。在这个尺度上，量化不是优化项，是生存项：不量化，本地压根不存在跑大模型这个选项。

我的日常主力是 GGUF 的 Q4_K_M。理由朴素到近乎无聊：Ollama 或 LM Studio 一条命令拉下来就能跑，CPU、Mac、显卡通吃；一个 8B 模型压完约 4.8 GB，一台普通笔记本的内存就装得下。再套上第 2 章的余量法则——权重之外留 1.2 到 1.5 倍给 KV Cache 与运行时——16 GB 内存的机器跑 8B Q4_K_M 加 8K 上下文，绰绰有余。这份从容是 INT4 买来的：没有量化，个人开发者与本地大模型之间，隔着一道跨不过去的显存鸿沟。

这位常驻居民，我做过一次横向对比：同一个 8B 模型，Q4_K_M 权重 4.8 GB，Q8_0 要 8.5 GB——多出来的 3.7 GB 在基准测试里只换来小数点后的质量，在一台 16 GB 内存的机器上却意味着要不要关掉浏览器再开终端。这就是个人尺度与机房尺度的估值差：机房里一个百分点是大事，书桌上 3.7 GB 也是大事。量化档位的选择从来不是质量最大化，是约束条件下的体感最大化。

> **数字卡片 10|选型决策表：什么场景签什么合同**
>
> | 场景 | 推荐方案 |
> |---|---|
> | H100/H200 生产服务 | FP8（vLLM/TensorRT-LLM），近无损 |
> | A100 / L40S 生产 | AWQ 4-bit（vLLM） |
> | RTX 4090 等消费级显卡 | AWQ 4-bit 或 GGUF Q4_K_M |
> | Mac（Apple Silicon） | MLX 4-bit 或 GGUF Q4_K_M |
> | 纯 CPU 部署 | GGUF Q4_K_M / Q5_K_M |
> | 跨 CPU、GPU、Mac 混合环境 | GGUF，唯一的全平台通用格式 |
> | 本地免折腾 | Ollama / LM Studio 直接拉 GGUF |
> | 7B 以下且质量敏感 | 退到 5-6 bit（如 Q5_K_M） |

个人经验法则是三级试水：先 Q4_K_M，不满意升 Q5_K_M——98% 到 99% 的保留率，只贵一个档位；再不满意上 Q8_0，约 99.7%，基本无损。反向的降级要谨慎得多：Q3_K_M 已有明显损失，Q2_K 是最后手段——3.4 bit 的世界里，模型开始一本正经地胡说。任务画像决定容忍度：聊天、总结、翻译，Q4_K_M 足够；代码与数学，我宁可升一档位宽——推理链条长的活，误差放大的机会也多。

降智不降智，我有一套土办法自检：留三道只有满血模型能稳定答对的题——一道多步算术、一道细节回忆、一道代码填空——换一个量化档位就重跑一遍。Q4_K_M 全对就安心用，错了先升 Q5_K_M 再试，还不行就老老实实回 Q8_0。土办法不严谨，但比任何跑分都贴近我的真实用法：评测跑的是别人的任务分布，自检跑的是我自己的。

日常的工作流大概是这样：新模型发布，先看社区有没有放出 GGUF 的 Q4_K_M——本地世界的搬运速度常常快过官方；拉下来先跑那三道自检题，过了就让它顶替旧模型上岗；磁盘吃紧时，Q8_0 的备份第一个删——反正随时能重新下载，权重文件是这个时代最不需要珍惜的资产。也有例外：写长代码会话时我会临时切到 Q5_K_M，4 bit 在上千行的上下文里偶有体力不支；纯翻译或闲聊则反方向降档，Q4_K_S 也够——S 与 M 的差别，在个人尺度上常常只是下载时间的差别。

有一节课我印象极深，关于文件名的欺骗性。k-quant 的超块要求张量的列数能被 256 整除，不满足时 llama.cpp 会静默回退到老式格式。某些小模型的隐藏维度不是 256 的倍数，于是那个名叫 Q4_K_M 的文件里，真正的 Q4_K 张量只占一小部分，其余被回退的格式顶替——实测有个 0.5B 的小模型，文件标着 Q4_K_M，实际有效位宽是 6.16 bit，真正的 Q4_K 只占 6%。体积对不上文件名，质量倒因此意外受益，但这事儿本身是个提醒：文件名是广告，字节数才是合同。从那以后我养成了习惯，下完量化模型先看文件体积、反推实际位宽，再决定信不信它的标签。

个人与生产的答案常常相反，这是本章最后想说的对照。生产环境的排序是质量、吞吐、成本，量化是精打细算过的权衡：旗舰卡 FP8，老卡 AWQ，一步到位，绝不含糊。个人环境的排序是住得下、下载快、不折腾，Q4_K_M 的够用，恰恰是最优解。同一位租客，工厂算的是每 GB 显存的产出，我算的是这台机器今晚还能不能跑起来——预算不同，合同自然不同。这也解释了为什么量化生态会有两张面孔：服务端的 AWQ 与 FP8 越做越精密，本地端的 GGUF 越做越通用，两边都是理性的，只是约束条件不同。

把个人的签法收拢成两条原则。其一，显存是硬约束，质量是软约束：先量出这台机器的可用内存与目标上下文的 KV 开销，反推住得下的最高位宽——约束没算清之前谈质量，是本末倒置。其二，别在红线上签 4 bit：给 KV Cache 与系统留足 1.2 到 1.5 倍余量，否则上下文一长、显存一紧，崩掉的不是分数是体验。这两条原则与机房里的容量规划完全同构——个人开发者真正该从 Infra 工程师那里借走的，从来不是工具，是先算账、后签约的顺序。

这一章从一行仿射量化公式出发，穿过四条技术路线，停在三张价目表上。全书的交易观在这里最赤裸：量化用精度换显存，显存省 4 倍而质量保住 98%——但定价权不在我们手里，在模型自身的冗余度手里；条款书上还印着两行小字：weight-only 不省计算，越大越耐量化。把 dtype_bytes 从 2 打到 0.5，是推理工厂里唯一能在模型出厂之后重写账本的手段，也是让显存利用率逼近 100% 的第一张底牌。第 6 章我们去看账本的另一半：权重压完之后，为什么第一个字还是慢、后面的字才快——prefill 与 decode，两段性情迥异的调度，正等着登场。

---

## 第6章 Prefill 与 Decode:两段式调度的秘密

# 第 6 章 Prefill 与 Decode：两段式调度的秘密

深夜十二点，我在自己网关的日志里看到一对耐人寻味的数字：同一次对话，第一个 token 用了 1.8 秒才抵达屏幕，此后每个 token 只要 40 毫秒上下——第一个字与第二个字之间，隔着四十五倍的等待。我最初的嫌疑人是网络，抓包看了链路；第二个嫌疑人是排队，特意挑了凌晨错峰重试。嫌疑人都排除了，差距纹丝不动。直到把推理引擎的执行轨迹摊开，我才看清这不是哪一处的故障，而是推理这件事与生俱来的两副面孔：第一个 token 之前，GPU 在做一份叫 prefill 的重活；第一个 token 之后，它切到一种叫 decode 的轻活。两份活共用一张卡，性情却南辕北辙——一份把 GPU 利用率顶到约 90%，一份只能做到 25%-30%（materials/raw_cost.md）。本章就把这对性格拆开看：先给两阶段画像，再算清带宽这道天花板，然后看它们同在一间厂房里的互相拖累，接着参观 2025 年以来整个推理行业最响亮的工程答案——PD 分离，最后回到我的网关，谈谈首字延迟的用户心理学。

## 6.1 两个阶段，两种性格

看懂这对性格，先要看清引擎眼里一次请求的一生。请求进门，prompt 三千 token。引擎做的第一件事叫 prefill，预填充：把整段 prompt 一次性并行送进网络，跑一遍完整的前向传播，为每个 token 算出每一层的 K 与 V，写进 KV Cache。请注意“并行”二字——第 1 章讲过，因果掩码只规定每个 token 只能看见自己之前的历史，并不强迫它们排队，这正是训练时整个序列可以一起前向的同一性质。于是 prefill 的本体是一次超大矩阵乘法：矩阵的行数就是 prompt 长度，三千行的大 GEMM 铺满整张卡，是 GPU 最擅长的那种活。这一遍跑完，第一个 token 出炉；请求随即转入 decode，解码：此后每一步只生成一个 token——把新 token 送进网络，读一遍全部权重，读一遍全部历史 KV，吐出下一个字——如此往复，直到终止符。

一个“一次算三千行”，一个“每次算一行”，两份活的性格在这里彻底分岔。prefill 的行数是三千，权重字节被上千次复用，算术强度高到算力吃紧、带宽反而清闲——这是计算密集（compute-bound）的活，CUDA 核心几乎每个节拍都有饭吃，利用率能顶到约 90%（materials/raw_cost.md）。decode 的行数是一：每序列每步只贡献一行计算，却要把 140GB 权重从头到尾读一遍，算术强度低到算力闲得发慌、带宽挤破头——这是访存密集（memory-bound）的活，利用率只剩 25%-30%，IEEE 综述的口径甚至低到 15%-35%（materials/raw_cost.md）。用工厂的话说：prefill 是整批原料一次入炉的高炉，decode 是一件一件出货的流水线——前者怕料不够，后者怕料运不进来。

在 prefill 与 decode 这对名字流行之前，工程师确实长期把它们当成一件事——反正都是前向传播，跑就是了。把前向劈成两半来对待，是模型服务化之后才长出来的意识：训练场里只有一种前向，一批数据齐步走，考核的是“这批数据多久算完”；车间里，同一份前向被时间劈成两段，考核的是“第一个字多久出来、后面的字多匀”。模型没有变，变的是问法，而问法一变，两个阶段就必须分开考核、分开调优、分开排班——承认这一点，是所有推理优化的起点。

两段性格差还直接写进了体感的两根时间轴。prefill 的计算量随输入长度线性增长：70B 模型吃进三千 token 的 prompt，一次前向约 420 万亿次浮点运算，按九成利用率粗算约半秒——这就是 TTFT 的物理底价，再叠加排队、调度与网络往返，才是你在屏幕上等到的那个数字。所以长上下文请求的“第一口”格外慢：不是服务商偏心，是 prefill 的长度在给首字定价。decode 的账则完全是另一副面孔：它的每步计算量小到几乎可以忽略，时间却长得反常——这笔反常的账，下一节单独算。

性格不同，考核指标也各发一张。prefill 的成绩单叫首 token 延迟（TTFT，Time To First Token）：从请求发出到第一个字抵达的时长，用户感知里的“反应快不快”。decode 的成绩单叫逐 token 延迟（ITL，Inter-Token Latency），另一个常见口径叫 TPOT：相邻两个 token 之间的间隔，用户感知里的“打字快不快”。TTFT 由 prompt 长度、队伍长度与 prefill 速度共同定价，ITL 由显存带宽与批次拥挤程度共同定价。两张成绩单，两套及格线，两套调优手段——请记住这句话，6.3 节的全部矛盾都从这句话里长出来。

把镜头再切回调度器：一个请求在它眼里，其实是两张先后提交的工单。第一张工单叫 prefill，重、短、并行度好开；第二张工单叫 decode，轻、长、节奏琐碎。一张三千 token 的 prompt 配一千 token 的回答，第一张工单半秒交货，第二张工单要占用流水线几十秒——工时比例通常在一比几十的量级。工厂里有句老话说的就是这层意思：prefill 是进门的一次安检，decode 才是漫长的在制品加工。第 4 章的连续批处理之所以能成立，是因为 PagedAttention 让两拨工单可以共享同一片显存车间；车间的共享成就了排班的高效率，也把两种性格锁进了同一个屋檐——这层因果，要到 6.3 节才收账。

> **数字卡片 1|Prefill 与 Decode 对照表**
>
> | 维度 | Prefill（预填充） | Decode（解码） |
> |---|---|---|
> | 做什么 | 一次前向并行算完整段 prompt 的 KV | 逐 token 自回归生成，复用 KV |
> | 计算特性 | 计算密集（compute-bound），大 GEMM | 访存密集（memory-bound），反复读权重与 KV |
> | GPU 利用率 | 约 90% | 25%-30%（低至 15%-35%） |
> | 决定的指标 | 首 token 延迟（TTFT） | 逐 token 延迟（ITL） |
> | 瓶颈 | 算力（FLOPs） | 显存带宽（HBM） |
> | 计算量形状 | 随输入长度线性增长，一次算完 | 每步很小，重复成百上千次 |

这张表值得盯上三十秒，因为 API 定价表里那行著名的差价就写在里面：输出 token 比输入 token 贵 3-5 倍，官方理由从来只有一句“输出需顺序生成、无法并行”（materials/raw_cost.md）。翻译成本章的语言：你付的输入价钱买的是 prefill——一段能并行的计算密集活，便宜；你付的输出价钱买的是 decode——成千上万次串行的带宽苦役，贵。同一块显卡，对两种活各有一套产能天花板，定价表诚实地把两套天花板都标了价。而更麻烦的问题藏在表格外：这两种性格相反的活，偏偏喜欢住在同一张卡上——第 4 章的连续批处理把它们混进同一个批次，省下了排班的空转，却种下了新矛盾的种子。要理解那颗种子，得先把 decode 的天花板一笔一笔算清楚。

## 6.2 带宽是 decode 的天花板

要量化“访存密集”这四个字，需要一个能上秤的指标，它叫算术强度：一份计算每搬运 1 字节显存数据，能换来多少次浮点运算。GPU 的性能有一条简明的天花板模型：一头是算力上限，一头是带宽上限，一份计算的实际速度由先撞墙的那一头决定。两条上限画在坐标上，交叉点有个名字，鞍点：算术强度低于鞍点的活，加多少算力都白搭，加速器在等数据；高于鞍点的活，数据在等加速器。以 H100 为例，FP16 算力约 989 TFLOPS，HBM 带宽约 3.35TB/s，两者相除，鞍点约等于 300 次运算对 1 字节（materials/raw_cost.md；数字为规格粗算）。记住这个刻度，decode 的宿命就一目了然了。

先算 decode 最寂寞的形态，批为 1 的单流生成。70B 模型 FP16 权重 140GB，每生成一个 token，都要把这 140GB 从显存完整读进计算核心一遍——第 2 章的显存公式算的是“住”，这里算的是“读”。按 3.35TB/s 的理想带宽，读一遍至少 42 毫秒，这就是单流速度的理论上限：约每秒 24 个 token。KV Cache 的附加读取呢？8K 上下文的 70B 序列每 token 另读约 2.5GB，合 0.75 毫秒——不是零，但与权重的 42 毫秒相比只是零头；上下文短时更是九牛一毛。再算上框架调度与通信的杂费，单流 70B 常年停在每秒十几到二十几个 token——不是引擎偷懒，是物理定律在发牌。算术强度呢？140 GFLOPs 的计算量对 140GB 的搬运量，强度约等于 1，离鞍点 300 差着两个数量级：上千个 CUDA 核心里，绝大多数节拍都在等数据。

把两个阶段的总账并排放，反差更刺眼。同是 70B：prefill 吃三千 token 的 prompt，计算量约 420 万亿次浮点运算，半秒交货；decode 吐一千 token 的回答，计算量只有约 140 万亿次——约为 prefill 的三分之一，却要占用流水线四十多秒。半秒干了三倍的活，四十秒干了更少的活，时间与计算量在这里公然倒挂。倒挂的原因本章已经给出：decode 的每一个节拍都在为搬运字节付过路费，计算本身几乎免费。理解了这次倒挂，就理解了推理优化与训练优化的分野——训练优化拼的是把 FLOPs 干快，推理优化的大半功夫，是把字节搬少。

第 5 章那张单卡实测表，现在可以在另一盏灯下重读：Llama-3.1-70B 在单张 H100 上，FP8 跑出 52 tok/s——FP8 权重 70GB，读一遍理论下限 21 毫秒，合每秒 48 个 token，实测几乎贴着带宽下限；AWQ 38、GPTQ 32、Q4_K_M 28，则被反量化的内核开销往下拽。这张表当年用来比量化路线的速度，此刻它证明了另一件事：decode 的速度天花板，就是“权重字节数除以带宽”这一道除法。这也顺手解释了第 5 章那条条款为什么在 decode 里格外值钱——weight-only 量化只省带宽不省计算，而 decode 的瓶颈恰恰不是计算是带宽：把每个参数从 2 字节压到 1 字节，这道除法的被除数直接减半，天花板应声翻倍。量化在 prefill 里省的是显存与功耗，在 decode 里省的却是速度本身。

那么批处理呢？第 4 章讲过，连续批处理的吞吐红利有一半来自这里：权重的一次搬运由全批分摊，批内 64 路序列共享同一遍 140GB 的读取，算术强度从 1 涨到约 64——离 300 还是差得远，但利用率已经从个位数爬到 25%-30%。为什么不再往上开批？因为每涨一路批，就要多驻一份 KV：70B 满 8K 每路 2.5GB，三百路就是 750GB，而一张 80GB 的卡连它的零头都装不下。要喂饱算力需要三百路，显存只允许几十路——decode 的利用率天花板，是带宽与容量的合谋：带宽刚点头，容量先摇头。这是本书两条主线在 decode 里的会师：第 2 章的显存几何学与本章的带宽几何学，在“利用率”这个数字上共同落款。

这份苦役还会随上下文加重。权重的读取是固定税，KV 的读取是累进税：8K 上下文每 token 附加约 0.75 毫秒，还是零头；128K 上下文每 token 要另读约 41GB，12 毫秒往上——第 2 章那张“128K 单请求 40GB”的显存账，在带宽账本上原样重演了一遍。长上下文请求的逐 token 节奏因此天然更慢，这是“长文本 API 更贵”的又一层物理依据。更麻烦的是，带宽这张天花板出厂即定型：算力还能靠张量核心、稀疏化在规格表上翻番，HBM 带宽的每一步增长却要靠堆叠工艺硬啃，贵且慢——天花板焊死在芯片出厂的那一天，留给软件的只有绕行。

还有一条更冷的趋势线：每代 GPU 的算力都涨得比带宽快。A100 的鞍点约 150，H100 约 300，代际翻倍——算力越便宜，decode 就越“访存密集”，这是硬件演进亲手写下的结构判决。所以业界的解法清单其实只有一个主题，给带宽减负：推测解码用一次权重搬运换回多个 token，MLA 与 KV 量化给 KV 瘦身，前缀缓存干脆让一部分计算不必再读显存——第 8 章的主角已经在幕后候场。带宽是 decode 的天花板，而 Infra 的大部分聪明才智，都在学习如何不去硬碰这面墙。

> **数字卡片 2|一道除法定生死：decode 的带宽账（H100，规格粗算）**
>
> ```
> 鞍点：约 989 TFLOPS ÷ 3.35TB/s ≈ 300 FLOPs/字节
> 70B FP16 单流：140GB 权重 ÷ 3.35TB/s ≈ 42ms → 上限约 24 tok/s
> 8K 上下文 KV 附加：每 token 另读约 2.5GB，约 +0.75ms
> 批 64 路：强度约 64，利用率爬到 25%-30%；喂饱算力需约 300 路
> 300 路 × 2.5GB KV = 750GB → 容量先摇头，带宽与容量合谋封顶
> 鞍点逐代上移：A100 约 150 → H100 约 300，decode 结构性更“带宽化”
> ```

## 6.3 混部的互相拖累

看完了两种性格，再看它们不得不同住一个屋檐下的日常。生产引擎的默认形态叫混部（colocation）：一张卡既跑 prefill 又跑 decode，第 4 章的连续批处理把 prefill 段与 decode 序列混进同一个批次，一张卡一张排班表。混部有充分的理由——排班满、空转少、部署简单——但它有一个无法通过调参消除的先天矛盾：两种活抢的是同一份显存带宽、同一组 CUDA 核心、同一块二级缓存，而它们对这三样东西的使用方式恰好相反。

拖累是双向的。正向看，prefill 的大 GEMM 一旦开跑，会长时间占据计算核心与缓存，同批 decode 序列每一步的小读取只能排队——队列里的等待直接写进每个用户的 ITL。反向看，decode 的频繁小读取会不断打断 prefill 的连续流水：大矩阵乘法最怕心流被切碎，缓存里的权重刚焐热就被 decode 的小请求冲掉，命中率塌方，prefill 自己也慢下来。系统研究的测量把这种干扰量化到令人咋舌：一个大型 prefill 进入混部批次，可以让同批请求的 TPOT 膨胀 2-30 倍（materials/ch06_补充.md）。上限的 30 倍固然是最坏情形，但这组数字的要害不在具体倍数，而在量级——干扰不是百分之几的摩擦，是换了一个数量级的降速。

摩擦之外还有一层更日常的两难：资源配置。prefill 想要的是又大又宽的 GEMM，decode 想要的是高频细碎的小步快跑；批开小了 prefill 喂不饱算力，批开大了 decode 的节拍被拉长。一个引擎只有一套批策略、一套并行度、一套内核配置，却要伺候两位口味相反的主子。像家里只有一个灶眼的厨房，同时接下炖汤与爆炒两张订单：炖汤要文火慢煨占灶一小时，爆炒要武火三分钟出菜，无论先炖后炒还是先炒后炖，总有一张订单的火候受损。chunked prefill 相当于把炖汤拆成二十次加料，每次给灶眼腾出三分钟——口感均匀了，两道菜依然出自同一个灶眼。

实测数字更有画面感。AMD 在单节点 MI300X 上跑 Qwen3-235B 的公开实验里，prefill 请求一进入共享引擎，decode 的 ITL 立刻出现 150 毫秒量级的尖峰，而服务目标是 50 毫秒——超了三倍（materials/raw_cost.md）。翻译成体感：一个正在流式输出的用户，屏幕上的打字机毫无预兆地冻住三分之一秒，再继续奔涌。他不知道发生了什么，只知道“这服务有点卡”；实际上那一刻，厂房门口正有一节超长车厢进门，全车人都为它停了一站。

第 4 章的 chunked prefill 是这片战场上的第一味药：把超长 prompt 切成 2048 token 的段，与 decode 交错执行，150 毫秒的整块尖峰被锯成一串小颠簸，队头阻塞的烈度大幅下降（materials/raw_vllm.md）。但止痛药不是根治术：切段改变不了两种活仍在抢同一份带宽的事实，它只是把一次大碰撞摊薄成多次小摩擦。要根治，就得承认一个朴素的结论——性格相反的两种活，不该挤在同一张卡上。

平心而论，混部不是错误的权宜，它是上一个时代的正解。2023 年前后的负载以短 prompt、短回答为主，prefill 与 decode 的工时都不长，两种性格的碰撞窗口小，一张卡从头干到尾的效率优势压过一切；是长上下文把矛盾放大到无法再视而不见——三万 token 的 prompt 进门，prefill 的独占时间以百毫秒计，混部的每一笔隐性账都开始显形。矛盾不是被谁发明的，是被负载长出来的；后来的解法也一样。

混部还有一笔更隐蔽的账，藏在容量规划里。混部之下，调度器必须同时满足 TTFT 与 ITL 两套最坏情况：decode 用户随时可能撞上一个巨型 prefill，prefill 用户随时可能被一片 decode 淹没。于是容量只能按两者之和管理，显卡为“最坏的同时发生”买单，平时的利用率反而更低。更要命的是尾部：大型 prefill 的到达时刻近乎随机，P99 延迟不再由平均负载决定，而由最倒霉的那次碰撞决定——容量按平均值规划，投诉按尾部发生，这笔错位是混部系统的日常。2024 年的 DistServe 论文给这笔糊涂账立了新度量，goodput，有效吞吐：每张卡每秒产出的“满足 SLO 的请求”才算数，超时的产出不计入成绩（materials/ch06_补充.md）。按这个口径，混部系统的账面吞吐里有相当一部分是不达标的废品——业界 5%-10% 的平均利用率（materials/raw_cost.md），混部要负相当大的责任。

> **数字卡片 3|同一张卡上的摩擦账**
>
> ```
> 干扰量级：一个大型 prefill 可让同批 TPOT 膨胀 2-30 倍（materials/ch06_补充.md）
> 实测尖峰：ITL 150 毫秒量级，对照 50 毫秒 SLO，超三倍（MI300X，Qwen3-235B）
> chunked prefill：2048 切块，把整块尖峰锯成小颠簸，止痛不根治
> 容量过配：混部必须同时满足 TTFT 与 ITL 最坏情况，按两者之和管理
> goodput 口径：不满足 SLO 的产出不计成绩，混部的废品率由此现形
> ```

用总纲的语法收拢本节：批处理是一笔延迟换吞吐的交易，而混部的问题在于它把这笔交易签成了糊涂账——尾部延迟是尾部游戏，不是平均游戏，平均速度再好看，一次 150 毫秒的尖峰就足以让用户判定“卡”。既然病根是两种性格同住，药方也就顺着病根长出来：让它们各开各的工厂。这正是过去两年推理行业动作最大的重构——PD 分离。

## 6.4 PD 分离：各开各的工厂

药方说来只有一句话：把 prefill 与 decode 拆到不同的 GPU 池，各开各的工厂。prefill 池不养 KV 长租客，专接重型进料，按吞吐调优；decode 池不再被大 GEMM 打断，专心逐 token 出货，按延迟调优。两个池子独立扩缩容——白天的流量 prefill 重，就多开 prefill；深夜长对话扎堆，就多开 decode。更根本的是，两套 SLO 从此各管各的：TTFT 只看 prefill 池的脸色，ITL 只看 decode 池的节奏，上一节那笔按“最坏同时发生”过配的糊涂账，有了一笔勾销的可能。

这个思路在学术谱系里成型于 2024 年。UCSD 的 Splitwise 论文登上 ISCA 2024，率先把“按阶段拆分、异构硬件”做成系统，测得约 1.4 倍吞吐与两成成本节省；OSDI 2024 的 DistServe（北大与 UCSD 团队）把故事讲得更完整——独立伸缩、TTFT 与 TPOT 各自达标、KV 跨实例传输与拉取式调度一并给出工程答案，goodput 这个度量也出自它；同年的 TetriInfer 则研究了分离之后同池内部的干扰。有意思的是这场思想的命运：2024 年，开源社区对它颇为冷淡，理由很实在——架构重构工程量太大；到 2025 年，它却几乎成了一切生产级推理栈的默认架构。DistServe 团队 2025 年 11 月发布复盘《Disaggregated Inference: 18 Months Later》，专门记录了这场十八个月的态度反转（materials/ch06_补充.md）——Infra 界的范式转移，有时不需要新论文，只需要某些玩家的账单大到不得不算。

效果数字先摆出来，再谈口径。Anyscale 报过 23 倍的吞吐提升；IEEE 的综述口径是最高 6 倍吞吐、15%-40% 的成本节省；AMD 与 vLLM 团队在单节点 8 张 MI300X 上，用 RDMA 传输 KV 实现 2.5 倍的 goodput 提升，同时把 ITL 抖动基本抹平（materials/raw_cost.md）。三组数字的负载、硬件、基线各不相同，不必互相换算——就像第 4 章对待 Orca 的 36.9 倍那样——但方向完全一致：把两种性格分开安置，两边的利用率就都上得去，全场有效产能成倍增长。

分离还打开了一扇混部时代打不开的门：异构与独立伸缩。两个池子性格相反，就没必要用同一种卡——prefill 池配算力强悍的卡，decode 池配带宽充裕的卡，各买各的长处，这正是 Splitwise 最初的立意，后来硬件厂商把这笔账算到了极致，专为 prefill 设计的 GPU 都上了货架，此是后话。独立伸缩则是另一重红利：白天企业流量 prompt 短回答长，就多开 decode；夜间批处理 prompt 长回答短，就多开 prefill——两个池子各自追着自己的负载曲线走，波峰错配的浪费远小于混部按最坏情况过配的老算法。混部像一间什么菜都得做的厨房，分离是把工厂拆成炒货间与炖品间，各自按订单节奏排班。

工程上真正的硬骨头只有一个：KV 交接。prefill 在 A 池算完的 KV，必须搬到 decode 池，B 池才能接着逐 token 生成——搬的不是抽象的“状态”，是实打实的字节：70B 序列满 8K 上下文，KV 约 2.5GB；32K 上下文约 10GB。走普通的主机内存中转，PCIe 与内核协议栈的延迟足以把分离的收益吃掉一半，所以答案是 RDMA——远程直接内存访问，绕开 CPU 与协议栈，网卡直接写对方的显存：vLLM 的 MORI-IO 连接器、NVIDIA 的 NIXL 传输库、Mooncake 的传输引擎，都是这一层的产品化（materials/raw_cost.md；materials/ch06_补充.md）。2.5GB 在一条 400Gb 的 InfiniBand 上约 50 毫秒——这就是分离的“过桥费”，记在 TTFT 头上。

再看一眼 2025-2026 年的生产现场，分离已经从论文落地成日常。DeepSeek 从 V3 起就用 3 个 prefill 节点配 9 个 decode 节点的阵型，连专家并行的宽度都按两阶段各自最优来配——decode 侧把并行域开到极致，专门伺候小计算大搬运；SGLang 在 2025 年 5 月复现 DeepSeek-R1 的 PD 分离部署，96 张 H100 按 24 个 prefill 配 72 个 decode 布阵，单节点输入 52.3k token/秒、输出 22.3k token/秒；Mooncake（Moonshot AI 与清华的合作，FAST 2025 最佳论文）把 KVCache 做成全集群调度的中心，连闲置的 CPU 内存、SSD 与网卡都被编进分布式 KV 池，Kimi 因此多接待 75%-115% 的请求，2025 年 7 月的 Kimi K2 更是在 128 张 H200 上跑出 prefill 每秒 224k token、decode 每秒 288k token 的产能（materials/ch06_补充.md）。NVIDIA 的 Dynamo 把 P/D 分离做成数据中心级的开源框架，2026 年底的 Rubin CPX 更进一步——业界第一个专为 prefill 设计的 GPU 家族，与主卡同机架搭档，“一张管进料、一张管出货”从软件架构写进了硬件版图。分离不再是一种可选项，而是工厂的默认格局。

生态层的跟进同样密集。编排一侧，NVIDIA 的 Dynamo 把 P/D 分离做成数据中心级开源框架，KV 感知路由与统一传输库 NIXL 都在其中；vLLM 原生支持多 prefill 配多 decode 的 xPyD 部署，Mooncake 的分布式存储在 2025 年 12 月直接集成进 vLLM V1 与 TensorRT-LLM 的连接器体系；云原生阵营的 llm-d 把分离推理纳入 Kubernetes 的运维模型（materials/ch06_补充.md）。分离的边界还在外推：维度上，从两段走向多段，SGLang 在 2025 年底推出编码、prefill、decode 三段独立的 EPD 分离来伺候多模态，MIT 与 DeepSeek 的研究者则把注意力层与前馈层也拆到不同节点，MegaScale-Infer 已在生产大规模部署——两段式之后会不会是 N 段式，已无人设限；空间上，从单机房走向跨机房，新一代混合注意力架构把 KV 体积缩小十几到三十几倍，KV 迁移第一次有望走普通以太网，Moonshot 与清华 2026 年提出的 Prefill-as-a-Service 干脆把长上下文的 prefill 放去异地的低价算力池（materials/ch06_补充.md）。2024 年那篇被冷落的论文，两年后长成了一整片森林。

> **数字卡片 4|PD 分离的价签（2024-2026）**
>
> ```
> Anyscale：吞吐提升 23×（其压测口径）
> IEEE 综述：最高 6× 吞吐 + 15%-40% 成本节省
> AMD/vLLM 单节点 8×MI300X：RDMA 传 KV，2.5× goodput，ITL 抖动抹平
> KV 交接：70B 8K 序列约 2.5GB，400Gb 网络约 50ms
> DeepSeek-V3：3 个 prefill 节点 + 9 个 decode 节点
> SGLang 复现 DeepSeek-R1：96 张 H100，24P + 72D，52.3k / 22.3k tok/s
> Mooncake：Kimi 多接待 75%-115% 请求；K2 在 128×H200 上 224k / 288k tok/s
> ```

代价也要照实标价。分离意味着显卡冗余——prefill 池与 decode 池各自要有余量，波峰错配时总有一侧闲着；KV 交接要占用昂贵的网络带宽；TTFT 因为多了排队与搬运，比混部略增。所以总纲第四条在这里照旧生效：PD 分离是一笔“硬件冗余换 SLO”的交易，没有免费的加速。也正因为过桥费记在 TTFT 头上，素材里那句结论才值得抄在墙上有意提醒自己——聊天这类 TTFT 敏感的场景，至今仍可能倾向混部（materials/raw_cost.md）；而对长上下文、重吞吐、ITL 苛刻的负载，分离几乎是唯一正解。选哪边，取决于你的用户更恨“等第一个字”，还是更恨“字与字之间的顿挫”。

## 6.5 个人尺度：首字延迟的用户心理学

两段式调度的故事讲到工程为止，但它的尽头站着用户，而用户的心理，恰好在两个阶段的边界上分野。人机交互领域有一条老的三分律：0.1 秒内，人觉得“即时”；1 秒左右，思路的连续感开始断裂；10 秒上下，注意力离场。放到推理工厂的语言里：TTFT 决定用户落在哪一档，ITL 决定用户在档内舒不舒服。第一个字到来之前，用户处于盲等——屏幕空白，他不知道系统是死了还是在干活，每一秒都在消耗信任；第一个字出现之后，等待变成了陪伴——流式输出把一次漫长的生成切成一串可感知的进度，用户的耐心被 token 的节奏续着。这就是为什么流式输出不是锦上添花，而是必须：它把 TTFT 的痛感提前止损，把 decode 的漫长翻译成“正在努力”的现场感。

体感研究里还有一个更刁钻的结论：冻结比慢更伤。平均 ITL 60 毫秒但偶发 300 毫秒尖峰的服务，用户评价会差于稳定 80 毫秒的服务——人对“节奏被打破”远比对“节奏偏慢”敏感。6.3 节那个 150 毫秒尖峰的实测，落到的正是这条心理曲线的最痛处；chunked prefill 与 PD 分离消灭的与其说是延迟，不如说是不可预期的顿挫。可预期性，是延迟工程里最被低估的指标。

产品端于是长出一整套给 TTFT 止痛的手艺。占位文案与骨架屏是第一层：在第一个字抵达之前，先把“已收到、正在处理”演给用户看，把盲等变成有反馈的等待；渐进披露是第二层：先给一句摘要再展开全文，让 TTFT 的定义从“全文第一个字”提前到“第一份可用信息”；预连接与预取是第三层：把网络握手、鉴权、模板渲染这些前置工作提前做完——TTFT 的构成里有一截根本不在 GPU 里，网关能帮用户省下的第一段延迟，往往在 prefill 开始之前。这些手法与工程侧的选择并不冲突：PD 分离与 chunked prefill 降的是 TTFT 的物理底价，产品设计降的是同一笔延迟的心理售价，两边都值得做，也都在为“第一个字”这座独木桥疏流。

我在自己的网关里，把这套心理学做成了监控面板。poeapi_go 的 internal/metrics 把 TTFT 与 ITL 分开记录，internal/trace 负责把每一次慢归因到具体渠道——两年日志看下来，渠道在延迟上呈现出两种截然不同的“人设”：有的渠道首字偏慢但节奏极稳，像深呼吸之后开口的演讲者，适合长文生成；有的渠道首字飞快、字与字之间却时有小顿挫，像语速飞快偶被噎住的聊天者，适合一问一答。起初我以为这是玄学，后来对上了本章的物理：前者多半 prefill 与排队偏重，decode 侧却从容；后者多半混部架构里 prefill 段不时插队，ITL 偶发颠簸。TTFT 与 ITL 是两个独立的性格维度，跑分把它们捏成一个“快”字，是用户视角最大的失真。

于是给自己立了三条个人版 SLO：TTFT 两秒以内——超过两秒我宁可让网关先回一条占位提示，也别让用户对着一屏空白猜；ITL 无 200 毫秒以上尖峰——均速可以慢一点，节奏不能破；失败要三秒内明说——与其让用户盲等三十秒后收到 504，不如三秒内熔断换渠道，失败得快也是体验。这三条线写进 poeapi_go 的健康检查与熔断逻辑之后，我的“体感投诉”（主要是我自己投诉自己）下降了一大半。它和第 7 章要讲的熔断与降级是同一套神经：监控 TTFT 是在监控别人的 prefill 排班，熔断切换是在替用户重排一张自己的排班表。

日志里的分布还印证了错峰的价值。同一条渠道，高峰时段的 TTFT 中位数能比深夜翻出一倍不止，而 ITL 的劣化温和得多——队列主要伤在 prefill 之前的等待，decode 的节奏由迭代节拍决定，拥挤时也会变慢，幅度却小得多。这正是第 4 章那句“错峰是免费的加速”在两段式视角下的第二次显形：想省 TTFT，把重 prompt 的批处理任务挪到深夜；想保 ITL，避开挤满长对话的时段。个人用户改不了工厂的架构，但挑得对进厂的时间。

> **数字卡片 5|个人版延迟 SLO 与体感对照**
>
> ```
> 三分律：0.1 秒即时 / 1 秒思路连续 / 10 秒注意力离场
> TTFT 目标：2 秒内，超时先给占位反馈——盲等是信任的漏斗
> ITL 目标：无 200ms 以上尖峰——冻结比慢更伤，可预期性优先于均速
> 失败目标：3 秒内明说——熔断换渠道，失败得快也是体验
> 渠道人设：首字慢而节奏稳 vs 首字快而偶顿挫——TTFT 与 ITL 是两个维度
> 定价对照：输出比输入贵 3-5 倍——decode 的串行苦役明码标价
> ```

两条个人守则附赠。其一，输出 token 是账单大户：3-5 倍的单价乘上成千步的串行，长文生成的成本大头永远在 decode 侧——TokenPolicy 的预算门要对输出长度单独设卡，这是把两阶段性格差翻译成自己的钱包管理。其二，能蹭缓存就蹭缓存：命中前缀缓存的请求跳过大部分 prefill，TTFT 直接受益，这也是第 8 章主角登场前我唯一的忠告——《终端觉醒》第 3 章算过那笔账，缓存读价约为常规输入的十分之一，省下的正是重复 prefill 的钱。

回望全章，我们从 1.8 秒对 40 毫秒的困惑出发，走进两阶段的性格解剖：prefill 吃算力，利用率约 90%；decode 吃带宽，只有 25%-30%；混部让两者互相拖累，ITL 尖峰 150 毫秒；PD 分离让它们各开各的工厂，goodput 与成本双双改写。把镜头拉远，这正是全书总纲的第 3 页：推理 GPU 利用率 5%-10% 对训练 50%+ 的鸿沟，一半就写在这对性格差里——而填沟的手段，不是更快制程的芯片，是承认不对称、顺应不对称的调度设计。大模型的摩尔定律不在制程工艺里，在调度算法里，两段式调度是这句话迄今最雄辩的注脚。下一章我们离开集群，回到我的网关：那些几百行的路由、熔断与预算门代码，正是这座大工厂排班学的微缩模型。

---

## 第7章 模型路由:从我的网关到集群调度

# 第 7 章 模型路由：从我的网关到集群调度

在写这本书之前，我一直以为“调度”是巨头们的课题——他们的集群、他们的排班系统、他们的亿级请求。直到我把自己那个不到七千行的 Go 网关翻出来重读了一遍，才意识到一件让人后背发凉的事：我在 poeapi_go 里手写的那几百行路由、熔断与降级代码，和超大规模推理集群的调度器，讲的其实是同一个故事，只是规模差了七个数量级。负载均衡、熔断、降级、优先级、预算门——个人网关与集群调度器之间是同构的。这一章是全书里“我的网关”戏份最重的一章：我们先解剖我自己的路由表，再复盘一次让我一夜白头的 400，然后讲渠道失效那天的容灾，最后抬头看集群的排班表，看个人尺度与工业尺度如何在每一个部件上遥遥相认。

## 7.1 我的网关：poeapi_go 的路由解剖

我的书桌上常年跑着一个用 Go 写的网关，名叫 poeapi_go——54 个文件、6629 行。放在动辄百万行的工业级系统旁边，这是个不折不扣的玩具尺寸；但它干的活并不玩具：对下，它把好几家模型渠道封装成一组统一的 OpenAI 兼容接口；对上，它给我的编辑器、脚本与 Agent 提供一份统一的模型清单。它最早只是一个薄壳：把 Poe 的接口翻译成 chat/completions 的通用方言；后来 Agent 运行时、工具调用、用量仪表盘一个个搬进来，薄壳才长成了小工厂。换模型不改业务代码，切渠道不惊动调用方——一句话，我是自己的调度科。

先看解剖图。这个项目的分层就是一座小工厂的组织架构：`cmd/main.go` 是大门口，381 行里装着装配逻辑——加载配置、初始化数据库与 Redis、把限流器与中间件挂上链条；`handlers` 是接待处，chat、responses、agent、model 四个入口负责听懂调用方的方言；`clients` 是外联部，poe、gemini、openai 三个客户家族各自对接一种渠道脾气；`internal` 是仪表室，auth 管身份、metrics 管仪表、sanitizer 管脱敏、trace 管追踪，23 条脱敏规则与 5 条高危直接 400 的安全线都驻扎在这里；而 `pkg/multi_provider` 是调度科——`router.go` 与 `circuit_breaker.go` 两个文件合计不到 600 行，本章的主角几乎全在这 600 行里。剩下六千行，是门面、账房与保安——它们不决定 token 去哪儿，却决定这座工厂能不能安全、体面地开门营业。调度常被想象成系统的全部，其实它只该是系统里最薄的一层：薄，才看得清全貌；看得清，才轮转得快。

> **数字卡片 1|poeapi_go 的路由解剖清单（54 个文件，6629 行）**
>
> | 层 | 职责 | 代表部件 |
> |---|---|---|
> | cmd | 大门与装配：配置、限流、健康监控 | main.go，381 行 |
> | handlers | 接待处：协议入口与透传 | chat、responses、agent、model |
> | clients | 外联部：渠道方言的翻译官 | poe、gemini、openai，loadbalancer.go 仅 67 行 |
> | internal | 仪表室：横切能力 | auth、logger、metrics、middleware、sanitizer、trace |
> | pkg/multi_provider | 调度科：路由与熔断 | router.go 447 行、circuit_breaker.go 131 行 |

调度科的第一件道具是路由表。配置里的 `ModelMapping` 极简到只有两个字段：`Provider` 与 `TargetModel`。调用方报上来的 model 名字，其实只是一个别名；网关拿着别名查表，翻出“该去哪家渠道、用真名喊哪个模型”，然后才把请求放出去：

```go
type ModelMapping struct {
    Provider    string `yaml:"provider"`
    TargetModel string `yaml:"target_model"`
}

// config 里的路由表
Models map[string]ModelMapping `yaml:"models"`
```

这个设计看起来平平无奇，用处却极大：调用方永远只认识别名，渠道侧的真名、渠道本身的更替，全部消化在路由表这一层。我后来在工业级网关的文档里看到 model_group 的概念——一组部署共享同一个对外名字，换后端只改注册表、不改路由逻辑——第一反应就是我的 `ModelMapping`：原来他们也是这么干的，只是表更大、字段更多。

查表的顺序写在我的 `GetClientForModel` 里，一共四级：先查显式映射，别名在路由表里登记过的，直接去指定渠道；没登记的，看名字里有没有 gemini 这个关键词，有就送去 Gemini 渠道；再不行，默认走 Poe；最后兜底，任意一个可用的渠道都行。四级查找，一条比一条宽泛，像医院分诊台：挂了号的去对应科室，没挂号的按症状猜，实在不行的先送急诊。而新渠道的接入甚至不需要改代码——OpenAI 兼容的 provider 在配置里登记即可，装配代码会在启动时逐个注册，日志里打出一行 Registered OpenAI-compatible provider。配置即路由，这是我对网关的第一条信仰。

客户的家族史也有讲究。Poe 与 Gemini 的接口脾气古怪，各有专属客户伺候；其余所有 OpenAI 兼容渠道，共用同一个通用客户，差别只是配置里的 key 与 baseURL。专用客户是定制西装，通用客户是均码制服——制服的好处是尺码表即配置，新渠道入职不需要裁缝，装配代码按表下单即可。

这套路由表还有一张对外的脸：`/v1/models` 端点返回的模型菜单，每一个名字都是别名，对应路由表里的一行。菜单的意义不止于好看——它把“有哪些可选”从文档挪进了接口，调用方程序化地拉取与选择，而不是把模型名硬编码在代码里。我是这设计最深的受益者：三茬主力模型换下来，编辑器配置一个字没动。

调度科的第二件道具是负载均衡。同一渠道申请了多把 API key 时，轮着用才不至于把一把 key 打到限流。这件事的工业级实现可以很花哨，我的实现只有 67 行，核心是教科书级的 round-robin——轮询，取模，加锁：

```go
func (lb *LoadBalancer) GetNext() string {
    lb.mu.Lock()
    defer lb.mu.Unlock()
    key := lb.keys[lb.current]
    lb.current = (lb.current + 1) % len(lb.keys)
    return key
}
```

不看价签、不看延迟、不看负载，只看轮到谁——这是负载均衡谱系里最朴素的起点。它当然简陋，但它把“流量要摊开”这个直觉固化成了 30 行代码；后来我读工业网关的各种均衡策略，发现它们全都是在这 30 行的基础上加判断条件，第 7.4 节我们把这个谱系摆开来比。

多把钥匙轮转，对我还有另一层意义：个人套餐的限流按 key 计，单把 key 的 rpm 天花板不高，轮转相当于把限流压力摊薄到每把钥匙头上——一个人也能凑出一支小船队。这个动作在集群尺度上叫水平扩容：不升级单点，只复制实例。从一把钥匙到一串钥匙，从一台网关到一组网关，道理一以贯之：容量不够时，先问能不能摊，再问能不能换。

到这里，调度科的家底已经亮齐了：路由表决定谁去哪，负载均衡决定谁先上。但一张排班表要真正跑起来，还缺三样东西——话说不通怎么办、渠道塌了怎么办、没人看着钱袋子怎么办。这三件事，分别对应接下来三节的关键词：方言、熔断、预算。

## 7.2 一夜白头的 400：mimo 渠道的接口方言

那天晚上，我把手头任务的主力模型切到 mimo-v2.5，想着套餐里这个渠道响应又快又便宜。第一发请求就吃了一记 400。我先是怀疑自己：model 名字拼错了吗？没有。key 失效了吗？是活的。余额耗尽了吗？充足。换了个更短的请求再试——还是 400。这一类事故最磨人的地方就在这里：故障不在网络层，不在鉴权层，甚至不在模型层，而在三者之间那条没有任何文档愿意描述的缝里。排查持续到深夜，答案揭晓时朴素得让人泄气：接口方言不通。那个晚上我把两份接口文档并排摊开，逐行比对两种端点的请求体差异，才发现自己犯的是外语学习者最经典的错误——用一门语言的语法，去读另一门语言的报纸。

OpenAI 世界的接口不止一种方言。老资格的 `chat/completions` 统治了几乎整个生态，是事实上的公共语；而新贵 `/responses` 是更新的接口形态，原生 function calling、服务端会话状态这些新能力都长在它身上。问题在于，mimo-v2.5 在套餐渠道里只讲 `chat/completions` 方言，而我的调用方却拿着 `/responses` 端点去敲它的门。门牌是对的，语言不对，主人只好报 400——它没有义务教你说它的话，它只是拒绝。

讽刺的是，我自己的网关里早就躺着一个为 `/responses` 服务的透传处理器，头部注释把这个教训的另一半写得很清楚——需要原生 function calling 的调用方，只有这个端点能给：

```go
// ResponsesHandler 把 /v1/responses 请求原样透传到 Poe 的 Responses API。
// 需要 Poe 原生 function calling——它只在 /v1/responses 端点提供。
// 只做：URL 换到 /responses、鉴权换成 Poe key、响应透明回传
//（SSE 流式与非流式 JSON 均支持）。
```

透传是代理里最诚实的形态：不解析请求体、不校验格式、不做任何格式转换，只换 URL、换鉴权、把响应按字节流原样回传，SSE 逐块 Flush。因为代理一旦试图“理解”请求，它就必须理解每种方言的全部细节——而方言永远在增加。透传的克制，换来的是渠道侧新特性的零成本跟进。这个处理器至今只对 Poe 渠道开放，因为 Poe 本身就是 Responses API 兼容的；mimo 不是，所以它走不了这扇门。

最终的修法不是改一行代码，而是给方言建档：在配置里为 mimo 单独立一个 provider，名字就叫 xiaomi，api 字段明明白白写着 openai-completions；而确实需要 Responses API 的模型挂在 meta 段，api 字段是 openai-responses。同一份配置文件里，两种方言各过各的日子，互不串门：

```yaml
xiaomi:
  api: openai-completions
  baseURL: https://opencode.ai/zen/go/v1
  models:
    - id: mimo-v2.5
meta:
  api: openai-responses
  models:
    - id: muse-spark-1.2-contributor
```

配置里那一行 api 字段，如今在我看来是路由体系里最便宜也最贵的信息：便宜，因为它只是一个字符串；贵，因为搞错它的代价是一整晚的 400。供应商的产品页从不会用大字写明“我们不支持哪种接口”，文档默认你读的是它支持的那一种——而世界上的方言，比任何一家文档愿意承认的都多。

> **数字卡片 2|同一场 400 的三个断面**
>
> | 断面 | 现象 | 本质 |
> |---|---|---|
> | 调用方视角 | 请求 `/responses` 返回上游 400 | 方言不被识别，门牌对、语言不对 |
> | 渠道视角 | mimo-v2.5 只服务 `chat/completions` | 能力与端点绑定，套餐即方言 |
> | 配置视角 | xiaomi 走 completions，meta 走 responses | 方言进了路由表，成为路由的一部分 |

这件事再往前推一步，会碰到一个更别扭的事实：同一个模型，在不同渠道上可能是不同的方言版本。mimo-v2.5 在这家套餐里只讲 completions，换一家渠道也许就讲 responses；模型同名，能力未必同名。所以我的路由表里，别名从来不只是指向一个模型名，而是指向“某个渠道在某个套餐下的某套能力”——这个名字的三段式语义，任何文档里都不写，只能自己踩出来、记进配置。

方言课还有第二讲，比第一讲更疼：错误掩码。我的路由器曾经有个自作聪明的默认——显式映射的渠道失败后，回退到 Poe 再试一次。听起来是更高的可用性，实际是灾难：像 ds-free 这种 Poe 根本不供给的模型名，会被原样送到 Poe 的接口，对方茫然地回一个 500，报文是那句著名的 Server got itself in trouble——真实的上游错误被埋在尸体底下，我拿着一条毫不相干的线索排查了半天。后来我在 `router.go` 里留了一段狠话式的注释：显式指定渠道的模型，绝不回退，哪怕看起来可用性降低了。路由器最坏的行为不是失败，而是失败得不对——让调用者拿着错误的线索，去排查错误的问题。

这次之后我给网关立了一条新规矩：错误要带出处上传。上游说什么，网关转发什么，顶多附注一行“已尝试渠道 A 失败”——绝不擅自改写诊断结论。日志可以详尽，报错必须诚实。掩码在小系统里是麻烦，在大系统里是事故：当一次调用跨越三五层代理，任何一层的自作聪明，都会让调用方与真相之间隔一层毛玻璃。

接口方言不兼容，是我给网关路由总结的第一课。它与第 9 章的引擎取舍是一枚硬币的两面：引擎用性能换绑定，渠道用接口换生态，而路由表的职责，是把这一切不兼容消化在调用方看不见的地方。一个模型名背后的完整语义，包括它讲哪种方言、登记在哪本户口上——这些都应该在路由表里，而不是散落在调用方的代码里。把方言知识从代码搬进配置，还有一重好处：配置可以一夜改定，代码要发布上线；渠道的方言从不提前打招呼，你能指望的，只有那张随时可改的表。

## 7.3 熔断与降级：渠道失效的那一天

方言课解决的是“说不上话”，熔断课解决的是“人没了”。智谱渠道失效的那一天，我至今记得日志的节奏：第一条超时报错出现在下午，几分钟内失败连成串，而我甚至没在电脑前——等我发现时，网关已经自己把这家渠道判了不可用，流量顺着候选链滑到了小米 MIMO，任务照常跑完。多供应商冗余这个词，在经历过那一天之前，只是架构图上的一行字；经历过之后，它是账单上真金白银的保险费，也是我敢把关键任务交给网关的胆量来源。

事后复盘，那天的无损切换并非全靠运气。第一，候选链是预先铺好的——小米 MIMO 早已在配置里登记为可用渠道，只是平时排在候选链后面；第二，熔断器的判定足够快，连续失败五次即拉闸，没让流量在死渠道上多流一滴；第三，切换发生在配置层，改一行 provider 字段重载即可，业务侧全程无感。冗余不是躺在硬盘里的备份文件，冗余是候选链上活着的下一个名字。

判决渠道生死的，是 `circuit_breaker.go`，131 行，一个教科书级的熔断器。三个状态：Closed——熔断器闭合，流量正常通过；Open——连续失败达到阈值，拉闸，请求一律快速失败；HalfOpen——冷却期结束，放进一个探头请求试路，成功则闭合复位，失败则继续拉闸。我的参数写死在装配代码里：

```go
circuit: NewCircuitBreaker("poe", 5, 30*time.Second)
// 连续失败 5 次熔断，30 秒后进入半开
```

> **数字卡片 3|我的熔断器与它的工业远亲**
>
> | 参数 | 我的网关 | 工业实践 |
> |---|---|---|
> | 熔断阈值 | 连续失败 5 次 | LiteLLM：一分钟内失败超限即冷却 |
> | 打开时长 | 30 秒 | LiteLLM cooldown 默认 60 秒 |
> | 半开策略 | 单探头，10 秒探测超时 | OpenRouter：30 秒故障窗口内降权排队 |
> | 状态共享 | 单进程内存 | 生产用 Redis 跨实例共享冷却状态 |
> | 可观测性 | provider_status 取值 1、0.5、0 | 同左，故障恢复在图上是一个台阶 |

熔断器不是孤岛，它嵌在路由主循环里。`RouteChat` 的骨架只有十几行：按候选顺序取渠道，先问熔断器放不放行，不放行就跳下一个；调用失败，给这只熔断器记一次 `OnFailure`，计数满阈值自动拉闸；调用成功，`OnSuccess` 清零复位。被熔断的渠道在 Prometheus 仪表盘上是 `provider_status` 等于 0 的一条线，半开是 0.5——渠道生病与痊愈的全过程，在图上是一个清晰的台阶。失败不是被藏起来的意外，而是被计量的状态。

这些指标还有一层事后价值：`provider_api_calls_total` 按渠道、按成败分列计数，哪条渠道哪天病了、病了多久、探测几次才痊愈，图上全留着病历。个人网关做可观测性常被笑作杀鸡用牛刀；那天之后我确信，没有病历的故障会二次发作，有病历的故障才叫学费。

降级链则是另一层保险。poe 客户端里的 fallback 顺序颇有生活气息：映射后的模型失败，用调用方的原始名字再试一次；再不行，Grok-3 与 Grok-4 互为备胎——旗舰缺席，上代旗舰顶班；最后的兜底交给 Qwen3.5-Omni-Flash，渠道里的经济舱，便宜、皮实、哪里都能去。降级链的本质是一条质量阶梯：每一级都比上一级便宜一点、皮实一点，也笨一点——好死不如赖活着，这句话在容灾设计里是褒义词。

降级链还有一笔暗账：重试不是免费的。同一个请求在渠道 A 处超时后转投渠道 B，如果 A 其实已经受理、只是应答迟了，B 再跑一遍就是双份计费。生产级网关用幂等键对冲这件事；我的个人网关规模小，错一笔不过是一杯咖啡，但我仍把超时设得比上游保守——宁可慢一点，不重复扣款。

但熔断器自己也会生病。`router.go` 里至今留着一段我写了又删、删了又写的修复注释：流式调用建立成功后，必须立刻调用 `OnSuccess`，否则半开探头会永远挂在“探测中”的状态，10 秒窗口内每一个请求都被跳过，网关持续报 no providers available——熔断器把整个网关锁死在了半开态。那是我第一次如此具体地体会到：状态机里一行遗漏的复位，能把其余 130 行的正确全部作废。熔断器是拿可用性下注的赌局，而赌局最怕的不是输钱，是庄家自己也昏了过去。

降级还有一层容易忽视的分寸：不是所有失败都值得降级。网络瞬断、5xx 服务端错误，切，毫不犹豫；429 限流，先读上游给的 Retry-After 再说，盲目重试是把压力放大给一个已经喘不过气的病人；4xx 参数错误，重试一万次也还是 400，该做的是把错误如实递回去，让调用方改请求；至于流式响应已经开始吐 token 的，无法中途换渠道，只对首 token 失败做 fallback——半句话换个人接着说，语义就对不上了。健康检查同样讲分寸：main.go 里的后台协程每 15 秒 ping 一次数据库与 Redis，连续 3 次失败才把健康指标置 0——单次抖动不该触发警报。防抖不是迟钝，是对上游最基本的礼貌：没有人希望因为一次网络咳嗽，被宣判死亡。这套分寸感，后来我在工业网关的文档里看到同款表述时并不意外——大家都是在赔过钱之后，才学会区分哪种错误值得重试。

## 7.4 集群的排班表：优先级队列与公平调度

我的网关只有我一个租户，所以它的排班表缺了最重要的一栏：当两个人同时伸手，先给谁？个人网关可以假装这个问题不存在，集群不行——集群的每一个推理实例上，永远同时排着几十上百个请求。把视线从书桌抬到机房，你会发现前面三节的所有部件，在集群尺度上都有一位同构的远亲，而且多出几件我根本没有的装备。这一节我们把这张对照表摆开。

先复习已见过的部分。vLLM 调度器里的三个队列——waiting、running、swapped——就是集群排班表的正身：新请求进 waiting，批内的请求在 running，被挤出显存的去 swapped 等待回收（第 4 章的主角，连续批处理让请求随时插队，混合吞吐翻 2 到 4 倍）。显存池满时，调度器动手抢占：或者把请求的 KV 全部作废、来日重新预填充——recompute；或者把它序列化搬到 CPU 内存里暂存——swap。两条路都收费，一条收算力，一条收带宽。这是空间维度上的排班：厂房只有一个，在制品太多，就得有人下岗。

而我真正没有的，是时间维度上的排班——优先级队列。工业网关的成熟做法是让请求自带 priority 参数，排队时按优先级分层，发车时先高后低；更进一步，2025 年下半年以来各网关普遍支持为关键 API key 预留容量：把总 TPM 的一定比例划给生产负载，开发流量再怎么洪峰，关键业务的保底额度也动不了。这件事的社会学含义比技术含义深：排队是公平的，但无差别的公平对无差别的负载才成立——凌晨的批处理任务和用户正在盯着屏幕等首字的对话，不该站在同一条队里。首字延迟 500 毫秒的体验承诺，本质上是一句排班表上的承诺：你的班次被保证。

预留与抢占是排班表的阴阳两面：预留是事先划出的保底座位，抢占是事到临头的拆东补西。成熟集群两者并用——对承诺了 SLO 的流量用预留，绝不失信；对可以延迟的流量用抢占，物尽其用。这与我 7.3 节的熔断跳过是一体两面：集群在显存维度上决定谁下岗，我在渠道维度上决定谁靠边。

公平本身也是一门学问。共享集群的经典事故是大客户饿死小客户：一个批量任务甩进来几千个长上下文请求，把队列填满，其他租户的短请求排到天荒地老。网关层的解法朴素而有效——按用户分队列，队列之间轮询调度，谁也别想独占发车口；再配一条工程经验：把后端队列深度控制在 3 左右，更低，新请求等待短但批处理效率低，更高，吞吐漂亮但延迟爬坡。3 这个数字是效率与公平谈判的结果，而谈判在每一个集群里都要重新开一次。

> **数字卡片 4|负载均衡策略对照：我的网关与生产网关**
>
> | 策略 | 我的网关 | 生产网关 |
> |---|---|---|
> | 轮询 | round-robin 轮转 API key，67 行 | per-user 队列加轮询，防饿死 |
> | 加权随机 | 无 | simple-shuffle：按 rpm/tpm 加权打散，官方默认 |
> | 最低负载 | 无 | least-busy：选在处理请求最少的部署 |
> | 最低延迟 | 无 | latency-based：缓存响应时间，TTL 窗口加缓冲 |
> | 最低成本 | costTier 只进预算门 | lowest-cost；OpenRouter 反平方价格加权 |
> | 用量感知 | 无 | 每请求查 Redis，官方不建议生产 |

负载均衡的谱系里，最有趣的分支是 OpenRouter 的反平方价格加权：在同样健康的提供商里，按价格的平方倒数分配尝试概率——1 美元与 3 美元两家并存时，先试便宜那家的机会是贵那家的 9 倍，因为 3 的平方是 9。平方不是炫技，是让流量强烈地倒向便宜方，而不是温吞地均摊：明明有健康的便宜货，就没理由付三倍价钱。再加上一条 30 秒故障窗口——最近 30 秒出过重大故障的提供商不删除、只排队尾——价格效率与容错稳健就被缝进了同一个概率分布。而 LiteLLM 的官方态度也很诚实：最朴素的 simple-shuffle 恰恰是生产推荐，因为每次请求都去 Redis 查用量的精细路由，在高流量下那点智能抵不上那点延迟。智能是要收费的，收费处就在每一跳的毫秒里。

调度器进化到今天，业界已经总结出一条清晰的路径：从固定模型，到按场景的规则路由，再到成本级联——小模型先试、不够再升级；然后是用嵌入向量做语义路由，用评测集做质量反馈路由，直到会自我调整的学习型路由。我的四级查找停在第二阶段，但 TokenPolicy 里那个 costTier 已经悄悄伸进了第三阶段：预算门给出的换模型建议，永远指向低档——这就是成本级联的个人版雏形。抢占、降级、优先级、准入，这四件事在集群里叫调度，在个人网关里叫熔断与降级——排队是时间维度的调度，降级是质量维度的调度，预算门是准入控制。名字变了，排班表还是那张排班表。

排班表上还有一层看不见的划分：池子分层。生产推理服务的通行做法，是把流量按服务等级调进不同的池子——旗舰池接付费关键业务，配额独占、抢占豁免；标准池接日常负载，尽力而为；免费池接容忍排队与降级的流量，高峰期第一个被掐。分层的目的不是歧视，是把不同请求对延迟的忍受度，翻译成调度器可以执行的参数。我的网关里也有同样的分层，只是更赤裸：主力渠道、备用渠道、经济舱兜底——一个人的流量也要分三六九等，因为不分级的调度等于宣布所有请求同样珍贵，也就等于宣布没有请求珍贵。

至于语义路由——用嵌入向量判断这句话该去哪个模型——我的态度是审慎的看好：个人尺度上，一次语义判断本身就是一次模型调用，省下的差价未必付得起判断的账；规模上去之后，这笔账才慢慢算得过来。路由的每一步升级，都要先过自己那一关：判断的成本，不能吃掉路由的收益。

## 7.5 预算门：TokenPolicy 与 Spend Limits 的隔空握手

第 2 章的 2.5 节预告过我的 TokenPolicy，那里讲的是上下文预算——窗口是租来的显存，每一格都要数着用。但 TokenPolicy 里还藏着另一笔账：钱的预算。上下文占用率管的是这一次请求胖不胖，costTier 管的是这一族模型贵不贵：常用模型按每百万 token 的价格分成低、中、高三档，警告区给出的换模型建议只指向低档或长上下文档位。就是这个小设计，让预算门从一道闸门变成了半台路由器——它不只会说不行，还会说怎么才行。

把四级门再抄一遍，因为它是全节的地基：

```
占用率 ≤ 70%     安全区：直接放行
70% ~ 80%        预警区：放行，记录警告
80% ~ 100%       警告区：拦截确认，给出多条出路
> 100%           阻断区：强制阻断，换模型或砍内容
```

实现上最得意的一处是估算与授权的分离。TokenEstimator 只调 estimate，从不调 resolve——先只拿条目的元数据估体积，按字节除以四粗算 token 数，算完决定这条要不要进上下文，授权之后才真正去读内容。估算是廉价的，读取是昂贵的；把昂贵的动作放在便宜的判断之后，先算钱再上车——这个顺序，就是预算门的全部尊严。

四级门的阈值本身也值得说一句：为什么是 70 与 80，而不是 95？因为预算的本质是留出反应时间——等占用率逼近 100% 才报警，可选项只剩硬发或放弃；70% 就亮黄灯，砍内容、换模型这些温和手段都还来得及。预算门的价值不在拦截那一刻，在它提前争取到的选择权。

后来我在企业级网关的架构图里看到同一条顺序被放在了更庄严的位置上。一次请求在 LiteLLM 这类网关里的生命周期是四步：第一步，鉴权加预算校验——虚拟 key 先查 Redis 缓存，未命中才落 PostgreSQL，预算不足在这里就直接拒绝；第二步，限流——rpm 与 tpm 按全局、key、用户、团队四级配额层层检查；第三步，才轮到路由器做负载均衡、fallback 与重试；第四步，协议翻译，把 OpenAI 格式翻成各家渠道的方言。预算检查站在路由之前——先确认付得起，再讨论去哪儿。云厂商控制台里按 key 设置 Spend Limits、超限自动降级的界面，与我那个手写的四级门，隔着整个行业遥遥握手。

预算门在企业里还兼任会计：按部门、按项目分摊费用，谁花的、花在哪个模型上、这个月还剩多少，控制台上一目了然；谁能用哪些模型，也一并挂在 key 的权限里。个人网关里，这个角色由账单邮件扮演——只是它只会事后报丧，不会事前拦人。这正是我坚持亲手写预算门的原因：报丧不能省钱，拦人才可以。

> **数字卡片 5|预算门的个人版与企业版**
>
> | 维度 | 我的 TokenPolicy | 企业级 Spend Limits |
> |---|---|---|
> | 检查位置 | 请求出门之前 | 生命周期第 1 步，先于路由 |
> | 预算单位 | 上下文占用率，加 costTier 三档 | 按 key 月度预算，加 TPM 容量预留 |
> | 超限动作 | 拦截确认，给出硬发、抽样、换模型、放弃 | 429 附带余量数字，或自动降级 |
> | 状态存放 | 单进程内存 | Redis 跨实例共享，多网关一致 |
> | 配套机制 | TokenEstimator 先估后读 | 虚拟 key 缓存，预算接口报余额 |

限流与预算，是一对容易被混为一谈的孪生门。我的网关里挂着 rate 包的令牌桶限流器：默认每秒 10 个请求、桶深 100——它管的是速率，是血压，防止一瞬间的高峰把任何渠道打穿；TokenPolicy 的 100% 阻断线管的是总量，是存款，防止单次或累计的花费越过承受力。血压正常的病人也可能倾家荡产，存款充足的人也可能猝死于高峰——两道门各管各的，缺一道，治理就是漏的。

两道门的参数也都不是拍出来的。每秒 10、桶深 100，对应的是我最重那条自动化流程的并发形状；70% 与 80% 的黄红线，对应的是一次人工确认的平均等待耐心——预警线太低，确认会频繁打断工作流；太高，拦截就成了事后追认。参数是负载形状与人的耐心共同拟合出来的曲线，别人的默认值只能参考，不能照抄。

现在可以把这一章的四个部件合成一张完整的进门图了。一个请求抵达我的网关，要依次经过四道门：身份门——auth 中间件验 key、查配额；预算门——TokenPolicy 估算占用率、比对 costTier；速率门——令牌桶看这一秒还放不放行；方言门——路由表翻译别名、选定渠道，熔断器确认渠道活着，候选链备好退路。四道门全部通过，请求才被放出去敲真正的主人的门。第 2 章说过，脱敏在更前面——先阻断、再脱敏、再落盘。所以治理不是路由的对立面，治理就是路由的前半段：每一个被路由的请求，都是先被治理、才有资格上路。

回头再看总纲上那句话——大模型 Infra 的核心，是让显存利用率接近 100%。这一章给了它一张排班表的具体形状：路由决定 token 落在谁的厂房里，负载均衡保证每间厂房都吃饱和，熔断保证厂房塌了订单能连夜搬家，优先级保证重要的订单先上车，预算保证订单总量不超出厂房的承受力。调度器的全部智慧，说到底就是一件事：让每一个字节的显存、每一毫秒的算力、每一分钱的预算，都花在当下最值得的那个请求上。个人网关如此，超大规模集群也如此——区别只在表格的行数。下一章我们走进厂房里最优雅的一笔复利：当相同的系统提示词每天都在重复计算，Prompt Cache 如何让重复的前缀不再重复收钱——前缀树、LRU 与内容哈希，那是复用经济学登场的日子。

---

## 第8章 Prompt Cache 的物理实现:前缀复用的经济学

# 第 8 章 Prompt Cache 的物理实现：前缀复用的经济学

第 3 章结尾，我欠下两笔账：为什么“把不变的东西放在开头”成了提示词写作里的经济行为，以及多租户场景下，谁能蹭谁的缓存。这一章把两笔账一起结清。先看一个价格：同一段 8000 token 的系统提示词，第一次送给模型是全价，第二次送过去，只收十分之一；写入缓存还要加收四分之一，但从此以后，每次复用都按一折结算。这不是营销折扣，是一个可以逐字节核算的物理事实——你为模型服务付的钱里，有一大块叫“重复的 prefill”，而重复，恰好是 KV Cache 最擅长消灭的东西。前缀树负责找到共享段，LRU 负责决定谁留下，内容哈希负责保证命中的真实性：三个零件，没有一个是新发明，拼在一起，却构成了大模型定价史上最大的一次单点让利。这一章我们先看清价签，再拆开机器，最后走到那道边界前——省钱的机制，什么时候必须停止省钱。

## 8.1 十分之一的价格：缓存读的定价学

先看价签。Anthropic 给 Claude API 上线 prompt caching 时，价目表上多出两行：缓存写入，按普通输入的 1.25 倍计；缓存读取，按 0.1 倍计。同一批 token，写进缓存比全价贵四分之一，从缓存里读，只收十分之一。以 Sonnet 级模型每百万 token 输入 3 美元为基准，写入价是 3.75 美元，读取价是 0.30 美元。默认档的缓存寿命是 5 分钟，嫌短，有 1 小时档可买，写入价从 1.25 倍涨到 2 倍，读取价不变。OpenAI 的路数一致而更无感：1024 个 token 以上的请求自动启用前缀缓存，不用打任何标记，命中的部分按半价计，官方口径里首 token 延迟最高可降约 80%。两家数字不同，骨架相同——都把一次输入的账单，拆成了三段。也别忽略了这对价格的结构含义：写价与读价同时存在，等于厂商与用户共担风险——写价贵出的两成五，是用户预付的施工费；读价让出的一折，是厂商返还的复用红利。谁把前缀设计得好、复用得多，谁就拿走红利的大头；缓存没人用，厂商手里也压不住损失。定价表在这里不再是价目，是一份激励合同。

三段账单值得逐行看。一次带缓存的请求，计费由响应里的三个字段各管一段：普通输入字段，是没有命中的部分，按全价；缓存写入字段，是这次新存进去的部分，按 1.25 倍；缓存读取字段，是从缓存里取出的部分，按 0.1 倍。对账时只需盯住第三个字段——它大于零，这一单就在省钱；它是零，说明你的前缀白写了。

把一次真实的调用放进这三段里。第一次请求：8000 token 的系统提示词整段未命中，全部落进 1.25 倍的那一栏，单是这段前缀就计 3 美分；几秒后的第二次请求到达，同样的前缀整段命中，同样的 8000 token 挪进 0.1 倍的那一栏，只计 0.24 美分。同一个数字，在两张账单上相差十二倍半——价目表一个字没改，改的是工厂为它干的活：第一次要施工，第二次只是开门。顺带一提，5 分钟租期是滑动窗，每次命中都重置计时，所以这两次请求隔多久算“命中”，答案不是“5 分钟内”这么机械，而是“上次访问之后 5 分钟内”——活跃的会话因此永不过期，这也是厂商把租期敢定得短的原因：它惩罚的是闲置，不是使用。

> **数字卡片 1|缓存读的定价学（基准：输入 3 美元/百万 token）**
>
> ```
> 普通输入        1 倍       3.00 美元
> 缓存写·5 分钟   1.25 倍    3.75 美元（一次性施工费）
> 缓存写·1 小时   2 倍       6.00 美元（长租，租金翻倍）
> 缓存读          0.1 倍     0.30 美元（参观价）
> 回本点          1.25 + 0.1 = 1.35，低于两次全价的 2.0
>                 → 第 2 次复用即回本；1 小时档第 3 次回本
> ```

省下来的到底是什么？不是显存租金——你的 KV 照样住在厂商的显存里，一分钱地皮费都没少；省的是重复的 prefill。第 6 章我们会专门解剖那两个阶段，这里先借一个结论：prefill 是计算密集的阶段，GPU 利用率能冲到约 90%，一段 prompt 的 KV 全靠它一次算齐。前缀缓存把这桩计算整段免掉：同样的前缀，算力只花一次，第二个请求进门时，这段 KV 已经躺在显存里，注意力从“算出来”切换成“取出来”。算一次，收一次的钱；第二次只收读取的钱——因为第二次真的只花了读取的力气。十分之一的定价不是让利，是如实结算：免掉一段 8000 token 的 prefill，就是免掉一次满负荷的矩阵运算，连同它挤占的排队窗口；用户侧的体感是首 token 提前到达，厂商侧的账本是这笔 FLOPs 根本没有发生。

顺带回答一个相邻的疑问：为什么输出 token 从不打这种折？因为输入的可复用性是结构性的——同样一段前缀，谁发来都是同一段 KV，算一遍就永远有效；而输出是自回归长出来的，每个 token 都依赖此前生成的每一个字，连你自己都无法复用自己上一次的回答。输入可以并行算、可以提前算、可以算一次用多次；输出只能一次一个、现算现卖。这正是输出定价比输入贵 3 到 5 倍的物理根源，也是缓存折扣只属于输入的原因——输入是工厂能预先备料的工序，备料复用的每一分红利，定价表上都挂着名字。把这笔折扣放进 2026 年的背景里看，意义还会放大：推理成本正以每年约 10 倍的速度下滑，而缓存是在降价曲线之上再打一折——两股力量相乘，用得好的人拿到的实际单价，能领先价目表整整一个身位。

这笔账我第一次认真算，是在《终端觉醒》第 3 章，站在用户视角，把缓存读约为常规输入十分之一的价目做了一遍成本演算。那本书算的是省下的钱，这一章要算的是省下的计算——这一次我们走到机器背面，看这十分之一是从哪里长出来的。我的结论是：它不是定价策略，是物理事实的会计表达。厂商把省下的计算让渡给你，换你把负载写成它好处理的样子：前缀稳定、可复用、命中率高。你配合它的显存管理，它退还一部分边际成本，一笔各取所需的交易。

定价学的全部秘密可以压成一句话：施工价和参观价，分开计费。第一次，全价施工，还要加收两成五的进料费；此后，一折参观。剩下的问题是，厂商凭什么敢把参观价定到一折还不亏本？答案藏在下一节——因为施工，真的只发生一次。

## 8.2 前缀的物理学：为什么缓存必须是前缀

凭什么施工只发生一次？换一个更工程的问法：为什么缓存的是前缀，而不是 prompt 的任意一段？若能缓存中段，复用空间岂不更大？答案在第 1 章就埋好了：因果掩码。Transformer 的信息流严格从左到右，第 i 个 token 的 K 与 V，只由第 1 到第 i 个 token 决定，后面的字对它没有任何影响。这个不对称性是双刃的：它让 KV Cache 成为可能——历史算一遍就存下来；它也让缓存只能沿序列轴单向分布——能被复用的 KV，当且仅当产生它的那段 token 序列完全一致，而序列的一致性只能从第一个 token 起向后延伸。前缀是公共段，中段与后缀是私有段。你在第 500 个 token 改一个字，从第 500 个起，整段 KV 全部作废；你改的是第 10000 个，前 9999 个的 KV 一个字节都不用动。KV 的可复用性沿位置单调递减：越靠前越公有，越靠后越私有——缓存必须是前缀，不是设计者的偏好，是因果结构的数学性质。

第二根支柱是确定性：相同的 token 序列，必然产生相同的 K 与 V。前向传播是确定的矩阵乘法，推理阶段没有随机性——温度与采样只作用在“挑下一个词”上，从不作用在 KV 上。这是缓存大厦的地基：若同样的输入会算出不一样的 KV，复用便无从谈起。相同前缀、相同 KV、只算一次——这句话反着读，就是 prompt caching 的完整物理定义。

值得把“共享”这个词的两副面孔分开。第 3 章讲的是批内共享：beam search 的几条候选、并行采样的几个回答，是同一个请求内部共享前缀，分叉即写时复制；本章讲的是跨请求共享：一分钟前某个请求算完的前缀，一分钟后被一个素不相识的请求整段命中——共享从批内的小圈子，扩展成时间上横跨全天、空间上横跨全部流量的公共段。这是两次不同的胜利：批内共享省的是一个瞬间里的重复，跨请求缓存省的是一天之内的重复；后者的复用次数在理论上没有上限，这正是它能撑起一折定价的原因。还可以做个思想实验，把“前缀必须是前缀”量化：一段 8000 token 的系统提示词，你在第 500 个 token 处改一个措辞，前 499 个 token 的 KV 依然有效，从改动处起 7500 余个 token 的 KV 全部作废，命中率从 100% 跌到约 6%；同样的改动若挪到末尾，作废的只有最后几十个 token。改动位置每前移一分，作废的 KV 就多出一寸——“把不变的东西放在开头”不是文风建议，是命中率的杠杆。

物理学就绪，剩下的是工程：用什么数据结构找到“相同的前缀”？三份答卷各有性格。vLLM 的前缀缓存（第 3 章埋过伏笔）把索引建在分页块上，哈希是链式的——一个物理块的哈希，由块内 token 与前一块的哈希共同算出：

```
H(1) = Hash(第 1 块的 token)
H(i) = Hash(H(i-1), 第 i 块的 token)
```

想命中第 N 块，必须从第 1 块起逐段对上，一个 token 都造不了假。它的匹配粒度是块，默认 16 个 token 一块，查表快，粒度略粗。SGLang 的 RadixAttention 走了另一条路：干脆把整个 KV 池组织成一棵压缩前缀树——基数树。树上每个节点存一段 token 序列和对应的 KV 块，从根到叶的一条路径，就是一个请求的完整前缀：

```
              root
               |
        [系统提示词 8000 token]          ← 公共段，只存一份
           /              \
   [用户甲的会话]      [用户乙的会话]     ← 私有段，各自生长
```

新请求到达，从根沿树往下走，做最长前缀匹配，耗时只与序列长度有关，与树的大小无关：完全命中就跳过整个 prefill，部分命中就只算没命中的后缀。两个请求的路径在中途分叉，树就分裂节点，公共部分保留为父节点；请求结束、引用计数归零之后，显存吃紧时从叶子开始按 LRU 淘汰，淘汰后孤悬的路径再自动合并，树始终保持紧凑。树结构还有一笔隐蔽的红利：公共前缀在哈希表里是一块一条的索引项，在树里被压缩成一条共享路径——共享越多，树越瘦，索引自身的开销反而越小，这是前缀同质负载偏爱基数树的第二个理由。这套树结构并不是推翻分页另起炉灶——RadixAttention 的节点里存的是分页块的编号，不是裸地址：最长前缀匹配命中的是一串物理块，直接填进块表的前缀位置，未命中的后缀才真正做 prefill，新算的 KV 写进新领的块，再作为新节点挂回树上。分页回答“KV 放在哪里”，前缀树回答“KV 找不找得到”，两层结构各答一题，拼起来才是一台完整的缓存机器。SGLang 团队自测，共享前缀负载下命中率可达 99%，同等负载比哈希表方案少用约 40% 的 KV 内存，吞吐提升数倍——自测口径要打折看，但量级可信。OpenAI 的云端缓存是第三种性格：黑盒，但技术资料里透出骨架——请求按开头约 256 个 token 的哈希做路由，把同前缀的流量粘到同一台引擎上；缓存以 1024 个 token 起步、按 128 递增对齐；调用方还能传一个 prompt_cache_key，为同前缀的请求再加一层粘性。

> **数字卡片 2|三份前缀索引答卷**
>
> ```
>           vLLM 前缀缓存        SGLang RadixAttention    OpenAI 缓存
> 结构      哈希表 + 链式哈希     压缩前缀树（基数树）      云端黑盒 + 哈希路由
> 粒度      块级，默认 16 token   token 级，任意长度       1024 起步，128 递增
> 匹配      逐块查表              沿树最长前缀匹配         路由层近似
> 淘汰      空闲池 LRU            叶子起 LRU + 引用计数    未公开
> ```

需求的规模值得一记：真实负载里，约有四到八成的 token 带着共享前缀，来源大体四类——所有请求共用的系统提示词；多轮对话里层层叠加的历史上下文；少样本示例；RAG 检索反复命中的同一批文档。四类来源共享同一个特征：算出来的 KV 与“谁来问、何时问”无关。一个客服机器人，8K 的系统提示词被一天几百个请求各自重复 prefill 一遍，九成以上的预填充算力花在重复施工上——工厂不能容忍这种事，缓存是它的自救。但要记住：命中率的天花板由负载画，数据结构只决定你能摸到多高；探索型负载人人一句、句句不同，再好的树也长不出公共路径。物理学的部分到此收束：因果掩码给前缀发了产权证，确定性给复用发了营业执照。池子就这么大，谁来住、谁搬走，是下一节的管理学。

## 8.3 LRU 与失效：缓存池的管理学

缓存的第一性问题不是怎么存，而是满了怎么办。KV 池是工厂里最贵的地皮，缓存块是住客里最特殊的一群：它们不参与当前任何一个请求的生成，不发一个 token，只是待命——等某个未来的请求撞上来。给它们留多大地方，是每个引擎都要回答的预算题：留多了，活跃请求装不下，吞吐掉；留少了，命中率起不来，缓存白建。这笔预算没有常数答案，所以真正稀缺的从来不是缓存技术，是显存本身——管理学的全部内容，就是决定谁留下、谁搬走。

裁决的规则是老熟人 LRU：最久未用的先走。赌的是访问的时间局部性——刚被用过的前缀大概率还会被用，五分钟没人理的大概率凉了。这套赌注在第 3 章的空闲池里赢过一次，在缓存池里再赢一次。但 LRU 不许碰正在用的东西：vLLM 的做法是让无人引用的前缀块先降级为“可回收”，引用计数归零才有资格进候选；SGLang 给树上每个节点挂了引用计数，非零的路径整条锁死，而且淘汰只能从叶子开始——中间节点一旦搬走，它所有后代的前缀同时失效。这条规矩等于承认了前缀的偏序结构：要死，先死最不公共的那段。淘汰之后，只剩一个孩子的父亲会把路径重新合并，树保持紧凑。变体当然有，LFU、FIFO 都能配，工程上没有圣旨，只有负载。池子的划法也有暗线：vLLM 不给缓存单开配额，缓存块与请求块同池而居，命中率的成本由高峰时的驱逐率自动结算；SGLang 则保留了一个开关，负载不合适时可以整棵关掉这棵树。给缓存留多少显存，没有教科书答案，只有“驱逐率与命中率的博弈”——每一次驱逐都是一次重写的预付款，每一次命中都是一次免算的红利，池子的大小就是这两笔账的汇率。

引擎里是 LRU，商业 API 上又加了一层合同：租期。Claude 的默认租期 5 分钟，而且是滑动窗——每次命中都重置计时，活跃的会话永不过期，真正闲置满 5 分钟才失效；嫌短，买 1 小时档，写入价从 1.25 倍涨到 2 倍。这层合同把缓存做成了租金分层的产品：你为“保留”付的钱，涨得比为“计算”付的钱快——因为保留占的是显存，计算费的是算力，而显存，正是这本书从头讲到尾的那块最贵的地皮。

管理学的底气，来自一笔可以摊开算的物理账：把一段前缀留在显存里，究竟花多少钱？按第 2 章的口径，70B 模型 FP16 下每个 token 的 KV 是 320 KB，一段 8000 token 的前缀占地 2.44 GB；一块 80 GB 的 H100 时租 2.85 到 3.50 美元，折下来每 GB 每小时约 3.6 到 4.4 美分——这段前缀驻留 5 分钟的地租，是 0.7 到 0.9 美分。再把定价表放到同一张桌上：同样这段前缀，全价 prefill 一次要 2.4 美分，缓存读一次只收 0.24 美分，写入时多收的溢价是 0.6 美分。三笔账并排，商业设计的精巧就现形了：写入溢价预付了大半地租，读价虽低，落在厂商头上的却是近乎纯利的增量——让出的十分之九，来自“不必再算”，不来自“倒贴地租”。一折的读价能长期成立，不是因为它便宜，是因为它便宜得仍然有赚头。这笔账也解释了 LRU 为什么是默认答案：缓存块的驻留成本按分钟计，久不使用的前缀就是趴在显存里的纯地租，按最久未用清场，是地租逻辑下的唯一理性。

失效有四种死法，值得一张卡片。

> **数字卡片 3|缓存失效的四种死法与对策**
>
> ```
> 死法一|TTL 过期    5 分钟滑动窗，命中即续期；闲置满租期，整段作废
> 死法二|前缀漂移    一个字节都不行：时间戳、UUID、JSON 字段顺序都是杀手
> 死法三|模型切换    换模型等于换缓存；长对话中途切模型，前缀全部重写
> 死法四|池子竞争    显存吃紧，前缀被 LRU 挤出；下次重写，再付 1.25 倍
> 对策    静态前置、动态后置；断点最多 4 个；把命中率当 uptime 监控
> ```

四种死法里，前缀漂移最冤。把“今天的日期”写进系统提示词开头，每天零点，全量用户的缓存同时死亡；把请求编号拼在前缀里，每一单都是全价；甚至 JSON 字段的序列化顺序抖动一下，前缀就断在半路。对策说来朴素：稳定的资产放前面——系统提示词、工具定义、少样本示例、知识库文档；易变的放后面——用户的问题、检索的结果、动态的状态。Claude 的 cache_control 断点一次最多打 4 个，断点即缓存边界，你在告诉工厂：到这条线为止是样板间，线后是每次不同的参观路线。工具定义属于前缀的一部分，会话中途增删一个工具，整条会话的缓存陪葬——要传新状态，追加一条消息，不要回头改开头。

另外两种死法，各有机理。模型切换之所以等于换缓存，是因为不同模型的 KV 空间根本不通约——层数、头数、维度对不上，缓存键里本就掺着模型的身份，换模型等于换了一套地址体系，旧 KV 就是留在显存里也无人能读；100K 上下文的长对话切一次模型，单是重建前缀的写入费，就是一笔足够肉疼的账。池子竞争则是共享池的宿命：你的前缀与活跃请求的 KV 同池而居，高峰一来，调度器永远优先保正在说话的请求，无人引用的前缀块第一个被请出去——这也是为什么命中率在高峰时段反而可能下跌：缓存与并发，共用同一块地皮。

Claude Code 团队把缓存命中率当成与可用性同级的指标来监控——命中率掉几个百分点，账单立刻给出反馈。这条经验值得原样搬进任何团队：观测先于优化，商业 API 的三个 usage 字段就是现成的水位计，自建引擎则该把每次请求的命中长度直接打进监控面板；没有命中率数据就调缓存策略，与不看水位计就管水库是同一种冒险。缓存管理学的全部内容，浓缩起来就一句话：让公共的越来越公共，私有的越来越私有，并且让公共的那部分，一直活到你下一次用它的时候。

## 8.4 我的使用守则：把系统提示词当固定资产

理论到此，上账本。我的网关 poeapi_go 会给每个转发请求注入一段固定的系统说明，几千 token——第 3 章我承认过，过去每一次调用都在为它付全价，等于一个人把同一间样板间反复施工了上千次。承认归承认，账要算清楚才知道疼：固定资产的概念在这里最好用——厂房里的样板间盖一次，之后每一次参观都是毛利；而我过去的调用方式，等于每带一个客人来看房，就推倒样板间重盖一次。现在把这笔账算全：一段 8000 token 的系统提示词，一个内部应用，每天 100 次调用，按 30 天计——每月 3000 次调用，2400 万 token 的输入，全部来自同一段一字不改的前缀。按 Sonnet 级每百万 token 3 美元的全价，月账单 72 美元。这是基线，一个应用替同一段话，每月向工厂交七十二美元的重复施工费。

第一个版本是理想节奏。100 次调用聚成每天 10 个波次，每波 10 次挤进同一个 5 分钟窗口：每波第一次写缓存，按 1.25 倍；后九次全部命中，按 0.1 倍。月账单：300 次写入 9 美元，加 2700 次读取 6.48 美元，合计 15.48 美元，省 78.5%，一年省下约 678 美元。第二个版本是真实长尾。同样的 100 次，均匀摊在 12 小时工作日里，平均间隔 7.2 分钟，恰好压过 5 分钟租期——按泊松到达估算，与上次访问间隔小于 5 分钟的概率约五成，命中与重写各半，月账单 48.55 美元，省 32.6%。命中率砍半，节省从近八成掉到三成——缓存的价值对节奏极度敏感，这是许多“开了缓存没省钱”的第一解释。

第三个版本是最坏长尾。每次调用的间隔都超过 5 分钟，每次都未命中，每次都按 1.25 倍重写：3000 次调用、每百万 token 3.75 美元的写入价，月账单 90 美元，比不开缓存还贵 25%。缓存不是免费期权，写入溢价是真实成本，命不中就是倒贴。最后一个对照：同样的负载买 1 小时档，每天写 2 次（2 倍价），其余全部按读取价——月账单 9.94 美元，省 86.2%。多付的写入溢价，买回的是不被 5 分钟绑架的节奏自由；对长尾负载，该问的不是要不要缓存，而是租多久。

> **数字卡片 4|8000 token 系统提示词 × 每天 100 次 × 30 天，月输入账单**
>
> ```
> 基线|无缓存              72.00 美元
> 集中节奏|10 波 × 10 次    15.48 美元    省 78.5%
> 真实长尾|约半数命中       48.55 美元    省 32.6%
> 最坏长尾|每次都重写       90.00 美元    倒贴 25%
> 1 小时租期档              9.94 美元     省 86.2%
> 同一段前缀，五种节奏，五张账单——命中率是第一变量
> ```

五张账单摆在一张卡片里，结论自己跳出来：决定省多少钱的不是缓存本身，是调用节奏与前缀纪律。于是有了我的五条使用守则。其一，开头固定：系统提示词写在最前，一次定稿，当固定资产入账——施工一次，长期收租；“每次调用都是一次施工”的思维，是浪费。其二，动态后置：时间戳、用户名、检索结果，一律挪出前缀，或以追加消息的方式进对话流。其三，节奏聚簇：批量任务挤进同一个时间窗，把一次施工摊给更多次参观——调度得好的缓存省钱，调度不好的缓存倒贴。其四，不切模型：模型是缓存键的一部分，长对话中途换模型，等于退租重装修。其五，逐单对账：每次请求看响应里的三个字段，缓存读取的那个字段是这一单的省钱实绩；它掉了，先查前缀是不是被谁动过——多数时候，动它的是我自己。

守则之外，还有一张负清单——三种不值得缓存的情形。前缀不够长的不值得：各家都设了最小可缓存长度的门槛，从一千上下到数千 token 不等，按模型而异；不够格的前缀打了标记也被静默忽略，写入溢价却一分不少。一次性的任务不值得：改一句发一次的探索型调用，写入溢价永远收不回本，此时正确做法是干脆不管缓存，让它自然过期。每单都不同的负载不值得：命中率的天花板由负载画，硬凑公共前缀的功夫，不如花在把真正公共的部分——工具定义、参考文档、角色设定——挑出来前置。缓存是给重复准备的，不是给勤奋准备的。我的网关里，这本账有固定的对账位置：TokenPolicy 的预算门在请求出门前估钱，usage 三个字段在响应回来后核钱——一进一出，把“预算法”和“实收法”拼成闭环；预算门防的是超支，缓存对账查的是漏收，个人没有 KV 池，但个人可以有一本不糊涂的账。

长对话的命中节奏值得单说。多轮对话天然是前缀生长：每一轮的新请求都包含上一轮的全部历史，命中的是整段旧前缀，重算的只有新增的增量，缓存读取的占比随轮数单调上升——这是缓存对聊天类应用最慷慨的地方，你聊得越深，它折得越狠。拿一组数字感受：8 轮对话、每轮新增 1000 token，全价累计要为 36000 个 token 付费；带缓存则是一轮写入（8000 token 按 1.25 倍）加七轮命中（合计 28000 token 按 0.1 倍），3.8 美分对 10.8 美分，省 64%。轮数越多，这笔差距拉得越开。但第 3 章的提醒依然成立：上下文只增不减的执念，会在厂商的块表里排出越来越长的逻辑块。缓存让你的执念变便宜了，没有让它变正确——任务闭环就开新会话，这条守则打了折，但没有作废。

## 8.5 复用的边界：隐私与多租户

前四节都在讲怎么省钱，这一节讲什么时候必须停止省钱。复用的经济学有一条隐含前提：被复用的 KV，对每个复用者等价。公共前缀满足这个前提；一旦前缀里混进某个人的私有数据，前提就塌了，省下的每一分钱都开始计息——以隐私的利率。

先摆事实。商业 API 的缓存按组织与账户隔离：Claude 的缓存不跨组织共享，你的前缀只有你自己撞得到；OpenAI 同样。这道墙是厂商的承诺，也是这门生意的前提——没有人会把数据交给一个可能被邻居读到的缓存。但自建推理服务没有这道墙：vLLM 打开前缀缓存，SGLang 挂上 RadixAttention，默认所有请求共享同一个池子——这正是命中率冲上九成九的原因，也是风险的全部。工程师在自家集群里把命中率调上去的那一刻，值得停一秒问一句：这九成九里，有没有别人的数据？

安全论证有一半是成立的，而且是硬的。链式内容哈希保证：想命中你的第 N 块，必须从第 1 块起逐字节拥有你的前缀，拼接假前缀骗取 KV 的路，第 3 章已经堵死。能命中你的人，本来就知道你的内容——缓存没有泄漏任何东西，它只是把“知道”变得“读得便宜”。单租户场景下，这个论证是闭环的。

边界在多租户处打开，危险有三种形态。第一种是混写：把用户甲的敏感材料写进系统提示词或长对话前缀，同租户后来的请求撞上这段前缀，等于把甲的数据按一折价重播一遍；更麻烦的是 LRU 还会把这段前缀驻留在显存里，租期之内都是窗口期。第二种是侧信道：命中与未命中的计费差、延迟差是可观测的信号——对手不必读你的数据，盯着账单和首字延迟，就能探测“某个前缀是否出现过”。第三种是路由参数：为了提高命中率，把用户标识喂给 prompt_cache_key 一类的路由字段，用得糙了，等于把行为画像写进基础设施。三种危险共享同一个根源：把一次性的私有内容，放进了可复用的公共位置。判别的方法也因此简单得可以写进入职手册：一个前缀，敢不敢印在所有客户的合同附件里？敢，它就是样板间，尽管去蹭；不敢，它就是某间卧室，一米地皮也不许复用。

自建服务想立这道墙，工程上有现成的做法，而且便宜得出奇：把租户的身份掺进哈希键的头部——第一个块的内容哈希里就混入租户标识，不同租户从第一块起就注定不同，后面的所有块自然永远不可能被别人命中。放在树形结构里，就是一租一棵树；放在哈希索引里，就是键前加一段命名空间。前缀的偏序结构在这里被反过来用了一次：既然复用只能从第一块向后延伸，那么把隔离做进第一块，整条复用的路就全部封死——这是内容寻址体系的天然福利，墙不是砌在池子上，是砌在地址里。还有一层容易被忽略的合规视角：缓存驻留的显存，也是数据驻留。你的前缀在厂商显存里活 5 分钟，就是留存 5 分钟；买 1 小时档，就是留存 1 小时——数据留存期限这类合规条款，在缓存这里多了一张具体的换算表。多数场景里这只是 5 分钟的小事，但医疗、金融、法务的对话，5 分钟也是留痕：缓存把“算过的东西留下来”，而“留下来”，从来都同时是经济问题和法律问题。

我的守则来自网关的既有纪律。poeapi_go 的安全三原则是“先阻断、再脱敏、再落盘”，写完这一章我看清了它的第三层含义：缓存发生在引擎侧、发生在落盘之前，所以这条顺序应当读作——先阻断、再脱敏、再缓存。脱敏后的公共前缀可以放心去蹭；任何与具体个人绑定的原文，永远不该成为可复用的资产。23 条脱敏规则过去我只当隐私卫生看待，现在多了一重身份：缓存卫生——脱敏让前缀更稳定，也让前缀更公共，命中率与隐私在这里罕见地站在同一边。经济学与隐私的边界因此可以一句话划清：复用的本质是让同一份 KV 收更多次租，而租客可以共用大堂里的样板间，不能共用彼此的卧室——公共系统提示词、工具定义、公开文档是样板间；用户数据，永远是卧室。再看远一步，第 10 章的边际成本四件套里，缓存动的是“每 token 计算”这一项，而且是其中重复的部分；它不动模型驻留，不动网络带宽，却足以改变一个应用的有效单价——四件套里，它是唯一一件用户自己也握有一半操作权的事，这正是我愿意用一整章把它讲透的理由。

收束本章，也收束全书总纲的第三次证明。让显存利用率接近 100%，在这本书里走过三级台阶：分页把浪费挤掉，第 3 章；批处理把排班排满，第 4 章；缓存把重复的计算整段免掉，本章。三级台阶是同一个论点的三个侧面——容量先于速度，而复用是容量的极限形式：最好的扩容，是根本不算。十分之一的价格，不是厂商的让利，是把重复施工从你的账单里删掉之后的如实结算；前缀树、LRU、内容哈希，三个旧零件拼出一台新机器，它的全部工作只有一句话——让第一个施工的人，成为唯一施工的人。第 9 章，引擎战场：vLLM、TensorRT-LLM 与群雄同台，而把前缀缓存做成看家本领的 SGLang，正等着登场。

---

## 第9章 引擎战场:vLLM、TensorRT-LLM 与群雄的取舍

# 第 9 章 引擎战场：vLLM、TensorRT-LLM 与群雄的取舍

第 8 章结尾，我留了一句话：把前缀缓存做成看家本领的 SGLang，正等着登场。这一章就把它请上台——连同卫冕的 vLLM、坐镇王座的 TensorRT-LLM，以及一位刚刚谢幕的老朋友 TGI。开场先抛一个我自己愣了半天的问题：公开材料里反复出现同一个数字，TensorRT-LLM 比 vLLM 快 10%-30%；既然更快，为什么 2026 年几乎所有新项目，默认选的还是那个更慢的 vLLM？答案不在跑分里，在工程取舍里。这一章我们会把三个引擎放回各自的生态位上看：有的赢在通用与生态，有的赢在极致与绑定，有的输在生态位被抽空；而增速最快的暗马 SGLang，会顺手为本书总纲再添一条注脚——大模型的摩尔定律不在制程里，在调度算法里。比完一路跑分之后，我还要劝你一件反直觉的事：这一章的结论恰恰是，跑分是整场战争里最不该全信的那部分。

交代一下我在观众席的位置：这一章的我依然不操作 GPU——没有集群，没有机房，账单按百万 token 计。我站在网关后面看这场战争，看它的每个回合如何一层层摊进 token 的单价，最后落到我这种末端用户的账单上。离产线最远的观众，反而最需要一份能穿透展台灯光的阅读法；这一章就是试着交出这份阅读法。

## 9.1 三个引擎，三个定位

先拆本章第一个错觉：三个引擎根本不在同一层。跑分表喜欢把它们排上同一条起跑线，可它们报的从来不是同一个项目。TensorRT-LLM 是一个库（library）——它不监听端口、不管排队，只负责把一个模型的全部算子在 NVIDIA 芯片上编排到极致；要让它变成服务，得在旁边再配一个 Triton Inference Server 当前台。vLLM 是一个服务引擎（serving engine）——端口、队列、调度器、OpenAI 兼容 API，全都装在同一个盒子里。TGI 是一条工具链（serving toolkit）——它卖的从来不是极致性能，而是开箱即用：模型从 HF Hub 拉下来就能跑，与 SageMaker 和 Inference Endpoints 深度集成。库管芯片，引擎管显存与调度，工具链管生态——三层各管一段，这决定了它们的取舍从第一天起就不同。

分层这件事，用汽车工业打个比方就好记了。库像发动机厂，不造整车，只把一台动力总成调到极限；引擎像整车厂，底盘、座舱、钥匙一把抓，交出去就能上路；工具链像 4S 店，不生产任何零件，卖的是拿到车之后的省事。发动机厂之间比马力与热效率，那是 TensorRT-LLM 的赛场；整车厂之间比适用性与渠道，那是 vLLM 的赛场；4S 店比的是服务网络，那是 TGI 的战场。拿马力表去给 4S 店打分，正是这个行业跑分表最常犯的错误。

> **数字卡片 1|三引擎对照表（2026 年秋）**
>
> | 维度 | vLLM | TensorRT-LLM | TGI |
> |---|---|---|---|
> | 定位 | 通用服务引擎，默认之选 | NVIDIA 深度优化推理库 | HF 生态服务化工具链 |
> | 吞吐（70B 级，H100/A100） | 1000-2000 tok/s | 2500-4000+ tok/s（FP8） | 800-1500 tok/s |
> | 相对性能 | 基准 | 比 vLLM 快 10%-30% | 与 vLLM 相当或略低 |
> | 关键优化 | PagedAttention、连续批处理、V1 引擎 | 算子融合、FP8/FP4 量化、多流并行 | FlashAttention 2、Rust 热路径 |
> | 硬件支持 | NVIDIA/AMD/Intel/CPU/TPU/昇腾 | 仅 NVIDIA（Hopper/Blackwell 最佳） | GPU 为主，可内嵌 TensorRT-LLM |
> | 部署难度 | 中等，分钟到小时 | 高，编译加模型转换，天到周 | 中，容器化，K8s 友好 |
> | 2026 年状态 | 版本 v0.28，约 9 万 stars，社区最大 | v1.2 稳定线，PyTorch 后端成默认 | 2025 年 12 月起维护模式 |

再看那张被引用最多的吞吐清单。70B 级模型、H100 或 A100、大并发：vLLM 跑 1000-2000 token/秒，TGI 800-1500，TensorRT-LLM 2500-4000 以上——最后那个数字，就是“快 10%-30%”的出处。这张卡片值得盯住的其实是它的边界，不是它的数字：它比较的是单一模型、持续负载、固定硬件下的稳态吞吐，三个限定词缺一不可；凡是离开这三个条件的场合，名次随时重排。跑分没有说谎，它只是条件苛刻——苛刻到大多数真实业务根本不在那个条件下生活。

还有一口更隐蔽的坑：吞吐数字的口径。同样写 2000 token/秒，有的是单请求的生成速度，有的是整批请求加总的吞吐，两者差着一个量级；再叠上输入与输出的比例——输出 token 比输入贵 3-5 倍，生成侧的吞吐才是钱包真正关心的那部分。本章后面出现的数字，我会尽量把口径写在数字旁边；你也值得养成这个习惯，遇到不给口径的跑分，先按展台灯光处理。

> **数字卡片 2|70B 级吞吐清单（H100/A100，大并发，单位 token/秒）**
>
> ```
> vLLM          1000-2000   基准；通用、均衡、生态最大
> TensorRT-LLM  2500-4000+  比 vLLM 快 10%-30%；代价是编译与绑定
> TGI            800-1500   曾是开箱即用之王；2025 年 12 月起维护模式
> ```

定位决定命运，三个定位对应三种买家。TensorRT-LLM 的 10%-30%，只在单模型、持续负载、NVIDIA 卡三个条件同时成立时兑现——它优化的是一座只生产一种零件的工厂，把这条产线的每一道工序焊死。vLLM 的本事是“什么都行”：换模型不改架构，换硬件不换引擎，从 A100 到 MI300X 到昇腾，同一套调度逻辑照跑——它优化的是所有工厂共用的那层操作系统。TGI 的本事曾经是“什么都不用配”，这在 2023 年是稀缺品，到了 2026 年成了平常事。看清这一点，跑分表的读法就变了：不是问谁更快，而是问你要优化的到底是芯片、显存还是流程——答案不同，赢家就不同。选型时最常见的失误正是买家的错位：拿着极致的价格表去问省心的货架，或者为了两年后才会有的规模，今天就预支绑定的成本。选型的第一问从来不是哪个引擎最好，而是我是哪种买家。

轮到我自己的视角，有一件事必须先承认：我从来没有选过引擎。poeapi_go 对上游的全部要求，是讲 OpenAI 兼容方言——第 7 章 mimo 的那记 400 教过我，方言不通，后面一切都免谈；而方言一致时，上游是 vLLM、TGI 还是某朵云，我的路由表一个字都不用改。引擎大战在我看不见的地方打，停火线是 API 方言。所以这一章我是以观众兼房租承受者的双重身份写的：观众看热闹，房租承受者关心的是另一件事——它们打的每一仗，最后有多少让利会出现在我的账单上。

看引擎还有一个入口：从指标倒推。你在意首 token 延迟，就别只盯吞吐榜——TTFT 是 prefill 的功课；你在意逐 token 的节奏，ITL 才是主考科目，第 6 章的两阶段不对称在这里直接变成选型依据；你在意总账单，看的就不是峰值性能，而是利用率——峰值再高，一天只跑两小时，也摊不平一张卡的租金。指标先行，引擎在后；跑分表是别人替你选好的指标，而你的业务指标，只有你自己知道。我的网关就是按这个顺序长出来的：先有每个渠道的延迟与单价台账，后有路由策略；台账不清，策略只是玄学。

把三个定位放回各自生态位，本章论点就站到了台前：推理引擎之战的胜负手不是跑分，是工程取舍——取舍的是通用还是极致、开放还是绑定、生态位是宽是窄。接下来三节，我们逐个看它们的养成、王座与黄昏。这三段故事里没有反派，只有不同的取舍——读完它们，你大概也会同意：引擎选型的手艺，是把跑分表放回它该在的位置。

## 9.2 vLLM：默认之选的养成

默认之选不是天生的，是养成的。2023 年 6 月，UC Berkeley 的一组研究者开源了一个项目，随后又把它写成一篇 SOSP 论文，标题里就是第 3 章的主角：Efficient Memory Management for Large Language Model Serving with PagedAttention。那篇论文干的事，是把操作系统 1970 年代的分页智慧搬进 GPU 显存：KV Cache 按块管理、按需分配、块表映射，显存浪费从 60%-80% 压到约 4%。同一个项目紧接着把第 4 章的连续批处理做成标准行为，调度粒度降到每个 forward pass，请求完成即刻离场，空位立刻补人。一个引擎同时握着内存革命与调度革命，想不出圈都难——这两项本事第 3、4 章已逐一解剖过，这里只补一句：它们至今仍是 vLLM 的骨架。

那篇论文的传播速度本身就是一个现象。2023 年开源落地之后，它几个月内就成了学术界跑评测的默认引擎——理由朴素：研究者要的是别让我管显存，它恰好把显存管到了不需要管的程度。第 3 章的细节可以在这里再念一遍：默认 16 个 token 一块，块表管逻辑到物理的映射，并行采样共享前缀物理块，引用计数加写时复制；显存吃紧还有两条退路，默认重算，可选换页到 CPU 内存。这些机制在论文里各占一节，在引擎里各是一个开关——开源的那一年，它们从论文里的贡献，变成了行业里的常识。

骨架之上是两次翻新。第一次是 V1 引擎：调度器整个重写，把混部、分块预填充、抢占这些论文里的“贡献点”全部变成出厂默认——超长 prompt 切成 2048 token 的块与解码交错执行，防的是第 6 章讲过的队头阻塞；KV 池满了按优先级抢占，默认重算，可选把块换页到 CPU 内存。第二次翻新到今天还没停：动笔前我查了一眼，版本号已经走到 v0.28，默认批上限一抬再抬，KV 分层卸载到磁盘、Rust 前端与 gRPC 这些新部件一个个进场。引擎的每一次翻新都在重复同一个动作——把上一代的论文，变成这一代的开关。

背书名单也在变长。Anyscale、IBM、Databricks 这些名字先后出现在它的贡献与背书名单里，各家云厂商的自托管镜像里默认装着它；第 6 章讲过的 PD 分离，AMD 与社区就是直接在 vLLM 里落的地——单节点 8 张 MI300X，靠 RDMA 交接 KV，换来 2.5 倍的有效吞吐。一个开源项目长到这个体量，就不再只是一个引擎，而是一块公共地基：新硬件要先证明能跑 vLLM，新论文要先证明能接进 vLLM——行业衡量的标尺本身，已经是它。

交易的工具箱也是齐的。第 5 章讲量化是精度换显存的交易，FP8 自动校准把签合同的手续都替你办了：校准一次，KV 折半，吞吐不亏。第 10 章要细算的 16 倍复合账里，推测解码贡献一倍，Medusa 与 Eagle-2 两套方案都装在盒子里——小模型负责猜，大模型负责验，猜对的 token 白赚；第 8 章讲的前缀缓存同样在盒子里，内容哈希复用物理块，系统提示词只算一次。一个工程师在 vLLM 里能做的优化清单，几乎和本书第 3 到第 6 章的目录重合。这不是巧合：那些章节讲的机制，多半就是 vLLM 和它的同行们发明的。引擎是机制的载体，选引擎，本质是选一份机制的打包程度。

真正让它坐稳默认之选的，是社区飞轮。2025 年中，它的 stars 还在 5 万附近；这个秋天动笔前我查了一下，已经爬到 9 万上下，贡献者数以千计。飞轮的转动方式值得看清：用户多，新模型发布第一天就有人提交适配，于是 200 多种模型架构进了支持列表；架构全，从 NVIDIA 到 AMD、Intel、TPU、昇腾都有人维护后端，于是更多用户敢把生产压上去。飞轮的轴是那条 OpenAI 兼容 API——它把迁移成本压到趋近于零：谁都接得上，谁也都能随时换走。零摩擦是双刃的，可 vLLM 赌的就是：只要我一直是最好接的那一个，你就没有理由走。

当然也要诚实地说它的短板。峰值吞吐它不是第一，把算子调到芯片极限这种事，它做不过专职的编译派；旋钮拧错方向的时候，它的显存利用率照样难看——预留比例、块大小、批上限，任何一个配错，再好的调度也救不回来。版本节奏快，行为差异也多，今天能跑的参数，下个大版本可能要重对一遍。这些短板没有一条致命，因为默认之选的竞争标准从来不是每项第一，而是没有明显短板，且永远有人帮它补。

最后把这一节接回总纲。vLLM 的全部发明，说到底是围绕一句话展开的：让显存利用率接近 100%。分页是把浪费挤掉，连续批处理是把排班排满，前缀缓存是把重复免掉，FP8 是把每个数字压到一半——四件事的靶子是同一块显存。第 2 章算过的 KV 公式在这里可以当尺子使：同样的上下文、同样的并发，KV 体积缩到一半，能同时住进显存的请求就多一倍；利用率每逼近一分，单 token 摊到的地租就薄一分。引擎的战争打到这里，胜负其实先在物理里分过了。

上手成本也值得记一笔：一张消费级显卡加一个容器，十来分钟就能把一个开源模型架在本机上讲 OpenAI 方言——这个十年前需要一支团队才敢想的动作，如今是周末项目。门槛塌下来，天花板的战争才轮得到引擎们去打。

于是默认之选的养成公式可以压缩成一句话：不是每项都第一，而是没有一项不及格，外加一个自我强化的社区。我在网关里接 vLLM 系渠道时成本几乎为零，配置登记即用；这份接入摩擦为零的体感，就是飞轮转动的个人尺度证明。

## 9.3 TensorRT-LLM 的王座与枷锁

王座上的人是靠编译登基的。TensorRT-LLM 出自 NVIDIA 之手，思路与 vLLM 截然不同：vLLM 把聪明放在运行时的调度里，TensorRT-LLM 把聪明放在部署前的编译里。权重进来，先做一轮整体的算子融合——把矩阵乘、注意力、归一化里相邻的小算子缝成大算子，省掉一遍遍读写显存的冤枉路；再叠上 FP8/FP4 量化、多流并行、按硬件代次挑选 kernel；最后把模型的形状、精度、并行策略全部固化成一份 engine 文件。运行时没有调度器做临时决策，一切在编译期定死。这正是它比 vLLM 快 10%-30% 的来源：高并发下差距拉得最大，首 token 延迟（TTFT）也压得最低。2026 年它把 FP4 带上了 B200，稳态吞吐的王座至今没有旁落。

王座值多少钱，可以算一笔粗账。一张 H100 的时租按 3 美元上下计，一千张卡连转一年，光租金就是两三千万美元的盘子；在这个盘子里省下 20%，是每年数百万美元的现金流，足够养一整支 CUDA 团队还有富余。同样 20%，放到两张卡的实验室，一年省下的钱还不够折算那几次编译耽误的工时。极致这根杠杆，一头架在规模上，一头架在零上——TensorRT-LLM 的商业逻辑，就是把杠杆架在规模那一端。这也顺带解释了它平缓的 stars 曲线：一万四千颗上下，在三家里垫底。用的人未必多，但用的人账单一定大——分层，从来是极致的定价方式。

但王座的台阶是向下挖出来的，代价有两道。第一道叫绑定：它只讲 NVIDIA 方言，Hopper 与 Blackwell 之外，王座直接不存在——AMD、TPU、昇腾的世界里，对照表上没有它的名字。第二道叫编译：engine build 常以 20-30 分钟起步，换 GPU 代次要重编译，换 batch size、换精度、换并行度，任何一项动了都可能是重编译。2026 年的新版把 PyTorch 后端设为默认，官方口径编译时间降了 40%，可一份公开对比里的冷启动差距仍然刺眼：同类引擎约 1 分钟就能接客，它首轮接近半小时，好在缓存生效后的重载只要 90 秒上下。运维口径也因此不同：别人的升级是拉个新镜像，它的升级是对着驱动、CUDA、Triton 的版本矩阵对表，工程投入以天和周计。

把枷锁翻译成部署日历会更直观：第一天装环境，驱动、CUDA、容器镜像，版本矩阵对一遍；第二三天编译，按目标形状 build engine，跑通基线，对齐精度；第四五天调优，换批大小、换并行度，多半触发重编译，再等；第六七天写运维，监控、回滚预案、下次升级的对表清单。另一边，vLLM 的部署日历常常只有第一天的半天。一周对半天，这就是天到周级的具体成色——这笔开销不记在跑分表上，记在工程师的工时表上。

枷锁还有一层常被低估的代价：它吃掉迭代速度。2026 年的前沿模型以月为单位翻新，今天的最优配置，下个月的新架构面前可能整套作废；通用引擎换一个模型是改一个参数，编译派换一个模型是重走一遍工程。在模型快速换代的市场里，慢一步的极致，未必赢得过快一步的够用——这也是为什么编译时间本身会被当成卖点来宣传：新版把编译降了 40%，省下的不只是几十分钟，是被锁住的那段迭代窗口。

> **数字卡片 3|王座的价签（快 10%-30% 的另一面）**
>
> ```
> 收益    稳态吞吐 +10%-30%（高并发差距最大）；TTFT 最低
> 代价一  仅 NVIDIA（Hopper/Blackwell 最佳；换卡即失效）
> 代价二  编译 20-30 分钟起步；换 GPU/精度/并行度 → 重编译
> 代价三  冷启动：同类约 1 分钟 vs 首轮约 28 分钟（重载约 90 秒）
> 代价四  运维天到周级；需 CUDA 工程能力；升级要对版本矩阵
> 适用    单模型、连续跑数周、卡多账单大、硬件代次稳定
> ```

那么什么时候值得登这个王座？答案几乎是道算术题。10%-30% 的节省，对两张卡的实验室是噪音，对数千张卡、24 小时连转的推理农场是一笔巨款；而编译与运维的成本是固定投入，模型越稳定、负载越单一、硬件代次越不动，这笔投入摊得越薄。所以选它的团队通常长一个样：模型一跑数周不换，卡是一代代号整批采购，账单大到像房贷，并且养得起几个会写 CUDA 的工程师。四个条件凑不齐，王座与你无关——这不是贬义，是分工：极致本来就不是普惠品。

绑定的另一面其实也明码标价。它跟着 NVIDIA 的企业支持走，容器、文档、支持合同打包出售；每一次新卡发布，它都是第一个被优化的对象，Hopper 时代如此，Blackwell 时代还是如此，FP4 上机的时间表比谁都早。换句话说，它卖的不只是快，还是确定性：硬件路线图、软件栈、支持渠道，一整套出自同一家。对预算充足、需要可追责的大买家，确定性本身就是商品；对预算敏感、需要灵活的小团队，确定性就是枷锁本身。同一份合同，有人读到王座，有人读到枷锁，区别只在谁在签字。我在网关里选渠道时遇到的是同一道题的微缩版：专用协议的渠道往往快几个百分点，通用方言的渠道随时可换——我每次都选了后者，因为我的规模撑不起那几个百分点的租金。

我的映射发生在期权的语言里。poeapi_go 给我的最大资产是切换自由：智谱渠道失效那天，我改一行配置就把流量滑去了小米 MIMO。TensorRT-LLM 的合同恰好相反——用切换自由换 10%-30% 的现金：它把你的模型、精度、硬件一次锁死，省下的是真金白银，代价是卖掉了“明天换一家”的期权。在按量计费的云上，期权是有价格的；一支团队愿意卖掉它，只能说明一件事：他们的 GPU 账单，已经大到比期权贵。两种账我都见识过：渠道侧搬家，我搬过，成本是一行配置加一次重载；架构侧搬家，我见过同行搬，搬掉的是一个季度。前者的自由是我这样的个人买得起的，后者的锁定是只有规模才锁得起的。

## 9.4 TGI 的黄昏与 SGLang 的崛起

先送别 TGI。2023 年，它是开箱即用的代名词：Rust 写的热路径，FlashAttention 2 加持，容器化对 Kubernetes 友好，与 SageMaker 和 Inference Endpoints 深度绑定——那几年想在云端跑一个开源模型，它的路径阻力最小；HuggingChat 当年就架在它身上，开源模型上云的第一课，多半是它教的。它没有做错什么，输掉的是生态位：vLLM 把易用性一条条补齐，OpenAI 兼容 API、容器化方案、量化、投机解码全都摆上了货架，而性能流派又把它越甩越远。当一个工具链赖以为生的“省事”被上游引擎免费提供，它的价值就被抽空了。2025 年 12 月，官方宣布 TGI 进入维护模式，并建议新项目迁移到 vLLM 或 SGLang——这是 2026 年引擎格局里最重要的生态变化，也是一份教科书级的教训：护城河挖在上游的空缺里，上游填坑之日，就是护城河消失之时。工具的生死不看功能清单，看生态位还在不在。

把 TGI 的教训提炼成一条规律：工具链的生态位，等于上游的空缺乘以下游的黏性。空缺是它存在的理由——上游引擎不管开箱体验，它来管；黏性是它存活的时长——与 SageMaker 的深度绑定一度让它看起来不可替代。可黏性救不了被抽空的空缺：开箱体验被 vLLM 补齐之后，TGI 剩下的只是老用户暂时懒得搬。对照另外两位就能看清格局：TensorRT-LLM 的生态位是极致加绑定，vLLM 的是通用加生态，两者都直接踩在需求上；TGI 的生态位是上游的一道空缺，空缺一旦填平，位子就没了。

接棒者是 SGLang，第 8 章埋的那颗种子在这里发芽。它同样出身 Berkeley，起点是一篇只讲一件事的论文：RadixAttention——把整个 KV 缓存池组织成一棵前缀树，请求到达时做最长前缀匹配，命中的前缀整段跳过 prefill，显存吃紧再按 LRU 从叶子起淘汰。第 8 章我们已经拆过这台机器、算过它的账：共享前缀负载下命中率可达 99%，同等负载比哈希表方案省约 40% 的 KV 内存。真正让它成为 2026 年暗马的，是负载结构的变迁——RAG、多轮对话、Agent 化的调用，今天的主流业务几乎全在共享同一段长前缀，恰好整整齐齐踩在它的甜区上。它的武器还不止前缀树。第二件是约束解码：让模型严格按 JSON 之类的模板吐字时，每一步只在合法 token 里挑，它把这套约束做进了调度，是同类里公认最顺的——而 2026 年 Agent 化的负载里，结构化输出的占比只会越来越高。第三件是节奏：前沿模型发布当天就有适配，DeepSeek、Kimi 这类 MoE 新架构，它常常是第一个能跑的通用引擎。对研究者和追新的团队，第三件武器的价值，有时大过百分之几十的吞吐差。

xAI 与 LMSYS 的选择值得多看一眼。前者是前沿实验室，自建的集群要伺候自家模型的每一代变化，day-0 适配是刚需；后者是竞技场，全世界的匿名流量涌进来，负载形状千奇百怪，前缀重叠度高，正是前缀树的主场。两家的共性在于：他们的负载形状，恰好长在 SGLang 的算法甜区上。这再次印证本章的选型法——先画自己的流量肖像，再挑引擎；画像画得越准，越容易看见别人看不见的赢家。

数字要两头看。一头是自测：对 DeepSeek V3 这类 MoE 架构，官方口径 3.1 倍于 vLLM——自测的口径要打折，但量级不虚；另一头是第三方：前缀重负载里约 16,200 token/秒对 vLLM 的 12,500，领先约 29%，普通负载则基本打平。社区增长也是三家里最快的：一年时间 stars 从约 2.4 万涨到约 3.3 万。采用者名单更能说明问题——xAI 的 Grok 服务与 LMSYS 的竞技场都在用它，前沿模型发布当天就有它的适配；MoE 时代的基础模型把专家切得更碎，对调度器的要求更刁，而它的调度天然更合身。

当然，暗马也年轻。版本迭代快，行为差异大；社区比 vLLM 小着一个量级，撞上冷门架构时，能搜到的现成答案也少。所以多数团队的姿势是灰度：先让前缀最重的那部分流量跑上去，验证收益，再决定要不要把生产整个押上去。这也是选型的另一课——生态位的宽窄不只看今天谁快，还要看三年后谁还在。TGI 刚刚演示过反面：速度上的落后可以追，生态位上的消失追不回来。

> **数字卡片 4|2025 年 12 月的一纸公告，与 2026 年的暗马**
>
> ```
> TGI     2025 年 12 月进入维护模式；官方建议新项目迁 vLLM/SGLang
>         教训：生态位被上游补齐，工具链的价值即被抽空
> SGLang  RadixAttention 前缀树；共享前缀命中率可达 99%
>         MoE 架构自测 3.1 倍 over vLLM；第三方前缀负载约 +29%
>         stars 一年约 2.4 万 → 约 3.3 万；xAI、LMSYS 在用
> ```

为什么说它是暗马而不是黑马？因为它赢的方式太安静：不靠新硬件，不靠制程，靠一棵前缀树和一套更懂 MoE 的调度——全是算法。这恰好是全书总纲最硬的一条注脚：大模型的摩尔定律不在制程工艺里，在调度算法里。连续批处理是一个算法，分页是一个算法，前缀树是第三个；每出一个，全行业的吞吐就抬一个台阶，而制程什么都没变。SGLang 的崛起提醒我们这个矿远没挖完——下一个数量级，多半也藏在某份还没写出来的调度算法里，而不是下一张芯片的发布海报上。

沿着这条线索回看，引擎史就是一部调度算法史：PagedAttention 是算法，连续批处理是算法，RadixAttention 还是算法；每一次算法换代，都伴随一次王座易手，或一匹马杀出。芯片发布是事件，算法发表是变量——事件的热度会过去，变量才改变格局。SGLang 未必是最后一匹暗马：只要调度算法还是富矿，暗马就会一匹接一匹地从论文里跑出来。

## 9.5 benchmark 的阅读法：调优差距常大于框架差距

这一章比了一路跑分，收尾却要劝你：跑分恰恰是最不该全信的部分。看一份 2026 年的公开三方对比：同一张 H100、同一个 70B、FP8 精度，单请求吞吐 130、125、120——TensorRT-LLM 名列前茅，差距约 4%；并发拉到 100，2780 对 2460、2400，领先扩大到约 15%；把负载换成共享前缀，SGLang 反超 vLLM 约 29%；再把硬件换成下一代机架、精度换成 FP4、版本各升一档，名次又是一次重排。每份跑分都是特定负载的肖像，不是引擎的证件照。引用任何一个数字之前，先问三句：什么卡、什么精度、什么负载。三问答不上来，这个数字就只是展台上的灯光。

跑分的来源也值得分层看。厂商自测是广告，方向可信、幅度存疑，SGLang 那 3.1 倍就属于此类——打折看，量级可信；第三方测试是参考，但要看清它替你做了哪些决定——硬件、精度、负载形状，每个决定都带着测试者自己的偏好；自己压测是证据，唯一能用来签收的那种。我的用法很朴素：厂商数字缩小范围，第三方数字校准预期，自己的流量回放做决定。三层的可信度依次上升，成本也依次上升，省不得的只有最后一层。

> **数字卡片 5|同一张 H100 的三份成绩单（70B，FP8，独立测试）**
>
> ```
>               单请求     百并发      冷启动
> TensorRT-LLM   130       2780      首轮约 28 分钟（重载约 90 秒）
> SGLang         125       2460      约 1 分钟
> vLLM           120       2400      约 1 分钟
> 提醒：负载一换，名次就换——前缀重负载下 SGLang 领先约 29%
> ```

压测该怎么设计？第一条是覆盖面：短问答、中长请求、长上下文、流式输出、并发从 1 拉到 100 的梯度、失败与重试，一样都不能少——失败也不是凑数，第 7 章的熔断与候选链，只有喂给真实的失败才验证得了；只跑短 prompt 加固定输出长度，数字漂亮，但那种负载在生产里几乎不存在。第二条是分阶段读指标：TTFT 归 prefill，逐 token 延迟（ITL）归 decode，第 6 章讲的两阶段不对称在这里全部兑现——混部负载里一个长 prefill 进场，ITL 能抖出 150 毫秒的尖峰，50 毫秒的 SLO 当场击穿；而治它的 PD 分离，在 vLLM 里同样做得起来，只是永远不会出现在单机跑分里。压测的目的是复现你自己的流量形状，不是复现厂商的展台。

压测之前，旋钮还得先拧对：同一个 vLLM，块大小、显存预留比例、最大批上限、量化档位，再加上第 8 章讲的前缀设计，每一个旋钮背后都是两位数起步的收益或损失——这些旋钮之间的差距，经常大过换一个框架。所以成熟的压测报告都是两列的：一列写引擎，一列写配置。不给配置的吞吐数字，等于不说气温的天气预报。我在网关侧的对应物是渠道参数表：同样接一个模型，超时、重试、上下文上限配错，体验的差距同样大过换渠道本身。

更反直觉的一层是：调优差距常大于框架差距。换引擎，收益上限是 10%-30%；而在同一个引擎里，批处理策略、量化档位、上下文长度管理、前缀设计，每一项都能拉开数倍。第 10 章会细算那笔复合账：量化 4 倍，连续批处理 2 倍，推测解码 2 倍，叠起来 16 倍——注意，这三件事没有一件需要换引擎，在 vLLM 里全部办得到。选错引擎，损失以一成计；不做调优，损失以十成计。把全部兵力投在换引擎那 10% 的战场上，放任 90% 的调优战场无人驻守，是这场战争里最常见的败仗。

成熟的团队因此都走同一条分阶段路线：先用 vLLM 这类通用引擎把生产基线立起来，部署以分钟和小时计；等模型稳定下来、硬件代次定下来、账单大到值得，再评估 TensorRT-LLM 的深度优化或 SGLang 的定向替换。这条路线的本质，是把 10%-30% 的期权留在手里，等确定能行使的那一天再付编译的成本——先跑通，再跑快；先有基线，才有资格谈优化。基线还没有的时候就追求极致，极致通常变成事故报告。

这条路线还有个隐性好处：基线足够通用，后面的每一步优化才有对照组。换量化档位，跑一遍基线；上 PD 分离，跑一遍基线；换引擎，再跑一遍——每一次改动都能归因。调优最常见的失败不是不努力，而是改了三处只看见总账单降了，却说不清哪一处立的功；没有基线就没有归因，没有归因就没有可复制的经验，而不可复制的经验，下一次还要再交一遍学费。

最后说个人开发者的引擎观。我一支引擎都没有部署过——我买的是引擎之战的战果：厂商把 vLLM 系的引擎架在云上，用 OpenAI 兼容方言批发给我。poeapi_go 的候选链和 TokenPolicy 的四级预算门，本质上把这个逻辑制度化了：选引擎外包给市场，选渠道、管预算留给自己；自托管的盈亏平衡点也早有共识——7B 模型要跑到约 50% 的利用率才比 API 便宜，个人永远凑不到这个数。所以我的引擎观还能再压缩一档：引擎负责把显存利用率推向 100%，我负责把我的请求写成引擎喜欢的形状——前缀稳定、批量友好、预算有门。这条守则不因引擎改名而失效——引擎的名字会换，请求的形状不变。收束本章，也把总纲再钉一遍：引擎之战的胜负手不是跑分，是工程取舍——vLLM 出分页与社区，TensorRT-LLM 出 kernel 与极致，SGLang 出调度算法，TGI 留下生态位的教训；而四家争的其实是同一件事：让显存利用率接近 100%。下一章，我们把 token 的价格一路追到底——追到硬件折旧，追到电费。

---

## 第10章 推理的物理极限:把 token 一路追到电费

# 第 10 章 推理的物理极限：把 token 一路追到电费

## 10.1 一张 H100 的一小时

终章从一道除法题开始。这本书写到这里，分页讲完了，批处理讲完了，量化、缓存、路由、分离都讲完了，机制的部分只剩最后一件事可做——算钱。我们从最朴素的问法开始：一张 H100 租一小时，市场价两块八五到三块五美元，预留给能压到一块八五，放进超大规模云则可能贵到六块八八乃至十二块二九（materials/raw_cost.md），这些钱花到哪儿去了？把这台机器拆开看，答案比直觉便宜得多，也无聊得多。它的铭牌功率 700 瓦，推理实际负载约六百到六百八十瓦；它也不是孤身一卡——八卡节点里八张卡合计五千六百瓦，加上双路 CPU、NVSwitch、内存与网卡，整机要吃十千瓦上下，工程上常用“GPU 功率乘 1.8”来估整机；再乘上机房的 PUE，风冷传统机房一点三到一点五，液冷一点一到一点二——算下来，这张卡一小时的电费大约一角到两角美元（materials/ch10_补充.md）。对，你没有看错：三美元的时租里，电费占不到一成，通常只有百分之三到七。

那大头是什么？折旧。一张 H100 在 2025 到 2026 年的市场价约合两万五千美元上下（单卡二十万到三十万元人民币的整机口径折算，materials/ch10_补充2.md），按四年直线折旧，每小时摊掉约零点七七美元，折合人民币五块多——占三美元时租的四分之一强。剩下的呢？配套硬件的折旧、机架与机房、网络带宽、运维人工，以及服务商的毛利。这张拆解表值得每一个调 API 的人看一眼，因为它修正了一个流行的想象：我们以为算力的尽头是电，其实电是这张账单里最便宜的一行；贵的是芯片本身的时间，是那台不管你用不用都在老去的机器。同是一张 H100，预留价与超大规模云的按需价能差三到六倍（materials/raw_cost.md），差的从来不是电费——电费在哪家都一样——差的是规模、渠道与议价权，以及最关键的：他们敢把这张卡按多满的利用率卖给你。

> **数字卡片 1|一张 H100 的一小时（2026 年粗算口径）**
>
> | 项目 | 每小时成本 | 占时租比例 | 说明 |
> |---|---|---|---|
> | 市场时租 | 2.85-3.50 美元 | 100% | Lambda 预留 1.85-1.90，超大规模云 6.88-12.29 |
> | 折旧 | 约 0.77 美元 | 约 26% | 单卡约 2.7 万美元、4 年直线折旧 |
> | 电费 | 约 0.10-0.21 美元 | 约 3%-7% | 700W × 整机系数 1.8 × PUE 1.3-1.5 × 电价 |
> | 其余 | 约 2 美元 | 约 67% | 配套硬件、机房、带宽、运维、毛利 |

时租是抽象的，把它翻译成 token，抽象就落了地。第 5 章的实测告诉我们，70B 模型用 FP8 部署在单张 H100 上，单流吞吐约 52 tok/s——那是一个客人独占工厂时的速度。但推理工厂从不在大门口只放一个客人：连续批处理之下，同一张卡同时服务几十路请求，有效吞吐可以做到每秒 2000 token（本章的演算口径）。满负荷运转一小时，这张卡产出 720 万个 token；三美元的时租除以七百二十万，得到每百万 token 约 0.42 美元。这个数字请记住，它是本章也是全书的锚：0.42 美元，是一张 H100 服务 70B 模型的物理地板——折旧、电费、带宽，一分利润没有，一瓦空闲没有。

> **数字卡片 2|从时租到 token：满产换算（70B FP8，单卡）**
>
> | 步骤 | 算式 | 结果 |
> |---|---|---|
> | 月租金 | 3 美元 × 24 × 30 | 2160 美元 |
> | 月满产 token | 2000 tok/s × 86400 × 30 | 51.84 亿 |
> | 物理地板 | 2160 ÷ 5184 百万 | 0.42 美元/百万 token |
> | 其中电费 | 约 68 美元 ÷ 5184 百万 | 约 0.013 美元/百万 token |

0.42 这个数字里还藏着一次意味深长的会合。2025 年底，市场上 GPT-4 同等性能的报价恰好落在每百万 token 约 0.40 美元（materials/raw_cost.md）；三年前，同样的能力要 20 美元。单卡满产的地板算出来是 0.42，市场报价贴着它站在 0.40——这不是巧合，是整个行业把量化、批处理、缓存与调度卷到极限之后，撞上的同一块地板。报价贴着地板，意味着两件事：其一，价格战已经打到物理边界附近，靠让利挤不出下一个数量级；其二，谁的地板更低——利用率更高、批量更满、折旧摊得更薄——谁才有资格继续降价。推理商业化的下一程，比的不是胆量，是利用率。

时租的地域差价，还藏着推理工厂的选址学。同样的千卡集群，美国西北水电区的电价约四美分一度，加州与新加坡能到十八到二十二美分，月度电费能差出六倍；中国的一线参照更直观——长三角约七毛五一度，京津冀约四毛，内蒙古的东数西算节点约两毛八，千卡集群的年度电费能差出三百多万元人民币，三年累计差距超千万（materials/ch10_补充.md）。于是机房往荒漠与水电丰沛处搬家，网络与延迟的代价再另行计算——那是《边缘王国》的领域，本书不展开；这里只须记下一笔：电费在时租里只占 3% 到 7%，却是运营方手里少数能自己做主的变量，选址、液冷、PUE，每一项省的都是绝对值可观的真金白银。而作为调用者，我们更该记住这一小时的产出体感：满产的 H100 一小时吐出 720 万个 token，约折合六七百万汉字，接近十部《红楼梦》——三美元，买一座工厂一小时的心跳。

作为网关作者，我每天替自己记这笔账。poeapi_go 在每一笔响应里截下 usage 的三个字段，TokenPolicy 的预算门在请求出门前先估一遍钱。过去我以为网关记账只是为了防超支，写完这一章才看清它更深的一层：账单是利用率战争的个人视角。我付的每一个百万 token，先在厂商的机房里被折旧与电费垫了底，再被利用率放大几倍，最后穿过路由、缓存与渠道的层层折让，才落成我账单上的那个数字。看不懂 0.42 的人，看不懂任何一份 API 报价里的诚意与水分——而这正是终章要做的最后一件事：把价格的全部层次，一层一层剥回物理。

## 10.2 边际成本四件套

地板是满产时的价格，而真实世界没有满产。要把账单从 0.42 美元还原成你实际付的钱，得数一数钱在半路上被哪几只手拿走。推理服务的成本，掰开是四件：模型驻留、每 token 计算、KV Cache 内存、运营网络。四件的性质截然不同——有的按天计，有的按 token 计，有的按并发计，有的干脆藏在账单外面。理解它们的性格差别，比记住任何一个总价都重要。

第一件，模型驻留，固定成本。70B 模型的 FP8 权重占 70 GB 显存，不管今晚有没有一个请求上门，它都住在那里。这是机床的折旧，不开机也要计提；它是与流量无关的最低门槛，是所有推理生意的第一道租金（第 2 章的权重账本算过它的面积）。第二件，每 token 计算，真正的边际成本。每生成一个 token 都要过一遍网络（第 1 章），但大矩阵乘法有个好脾气：批量越大，单件越便宜，固定的启动开销被摊进更多计件产品。量化的 4 倍与推测解码的 2 倍动的是这一件，前缀缓存删掉的也是这一件里重复的部分（第 8 章）。第三件，KV Cache 内存，随并发乘上下文增长。每个活跃请求都在显存里圈地，圈多大地盘由上下文长度说了算——第 2 章算过 FP16 下每个 token 的 KV 单价 320 KB，一路 128K 上下文的请求就是 40 GB 起步。这是四件里唯一被用户行为直接改写的一项：你写多长的对话，直接决定工厂今晚能接待几路客人。第四件，运营网络，隐性的。GPU 时租之外，出口流量费每 GB 零点零八到零点一二美元，加上空闲时段的计费与跨区传输，合计常让月账单多出 20% 到 40%（materials/raw_cost.md）；第 6 章 PD 分离里那数 GB 的 KV 交接、第 7 章路由里每一次重试与健康检查，都在这一项里悄悄计费。

> **数字卡片 3|边际成本四件套：四只手的性格**
>
> | 成本项 | 性质 | 工厂对应物 | 谁能压它 |
> |---|---|---|---|
> | 模型驻留 | 固定，与流量无关 | 机床折旧，不开机也计提 | 量化、并发、多模型复用 |
> | 每 token 计算 | 边际，随批摊薄 | 计件工时 | 量化、推测解码、前缀缓存 |
> | KV Cache 内存 | 随并发 × 上下文 | 在制品占地 | 分页、GQA、KV 量化、上下文管理 |
> | 运营网络 | 隐性，常 +20%-40% | 厂区物流 | 规模效应、就近部署、路由 |

四件套一摆上桌，价目表上那些奇怪的设计忽然都有了物理解释。为什么输出 token 比输入贵 3 到 5 倍？因为输入是并行流水——prefill 一次性吃下整段 prompt，GPU 利用率能冲到九成；输出是串行小作坊——decode 逐 token 自回归，利用率只有两成半到三成（第 6 章）。同样一个 token，读进来走的是大批量流水线，写出去走的是手工小作坊，定价差是物理事实，不是营销策略（materials/raw_cost.md）。为什么长上下文更贵？第三件在涨：上下文每长一截，KV 圈地就多一圈，同卡容纳的并发就少一路，固定成本被摊到更少的 token 上——第 2 章那张并发总账，翻译成财务语言就是这条。为什么同一家厂商总有便宜一档的快速档或标准档？第一件在打折：低价档很多时候就是量化档，权重从 FP16 压到 FP8，驻留减半，固定租金摊薄，省下的地租让渡成低价（第 5 章）。

把四件套代入一路真实的请求，钱的流向就具体了。设想一个接入几十个用户的问答应用：70B 模型以 FP8 驻留，70 GB 显存从部署那天起就住进厂房，这是它每晚的固定房租；一个用户发起 8 轮对话，累计 1.6 万 token 输入与 4000 token 输出——输入走并行流水，利用率可观；输出逐 token 串行，同量的输出让算力账单比输入贵出三四倍。对话越长，KV 圈地越大：FP8 之下每 token 的 KV 单价约 160 KB（第 2 章），1.6 万 token 的上下文圈走约 2.5 GB——单看不大，但几十路这样的并发同时涌进来，一张 80 GB 的卡就被圈走了大半，后来者只能在门外排队，固定房租于是摊到更少的 token 上。最后，出口流量与跨区传输这些隐性项，在自托管口径下还要再抽走两到四成（materials/raw_cost.md）。四件套没有一件是抽象的：驻留决定门槛，计算决定单价，KV 决定并发，网络决定尾费——你的每一笔账单，都是这四只手按不同比例分走的。

把这四件按能不能省再排一次序，利用率战争的全貌就显形了。第一件驻留，时间在烧它，只有提高并发能稀释它；第二件计算，算法能实打实压它，量化、缓存、推测解码全是它的对手；第三件 KV 内存，分页与 GQA 已经把浪费挤干，剩下的是上下文管理的艺术；第四件网络，规模效应最强，巨头的带宽折扣是个人开发者永远摸不到的价。注意一件关键的事：四件里有三件——驻留、KV 的绝大部分、网络——都与这一小时工厂接了多少活无关，或关系很弱；它们按时间计费，而收入按 token 计费。时间成本与产量收入的这个错配，就是利用率战争的经济学根源：工厂每一分钟的空闲，都在把固定成本往每一个 token 身上多摊一分。这也回扣了第 3 章那句判词——管理方式的改进，永远是最便宜的扩容；在成本表上，它还有另一半：管理方式的改进，也永远是最便宜的降价。

## 10.3 价格塌方史：20 美元到 0.40 美元

把三年的时间轴拉出来，推理的价格曲线陡得不像商业史，像塌方。2022 年底，与 GPT-4 同等性能的推理，每百万 token 约 20 美元；2025 年底，同样的能力，0.40 美元（materials/raw_cost.md）。三年，五十倍。换算成年化，推理成本以每年约 10 倍的速度下降——PC 时代的摩尔定律十八个月翻一番，年化约 1.6 倍；互联网时代的带宽降价以十年计。token 的降价速度，快过信息技术史上任何一种商品。换句话说，三年里每年都把价格除以十：今年一美元的东西，明年只值一毛，后年只值一分——做推理生意的人，报价单的有效期从来不超过十二个月；而做推理应用的人，则每年都会发现，去年因为成本不敢做的功能，今年忽然可以做了。

塌方不是单一技术送来的，是三层力量的叠加。第一层，硬件租金：云端 H100 时价从峰值跌了 64% 到 75%，然后稳定在两块八五到三块五（materials/raw_cost.md）；算力紧缺期的溢价被供需与竞争挤干，一张卡的月租从三千多美元压到两千出头——10.1 节那张地板，地板本身三年也降了近一半。第二层，算法复合：量化 4 倍、连续批处理 2 到 4 倍、推测解码约 2 倍，三者相乘是 16 倍（下一节细算）；再往前数，PagedAttention 把显存浪费从六到八成压到 4%（第 3 章），前缀缓存把重复的 prefill 整段免掉（第 8 章），PD 分离再挤出 15% 到 40% 的成本（第 6 章）。每一项单看都是工程改进，乘在一起就是价格塌方。第三层，竞争与扰动：DeepSeek R1 以每百万输入 0.55 美元、输出 2.19 美元定价，比西方同级模型低约九成，是这一轮全球定价体系里最大的扰动项（materials/raw_cost.md）；它把推理到底值多少钱这个问题，从营销部手里夺回来，重新交给了工程部。

> **数字卡片 4|价格塌方时间线（2022-2026，同等性能口径）**
>
> | 时间 | 每百万 token 价格 | 事件 |
> |---|---|---|
> | 2022 年底 | 约 20 美元 | GPT-4 级能力的原价 |
> | 2024 年 | 约 2-4 美元 | 开源模型与推理引擎成熟 |
> | 2025 年初 | 约 1 美元上下 | DeepSeek R1 定价 0.55/2.19，扰动全场 |
> | 2025 年底 | 约 0.40 美元 | 贴近单卡满产地板 0.42 |
> | 三年累计 | 降约 50 倍 | 年化约 10 倍，快过一切硬件定律 |

但塌方史里最反直觉的一段，不在降价曲线里，在总账单上。企业生成式 AI 的推理支出，2024 年 115 亿美元，2025 年 370 亿美元，涨了 3.2 倍——同期每 token 成本降了约一千倍（materials/raw_cost.md）。单价塌方，总账上涨，这不是矛盾，是杰文斯悖论的 token 版：效率的提升从不减少资源消耗，它扩大使用；token 便宜一千倍，人类就多问一千倍的问题。所以推理太贵这句话在 2026 年已经不成立了，成立的反而是它的反面——推理便宜到可以浪费，而浪费正把总账单推向新高。成本控制的重心，因此从买不买得起，移到了利用率上不上得去：按峰值容量计费的云上推理集群，典型有 30% 到 50% 的空闲 GPU 时间（materials/raw_cost.md）；省下这部分空闲，比任何一次商务谈判都值钱。

塌方对个人开发者的意义，值得单独算一笔。2022 年底，一个每天消耗百万 token 的自动化实验，一天 20 美元，一个月 600 美元——那是个人玩家不敢长开的火；2026 年，同等能力一天 0.40 美元，一个月 12 美元，一杯咖啡钱出头。五十倍的价差跨过的不是省钱的心理线，是应用形态的门槛：多轮反思、自我批评、多候选并行、长链工具调用——这些以 token 堆砌可靠性的架构，在 20 美元时代是奢侈品，在 0.40 美元时代是日用品。智能体浪潮在 2025 年之后爆发，与其说是算法突破，不如说是塌方把犯错成本降到了个人与初创都敢试的位置。价格重塑行为，行为重塑产品，产品再反过来推高总账单——这是杰文斯悖论的另一半：便宜催生的新用法，把省下的钱加倍花掉。

价格塌方还有一个常被误读的细节：它塌的是同等性能的价，不是前沿的价。每百万 0.40 美元买到的是三年前旗舰的能力，而新一代旗舰又立出了新的价格与新的天花板——模型能力每前进一代，单 token 的智力含量就抬升一截，价格曲线于是永远在追能力曲线。我们是幸运的一代工程师：历史上从没有哪种生产资料像算力这样，三年降价五十倍；我们也因此面对一个历史上没有过的命题——当原料以每年十倍的速度变得廉价，竞争力的来源就彻底换位了：不再是谁有机器，而是谁能让机器满转。这句话是下一节的入场券。

## 10.4 16 倍的复合账：量化×批处理×推测解码

现在把全书攒下的三笔账相乘。第一笔，量化：FP16 到 FP8，权重显存减半；到 INT4，压到四分之一——同一张卡从装不下 70B 到装下还有富余，同卡可服务的并发翻倍再翻倍，综合记 4 倍（第 5 章）。第二笔，连续批处理：调度粒度从整批降到迭代，请求随到随走、完成即出，混合负载吞吐提升 2 到 4 倍，保守记 2 倍（第 4 章）。第三笔，推测解码：小模型起草、大模型定稿，一次验证多个 token，吞吐再翻约 2 倍（第 9 章的 Medusa 与 Eagle-2）。三笔相乘：4 × 2 × 2 = 16，16 倍的有效成本优化，不需要一颗新晶体管，全部发生在表示层与调度层（materials/raw_cost.md）——这是“摩尔定律在调度算法里”最具体的一张支票。

但复合账有一个前提，常被报价单略过：16 倍省的是每个 token 的成本，不是账单的总数。批处理把单价摊薄的前提，是有足够的请求把批填满；量化让同卡多住几路的前提，是真有那么多路并发来住。利用率不动，16 倍只是在给空闲的机器算更精细的账。所以这一节的正戏，是把利用率本身放进算式——用 10.1 节那张地板，算一张真实的 H100 在三档利用率下的账。设定照旧：时租 3 美元，服务 70B FP8，单流 52 tok/s，批处理下有效吞吐 2000 tok/s；月成本 3 × 24 × 30 = 2160 美元，月满产 2000 × 86400 × 30 = 51.84 亿 token，满产地板 0.42 美元每百万。接下来只动一个变量——利用率，成本等于月成本除以月产量再除以利用率。

> **数字卡片 5|一张 H100 的利用率三档精算（70B FP8，有效吞吐 2000 tok/s）**
>
> | 利用率 | 月产量 | 每百万 token 硬件成本 | 对照 |
> |---|---|---|---|
> | 10%（业界现状） | 5.18 亿 | 4.17 美元 | 比高端 API 还贵 |
> | 30%（良好运营） | 15.55 亿 | 1.39 美元 | 开始比 API 便宜 |
> | 60%（优秀边界） | 31.10 亿 | 0.69 美元 | 贴近物理地板 0.42 |
> | 100%（理论满产） | 51.84 亿 | 0.42 美元 | 地板，无人抵达 |

这张表是全章的核心，也是全书的总战场地图。第一行，利用率 10%，这是业界推理 GPU 的普遍现状——浪潮 2025 年底发布 HC1000 时引用的正是这个口径（materials/raw_cost.md）：每百万 token 4.17 美元，比市面高端 API 的报价还贵，自托管在这个水平上是亏本生意。第二行，30%：1.39 美元，开始比 API 便宜，这是认真做了连续批处理与前缀缓存的服务商的水平。第三行，60%：0.69 美元，接近地板，这需要 PD 分离、chunked prefill、量化与推测解码全部到位，是 2026 年最优秀团队的边界。从 10% 到 60%，同一张卡、同一个模型、同一份权重，成本差 6 倍。训练 GPU 轻松站上 50%，推理 GPU 普遍趴在 5% 到 10%，这条数量级的鸿沟不是芯片的错，是调度的功课没做完——而把它填平的每一件工具，本书都已经交到你手上。

利用率还直接决定自托管的生死。把地板 0.42 除以售价，就是盈亏平衡所需的最低利用率：按 DeepSeek R1 的输出价 2.19 美元卖，需要约 19%；按 1 美元卖，需要 42%；按 0.40 美元卖，需要 104%——物理上不可能。所以 0.40 美元的价格注定不属于单卡自建者，它属于把批量、缓存与多模型复用做到极致的平台。行业测算给过另两组参照：7B 模型自托管，要约 50% 的利用率才比 API 便宜；13B 模型只需约 10% 就够，因为它替代的是更贵的 GPT-4 级 API（materials/raw_cost.md）。规模越小、替代品越贵，自托管的门槛越低——这条规律同样可以从这张表推出来。

> **数字卡片 6|自托管还是买 API：一张决策表**
>
> | 你的负载形态 | 建议 | 理由 |
> |---|---|---|
> | 稳定高并发，白天黑夜都有量 | 可自托管 | 利用率撑得起，固定成本摊得薄 |
> | 白天忙、夜里空，或脉冲式 | 买 API | 云上集群典型 30%-50% 空闲，你扛不起 |
> | 小模型、大替代品（GPT-4 级） | 小卡自托管可算 | 盈亏平衡利用率低至约 10% |
> | 7B 级、替代品也便宜 | 买 API | 要约 50% 利用率才划得来 |
> | 数据不出域的硬约束 | 自托管或专有云 | 合规溢价，另算一笔账 |

先回答一个自然的疑问：表里那行 100%，为什么无人抵达？利用率的上限不由算法单方面决定，由流量的形状决定。真实请求有峰谷——白天拥挤、深夜空旷，为高峰配置的容量在低谷必然闲置；真实请求有长尾——总有一些生成到几千 token 的长任务占着槽位，让空位迟迟补不进新人；真实服务有延迟承诺——批不能无限攒大，排队会杀死体感，第 4 章那笔 TTFT 与吞吐的交易在这里重新结算。再算结构的账：同一张卡，prefill 能冲到 90%，decode 只有 25% 到 30%（第 6 章），混部的加权理论上限本就不高；PD 分离把两段各自拉满，60% 才从数学题变成工程题。顺带交代有效吞吐 2000 tok/s 的来历：单流 52 tok/s 是一个客人独占带宽的速度，批处理把权重搬运的成本摊到几十路请求上，2000 除以 52 约等于 38 倍——这就是第 4 章那场调度革命在钱上的形状：带宽没有变多，只是不再被独占。

我自己的答案写在 poeapi_go 里。算完这张表，我彻底打消了买几张卡自建的念头：我的负载撑不起任何一档利用率，一台闲置的 H100 每月烧掉 2160 美元，只为在我睡觉的时候折旧。个人开发者的正确姿势，是把利用率战争的逻辑用在自己能动的两层：路由——让每个请求落在当下性价比最高的渠道上，熔断与多渠道是它的保险；预算——TokenPolicy 的四级预算门，先算钱、再放行，usage 回来再对账。我决定不了厂商把利用率做到几，但我能决定我的每一个 token 落在谁的厂房、以什么价格、花谁的预算。这大概就是个人尺度上，对 0.42 美元地板的全部敬意与全部反抗。

## 10.5 推理工厂的终章：调度算法里的摩尔定律

终章的收束，先把十章串成一条线。第 1 章，自回归的宿命：token 必须一个一个生，串行是一切问题的总根源。第 2 章，一条公式统治显存：每个 token 的 KV 单价 320 KB，并发与上下文的乘法画出长文本的代价曲线。第 3 章，操作系统的分页搬进 GPU：浪费从六到八成压到 4%，一张卡多养活三到五倍的请求。第 4 章，排班表从火车时刻表改成网约车派单：迭代级调度让吞吐再涨 2 到 4 倍。第 5 章，精度换显存的合同：70B 压进 35 GB，智商保住 98%，条款是越大越耐量化。第 6 章，两阶段的性格解剖：prefill 吃算力、decode 吃带宽，PD 分离让它们各开各的工厂。第 7 章，个人网关与集群调度器的同构：负载均衡、熔断、预算门，治理即路由。第 8 章，前缀复用的经济学：缓存读十分之一的价，是删掉重复施工后的如实结算。第 9 章，引擎战场：生态赢过跑分，TGI 谢幕，SGLang 证明调度算法仍是新变量的富矿。第 10 章，也就是本章：把 token 一路追到电费，追到了 0.42 美元的地板。

十章合起来，是全书论点的最后一次显形：大模型 Infra 的核心，不是让模型跑得更快，而是让显存利用率接近 100%。利用率 5% 到 10% 的推理 GPU 与 50% 的训练 GPU 之间的鸿沟，就是所有 Infra 优化的总战场；而填沟的每一件工具——分页、连续批处理、量化、PD 分离、前缀缓存、推测解码——没有一件需要制程前进一个纳米。大模型的摩尔定律不在制程工艺里，在调度算法里。这句话写了十章，最终它可以翻译成三条可执行的判断：当你要降本，先问利用率，再问制程；当你选型，先看调度粒度，再看跑分；当你设计系统，先假设显存会满、带宽会堵、机器会闲，再让算法去填这些坑。

追到电费之后，还有三点冷静，算作全书的物理注脚。其一，电费只占时租的 3% 到 7%，所以能源不是推理的瓶颈，调度才是——把力气花在省电上，不如花在把卡填满上；东数西算的选址能省电费，省不出一个数量级，利用率能（materials/ch10_补充.md）。其二，折旧占时租的四分之一，而且按小时走得分毫不差，所以时间才是真正的敌人——卡每闲一小时，折旧照扣，这就是为什么所有调度算法的终极目标都是别让显存睡觉。其三，decode 是搬运问题，带宽是天花板（第 6 章），所以在后摩尔时代，搬多少字节比算多少次更值得优化——KV 的每一次瘦身、每一回复用、每一趟 RDMA 交接，都是在给带宽账本打折。

第 1 章说过，一张 H100 一小时的租金约一杯咖啡；终章算完，可以给这个比喻补上下半句——满产时，这杯咖啡兑换 720 万个 token；空转时，它一个 token 也不兑换。咖啡还是那杯咖啡，差的是排班表。这个道理，一百年前的电气工业已经演过一遍：电动机取代蒸汽机之初，工厂只是把蒸汽机原位换成电机，生产率纹丝不动；直到人们围绕“电可以随处取用”重新设计厂房与流水线，生产率才真正爆发。算力今天正走到这一步的中段——按量付费的公共品已经就位，围绕它重新设计应用形态的人，拿到这一轮的红利；只把显卡当蒸汽机替身的人，付的是账单，拿回来的是电费单。

收拢全书，利用率战争其实在三条战线上同时打响：硬件层省的是每一瓦与每一字节——液冷、选址、HBM 带宽；调度层省的是每一秒——分页、连续批处理、PD 分离、前缀缓存；应用层省的是每一个 token——把请求路由到合适的模型、把公共前缀缓存起来、把上下文当资产管理。三层相乘，才是从 5% 到 60% 的全部路程；三层里没有一层叫制程，这就是本章标题的全部含义。

最后，给三部曲收尾。《边缘王国》在网络侧：流量如何被调度、被缓存、被送到最近的边缘，延迟如何在地理上被消灭。《命令之下》在交互侧：人与机器的界面如何被重新发明，命令行、对话框与智能体的分工如何重排。《推理工厂》在计算侧：token 如何被制造、被计价、被一路追到电费。三部曲合起来是一个判断：算力与网络，正沿着电与带宽的老路，变成按量计费的公共品——你不必再拥有发电机，不必再铺设光缆，也不必再囤积显卡。公共品时代只留下一个问题：当一切基建都可以按量买到，个人开发者还剩什么不可替代？

我的答案有三样，而且一样都不会被降价。第一样，判断力：知道该把哪个请求路由到哪个模型、哪些问题值得问、哪些答案值得信——品味无法按量计费，而路由的本质是品味（第 7 章）。第二样，责任：预算门、脱敏、审计、熔断——治理无法外包，当一个 token 的成本趋近于零，为它设边界的责任反而趋近于无穷大；23 条脱敏规则与四级预算门，是我对这个责任的个人签收。第三样，问题本身：对真实场景的知识、对具体用户的信任——公共品给不出你的问题，也替不了你的判断。工厂越大，会开工厂的人越廉价，知道该生产什么的人越昂贵。机器归机器，电费归电费，判断归人。这是推理工厂的终章，也是三部曲的最后一页。我的网关还在运行，每天几百次调用，每一笔都被记账；下一次你看着屏幕上流出的第一个字，你知道它来得多贵、多便宜、多不容易——以及，它本可以更便宜。

---
