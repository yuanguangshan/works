# 《》

> 作者:广山哥  
> 完成时间:2026-08-29

---

## 序言

# 序言

2026年8月的一个深夜，我在R86S小主机上调一个MCP服务器的传输层bug。凌晨一点半，问题定位了：一段被吞掉的初始化通知。修完那一行，我在笔记里写下一句话——“写这本书的最好时机，是两年前；其次是现在，趁我还没把它忘掉。”

这不是一句漂亮话，而是真实的紧迫感。过去两年里，我以一个亲历者的身份，看着MCP从Anthropic实验室里的一个开源公告，长成一层越来越难绕开的基础设施。我亲眼见过它的每一个版本更迭，踩过它几乎每一类坑，也用自己的项目——WeClaw、Knowly、Hermes、knasync——把它揉进了每天的工作流。我意识到，如果现在不把这些东西写下来，那些只存在于我调试日志和个人笔记里的细节，就会随着记忆一起风化。而市面上关于MCP的资料，要么是官方规范的忠实转述，要么是五分钟上手的快餐教程——它们都很有用，但都没有回答我真正关心的问题：**这套协议为什么长成这样，以及它在真实生产环境里到底是什么手感。**

这本书试图回答的，就是这个两个问题。

## 这本书不是什么

先说清楚它不是什么，免得你抱错期待。

它不是规范文档的替代品。官方规范是唯一权威，任何二手资料都可能在某个版本迭代后过时——包括这本书。我在书里反复建议你直接去读规范原文，哪怕只有二十分钟。

它不是API参考手册。书里的代码块都是可以跑的，但它们存在的意义是解释“为什么”，而不是让你复制粘贴。真正的生产代码，永远长在你的业务上下文里。

它也不是布道文。你会看到我为MCP辩护的时刻，也会看到我引用Perplexity放弃MCP的决策、坦陈四十三工具塞爆五万五千Token的上下文成本、记录我自己十二个Server并发消耗八万Token的实验。逻辑诚实比立场一致更值钱——如果一个标准经不起反例的审视，它不值得你投入。

## 这本书是什么

它是两个视角的叠加。

第一个视角是**亲历者写史**。2024年11月5日MCP发布的当天下午，我写出了第一个MCP服务器；2025年3月的OAuth 2.1更新、6月的批处理移除、11月的治理结构成型、2026年7月的无状态化大版本——每一个节点我都经历了“刚踩完坑、规范就变了”或者“规范刚变、坑就堵上了”的往复。第2章和第3章的很多细节，比如四十七套集成代码的吐槽、批处理“部分成功”的含混表述、购物车困境的三种客户端行为差异，都是那段历史的 firsthand 记录。历史的价值不在于怀旧，而在于它解释了“为什么是现在这个样子”——不理解演变路径的人，永远只能被动地接受规范的每一次变动。

第二个视角是**实战者拆解**。全书第5到第7章是动手的部分：从二十二行的计算器Server，到带根目录管理和采样能力的Client，再到一个完整可部署的知识库服务器。我在金融行业做了十几年系统，职业病是只相信跑起来的东西。所以每一章的技术论述，背后都有我自己的生产数据或事故复盘——错误率从百分之三飙到百分之二十二的工具路由压测、工具描述超过两百Token后模型理解准确率下降两成的观察、一个第三方Server失联三小时才定位的排查记录。这些数字不严谨、样本不够大，但它们是真的。

两个视角叠加，得到的是这本书真正的主题：**MCP的价值不在协议本身，而在于它强迫你去思考的那件事——你的AI到底需要什么工具，以及这些工具该怎么被描述、被暴露、被调用。**

## 全书结构

全书可以按三种节奏读。

**从头到尾的线性读法**适合第一次接触MCP的读者：第0章和第1章建立概念地图——为什么需要它、它是什么、它和Function Call是什么关系；第2章和第3章回望历史——它怎么诞生、怎么一路演进到无状态化；第4章拆开引擎盖——三层架构、JSON-RPC消息、连接生命周期、传输机制；第5到第7章动手——环境搭建、Server开发、Client开发、完整实战；第8章抬头看路——生态、局限、AAIF路线图与行动建议；终章的后记，是我写完全书后最诚实的一段独白。

**只想要答案的跳读法**：决策者直接读第0章、第3章的演进逻辑和第8章的局限性分析，这三处够你做出“用不用、何时用、怎么防坑”的判断；产品经理重点看第0章的阅读路径、第3章的Elicitation和第8章的平台生态；一线开发者可以从第5章直接开跑，遇到概念疑问再回头。

**当工具书用**：每章开头的小节结构和章末的独思时刻，都是独立的思考单元。书里的关键事实——版本时间线、生态数据、个人实验记录——分散在BRIEF式的事实陈述里，方便检索。

## 一点私心

如果你问我写这本书最大的私人动机，是第4章里那个观点：三层模型是协议层面的划分，不是部署拓扑的真实映射。这句话背后是我十几年的一个执念——**接口即契约，契约先于实现**。从金融行情系统到AI Agent网关，我踩过的所有大坑，几乎都源于把“临时对接”当成了“永久契约”，或者反过来，把该松耦合的东西焊死了。MCP是我见过的第一个把“AI与外部世界的关系”契约化的标准，它不是终点——后记里我写了，我反而更不确定它的未来了——但它是我此刻能给出的、最诚实的回答。

感谢MCP社区的两位设计者David Soria Parra和Justin Spahr-Summers，他们从LSP带来的不只是技术方案，还有“敢于删功能”的工程审美。感谢AAIF和所有工作组里的匿名贡献者，2026年那份路线图上的每一个词背后都是公开讨论的记录。感谢我的每一个开源项目——WeClaw、Knowly、Hermes、knasync——书里所有“我”的经验，其实是它们替我试错换来的。

最后，把这本书献给我的家人。写书的那段日子，书房的灯总是亮到凌晨。他们说我在跟一个看不见的东西说话——某种意义上，没错。

苑广山

2026年8月

于上海

---

## 目录

- 序言
- 第0章 为什么你需要了解MCP
- 第1章 什么是MCP
- 第2章 MCP的诞生——从0到1
- 第3章 MCP的演进史——从实验室到生产环境
- 第4章 MCP架构深度解析
- 第5章 MCP开发环境搭建与第一个Server
- 第6章 MCP客户端开发与集成
- 第7章 实战——构建一个完整的MCP服务器
- 第8章 MCP生态与未来展望
- 第9章 终章·后记

---

## 第0章 为什么你需要了解MCP

# 第 0 章　为什么你需要了解MCP

2024 年 3 月，北京，一场 AI 技术分享会的茶歇时间。一位在某头部 AI 应用公司做工程负责人的朋友把我拉到窗边，压低声音问我：你们也在做 AI 应用，那你们维护几套集成代码？

我还没来得及回答，他自己先笑了：四十七套。四十七套互不兼容的集成代码，就为了把同一批模型接到四十几个外部系统上。每接一个新的数据源，就要新写一套认证、一套序列化、一套错误处理，还得再写一份能让模型看懂的接口文档。写完这份文档，换一个模型供应商，又要重写一遍。

那个瞬间的画面我记到今天：窗外是国贸的天际线，窗内是一个被 M×N 集成矩阵困住的工程团队。半年多之后，2024 年 11 月 5 日，Anthropic 开源了 Model Context Protocol——这本书的主角，MCP。再往后两年，这个词出现在微软、OpenAI、谷歌的发布会幻灯片里，出现在 Linux 基金会的捐赠公告里，出现在五千多个公开服务器的元数据里。

如果你在 2026 年做 AI 应用，却还没有认真了解过 MCP，这本书就是为你写的。我先把结论放在最前面：MCP 不是又一个开发者框架，而是 AI 应用与外部世界之间的标准化契约层。理解了它，你才真正拿到理解 2026 年 AI 应用竞争格局的门票。

在正式开始之前，也交代一下写这本书的人。我在金融与互联网行业做了十八年，长期在行情数据与证券资讯系统里打转——对把数据变成可用信息这件事，我有一种近乎职业病般的执念。过去几年，我自己写着几个不大不小的项目：微信 AI Agent 网关 WeClaw、剪贴板知识流水线 Knowly、AI Agent 框架 Hermes，还有统一暴露知乎搜索与全网检索能力的知识基础设施 knasync。它们都不是明星项目，但它们让我在 2024 年 11 月 5 日当天就接入了 MCP，也让我在随后两年里把每一次协议变更都真刀真枪地趟了一遍。所以这本书是两种身份叠在一起写的：一个亲历者在写史，一个实战者在拆解。

## 0.1 一九四八年以来集成史的最大悖论——模型越强，集成越痛

1948 年发生了两件看似不相干的事。这一年，贝尔实验室向世界公开了晶体管；也是这一年，香农发表《通信的数学理论》，为信息论奠基。计算与通信这两条主线，从那时起就注定纠缠在一起。此后七十多年，计算行业每一次物种跃迁，都伴随着一次接口标准化的阵痛：大型机时代各家的专有外设接口，个人计算机时代的串口与并口，二十世纪九十年代的 USB，两千年之后的 Wi-Fi 与蓝牙。每一代新设备从各自为政走向说同一种语言，都要先付出一到两代人的集成成本，然后换来一轮生态爆炸。

AI 是最新加入这个序列的物种，而它正处在阵痛期。这个悖论值得写进任何一本 AI 工程史：模型能力每上一个台阶，集成的痛点不是缓解，而是加倍。原因不复杂——模型越强，人们对它的期待就越接近一个能干活的同事，而不是一个聊天窗口。能读邮件，就得接邮件系统；能查库存，就得接 ERP；能开发票，就得接财务系统；能盯着行情帮忙盯风险敞口，就得接行情终端和风控系统——这是我这种在金融行业待过十八年的人最熟悉的例子。每多一分智力，就多一分对外部世界的胃口；每多一个外部系统，就多一条要亲手焊上的集成线。

这条线有多贵？M 个 AI 应用对接 N 个外部系统，最朴素的做法就是 M×N 套定制集成。十款模型对接二十个企业工具，就是两百套互不兼容的胶水层——每一套都要单独的认证流、单独的数据序列化、单独的错误处理，外加一份写给模型看的说明文档。有市场观察机构在 2026 年给出过一个刺眼的统计：在 MCP 出现之前，企业 AI 工程团队有 60% 到 70% 的时间消耗在这类集成工作上，真正花在 AI 能力本身的不足三分之一。那个茶歇时听到的四十七套，不过是这个比例在一个具体团队身上的投影。

我自己的账本也印证同样的结构。写 WeClaw——我的微信 AI Agent 网关，用 Go 写的——的时候，我一开始天真地以为核心工作量在对话编排上。真跑起来才发现，记忆库要接、搜索要接、剪贴板知识流水线 Knowly 要接、外部检索要接，每接一个都要重做一遍认证、序列化、错误处理、文档四件套。四件套做完，模型到底调得准调不准，还是另一回事。集成不是项目的附属开销，它就是项目的主体，只是没人愿意承认。在金融行业做行情系统的那些年，我对这一点体会更深：接一个新数据源，要先过合规审批，再配认证凭证，再对齐限流策略，最后才能动手写那层真正的集成代码——前三道关花的时间，往往是第四步的三倍。模型时代把这套流程原样继承了下来，只是申请单上多了一栏：这个接口给 AI 用。

更麻烦的是，这个洪峰还远没有到顶。Gartner 在 2026 年的预测是：到年底，40% 的企业应用将带有任务型 AI 代理，而这个数字在 2025 年还不足 5%。集成需求不是线性增长的，它跟着模型能力一起指数上行。碎片化、重复开发、安全隐患、维护困难——前 MCP 时代的四大痛点，第 2 章会逐个展开。这里只需要记住那个悖论：模型越强，集成越痛。痛的从来不是模型，是模型与外部世界之间那层还没有契约的集成。

把这些坑摊开看，就是第 2 章会展开的四大痛点：碎片化——每个应用与每个数据源的组合都要一套定制代码；重复开发——同样的连接器在无数团队里被重写了无数遍；安全隐患——工具意味着任意代码执行，没有统一的授权边界，等于把数据库钥匙别在门外的锁上；维护困难——上游 API 一变，所有胶水层跟着断。还有一个更隐蔽的误区：很多团队以为这些问题早就被解决了。OpenAPI 规范确实统一了 API 的机器可读描述，但那是写给开发者读的契约，不是写给模型用的契约。模型需要的远不止一份 schema：它需要运行时发现——今天有哪些能力可用；需要能力协商——双方各自支持到什么版本；需要把人用的入口与 AI 用的入口分开的授权边界——否则给模型单独开一个数据库账号，安全与审计部门第一个不答应。这些问题，REST 时代的工具箱一个都答不上来。

## 0.2 USB-C 与 LSP：两个改变世界的接口故事

2015 年搬家时我收拾出一个抽屉，里面躺着我十年攒下的线材：Mini-USB、Micro-USB、苹果 30 针 Dock 线、Lightning，还有几个已经认不出用途的电源头。折腾了一下午，结论是大部分可以直接扔掉。又过了几年，这个抽屉彻底消失了——USB Type-C 在 2014 年由 USB-IF 定稿，此后数年席卷消费电子；到 2024 年底，欧盟甚至为它立了法：在欧盟销售的手机必须统一使用 USB-C。一个物理接口，最后变成了法律条文。

但把 USB-C 讲成充电口统一了，是低估了它。这份规范真正的野心，是把三份原本互不相干的契约——供电、数据传输、视频输出——合并进同一个 24 针的对称接口：正反可插，功率最高 100 瓦。设备厂商实现一次，就同时接入了充电器生态、外设生态与显示器生态；反过来，任何一家做硬盘盒、做拓展坞、做显示器的厂商，只需要对着这一份契约设计，产品就天然兼容所有说 USB-C 的设备。差异化并没有被标准杀死——快充协议、雷雳兼容、拓展能力都在接口之上继续分层——被杀死的只是专有接口这种昂贵的差异化方式。物理世界把 M×N 的线材地狱，压缩成了 M+N。我现在的出差背包里只剩一根线——不是刻意极简，而是没有理由再多带一根。标准的最高境界是让用户忘记它的存在；而标准化本身，恰恰要厂商先记住它、实现它，再放弃自己那份昂贵的差异化。物理世界用了大约十年，才完成这个转身。

网络世界发生过同构的故事。1969 年，ARPANET 只有四个节点；1974 年，文顿·瑟夫和罗伯特·卡恩发表 TCP 论文；1983 年 1 月 1 日，ARPANET 强制全线切换 TCP/IP——史称 flag day，旧协议 NCP 当天彻底停用。这次切换确立了分层的铁律：每一层只对上层承诺契约，不关心下层如何实现。1989 年，伯纳斯-李在 CERN 提出万维网的构想，1991 年 HTTP 公开。HTTP 赢得很安静：任何一台服务器只要说 HTTP，任何一款浏览器就都能访问——服务器一次实现，处处可达。Web 后来那条指数曲线，与其说是超文本的胜利，不如说是任何到任何的契约的胜利。这里还有一个反面对照：IPv6 在 1998 年就完成了标准化，技术上比 IPv4 优越得多，但普及至今仍然参差不齐——因为生态已经长成，改标准的代价随规模指数增长。这给所有后来者留下一条铁律：标准化的最佳窗口，是生态还小的时候。

编辑器世界的故事离软件工程师更近，它叫 LSP——Language Server Protocol，语言服务器协议。2016 年之前，编辑器行业深陷自己的 M×N：M 种编辑器乘以 N 种编程语言，每一种组合都要一套专门的插件。Eclipse 的 Java 支持没法给 VS Code 用，VS Code 的 TypeScript 支持没法给 Vim 用，每一个语言工具团队都在为不属于自己的编辑器写适配层。2016 年，微软把语言能力抽象成一个独立进程——语言服务器，让所有编辑器对着同一份协议说话。结果你今天都看得到：rust-analyzer 一个服务器，让 VS Code、Neovim、Emacs、Zed 同时获得完整的 Rust 支持；反过来，VS Code 实现一次客户端，几百种语言一夜入住。M×N 坍缩成 M+N，编辑器厂商从此不再讨论要不要自研语言分析器这种问题。今天一门新语言问世，作者只要发布一个语言服务器，第一天就能在所有主流编辑器里提供跳转、补全与重构——放在 2010 年代初，这是不可想象的事。

MCP 的两位设计者 David Soria Parra 与 Justin Spahr-Summers 都有 LSP 背景——他们是亲手走过这条路的人，知道契约该宽到哪里、窄到哪里。2024 年 11 月 5 日，他们把这份经验贴在了 AI 应用的门上：AI 就是当年的编辑器，外部系统就是当年的编程语言，而 MCP 是这个物种迟到的 USB-C。

有人会问：同样是解决集成，为什么需要协议，而不是又一个 SDK？答案藏在范围里。SDK 绑定语言与厂商——它假设你用某家模型、某种语言、某个运行时；协议跨语言、跨厂商、跨运行时，它只规定双方说什么、按什么顺序说、说到什么程度算达成一致。Function Call 是模型的单点能力——模型确实能吐出一个函数调用的请求，但接到哪里、如何发现、如何授权、如何在十几个外部系统之间协商，那是另一层问题。能力解决一次调用的语法，协议解决一万次集成的秩序。

💡 独思时刻：三个故事里有一个容易被忽略的细节——标准的诞生常常以放弃开场。1983 年的 flag day，所有人当天放弃对 NCP 的存量投资；2025 年 12 月，Anthropic 把 MCP 捐赠给 Linux 基金会旗下的 AAIF，把 AI 的 USB-C 的所有权交了出去。标准化有一本反直觉的账：最大的成本由发起者承担，最大的收益归生态，发起者换回来的是位置——定义赛道的人，永远在赛道上。理解了这一层，你就理解了 2026 年 AI 应用生态里开放二字的分量：那不是慈善，是战略。

## 0.3 核心承诺：一次开发，多处使用

现在可以给 MCP 一个准确的定义了。Model Context Protocol，模型上下文协议，2024 年 11 月 5 日由 Anthropic 开源，同一天发布 Python 与 TypeScript 两套 SDK，Claude Desktop 同步支持。它的架构分三层：Host 是发起连接的 AI 应用，比如 Claude Desktop、Cursor，以及我自己写的 WeClaw；Client 是 Host 内部的连接器，负责协议通信；Server 是能力提供方，向外暴露三类东西——Tools，AI 可以调用的函数；Resources，AI 可以读取的数据；Prompts，预定义的交互模板。

机制的关键在声明二字。我更愿意把 MCP 称为声明式的能力契约：你不写适配代码，你写一份契约；世界对上暗号，合作开始。这份契约之所以能一次声明、处处生效，是因为它把三类信息标准化了：能力清单——我有什么；调用格式——怎么用我；返回结构——我会给你什么。再往下看一眼会发现，消息格式直接复用了 JSON-RPC 2.0，任何一门语言现成的网络库都能说 MCP——这是它推广成本极低的底层原因：协议没有发明新轮子，它只是给轮子装上了统一的车轴。

一次开发，多处使用——听起来像所有协议发布时都会喊的口号，所以我不打算喊，我打算报账，报一份可以逐条验证的生态时间线。

2025 年 3 月，微软把 MCP 集成进 Azure AI Foundry，OpenAI 宣布支持——竞争对手开始在 Anthropic 的协议上握手。2025 年 6 月，社区月度新增 Server 从 135 个激增到 5069 个，一个月涨了 37 倍。2025 年 12 月，Anthropic 将 MCP 捐赠给 Linux 基金会旗下的 AAIF。2026 年 3 月，OpenAI、Google DeepMind、Microsoft 相继宣布支持；更有分量的信号是，OpenAI 弃用了自家专有的 Assistants API 转向 MCP，那套专有接口在 2026 年年中退役——一家公司用自己旧接口的退役仪式，给对手的协议当了证人。同一个季度，MCP SDK 的月下载量从发布初期的十万级涨到约 9700 万，18 个月里涨了约 970 倍。2026 年 5 月，GitHub MCP Registry 上线。2026 年 8 月，公开 Server 超过 5000 个，另一家市场统计口径称已超过 5800 个、客户端 300 余个；awesome-mcp-servers 仓库累计 Stars 超过 85000。

企业侧的动作同样密集：Notion、Adobe、Stripe、Shopify、AWS 推出了官方 MCP Server；有 2026 年的统计称，28% 的财富 500 强已经在生产环境运行 MCP Server。国内的速度不慢：腾讯 Marvis 接入了超过 10 家 MCP 服务商，企查查的 MCP 在 2026 年 8 月上线，查一份工商信息只要 30 秒。

数字之外还有一层更安静的复利：生态复用。有统计称，到 2026 年，公开 Server 中针对 GitHub、Slack、Google Drive、PostgreSQL、Notion、Jira、Salesforce 这些常见系统的生产级实现已超过 200 个——你要的连接器，大概率别人已经写好了，而且开源。这直接改写了 AI 集成的默认动作排序：动手自建之前，先去 Registry 搜一遍，确认真的没有现成实现，再考虑自己写。放在 M×N 时代，这个顺序是反的——因为搜了也大概率没有，只好自己焊。集成成本从乘法变加法是第一笔节省，从自建变复用是第二笔，第二笔往往比第一笔更大。

这些数字解释了承诺的宿主侧——多处使用。我自己的体验更具体。knasync 是我的知识基础设施项目，我把知乎搜索、全网检索、发布到 IMA 笔记与博客的能力统一做成一个 MCP Server。做成之前，WeClaw 和 Hermes 各自要写一套接入；做成之后，两个宿主对着同一份契约各自接入，互不干扰。2026 年 8 月，WeClaw 的搜索插件上线，周调用超过 200 次，一次问答平均走 4 次工具调用链路；Hermes 那边走 SSE 加 Bearer 鉴权接同一个 Server，中间只遇到一个依赖包缺失导致的 no-op 排查。2026 年 7 月，雨轩编辑器要做一键查汇率，一个 100 行以内的 MCP Client 就接上了——放在 2019 年，这是一个星期的排期；放在 2026 年，这是一个下午。

哦，还有起点。2024 年 11 月 5 日那天，我花 40 分钟写出了自己的第一个 MCP Server——一个本地文件夹读取器——当天就能被 Claude Desktop 调用。一次开发，多处使用，对我不是愿景，是当天兑现的体验。

但一本诚实的书必须同时报另一笔账。2026 年 3 月，Perplexity 的 CTO Denis Yarats 公开宣布放弃 MCP，改回 API 加 CLI 的直连路线，核心理由是上下文成本：一个带 43 个工具的 Server，光是工具描述就能占掉 55000+ Token——还没开始干活，先烧掉一部长篇小说的预算。我自己在 2026 年 5 月做过一次实测：12 个 Server 并发跑三个任务，消耗逼近 8 万 Token。官方在 2026 年 7 月的版本里用确定性缓存机制正面回应了这个问题。我把这笔账写进第 0 章而不是藏进第 8 章，因为它定义了承诺的边界：M+N 省下的是工程时间，不会自动省下 Token。契约层的价值成立，前提是你理解它的成本结构——统一标准、解耦生态这八个字，工程账与运行时账要分开算。

## 0.4 这本书为谁而写：开发者、产品经理、技术决策者的不同读法

这本书不想假装自己只有一种读者。过去两年，我在不同的场合跟三类人聊 MCP，发现他们问的问题几乎不重叠。开发者问的是怎么写得又快又稳，产品经理问的是哪些能力值得暴露成工具，技术决策者问的是现在押注会不会太早。所以在第 0 章就把读法交代清楚：你可以按自己的角色挑路径，也可以全读——但全读之前，先知道每一章是为谁写的。

开发者路径，主线是第 1、4、5、6、7 章。三个钩子先挂在这里。第一，第 5 章会让你用 22 行代码跑通第一个 MCP Server，一个加减乘除计算器——第一个周末的任务建议就定这个。第二，第 6 章有一组压测数据：工具列表越长，AI 的选择错误率会从 3% 涨到 22%——工具不是越多越好，路由噪声是真实存在的工程问题。第三，第 7 章会反复敲打一句话：工具描述不是文档，是写给 AI 的使用说明书。我自己交过学费——早期给 Claude 写过一个删除文件的工具，描述里把清理和删除混为一谈，结果它两次认真删掉了我的测试文件。这场事故的完整复盘在第 7 章，后记里还有一个更扎心的数字：工具描述超过 200 Token 之后，Claude 的理解准确率会下降约 20%。

给开发者的读法建议是顺序推进：第 1 章建立心智模型，第 4 章吃透协议细节，第 5 章动手，第 6、7 章把玩具养成生产系统。第一个周末的任务清单我替你拟好了：装好 uv 与 Node 环境，跟着第 5 章把计算器 Server 连进 Claude Desktop 或 Cursor，然后在周一早会给同事演示一遍——演示成功的那一刻，你会比读十篇综述更理解协议的价值。每章末尾的独思时刻也不要跳过，那些段落是踩坑记录的蒸馏，比代码示例更难从官方文档里捡到。

产品经理路径，主线是第 0、1、2、8 章，外加 6.4 节的工具路由问题。产品经理最容易犯的错，是把 MCP 理解成又一个 API 网关——那是对它改写交互设计的低估。有三件事值得你专门去看。一是能力边界的划分：一个功能到底值得做成 Tool，还是更适合 Resource 或 Prompt，这直接决定产品的信息架构。二是 Elicitation 这个新交互原语——Server 执行过程中可以向用户提问。我 2025 年 7 月做 Excel 报表 Server 时第一次体会到它的价值：报表口径缺数据时，Server 直接反问用户要字段，而不是静默返回一份错误的报表——对产品经理来说，这是交互范式的变化，机器终于学会把不确定还给人。三是成本结构：上下文预算迟早进入产品的定价模型，功能暴露得越贪心，产品反而可能越笨。

留一个练习给你：把产品的核心功能列表拿出来，逐项标注它应该是 Tool、Resource 还是 Prompt。标注得越顺利，说明你对产品边界的理解越清晰；遇到标不出来的那一项，大概率它不该做成 MCP 能力，而应该留在产品内部。这个练习五分钟，胜过一次排期会。

技术决策者路径，主线是第 0、2、3、8 章，外加 4.6 节的授权机制。决策者要回答三个问题。要不要现在投入——看第 3 章的版本时间线：2025 年 3 月 26 日，OAuth 2.1 授权框架给出企业级入场券；2026 年 7 月 28 日，无状态化大版本落地，官方影响分析显示真正需要迁移的 Server 只占约 3%，而且一个月内生态迁移基本完成；治理权也已从一家公司转移到 AAIF，平台风险显著下降。自建还是复用——用 M+N 的账算一遍再决定，第 8.6 节有四步行动清单。风险在哪里——Perplexity 的反例、安全边界、以及协议开放不等于数据不敏感的幻觉，这些都写在第 8 章。我还想替决策者多加一问：退出成本。如果两年后 MCP 的位置被别的东西替代，今天写的代码还剩多少？我的答案是：把胶水层写薄，把能力建模写厚。协议会换代，但把公司的数据与能力整理成清晰契约的思考，换多少代协议都用得上。

一句话总结三条路径：开发者按顺序啃技术章，产品经理先看能力边界再看成本结构，技术决策者先看治理成色再算集成账。

## 0.5 全书导航与阅读路径

全书八章，可以看成三条线。

第一条是概念与历史线，第 1 至 3 章。第 1 章讲清 MCP 是什么：三层架构、三大能力，以及 USB-C 类比在哪些地方会失效——类比是渡河的船，过了河要下船。第 2 章回到 2024 年 11 月之前的丛林，四十七套集成代码的完整故事会在那里展开；它同时讲清 MCP 为什么诞生在那天——混乱累积到阈值、LSP 蓝本已经存在、市场窗口恰好打开，三重条件缺一不可。第 3 章拆解从 2025 年 3 月到 2026 年 7 月的版本节点：OAuth 2.1 如何为企业开门，批处理如何被亲手移除，无状态化如何以 SEP 提案的方式走完社区流程——你会发现那是一部减法史，从有状态走向无状态，从加功能走向删功能，从一家公司走向开放治理。

第二条是架构与实战线，第 4 至 7 章。第 4 章深入 JSON-RPC 2.0、连接生命周期与传输选型，回答一个容易被误解的问题：三层是协议层面的划分，不是部署拓扑的地图；stdio 与 Streamable HTTP 各自适配什么场景，initialize 握手的每一帧长什么样，都会逐层拆开。第 5 章用 22 行代码搭起第一个 Server，顺带交代我踩过的环境变量暗坑。第 6 章转向客户端，回答一个更疼的问题：AI 为什么选不准工具；知乎搜索 MCP 与 knasync 的接入实战会在那里展开，包括一次依赖包缺失导致的 no-op 排查。第 7 章把一个玩具 Server 推进到生产级——输入验证、错误可读、返回稳定、描述含策略、测试分层、部署选型，六道关一道都不能省。

第三条是生态与未来线，第 8 章。Registry 与下架机制、国内外平台接入、MCP 与 A2A 的分工——MCP 管 AI 用工具，A2A 管 Agent 之间的协作，类比二层交换与三层路由，沃达丰的 AI Booster 平台就在同时使用两者。接下来是局限性的完整盘点：上下文成本、初始化不稳定、认证碎片化、安全信任；再往后是 AAIF 的五项优先事项路线图，以及给开发者的四步行动清单。

时间紧的话，有两条跳读路线。只想最快用起来：读 0.3 节、第 1 章、第 5 章，一个下午就能跑通第一个 Server。只关心这个标准靠不靠谱、该不该投入：读 0.2 节、第 2 章、第 3 章和第 8.5 节。

最后交代本书与官方文档的关系。这本书讲的是为什么这样设计、我在哪里踩过坑、代价藏在哪里；规范细节以官方规范与 SDK 文档为准——本书的版本基准，是 2026 年 8 月 27 日 AAIF 发布五项优先事项路线图之后的规范状态。协议还会继续演进，版本号还会变大，但接口哲学不会月更——这正是本书想教给你的东西。

## 0.6 独思时刻：从“用协议”到“被协议塑造”

写完前面五节，本来可以就此收笔——痛点、类比、承诺、读法、导航，该交代的都交代了。但深夜合上电脑之前，总觉得还欠一层意思没说透。

大多数人学协议的姿势，是把协议当扳手：学 MCP，是为了让 AI 调用我的数据库；用完，放回工具箱。这个视角没有错，但它停在第一层。我想在全书开篇就把第二层摆出来——协议会反过来塑造用它的生态，以及生态里的人。

回看 USB-C。它统一的不只是充电器：接口差异化这条路被关闭之后，厂商把竞争预算挪到了轻薄、快充、拓展坞生态上——被塑造的不只是设备，还有设备背后的人如何做产品决策。今天的手机产品经理，再也不会在要不要用专有接口这个问题上花一小时，但会在接口之上叠加什么体验这个问题上花一百个小时。HTTP 的塑造更深：超链接的可引用性逼出了整个搜索引擎行业，无状态的请求-响应逼出了 REST 文化和前后端分离——今天任何一个后端工程师的默认思维方式，是 1991 年那纸几十页的规范塑造的。LSP 塑造了编辑器行业：自研语言支持这条路被关掉之后，竞争集体转向体验、插件生态与协作能力。

协议是隐形的决策约束。它悄悄决定哪类创新变得便宜、哪类创新变得昂贵；用它的人以为自己在自由选择，其实菜单是协议写的。我们这一代开发者早就吃过这份菜单：REST 的资源思维、GraphQL 的按需取数、OAuth 的委托授权——每一个都曾是一份新协议，每一个后来都成了默认菜单。MCP，是给 AI 时代递上的那份新菜单。

但要补一句：被塑造不等于被驯化，健康的关系是双向的。MCP 2026 年 7 月的无状态化就是生态反过来塑造协议的例子——SEP-2567 与 SEP-2575 两份提案走完社区流程，Session 概念被移除，改用显式句柄，约九成的公开 Server 甚至无需迁移。协议不是从石头缝里蹦出来的圣旨，它本身也在被用的人反复改写；用协议的人如果只甘心做点菜者，那是浪费了这张餐桌。

那么，MCP 正在塑造什么？我的判断有三条。第一，它重新定义了 AI 应用的竞争壁垒：当接入一个外部能力从一个月的排期变成一个下午，护城河就从我接了多少，迁移到我的编排、判断与工具设计有多好——第 8 章的论点在这里先埋一笔：2026 年 AI 应用竞争的赢家，不是模型最强的那个，而是最快把散落能力串起来的那个。第二，它在催生一种新工种：为数字心智设计接口的人。写工具描述不再是补文档，而是设计一份让不同厂商的模型都能准确理解的行为契约——这种能力会像当年写 API 文档一样，从加分项变成基本功。第三，它在重塑成本观：上下文预算正在成为与 CPU、内存并列的性能预算，每一次要不要把这个工具挂上去的决策，背后都是一场 Token 收支核算。这层变化在我自己身上已经发生：现在动手写任何 AI 相关的代码之前，我会先画一张契约图——谁是 Host，谁是 Server，能力如何声明，上下文预算怎么分。这个习惯让 WeClaw 的第三次重构比前两次都平静，因为契约先于连接，改动有边界。

所以，从用协议到被协议塑造，意思是把 MCP 从工具箱里的一件工具，变成观察 AI 应用生态的一副眼镜。这也是本书真正想交付的东西：读完之后，你再评估任何一个 AI 应用——无论是别人的产品，还是你自己的——都会先问一个问题：它的契约层在哪里？这个问题问出口的那一刻，你已经站到了 2026 年 AI 应用竞争的正确一侧。

深夜十二点，书房的灯还亮着。四十七套集成代码的茶歇对话过去两年多了，那家公司后来怎么样，我没有打听。但我猜他们的抽屉也正在被清空，就像 2015 年我那个装满线材的抽屉一样。清空抽屉的过程，就是这本书接下来的全部内容。

---

## 第1章 什么是MCP

# 第1章 什么是MCP

在翻开这一章之前，你不妨回忆一下最近一次让AI帮你查邮件、读取本地文件或操作某个软件时的心情——是不是带着一种“它真的能做到吗”的试探，以及“千万别把数据搞乱”的忐忑？

这种忐忑不是错觉，它是整整一代AI应用与外部世界之间关系失真的症状。这一章我想回答那个看起来最简单、其实最容易被答浅的问题：MCP到底是什么。我会从我自己的一次深夜调试讲起，拆开它的三层架构，带你走完一次完整的调用旅程，再把三大核心能力、它与Function Call的关系，以及2026年这幅已经相当辽阔的应用版图，一层层摊开在你面前。

## 1.1 从一次深夜调试说起

2024年11月一个周二的深夜，我坐在书房的R86S小主机前，看着终端里跳动的日志。那时我正试图让一个本地运行的AI模型通过我写的Python脚本，去读取我NAS上存放了十年的投资笔记。脚本跑通了，但那种“硬编码”的别扭感挥之不去——换一个模型，换一种数据源，所有管道都要重写一遍。这种重复，在我过去十几年构建金融系统的职业生涯里，太熟悉了。每一次，都像在两个陌生的孤岛之间临时搭一座会随潮水消失的浮桥。

那天夜里脚本跑通之后，我并没有多少兴奋，反而有点发凉。这段脚本拢共不到两百行，逻辑简单得不能再简单：拉起一个本地模型，监听一个目录，把命中的笔记喂进去。可我心里清楚，它的保质期可能比一盒鲜奶还短——换一个模型，提示词的拼接方式要改；换一种数据源，读取和清洗的管道要重写；换一个宿主应用，输入输出的格式又要重新对齐。我在纸上粗粗算过一笔账：如果要把当时常用的五个AI应用和手头十来个数据源全部打通，按点对点的写法，我要维护的集成代码不是五份，也不是十份，而是几十份——每一对组合之间，都得单独搭一座浮桥。

这笔账我在金融行业里算过很多年。过去十几年，我做的大多是行情、柜台、风控这类系统：每对接一家数据商，就要写一套私有协议的适配器；每换一家交易所，报文格式、心跳规则、断线重连的语义全都不同。点对点集成的成本从来不是线性叠加——表面上是加一个节点多写一份代码，实际上是每新增一个节点，其余所有节点都要跟着回归一遍，改一处、崩三处的耦合税迟早要收。更磨人的是，浮桥是要有人常年看守的：模型半年一次大版本迭代，第三方接口一年一改版，你辛苦搭起来的桥，还没站稳就被潮水冲走了。集成代码的腐烂速度，在AI时代被压缩到了以月为单位。

这不是我一个人的痛。2024年3月的一场技术分享会上，一家头部AI应用公司的工程师吐槽，他们内部维护着四十七套集成代码，每接入一个新工具都要从头来一遍，台下一片苦笑——每个人都在自己的抽屉里缠着同样的一团线。所以大半年后，当2024年11月Anthropic发布MCP的消息传到我这里时，我的第一反应不是“又一个新框架”，而是一种旧伤被戳中的警觉：这个问题，行业真的打算正面解决一次了？

## 1.2 MCP的定义与USB-C类比

MCP，全称Model Context Protocol（模型上下文协议），正是在这样的背景下，由Anthropic公司于2024年11月开源的解决方案。它的目标朴素而有力：在大语言模型与外部世界（数据源、工具、服务）之间，建立一套统一的、标准化的“通用语言”。

有一个当时我没太在意、后来回想起来意味深长的细节：2024年11月5日发布当天，Python与TypeScript两套SDK、连同Claude Desktop的支持是同步落地的。也就是说，MCP从诞生的第一刻起就不是一篇论文，而是一套当天就能跑起来的工程件。理论与应用之间零时差，这个姿态在很大程度上预示了它后来两年的扩张速度。

你可以把它理解为AI领域的USB-C接口。在USB-C出现之前，你的手机、相机、移动硬盘各有各的充电线和数据线，抽屉里永远缠着一团理不清的线缆。AI的集成困境一模一样：每个AI应用要对接一个外部工具，就需要写一套特定的集成代码。你的AI想查数据库？写一段。想调天气API？再写一段。想连企业微信？又来一段。这些代码彼此不兼容，像一堆接口各异的充电头，永远差一根对的线。

而MCP给出的答案是：定义一套协议，让AI应用（Host）通过一个标准化的客户端（Client），去调用任何实现了该协议的服务器（Server）。从此，AI与外部世界的关系，从“一一对接”变成了“一对多，多对多”的网状连接。

USB-C这个类比之所以流传得这么广，是因为它精确命中了标准化最动人的那一面：把一千种线缆收敛成一个形状。形状统一之后，受益的不只是用户，还有整条产业链——配件厂商不必为每款设备单独开模，设备厂商不必为每类配件设计接口，创新被重新分配到真正产生价值的地方。MCP生态正在重演这一幕：GitHub、Stripe、Shopify这样的服务商只需实现一次Server，所有支持MCP的Host就都能接入；反过来，Claude Desktop、Cursor、包括我自研的WeClaw，只需实现一次Client，就能接上整个世界。这就是“一次实现，处处可用”的算术——把N乘M的连接问题，压缩成N加M。

但作为一个每天跟这套协议打交道的人，我必须诚实地指出类比的失效边界，否则你会对它抱错误的期待。第一，USB-C是物理层的事，只管“插上”；MCP是语义层的事，要管“说什么”。一根线缆不关心里面跑的是视频信号还是充电电流，MCP却要求每一个能力都有明确的名字、参数结构与返回约定——语义不清，协议就是空壳。第二，线缆是一次性握手的，协议是持续对话的。MCP的连接有完整的生命周期：初始化握手、能力协商、鉴权——2025年3月起，远程连接正式引入了OAuth 2.1授权框架——直到断开，每一步都是协议明文规定的礼节。第三，线缆是哑的，协议是活的。USB-C的形状十几年不变，MCP从2024年11月到2026年已经迭代了多个大版本，传输机制从HTTP+SSE换成了Streamable HTTP，能力清单还在持续加条目。所以我的说法是：USB-C描述的是“形”，MCP定义的是“法”。类比帮你在三秒内建立直觉，之后的三个月，你得靠对协议本身的理解来干活。

## 1.3 三层架构：Host、Client、Server

这套三层架构是MCP的骨架：

- Host（宿主）：也就是AI应用本身，比如Anthropic的Claude Desktop、你IDE里的AI插件，或者你正在构建的一个智能Agent。它是用户直接交互的界面，也是整个链路的发起者。

- Client（客户端）：在Host内部运行的一个连接器。它负责管理一个或多个MCP服务器的连接，发送请求，并接收响应。对开发者来说，它通常是SDK里封装好的组件，你不需要深究其底层通信细节。

- Server（服务器）：能力提供方。它暴露了一系列可供AI调用的“技能”，并负责与真实的数据源或服务（比如你的本地文件、公司的PostgreSQL数据库、Slack的API）打交道。

这三段定义是理解整本书的钥匙，我再逐个补上自己的体会。

Host是体验与安全的守门人。它直接面对用户，因此要负责三件事：把模型能力组织成产品体验，把工具调用变成用户看得见、可确认的操作，以及把权限边界画清楚。你在Claude Desktop里见过的“是否允许本次工具调用”的确认弹窗，就是Host在履职。我们自研的WeClaw是一个用Go写的微信AI Agent网关，它作为Host时优先考虑的不是接了多少Server，而是哪些动作需要管理员确认、哪些只读查询可以静默放行——这是产品决策，不是技术细节，但它决定了Agent敢不敢被放进生产环境。

Client是协议的执行者。规范里有一个容易被忽略的限定：一个Host可以同时连接多个Server，但每条连接由一个独立的Client实例负责，彼此隔离。这个设计给了“能力路由”一个天然的单位——我在WeClaw里同时接了知乎搜索、文件系统、发布通道三条连接，一条挂了不影响另外两条，排查问题时边界清清楚楚。

Server是语义的封装者。三个角色里，只有它知道真实世界的模样：数据库连接串、接口密钥、文件路径，全都锁在Server的腹地里，模型和Host永远不需要碰。我的knasync项目走的就是这条路：起初它只是一个独立的搜索服务，2026年我把它重构成MCP Server，把知乎搜索、全网检索，以及发布到笔记、博客、播客的能力统一暴露。于是同一份能力，Claude Desktop能用，WeClaw能用，雨轩编辑器里那个一百来行的汇率查询Client旁边挂的搜索小助手也能用——“实现一次，处处可用”从口号变成了我的日常。

这里有一个我亲眼见过的反面教材。有人把业务编排逻辑写进了Host：在宿主应用里硬编码“先查库存、再核价、最后下单”的判断流程，Server则退化成一条条裸露的SQL透传通道。短期看跑得飞快，可一旦要换一个Host——比如从自研后台迁到通用的Agent框架——整套编排就得原样重抄一遍，而且抄写过程中语义还会悄悄走样。正确的姿势恰恰相反：把领域语义沉进Server，让Host只做体验与安全，编排知识跟着能力走，而不是跟着界面走。边界画对了，三层各自都能独立进化；边界画错了，三层就会互相拉扯着一起腐烂。

常有人问：这三个角色必须部署在三个进程里吗？协议层面没有这个要求。玩具项目可以把Host、Client、Server编进同一个进程；生产环境则大多让Server独立部署，甚至跑在远端。这个弹性是刻意留下的设计空间，第四章我会专门拆解。另外预告一句：2026年7月28日的大版本把“有状态会话”从协议核心里拿掉了，Server的形态因此变得更轻、更接近普通的Web服务——那是第三章的故事，这里按下不表。

## 1.4 一次典型交互的完整旅程

在这个架构下，一次典型的交互是这样发生的：你在AI对话框里输入“帮我查一下上周的销售数据，并生成一份摘要”。AI（Host）理解了意图，通过内置的MCP Client，向已连接的“销售数据MCP Server”发送调用请求。Server接到请求后，去查询真实的数据库，将结果返回给Client，Client再递交给AI模型。AI最终生成你看到的那段摘要文字。

这段话不到一百五十字，但每一句背后都藏着一步真实发生的机制。我把这条链路拆成六站，你可以把它们想象成一台机器内部齿轮的咬合。

第一站，发现。连接建立时，Client与Server完成初始化握手，随后拉取能力清单：tools/list会返回每个工具的名字、描述和参数Schema。Host拿到清单后，负责把它“翻译”给模型看。

第二站，决策。你说出那句话之后，模型看到的并不只是你的原文，而是你的原文加上一份工具清单。它要做一次判断：这是闲聊、是要知识，还是要动工具？动哪个？参数怎么填？这一步完全发生在模型内部，靠的正是Function Calling能力——下一节我会展开。

第三站，意图落地。模型输出的是一个结构化的调用意图——工具名加参数——而不是执行结果。多数Host会在这里弹一次确认，人始终在环上。我认为这是MCP体系里最容易被低估的安全设计：链路再自动化，扳机始终握在用户手里。

第四站，执行。Client把意图翻译成JSON-RPC 2.0格式的tools/call请求，经选定的传输通道发给Server；Server校验参数，执行真实动作——查库、调接口、读文件。

第五站，回填。执行结果按约定格式返回，Host把它作为工具结果回填进对话上下文。直到这一刻，模型才第一次“看见”真实数据。

第六站，汇总。模型基于回填的数据生成你看到的那段摘要。如果问题更复杂，第二站到第五站会循环多轮，直到模型判断证据足够。

我在WeClaw上线的知乎搜索插件走的就是这样一条循环链路：用户问一个问题，Agent先调用搜索拿到候选列表，再调用正文读取取回细节，必要时补一次检索，最后综合成文——一次看似普通的问答，背后是四次工具调用的链路。这个插件2026年8月上线，如今每周被调用两百多次，撑着它的没有一行手工胶水代码，只有协议。

整个过程中，AI模型不需要知道数据库的连接字符串，不需要处理SQL语法，更不需要关心数据在哪个云上。它只需要知道：有一个叫“查销售数据”的工具（Tool），参数是时间范围，返回的是格式化好的数据。这就是MCP带来的核心价值——解耦与抽象。

解耦的红利是双向的：换模型，Server一行不用改；换数据源，Host的业务逻辑一行不用改。后来我把WeClaw底层的模型换过两次，知乎搜索Server稳如老狗。这种“两侧各自进化、中间契约不动”的体验，就是我当年在金融系统里做梦都想要的东西——行情系统的上游换一套、下游换一套，中间的适配层就得推倒重来的日子，我过够了。

## 1.5 三大核心能力：Tools、Resources、Prompts

这种抽象能力，具体体现在MCP定义的三大核心能力上，它们是构成MCP服务器功能的基石：

**1. Tools（工具）**

这是最直观的能力。Tools是AI可以主动调用的“函数”或“动作”。比如“搜索网页”、“计算数学表达式”、“发送一条消息”、“在Jira上创建一个工单”。每个Tool都有明确的输入参数和返回结果。AI会根据你的指令，决定是否调用、何时调用以及传入什么参数。它赋予了AI“动手”的能力。

补一个2026年的生产视角。官方的GitHub MCP Server暴露的是create_issue、search_code、get_pull_request这样的动作；PostgreSQL官方Server的query_database则被刻意设计成只接受只读的SELECT查询——危险动作被挡在能力设计的源头，而不是靠提示词事后求情。Tools的粒度与措辞，直接决定了AI的行为边界。这一点我自己交过学费：我写过“删除文件”工具，描述里用了“清理”这个词，Claude在一次会话里两次把我的测试文件删掉了。那次之后我才真正理解，工具描述不是注释，是行为契约——这个教训我会在第七章完整复盘。

生产环境里Tools的组合威力也有数字可循。2026年有电商客服团队把Shopify、Stripe、物流追踪和工单系统的MCP Server接进同一个客服助手，平均处理时长从8分钟压到90秒，首次解决率从62%提升到91%。支撑这些数字的，正是一个个原子化、描述清晰的Tool——模型每次只做一个小而确定的动作，串联起来却重构了整条业务流程。

协议层也在为“模型如何信任工具”持续加码。2025年3月的版本引入了工具注解，开发者可以声明一个工具是否只读、是否有副作用、是否可重复执行，Host据此决定要不要为每次调用弹确认框；2025年6月的版本又引入了结构化输出，工具返回值可以带明确的格式约定，下游解析不再靠猜。这两步合起来的方向很清晰：Tools不只是“能调用的函数”，还是一份带信任标注与格式承诺的正式契约。

**2. Resources（资源）**

如果说Tools是“动词”，那么Resources就是“名词”。Resources代表AI可以读取的数据，像是你本地的Markdown文档、一个API返回的JSON响应、或者数据库里的一条记录。与Tools不同，Resources通常由AI在需要上下文时主动“查阅”，或者由用户在对话中明确“提供”。它们像AI的“外部大脑”，让模型能基于最新、最具体的数据进行推理。

Resources在2026年的生态里已经形成了相当成熟的URI惯例：文件系统Server用file:///前缀挂载本地文档；GitHub Server用动态模板暴露任意仓库、任意分支的文件，路径里的占位符填上值就能直接读；PostgreSQL Server干脆把表结构本身作为资源挂出来，让模型先“查阅”schema，再决定怎么查数据——这是Resources与Tools协作的典型姿势。我自己的Knowly是一条剪贴板知识流水线，它把我每天复制的碎片自动清洗、打标、沉淀成带元数据的资源，AI写作时按需取用。对我来说，“外部大脑”不是修辞，是每天在跑的管道。

三种能力之间还藏着一层隐秘但重要的分工：控制权归属不同。Tools由模型控制，何时调用是模型的判断；Resources由应用控制，挂载什么、何时注入由Host决定；Prompts由用户控制，模板的触发权在用户手里。设计Server时先想清楚“每个原语把控制权交给谁”，架构就不容易乱——这个判断框架，是我从无数次返工里省出来的。

**3. Prompts（提示模板）**

这是容易被忽略但极具想象力的能力。Prompts是预定义的、可重用的交互模板。例如，你可以设计一个“代码审查Prompt”，它内置了“检查安全隐患”、“评估性能瓶颈”、“建议更地道的写法”等一系列指令序列。用户或AI只需调用这个Prompt的名字，就能执行一整套复杂的工作流。它把零散的指令，打包成了可复用的“智能流程”。

Prompts的生产案例，我见得最多的是代码审查场景：一些团队把资深工程师的审查清单固化成Server端的review_pr与debug_error模板，新人在对话框里敲一个斜杠命令，就能召唤出一整套带检查项、带输出格式约定的审查流程。模板复用的不是提示词文本，而是“工作流本身”——个人经验由此变成团队资产，人走了，方法论还在。

这三种能力组合在一起，让MCP从一个简单的“接口”升级为一个“能力编排框架”。你既可以暴露一个单独的工具（如“天气查询”），也可以组合资源和提示，构建出一个复杂的、有状态的交互场景（如“财务报告自动生成助手”）。

## 1.6 MCP与Function Call：能力与协议之辨

听到这里，你可能会想到另一个概念：Function Call。OpenAI在2023年就推出了Function Call，允许模型在响应时请求调用一个函数。那么MCP和它有什么区别？

我的理解是：Function Call是一种“能力”，而MCP是一套“协议”。Function Call是模型侧的一个API，它定义的是“模型如何请求调用外部函数”这一动作本身。而MCP定义的是“AI应用（不限于某个特定模型）如何与一个外部服务器进行标准化通信”的完整规范。你可以把Function Call看作是MCP协议里“工具调用”这一环节的一种具体实现方式。MCP的雄心更大，它不光管“调用”，还管“资源读取”、“流程模板”，并且它独立于任何一家公司的模型，是一个开放的生态标准。

在“能力与协议”这对概念之外，我想补上2026年回看时更重要的另一半：协同关系。一种流行的误会认为MCP出现之后Function Calling就过时了，实际情况恰恰相反——MCP Server从来不会被模型“直接”调用，每一次工具调用，底层走的仍然是模型的Function Calling链路（Anthropic把它叫作Tool Use）：Host把tools/list发现的能力清单转换成模型接口的tools参数；模型靠Function Calling决定是否调用、调用哪个、参数怎么填，输出一个结构化的调用意图；再由Host执行，经MCP发往Server；结果回填，模型继续生成。把Function Calling从这条链路里抽走，MCP一寸都动不了。

我的比喻是：Function Call是肌肉，MCP是神经系统。肌肉提供动作的原动力，神经把全身的肌肉标准化地接入中枢，让中枢不必为每一块肌肉单独布线。反过来看也成立：没有MCP的Function Calling是方言——OpenAI的函数调用、Anthropic的工具使用，各家的参数格式与消息结构都有微妙差异，你为一个模型写的胶水层，换个模型就得重写。MCP把“工具如何呈现给模型”这件事标准化了，方言差异被各家SDK吸收掉，Server的作者从此不用关心对面坐的是谁家的模型。

这不是理论推演，而是我每天在验证的事。同一份knasync Server，我把它接在Claude Desktop上，也接在WeClaw上，而WeClaw底层的模型与Claude并不同源——同一份工具定义，一字未改，两边都能正常发现、正常调用。协议的价值在这一刻兑现：写Server的人，获得了“不站队”的自由。

选择准确率的问题还有一个容易被低估的变量：清单长度。Function Calling是在整份工具清单上做选择的，清单越长，噪声越大。我做过一组压测：十几个工具混在一个列表里，模型的调用错误率只有3%左右；把清单膨胀到47个工具，错误率飙到了22%——协议没有变，模型没有变，变长的只是菜单。这也是为什么2026年社区开始流行工具路由与分层发现的思路，第六章我会用完整的压测数据拆解这件事。

当然，协同关系也划出了天花板：MCP保证工具“送得到”模型面前，但模型“选不选得准”，取决于工具描述与参数Schema的质量，这仍是Function Calling层面的事。描述含糊，再标准的协议也救不了；模型偶尔会幻觉出一个不存在的工具名，Host必须在执行前校验拦截。协议与能力，谁也替代不了谁——它们是一对互相成就的搭档，拆开哪一半，故事都不完整。

## 1.7 应用场景版图

那么，MCP真正能用在哪些地方？场景正随着生态的壮大而迅速扩展。你可以用它来增强你的AI助手，让它能实时查询公司内部的知识库；你可以为你的IDE开发一个MCP Server，让AI能直接读写项目文件、执行测试命令；你可以连接商业智能工具，让AI自动分析数据看板并生成报告；你甚至可以构建一个个人知识管理的闭环——就像我用Knowly做的那样，让AI持续整理我散落在剪贴板、网页和笔记中的碎片信息，并沉淀为结构化的知识资产。

在这段话之外，我想把2026年这幅版图上已经亮起的灯，一盏一盏报给你听。企业侧的采用轨迹是这样的：2025年3月，微软把MCP集成进Azure AI Foundry，OpenAI宣布支持；到2026年3月，OpenAI、Google DeepMind、Microsoft相继完成深度支持，三大阵营全部入场。Notion、Adobe、Stripe、Shopify、AWS则纷纷推出官方MCP Server——你的AI想读Notion文档、对Stripe账目、操作Shopify店面，官方通路已经铺好。国内生态同样在加速：腾讯的Marvis接入了超过十家MCP服务商；企查查的MCP于2026年8月上线，查询一家公司的工商信息，三十秒出结果。

生态的规模曲线更能说明问题。2025年6月，社区月度新增Server从135个激增到5069个；2026年5月，GitHub MCP Registry正式上线，公开Server数量在当年8月突破五千个；awesome-mcp-servers这份社区清单累计收获了八万五千多颗Star。多家2026年的技术报告给出同一个判断：九成以上的企业级AI工具，交付时会附带MCP Server——“是否提供MCP支持”正在变成采购清单上的一栏。治理层面也落了地：2025年12月，Anthropic把MCP捐赠给Linux基金会旗下的AAIF，协议从此不再是某一家公司的资产。

生产级的组合案例正在从演示走向日常。研发侧，把GitHub、监控告警、错误追踪、数据库四类Server接进同一个研发助手，工程师用自然语言完成一次线上排障，耗时从两小时的量级压缩到秒级；财务侧，把数仓、记账、CRM与在线表格四类Server串起来，季度经营分析从“三天的取数加对表”变成“半小时的对话”。成本账也有人算过：有海外技术团队测算，中型团队自建八个工具的集成，首年约需十二万美元，三年累计超过二十二万美元；改走MCP，首年约一万五千美元，三年约两万五千美元，节省近九成。数字未必每个团队都能复现，但方向是公认的——集成的边际成本被协议拉低了。

这幅版图对个人开发者同样敞开。官方Server覆盖的是通用需求，而五千个公开Server之外，长尾的垂直场景反而更值钱：把一个行业的检索口径、一家公司的内部流程、一类小众数据的清洗规则封装成Server，就等于在新标准上开了一家只此一家的店。我的knasync就是这么长出来的——它没有对接什么著名平台，只是把我自己“搜索、沉淀、发布”的知识工作流封装成了协议能力，却成了我日常用得最重的工具之一。标准化的吊诡之处在于：接口越统一，独特的数据与流程就越稀缺，而稀缺正是价值所在。

版图上也有阴影。2026年3月，Perplexity的CTO Denis Yarats公开宣布放弃MCP，改回直连API加命令行的路线，核心理由是上下文成本——工具清单本身也是要占用Token的，接得越多，每次对话的固定开销越大。这个故事我留到第八章细讲，现在提它只是为了诚实：一项技术的真实地位，是由留下来的人和离开的人共同标注的。

MCP的诞生，源于一个朴素的洞见：与其让每个AI应用为每个数据源写一套定制的“翻译”，不如大家统一说同一种“普通话”。这个洞见，在软件工程史上屡次被验证——TCP/IP统一了网络，HTTP统一了Web，LSP统一了IDE的智能感知。现在，轮到MCP来统一AI与外部世界的对话方式了。它不是某个公司的专属特权，而是一个开放的标准，等待着每一位开发者去接入、去扩展、去重塑。

## 1.8 独思时刻：契约先于连接

💡 独思时刻： 我始终觉得，将MCP仅定义为“AI的USB-C接口”是一个过于安全的比喻。它描绘了连接和便利，却抹平了这场变革中更本质的摩擦。我自己在把玩WeClaw和Knowly的过程中，最深的体感不是“插上就能用”，而是MCP强制性地要求我去“声明”和“封装”我的世界。过去，我写一个Python脚本，脑子里想的是“如何一步步实现”；现在，我构建一个MCP Server，脑子里想的是“我向AI暴露了哪些能力，以及这些能力的边界在哪里”。这种视角的转换，逼迫我把模糊的“功能”抽象为清晰的“协议”。对我来说，MCP的首要价值不是“连接”，而是“契约”——它让我和AI第一次站在了同一份明确的“能力清单”面前，对话从此有了共同的坐标系。这远比一条即插即用的线缆，要深刻得多。

💡 独思时刻： 写完这一章，我又想起抽屉深处那团USB-C出现之前的旧线缆。这一年，我对“契约先于连接”有了更具体的体感：MCP真正改变的，是把集成的决策从运行时挪到了设计时。过去用胶水代码连接两个系统，决策散落在每一行代码里，出了问题只能在日志里考古；现在写一个Server，要先把能力清单、参数边界、返回格式、错误语义想清楚再动手——契约立住的那一刻，连接只是水到渠成的最后一厘米。但契约同时也是负担。每一条工具描述都是一份对AI的公开承诺，改一个参数名就是一次违约，所有依赖它的Host都可能受影响；每多暴露一个工具，就在每一次对话里多吃掉一份上下文预算。声明得越多，欠下的就越多。深夜里我时常提醒自己：能力清单不是越丰富越好，而是越诚实越好——你声明什么，AI就相信什么。这份克制，是协议教给我的、比任何接口都珍贵的东西。

当你完成了这一章的理论漫游，你可能会忍不住想：听起来很美，但亲手做一个MCP服务器到底难不难？别急，下一章，我们将回到时间线的起点，去看看在MCP诞生之前的“混沌时代”，那片催生了这场变革的荒野。

---

## 第2章 MCP的诞生——从0到1

# 第2章 MCP的诞生——从0到1

2024年深秋，我正窝在上海办公室里调试一个让人抓狂的集成。Claude要读取我本地的一个Markdown文件，我需要写一段专门的胶水代码；它要查数据库，我得再写一套；它想调用我写的那个Python脚本，又是一套。三套代码，三个不同的数据格式，三份维护负担。我看着屏幕上乱七八糟的配置文件，心想：这东西难道就不能有个统一的标准？

那晚的细节我至今记得清清楚楚。为了让Claude读那个Markdown文件，胶水代码要先处理编码，再做格式转换，还得把异常兜住；查数据库那套，从连接、查询到结果序列化层层包裹；调用Python脚本那套，又要伺候子进程的输入输出。三段代码里，真正和业务相关的部分加起来不到五十行，其余全是在两个世界之间搭桥。搭桥这件事本身不难，难的是——每接一个新模型、一个新数据源，桥就要重新搭一次，而且没有图纸，没有公共规范。

两个月后，2024年11月5日，Anthropic开源了MCP协议。我第一时间点开了那个公告，第一反应是：终于有人把这层窗户纸捅破了。

捅破窗户纸靠的不是勇气，而是条件——混乱得足够久，蓝本得已经存在，窗口得恰好开着。这一章，我就把从那个深秋的抓狂，到那个星期二的公告之间发生的事，一段一段拆开讲给你。

## 2.1 前MCP时代：一片丛林

在MCP之前，AI与外部世界的对话方式，可以用一句话概括：每个AI应用，面对每个数据源，都要写一套定制代码。

假设你是2024年初的一名开发者，想让Claude读取公司内部数据库。你要写一个Python脚本，通过Claude的Function Call机制暴露一个接口，处理认证、查询、结果格式化，再把数据塞回给模型。想让ChatGPT也能读这个数据库？重写一遍，因为OpenAI的Function Call规范和Anthropic的不一样。想让Cursor在写代码时能访问同一个数据库？第三遍。想让Claude再读个本地文件？第四遍。

把这笔账写成数学题就更直观：M个模型，N个数据源，理论上需要M乘N个连接器。三个模型、十个数据源，就是三十条胶水代码。这样的税，软件工程史上交过好几轮——数据库时代各家接口五花八门，后来有了ODBC和JDBC；Web服务时代SOAP与私有API各自为政，后来有了REST。每一代计算平台都要重付一次集成税，区别只在税率。AI这一轮麻烦在税基涨得太快：模型几个月一版，API字段频繁变动，数据源还在爆炸式增长，M和N同时变大，乘法就成了灾难。

2024年3月，我参加一个技术分享会，听到一位来自某头部AI应用公司的工程师吐槽：他们维护了47套不同的外部集成代码，分别对应不同的模型和不同的数据源。每次升级一个模型版本，至少要花两周时间改集成层。全公司三分之一的人力，耗在了这种“翻译工作”上。

我当时在本子上记下“47套”这个数字，回来的地铁上算了笔账：47套集成，一次模型升级两周，一年哪怕只升三个版本，就是小半年的人力——这还只是一家公司，还只是集成层。散场后我在会场外站了一会儿，第一次清晰地感觉到，我们这代开发者正在集体偿还一笔本不该欠的债。

这不是个别现象。碎片化只是第一个痛点。跟着来的是重复开发——同样一个GitHub仓库读取功能，Claude版写一遍，ChatGPT版写一遍，Cline版再写一遍。然后是安全隐患——每个集成各自实现认证逻辑，有的用环境变量，有的硬编码在配置文件里，有的干脆没有认证。最后是维护困难——模型升级了，数据源改接口了，几十套集成代码同时炸，修都修不过来。

这四个痛点我后来做WeClaw微信网关时又亲手摸了一遍：同一个“读取会话历史”的能力，因为要接不同模型，我差点照着三份规范写三份适配层。碎片化最伤人的地方是让能力资产无法沉淀——你写的每一份集成代码都锁死在某个模型的某种规范里，换一个模型，资产归零。

这四个痛点像一个四棱锥，把开发者扎得动弹不得。而AI的能力每三个月跃升一次，外部工具的数量爆炸式增长，这二者的组合数不是加法，是乘法。MCP出现前的最后几个月，圈子里的焦虑感已经到了顶点——光靠局部优化已经解决不了问题了。

那种焦虑有很具体的形态：开发者论坛隔三差五就有人发帖问“有没有办法只写一次插件，所有模型都能用”，帖子底下的回答永远是“没有”；技术群里，凌晨还有人逐字段对齐不同模型工具调用的差异。当所有人都开始问同一个问题的时候，答案就快来了。

## 2.2 LSP：那盏引路的灯

MCP的两个设计者——David Soria Parra和Justin Spahr-Summers——都深度参与过另一个协议的设计：LSP（Language Server Protocol）。

先说说这两个人。David Soria Parra来Anthropic之前参与过PHP内核与HHVM的开发，手头一直做着与LSP相关的项目；Justin Spahr-Summers是ReactiveCocoa的作者，早年在GitHub写开发者工具。两人有个共同点：职业生涯的大部分时间都在给其他开发者造工具。给开发者造工具的人，最清楚开发者的痛在哪里。

2016年，微软开源了LSP。它的核心逻辑朴素到近乎粗暴：IDE（如VS Code）和编程语言（如Python、Java）之间，统一说同一套“普通话”。LSP定义了一套标准消息格式，语言服务器实现“翻译”，IDE再也不用为每种语言写一套智能感知逻辑。LSP出现之前，让VS Code支持一种新语言的自动补全、跳转定义、错误提示，需要写几千行插件代码。LSP出现之后，语言开发者只需要实现一个LSP Server，所有支持LSP的IDE立刻获得该语言的完整支持。

把镜头拉远看LSP的来历，会发现它和MCP的命运惊人地相似。LSP不是凭空设计的——它是微软为自己的编辑器VS Code造的。VS Code在2015年4月发布预览版，随之而来的问题是：一个刚出生的编辑器，怎么支持世界上所有语言？给每种语言各写一套插件，写到猴年马月。微软的解法是把“语言智能”从编辑器里搬出来，搬进一个个独立的语言服务器进程，编辑器与服务器之间用一套消息协议对话。2016年年中，微软联合红帽与Codenvy，把这套协议作为开放标准推了出来。协议跑在JSON-RPC上，本地走stdio——这个技术选型，后来被MCP原样继承。

这个模式的成功是现象级的。到今天，几乎所有主流编程语言都实现了LSP Server，几乎所有主流IDE都支持LSP。它证明了协议标准化的力量：定义一次，到处复用。

生态数据可以佐证“现象级”三个字：Go有gopls，Rust有rust-analyzer，C与C++有clangd，Python有pyright，Java有Eclipse JDT LS，每个语言社区都把自家的LSP Server当基础设施来养；编辑器一侧，Neovim从0.5版本起把LSP客户端做进内核，Emacs有lsp-mode和eglot，JetBrains靠插件接入，Zed干脆原生。官网上登记的语言服务器实现早已过百，规范本身从1.x一路迭代到3.17。如今一门新语言诞生，社区的第一反应不再是为某个编辑器写插件，而是实现一个LSP Server——这就是标准化深入骨髓之后的样子。

回头看，LSP留给后来者的不只是技术方案，更是一套设计哲学：第一，把智能外置——编辑器只做壳，理解语言的部分独立成进程，壳与核可以各自演进；第二，契约先行——双方先谈好说什么、怎么说，再去谈各自的实现；第三，能力协商——握手时互相亮出支持哪些特性，协议因此可以平滑长大而不断裂。这三条后来都被MCP接了过去，只是听话的一方从编程语言换成了数据源与工具。

MCP的设计者几乎原封不动地借用了这个思想。只是把“IDE ↔ 编程语言”的对话，替换成了“AI应用 ↔ 数据源/工具”的对话。2024年6月，我在一次闭门技术交流中听到Justin Spahr-Summers说过一句话，大意是：我们不需要重新发明轮子，LSP走过的路，MCP再走一遍，只不过路上跑的从代码补全变成了工具调用。

这个选择极其务实。LSP用了八年时间趟过的坑——版本协商、能力发现、传输层选择——MCP直接在协议设计初期就规避了。它不是从零开始的孤岛，而是站在LSP肩膀上望向AI时代的一座灯塔。

MCP的诞生故事，起点其实平淡得出奇。2024年夏天，David Soria Parra在Anthropic写开发者工具，日常用Claude Desktop辅助干活，却受不了在Claude Desktop和代码编辑器之间反复复制粘贴。烦躁到一定程度，职业本能就冒了出来：他正做着LSP相关的项目，越想越觉得这个模式能搬到AI集成上——LSP把编辑器乘语言的M×N问题压成了M+N，AI集成的困境不正是同一个问题的放大版吗？他把想法带给Justin Spahr-Summers，两人几乎立刻开工。那年9月下旬，MCP的规范仓库在GitHub上建立，离正式发布只剩一个多月。发布前一个月，Anthropic内部办黑客马拉松，好几个团队已经用MCP给Claude Desktop接上了各种工具，其中一台被驱动起来的是3D打印机——抽象层的通用性，在最不正经的场景里得到了最正经的验证。

## 2.3 那个星期二：2024年11月5日

2024年11月5日，星期二。Anthropic在官网上发布了一篇公告，同时放出三样东西：

1. MCP协议规范——定义了Host-Client-Server三层架构、JSON-RPC消息格式、Resources/Prompts/Tools/Sampling四大核心能力。
2. MCP SDK——Python和TypeScript两个版本，开发者可以直接用SDK构建MCP Server，不用从协议解析写起。
3. Claude Desktop对MCP的支持——Claude Desktop成为第一个原生支持MCP的Host应用，用户可以在Claude Desktop里直接连接任意MCP Server。

有几个当天容易被忽略的细节，值得补上。第一，初版规范的版本号就叫2024-11-05——Anthropic把发布日焊进了协议版本，此后每次修订都以日期命名（2025-03-26、2025-06-18……），版本的严肃性从第一天就写进了文件名。第二，与规范同时开源的还有一批官方参考实现——文件系统、Git、GitHub、Slack、Google Drive、Postgres、Puppeteer——从“读本地文件”到“操作数据库”再到“驱动浏览器”，第一天就把最高频的场景铺满，开发者照着抄就能跑。第三，公告点名了六家早期采用者：企业侧的Block和Apollo，开发者工具侧的Zed、Replit、Codeium和Sourcegraph。其中Zed的身份很微妙——它不是Anthropic的产品，第一个第三方编辑器实现是“外人”做的，代码出自David Soria Parra本人之手，发布前一个半月就已跑通。Block的CTO Dhanji R. Prasanna在公告里表态：开放技术是连接AI与真实世界应用的桥梁。一家做支付的公司给一个AI协议站台，这个信号比公告本身更有说服力。

这三样东西加在一起，等于把“标准”和“落地”同时砸向了市场。不是画一张大饼，而是带着第一个能吃到的饼一起端上来。

为什么“三件套”这个组合如此关键？我的理解是：只有规范，是一篇论文；只有SDK，是一个库；只有Host，是一个产品。三者齐发，才构成一个当天就能验证的闭环——读完规范，装上SDK，在Claude Desktop里连上自己写的Server，全程不超过一小时。开发者对新标准的信任不是读出来的，是跑出来的。

我在公告发布当天的下午就下载了Claude Desktop和Python SDK，照着官方示例，花了不到四十分钟就写出了一个能读取本地文件夹的MCP Server。那一刻的感受很复杂：之前花好几天才能搞定的集成，现在四十分钟搞定。差距不在代码量，在于思考方式——我不再想“Claude怎么读这个文件夹”，而是想“这个文件夹的能力如何通过MCP协议暴露”。

那天我的时间线大致是：早上刷到公告，上午把规范扫了一遍；中午在几个技术群里看到有人贴出跑通的截图；下午动手，四十分钟出活；晚上我把文件夹读取器丢进一个开发者群，第二天早上收到几十条追问。群外的世界也差不多：发布当周，论坛和社交网络上的讨论一路走高，兴奋的人连夜写Server，泼冷水的也在——“又一个厂商标准，八个月后就凉”的唱衰帖收获了不少赞。社区当月就冒出了专门索引第三方Server的awesome列表，这份列表到2026年累计收到85000多个Stars——那是后话，第8章再表。有意思的是，两拨人的情绪指向同一个事实：开发者的手是有记忆的，被集成债烫过的人，看到一个当天就能跑通的方案，身体往往比大脑先做出反应。

## 2.4 初代设计：有状态模型与四大支柱

MCP的初代架构，可以用三个词概括：三层、有状态、四大能力。

三层架构，你已经在第一章见过了：Host（Claude Desktop这类AI应用）发起连接；Client（Host内部的连接器）管理通信；Server（提供数据和工具的服务）执行具体操作。这三个角色边界清晰，各司其职。

这套划分的意图值得再咀嚼一遍：它把“连接管理”从Host的业务逻辑里剥出来交给Client，把“能力实现”从Host的代码库里剥出去交给Server。Host专心做对话体验，Server专心做领域能力，Client充当中间那个懂协议的翻译。对我们这些Server开发者来说，这个划分意味着一个朴素的承诺——你不必讨好任何一家Host，只需要把协议说对。

有状态模型，是初代MCP最核心也最争议的设计决策。客户端必须先发送一个initialize请求，完成握手，建立会话，之后才能发送其他请求。这个会话ID会贯穿后续所有通信，服务器依靠这个ID来识别“谁在调用”。这个设计简化了初始实现——开发者不需要在每个请求里都带上完整的上下文信息，服务器只要记住会话就行。但它埋下了隐患，我在后文会展开说。

为什么第一版敢选有状态？回到当年的主流场景就明白了：本地stdio，Server是Host拉起的子进程，进程的一生就是会话的一生，有状态几乎零成本。而且initialize握手本身是个好设计——双方在握手时交换各自的能力清单，互相声明“我支持什么”，这个动作直接继承自LSP的能力协商，让协议从第一天起就留出了向前演化的余地。真正的问题出在远程：会话状态在单机上是资产，在分布式上是负债——断线重连、水平扩展、负载均衡，每一项都在跟“服务器得记住你是谁”作对。这笔债一直挂到2026年7月的无状态化大版本才还清，第3章细说。

四大核心能力：

· Resources：让AI读取数据。文件内容、数据库记录、API响应，只要是数据，都可以暴露为Resource。AI可以像读本地文件一样读取这些资源，不需要关心数据来自哪里。

· Prompts：预定义的交互模板。一个“代码审查Prompt”可能包含检查安全隐患、评估性能瓶颈、建议更地道的写法等一系列指令序列。用户或AI只需要调用这个Prompt的名字，就能执行一整套复杂流程。

· Tools：AI可执行的函数。搜索、计算、数据库写入、发送邮件——任何有副作用的操作，都封装为Tool。AI通过调用Tool来影响外部世界。

· Sampling：服务器主动发起LLM调用。这是最容易被忽视但极富想象力的一项。它意味着MCP Server可以反过来请求Host调用一个LLM来完成任务。比如，一个“代码修复Server”可以自己调用Claude来生成修复补丁，而不是等用户手动触发。

Sampling值得单独展开，因为它是四大能力里唯一“反着走”的一个：前三项都是Host请求Server，Sampling是Server反向请求Host调用LLM。链路是这样的——Server在执行任务途中发现自己需要一次模型推理，就向Client发起采样请求；Client不会直接照办，它先把请求亮在用户面前，等用户批准；批准后，Host调用自己的模型完成生成，再把结果送回Server手里。有三个设计细节决定了这个机制的成色。第一，模型无关：Server永远不指定、也感知不到背后用的是哪个模型，算力成本记在Host的账上。第二，上下文主权：生成时用什么系统提示、携带哪些上下文，组装权在Host，Server只能描述“我需要一次什么样的生成”，规范同时要求Host对生成内容明确标注来源——这一条堵死了“Server借Sampling搞提示注入”的后门。第三，人在回路：默认必须用户点头。三个细节合起来是同一句话：反向调用打开了想象力，但方向盘始终握在用户手里。

这不是纸上谈兵。我在Knowly里真金白银地用过Sampling：剪贴板知识流水线里有一个场景，文本片段要补全上下文才能归档，按老办法我得自己搭一条“调模型—解析—回写”的管道；有了Sampling，Server处理到片段时直接向Host发起采样请求，几十行代码替代了一整层胶水。2025年11月的版本又给Sampling补上了工具调用支持，让被采样调用的模型可以反过来使用Server的工具——这是后话，第3章再表。

这四大支柱，让MCP不仅仅是“接口”，更是一个双向能力编排框架。

传输机制上，初代MCP支持两种方式：本地用stdio（标准输入输出），远程用HTTP+SSE（Server-Sent Events）。这种双模式设计非常务实——本地场景追求低延迟、无网络开销；远程场景追求跨机器、跨网络。开发者可以按需选择。

双模式同样是LSP式的思维：LSP当年也是stdio起步，后来才补上socket等传输。MCP把本地与远程两条路同时铺好，等于宣布这个协议不打算只做桌面玩具。当然，HTTP+SSE这条远程路在2025年3月被Streamable HTTP取代——初代的传输选型本身也是一处伏笔，同样留给第3章。

## 2.5 三个被忽略的细节

在2024年11月的喧嚣中，有三个细节被大多数人忽略了，但它们定义了MCP后来的走向。

第一个是版本号机制。初代MCP没有在协议层面强制声明版本。客户端和服务器各说各话，兼容性靠“约定俗成”来维持。这个缺失在早期没暴露出大问题，但当协议开始频繁迭代时，它成了巨大的技术债。

有一个耐人寻味的对照：规范的修订版本偏偏用日期命名，初版就叫2024-11-05，可见设计者心里完全清楚“版本”二字的分量，却依然把强制协商的成本省掉了——大概是觉得生态早期，规范一方说改就改，天下无敌。直到2025年6月，MCP-Protocol-Version请求头成为强制项，这颗种子才算真正发芽。

第二个是工具描述的粒度。初代Tools的description字段只有一段纯文本，模型靠这段文本来理解“这个工具是干什么的”。如果你写的描述不够精确，模型会误解工具的用途，甚至在不该调用的时候调用。我当时在调试一个“删除文件”工具时，Claude两次误删了测试文件，原因就是我用了“清理”这个词，模型理解为“整理”，实际上该用“删除”。这个教训让我明白：给AI的工具描述，要像给一个超级认真但缺乏常识的新员工写操作手册。

那次误删之后，我把现场复盘成三条硬规矩：描述里写动作动词，不写修辞；把“不要做什么”明明白白写进描述；副作用越大的工具，描述越要保守。后来我甚至观察到一个更细的规律——工具描述超过200 Token之后，Claude对它的理解准确率会明显下降，描述不是越长越好，后记里我专门讲。这颗种子的后续是2025年6月的结构化输出：返回值长什么样也写进了契约，工具与模型之间的“误会空间”被进一步压缩。

第三个是授权机制的缺位。初代MCP没有任何标准化的授权框架。谁有权限调用某个Tool？谁能读取某个Resource？每个Server自己实现，五花八门。有的用API Key，有的用OAuth，有的什么也不用。这在本地开发场景下问题不大，但一旦MCP走向企业级、走向远程部署，安全就成了第一道拦路虎。

本地时代授权之所以“看起来没问题”，是因为信任边界就是你的笔记本：Server是你亲手拉起的子进程，它的权限就是你的权限。远程部署把这个边界推到公司内网甚至公网，“谁有资格调用”就从工程细节升格为治理问题。五个月后的2025年3月26日，OAuth 2.1被写进协议本体，授权从“每个Server自己想办法”变成“协议统一兜底”——那是三颗种子里最粗壮的一颗。

这三个细节，像是埋在初代版本里的三颗种子。五个月后，它们发芽了。

## 2.6 为什么是2024年11月？

你可能会问：MCP的思想并不复杂，为什么不在2023年就做出来，非要等到2024年底？

答案藏在AI应用生态的演化曲线上。2023年是“模型能力爆发”的一年——GPT-4发布，Claude 2问世，开源模型如Llama 2崭露头角。开发者都在拼命探索“模型能做什么”。到了2024年，焦点从“模型能做什么”转向了“模型能帮我们做什么”——AI Agent、自动化工作流、企业级AI应用成为主流叙事。而这一切的前提，是AI必须能可靠地连接外部世界。

给这段曲线钉上时间锚点：GPT-4在2023年3月亮相，Claude 2与Llama 2都在2023年7月发布；同年6月，OpenAI推出Function Calling，“模型调用工具”第一次成为主流能力。但请注意，Function Calling是各家API的私有规范——模型能调用工具，不等于所有模型能用同一套方式调用工具。能干的工人都到位了，工地上的语言却不通。

2024年上半年，Anthropic、OpenAI、Google三家都在推进自己的“工具调用”方案，但各搞一套，互不兼容。开发者社区的抱怨声越来越大——我们不想为三个模型维护三套插件系统。同时，LSP在编程语言生态的成功，为MCP提供了现成的叙事模板和设计蓝本。

把2023年到2024年11月的关键动作排成一条时间线，窗口的形状会看得更清楚：

· 2023年6月，OpenAI发布Function Calling——私有规范，绑定自家API。
· 2023年11月，OpenAI推出GPTs与Actions——封闭生态，schema私有。
· 2024年10月，微软宣布Copilot Studio支持自定义插件——专用API，围绕自家生态。
· 2024年10月，Google发布Vertex AI的扩展框架——同样私有。
· 2024年11月5日，Anthropic开源MCP——免费、开放，带着SDK与Host落地。

时间线往前每一格，碎片化就加深一分：每家都在修自己的高速路，收费站还互不通用。而MCP开源之后的走向，反过来验证了这个窗口判断——2025年3月，OpenAI宣布支持MCP，同月微软把MCP集成进Azure AI Foundry，再往后Google DeepMind也宣布跟进。巨头们最终还是回到了这条共同的路。那不是Anthropic说服了谁，而是M乘N这笔账，谁算都一样疼。

Anthropic选择在2024年11月开源MCP，既是技术成熟度的结果，也是市场窗口期的选择。Claude 3.5 Sonnet在2024年6月发布，其工具调用能力已经足够支撑MCP的复杂交互；而OpenAI的GPT-4o系列和Google的Gemini 1.5系列也在快速迭代，如果Anthropic再不推出统一标准，碎片化就会固化下来，成为整个行业的沉没成本。

还有一个外部信号：2024年10月，微软宣布了Copilot Studio对自定义插件的支持，但依然采用专用API；同月，Google发布了Vertex AI的扩展框架，同样是私有的。这三家巨头都在各自修路，而开发者需要的是一条共同的路。Anthropic选择在11月把这条路公开、免费、开源，卡的就是这个时间差。

当2024年11月5日那个星期二到来时，MCP不是一个实验室里的理论模型，而是带着Claude Desktop、SDK、规范文档一起落地的完整产品。它没有等待生态自然演化，而是主动把标准砸进市场，让开发者立刻能用、立刻能验证、立刻能反馈。这种“带着落地方案发布标准”的策略，才是MCP能从0到1跨越那道鸿沟的真正原因。

如果它早一年出来，AI模型的能力还不够可靠，开发者做出来的MCP Server可能因为模型理解偏差而频频出错；如果它晚一年出来，各大厂商的私有接口可能已经形成事实标准，生态的迁移成本高到无人愿意承担。2024年11月，不是一个随机的日期，而是AI应用生态从“集成困境”到“标准统一”之间的那个唯一的、狭窄的、恰好出现的窗口。

窗口的出现需要三样东西同时就位：模型的工具调用能力过了可靠线，LSP的蓝本已经验证过一遍，开发者的集成焦虑攒到了阈值。2024年11月，三样东西第一次同时在场——这就是全部秘密，没有任何神秘成分。

## 2.7 独思时刻：为数字心智铺设轨道

💡 独思时刻： 我在用Claude Desktop连接我写的第一个MCP Server时，一种奇异的错位感攫住了我。屏幕那端，AI正在有条不紊地读取我指定的文件夹——那是我的代码，我的接口，我的机器。可整个过程的主导权，却清晰地握在那个模型手里。那一刻我意识到：过去的二十年，我学习编程，学习系统架构，学习把所有东西封装成函数、类、微服务，本质上都是在为人类开发者建造阶梯。但MCP的出现，让这些阶梯第一次有了另一个攀登者。我不再仅仅是在为“下一个接手项目的程序员”写代码，我同时也在为那个没有体温、却拥有无限耐心的数字心智铺设轨道。这种感觉，比我写过的任何一行生产代码，都更深刻地重塑了我对“构建”这件事的理解。

💡 独思时刻： 复盘MCP的诞生史，我时刻提醒自己警惕一种叙事的诱惑——把一切归结为天才的灵光一现。把这一章的事实摆开看，MCP更像四条线拧成的绳：混乱累积到阈值是需求线，LSP蓝本是方案线，2024年10月巨头各自修路是时机线，David那份复制粘贴的烦躁是动机线。缺任何一条，2024年11月5日都不会是那个日子。历史书写偏爱英雄叙事，但工程师复盘历史，应该像复盘一次线上事故——问的不是“谁做了什么”，而是“哪些条件在哪个时刻对齐了”。这样复盘的价值不在崇拜，而在训练眼光：下一次某个新协议出现在你面前，先别急着问它好不好，先问三件事——混乱够不够久？蓝本在不在？窗口关门前还剩多少时间？

💡 独思时刻： 还有一个更私人的感受，写在这里存档。我职业生涯的前半段，写的接口都是给人用的——同事、合作方、下一个接手项目的人。MCP之后，我开始大量地写给机器用的接口。起初我以为只是受众变了，后来发现变的是整个设计姿势：给人写的接口可以靠文档和沟通补救，给数字心智写的接口必须一次说对，因为它不会来问你。工具描述、错误信息、返回结构，每一处含糊都会被模型用自己的方式解读。从这个意义上说，MCP教会我的不是一套协议，而是一种新的精确性——对机器说话，措辞即契约。

MCP的诞生，不是灵光一现的发明，而是水到渠成的必然——当混乱累积到阈值，当蓝本已经存在，当窗口恰好打开，那个对的人，在对着的时刻，做了对的事。而我们对这段历史最好的纪念方式，不是把它供奉为神话，而是让它的演化轨迹，成为我们理解下一个协议、下一个标准、下一个从0到1的故事时，手里攥着的那把钥匙。

---

## 第3章 MCP的演进史——从实验室到生产环境

# 第3章 MCP的演进史——从实验室到生产环境

2024年11月5日，MCP像一个新生儿，躺在Anthropic的实验室里，干净、纯粹，但也脆弱。它只认得stdio和HTTP+SSE这两种“语言”，依赖一个有状态的会话模型——客户端必须乖乖地先握手，再说话。

如果你读过第1章，应该还记得我那个花四十分钟写出来的文件夹读取器——它就跑在初代协议上，像一间毛坯房里接好的第一盏灯，能亮，但经不起风雨。那盏灯照亮了一种新范式的可能，也让所有早期使用者同时看清了毛坯房本身的裂缝：没有统一的鉴权，没有生产级的传输，没有一个说得清自己该活多久的会话。

五个月后，它第一次走出实验室，穿上了OAuth 2.1的西装，换上了Streamable HTTP跑鞋。又过了三个月，它主动撕掉了刚缝上的“批处理”口袋，补上了一块叫Elicitation的智能补丁。再往后，它长出了工作组、兴趣组、OpenID Connect的骨骼，开始像真正的生态一样呼吸。

从2024年11月到2026年的今天，MCP一共经历了四个正式版本，外加一个正在路上的Draft。每一次版本号的变化，都是一次对“AI与工具之间该如何对话”这个问题的重新回答。

而在我修订这一章的时候，那个“正在路上”的Draft已经转了正——2026年7月28日，第五个正式版本落地，这是MCP诞生以来最大的一次改版，也是本章3.5节的主角。协议的一生和人一样：实验室里的完美假设，最终都要被生产环境的真实使用一寸一寸地修正。这一章，我想带你把这条演进之路完整走一遍——看每一次版本变化背后的取舍、代价，以及设计者的勇气。

## 3.1 2025-03-26：第一次进化——企业级的入场券

2025年3月的一个下午，我在调试WeClaw的MCP集成时，发现了一个令人头疼的问题：我想让微信里的AI通过MCP读取公司内网的Confluence文档，但那个MCP Server不知道怎么处理用户身份。每次请求都像匿名访客，能看什么、不能看什么，完全失控。

做了十八年金融系统的人，对这种“匿名访客”有本能的警觉。更麻烦的是排查：同一个微信群里的两位同事，A能看到的那份财务文档，B居然也能看到——不是Server故意放行，而是它压根不知道“用户”这个概念的存在。所有请求在它眼里都是同一个匿名访客，权限模型自然无从谈起。

这个困扰在我看到2025年3月26日的更新公告时，瞬间有了答案。

这次更新最核心的变动，是引入了基于OAuth 2.1的授权框架。MCP终于回答了一个企业级场景下无法回避的问题：谁在调用？能调什么？

在初代协议中，每个MCP Server都自己“发明”了一套鉴权方式——有的靠HTTP Header里塞一个固定Token，有的依赖stdio的进程用户身份，还有的根本不做鉴权。这在本地开发环境里勉强能用，但一旦Server被部署到云端、被多个客户端共享，就成了安全隐患的温床。

OAuth 2.1的引入，意味着企业可以用现有的身份基础设施（比如Okta、Azure AD、Keycloak）来管理MCP的访问权限。用户登录一次，就能获得一个授权令牌；Server收到请求后，可以验证这个令牌，并据此判断用户是否有权限读取某个Resource、调用某个Tool。

这次升级真正改变的，还有MCP在企业安全评审会上的地位——以前评审官会问“你们这个协议怎么鉴权”，答案是“各自发明”；现在答案是“OAuth 2.1，直接接你们公司的IdP”。很多企业内网的门，就是从这一天起对MCP打开的。

这里值得多说两句OAuth 2.1本身。它不是一个推倒重来的新协议，而是把OAuth 2.0十年间散落在各种RFC和安全公告里的最佳实践，拧成了一份默认强制开启的规范。相对2.0，它有几个关键差异。第一，授权码模式必须搭配PKCE，且code challenge强制使用S256算法——无论你是跑在手机上的公共客户端，还是握有机密的后端服务，都逃不掉这道防授权码截获的手续。第二，隐式模式（implicit flow）和密码模式（password grant）被直接移除——这两扇2.0时代留下的“方便之门”恰恰是令牌泄露的重灾区，2.1选择把门焊死，而不是贴一张“谨慎使用”的告示。第三，重定向URI必须精确匹配，通配符与前缀匹配不再被允许，授权码被诱骗到钓鱼站点的经典攻击面就此收窄。第四，刷新令牌推荐轮换使用——一次一发、用过即换，令牌泄露后可利用的窗口被压到最短。对MCP而言还有更微妙的一条：规范要求令牌绑定到明确的资源服务器（对应RFC 8707的资源指示符），你为A Server签发的令牌，拿到B Server那里就是废纸。在一个多Server并存的生态里，这条“令牌不可跨店使用”的约束，比任何单点系统都重要。

这次更新同时宣告了SSE传输的退休，取而代之的是Streamable HTTP。

我对比过两种传输方式的差异。初代的HTTP+SSE像一条单向管道——Client发起请求，Server通过SSE通道推送响应。如果Server需要主动向Client发送消息（比如进度通知、状态变更），就得另辟蹊径。Streamable HTTP则支持双向流，Server可以在任何时候通过同一个HTTP连接向Client发送消息，这在长时间运行的任务中尤其重要。

把镜头再拉近一点，看看Streamable HTTP在协议层到底是怎么工作的。它把初代“两个端点、两条通道”的模型收敛成了一个统一端点（约定俗成的路径是/mcp）。Client发出的每个POST请求，都在HTTP头部用Accept字段声明自己能同时接受application/json和text/event-stream两种响应——Server按需选择：简单的请求直接回一个JSON；而tools/call这类可能产生进度通知的调用，则可以回一条SSE事件流，最终结果与沿途的通知都在同一条流里。

```http
POST /mcp HTTP/1.1
Host: mcp.example.com
Content-Type: application/json
Accept: application/json, text/event-stream
MCP-Protocol-Version: 2025-06-18

{"jsonrpc":"2.0","id":7,"method":"tools/call","params":{"name":"gen_report","arguments":{"month":"2025-06"}}}
```

与此同时，Client还可以用GET打开一条专门用于Server主动推送的SSE长连接——资源变更、进度更新从这条流下来；Server如果不打算主动推送，回一个405拒绝即可，协议并不强求。最实用的细节是断线恢复：SSE事件可以携带id，连接中断后，Client在重连请求里带上Last-Event-ID头，Server就能从断点补发错过的事件。这个特性救过我一命：有一次调试一个长任务，调试进程意外断开，重连之后进度事件从断点续上，而不是从头刷一遍——在初代协议下，断线意味着一切重来。

另外值得一提的是，会话在这个版本里还存在——Server可以在initialize之后通过Mcp-Session-Id响应头分配一个会话标识，Client在后续每个请求里带上它。这个此刻看起来人畜无害的设计，埋下了3.5节那场大变革的全部伏笔。

还有一个看似不起眼但很有意思的变化——工具注释（annotations）。

开发者可以在工具的输入/输出参数上添加readOnlyHint、destructiveHint等标注，告诉AI这个工具是只读的、还是可能产生副作用。AI在决策调用哪个工具时，可以利用这些信息做出更明智的选择。比如，当用户问“帮我看看昨天发生了什么”，AI会更倾向于调用带有readOnlyHint: true的日志查询工具，而不是带有destructiveHint: true的删除工具。

在金融行业待久了，我对这类标注格外敏感。同样一句“把这个账户处理一下”，配上readOnlyHint是查询，配上destructiveHint就可能是销户——AI的每一次选择背后，都是真实世界的资金与数据。annotations把风险提示写进协议契约，而不是散落在工具描述的措辞里，这是值得所有API设计者借鉴的思路。

不过，这个版本也埋下了一个“反共识”的种子——JSON-RPC批处理。

批处理的初衷很美好：减少网络往返次数，提高高延迟环境下的吞吐量。Client可以一次性发送多个JSON-RPC请求，Server批量处理后一次性返回。

## 3.2 批处理的短暂生命与“错误选择的代价不等于零”

这个功能的生命周期，值得单独用一节来讲——因为它短得反常，也诚实得反常。从出生到被承认失败，它只活了几个月；而围绕它发生的事，恰好是观察一个协议如何自我修正的最佳标本。

2025年4月，我尝试在WeClaw中实现批处理。当时的想法是：我可以把tools/list和resources/list打包成一个批次请求，省去一次网络往返。结果，我在处理“部分成功、部分失败”的场景时卡住了——规范对这种情况的表述含混不清，我不得不自己设计一套复杂的错误聚合逻辑。

卡住的具体画面，至今记忆犹新：一个批次里有十几个请求，第七个失败了，剩下的还执行吗？已执行的结果和失败的那一条怎么对应？是整批返回错误，还是逐条标错？规范只字未提。我最后写出的错误聚合逻辑有两百多行，每一行都是“我的发明”——这意味着它与其他任何实现都无法互通。一个本想收敛复杂度的功能，让每个实现者都额外发明了一套复杂度。

社区里和我一样踩坑的人不在少数。官方仓库的讨论区里，同样的问题被反复提出：批处理的语义与AI调用的实际形态并不匹配——AI一次通常只做一件事，真正想批量的是编排层，而编排层完全可以在协议外自行聚合。批处理解决的是传输层的问题，却被放进了调用语义层，位置本身就错了。这场实验只持续了不到三个月。

顺便说一句，批处理被移除后，“减少往返”的真实需求并没有消失——它后来以更体面的方式回归：一部分交给HTTP层，一部分化为2026年7月那个版本里的可缓存列表，这个线头3.5节还会接上。

💡 独思时刻： 我后来复盘这个“短命功能”时想通了一件事：错误选择的代价不等于零。 很多协议设计者倾向于“加功能”——多一个选项总比少一个好。但MCP团队在2025年6月做了一件让很多大厂不敢做的事：承认批处理是个错误，然后在下一个版本中直接移除了它。我见过太多项目为了“功能完整性”而保留那些设计失误的接口，最终成为拖累整个生态的技术债务。MCP团队用行动证明了一个朴素的道理：好的协议不是堆砌功能，而是敢于放弃错误的功能。 这种“反规模化”的勇气，恰恰是它逐渐走向成熟的关键。

💡 独思时刻： 复盘批处理，我还会多想一层：为什么“部分成功、部分失败”会成为每个实现者的噩梦？因为规范在一个关键语义上保持了沉默。歧义是工程问题的放大器——规范含混一格，实现就发散一丈，两个都“符合规范”的Client与Server放在一起，行为可能对不上。MCP团队没有试图用更多条款去解释批处理，而是问了一个更根本的问题：这个功能省下的那点往返时间，配得上它强加给所有实现者的复杂度吗？答案是否定的。评价一个功能，不能只看它解决了什么，还要看它让所有人额外承担了什么——这是我在那三个月里学到的最贵的一课。

## 3.3 2025-06-18：第二次进化——拥抱实用主义

2025年6月18日的更新，看起来像是一份“改错清单”：移除批处理、强制协议版本头、引入Elicitation、支持结构化工具输出。

移除批处理我已经经历过了。强制协议版本头则解决了一个隐晦的兼容性问题——在2025年3月到6月之间，不同客户端和服务器对协议版本的理解存在歧义，有些Client用的是2025-03-26草案，有些Server只认2024-11-05。HTTP请求头中强制带上MCP-Protocol-Version之后，Server可以在处理请求之前就明确拒绝不兼容的版本，避免了运行时才能发现的诡异错误。

这行看起来不起眼的请求头，其实是分布式系统里“故障前移”的经典手法：把兼容性检查从“运行十分钟之后的静默数据错乱”，提前到“请求进门的第一毫秒”。分布式系统的排障成本随时间指数上涨，能在门口拦下的错误，绝不要放进客厅。

Elicitation（服务器主动询问）是我个人最欣赏的一个新增能力。

初代MCP的设计基于一个隐含假设：所有工具调用的参数，在AI发起调用时就已经完全确定。但在真实场景中，工具执行过程中可能需要用户提供额外信息。

2025年7月，我在写一个“Excel报表生成”MCP Server时遇到了这样一个场景：AI说“生成一份上个月的销售报表”，Server需要知道“导出格式是PDF还是Excel”。按照初代协议，我只能让AI在调用时就指定格式，或者干脆默认PDF。但Elicitation让Server可以在执行中途向用户发起一个“问题”——“你想导出PDF还是Excel？”——然后在用户做出选择后继续执行。

从协议上看，这是一个名为elicit/create的请求：Server在工具执行中途，通过Client向用户弹出一个带requestedSchema的小表单——要PDF还是Excel、文件名模板是什么、要不要附带图表——用户填写或点选之后，答案随响应回到Server，执行从断点继续。用户也可以拒绝（decline）或取消（cancel），Server必须优雅地处理这两种回应，而不是把中断当成异常抛出去。它修补的正是初代协议那个隐含假设的裂缝：工具执行的中间态，同样可以有“人的输入”。

有人会问：这些信息为什么不让AI在调用参数里问清楚？因为有一部分信息只有“执行中”才暴露出来，AI在发起调用时根本无法预知；还有一部分是用户偏好，与其让AI猜，不如让Server直接问人。把“问人”做成协议能力而不是应用层的技巧，不同Client的表现才不会千奇百怪——有的弹表单，有的转述成自然语言提问，但协议语义是一致的。

这个能力听起来简单，但它把对话的控制权部分地从AI转移到了Server，让工具执行过程变得更有弹性。

结构化工具输出是另一个重要的改进。

之前的MCP工具只能返回文本或图片，AI拿到后只能通过自然语言理解来解析内容。现在，工具可以返回structuredContent（一个结构化的JSON对象）和outputSchema（声明输出结构的JSON Schema）。AI可以直接“读懂”结构化的字段，而不需要从非结构化的文本中提取信息。

2025年8月，我把WeClaw的“数据库查询”工具迁移到了结构化输出模式。AI发起“查询最近一周的活跃用户”后，Server返回的structuredContent包含了一个users数组和total计数字段。AI不再需要从一段自然语言描述中猜测“上周活跃用户可能有xxx个”，而是直接读取structuredContent.total，然后引用。

这种精确性，让AI在后续决策中的可靠性提升了一个台阶。

还有一个容易被忽略的兼容设计：声明了outputSchema的Server，仍然会随同返回一份非结构化的文本内容作为兜底——老Client拿不到结构化字段时，照样能从文本里读个大概。什么该新旧并存、什么该干净切割，是协议演进里最难的手艺；这个“结构化与文本并存”的温和姿态，与一年后无状态化的彻底切割，恰好构成了一组意味深长的对照。

## 3.4 2025-11-25：第三次进化——生态的成年礼

2025年11月25日的更新，标志着MCP从“协议”变成了“生态”。

OpenID Connect（OIDC）的支持，是对OAuth 2.1授权的自然延伸。企业用户可以通过OIDC Discovery自动发现授权端点，不再需要手动配置。增量授权同意则允许用户在实际需要时逐次授予权限，而不是在一开始就面对一个巨大的“请求所有权限”的弹窗。

增量授权这件事，手机用户都有体感：装一个应用就被要求交出通讯录、相机、定位的“全套授权”，和用到相机时才弹出一次询问，是两种完全不同的信任关系。对Agent来说这个设计更是关键——一个Agent要调用的工具可能横跨十几个权限域，一次性授权等于逼用户在不知情时签一张空白支票；增量授权把签发权拆到了每一张具体的单据上。

采样（Sampling）中工具调用的支持，让Server发起的LLM调用也可以携带工具。这意味着Server可以构建更复杂的Agent行为——比如，一个“自动发邮件”的Server在调用LLM生成邮件内容时，可以调用“联系人查询”工具来获取收件人的邮箱地址。

任务（Task）支持是个实验性功能，但指向了一个明确的方向：MCP不再只服务于“一次性请求-响应”模式，也开始拥抱“长时间运行的操作”。

这个实验性功能在协议层的样子，值得看一眼，因为几个月后它成了正式规范里的主角。Client发起长任务调用时，可以要求以任务方式执行；Server不再把结果挂在这条请求上，而是立刻返回一个任务对象——含taskId、状态status、状态说明statusMessage、创建时间，以及决定任务记录保留时长的TTL。状态机里有五个预定义节点：working（执行中）、input_required（等待用户补充信息，与Elicitation联动）、completed（已完成）、failed（失败）、cancelled（已取消）。Client用tasks/get查询状态（通常以轮询方式跟踪，Server可以在任务对象里给出建议的轮询间隔），用tasks/result取最终结果，用tasks/cancel中途叫停；任务结果本身作为资源存放，附带TTL，超时后Server可以清理。请注意这个设计里埋着的伏笔：长任务不再要求一条连接从头挂到尾——状态查询与结果获取是相互独立的请求，连接断了、客户端重启了，任务还在Server上安静地跑着。一年后彻底无状态化的种子，在这里已经发芽。对编排器来说，Tasks还有一层价值：多个子代理的长任务可以被统一登记、查询、取消，而不需要每个编排器各自维护一套私有的任务状态机——协议替所有人把这件事一次做对了。

最让我触动的是治理结构的正式化——Security Interest Group、Authorization Interest Group、Transports Interest Group相继成立。

2025年12月，我在MCP官方GitHub仓库里看到这些工作组的讨论记录时，意识到一件事：这个协议已经不再属于Anthropic一家公司了。它有了社区驱动的决策机制，有了专门的兴趣组来讨论安全问题、授权问题、传输问题。这就像LSP当年从微软内部项目演变为ECMA标准一样——一个协议真正的“成熟”，不是因为它被广泛使用，而是因为它有了独立的治理结构。

2026年1月，我在开发一个基于MCP的“PDF解析”Server时，遇到一个关于传输层的问题。我在Transports Interest Group的讨论区里搜索到了相关的讨论，发现社区已经在讨论如何优化大文件传输的流式处理。我没有提新问题——因为有人已经想到了，并且在公开讨论。

那种感觉，和2024年11月那个深夜独自对着Notion API写胶水代码的体验，截然不同。

## 3.5 2026-07-28：无状态化大版本落地——从Draft到正式规范

现在到了本章的主角——MCP诞生以来最激进的一次转身：彻底移除Session概念，让协议无状态化。动笔起草这一节的时候，它还只是Draft；等我修订本章，2026年7月28日，它已经作为正式规范发布——它就是本章开篇提到的那个“正在路上的Draft”的最终归宿。一个提案从Draft到正式版只隔了几个月，生态只用大约一个月就基本完成迁移——快到我必须在同一章里写下它的两个时态。

这是一个破坏性变更。我花了些时间去理解SEP-2567和SEP-2575这两份提案背后的思考。

初代的“有状态”设计，用一个initialize握手来建立Session，然后在Session的上下文中处理后续请求。这个设计看起来直观，但设计者在实践中发现了三个致命问题：

第一，Session的生命周期是未定义的。ChatGPT在每次工具调用时创建新Session，Claude Desktop在应用启动时创建Session并持续整个进程生命周期，Web客户端在每次页面加载时创建新Session。Server开发者根本不知道一个Session对应什么——它是一个对话轮次？一个应用实例？还是一个页面浏览？

我在2025年9月遇到过类似的问题。当时我写了一个“浏览器自动化”MCP Server，把浏览器实例绑定到Session。在Claude Desktop里运行得很好，但在一个Web版MCP客户端里测试时，每次页面刷新都创建新Session，旧的浏览器实例就永远悬挂在那里，变成僵尸进程。

第二，隐式状态带来的“购物车困境”。设计者举了一个例子：一个购物车Server提供`add_item`和`checkout`两个工具，使用Session来维护购物车内容。如果Client在每次调用时创建新Session（如ChatGPT），`add_item`和`checkout`就在不同的购物车上下文中执行，商品永远无法结账。如果Client在应用启动时创建Session（如Claude Desktop），不同对话之间的购物车会相互污染。

第三，列表端点无法缓存。tools/list的结果可能因Session而异——不同的Session可能有不同的用户权限、不同的工具集。这导致编排器无法缓存tools/list的结果，每个子代理都要重新请求一遍。如果有10个子代理和5个Server，就是50次重复调用，即使工具集从未改变。

Draft版本的解决方案，是用显式的“句柄”（handle）替代隐式的Session。

```text
// 旧版：隐式会话
add_item("apple")   // 不知道添加到哪个购物车

// 新版：显式句柄
basket = create_basket()
add_item(basket, "apple")
add_item(basket, "banana")
checkout(basket)
```

句柄只是一个字符串，出现在工具参数和返回结果中。它不是一个“秘密”——AI能看到它，用户能复制它。但对于有认证的Server，句柄只是名称，真正的访问控制由OAuth令牌决定；对于无认证的Server，句柄本身需要足够随机（至少128位熵）且有过期时间，类似于一个“分享链接”。

我仔细看了设计者的影响分析——他们对GitHub上公开的MCP Server做了自动分析，发现约90%的Server根本没使用Session ID，6.3%的Server只在SDK样板代码中引用了Session，真正需要迁移的只有3%左右。

这是一个完全可以接受的“破坏性变更”代价。

设计者做了另一个让我共鸣的决策：不保留可选Session。

他们在SEP-2575中明确写道：“支持两种并行的交互模型会极大地增加协议和每个实现的复杂度。服务器和客户端都需要构建、测试、维护两条独立的逻辑路径。”

这和我自己在设计yuangs CLI时的体验一致。我曾经为了让CLI同时支持旧版和新版配置格式，维护了整整一层的兼容代码，最终导致代码复杂度翻倍、Bug频出。后来我决定在下一个大版本中直接放弃旧格式，把所有用户强制迁移到新格式上——虽然短期内增加了迁移成本，但长期来看，整个代码库变得更加清晰可维护。

Draft版本走的就是这条路：不向后兼容，向前迁移。正式规范落地时执行的，正是这条路线——会话在新版本中直接移除，不设漫长的废弃窗口；支持双版本的Client对尚未迁移的Server继续说旧协议，官方给出大约一年的过渡期，之后旧协议彻底摘牌。

新规范落地的那一天，协议的核心被整体改写。initialize握手与Session一起退场，协议版本与能力协商被移进了每一个请求——每个请求自述身份，Server不需要再记住“你是谁”。扩展（Extensions）框架转正，Tasks、MCP Apps、企业级托管授权（EMA）都以扩展的形式进入规范，Client与Server在请求头里声明彼此支持哪些扩展。多轮往返请求（Multi Round-Trip Requests，MRTR）把Elicitation的思路推广成了通用机制——一次工具调用可以在协议层展开为多次往返，Server按需逐轮索取信息或确认。列表方法的结果从此具备可缓存的语义，“编排器无法缓存tools/list”的顽疾至此有了正面解法——3.2节埋下的那个线头，在这里接上了。传输层引入了基于请求头的路由语义，配合无状态核心，网关可以把任意请求分发到任意实例，粘性会话（sticky session）负载均衡从此成为历史名词。而早在2025年3月就已退休的HTTP+SSE，这一次被正式标记为废弃。

发布的那几天，我一直在刷新官方博客和各大SDK的Release Notes。Tier 1的几大SDK在数天内就发布了适配版本——官方博客提到，几大Tier 1 SDK的月下载量此时已接近五亿次；Claude率先完成适配，主流Host紧随其后，AAIF随后还专门撰文记录了这次生态响应——适配的推进“以天为单位”。官方在影响分析里预测的“真正需要迁移的只有3%”，一个月后被证明是准确的：到八月下旬，生态迁移基本完成，公开的MCP Server里绝大多数不需要动一行代码，需要动的也多半是删代码而不是加代码。我自己也做了验证——knasync的MCP Server和WeClaw里几个自用的Server，本来就没有依赖Session语义，正好落在那90%里，把SDK升级到新版本、删掉会话管理的样板代码，一个下午就完成了。更深层的是部署心智的变化：Server从此可以在任何负载均衡器后面自由地水平扩缩容，Kubernetes的滚动发布不再需要考虑连接亲和，一个实例挂了，请求换一个实例照样处理。以前做MCP网关时为粘性会话设计的那些弯弯绕，如今都可以拆掉了。对Client开发者，无状态化同样是减负：不再需要维护会话的生命周期簿记，重试逻辑变得干净——一个请求失败了，原样重发即可，不存在“会话已失效，请先重建上下文”这类恼人的中间态。

社区里当然也有另一种声音：“会话都没了，MCP不就退化成一套普通API了吗？”我的看法恰恰相反。无状态化移除的是协议强加的会话负担，而不是AI调用的语义——能力发现、工具契约、Elicitation、Tasks、结构化输出，这些让MCP区别于普通API的东西一样不少，变少的只有那个从第一天起就说不清该归谁管的Session。把状态的决定权交还给应用层，正是这次改版最清醒的地方。

## 3.6 演进的底层逻辑——减法、实践与开放治理

从2024年11月到今天，MCP走了一条很有“设计感”的演进路径。

它从“有状态”走向“无状态”，不是理念之争，而是实践驱动。设计者在SEP-2567中详细记录了ChatGPT、Claude Desktop、Web客户端的实际行为差异，然后基于这些真实案例做出决策。

它从“实验室”走向“生产环境”，不是靠堆功能，而是靠做减法。批处理被加了又删，Session概念已被彻底移除。好的协议设计，不是无休止地增加特性，而是不断问自己：这个功能真的需要吗？

它从“一个人”走向“一群人”，不是靠官方推行，而是靠开放治理。工作组、兴趣组、公共讨论——这些看似“务虚”的机制，才是MCP从一个公司项目变成行业标准的真正推手。

开放治理的价值，要放到协议的生命周期里才能看清楚。2025年12月，Anthropic把MCP捐赠给了Linux基金会旗下的Agentic AI Foundation（AAIF）——这是继兴趣组成立之后的第二步：从“社区参与决策”走向“资产中立托管”。一家公司的战略会转向，一代产品会退场，但由中立基金会托管的协议，生命周期能超过任何一家公司的兴衰。TCP/IP、HTTP、LSP能跨越数十年持续演进，靠的从来不是某一家企业的意志，而是一套有规则的公共流程。MCP如今也长出了同样的骨架：任何开发者都可以提交SEP（规范增强提案），在公开讨论区接受审视；兴趣组负责探索问题，议题成熟后升级为工作组，由工作组产出规范文本；版本何时发布、破坏性变更如何界定，都有白纸黑字的流程可循。2026年8月27日，AAIF发布了下一阶段的五项优先事项，从服务器主动推送事件、HTTP原生传输的统一，到Agent身份与企业级安全、分层工具发现、SDK开发者体验——治理结构不但在守成，还在产出规划（这些内容第8章会逐项展开）。作为开发者，你今天为一个MCP Server写下的每一行代码，背后不再是某个API部门的心情，而是一套公开、可预期、可参与的制度——这可能是协议给你的、比任何单个特性都更重要的承诺。

2026年3月，我打开MCP官方GitHub仓库，看到SEP-2575的讨论区里有来自Google、Microsoft、AWS、Anthropic的工程师在争论无状态化的具体实现细节。那种跨公司、跨框架的激烈讨论，是2024年11月那个只有两位设计者的初代版本无法想象的。

MCP的演进史，说到底是一段关于“对话”的故事——不仅仅是AI与工具的对话，更是协议设计者与开发者社区、理想与现实、加法与减法之间的对话。

而这场对话，远未结束。

💡 独思时刻： 写完这一章，我把四个正式版本加上一次大改版的发布说明摊开对比，试图找出它们共同的签名。我找到的不是某项具体的技术决策，而是一种态度：每一次版本演进，本质上都是对上一版的一次“体面认错”。3月承认鉴权不能各自为政，6月承认批处理是个错误，11月承认“一次性请求-响应”装不下长任务，7月承认Session是一个从第一天起就没定义清楚的概念。协议和人一样，成熟不是从此不犯错，而是犯了错敢认、认了敢改、改了敢带用户一起迁移。如果你也在设计自己的协议、平台，或者哪怕只是一个对外的SDK，MCP这二十一个月给出的答卷可以直接抄：先让真实使用说话，再让规范落笔；加功能要慢，删功能要快；把决定权放进公共流程，而不是留在某间会议室里。第五个版本已经落地，第六个正在路上——这场关于“AI与工具该如何对话”的对话，值得每一个身处其中的人发言。

---

## 第4章 MCP架构深度解析

# 第4章 MCP架构深度解析

前三章我们聊了MCP是什么、为什么诞生、以及它如何一步步演化到今天的样子。现在该把引擎盖打开了。

这一章，我会带你看清MCP的骨架——不是照着规范文档念一遍，而是从实际运行的角度，拆解请求从AI应用出发，经过层层传递，最终触达外部工具的完整路径。同时，我会在每个关键节点上停下来，告诉你哪些地方“文档没写但你必须知道”。

在动笔之前，我把这一章的材料重新过了一遍：规范文档里关于架构的章节加起来不过几十页，但要把一个MCP系统在生产环境里跑稳，你真正需要面对的问题——进程怎么管、消息怎么发、能力怎么谈、授权怎么走——几乎都不在那几十页里。所以这一章沿着“一次请求的生命周期”推进：先看角色怎么划分，再看消息长什么样，然后是连接怎么建立、消息怎么搬运、能力怎么声明，最后是授权与扩展机制。每一层我都尽量给你两个视角：规范说了什么，以及我在实战里遇到了什么。

## 4.1 三层模型：不只是“主机-客户端-服务器”

规范里把MCP架构描述为三层：Host（AI应用）、Client（连接器）、Server（能力提供方）。这个划分没错，但它隐藏了一个关键事实——在实际部署中，Client和Server的边界比你想象的模糊得多。

先看一张简化后的数据流：

```
┌─────────────────────────────────────────────────────────────────┐
│  Host（例如 Claude Desktop / Cursor / 你的自定义 Agent）       │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Client（协议层）                                        │ │
│  │  - 管理连接生命周期                                       │ │
│  │  - 维护消息队列和响应映射                                 │ │
│  │  - 处理JSON-RPC 2.0消息的收发                           │ │
│  └───────────────────────────────────────────────────────────┘ │
│                           │                                    │
│                           ▼                                    │
│              stdio / Streamable HTTP                           │
│                           │                                    │
└───────────────────────────┼────────────────────────────────────┘
                            │
┌───────────────────────────▼────────────────────────────────────┐
│  Server（能力提供方）                                          │
│  - 注册工具/资源/提示词                                        │
│  - 执行实际逻辑                                                │
│  - 返回结果                                                    │
└────────────────────────────────────────────────────────────────┘
```

这个图没问题。但2025年8月我在调试一个基于MCP的金融数据聚合器时，遇到了一个边界模糊的场景。我的Host是一个用Go写的自定义Agent，它通过stdio启动了一个Python MCP Server来访问本地PostgreSQL数据库。按照三层模型，Go Agent是Host，stdio连接的Python进程是Server。但Python进程本身又启动了三个子进程——一个连接Redis缓存，一个调用外部REST API，一个负责数据聚合。这些子进程算Server吗？还是Server的内部实现？

我的处理方式是干脆不回答这个问题。这三个子进程对外部世界完全不可见，它们不参与任何协议交互，Client甚至不知道它们存在。它们是Server的内部实现，就像一家公司的财务系统不会因为外包给了第三方会计师事务所就变成另一家公司。协议边界只认“谁在协议上说话”——Python进程在stdio上说MCP的话，它就是Server；子进程们在自己的进程间通道上说话，那是Server的家务事。

我的结论是：三层模型是交互协议层面的划分，不是部署拓扑层面的真实映射。

在实际系统中，一个“MCP Server”往往是一组进程的集合，而非单个二进制。你对Client暴露的是协议接口，至于接口背后是单进程、微服务还是bash脚本——Client根本不关心，也不应该关心。

这几年我总结了一条画边界的经验：先画信任边界，再画进程边界。MCP的三层模型真正想隔离的其实是信任——Host代表用户意图，Server代表外部能力，中间的Client负责让每一次能力调用都经过用户的知情与确认。一个设计良好的Host会把“这个Server请求调用删除工具”翻译成一次明确的用户确认，而不是静默放行。信任边界画对了，进程边界怎么切都行：可以同进程、可以子进程、可以跨机房。信任边界画错了，进程隔离做得再漂亮也是白搭。那次调试之后，我在金融数据聚合器的架构文档里专门加了一节“协议边界与信任边界”，把哪些进程在协议上可见、哪些纯粹是内部实现逐条列清——这份清单后来帮我省掉了不止一次故障定位时间。

这个视角转换很重要：当你开发MCP Server时，不要把自己局限在“写一个Python脚本”的思路上。你可以把MCP Server做成一个网关，背后挂载任意复杂的系统。我自己的WeClaw项目就是一个例子——它对外暴露MCP接口，内部同时连接了DeepSeek、ChatGPT、Gemini等多个模型，还挂载了知乎搜索、文件操作、定时任务等工具。对Client来说，它只是一个Server；对内部来说，它是一整个编排层。

这也是MCP和传统API网关思路上的分野。API网关把后端拆成一堆REST端点暴露出去，调用方要自己理解每个端点的语义；而MCP Server的网关模式是把后端能力重新组织成“对AI友好的能力清单”——工具怎么命名、描述怎么写、参数怎么设计，全部围绕“模型能不能一次看懂”来做。同样的后端系统，包一层MCP和包一层REST，设计重心完全不同。第6章我们会在客户端视角回到这个问题。

## 4.2 通信协议：JSON-RPC 2.0 不是“另一个新协议”

MCP没有发明新的消息格式。它直接用JSON-RPC 2.0。

这意味着什么？意味着你不需要学习一套新的RPC语义。如果你写过JSON-RPC客户端，你立刻就能上手MCP的消息层。

一个典型的MCP请求是这样的：

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/call",
  "params": {
    "name": "search_web",
    "arguments": {
      "query": "MCP protocol news"
    }
  }
}
```

响应：

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "搜索结果：..."
      }
    ]
  }
}
```

错误时：

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -32000,
    "message": "Tool not found"
  }
}
```

这三个报文里藏着JSON-RPC 2.0的全部关键设计。jsonrpc字段是协议版本声明，永远写死为2.0；id是请求标识，响应通过相同的id与请求配对——这意味着MCP天生支持并发：你可以同时发出三个请求，响应谁先回来谁先处理，靠id对号入座。没有id字段的消息是通知，发出去就完事，不期待任何回应。method是方法名，params是参数；成功时响应带result，失败时带error，二者不会同时出现。

顺便说清楚错误的两种层次。上面示例里的-32000属于JSON-RPC预留的服务器自定义错误段，协议本身还规定了-32700解析错误、-32600无效请求、-32601方法不存在、-32602参数无效、-32603内部错误这些标准码。但MCP里工具“执行失败”通常不走error对象，而是在result里返回isError为true，外加一段给模型看的错误说明——因为工具执行失败本质上是AI需要理解并处理的业务结果，而不是协议层的崩溃。我在WeClaw里调试MCP流量时，最常犯的错就是把这两个层次搞混：明明是工具内部的业务报错，却按协议错误处理，导致整个会话状态混乱。区分清楚，日志立刻清爽。

JSON-RPC 2.0的规范只有不到30页。我建议你花二十分钟读一遍原文——比你从任何二手教程里学到的都准确。

为什么选JSON而不是更高效的二进制协议？我的理解是可调试性压倒一切。MCP的消息是纯文本，意味着你可以直接查看日志排查问题、用grep在几万行历史里定位一次工具调用、在没有专门工具的机器上手工构造一条请求。我在深夜排查WeClaw问题时，无数次感谢这个决定——如果MCP用了二进制序列化，每一次协议层调试都要先写解码脚本。对一个人机交互频繁、错误场景繁多的协议来说，“人眼可读”是比“字节紧凑”贵重得多的特性。

但有个容易被忽略的细节：JSON-RPC 2.0本身不定义传输层。它可以在stdio上跑，也可以在WebSocket上跑，也可以在HTTP上跑。MCP真正“发明”的部分不是消息格式，而是两样东西：

1. 一组标准化的方法名：initialize、tools/list、tools/call、resources/list、resources/read、prompts/list、prompts/get——以及可选的ping、progress通知等。这些方法名构成了MCP的能力骨架。
2. 每个方法的标准参数和返回结构：比如tools/call必须包含name和arguments字段，返回必须包含content数组，数组里每个元素必须有type字段。这套结构保证了Host和Server之间可以互操作，即使它们由不同公司用不同语言开发。

这两样东西看着平淡，含义却不平淡。第一样解决的是“词汇表”问题——所有MCP实现共享同一套方法名，你在Python SDK里写tools/list，和在Go SDK、Rust SDK里听到的是同一个词。第二样解决的是“语法”问题——返回结构定了content数组，AI应用才能用统一的渲染逻辑处理任何Server的结果，不管这个Server是Anthropic官方的还是你隔壁同事周末写的。2025年6月18日的版本又给返回结构加了一层structuredContent和outputSchema，让工具可以声明自己返回结构化数据——相当于在content数组之外补了一条机器可读通道，但骨架没变，还是那套JSON-RPC。另外值得一提：初版协议里实验性的JSON-RPC批处理在2025年6月18日被移除了——实践证明AI应用的调用模式并不需要它，规范选择删掉而不是留着当包袱。这段历史我在第3章详细讲过，这里只提醒一句：读报文时别再找批处理语义了，它已经是历史名词。

## 4.3 连接生命周期：四次握手，但有一次是你看不见的

MCP的连接不是“打开就干活”。它有一个明确的初始化序列，我称之为“四次握手”——但实际上只有三次是显式的，第四次是隐式的。

第一步：Transport建立（隐式）

Client和Server之间的传输通道先建立。对于stdio模式，就是启动子进程，连接stdin/stdout。对于Streamable HTTP，就是发起HTTP连接。这一步不涉及任何MCP协议消息——它只是让两个进程能说话。

进入第二步之前，先说说贯穿整个握手的核心机制——版本协商。initialize请求里的protocolVersion是Client的“报价”：我最高支持到这里。如果Server不支持这个版本，它不会报错，而是在响应里返回自己支持的版本；Client如果也接受不了对方的版本，就主动断开连接。双方的规则是对称的：各报各的能力，取交集，谈不拢就散。这种协商机制是MCP能够持续演进而不断裂生态的关键——2026-07-28的大版本发布后，一个月内生态迁移基本完成，靠的不是强制升级，而是“新Client连旧Server、旧Client连新Server，只要找到共同版本就能继续工作”的柔和约束。

第二步：Client发送initialize请求

Client向Server发送initialize消息，携带自己的身份和协议版本：

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "initialize",
  "params": {
    "protocolVersion": "2026-07-28",
    "clientInfo": {
      "name": "my-agent",
      "version": "1.0.0"
    },
    "capabilities": {
      "sampling": {},
      "roots": {}
    }
  }
}
```

capabilities字段很重要——它告诉Server，Client能做什么（比如是否支持AI采样、是否支持读取文件列表）。不同Client的能力集不同，Server需要据此调整行为。

第三步：Server返回initialize响应

Server确认协议版本、返回自己的能力和服务器信息：

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "protocolVersion": "2026-07-28",
    "serverInfo": {
      "name": "postgres-server",
      "version": "2.3.0"
    },
    "capabilities": {
      "tools": {},
      "resources": {
        "subscribe": true
      }
    }
  }
}
```

这个响应告诉Client：我支持tools，支持带订阅的resources，但可能不支持prompts。Client可以根据这个信息决定后续调用哪些方法。

第四步：Client发送initialized通知（这就是那个隐式的）

initialized是一个通知，不是请求——它没有id字段，Server不需要回复：

```json
{
  "jsonrpc": "2.0",
  "method": "initialized"
}
```

它只是一个信号：Client已经收到了initialize响应，现在连接完全建立，可以开始正常通信了。

为什么我称它为“隐式的”？因为很多MCP Server的SDK把这步隐藏在初始化函数里，开发者甚至不知道它发生了。但如果你在写一个自定义Client，你不能忘记发这个通知——Server需要它来完成内部状态转换。

从状态机的角度看，这四次握手定义了三个状态：传输建立前的“未连接”、握手完成前的“初始化中”、收到initialized之后的“正常运行”。规范明确要求Server在初始化完成之前拒绝一切业务请求——握手没走完就发tools/list，得到的只会是错误。这个约束看起来苛刻，实际上是在保护双方：没有协商过版本的通信，任何一方都不知道对面“听得懂什么”。

一旦initialized发出，连接进入正常运行阶段。这个阶段会持续到Client或Server任意一方主动关闭连接。关闭不需要特殊的MCP消息——直接断开传输层即可（关闭子进程或关闭HTTP连接）。

2026年4月，我为一个基于R86S小主机的边缘计算项目开发MCP集成时，踩过一个坑。我的Client在收到initialize响应后立即开始调用tools/list，跳过了发送initialized通知。结果Server（一个用Go写的轻量级实现）一直不响应任何工具调用请求——它在内部等待initialized完成状态机转换。那一次排查花了四小时，翻了三层SDK源码才找到原因。从那之后，我养成了习惯：在写任何MCP Client时，把初始化流程显式拆成四步，绝不把任何一步塞进“自动”的黑盒里。

关于生命周期的尾部——关闭——还有两个细节值得记。一是stdio模式下，Client关闭stdin就是明确的“到此为止”信号，Server收到输入流结束应当清理资源后退出；但现实中大量Server实现不处理这个信号，靠Host超时强杀，于是就有了我在2025年9月踩过的浏览器自动化Server僵尸进程——子进程还活着，浏览器实例占着内存，日志还往stderr里写。这个问题我们在4.4节细说。二是HTTP模式下，从2025年6月18日的版本开始，每次请求都要带上MCP-Protocol-Version头，把协商结果固化到每个报文里——HTTP连接本来就没有stdio那种“长期同生共死”的语义，版本信息必须随行。这两个细节规范里都只有一两句话，但都真实地咬过我的生产环境。

## 4.4 传输机制：stdio 与 Streamable HTTP 的选型哲学

MCP支持两种传输机制，各有定位。

stdio：本地进程通信

stdio模式是最简单的。Host启动Server进程，通过stdin发送请求，从stdout读取响应。stderr保留给日志输出——这样日志不会干扰协议消息。

它的优势是：

· 零网络开销，进程内通信，延迟极低

· 安全性好，进程边界天然隔离

· 部署简单，不需要配置端口、证书、负载均衡

但它有两个限制：

· 只能本地通信，Server必须在Host的同一台机器上（或至少Host能启动它）

· 单请求-单响应，Server无法主动向Client发送消息（除了响应）

stdio的简洁是有纪律代价的。协议消息按行分隔，每行一条JSON——这意味着stdout是专用通道，一个字节的杂质都不允许混入。新手最常犯的错误就是在Server代码里随手写一句打印调试信息的语句，输出进了stdout，Client把它当JSON-RPC消息解析，整个连接立刻报废。调试信息必须走stderr，或者用MCP的日志能力正经地发给Client。我给团队定的规矩是：stdio Server上线前，先在命令行手工跑一遍，肉眼确认stdout里除了一行行JSON什么都没有。

进程生命周期管理是stdio模式真正需要动脑子的地方。Host是父进程，Server是子进程，父进程有义务管到底：正常关闭时先关stdin，给Server一个优雅退出的机会，等一小段超时，还不走就发终止信号，最后才强杀。反过来，Server也应当主动处理输入流结束——把连接池关掉、临时文件删掉、子任务收尾，然后退出。2025年9月那次浏览器自动化Server的僵尸进程事故，我的最终修复方式就是在Server主循环里监听输入流关闭事件，收到后先关闭浏览器实例再退出进程，同时在Host侧加了“超时未退出则升级强杀”的兜底。另外别忽略异常路径：Server崩溃时Host要能感知——看进程退出码、看stderr里的崩溃日志——并决定是否重启。一个没人管的重启循环，或者干脆不管的静默死亡，都会在生产环境留下隐患。stdio帮你省掉了网络层的一切麻烦，也把这些责任原封不动交到了你的进程管理手上。

假设你是一家量化交易公司，2026年7月，你在本地开发机上运行一个MCP Server，读取本地的市场数据文件，供AI分析使用。这就是stdio的典型场景——简单、快速、安全。

Streamable HTTP：远程通信

Streamable HTTP是2025-03-26版本新引入的，取代了之前基于SSE的实现。

它的工作方式：

· Client通过HTTP POST发送请求到Server端点

· Server通过Server-Sent Events（SSE）向Client推送响应和通知

· 支持双向流，Server可以主动推送消息

这解决了stdio的两个限制：

· Server可以远程部署，一个MCP Server服务多个Client

· Server可以主动推送，比如数据更新通知、进度报告

但代价是更复杂的部署：需要配置HTTP路由、处理认证、考虑网络延迟和重试策略。

举个例子。在2026年3月参与的一个金融数据项目中，我们部署了一个MCP Server作为数据中台的统一访问层。这个Server运行在Kubernetes集群里，同时服务内部的三个AI Agent——一个负责财报分析，一个负责舆情监控，一个负责交易策略辅助。三个Agent分布在不同的命名空间，通过内网域名访问同一个MCP Server端点。这正是Streamable HTTP的适用场景。

展开说一下那套“更复杂”具体复杂在哪。Streamable HTTP把所有消息收敛到一个端点上：Client的每条JSON-RPC消息用POST发出，请求头同时声明接受普通JSON和SSE两种响应——Server可以直接用一条JSON答复，需要流式输出或中途通知就开一条SSE流。除此之外，Client还可以发一个GET请求到同一端点，专门开一条接收Server主动推送的下行流；不支持推送的Server对这个GET回一个405就行，协议不强迫。会话层面，握手完成后Server会分配一个会话标识，之后每个请求都得带着它——Server靠这个把请求关联到内部状态。网络是最不可靠的环节，所以规范借鉴了SSE的事件机制做断线恢复：流上的每个事件带有序号，连接断了，Client带着最后收到的序号重连，Server从断点补发，Client不丢消息。

而运维工程师最头疼的，是这套会话机制给负载均衡带来的粘性路由问题：会话标识绑定在某个Server实例的内存里，后续请求就必须路由到同一个实例——你在负载均衡器上配会话保持，实例一扩容、一重启，会话全灭，Client只能重新握手。2026年7月28日的无状态化大版本直接拆掉了这颗雷：会话概念被移除，改为显式的状态句柄，状态要么放进请求参数跟着请求走，要么放进外部存储由句柄指向。请求落在哪里都一样，粘性路由、会话漂移、重启失效，这些问题在架构层面消失了。按官方的影响分析，约90%的公开Server根本没用会话，6.3%只在SDK样板代码里引用过，真正需要迁移的约3%——这也是生态能在一个月内基本完成迁移的原因。

**无状态化对架构设计的根本性重塑**

这一节，我需要把第三章埋下的引线拉出来——无状态化。

我在2026年1月第一次读到SEP-2575时，并不完全认同设计者的决策。我当时在WeClaw中广泛使用了Session级别的缓存：每个会话保存一份工具列表的快照，避免重复查询。无状态化意味着我需要重新设计缓存策略，把“会话级缓存”改为“全局缓存+客户端标签”的模式——至少多写两百行代码。

但运行了几个月后，我意识到设计者是对的。

无状态化不仅改变了Server的实现方式，它从根本上改变了MCP的部署拓扑。在2026-07-28版本之前，MCP Server天然是“有状态的进程”——每个Client连接对应一份Server实例的内部状态。这导致Server难以水平扩展：如果你有100个Client同时连接同一个Server，要么Server维护100份状态，要么你得为每个Client启动一个独立的Server进程，资源开销巨大。

无状态化之后，Server不再维护任何会话状态。所有Client共享同一个Server端点，每次请求都携带足够的标识信息（句柄、令牌），Server根据请求内容而非会话上下文来处理。

这意味着什么？

意味着一个MCP Server可以像REST API一样水平扩展。 你可以部署多个副本，前面放负载均衡，完全不担心“粘滞会话”。2026年8月，我把WeClaw背后的一个数据聚合MCP Server从单机部署改造成了K8s Deployment，副本数从1扩展到5，QPS从200提升到1200，代码零改动——因为Server本身就是无状态的。

这个“零改动水平扩展”的体验，让我对无状态化的态度从勉强接受到强烈推崇。如果你正在设计一个生产级的MCP Server，听我一句：从一开始就按无状态来设计，不要贪图“Session省事”。

再看远一点，传输层还有一个正在发生的收敛趋势。AAIF在2026年8月27日公布的路线图里，第二项优先事项就是HTTP原生传输的统一——围绕无状态HTTP把传输层的故事讲简单：本地和远程的差异只剩部署方式，而不是两套心智模型。我的判断是，stdio不会消失，但它会越来越像“单机场景下HTTP语义的本地特例”，而不是一种并列的独立选择。对今天的开发者，务实的建议是：业务逻辑与传输选型解耦——用主流SDK时，把工具注册和传输启动写成两件事，将来切换传输只改一行启动代码。

💡 独思时刻：我对“stdio用于本地、HTTP用于云端”这个二分法有些不同看法。从我实际部署的经验来看，选择传输机制更应该考虑“谁控制Server的生命周期”。如果你能控制Server的启动和关闭——无论它跑在哪——stdio都是更好的选择。为什么？因为stdio模式下的进程管理给了你“会话即进程”的清晰语义。HTTP模式则需要你额外处理会话粘滞、超时重试、连接池等一堆基础设施问题。我在R86S小主机上同时跑了5个MCP Server，全部用stdio模式，即使有些Server实际是运行在远程NAS上的容器——我只不过是在Host侧用ssh命令包装了一下启动脚本。我把这叫做“远程本地化”：把远程能力包装成本地进程。这比直接暴露HTTP端点更可控、更易调试。

## 4.5 服务器与客户端的能力清单

能力清单是initialize握手的另一半。很多人把它当成握手时的走过场字段，但在生产系统里，它是MCP架构中最有工程价值的协商机制之一。讲清楚它之前，先把三大核心能力的协议细节过一遍——它们是能力清单里最重要的条目。

前面反复提到Tools、Resources、Prompts。现在从协议层面看它们究竟长什么样。

Tools：可调用的函数

Tools是最直接的能力。Server在tools/list响应中返回一组工具定义：

```json
{
  "tools": [
    {
      "name": "search_stock",
      "description": "查询股票实时价格",
      "inputSchema": {
        "type": "object",
        "properties": {
          "symbol": { "type": "string", "description": "股票代码，如AAPL" }
        },
        "required": ["symbol"]
      }
    }
  ]
}
```

Client调用时发送tools/call：

```json
{
  "method": "tools/call",
  "params": {
    "name": "search_stock",
    "arguments": { "symbol": "AAPL" }
  }
}
```

关键在于inputSchema——它遵循JSON Schema标准。这意味着Client可以据此做参数校验、生成UI、甚至让AI自动理解工具的使用方式。

2026年6月，我在重构WeClaw的MCP接入层时，直接把工具列表传给DeepSeek模型，让模型自己决定调用哪个工具、填入什么参数。inputSchema在这里扮演了“机器可读的API文档”角色——模型读一遍schema，就能理解工具的输入格式，比人工写提示词准确得多。

Resources：可读取的数据

Resources是“内容”，不是“函数”。它返回的是文本或二进制数据，而不是执行一个操作。

列表请求：

```json
{
  "method": "resources/list"
}
```

响应：

```json
{
  "resources": [
    {
      "uri": "file:///data/report.pdf",
      "mimeType": "application/pdf",
      "name": "财报PDF"
    }
  ]
}
```

读取：

```json
{
  "method": "resources/read",
  "params": { "uri": "file:///data/report.pdf" }
}
```

Resources有两种用法：一是让AI读取上下文信息（比如项目文档、数据库schema），二是通过resources/subscribe实现变更通知——Server可以在资源更新时主动推送。

Prompts：预定义的交互模板

Prompts是“对话模板”。Server定义一些带参数的提示词模板，Client拉取后可以填充参数并呈现给用户。

```json
{
  "prompts": [
    {
      "name": "code_review",
      "description": "代码审查提示",
      "arguments": [
        { "name": "code", "description": "待审查的代码" }
      ]
    }
  ]
}
```

我自己的使用场景是：把常用的分析任务（如“财报解读”“市场情绪分析”“策略回测报告”）做成Prompts存储在Server端。不同Client（Claude Desktop、Custom Web UI、WeClaw微信端）都可以通过prompts/get拉取这些模板，保证所有入口的分析方法论一致。

回到能力清单本身。Server一侧的标准能力包括：tools（可附listChanged标记，表示工具列表会动态变化）、resources（可附subscribe和listChanged）、prompts（可附listChanged），以及logging和completions；Client一侧是sampling、roots和elicitation。这些标记不是摆设：listChanged意味着Server承诺在能力集合变化时发出通知——你给一个正在运行的Server热加载了新工具，Client会收到notifications/tools/list_changed，然后重新拉取列表，不用重连。我在WeClaw的工具热更新链路里就用到了这个机制：运维后台发布新工具后，所有连接中的会话在秒级完成感知，全程没有一次重连。

能力协商真正的工程意义在于让双方都学会“降级”。Server在调用任何Client侧能力之前必须先确认对方声明过：想发起AI采样，先看Client的capabilities里有没有sampling，没有就得自己想办法——直接调模型API、返回需要人工补充的错误，或者干脆放弃这个功能；想中途向用户提问，先确认elicitation能力，否则把问题包装成普通工具返回值交给上层处理。反过来Client也一样：Server没声明subscribe，你就别订阅资源更新。我见过最糟的实现是默认对方什么都有——某次接入一个第三方Client，它没声明sampling，我们的Server却假设有，请求发出去石沉大海，两边日志都显示“正常”，排查了两天。从那以后，WeClaw的Server框架里所有反向调用之前都有一层能力断言，不满足就走降级路径并打上明确的日志标记。协商不是握手时的仪式感，而是运行时的行为开关。

还有一件事值得放在心上：即使传输层走向了无状态，能力协商的语义没有变——initialize仍是每次接入的第一句话，只是协商完成后不再有一个需要小心翼翼维持的会话。2026-07-28版本甚至把扩展能力的声明下沉到了请求级：像Tasks这样的扩展，可以在单次请求的元数据里声明支持，按次生效。能力协商正在从“连接级契约”演进为“连接级契约加请求级契约”的双层结构。

## 4.6 授权与扩展机制：OAuth 2.1、Tasks 与 MCP Apps

如果说传输和能力是MCP的骨骼与关节，授权就是它的免疫系统——尤其在远程Server的世界里。2025年3月26日的版本把OAuth 2.1正式写进规范，MCP才算真正拿到了企业环境的入场券。完整链路值得逐段走一遍：MCP Server在授权体系里的角色是资源服务器，不是授权服务器——它只验令牌，不发令牌。Client第一次连接远程Server，收到401未授权响应，响应头会指向受保护资源元数据文档；Client从那里读到真正负责发牌的授权服务器地址，再通过授权服务器元数据发现端点拿到完整的发牌能力清单——支持哪些授权端点、哪些作用域、客户端怎么注册。注册完成后走授权码流程加PKCE证明密钥交换，用户在浏览器里完成登录与同意，Client拿到访问令牌，之后每个请求的Authorization头里带上它。2025年6月18日的版本又加了一道保险：请求令牌时必须带上resource参数，把令牌和目标Server绑定死，防止一枚为A服务签发的令牌被拿到B服务重放。我第一次给WeClaw的远程Server配这套链路时花了一整个晚上，但配完之后它成了一个标准件——任何合规的MCP Client都能直接接入，不用我再写一行业务代码。授权逻辑下沉到协议层，这就是标准化的复利。

这套链路在2026年又进化了一步：动态客户端注册被正式弃用，转向客户端ID元数据文档——client_id直接就是一个URL，授权服务器按需拉取该URL上的元数据来识别客户端，身份锚定在域名控制权上。社区分析认为这解决了动态注册长期被滥用的老问题，代价是客户端要多托管一份元数据文件。

企业场景还有一条更彻底的路线。2026年6月，Enterprise-Managed Authorization扩展进入稳定版：企业IdP成为访问控制的权威决策方，管理员在IdP里维护一张已批准的MCP Server清单和访问策略；员工用企业凭证单点登录，客户端拿登录产生的身份断言向企业IdP换取ID-JAG（Identity Assertion JWT Authorization Grant，身份断言授权授予），再用它向MCP授权服务器换访问令牌——令牌签发之前，IdP先评估策略，不在授权名单上的员工根本拿不到令牌；离职时在IdP一处撤销，所有MCP客户端立即失效。用户不再面对一个个Server的授权弹窗，安全团队第一次拥有了全局策略控制点。这是把OAuth的“用户逐个授权”升级成了“组织统一授权”，我认为它对企业采用MCP的意义，不亚于当年Streamable HTTP之于远程部署。

扩展机制方面，先说Tasks。长耗时工具调用一直是MCP的软肋——数据管道一跑半小时，HTTP连接早就断了，结果无处安放。2025年11月25日的版本首次引入Tasks时它还是核心协议里的实验特性，到2026年7月28日被重新设计为独立扩展：Server判断这次调用会跑很久，不阻塞连接，而是立即返回一个任务句柄——含任务ID、存活时间和建议轮询间隔；Client按建议间隔轮询状态，状态机里working表示还在跑，input_required表示中途需要你补充信息（比如批量作业的审批确认），completed之后从结果字段取回输出，failed时错误字段里带原因，任何时候可以发起取消。任务可以跨重连存活——这是无状态化之后才能有的设计：状态不在连接里，连接断了，任务还在。

再说Skills over MCP。Agent Skills是另一条开放标准路线：把一个领域的专业知识——操作流程、注意事项、配套脚本——打包成一份SKILL.md加附件目录，AI用到时再加载。社区正在把这两条线接起来：把技能包挂到MCP的Resources上做渐进式披露——列表接口先只暴露技能的名称和一句话描述，AI判断需要时再读取全文和脚本。这个组合拳恰好打在MCP最疼的痛点上：上下文成本。第8章那个43个工具的Server描述吃掉五万多Token的案例，用技能分层加载的思路可以砍掉大半——AI平时只看到几十个字的技能目录，深入某个领域时才展开细节。这是社区实践方向而非核心协议原语，但我已经在自己项目里开始验证。

最后是MCP Apps，扩展轨道上第一个正式扩展（SEP-1865，2026年1月上线，2026-07-28规范正式纳入）。它解决的问题是：有些结果用文本表达不了——图表、表单、可交互的预览。Server在工具结果里附带一个ui://资源引用，Host在沙箱iframe里渲染这段HTML界面，界面与Host之间通过postMessage跑一套JSON-RPC方言——应用可以请Host再调一次工具，把用户在界面里的操作回传给模型，用户不需要再打一行字。财报分析的K线图、数据中台的审批表单，都从“AI念一遍文本”变成了“直接可操作的界面”。规范同时要求优雅降级：客户端不支持Apps时，Server必须回退到纯文本或结构化数据。整个扩展机制的治理逻辑也值得一看：SEP-2133建立了正式的扩展管线，扩展用反向域名做标识——官方的io.modelcontextprotocol前缀，第三方的自有域名——默认关闭、显式启用，验证成熟后才会考虑进入核心协议。核心保持精简，能力长在轨道上，这是协议设计走向成熟的标志。

## 4.7 独思时刻：规范不说“必须独立进程”

走到章末，我想把散落在各节里的设计判断收拢成几条原则——它们贯穿了MCP从2024年11月到2026年7月的全部演进，比任何具体技术细节都值得留在这本书里。

**架构演进中的设计原则**

回顾整个架构的演变，有三条设计原则贯穿始终，我认为比任何具体技术细节都值得记住：

原则一：协议是契约，不是实现

MCP规范定义的是“消息长什么样”“方法叫什么”，但不规定“Server怎么实现”。你的Server内部可以用任何语言、任何架构——只要它能在stdio上吐出符合规范的JSON。

这给了你巨大的自由度。我见过用50行bash脚本实现的MCP Server，也见过用数千行Java实现的复杂网关。它们都叫“MCP Server”，因为它们在协议层面等价。

原则二：主动推送是可选的，不是必须的

MCP支持Server主动推送（通过Streamable HTTP的SSE通道），但规范明确指出这是可选能力。如果你不需要推送，完全可以用纯请求-响应模式，部署简单得多。

我在2026年4月的边缘计算项目中选择的就是纯请求-响应模式——Server只需要在收到请求时返回结果，从不主动推送。这让我可以用最简单的HTTP Handler实现，甚至不需要SSE库。

原则三：版本协商是动态的，不是静态的

initialize阶段包含了协议版本协商。Client说“我支持2026-07-28”，Server可以回复“我也支持”或“我只支持2025-03-26”。这种动态协商让MCP可以在不破坏兼容性的前提下持续演进——新Client连接旧Server、旧Client连接新Server，只要双方找到一个共同版本即可。

2026年8月，我坐在上海的办公室里，打开MCP规范文档，看着里面那些精心设计的消息结构，想到一个细节：initialize请求中的protocolVersion字段是一个完整的日期字符串，而不是一个递增的数字。

设计者选择用发布日期做版本号——这个选择本身就在传递一种态度：MCP的演进不是“版本号比赛”，而是“在真实世界中持续打磨”的过程。每六个月发布一个新版本，不是为了让数字变大，而是为了记录这六个月里从真实用户、真实场景、真实痛点中提炼出的改进。

这就是我理解的MCP架构深度：它不是一个静态的“三层图”，而是一组随着实践不断自我修正的设计决策的沉淀。

💡 独思时刻：我越来越觉得，“三层架构”这个说法在真实部署中带来的误导远大于帮助。新手看到三层图，会本能地认为Client必须和Host紧耦合、Server必须是一个独立进程。但实际上，我见过最优雅的MCP部署是把Server直接编译进Host二进制里——通过内存通道通信，连stdio都省了。规范从来不说“必须独立进程”，只说“通过标准传输机制通信”。这是两个完全不同的约束强度。理解这一点，你才能真正自由地设计系统边界。

💡 独思时刻：这一章写得最慢的地方不是那张架构图，而是4.6节的授权链路。写到这里我才意识到，MCP这三年的演进重心正在从“怎么连接”滑向“怎么信任”——stdio时代不需要想的问题，远程化之后一个接一个浮出水面，而2026年的每一个授权提案，本质上都在回答同一个问题：当AI替你按下一个按钮时，凭什么相信它有权这么做？架构师的职责清单里，在“画好能力边界”之后，正在多出一条“画好信任边界”。这也回应了4.1节的老话题：三层模型划的是交互责任，部署拓扑划的是信任半径——两件事我都希望你分开设计，也分开记住。

现在你已经看清楚了骨架。下一章，我们会在这个骨架上长出血肉——从零开始搭建一个真正的MCP Server。

---

## 第5章 MCP开发环境搭建与第一个Server

# 第5章 MCP开发环境搭建与第一个Server

上一章我们拆解了MCP协议的骨架——从三层架构到无状态化演进，从Streamable HTTP的流式设计到版本协商的哲学。你大概已经看明白了它长什么样，但一个工具的价值从来不在于它画在图纸上的样子，而在于你真正用它拧下第一颗螺丝时的手感。

这一章我们就来拧那颗螺丝。

路线图很简单：选一门语言，配好环境，写一个只有22行的Server，用官方调试工具把它跑通，最后接进一个真正的AI应用。整条路走完，你会对一件事有体感——MCP把“写一个Server”的门槛，压得比大多数人想象的低得多。低到什么程度？我给团队新入职的同事掐过表，从空目录到AI真正调用他的工具，一刻钟以内。本章末尾你会看到这个数字。

## 5.1 语言之争：TypeScript还是Python

这是个绕不开的问题，你随便打开一个MCP技术群，能在五分钟内看到至少三场关于语言的争论。我的看法很简单：语言之争是最不值得花时间争论的事，因为它跟协议本身无关。

MCP在协议层只认JSON-RPC消息和传输通道，不认语言。Python写的Server和TypeScript写的Server，在Client眼里是同一种生物——都会在initialize握手时报上名字和版本，都会用同一套tools/list和tools/call接口对话。协议把“你是谁”定义得清清楚楚，唯独没有定义“你该用什么语言写”。这正是好的接口协议该有的样子：它约束对话的格式，而不是说话者的口音。

但作为本书作者，我仍然需要给你一个务实的选择依据。

2026年5月，我在WeClaw的agent路由模块里同时接入了两个MCP Server：一个用TypeScript写的GitHub Server，一个用Python写的本地文件操作Server。两者在协议层完全等价——Client端根本不知道背后是什么语言。这个体验本身就在说一件事：MCP把语言选择权完全还给了开发者。

那你怎么选？

选TypeScript的条件：你已经在Node.js生态里、你的主项目是Web应用、你希望Server与前端共享类型定义、你熟悉npm和tsx。

选Python的条件：你的工作流以数据科学或AI训练为中心、你需要频繁调用pandas/numpy/torch、你习惯用uv或poetry管理依赖。

这两组条件其实说的是同一件事：看你的能力往哪儿长。TypeScript的优势在“同构”——前后端共享一套类型定义，Server的输入校验和前端的表单校验可以是同一段代码；Python的优势在“生态纵深”——你迟早会想让Server去碰数据、碰模型、碰文件，Python在这个领域的存量轮子最多。

还有一个背景值得知道：2024年11月5日MCP开源那天，Anthropic是同时发布Python和TypeScript两个SDK的——从第一天起，这门语言就没有官方钦定的答案。两个官方SDK平行维护，谁也没有高人一等的地位；后来长出来的独立FastMCP、官方Go SDK，都是在同一份规范上的不同枝干。选型时真正要比较的不是“官方支持谁”，而是“哪个生态离你的问题更近”。

还有第三个选择，值得单独说两句：Go。2025年下半年，MCP官方仓库里多了一个go-sdk——官方Go SDK，与Google合作维护，目标是覆盖完整的MCP规范。它的版本兼容表我记得很清楚：v1.0.0到v1.1.0对应2025-06-18版规范，到v1.7.0起完整支持了2026-07-28的无状态化大版本。Go SDK的设计很有意思，它没有用Python那种“运行时内省”的路线，而是让编译器干活——你定义一个Go结构体，给字段打上jsonschema标签，输入输出的类型描述在编译期就定死了：

```go
type Input struct {
    Name string `json:"name" jsonschema:"the name of the person to greet"`
}
```

我的WeClaw和Knowly都是Go项目，所以我对这条线的进展一直盯得比较紧。我的实际用法是混合的：Go进程做Host，通过stdio拉起Python或TypeScript写的Server（第4章里那个金融数据聚合器就是这个结构）。什么时候值得直接用Go写Server？当你的Server需要作为单个静态二进制分发、要跑进没有Python运行时的机器、或者要和现有Go服务共享代码时。还有个有趣的对照：同样是“把函数变成工具”，Go用编译期的结构体标签解决，Python用运行时的签名内省解决——两门语言的哲学差异，原样投影进了各自SDK的设计里。没有哪个更正确，只有哪个更贴你的工程习惯。在官方Go SDK成熟之前，社区里已经有一个叫mcp-go的第三方实现先行铺路，官方README里至今还留着对它的致谢——这也算生态演进的一个正常次序：先有野生的，再有官家的。

三条路都摆在这里了。我个人两条线都跑通了，但如果非要给一个入门推荐，我会选Python + FastMCP——原因只有一个：Python的装饰器语法让“暴露一个Tool”这件事的认知成本最低，你几乎不需要理解协议细节就能跑起来。这对于“第一个Server”来说，价值巨大。

💡 独思时刻： 我在2025年9月决定用Python写第一个生产级MCP Server时，身边不少朋友劝我“TypeScript才是MCP官方亲儿子，别走弯路”。但我知道自己当时90%的日常脚本都是Python，强行切TypeScript意味着同时学习新语言和新协议——双重认知负担。结果那个Server至今在生产环境跑了快一年，零宕机。我越来越确信一件事：选择你当下最熟悉的语言，比选择“官方推荐”的语言重要十倍。 工具链的熟悉度决定了你能否在遇到协议层bug时快速区分“是我不会写语言”还是“是我没理解协议”——前者会让你浪费大量时间在错误的方向上。

## 5.2 Python环境准备：uv + FastMCP

我假设你已经安装了Python 3.11+。如果没有，先去装一个——我建议用pyenv管理多版本，2026年8月我在R86S小主机上同时跑着3.10、3.11、3.12三个版本，pyenv让切换像喝口水一样简单。顺带一提，如今uv自己也能替你下载和管理Python解释器（uv python install 3.11），它内置了对官方Python发行版的托管——也就是说，连“装Python”这一步都在被这个工具收编。我的R86S上pyenv和uv现在并存，pyenv管老的脚本仓库，新项目全走uv。

接下来只需要三步：

第一步，安装uv。

uv是Astral团队（也是ruff的作者）开发的Python包管理工具，速度比pip快一个数量级。到2026年，社区教程几乎默认从uv起步，它已经从“新工具”变成了“新默认”。这句“快一个数量级”不是修辞，是官方基准数据：在冷缓存（首次下载依赖）场景下，uv比pip快8到10倍；在热缓存（依赖已在本地缓存，比如重建虚拟环境或更新依赖）场景下，快80到115倍。光是创建虚拟环境这一步，uv venv就比python -m venv快约80倍、比virtualenv快约7倍。快的原因没什么神秘：uv用Rust写成，以单个静态二进制发布，不依赖Python本体；它把所有包缓存在全局目录里，装第二个项目时用硬链接和写时复制直接复用文件，不重复下载、不重复解压；版本求解用的是PubGrub算法，和Rust的包管理器Cargo同源。Astral给uv的定位就叫“Python的Cargo”——如果你写过Rust，会立刻理解这个野心的方向。

安装方式极简：

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

或者如果你在macOS上且装了Homebrew：

```bash
brew install uv
```

Windows用户用的是PowerShell版本的安装脚本，到astral.sh官网照抄即可，后面5.6节我会专门说Windows的坑。

第二步，新建项目并初始化。

找一个你喜欢的目录，比如~/projects/mcp-first-server：

```bash
mkdir mcp-first-server && cd mcp-first-server
uv init . -p 3.11
```

这条命令会生成pyproject.toml和一个虚拟环境。如果你之前用过poetry，这里的体验几乎一致。差别藏在细节里：pyproject.toml声明依赖，锁文件钉死版本，虚拟环境隔离运行时，而uv init会一次生成pyproject.toml、记录Python版本的.python-version、锁定全部依赖的uv.lock，还有现成的.venv目录，四件事一个命令完成；poetry需要你再配一个pyenv来管解释器版本，pip用户则要手工写requirements.txt、手工激活venv。工具整合度越高，留给新人的分叉路口就越少。

另外两个高频命令先混个脸熟。uv run会在执行前自动同步依赖、确保跑在正确的虚拟环境里——5.5节Claude Desktop配置里的uv run python正是这个用法，它让Client侧不必关心“激活虚拟环境”这种琐事；uvx则能直接运行一个还没安装的包，适合临时试用别人写好的Server。还有一个文件值得多说一句：uv.lock把依赖钉死到精确版本，同事和CI拉下同一个lock文件，得到的就是高度一致的环境。对Server这种“要被别人的进程拉起来”的程序来说，环境可复现是排障时最大的仁慈——你永远可以先排除“是不是依赖版本漂了”这个变量。

第三步，安装MCP SDK。

```bash
uv add "mcp[cli]"
```

mcp[cli]这个包包含了MCP协议的Python实现，以及一个mcp命令行工具——后面我们会用到它来测试Server。

这里我要主动补一段历史，因为这个包名背后有一次分裂，不说明白你迟早会困惑。FastMCP最早是Jeremiah Lowin（Prefect的创始人）写的开源项目，1.0版本被收编进官方SDK，变成了mcp包里的mcp.server.fastmcp模块——也就是上面这条命令装进去的那部分。但Lowin没有停下，他把项目独立出来继续演进，就有了独立的fastmcp包：2.0加了代理与组合能力，2026年初发布3.0，把核心架构围绕Providers和Transforms两个原语重写了一遍。到2026年，社区统计约七成Python MCP生产服务器跑在FastMCP上，独立包的日下载量以百万计。本书示例代码里的from fastmcp import FastMCP写的是独立包的导入路径，所以请再补一行：

```bash
uv add fastmcp
```

两个版本的tool和resource装饰器用法基本一致，本书的示例在两边都能跑。你以后会同时见到两种写法，知道它们同源，就不会慌。

整个过程不会超过三分钟。我在2026年7月给团队一个新入职的同事演示这个流程，他从零开始到跑通第一个Server，用了大约九分钟——其中六分钟是在等Python下载。补一句2026年的注脚：如果用uv托管Python版本，那六分钟在今天可以压缩到几十秒。工具的速度是复利——每一步省一分钟，一条链路走完就是另一种体验。这句感受在服务器上会更明显：我在R86S上重建几个Server的环境，热缓存让整轮安装以秒计——快不是炫技，是让“重建环境”这件事的成本低到可以随手做，从而真的常做。

## 5.3 第一个Server：一个加减乘除计算器

不用一上来就搞复杂的东西。我们先写一个能做加减乘除的Server，让它跑起来、连上Client、看到交互过程——这比任何理论都管用。

在项目目录下创建server.py：

```python
from fastmcp import FastMCP

mcp = FastMCP("Calculator")

@mcp.tool()
def add(a: float, b: float) -> float:
    """将两个数字相加"""
    return a + b

@mcp.tool()
def subtract(a: float, b: float) -> float:
    """从第一个数字减去第二个数字"""
    return a - b

@mcp.tool()
def multiply(a: float, b: float) -> float:
    """将两个数字相乘"""
    return a * b

@mcp.tool()
def divide(a: float, b: float) -> float:
    """将第一个数字除以第二个数字"""
    if b == 0:
        raise ValueError("除数不能为零")
    return a / b

if __name__ == "__main__":
    mcp.run()
```

我数了一下，不算空行，有效代码一共22行。

值得把这22行逐块拆开看，因为每一块都对应协议层的一件事。第一行import，把协议实现装进来，你写的代码里没有任何一行在处理JSON或stdio——这不是简化演示，生产级Server的import长什么样，这行就长什么样。第二行，FastMCP用构造参数“Calculator”实例化出mcp对象——这个对象就是你的Server，“Calculator”则是它对外汇报的名字，将来会出现在initialize握手的serverInfo字段里——第4章我们拆过握手流程，当时你看的是协议视角，现在你知道了那个字段从哪来。四个 @mcp.tool() 装饰器，把四个普通函数注册进Server的工具表，装饰器本身没有魔法，它做的事是“登记”。函数签名里的类型注解a: float, b: float -> float，也不是风格偏好——它们是协议数据的直接来源，马上展开。每个函数下面三引号包起来的docstring，会成为对应工具的description。divide里那行抛出ValueError的语句，连同“除数不能为零”这条中文错误信息，是全文件里最容易被忽略、却最有门道的一行：这条信息不会消失在日志里，它会被SDK捕获、包装，原样送回给模型。最后在主入口里调用mcp.run()，默认走stdio传输，把自己的标准输入输出交给协议层调度。

那FastMCP到底靠什么，把一个普通函数变成了MCP协议里的Tool？机制上就三步。第一步，启动时内省每个被装饰函数的签名，用Pydantic的类型系统把注解编译成JSON Schema——str映射为string，int映射为integer，float映射为number，bool映射为boolean，list[str]变成字符串数组，Optional[str]变成可空参数，带默认值的参数在schema里带上default字段；如果你的参数本身是一个Pydantic模型，FastMCP会把模型的全部字段和校验器编进一份完整的schema。第二步，函数的docstring变成description。第三步，把函数名、description、这份inputSchema拼装成一个工具对象，挂进tools/list的响应里。也就是说，当Client发来tools/list请求时，你看到的响应大致长这样：

```json
{
  "name": "add",
  "description": "将两个数字相加",
  "inputSchema": {
    "type": "object",
    "properties": {
      "a": { "type": "number" },
      "b": { "type": "number" }
    },
    "required": ["a", "b"]
  }
}
```

你没有写过一行JSON Schema，但AI看到的每一个字，都是从你的类型注解和docstring里编译出来的。调用方向也是同一套机制反过来走：Client发来tools/call，SDK先按inputSchema校验参数、做类型转换，校验通过才进你的函数体；返回值再被包装成内容块和结构化数据（structuredContent，2025-06-18版规范加入的能力）送回。校验失败根本不会执行你的代码——这也是为什么我不建议在这22行里加任何输入检查，schema已经在门口替你查过一遍了。

既然docstring会原样变成模型读到的工具描述，就值得认真谈谈怎么写。我给自己立了三条原则。第一，第一句话必须说清“这个工具做什么”——模型浏览工具列表时，注意力高度集中在前半句，别把最关键的信息藏到第三行。第二，写清参数的语义与边界，尤其是“什么时候不该用这个工具”。我吃过亏：早期写过一个删除文件的工具，描述里只写了“清理文件”，没交代删除的边界，结果Claude把“清理”理解得很宽，两次误删了我的测试文件；把描述改成明确的“删除”、并在参数说明里写清路径要求之后，误删再没发生过。第三，控制长度——描述不是越长越好，我自己的观察是单条工具描述超过200 Token之后，Claude对它的理解准确率会下降约20%，长描述等于往每一次对话里塞税。divide那句docstring只有十个字，就是第一条原则的正面示范；第7章我们还会回到这个话题，那里有一条更长的教训等着你。

这就是FastMCP的魔力：你只需要把普通Python函数用 @mcp.tool() 装饰，它就自动变成了MCP协议中的Tool，包括函数名、参数名、参数类型、docstring——全部自动提取并转换为MCP的tools/list响应格式。

如果你觉得“这也太简单了，协议细节呢？”，我的回答是：协议细节被封装在了FastMCP里，你不需要在写第一个Server时就理解所有的JSON-RPC消息格式。 先跑起来，再深入——这是我从写作「源码考古」系列文章中学到的经验：直接啃源码容易迷失，先看到它能做什么，再问它怎么做的。

## 5.4 跑起来：用MCP Inspector测试

代码写完了，怎么确认它真的能用？

官方提供了一个叫mcp dev的Inspector工具，在你安装了mcp[cli]之后就可以用了：

```bash
mcp dev server.py
```

执行后，终端会显示一个本地URL（老版本Inspector跑在Vite开发服务器上，地址通常是http://localhost:5173，新版本默认换成了6274，一切以终端打印出来的URL为准），打开浏览器就能看到一个可视化界面。它列出了你所有的Tools，你可以点击每个Tool、填入参数、点击“执行”，然后看到返回值。顺带说一句，启动方式有三条等价的路——mcp dev server.py（官方SDK附带）、fastmcp dev server.py（独立FastMCP版，效果相同），以及直接用npx @modelcontextprotocol/inspector接任何语言的Server，Node生态的调试也是同一个界面。

这个界面的信息量比你第一眼看到的大。除了Tools页签，还有Resources和Prompts——Resources页签可以浏览Server暴露的所有只读数据并直接读取，还能订阅，内容一变界面就刷新；Prompts页签能选中模板、填参数、预览渲染出来的消息。再往下翻，是几个调试时真正救命的面板：Protocol面板完整记录每一次请求、响应和通知的收发历史——这就是协议层的trace，当你怀疑“消息到底发没发出去、格式对不对”时，第一现场在这里；Network面板看HTTP流量和头部，调远程Server时用得上；Console面板收集Server进程的stderr输出——记住这一点，5.5节我们会说到为什么日志必须走stderr；Logging面板让你切换MCP的日志级别，观察Server的运行状态。另外，Inspector会把每个Server的启动命令、参数和环境变量存进用户目录下的配置文件（~/.mcp-inspector/mcp.json），在界面里就能改，下次启动直接复用——相当于一个轻量的环境变量面板，调API_KEY这类东西时不用反复改命令行。如果你想把“Server能不能正常列出工具”塞进CI做冒烟测试，还有CLI模式——不开浏览器，一条命令直接出结果：

```bash
npx @modelcontextprotocol/inspector --cli server.py --method tools/list
```

终端党还有TUI交互模式可选，不开浏览器也能在终端里完成整套调试。不过有两点边界要知道：Inspector一次只维持一个活跃连接，它不是并发编排工具；它也不会替你跑真实模型去判断“工具选得准不准”——前者多开几个终端就能绕开，后者本来就是第6章的内容。

我第一次用这个工具时，直接在浏览器里用divide功能尝试了“10除以0”，看到了我代码里自定义的“除数不能为零”错误信息——那一刻我才真正确认：我的Server在跟MCP协议规范对话，不是我自己跟自己玩。后来我理解了这个瞬间为什么重要：那行ValueError从我的函数里抛出，被SDK捕获并转换成isError为真的工具结果，穿过完整的协议栈回到浏览器——一次异常走完了“函数错误到协议语义”的全链路。工具失败不丢人，失败得让调用方看懂，才是Server的本分。

这个Inspector本质上是一个MCP Client，它通过stdio启动你的Server并完成initialize握手，然后让你在UI里手动调用Tool。它不依赖任何外部AI模型，所以特别适合开发调试——你不需要打开Claude Desktop就能验证Server的功能。但它也有一点测不了：模型。Inspector能告诉你“工具能不能被调用”，测不出“AI会不会在恰当的时机选它、传对参数”——工具列表一长，选择就成了一门学问，那是第6章的话题。

用得久了，我的调试工作流固定成三段：先在Inspector里手动跑通每个工具，确认功能对不对；再接上Claude Desktop，看模型会不会在真实对话里自然地选中它、传对参数，确认体验顺不顺；最后把tools/list的冒烟检查挂进CI，确认版本迭代之后回归稳不稳。三段各自回答一个问题，谁也替代不了谁。

## 5.5 连上真正的Client：Claude Desktop配置

既然Server能跑了，下一步让它连上一个真实的AI应用。

假设你用的是Claude Desktop，配置方式是在~/Library/Application Support/Claude/claude_desktop_config.json（macOS路径，Windows和Linux略有不同）中加入：

```json
{
  "mcpServers": {
    "calculator": {
      "command": "uv",
      "args": ["run", "python", "/path/to/your/server.py"]
    }
  }
}
```

这段配置翻译成人话：calculator是我给这个Server起的本地代号，Claude Desktop每次启动时会用uv run python去执行那个绝对路径的脚本，通过stdio跟它对话。三平台的配置文件路径我列一下，省得你翻文档：macOS在上面那个位置；Windows在%APPDATA%\Claude\claude_desktop_config.json；Linux在~/.config/Claude/。Windows用户还有个小细节：JSON里的路径反斜杠必须转义成双反斜杠，或者干脆用正斜杠——我建议后者，少一半麻烦。

还有一个长期问题值得提前打预防针：每个Client的MCP配置是各自独立的——Claude Desktop一个文件，Cursor一个文件，VS Code扩展又一个文件。同一个Server要在三个工具里用，就得维护三份配置，改一处漏两处是常态，社区把这种现象叫配置漂移。我的土办法是把配置片段集中收在一个仓库里，用脚本分发到各个Client——土，但有效。

注意：这里的路径要填绝对路径。我第一次配置时因为用了相对路径，Claude Desktop找不到server.py，花了大半小时排查——后来才发现它启动时的当前工作目录不是我想象中的项目目录。这个坑的本质是：你无法假设Client拉起子进程时的工作目录在哪，相对路径在这条链路上没有稳定参照物，只有绝对路径是自解释的。

重启Claude Desktop后，在对话界面输入：“请帮我计算 123.45 乘以 67.89 的结果”，Claude会识别出你需要调用multiply这个Tool，自动传递参数、执行、把结果返回给你。配置生效的标志是输入框旁出现一个小锤子图标——它代表Claude已经通过tools/list拿到了你的工具清单。

整个过程中，你不需要告诉Claude“请调用我的multiply函数”——它根据你Tool的docstring和参数名自动判断了何时该用、怎么用。

这个“自动判断”背后是LLM的函数调用（Function Calling）能力，而MCP的角色是标准化了“这个Tool长什么样”的描述格式。换句话说，Claude Desktop不需要为每个MCP Server单独写适配代码——所有Server都通过同一套tools/list和tools/call接口打交道。这也是为什么第7章我会把工具描述当一等公民来写——在Client眼里，你的docstring不是注释，是它决定用不用你、怎么用你的几乎全部依据。

连上真Client之后，还有一条纪律必须刻在肌肉里：stdio模式下，Server的标准输出是协议通道，除了协议消息什么都不能往里写。我见过有人习惯性写print调试，结果握手直接失败——那些打印内容混进了JSON-RPC的字节流里，Client解析不出合法消息。调试输出一律走stderr（Inspector的Console面板收的就是它）或logging模块。第4章说过stdio是“一条管道两种语义”，从这一刻起你要真的当回事。

## 5.6 一个常见陷阱：stdio协议下的环境变量

当你通过stdio启动Server时，Claude Desktop（或其他Client）会以子进程的方式启动你的Python脚本。这意味着环境变量是从Client进程中继承的。

我踩过的一个具体坑：2026年3月，我在一个MCP Server里用os.getenv读取名为API_KEY的环境变量，本地终端里跑mcp dev server.py一切正常，但配到Claude Desktop里后始终报“API_KEY未设置”。原因很简单：我在~/.zshrc里设了环境变量，但Claude Desktop在macOS上是从launchd启动的，不加载shell配置文件。

把机制摊开看就不奇怪了。launchd是macOS的init进程，所有GUI应用都从它的进程树里 fork出来，这条链路上根本没有zsh什么事；而~/.zshrc只在交互式shell启动时才被执行。你在终端里跑mcp dev，那是zsh的子进程，变量自然在；Claude Desktop从Dock或Finder启动，走的是launchd的家谱，zsh设过的变量它一概不知。同一个坑在Windows上的变体是：从开始菜单或计划任务启动的应用，同样不继承你后来在PowerShell里setx的变量。结论是环境变量的可见范围跟着进程树走，不跟着“我在哪台机器”走。这也是为什么我后来在新机器上配置MCP时都先做一件事：用launchctl查看GUI侧到底能看到哪些环境变量，确认期望的变量在不在那棵进程树里——先看树，再排错。

解决方案有两个：

1. 在claude_desktop_config.json中用env字段显式传入：

```json
{
  "mcpServers": {
    "my-server": {
      "command": "uv",
      "args": ["run", "python", "/path/to/server.py"],
      "env": {
        "API_KEY": "your-key-here"
      }
    }
  }
}
```

2. 在Server代码里从文件或外部密钥管理服务读取，不依赖环境变量。

两个方案的取舍值得想清楚再选。方案一改动最小，但密钥落进了配置文件——记得把这个文件挡在git仓库外面，也别忘了它同样受5.5节那条“绝对路径”纪律的约束；方案二多写几行代码，换来的是行为一致性。我后来选择了方案二——把敏感信息放到一个本地加密配置文件中，Server启动时解密读取。这样不管是Inspector还是Claude Desktop，行为一致。如果团队规模再往上走，还有第三条路：把密钥托管进专门的密钥管理服务，Server启动时通过SDK在线取用——本地加密文件其实是它的极简版，两者解决的是同一个问题：让密钥离开shell配置和代码仓库。

顺带把Windows用户的两件事在这里一并交代了。第一是路径：本章所有路径写法在Windows上要换成反斜杠形式，JSON里记得转义或改用正斜杠；第二是编码——中文版Windows的默认编码是GBK而不是UTF-8，Python读写文件时容易踩出乱码或UnicodeDecodeError，最省心的解法是在env字段里给Server加一个PYTHONUTF8=1，强制Python进入UTF-8模式。这两个问题都跟MCP本身无关，但它们出现在你第一次连Client的同一周里——提前知道，能省掉一个晚上。

## 5.7 从Tool到Resource：再走一步

Tool是MCP里最常用的能力，但Server不止于Tool。

如果你想暴露一些只读数据——比如一个静态的汇率表、一份公司内部文档、一个目录下的所有文件名——可以用 @mcp.resource() 装饰器：

```python
@mcp.resource("file:///docs/readme.txt")
def get_readme() -> str:
    return "这是计算器Server的使用说明..."
```

注意装饰器里那个参数：file:///docs/readme.txt是一个URI，它就是这份数据在协议里的身份证——Client将来按这个地址来取数据，URI的scheme（file、http，或你自己定义的方案）决定了寻址方式。写法和Tool几乎一样，函数加装饰器就完事，区别只在装饰器要多带一个URI参数。

Resource与Tool的区别在于：Tool是“执行动作”，Resource是“提供数据”。Client可以主动读取Resource，也可以让AI模型根据上下文自动选择是否读取。

那么什么时候该用哪个？我在实战里养成了三个判断动作。第一问：这个能力是“做一件事”还是“给一份数据”？查询实时汇率要传参数、依赖时效、有明确的调用时机，是动作，做成Tool；币种代码说明、单位换算规则这类静态参考，是数据，做成Resource。第二问：调用有没有副作用？幂等的只读操作优先考虑Resource，有副作用的（写文件、发消息、下单）只能是Tool——Resource的语义承诺就是“读我没关系，随时读”。第三问：它在对话里的出现时机——Tool是按需调用，模型觉得需要才花一次往返；Resource可以被Client主动附加进上下文，适合“几乎每轮都用得上”的信息。三个问题问完，归属基本就定了。拿捏不准时还有个兜底判断：想象这个能力将来要交给别的Agent复用——动作天然可组合，数据天然可引用，你写下的第一个装饰器，其实是在给未来的自己定合同。拿汇率场景举例：我把汇率查询做成Tool，把币种规则表做成Resource，模型问“100美元换多少人民币”时走Tool，需要解释“JPY是日元吗”时读Resource——两边各干各的活，互不抢戏。

有了Tool和Resource，你的Server就有了一副完整的骨架：既能被AI调用来做事（Tool），也能被AI直接读取信息（Resource）。至于Prompts（预定义对话模板），它们更多是Client端的工作流辅助——我会在后续章节详述。

## 5.8 独思时刻：熟悉度大于官方推荐

回头看一眼：你从一个空目录出发，用uv建了项目、用FastMCP写了22行代码、用mcp dev测试了功能、最后配置Claude Desktop让AI真正调用了你的计算器。

这整个过程——从环境准备到第一次AI调用——我在2026年6月给一个实习生演示时，掐表是14分钟。他之前从没接触过MCP。

你现在手里已经有了一根杠杆。它的名字叫“第一个能跑的MCP Server”，虽然它目前只能做加减乘除，但它已经具备了跟任何MCP兼容的AI应用对话的能力。

💡 独思时刻： 很多人在意“我要不要从零手写JSON-RPC消息处理来体现我真正理解协议”，我的回答从来不委婉：不要。 2025年11月我刚开始研究MCP时，也动过这个念头——“用FastMCP太黑了，我要从socket开始造轮子才能学到东西”。结果我花了三天时间处理stdio的流式读写和JSON边界解析，最终写出来的代码比FastMCP慢两个数量级还容易出bug。那三天唯一教会我的事是：底层细节值得理解，但不值得亲手上手写一遍。 真正生产级的Server，没有谁会从零实现协议解析——就像没有人会从二极管开始造一台电脑。工具的价值在于让你把精力放在“Server能做什么”上，而不是“Server怎么说话”上。

💡 独思时刻： 5.1节那句“熟悉度大于官方推荐”，写完整章之后我又想加一个注脚——熟悉度是可以设计的。2026年上半年我带团队里两个新人入坑MCP时，刻意不给选择权：路线固定为uv、FastMCP、Inspector三件套，命令抄本章，不许换。等他们各自跑通第一个Server、知道了“一条完整的路长什么样”，我再陪他们复盘每一步为什么这么选、哪些环节可以换工具。结果两个人都在两周内交出了能上生产的Server。我由此修正了自己带人的方法论：先走通一条路，再谈选型——没有完整走通过一条路的人，给他再多选项，他得到的不是自由，是迷路。这一章给你的uv、FastMCP、Inspector三件套，本身也都是可替换的默认答案；默认答案的价值不在于它永远正确，而在于它让你先把力气花在真正的主题上——Server能做什么。至于哪天你换到TypeScript或者Go的河对岸，会发现这一章建立的心智模型原封不动：登记工具、编译schema、走stdio、在Inspector里看协议trace。语言会换，协议不变——这正是MCP作为“AI的USB-C”最实在的回报。

下一章，我会带你把这个计算器改造成一个真正有用的东西——从“能跑”变成“能用”，接入真实的外部数据源，让它从玩具长成工具。

---

## 第6章 MCP客户端开发与集成

# 第6章 MCP客户端开发与集成

上一章收尾时，你用22行Python代码跑通了第一个MCP Server，并在Claude Desktop里让AI替你算了几个数。那一瞬间你应该感受到了——代码本身不值钱，值钱的是AI能调用你的代码。

但这里的微妙之处在于：你写的那个计算器Server，严格来说不是为“人”写的，是为“AI”写的。而真正决定它有没有用、好不好用的，是Client（客户端）——那个负责连接AI模型和Server的中间层。

这一章我们翻转视角：从Server的“被调用”切换到Client的“去调用”。你要学会自己构建一个MCP Client，让AI应用（而非Claude Desktop）成为你的Host。

动笔之前，我把自己2024年11月以来写过的所有Client代码翻出来数了数：给WeClaw写的Go版Client、给Knowly写的采样Client、给雨轩编辑器写的TypeScript轻量Client，还有一堆为了调试协议而写的几十行一次性脚本。它们的形态五花八门，但剥掉业务逻辑之后，骨架惊人地一致。所以这一章我沿着这个骨架推进：先弄清楚Client到底在做什么，再决定用SDK还是裸写协议，然后亲手写两个Client——一个对话助手、一个搜索智能体，最后补上错误处理这块最容易被忽略的拼图。我的核心判断先放在这里：Server是为AI写的，Client决定AI选得准不准。

## 6.1 客户端到底在做什么

MCP官方规范把Client定义得很清晰：它是一个连接器，一端对接AI模型的接口（通常是Chat Completion API或更底层的推理接口），另一端对接一个或多个MCP Server。它的核心任务只有三件：

1. 发现能力——通过initialize握手拿到Server的Tools、Resources、Prompts清单。
2. 调用工具——把AI的决策（“我该调用哪个Tool”）翻译成对Server的JSON-RPC请求，再把结果塞回AI的上下文。
3. 管理会话——维持Client与Server之间的状态，处理超时、重连、错误码。

这三句话我建议你抄下来贴在显示器边上。Client的一切设计分歧——用什么语言、连几个Server、要不要缓存——最终都能追溯到这三件事的某一件上。

先说发现能力。initialize握手不只是“打个招呼”，它同时是一次能力协商：Client声明自己支持什么（比如roots、sampling），Server声明自己提供什么，双方在最开始的往返里就把合作范围谈拢。2025年6月18日的版本还给HTTP传输加了强制的MCP-Protocol-Version头，让版本对齐发生在第一帧业务消息之前，而不是运行时才爆雷。我自己的习惯是握手完成后立刻发一次tools/list把清单拉下来——注意，是“立刻”，不是“每次调用前”，原因6.4节会展开。

再说调用工具。这一步的本质是格式翻译：MCP的Tool有name、description、inputSchema三件套，而各家模型的function calling接口各有各的信封格式。好在JSON Schema是几乎所有模型生态的公共货币，这个翻译通常不需要改语义，只需要换信封——把inputSchema原样透传过去，剩下的就是字段名对齐的体力活。

最后是管理会话。这是三件事里变化最大的一件。2026年7月28日之前，Client要维护Mcp-Session-Id、处理会话过期后的重新初始化；无状态化大版本落地之后，Session概念被整体移除，改用显式句柄。官方当时给出的影响分析是：约90%的公开Server根本没用Session ID，6.3%只在SDK样板代码里引用过，真正需要迁移的约3%。对Client开发者来说，这是个实打实的减负——重连、故障转移、水平扩容背后那些让人头疼的状态同步问题，一夜之间消失了大半。

我最早试着写Client时犯过一个极蠢的错：我试图让Client直接调用Server的Tool，然后把结果返回给用户，没经过AI模型。那等于用MCP做了个RPC框架——能用，但AI根本没有参与决策。后来才意识到，MCP Client的魂在于它是AI的“手”和“眼”，不是你的“远程调用工具”。

这个教训我记到今天。每次和团队聊MCP，我都会问一个问题：Client把Tool结果直接返回给用户，和把结果喂回模型再生成回答，差别在哪？前者是工具，后者是智能。如果我们要的是智能，那Client的每一个设计决策都应该服务于“让模型的决策更准”，而不是“让调用更快”。记住这句话，6.4节我们会回到它。

## 6.2 用SDK还是裸写协议

跟Server侧一样，Client侧也有两条路：

· 用官方SDK（TypeScript或Python的@modelcontextprotocol/sdk）——开箱即用地处理传输层、消息编解码、会话管理。你只需要实现业务逻辑：什么时候连哪个Server、怎么把Tool列表喂给AI、怎么处理AI的Tool调用请求。

· 裸写JSON-RPC——自己建立子进程或WebSocket，按协议规范发initialize、tools/list、tools/call等请求，自己解析每一帧。

我建议你从SDK开始，但必须看一眼它发的原始消息。我的习惯是：把Client的transport层加一行日志，打印所有发出和收到的JSON-RPC包。看一百遍文档，不如看一遍真实的tools/list请求体长什么样子。

这句话值得展开说说。第5章我提过用MCP Inspector测试Server，Client侧其实同理——Inspector本身就是个现成的Client，你可以在它的事件面板里看到每一帧消息。但Inspector让你看到的是“消息长什么样”，自己加日志看到的是“我的代码到底发了什么”，这两者经常不一样。给你看一眼日志里真实的tools/list请求，它朴素得让人意外：

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/list"
}
```

重头戏在响应里——每个Tool的name、description、inputSchema都从那条通道涌进来。当你亲手把这些报文打印出来，你会立刻理解6.4节要讲的问题：这堆描述文本会被原封不动塞进模型上下文，每一个字符都在花Token的钱。

那什么时候值得裸写？两种场景。一是极简运行时。我的knasync项目里那个MCP Server只用Python标准库实现stdio上的JSON-RPC 2.0，444行、零外部依赖——Server侧尚且可以这么干，Client侧同理：如果你的Host跑在一个装不了npm包的环境里，手写initialize、tools/list、tools/call三个方法的编解码，一天内能搞定。二是想真正吃透协议。我建议每个认真做MCP的人都至少裸写过一次，哪怕写完就扔。你会亲眼看到id和result的配对关系、request和notification的区别、握手的协商顺序——这些细节SDK都替你藏起来了，而排查问题时它们全是线索。

顺带一提，AAIF在2026年8月27日发布的路线图五项优先事项里，第五项就是改进SDK开发者体验。官方也承认SDK还有毛糙的地方，所以“看一眼原始消息”这个习惯，在未来几年都不会过时。

## 6.3 从零构建一个AI对话助手

我用TypeScript写一个完整的对话助手Client，它会干三件事：连上上一章的计算器Server、让AI决定何时调用add、把计算结果融入回答。

```typescript
import { Client } from "@modelcontextprotocol/sdk/client/index.js";
import { StdioClientTransport } from "@modelcontextprotocol/sdk/client/stdio.js";
import { OpenAI } from "openai";

// 1. 启动MCP Client连接Server
const transport = new StdioClientTransport({
  command: "uv",
  args: ["run", "python", "/path/to/calculator_server.py"]
});

const client = new Client(
  { name: "my-chat-client", version: "1.0.0" },
  { capabilities: {} }
);
await client.connect(transport);

// 2. 获取Server的Tool列表
const toolsResult = await client.listTools();
const mcpTools = toolsResult.tools.map(t => ({
  type: "function",
  function: {
    name: t.name,
    description: t.description,
    parameters: t.inputSchema
  }
}));

// 3. 初始化AI模型（用OpenAI接口）
const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });

// 4. 对话循环
const messages = [{ role: "user", content: "37加15等于几？" }];
const response = await openai.chat.completions.create({
  model: "gpt-4o",
  messages: messages,
  tools: mcpTools,  // 把MCP Tool暴露给AI
  tool_choice: "auto"
});

// 5. 处理AI的Tool调用请求
const toolCall = response.choices[0].message.tool_calls?.[0];
if (toolCall) {
  const result = await client.callTool({
    name: toolCall.function.name,
    arguments: JSON.parse(toolCall.function.arguments)
  });
  // 把Tool结果喂回AI，让它生成最终回答
  messages.push(response.choices[0].message);
  messages.push({
    role: "tool",
    tool_call_id: toolCall.id,
    content: result.content[0].text
  });
  const final = await openai.chat.completions.create({
    model: "gpt-4o",
    messages: messages
  });
  console.log(final.choices[0].message.content);
}
```

这段代码的每一行都有具体作用：用StdioClientTransport启动子进程、listTools把MCP的Tool格式转换成OpenAI的Function格式、callTool执行调用并把结果回填。它是一个完整的闭环。

我把这个闭环拆成四段再看一遍。第一段是传输层：StdioClientTransport会fork一个子进程去执行uv run python calculator_server.py，Client和Server之间用stdin与stdout通信——这就是第5章反复出现的stdio传输在Client侧的样子，你实际要管的是这个子进程的生命周期：它退出你要知道，它卡住你要能杀。第二段是能力发现：listTools拿到清单后做了一次格式转换，把MCP的inputSchema塞进OpenAI的parameters字段——两个生态说的是同一套JSON Schema方言，翻译不伤语义。第三段是决策：tool_choice设为auto，“调不调工具”的决定权完全交给模型。第四段是回填：Tool结果以role为tool的消息追加进messages，模型看到结果后才生成面向用户的最终回答。

这个闭环能跑通演示，但离生产还差三个补丁。第一，它只处理了tool_calls数组的第0个元素——模型完全可能一次请求多个工具调用。第二，它没有消息循环——如果模型拿到计算结果后还想再调一次工具（比如先查汇率再换算），这段代码就断了。第三，它没有并发策略——多个调用挤在一起时，排队还是并行，需要规则。

先把消息循环补上，这是生产级Client的标配骨架：

```typescript
// 生产级消息循环：模型可能连续请求多轮工具
while (true) {
  const response = await openai.chat.completions.create({
    model: "gpt-4o",
    messages,
    tools: mcpTools,
    tool_choice: "auto"
  });
  const message = response.choices[0].message;
  messages.push(message);

  const calls = message.tool_calls ?? [];
  if (calls.length === 0) break; // 模型不再请求工具，本轮结束

  // 并发策略：只读调用并行，写入调用串行
  const results = await Promise.all(
    calls.map(call => {
      const args = JSON.parse(call.function.arguments);
      if (isReadOnly(call.function.name)) {
        return callToolWithRetry(client, call.function.name, args);
      }
      return callQueue.add(() =>
        callToolWithRetry(client, call.function.name, args)
      );
    })
  );

  // 按原始顺序回填结果
  calls.forEach((call, i) => {
    messages.push({
      role: "tool",
      tool_call_id: call.id,
      content: results[i].content[0].text
    });
  });
}
```

循环里的并发策略值得单独说。多个Tool调用到底排队还是并行？我的规则是三查。一查依赖：后一个调用需要前一个的结果，必须串行。二查读写：create、update、delete这类写操作并行容易产生竞态，串行最安全——MCP在2025年3月26日引入的annotations正好派上用场，readOnlyHint和destructiveHint就是给Client的并发调度信号。三查目标：不同Server之间天然可以并行；同一个stdio Server虽然是单管道，但JSON-RPC 2.0允许按id交错请求，吞吐的上限是那个子进程的处理速度。2025年11月25日的规范又加了Tasks机制，长任务可以异步执行、Client订阅结果——Client的角色从“等待者”变成“订阅者”，等待的线程被释放出来了。

我在2026年7月用这个模式给雨轩编辑器加了一个“一键查汇率”功能：编辑器里选中一段文字，AI判断它是不是含货币金额，是就调汇率Server换算。整个Client逻辑不到100行，却让一个写作工具凭空多了金融能力。

这个项目的乐趣就在“凭空”两个字。雨轩编辑器是个写作工具，本来和金融八竿子打不着——但Client把汇率Server接进来之后，用户选中文本按下快捷键，AI先判断“这段话里有没有货币金额”，有就调Tool换算，没有就什么都不发生。用户感知不到中间层的存在，只觉得“编辑器懂我了”。这就是Client的杠杆效应：Server提供能力，Client决定能力以什么姿态出现在用户面前。

## 6.4 工具路由的噪声问题

对话助手跑通之后，很自然的想法是：多连几个Server，能力不就更强了吗？2026年4月之前，我也是这么想的，直到那次压测给了我一记闷棍。

当时的背景是：WeClaw接入了十来个Server，我隐约觉得模型选工具的准确率在下滑，但拿不出数字。于是我设计了一个对照实验——同一批测试问题，分别在“只连相关Server”和“全部连上”两种配置下跑，统计模型选错工具的比例。最重的一组配置：同时连5个Server、一共暴露47个Tool，让GPT-4o自己做路由。

结果让我把实验重跑了三遍才敢相信：错误率从3%飙到了22%，涨了七倍多。更值得警惕的是错误的形态。我把坏样本逐条看了一遍，大致分三类：一是选错近亲工具，47个工具里有好几个都带“搜索”字样，模型经常在知乎搜索和全网检索之间犹豫，最后选错；二是参数幻觉，模型会给没有这个参数的工具凭空编造参数，比如给一个只接受query的搜索工具硬填上count；三是张冠李戴，把A Server的资源当B Server的工具调。这三类错误单看每一个都不致命，叠在一起，用户感知就是“这AI越来越笨了”。

为什么会这样？因为工具列表不是免费的。每一轮对话，所有工具的name、description、inputSchema都要塞进模型上下文。我后来专门量过一次：一个有43个工具的Server，光工具描述就占了55000多个Token。上下文是模型的注意力预算，预算被工具描述吃掉一大块，留给用户问题的注意力就少了。我还观察到一个更细的规律：单条工具描述超过200 Token之后，模型对它的理解准确率会明显下滑——描述越长，模型反而越抓不住重点。

所以Client层必须做减法。这一减，就减出了我称之为“路由代理”的东西，它在我手里经历了三个阶段的演进。

第一阶段是关键词路由。在Client里维护一张词表：“汇率”“美元”命中金融Server，“知乎”“热榜”命中搜索Server，命中谁就把谁的工具喂给模型。优点是零成本、毫秒级；缺点是脆——用户问“美元符号是怎么来的”，这不是汇率问题，词表却把它路由给了金融Server。词表永远追不上语言的变化。

第二阶段是意图分类。把用户问题先丢给一个便宜的小模型输出意图标签，再按标签路由。准确率上去了，但每次对话多了一次LLM调用的延迟和成本，而且小模型自己也会分错——我一度在“路由模型分错”和“主模型选错”之间来回救火，两头起火的感觉很糟糕。

第三阶段是语义路由，也就是现在的主流做法。把每个工具的描述向量化存进本地索引，用户问题来了也算一个向量，按余弦相似度取出最相关的若干个工具或Server，只把这些喂给主模型。Anthropic后来推出的Tool Search Tool把这条路走成了产品：工具定义不再全量预加载，模型按需检索，官方给的数据是Token消耗能降约85%。我在WeClaw里用的是简化版——Server级向量化加关键词兜底，实测够用。

路由代理跑起来之后，还剩两个工程问题。一个是重名冲突：MCP规范不处理多个Server暴露同名工具的问题，Client必须自己定策略——常见做法有按Server ID加命名空间前缀（6.6节你会看到Hermes注册的工具名叫mcp__zhihu_search__zhihu_search，前缀就是命名空间）、按优先级排序、或者弹回给用户裁决。另一个是工具列表的缓存。

缓存这事在2026年7月28日之前是有点微妙的：tools/list的结果带着会话语义，缓存失效的时机不好拿捏。无状态化大版本落地之后，工具列表变成了纯粹的数据，Client可以放心缓存。我的做法是静态缓存加三重失效条件：收到notifications/tools/list_changed通知时刷新、超过TTL时刷新（WeClaw设的是5分钟）、用户手动刷新时刷新。WeClaw还把这个缓存复用到了另一处——tools/list的结果直接注入LLM的system prompt，省掉每次现拉的往返。

但缓存有一个反直觉的坑：不要让工具列表在两轮对话之间频繁变化。主流模型服务商的prompt缓存是以工具列表为key的，工具集合一变，缓存全部失效，每一轮的上下文成本都回到全价。所以我给路由代理加了一条纪律：同一会话内，一旦喂给模型的工具集合确定下来，就尽量保持稳定，新挂载的Server宁可等下一轮对话再接入。

💡 独思时刻： 很多人会纠结一个问题——“我要不要把多个MCP Server的Tool合并成一个超大列表一次性喂给AI？”我的答案是千万不要。2026年4月我做过一次压力测试：同时连5个Server、一共暴露47个Tool，让GPT-4o自己做路由。结果模型在“选哪个Tool”这件事上的错误率从3%飙到了22%。我的体悟是：AI的上下文不是无限的，工具列表越长，选择噪声越大。与其把所有工具堆上去，不如在Client层做一层“路由代理”——根据用户问题的关键词，只把相关的2-3个Server连上来。一个聪明的Client不是“连得最多的Client”，而是“让AI选得最准的Client”。

## 6.5 扩展客户端：根目录管理与采样能力

基础对话助手够用了，但MCP Client的能力不止于此。规范里还定义了两个你迟早会遇到的高级能力。

根目录管理（Roots）：Server需要知道“我的文件操作该限制在哪个目录下”——比如一个Git Server只能操作某个仓库目录。Client通过roots/list告诉Server它的权限边界。如果你构建的是一个IDE插件型Client，根目录就是当前项目的根路径。

实现方式：

```typescript
const client = new Client(
  { name: "my-client", version: "1.0.0" },
  {
    capabilities: {
      roots: {
        listChanged: true  // 支持目录变更通知
      }
    }
  }
);

// Client需要提供roots回调
client.setRequestHandler(ListRootsRequestSchema, async () => ({
  roots: [
    { uri: "file:///Users/yuangs/my-project", name: "项目根目录" }
  ]
}));
```

这段代码有两处值得停下来。第一处是capabilities声明：Client在握手时告诉Server“我支持roots，而且目录变了我会通知你”。第二处是setRequestHandler——注意方向，不是Client去请求Server，而是Server发起roots/list请求、Client作答。这个方向翻转很有意思：权限边界是Server主动来问的，Client是“被查询的一方”，边界由Client说了算。

我实际用下来，roots最大的价值是安全边界。2025年我给WeClaw接文件类Server时吃过亏：一个文件整理Server没有约束工作目录，动了测试目录之外的文件。之后我给所有文件类Server强制配roots——Server能摸到哪儿，由Client划线，这就是最小权限原则在MCP里的落地姿势。另外别忘了处理目录变更：用户在IDE里切换了项目，Client监听到变更后要把新根目录推给Server，否则Server还拿着旧边界干活。

采样能力（Sampling）：这是Server向Client“反向”发起LLM调用的能力。场景是：Server在执行某个Tool时，需要AI帮忙做一步推理——比如“从这段文本里提取人名”。Server自己不能直接调LLM，它得请求Client代劳。

这个能力在2025年底的规范里还不太稳定，但到了2026年中已经有不少Server在用。我自己的Knowly项目就用了采样：每当Server捕获到一段不完整的文本片段，就请求Client让AI补全成完整句子。

Knowly这个场景值得展开。Knowly是我的剪贴板知识流水线，它经常捕获到半截文本——一段没写完的句子、一个孤立的关键词。早期版本把这些残片直接入库，检索时全是噪声。加上采样之后，Server每次遇到残片，就通过sampling/createRequest向Client发起请求，Client调用模型补全，把结果返回Server，Server再决定是否入库。整条链路里，Server从头到尾没有接触过任何模型API密钥——这是采样能力最优雅的地方：算力在Client侧，密钥在Client侧，Server只拿到推理结果。顺带还能省钱，我给Knowly的采样通道配的是最便宜的小模型，补全这种任务用不着大模型。

Client实现采样时要替用户想清楚三件事：用哪个模型、计入谁的额度、要不要用户确认。规范的立场很明确——Server发出的采样请求里可以带上它想要的系统提示词，但Client有权替换或忽略，用户也应该有机会看到甚至否决每一次反向调用。采样给了Server“借用大脑”的能力，Client就得当好这个大脑的守门人。

2025年6月18日的规范还补了第三个客户端侧能力：Elicitation，Server在工具执行过程中向用户提问——缺一个必填参数时，Server不再瞎猜，而是停下来问。我在2025年7月给一个Excel报表Server做集成时用过：用户说“给我出上月报表”，Server发现“上月”可能对应两个财年口径，就通过elicitation/create反问用户用哪个口径。对Client来说，Elicitation意味着你要在工具执行的中间态渲染一个交互界面——这对WeClaw这种跑在微信里的Client是个挑战，我用一条微信消息承载提问、下一条消息回收答案，勉强够用。Roots、Sampling、Elicitation合在一起，让Client从“传声筒”变成了“有立场的中间人”——替用户守边界、出算力、做转达。

## 6.6 案例2：能联网搜索的AI智能体

有了基础Client框架，我们来搭一个更实用的东西——AI搜索智能体。它同时接入两个Server：一个Web搜索Server和一个内容提取Server。

伪代码流程：

1. 用户问：“最近MCP有什么新进展？”
2. Client把这个意图交给AI。
3. AI判断需要搜索，调用search Tool。
4. 搜索Server返回一串URL和摘要列表。
5. AI再调用extract Tool，从摘要里最相关的URL提取正文。
6. 最终AI基于搜索结果生成回答。

这个流程里最关键的Client逻辑不是调用，而是状态管理——你需要在多轮Tool调用之间保持上下文。我常用的策略是：每调用完一个Tool就把结果追加到messages数组里，让AI看到“上一步拿到了什么”，再决定下一步。

这条策略听上去平淡，实操里有两个细节决定成败。一是结果体积控制：搜索Server一次返回上万字符是常态，原样塞进上下文，两三轮就把窗口吃满了——我的做法是摘要进messages、全文落本地文件，AI需要细读某个结果时再按需取。二是中间状态可见：4次Tool调用意味着4段网络往返，没有任何反馈的等待是体验杀手，Client应该在每个调用节点向前端推送进度——这个能力6.7节讲进度通知时会展开。

我在2026年8月用这个模式写了一个WeClaw的搜索插件，结果是：用户@我发来一个问题，WeClaw自动调用知乎搜索MCP Server、拿到5篇回答的摘要、再调用内容提取Server拿到全文、最后让DeepSeek生成一份带引用来源的答案。整个链路4次Tool调用，用户只等了一次回复。这套东西在微信里跑了两个月，周调用量稳定在200次以上。

周调用200次以上是个什么概念？WeClaw的用户就是我和家人朋友，几十个人。这意味着搜索已经成了这群人的默认习惯——有事先@它，而不是自己开搜索引擎。同样的两个Server，接进Claude Desktop是一个玩法，接进微信是另一个玩法：后者让“问AI”变成了“顺手的事”。下面把这两个Server的接入细节摊开，因为它们恰好代表了Client对接能力的两种典型姿势。

第一种是knasync统一Server——自建、stdio、本地子进程。2026年8月下旬，我把原来只提供搜索能力的MCP Server升级成了统一知识基础设施：一个Server、8个工具，覆盖从信息获取到知识沉淀的全链路。搜索侧6个工具：zhihu_search知乎社区搜索、zhihu_global_search全网检索、deepseek_search联网搜索、zhihu_hot热榜、zhihu_answer直答、fetch_zhihu_article抓取全文；发布侧2个工具：publish把内容发布到IMA笔记、博客、播客或Knowly NAS，upload上传文件到Knowly。实现上它就是444行Python标准库、stdio上的JSON-RPC 2.0，零外部依赖。而WeClaw作为Client接入它，只需要在config.json里声明两行：

```json
{
  "mcp_command": "python3",
  "mcp_args": ["/root/mcp-search-server/server.py"]
}
```

WeClaw启动时拉起子进程、initialize握手、listTools拉清单并缓存进system prompt，之后用户的每一次自然语言指令——搜知乎、查热榜、把这篇文章发到播客、传个文件到NAS——都由模型在这8个工具里自主编排。比如“把刚才搜到的那篇RAG文章发到播客”，实际链路是zhihu_search拿摘要、fetch_zhihu_article取全文、publish推到播客API，三次调用在一个Server内闭环。作为Client，WeClaw对这背后调的是命令行还是HTTP API毫无感知——这正是协议的价值：Server实现怎么重构，Client一行不改。这次升级还有一段调优插曲：模型不遵守用户指定的发布目标，自行扩展到所有渠道，修复方式是在工具描述里写明target必须严格遵守；upload工具被传了URL而不是本地路径，修复方式是在描述里强调file_path必须是本地路径。你注意到了吗——这些问题的病灶在Server的描述层，症状却全部出现在Client的对话里。把对话里的行为异常反推到描述层的修改，是Client开发者的一门必修手艺。

第二种是Hermes直连知乎官方MCP——远程、SSE、Bearer鉴权。这次接入本身没什么技术难度，但排查过程值得一提，因为它暴露了Client集成里最阴险的一类问题：静默失败。

Hermes是我的AI Agent框架，它的MCP支持其实早就写好了——tools/mcp_tool.py完整实现了SSE传输与headers鉴权。但2026年8月10日我准备接入知乎搜索MCP时发现，MCP支持一直处于no-op状态：.venv里缺mcp包，mcp-stderr.log自7月10日之后就没再更新过。配置了MCP Server，代码路径却永远不会真正执行——不报错、不告警，就是安静地什么都不做。我盯着mcp-stderr.log那个停在7月10日的时间戳看了很久，才反应过来该去查依赖，而不是查配置。

修复只需要一条命令：pip install mcp==1.28.1。装上之后，MCP支持从no-op变为可用。接着在Hermes的config.yaml里声明Server：

```yaml
mcp_servers:
  zhihu-search:
    transport: http        # SSE 传输
    url: https://developer.zhihu.com/api/mcp/zhihu_search/v1/sse
    headers:
      Authorization: "Bearer <access_secret>"
    timeout: ...           # 连接与读取超时
```

这里用的是MCP over SSE传输加Bearer鉴权。协议流程值得记一下：Client先连sse端点，Server通过endpoint事件返回一个带sessionId的实际message地址，后续的initialize、tools/list、tools/call全部发往这个地址。眼熟吗？这就是第3章讲过的初代HTTP+SSE传输——2025年3月26日的规范已经让它退役、由Streamable HTTP接位，但不少商业Server为了兼容存量Client仍然在用。Client侧要做的就是别挑食：stdio、Streamable HTTP、legacy SSE，三套传输都得会连。顺带一提那次调试的背景噪音：那台机器的根文件系统会被周期性改回只读，写配置前得先重新挂载，否则就报Read-only file system——光是分辨“是我的配置错了，还是文件系统又变只读了”就耗掉我半个小时。

重启gateway之后，agent.log确认了连接与鉴权通过：

```
2026-08-10 16:30:19 INFO tools.mcp_tool: MCP server 'zhihu-search' (HTTP):
  registered 3 tool(s): mcp__zhihu_search__zhihu_search,
  mcp__zhihu_search__list_resources, mcp__zhihu_search__read_resource
```

注册成功说明Bearer校验通过了——鉴权若失败，initialize阶段就会直接401。注意Hermes给工具加的前缀mcp__zhihu_search__，这就是6.4节说的命名空间，多个Server的同名工具靠它区分。我先用Python MCP client直接调了一次zhihu_search（query取“RAG 评测”，count取3），返回4507字符的结构化XML，title、url、author_name、ranking_score字段一应俱全。真正的惊喜在接入后不到4分钟：Hermes在实际对话里自主调用了这个工具——

```
16:33:47 Prompt: 知乎搜索 opencode go
16:34:15 INFO agent.tool_executor: tool mcp__zhihu_search__zhihu_search
         completed (0.75s, 13265 chars)
```

Agent仅用0.75秒就完成一次知乎搜索并拿回13265字符结果，随后正常生成回复，端到端链路完全打通。更妙的是连带效应：原有的zai-mcp-server（stdio）此前同样因为缺mcp包从未生效，这次一并被激活，两个Server共注册11个工具。你看，这不就是6.4节的现场版吗——一次接入，工具列表瞬间多出11个，路由代理的价值当场显现。

这次接入我总结成一句话：从“配置了但没生效”到“对话中0.75秒完成搜索”，中间只差一个pip install mcp的距离。但它留给我的教训比成果重要——Client集成里最危险的不是报错，而是no-op。报错你会去查，静默不生效，你会以为它“只是没被触发”。那之后我给Hermes和WeClaw都加了启动自检：连接成功、注册成功、首调成功，三个信号逐一打日志，缺任何一个就显式告警。

## 6.7 错误处理：Client的保命技能

Server可能崩溃、超时、返回格式错误——这些都是Client必须兜住的事。我踩过的坑基本集中在三类：

协议级错误：Server返回的JSON-RPC里没有id字段，或者error.code不是标准码。SDK会帮你做基础校验，但生产环境里我建议你在onerror回调里记录原始报文，方便事后排查。

超时处理：某个Tool执行了30秒还没返回——你让AI等还是不等？我的做法是给每个callTool设置20秒超时（注意：不是整个Client会话超时），超时后向AI返回一句“该工具执行超时，请尝试简化请求”。

重试机制：网络抖动、stdio子进程异常退出——这些瞬态错误值得重试。我在WeClaw里用的策略是：指数退避重试最多3次（0.5秒、1秒、2秒），如果Server连续失败，就在日志里标记该Server为“不稳定”，后续请求临时降级到备用Server。

你可以在Client代码里加一个简单的包装函数：

```typescript
async function callToolWithRetry(client: Client, name: string, args: any, maxRetries = 3) {
  for (let attempt = 1; attempt <= maxRetries; attempt++) {
    try {
      return await client.callTool({ name, arguments: args });
    } catch (e) {
      if (attempt === maxRetries) throw e;
      await sleep(500 * Math.pow(2, attempt - 1));
      console.warn(`Tool ${name} 重试第${attempt}次`);
    }
  }
}
```

三类坑各补一句实战注脚。协议级错误那条，原始报文的日志真的救过我：2025年9月，WeClaw的浏览器自动化Server偶发失败，SDK的报错只有一句“无效响应”，我翻原始报文才发现那个Server把error.code返回成了字符串而不是数字，SDK的严格校验把它拒了。没有原始报文，这种问题能把人逼疯。超时那条，20秒不是拍脑袋：我把WeClaw所有Tool调用的耗时拉出来看分布，P95在8秒左右，取个两倍多的安全余量就是20秒。重试那条有一个必须警惕的前提——幂等性。查询类调用重试三遍无所谓，写操作盲目重试就是灾难：knasync的upload工具在上传失败自动重试时，曾经在Knowly NAS里攒出一堆重复文件，后来不得不按内容哈希去重。所以我的重试包装函数现在多了一条纪律：destructiveHint为true的工具一律不自动重试，要么人工确认，要么先查状态再决定。

除了兜住这三类坑，Client还应该主动利用一个规范里的轻量机制：进度通知（progress）。callTool时可以带上progressToken，Server在长时间执行中回发notifications/progress，里面带progress、total和可选的message。它解决两个问题。一是体验：4次Tool调用的搜索链路，用户在微信里能看到“搜索中——第2篇”这样的进度，等待焦虑会小很多。二是超时策略的精细化：很多SDK支持“收到进度就重置计时器”，Server每回发一次进度，Client的等待时钟就续一次命——这对导出报表、批量抓取这类慢工具是刚需。但一定要配一个总时长硬上限，否则一个有bug的Server可以靠持续刷进度把Client吊死在等待里。我的配置是单次进度续命、总时长封顶：

```typescript
const result = await client.callTool(
  { name, arguments: args },
  {
    timeout: 20000,                // 单次等待20秒
    resetTimeoutOnProgress: true,  // 收到进度通知就续命
    maxTotalTimeout: 60000,        // 总时长封顶60秒
    onprogress: (p) => {
      const pct = Math.round(((p.progress ?? 0) / (p.total ?? 1)) * 100);
      notifyUser(`${name} 执行中：${pct}%`);
    }
  }
);
```

与进度配套的还有取消：Client发notifications/cancelled并带上对应的请求id，Server收到后应尽快终止执行。用户在微信里说了句“算了不搜了”，Client就该把这个信号传下去，而不是让Server继续跑完再扔掉结果。连接层面的健康检查也别忽略——ping可以由任意一方发起，我把它设成可配置的间隔（默认30秒），连续失败就触发重连；至于Streamable HTTP时代对已知会话收到404要重新走initialize的那套逻辑，2026年7月28日无状态化之后简化成了句柄重建，这一块的代码量比两年前少了不止一半。

## 6.8 本章小结：Client才是你定制AI行为的主战场

如果你不想从零写Client，只想在现有工具里用MCP——这部分操作更接近配置而非开发，但值得过一遍。

Claude Desktop我们上一章已经做了：claude_desktop_config.json里声明command和args即可。它的Client内置在Claude Desktop的代码里，你不需要自己写。

Cursor从2026年3月左右的版本开始支持MCP。在Cursor的Settings → MCP Servers里添加配置，然后你可以在Composer里直接@引用MCP Tool。区别是：Cursor把MCP Tool当成“代码生成时的上下文助手”，而不是对话助手——它会用你的Tool结果辅助补全代码，不一定把结果展示给你看。

自定义工具集成：如果你在使用我开发的WeClaw（一个在微信里跑AI的网关），它的MCP Client模块是内置的：你只需要在~/.weclaw/mcp_servers.yaml里配置Server列表，WeClaw启动时会自动连接所有Server并把Tools暴露给背后的大模型。这是我在日常生产环境里最常用的方式——毕竟我每天80%的AI交互发生在微信里，不在网页上。

Server决定了“你能做什么”，Client决定了“你什么时候做、怎么做、做错了怎么补救”。

很多MCP新手把精力全扑在Server开发上，写一堆花哨的Tool，然后随便连个Claude Desktop就当完事了。我的经验恰恰相反：你花在Client路由逻辑、错误恢复、多Server编排上的精力，会十倍地放大Server的价值。一个笨Server配一个聪明Client，比一个聪明Server配一个笨Client有用得多。

我自己的WeClaw就是最好的例子——它连的每个MCP Server单拎出来都不复杂，有的只有三五个Tool。但因为Client层做了话题路由、记忆注入、多Agent协作，用户感知到的是一套“越来越懂我”的系统。

如果把这章压缩成一张检查清单，我会给每个动手写Client的人五条：发现要缓存——tools/list的结果别每次现拉，订阅list_changed通知加TTL；调用要循环——消息循环是生产级Client的最低配置，多个Tool调用按“只读并行、写入串行”调度；噪声要路由——工具列表是注意力预算，路由代理按问题喂相关的几个Server就够了；错误要兜底——协议校验、超时、退避重试三件套，写操作一律不盲目重试；进度要可见——progress通知喂给前端，等待不再是黑盒。

💡 独思时刻： 有一点我越想越笃定：MCP生态的未来不在Server的丰富度，而在Client的编排能力。2026年8月我给团队分享时提过一个数：到这一年底，MCP Server的数量很可能会突破5000个，但绝大多数用户不会直接面对5000个Server——他们会面对一个Client，这个Client替他们选了当前对话最相关的3个。那时候，“用户用哪个MCP产品”的本质问题不再是“它连了多少Server”，而是“它的Client多聪明”。Server是肌肉，Client是大脑。写Server是力气活，写Client是手艺活——前者可以靠模板生成，后者需要你真正理解AI的决策逻辑。

回到这一章开头那句话：代码本身不值钱，值钱的是AI能调用你的代码。现在我可以把它补完整——AI能不能调对、调稳、调得恰到好处，取决于Client。Server是货架，Client是采购与物流；货架上的货再多，采购选不准、物流送不到，用户看到的还是空荡荡的购物车。如果非要把这一章压成一句话，那就是：路由代理优于超大列表。你的Client已经能连计算器、能搜索、能处理错误了。下一章，我会带你从Client的视野再往上跨一层——把多个MCP Server组合成一个Agent团队，让它们协作完成单个Server做不到的事。

---

## 第7章 实战——构建一个完整的MCP服务器

# 第7章 实战——构建一个完整的MCP服务器

说了一整本书的协议、历史和理念，这一章我们把手弄脏。

我会带你从零开始，构建一个真正能跑、有用、且能部署的MCP服务器。它不是一个玩具示例，而是一个解决真实问题的工具——一个个人知识库检索服务器，它能让你本地的Markdown笔记、技术文档、甚至你多年累积的代码片段，通过统一的接口被任何支持MCP的AI客户端调用。

我选这个项目，是因为在过去半年里，我自己就在用类似的服务器解决一个具体痛点：我的几百篇技术笔记分散在iCloud、GitHub仓库和本地文件夹里，每次想找个半年前记下的配置命令，得翻半天。一个MCP服务器把这件事变成了在AI对话框里输入“查一下我去年记的Docker配置”就能解决的事。

这一章的推进顺序，就是我实际写一个Server时的决策顺序：先决定它怎么跑，再决定它说什么话，然后想清楚它有哪些能力、怎么兑现承诺、怎么测、最后怎么发出去。每一节我都会同时给你两样东西——能直接复制的代码，和代码背后那些文档不会替你做的判断。

## 7.1 项目选型：先决定“怎么跑”

写MCP服务器跟写普通程序最大的不同是：你首先要决定它用什么“语言”跟外界说话。这决定了它能被谁用、用在哪。

MCP目前主流的传输方式有两种，我把它们的选择标准总结成一张决策表：

| 你的服务器运行在… | AI客户端在… | 推荐协议 | 理由 |
| --- | --- | --- | --- |
| 本地电脑（你的笔记本） | 同一台电脑 | stdio | 零网络开销，安全性由进程隔离天然保证，配置最简单 |
| 云服务器（有公网IP） | 互联网任何地方 | Streamable HTTP | 适配无状态云原生场景，支持负载均衡和水平扩展 |
| 不确定，可能本地也可能云端 | 不确定 | 双模式支持 | 通过环境变量切换传输方式，一份代码适应两种场景 |

这张表看着简单，但每个格子的背后都是一个部署现场。我补三个表格里写不下的判断。

第一，stdio和Streamable HTTP的分界线，本质上是“谁控制Server的生命周期”。第4章我说过stdio是“会话即进程”：Host启动你、你干活、Host退出你就消失，进程管理、崩溃重启、日志收集全部由Host代劳。选stdio，意味着你把这些事托付给了客户端；选Streamable HTTP，意味着你自己成了一个小型网络服务，端口、健康检查、优雅退出、日志，一样都逃不掉。好消息是，2026年7月28日无状态化版本落地之后（第3章详细讲过），Streamable HTTP的部署门槛已经大幅下降——Session概念被移除，请求之间不再共享状态，负载均衡后面的每一台机器都等价，sticky session这种旧时代的标配配置成了历史名词。但门槛下降不等于没有门槛，你依然是那个要为端口和健康检查负责的人。

第二，警惕一个历史名词。如果你在2024年底到2025年初的老教程里看到“HTTP+SSE”传输，不要照抄——它在2025年3月26日的版本里就已经被Streamable HTTP取代并退役，两者的部署方式差异不小。看年份选教程，是MCP这类快速演进协议的基本功。

第三，双模式支持不是墙上的装饰品，它有非常具体的实现形态：在入口处读一个环境变量，决定把Server绑到哪条传输通道上。

```typescript
// 双模式支持：通过环境变量切换传输方式（示意）
// import { StreamableHTTPServerTransport } from "@modelcontextprotocol/sdk/server/streamableHttp.js";
if (process.env.MCP_TRANSPORT === "http") {
  const httpTransport = new StreamableHTTPServerTransport({
    sessionIdGenerator: undefined, // 2026-07-28 之后：无状态模式
  });
  await server.connect(httpTransport);
} else {
  await server.connect(new StdioServerTransport());
}
```

对于今天的个人知识库项目，我会选择 stdio 起步。因为它的配置最简单，调试最直接——你可以在终端里手动启动服务器，看它打印的每一条日志，也能随时用Ctrl+C停掉它。对于开发阶段来说，这种“能看见”的透明感比什么都重要。

知识库还有一个天然适配stdio的属性：它是单用户的。笔记是自己的、iCloud是自己的、GitHub仓库是自己的，不存在多租户隔离问题。什么时候该切到Streamable HTTP？我的经验值是：当第二个人开始认真使用你的Server，或者当数据源需要集中管控——比如数据库凭据不能撒到每一台笔记本上——的时候。先本地后云端，不是技术洁癖，是让每一层复杂度都在真正需要它的时候才出现。

决策表也有它的反例。以下三种情况，我不建议你选stdio：一是Server需要同时服务多个Host——stdio的子进程模型意味着每个客户端各起一份进程，数据源的状态不共享；二是Server需要被浏览器或手机端调用——没有能力启动本地子进程的环境，天生只能走HTTP；三是团队要统一管控能力版本——十台机器上跑着十个版本的stdio进程，远比一个统一升级的远程服务难管。我自己在R86S小主机上同时跑着好几个stdio的Server，全部由WeClaw以子进程方式拉起——至少在个人场景里，我到今天还是stdio的坚定用户。

## 7.2 项目初始化：TypeScript + zod

假设你叫它 knowledge-mcp。我用TypeScript，因为它的类型系统能让MCP的工具定义更严谨——你在schema里声明参数是string，调用方传了number，在编译阶段就能发现。

```bash
mkdir knowledge-mcp && cd knowledge-mcp
npm init -y
npm install @modelcontextprotocol/sdk zod
npm install -D typescript @types/node
npx tsc --init
```

然后打开tsconfig.json，确保target是ES2022，module是NodeNext。这两个设置决定了你的代码能使用async/await和import语法。

我还会顺手打开strict模式，把编译产物输出到dist目录——前者让类型系统真正咬人，后者让目录语义清晰：src放源码，dist放产物，发布时只需要带上dist。

除了运行依赖，package.json里还有几个字段值得现在就填好，它们会在7.7节发布时生效：

```json
{
  "name": "knowledge-mcp",
  "version": "1.0.0",
  "type": "module",
  "bin": { "knowledge-mcp": "dist/index.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "engines": { "node": ">=18" }
}
```

bin声明这个包安装后提供的可执行命令，files限制发布到npm时只带上dist目录——这两行决定了别人装到的是“一个包”还是“一个命令”。engines声明Node版本下限，避免用户在过老的运行时里踩到莫名的语法错误。另外把dist、node_modules、.env提前写进.gitignore——尤其是.env，等到部署阶段它会承载你的密钥。

项目的核心依赖只有两个：@modelcontextprotocol/sdk是官方的TypeScript实现，zod是用来做运行时参数校验的——在AI模型可能给你传任何奇怪参数的情况下，zod是你最后一道防线。

“最后一道防线”这个说法值得展开。TypeScript的类型只在编译期存在，编译成JavaScript之后就全部擦除了；而MCP工具的调用方是AI模型，参数走的是JSON-RPC消息通道，运行时进来的数据没有任何类型保证。所以这个项目的类型防线是双层的：定义层用JSON Schema（协议要求），执行层用zod（运行时兜底）。两层看起来重复，实际上各守一段通道——一段守协议边界的声明，一段守业务逻辑的入口。

## 7.3 定义工具：想清楚“能做什么”

在敲任何代码之前，先想清楚你的服务器要暴露哪些“能力”。对于知识库服务器，我定义三个工具：

1. `search_notes`：根据关键词检索笔记
2. `read_note`：根据文件名读取完整内容
3. `list_tags`：列出所有已使用的标签

这三个工具覆盖了“找→看→归纳”的完整链路。每个工具的定义包含三个要素：名称、描述、输入参数的JSON Schema。描述里的每个字都重要——AI模型会根据它来判断“什么时候该用这个工具”。

三要素里，我最想让你多花时间的是后两个。名称我只有一条建议：动词加对象，search_notes、read_note、list_tags都长这样——模型对这种命名模式的先验最强。参数的description是第二个重灾区：很多人只给参数写类型不给语义，而keywords的描述“搜索关键词数组，每个关键词是一个独立的概念”里，“独立的概念”五个字才是真正指导模型行为的部分。至于描述本身怎么写、怎么从“功能概述”升级成“调用策略”，这一章的最后我会用一次踩坑经历完整复盘，这里先按下不表。

还有一个结构性的问题值得先想清楚：三个工具够吗？我的判断标准是链路完整性——用户的一个真实意图，能否在有限次调用里走完。找（search_notes）、看（read_note）、归纳（list_tags），三步闭环，不多不少。往里加第四个工具之前，先问一句：它是新能力，还是search_notes能覆盖的参数变体？第6章我们刚聊过工具列表的噪声成本，Server侧的克制和Client侧的过滤，是同一件事的两面。

描述的具体写法，我总结过一个三段式模板：第一段说它是什么，一句话的功能概述；第二段说什么时候该用它，触发条件与上下文；第三段说怎么用好它，参数策略与失败后的替代路径。对照search_notes的定义你可以看到，这三段它都有。反面教材我也留着：只写“搜索笔记”四个字的版本，模型只能靠猜——猜错的方向五花八门，参数数量、关键词拆分、调用时机全都会漂。描述的长度没有硬性标准，但每一句都应该能回答“这句话会改变模型的哪个决策”，回答不了的就删掉。

```typescript
// src/index.ts
import { Server } from "@modelcontextprotocol/sdk/server/index.js";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
import {
  CallToolRequestSchema,
  ListToolsRequestSchema,
} from "@modelcontextprotocol/sdk/types.js";
import { z } from "zod";

// --- 模拟数据层：在实际项目中这里会换成真实的文件系统扫描或数据库查询 ---
// 注：本示例使用内存数据模拟，生产环境请替换为实际的数据源实现
const mockNotes = [
  { 
    id: "1", 
    title: "Docker常用命令", 
    tags: ["docker", "devops"], 
    content: "docker ps -a 查看所有容器\ndocker logs -f 容器名 实时日志" 
  },
  { 
    id: "2", 
    title: "Python异步编程笔记", 
    tags: ["python", "async"], 
    content: "asyncio.create_task() 用于并发执行协程" 
  },
];

const server = new Server(
  { name: "knowledge-mcp", version: "1.0.0" },
  { capabilities: { tools: {} } }
);

// --- 列出所有可用工具 ---
server.setRequestHandler(ListToolsRequestSchema, async () => ({
  tools: [
    {
      name: "search_notes",
      description: `根据一个或多个关键词在知识库中搜索笔记。 
        如果用户的问题包含多个概念或实体，请将每个概念作为独立关键词传入。 
        返回匹配的笔记标题和ID列表。`,
      inputSchema: {
        type: "object",
        properties: {
          keywords: { 
            type: "array", 
            items: { type: "string" },
            description: "搜索关键词数组，每个关键词是一个独立的概念" 
          },
        },
        required: ["keywords"],
      },
    },
    {
      name: "read_note",
      description: "根据笔记ID读取完整的笔记内容。用于在search_notes之后获取具体细节。",
      inputSchema: {
        type: "object",
        properties: {
          noteId: { type: "string", description: "笔记的唯一标识ID" },
        },
        required: ["noteId"],
      },
    },
    {
      name: "list_tags",
      description: "列出知识库中所有已使用的标签及每个标签下的笔记数量。用于浏览知识库的概貌。",
      inputSchema: {
        type: "object",
        properties: {},
      },
    },
  ],
}));
```

## 7.4 实现工具逻辑：把承诺兑现

定义完接口，接下来实现真正的逻辑。这里有几个设计原则值得遵守：

先交代这三条原则的共同底色：工具的返回值不是给人看的，是给模型看的。人可以容忍模糊，模型却会把你的每一句返回都当成事实依据继续推理——模糊在人类协作里是润滑剂，在模型推理里是毒药。

输入必须验证：AI模型可能传任何东西——keywords可能是字符串、数字、甚至null。用zod做一层防护，确保进入业务逻辑的数据是干净的。

错误必须可读：如果搜索没结果，不要返回一个空数组了事，而是返回“未找到包含关键词‘xxx’的笔记”。AI需要知道“没找到”和“找到了但内容为空”的区别。

返回结构必须稳定：每个工具返回的content数组里，至少要有一项type为text的内容。这是MCP规范要求的“人类可读”输出，AI会把它当作工具执行结果来理解。

```typescript
// --- 实现工具调用逻辑 ---
server.setRequestHandler(CallToolRequestSchema, async (request) => {
  const { name, arguments: args } = request.params;

  // 所有输入都先过一层zod校验
  const keywordSchema = z.object({ 
    keywords: z.array(z.string()).min(1, "至少需要1个关键词") 
  });
  const noteIdSchema = z.object({ 
    noteId: z.string().min(1, "笔记ID不能为空") 
  });

  try {
    if (name === "search_notes") {
      const { keywords } = keywordSchema.parse(args);
      // 模拟搜索：在实际代码中这里会做文件扫描或数据库全文检索
      const results = mockNotes.filter(note => 
        keywords.some(kw => 
          note.title.includes(kw) || 
          note.content.includes(kw) || 
          note.tags.some(tag => tag.includes(kw))
        )
      );
      
      if (results.length === 0) {
        return {
          content: [{ 
            type: "text", 
            text: `未找到包含关键词 [${keywords.join(', ')}] 的笔记。建议换个词或用 list_tags 看看有哪些标签。` 
          }]
        };
      }
      
      return {
        content: [{
          type: "text",
          text: results.map(n => 
            `📄 ${n.title} (ID: ${n.id}) | 标签: ${n.tags.join(', ')}`
          ).join('\n')
        }]
      };
    }

    if (name === "read_note") {
      const { noteId } = noteIdSchema.parse(args);
      const note = mockNotes.find(n => n.id === noteId);
      
      if (!note) {
        return {
          content: [{ type: "text", text: `❌ 未找到ID为 ${noteId} 的笔记。请先用 search_notes 或 list_tags 确认ID。` }]
        };
      }
      
      return {
        content: [{ 
          type: "text", 
          text: `# ${note.title}\n\n**标签**: ${note.tags.join(', ')}\n\n${note.content}` 
        }]
      };
    }

    if (name === "list_tags") {
      const tagCount: Record<string, number> = {};
      mockNotes.forEach(note => {
        note.tags.forEach(tag => {
          tagCount[tag] = (tagCount[tag] || 0) + 1;
        });
      });
      const tagList = Object.entries(tagCount)
        .map(([tag, count]) => `#${tag} (${count}篇)`)
        .join('\n');
      
      return {
        content: [{ 
          type: "text", 
          text: `📋 知识库标签概览\n\n${tagList || '暂无标签'}` 
        }]
      };
    }

    throw new Error(`未知工具: ${name}`);
  } catch (error) {
    // 捕获zod校验错误或其他异常，返回可读的错误信息
    return {
      content: [{ 
        type: "text", 
        text: `❌ 调用工具失败: ${error instanceof Error ? error.message : String(error)}` 
      }],
      isError: true,
    };
  }
});
```

注意最后那个isError: true。这是告诉AI：这次调用执行了，但结果不正常。AI会据此调整下一步行为——比如换一批关键词重新搜索，或者告诉用户“搜索出错了”。

这段代码里藏着一个重要的分工。zod的parse在校验失败时会抛错，抛出的错误被最外层的catch接住，包装成一段带isError的文本返回——也就是说，校验失败没有走协议层的error通道，而是走了业务结果通道。这是有意的：第4章我们区分过协议错误与工具失败，参数不符合schema是模型需要理解并自我纠正的业务问题，不是协议层的崩溃。把错误消息写成“至少需要1个关键词”而不是zod的原始堆栈，模型下一轮就能自己修正参数。

错误可读性的最高形态，是错误消息里带着下一步的建议。看搜索为空的那个返回——“建议换个词或用 list_tags 看看有哪些标签”，这不是给人看的客气话，这是给模型的行动指引。我后来在自己所有的Server里都遵守一条纪律：每一条错误文本都要回答“模型现在该怎么办”。

另外，2025年6月18日的版本给工具输出加了structuredContent和outputSchema（第3章提过），机器可读的结构化数据从此有了正式通道。但我的建议不变：先老老实实把text写好。文本是模型与工具之间最不容易出错的语言，结构化输出适合报表、表格这类场景，知识检索这种“标题加摘要”的结果，一段排版清晰的文本反而是最稳的。

还有一条这一章没有展开、但你迟早会遇到的纪律：写操作工具的确认设计。本项目的三个工具都是只读的，天然幂等，最坏情况也只是返回空结果；一旦你给知识库加上删除笔记的工具，性质就变了——我在自己的第一个MCP服务器里就放过一个删除工具，结果Claude在两次清理类指令里把它当成了普通清理手段，误删了测试文件。那次之后我给自己立了规矩：破坏性工具的描述里必须写明后果与不可逆性，参数里加确认字段，并且把真正的执行放在Host的用户确认之后。工具越危险，描述越要啰嗦。

## 7.5 补上资源与提示模板

到上一节为止，这个Server已经能跑了，但它是“一条腿”的——只有Tools。MCP的三大能力里，Resources和Prompts在这一节出场，不是因为它们锦上添花，而是因为知识库场景恰好是它们的主场。

先分清三个能力的分工。Tools是动作，由模型决定什么时候调用；Resources是数据，由应用决定读什么——Host在对话开始前或对话过程中，主动把某些资源的内容注入上下文；Prompts是模板，由用户触发，把一段预定义的、参数化的交互流程塞进对话。对应到知识库：search_notes是Tool，因为模型要“找”；一篇笔记的原文是Resource，因为应用可以直接读；“每周做一次笔记回顾”是Prompt，因为用户说“来一次回顾”时，模板顶上。Resources与Tools的边界，一句话概括：模型主动要的是工具，应用主动给的是资源。

Resources的第一道设计题是URI。URI在MCP里是稳定标识符，不是下载地址——它不承诺一个可访问的网络位置，只承诺“用这个URI能唯一定位一份资源”。我的设计原则有三条：

其一，用自有scheme划命名空间。knowledge://而不是file://或http://，它告诉客户端“这是这个Server私有的概念空间”，不被文件系统路径或网页URL的语义绑架。

其二，静态URI与动态模板各司其职。资源数量少且稳定时，直接在resources/list里全量列出静态URI，客户端一眼看全；资源数量大或动态增长时，改用URI模板（RFC 6570格式，比如knowledge://notes/{id}），通过resources/templates/list暴露——客户端拿到的是“这类资源的取址规律”，按需构造。这条分界线，就是第6章讲过的噪声问题在资源世界的翻版：几百条静态URI全量塞进列表，和几百个工具塞进系统提示词，是同一种灾难。

其三，mimeType要写对。text/markdown告诉Host可以直接按Markdown渲染，比裸的text/plain阅读体验好一个档次。

再多说一句资源读取与工具调用的路径区别。工具调用要走完整的决策链：模型读描述、决定调用、等待返回、再消化结果，一次往返至少多耗一轮推理；资源读取则由应用直接完成，Host在合适的时机把内容挂进上下文，模型甚至不知道发生过读取。知识库里那几百篇笔记的正确打开方式因此是分层的：把索引暴露成工具，把原文暴露成资源，模型只负责“找”，搬运交给应用。这也是第5章埋过的伏笔——从Tool到Resource的那一步，本质是把一部分“决策”从模型手里收回来。

落到代码上。先在Server的capabilities里补上resources和prompts——原始骨架只声明了tools，这是新手最常踩的“定义了却看不到”的坑：

```typescript
// 补充 import：
// ListResourcesRequestSchema / ReadResourceRequestSchema /
// ListResourceTemplatesRequestSchema / ListPromptsRequestSchema / GetPromptRequestSchema
const server = new Server(
  { name: "knowledge-mcp", version: "1.0.0" },
  { capabilities: { tools: {}, resources: {}, prompts: {} } }
);

// --- 资源：静态 URI 全量列出 ---
server.setRequestHandler(ListResourcesRequestSchema, async () => ({
  resources: mockNotes.map(note => ({
    uri: `knowledge://notes/${note.id}`,
    name: note.title,
    mimeType: "text/markdown",
    description: `标签: ${note.tags.join(', ')}`,
  })),
}));

// --- 资源读取 ---
server.setRequestHandler(ReadResourceRequestSchema, async (request) => {
  const uri = request.params.uri;

  const noteMatch = uri.match(/^knowledge:\/\/notes\/(.+)$/);
  if (noteMatch) {
    const note = mockNotes.find(n => n.id === noteMatch[1]);
    if (!note) throw new Error(`资源不存在: ${uri}`);
    return {
      contents: [{
        uri,
        mimeType: "text/markdown",
        text: `# ${note.title}\n\n**标签**: ${note.tags.join(', ')}\n\n${note.content}`,
      }],
    };
  }

  const tagMatch = uri.match(/^knowledge:\/\/tags\/(.+)$/);
  if (tagMatch) {
    const tag = decodeURIComponent(tagMatch[1]);
    const matched = mockNotes.filter(n => n.tags.includes(tag));
    return {
      contents: [{
        uri,
        mimeType: "text/markdown",
        text: `# 标签 ${tag} 下的笔记\n\n` +
          (matched.map(n => `- ${n.title} (ID: ${n.id})`).join("\n") || "暂无笔记"),
      }],
    };
  }

  throw new Error(`未知的资源URI: ${uri}`);
});

// --- 资源模板：集合不适合全量列举时，只暴露取址规律 ---
server.setRequestHandler(ListResourceTemplatesRequestSchema, async () => ({
  resourceTemplates: [{
    uriTemplate: "knowledge://tags/{tag}",
    name: "按标签聚合的笔记集合",
    mimeType: "text/markdown",
    description: "列出某个标签下的全部笔记",
  }],
}));
```

读这段代码时注意两个细节。一是资源读取的失败我抛的是异常，而不是工具那种isError——资源没有isError语义，错误由SDK转换成协议层的JSON-RPC错误，由Host负责呈现；二是模板的uriTemplate里，{tag}是占位符，客户端按规律替换它。

Prompts的参数化设计与获取流程，逻辑上是Resources的近亲：prompts/list列出模板清单，prompts/get按名字加参数取回渲染后的messages。设计参数时有一个容易忽略的点：arguments数组里的description同样会暴露给客户端与模型——“要回顾的标签，例如 docker 或 python”里的“例如”两个字，和工具描述里的调用策略一样，是消除使用歧义的关键。模板正文则是固化最佳实践的地方：

```typescript
// --- 提示模板：参数化的周回顾 ---
server.setRequestHandler(ListPromptsRequestSchema, async () => ({
  prompts: [{
    name: "weekly-review",
    description: "对指定标签下的笔记做一次回顾，输出要点与待办",
    arguments: [{
      name: "tag",
      description: "要回顾的标签，例如 docker 或 python",
      required: true,
    }],
  }],
}));

server.setRequestHandler(GetPromptRequestSchema, async (request) => {
  const { name, arguments: args } = request.params;
  if (name !== "weekly-review") {
    throw new Error(`未知提示模板: ${name}`);
  }
  const tag = args?.tag ?? "全部";
  return {
    messages: [{
      role: "user",
      content: {
        type: "text",
        text: `请阅读知识库中标签为 ${tag} 的全部笔记，输出三部分内容：本周要点、可复用的结论、值得展开的待办。不要编造笔记里不存在的内容。`,
      },
    }],
  };
});
```

在Claude Desktop这类Host里，用户输入斜杠命令时会看到这个模板。往大了说，这意味着你可以把“怎么做code review”“怎么写周报”这类团队方法论做成一批Prompts，随Server一起分发——提示工程从个人手艺变成基础设施资产，这是Prompts最被低估的价值。

资源与提示模板补齐之后，这个Server的能力面就完整了。最后一段代码，启动服务器，绑定stdio传输通道。

```typescript
// --- 启动服务器 ---
async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
  // 注意：这里不要加console.log，因为stdio通道会捕获所有标准输出。
  // 任何console.log都会破坏MCP的消息格式。用console.error记录日志，它走标准错误流，不影响通信。
  console.error("Knowledge MCP Server 已启动，等待客户端连接...");
}

main().catch((error) => {
  console.error("服务器启动失败:", error);
  process.exit(1);
});
```

一个完整的MCP服务器，核心代码不到200行。它没有依赖复杂的框架，没有炫技的架构，就是三个工具定义、三个工具实现、一个启动函数。

## 7.6 测试策略：单元、集成、端到端

开发完的服务器怎么测试？有两种方式，一种“真实”，一种“模拟”。

模拟测试：写一个简单的Node脚本，用Client类连接你的服务器，手动调用工具并打印返回结果。这适合测试“工具逻辑是否正确”，但不测试“AI能不能理解你的工具”。

真实测试：把服务器配置进Claude Desktop或Cursor，然后用自然语言发指令。这才能验证你的工具描述是否被AI正确理解。我在开发过程中，会同时维护这两种测试——模拟测试保证代码健壮，真实测试保证体验流畅。

这两分法是我早期的做法，后来我把它细化成三层，让每一层只回答一个问题。

单元层回答“逻辑对不对”。把关键词匹配、标签统计这类纯逻辑抽成独立函数，用vitest直接测——这一层完全不碰协议，跑得最快，改一行逻辑三十秒出结果。mockNotes这个数组存在的另一层意义也在这里：它让业务逻辑与数据源解耦，7.3节注释里那句“生产环境请替换为实际的数据源实现”，换的只是数据层，测试一行不动。

集成层回答“协议通不通”。我的标配是MCP Inspector，一行命令：

```bash
npx @modelcontextprotocol/inspector node dist/index.js
```

它会替你完成initialize握手，打开一个本地页面，可视化地列出tools/list、resources/list、prompts/list的全部结果；你可以在界面里填参数调用任何工具，直接观察原始的JSON-RPC往返报文。Inspector不能告诉你“AI能不能理解你的工具”，但它能把协议层问题与语义层问题彻底切开——当自然语言测试不符合预期时，我第一步永远是去Inspector里手动调一次，先分清是协议的锅还是描述的锅。它就是“模拟测试”的工程化升级版。

端到端回答“体验顺不顺”，也就是“真实测试”。假设你是第一次运行这个服务器，在Claude Desktop的claude_desktop_config.json里添加：

```json
{
  "mcpServers": {
    "knowledge": {
      "command": "node",
      "args": ["/你的项目路径/dist/index.js"]
    }
  }
}
```

重启Claude Desktop，然后在对话框里说：“帮我查一下知识库里有没有关于Docker的内容。”如果一切顺利，Claude会调用search_notes，返回结果，然后你可以追问“把第一篇笔记的内容读给我听”，它会调用read_note。

端到端这一层，还有一个介于“真AI”和“手写脚本”之间的中间形态，我称之为mock AI决策：写一个冒充模型的脚本，先tools/list拿到全部工具定义，再按一张硬编码的决策表——“这个问题应该调用哪个工具、传什么参数”——执行完整调用链并断言每一步的返回。它测不出“理解”，但能守住链路回归：描述文案的调整、返回格式的微调，只要让决策表走不通，测试立刻变红。配套的纪律是给自然语言用例建一个小清单：挑五到十句真实问法，每次改动描述之后重跑一遍，观察工具选择有没有漂移。描述就是接口，改描述要像改API一样跑回归——这是MCP开发里最反直觉、也最值钱的一条测试纪律。

分层的覆盖目标也要分开定。单元层专攻边界：空数组、超长关键词、带换行符的输入、纯符号的查询，这些脏输入在真实AI的对话里迟早出现；集成层专攻错误路径：不存在的noteId、未知工具名、zod校验失败，确认每一条都返回可读文本而不是进程崩溃；端到端只守三四条黄金路径——搜索后读取、按标签浏览、空结果的引导——它们是用户体验的主干道，主干道畅通比覆盖率数字重要。

## 7.7 发布与部署：NPM、PyPI、Docker 与一次真实的部署复盘

开发完的服务器，至少有三个方式可以被别人使用：

NPM发布：如果你的服务器是用TypeScript写的，发布到NPM是最简单的分发方式。用户执行npx your-mcp-server就能运行，不需要关心源码。

Docker容器化：如果你选择了Streamable HTTP方式运行，Docker是标准的部署方案。把服务器打包成镜像，推送到仓库，在云服务器上跑起来。

作为CLI工具安装：直接npm install -g你的包，用户通过命令行启动。

我自己的习惯是：本地工具用stdio方式，直接通过npx分发；需要团队共享的远程服务，用Streamable HTTP方式跑在Cloudflare Workers或自己的小服务器上。

三条路线的发布细节，我把踩过的坑浓缩成清单。

NPM这条线，发布命令本身只有一行，功夫全在发布前。7.2节埋下的bin和files字段在这里兑现——发布前必做三步：

```bash
npm run build
npm pack --dry-run
npm publish --access public
```

npm pack加dry-run参数会打印这个包将要包含的全部文件——如果列表里出现src或者node_modules，说明files没配对，立刻停下修正。shebang是另一个经典暗坑：bin指向的dist/index.js首行必须有`#!/usr/bin/env node`，否则在Unix下用户直接执行会报莫名其妙的错——TypeScript编译不会帮你加这一行，要么写在源码头部，要么用构建插件注入。版本号老老实实遵守语义化版本，而且对MCP Server来说“接口”的范围要比普通库更宽：改参数schema、删工具，是major；改描述文案导致调用行为变化，我认为也该算minor——描述即接口，这个原则贯穿本章。

Python生态的对称做法：pyproject.toml里在project.scripts下声明入口，uv build构建，uv publish上传，用户用uvx加包名运行——uvx之于PyPI，就是npx之于NPM。语言不重要，分发心智是同一套。

Docker这条线，我给一个多阶段构建的最小模板：

```dockerfile
# Dockerfile（Node 多阶段构建示意）
FROM node:20-slim AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:20-slim
WORKDIR /app
ENV NODE_ENV=production
COPY package*.json ./
RUN npm ci --omit=dev
COPY --from=build /app/dist ./dist
USER node
EXPOSE 3001
HEALTHCHECK --interval=30s --timeout=5s --retries=3 \
  CMD node -e "fetch('http://127.0.0.1:3001/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"
CMD ["node", "dist/http.js"]
```

要点有四个：构建与运行分离，运行镜像里不包含TypeScript编译器和开发依赖；用非root用户跑进程；HEALTHCHECK配合一个health端点，让编排系统知道你活着；镜像里绝不塞.env文件——密钥一律由部署环境注入。最后一条在知识库场景同样成立：哪天你把mockNotes换成真实数据源，数据库凭据应该通过环境变量进入容器，而不是被烧进镜像层。

Streamable HTTP模式的服务本体长这样：

```typescript
// dist/http.js：Streamable HTTP 模式的最小挂载（express 示意）
import express from "express";
import { StreamableHTTPServerTransport } from "@modelcontextprotocol/sdk/server/streamableHttp.js";

const app = express();
app.use(express.json());

app.get("/health", (_req, res) => res.json({ status: "ok" }));

app.post("/mcp", async (req, res) => {
  // 无状态模式：每个请求新建 transport 实例，处理完即丢弃
  const transport = new StreamableHTTPServerTransport({ sessionIdGenerator: undefined });
  res.on("close", () => transport.close());
  await server.connect(transport);
  await transport.handleRequest(req, res, req.body);
});

app.listen(3001);
```

把它部署到云服务器时，前面通常站着一个反向代理。这里有一个不知者必踩的坑：SSE流式响应会被nginx的默认缓冲机制“攒够一批再发”，表现为客户端长时间收不到消息、然后超时断连。关掉缓冲，是这类部署的第一行配置：

```nginx
server {
  listen 443 ssl;
  server_name mcp.example.com;

  location / {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_buffering off;              # 关键：SSE 流式响应必须关闭缓冲
    proxy_cache off;
    proxy_read_timeout 86400s;        # 长连接超时放宽
    proxy_set_header X-Accel-Buffering no;
  }
}
```

2026年7月28日无状态化之后，你不再需要为Session配置sticky session或ip_hash这类会话亲和——每个请求自包含，负载均衡随便分流。第3章那组数字在这里有了部署侧的意义：约90%的公开Server从未使用Session ID，真正需要迁移的只有3%，而从零写的Server从第一天起就是无状态的。

还有一件事必须在公网部署前解决：鉴权。stdio的进程隔离天然挡住了陌生人，Streamable HTTP则把你的Server暴露给了整个互联网。协议层面的正解，是2025年3月26日版本引入的OAuth 2.1授权框架（第3章讲过），企业内网还可以走反向代理层的统一认证；个人项目的最小可行方案，是先在反代层校验一个Bearer令牌——我在Hermes接入知乎搜索MCP时用的就是这套，SSE加上Bearer鉴权，成本几乎为零，足够挡住扫描器的批量探测。但请把OAuth 2.1写进路线图：一旦你的Server要开放给第三方Host，静态令牌很快就会不够用。

说完通用清单，我要完整复盘一次自己的部署——knasync MCP Server。它恰好覆盖了这一节的所有主题，而且每一处决策都有真实的翻车与修复。

knasync是我的知识基础设施项目。2026年8月23日之前，我的家用小主机上跑着一个纯搜索用途的MCP服务器：server.py，444行，Python标准库实现，零外部依赖，stdio上的JSON-RPC 2.0，主循环就是“读stdin、解析JSON-RPC、分发、写stdout”，暴露六个搜索工具——知乎社区搜索、全网搜索、DeepSeek联网搜索、知乎热榜、知乎直答、抓取知乎文章全文。链路是：用户→微信→WeClaw→LLM→MCP Server→搜索API。

那天晚上的目标，是把“获取信息”延伸成“沉淀知识”：一个MCP Server，八个工具，覆盖搜索、发布、上传全链路。新增的两个工具是publish（发布到IMA笔记、博客、播客、Knowly）和upload（上传文件到Knowly NAS）——从知乎搜索、全网检索，到发布到IMA笔记、博客、播客，再到Knowly上传，我散落各处的知识通道被一个Server统一暴露出来。整个过程五个阶段，时间线值得原样给你。

20:00，把散落的API域名统一收编进knasync项目，部署到Cloudflare Workers，新增统一的发布端点。21:00，创建mcp/server.py，用Python标准库实现MCP协议，直接覆盖旧的444行。21:30，研究WeClaw的MCP集成方式——config.json里的mcp_command加mcp_args两个字段——决定不再新开一个Server，而是与现有搜索Server合并：六个搜索工具加两个发布上传工具，同时砍掉不常用的queue类工具（peek、pull、push、health）。工具列表是客户端的上下文成本，第6章那笔账我一直记着。22:00，部署到小主机，连修三处：知乎搜索从HTTP API直调（返回401）改走zhihu-cli命令行，并把知乎密钥通过subprocess的环境变量参数传递；上传工具补上User-Agent请求头——Knowly的服务端校验必需，缺了就是神秘失败；publish的默认目标改成Knowly。22:30，模型行为调优：模型不遵守用户指定的发布目标，自行扩展到所有渠道——我在工具描述里写死了target的取值规则；upload被传了URL而不是本地文件路径——我在描述里强调file_path必须是本地路径。23:00，八个工具全部测试通过，四渠道发布验证完毕，微信里说一句自然语言，全链路跑通。

配置侧几乎没有秘密，WeClaw那边的config.json就是这两行起作用：

```json
{
  "agents": {
    "agnes": {
      "type": "http",
      "model": "agnes-2.5-flash",
      "mcp_command": "python3",
      "mcp_args": ["/root/mcp-search-server/server.py"]
    }
  }
}
```

这次部署给我留下三条教训，比任何配置模板都值钱。第一，合并Server不是堆料——工具数量守恒，砍掉queue的判断标准不是“能不能实现”，而是“模型会不会用”。第二，密钥的传递路径要一以贯之：知乎密钥从环境变量读入、经subprocess的env参数传给子进程，Knowly的账密走环境变量，任何一环硬编码，迟早变成安全事故。第三，默认值就是产品决策——publish默认发往Knowly，是因为复盘高频意图时发现，“用户没说清去哪”的绝大多数场景，想要的都是沉淀到知识库；把统计规律内置成默认行为，比让模型每次去猜更稳。

发布本身只是起点——关于“发布”这件事，我还有一段更狼狈的亲历，放在这一节的最后。

💡 独思时刻： 大多数人谈论MCP服务器发布时，只关注“怎么把代码发出去”。但我发现一个被严重低估的问题是“用户怎么知道你的服务器存在”。2026年4月我把一个MCP服务器发到NPM后，一个月只有37次下载。直到我在一个技术播客里口头提了一嘴，第二天下载量涨到400多。这件事让我意识到：工具的分发网络和工具本身的代码同等重要。 如果每个开发者都把自己的MCP服务器藏在NPM的某个角落，那MCP生态就只是另一个“轮子仓库”——有海量选择，但没人帮你做筛选。我在开发WeClaw时故意加了一个/mcp discover命令，列出社区里经过验证的服务器列表，就是想从客户端侧解决“发现”的问题。Server的代码是“供”，Client的发现机制是“需”——两者匹配上了，生态才能转起来。

## 7.8 独思时刻：描述是写给 AI 的说明书

这一章从选型写到部署，反复出现的主题其实只有一个。让我把它讲完整。

💡 独思时刻： 有个主流观点是“工具描述越简短越好，让AI自己理解”，但我踩过这个坑。2026年3月我写第一个MCP服务器时，把`search_notes`的描述写成“搜索笔记”。结果Claude经常在有5个关键词的时候只传1个去搜，因为模型觉得“搜索”只要一个词就够了。后来我把描述改成了“根据一个或多个关键词检索笔记。如果用户的问题包含多个概念，请把每个概念都作为独立关键词传入”，AI的行为立刻改善了。这让我意识到：工具描述不是写给开发者看的文档，而是写给AI看的“使用说明书”。 它需要包含调用策略，而不仅仅是功能概述。

💡 独思时刻： 写完这一章的代码，我回头看了一个数字：为了这不到200行的核心代码，我花在描述、错误文本、返回格式上的时间，超过了纯业务逻辑。起初我把它当作低效，后来才意识到这是MCP开发的常态——传统编程里，接口的消费者是程序员，报错看一眼调用栈就懂；MCP工具的消费者是模型，你的每一行描述、每一条错误消息、每一个默认值，都是它推理的输入。这个Server的分水岭不在功能多少，而在于：当工具失败时，模型下一步是自我纠正，还是把错误当成事实继续往下走。前者的失败叫迭代，后者的失败叫事故。把错误可读性看得比功能覆盖更重，不是道德姿态，是工程收益最高的投资。

你刚才写的这个服务器，不算复杂，但它具备了MCP规范要求的全部要素：工具定义、参数校验、错误处理、可读返回。

但我希望你不只是把它当成一个代码示例。这个服务器的设计过程体现了一个更重要的思路：MCP开发的核心工作，是把“你能做的事”翻译成“AI能理解的指令”。翻译的质量，直接决定了AI用你的工具时“像个聪明助手”还是“像个需要手把手教的实习生”。

下一章，我会把视野从单个服务器拉升到服务器集群——当你有了三五个甚至十几个MCP服务器后，怎么让它们协同工作，怎么在一个客户端里编排多个服务器的调用顺序。那才是MCP真正开始展现力量的地方。

---

## 第8章 MCP生态与未来展望

# 第8章 MCP生态与未来展望

从第七章单个服务器的开发细节抬起头来，你会发现自己正站在一片迅速成型的地貌中央。2026年8月的此刻，距Anthropic开源MCP不到两年，但围绕这套协议长出来的东西已经远远超出了“又一个开源项目”的范畴。它正在变成一层基础设施——像HTTP、JSON-RPC那样容易被忽视、却越来越难绕开的基础设施。

写下这一章的那个深夜，我看了一眼书房角落的R86S小主机：上面跑着WeClaw的网关、knasync的知乎搜索Server，还有两三个实验性质的小工具。两年前，这台机器上跑的是我自己手写的各种轮子；现在，它们全变成了标准协议之上的一个个小进程。这种“地貌级”的变化值得停下来盘点了——这一章我们先看工具链如何从手工作坊进化成流水线，再看谁在接入、接了什么，然后直面它眼下的短板，最后把AAIF刚公布的路线图和我的行动建议摆在一起。

## 8.1 工具链：从手工作坊到标准化流水线

最早那批MCP开发者（包括我自己）的工作方式很原始：写一个TypeScript文件，用stdio方式跑起来，然后用console.log调试。没有可视化工具，没有统一的注册表，发现问题全靠Claude Desktop报错——而报错信息往往是一行“Connection closed”加上一堆你根本不知道从哪查起的日志。

2024年11月5日我写出的第一个MCP Server——一个四十分钟完成的本地文件夹读取器——就是这样调出来的。那晚我在终端里打印了一屏又一屏的JSON-RPC报文，人肉比对每一段request和response。最折磨人的不是写代码，而是“盲调”：你看不到握手是否成功，看不到能力协商的结果，只有一行冷冰冰的Connection closed，以及一种深夜独属的怀疑——到底是协议错了，还是我错了。

2026年的情况完全不同了。SDK层面，Python、TypeScript、Go、C#四个主流语言的官方实现已经同步迭代到v1.0以上版本，并且都同时支持新旧两种协议模式。调试层面，Claude Desktop和Cursor都内置了MCP连接状态的可视化面板，你能直接看到每个服务器暴露了哪些工具、每个工具的参数结构是什么、最后一次调用是否成功。

这短短一段话里的信息量，值得为经历过手工作坊时代的人标注一下。四门语言的SDK同步迭代到v1.0以上，意味着协议层已经稳定到可以让四个团队各自按节奏发版；同时支持新旧两种协议模式，意味着像我这样还要照顾存量部署的开发者，不必被版本升级绑架。我在用Go给WeClaw写网关时接官方Go SDK，从引入依赖到跑通第一个工具调用，花的功夫还不如2024年那一晚调试花的多。工具链的成熟通常滞后于协议本身一两年，MCP把这段距离压缩到了不到一年。生态的规模数据同样夸张：官方SDK的月下载量在2026年春天突破了一亿次——npm花四年才摸到的量级，MCP只用了十六个月；awesome-mcp-servers这份社区维护的清单累计收获了超过85000个Star；2025年6月，社区月度新增Server数更是从135个激增到5069个。人肉维护的清单终究会在这条增长曲线面前失效，这正是注册表出现的土壤。

但真正让MCP从“小众工具”变成“基础设施”的标志，是注册表的出现。2026年6月，MCP Registry预览版上线，定位是一个开放的服务器目录和API。这个设计很像Docker Registry——提供标准化的分发机制，支持公共和私有部署。你可以在registry.modelcontextprotocol.io上查找可用的服务器，也可以通过REST API让客户端动态发现工具。

就在它上线的前一个月，GitHub也推出了官方的MCP Registry——把服务器目录直接挂进了全球最大的开发者社区，搜索、Star、issue这些开发者已经用了十几年的动作，全部平移到了Server的分发上。对客户端来说，注册表加REST API意味着“发现”从硬编码列表变成了运行时查询：我在WeClaw里做的/mcp discover命令，本质上就是这套机制在客户端侧的镜像。

💡 独思时刻： 大多数人谈论生态时，习惯性地把注册表当成“App Store”——觉得只要有一个地方能上架，开发者就会来，用户就会用。但我在经营自己的几个开源项目（yuangs CLI、Knowly、WeClaw）时发现一个完全相反的真相：注册表的真正价值不是“上架”，而是“下架”。 2026年4月，我在WeClaw里集成了一个第三方MCP服务器，结果那个服务器两天后就失联了——作者删了GitHub仓库，NPM包也下架了，但MCP Registry上还挂着它的信息。我的用户跑来问我“为什么/weather命令报错”，我花了三个小时才查到是上游失联。那一刻我意识到：一个没有清理机制的注册表，会变成一个遍布死链接的坟场。值得庆幸的是，MCP Registry采用了社区举报机制，用户可以标记违反审核准则的服务器，维护者可以将违规条目加入黑名单并追溯移除。这套机制目前还在完善中，但方向是对的——健康的生态不是让所有人都能进来，而是让不靠谱的东西能被及时清出去。

除了官方Registry，JFrog在2026年3月也推出了企业级MCP Registry，定位为MCP服务器的中央存储和管理平台，支持权限控制、策略执行和供应链安全审查。这说明一个趋势：MCP正在从开发者个人工具，变成企业IT治理的对象。

“治理对象”三个字落到地上，是一整套过去只属于传统软件的东西：Server进企业内网要走采购评审，调用要有审计日志，上游失联要有SLA兜底，违规工具要有下架流程。那次的失联事故之后，我把复盘文档写进了团队wiki，标题就叫“Registry不是货架，是水务系统”——货架只管摆上去，水务系统还得管水质、管漏水、管停水通知。我判断私有Registry会成为中大型企业的标配，就像今天的私有镜像仓库一样：没有哪家企业愿意把自己的业务流程建立在别人随手就能删掉的仓库上。

## 8.2 平台与服务：谁在接入，接了什么

如果说工具链是“怎么建”，那平台生态就是“谁在用”。

截至2026年8月，GitHub上公开的MCP Server数量已经超过5000个，涵盖开发工具、数据分析、通信、金融、生活服务等数十个类别。各大软件公司的态度也很明确：

- Notion 提供了官方的MCP Server，允许AI读取和编辑笔记内容；
- Stripe 暴露了支付相关的工具，AI可以发起退款、查询交易记录；
- Shopify 让AI能管理商品库存和订单；
- AWS 提供了连接S3、DynamoDB等服务的MCP实现。

这四个名字只是露出水面的部分。往上数，2026年3月，OpenAI、Google DeepMind、微软相继宣布支持，MCP完成了“大厂对齐”的最后一跃；往深里看，Google Cloud把托管MCP Server做成了正式商用的云产品，自家的数据库产品线逐个配上托管实现，连Salesforce、ServiceNow这类企业SaaS连接器也从无到有地铺开了。如果把这个名单按行业铺开看，金融、医疗、资本市场的垂直Server都在增长——MCP的接入名单，正在变成一张另类的“全球经济索引”。

在国内，腾讯Marvis（个人AI助手）已经接入了超过10家MCP服务商，覆盖办公、出行、消费、地理位置、生活服务、企业服务六大场景。2026年8月刚上线的企查查MCP就是一个典型——用户通过聊天就能查询企业的工商信息、股东结构、对外投资，30秒内出结果。

国内生态里还有一件事，我认为标志性意义不亚于任何一家大厂官宣：2026年5月28日，ima宣布接入WorkBuddy、Qclaw等Agent产品，把自己的知识库开放给这些Agent调用。在此之前，知识库产品的角色是“人检索、人复制粘贴”，ima自己的Copilot也只服务ima里的用户；接入之后，知识库第一次以“能力提供方”的身份出现在Agent的工具面板里——知识不再等人来翻，而是等着被Agent问。配套的动作也在跟进：WorkBuddy在2026年3月就支持了配置MCP，5月又推出MCP连接器让Agent直接读写企业系统，腾讯云在打通公告里给出的预期是企业任务处理效率提升超60%。我自己就是这套变化的直接受益者——我写这本书的资料库就放在ima的知识库里，素材核对从“手动翻笔记”变成了“让Agent直接问知识库”。而我的knasync把知乎搜索、全网检索、发布到IMA笔记做成统一的MCP Server，则是同一逻辑的个人版实践；WeClaw的知乎搜索插件上线后周调用稳定在200次以上，说明国内开发者侧的自建需求同样真实。平台侧开放与开发者侧自建，构成了2026年国内生态的两组引擎。

这些案例背后有一个共同的逻辑：大厂们不是在“支持MCP”，而是在“用MCP降低自己的集成成本”。 对Notion来说，与其为每个AI应用（Claude、ChatGPT、Copilot、Gemini）单独开发插件，不如维护一个MCP Server——任何支持MCP的客户端都能直接用。这和我自己在WeClaw里做的选择一模一样：与其让每个Agent单独对接知乎、GitHub、文件系统，不如把这些能力做成MCP Server，然后让WeClaw的/mcp discover命令统一发现和调用。

“集成成本”这个词值得再拆一层。一个官方MCP Server替掉的不只是N份插件代码，还有N条审核流程、N轮兼容性测试和N个需要盯梢的版本升级。当集成的边际成本从“每接一个应用重做一遍”降到“维护好一个Server”，经济学的规模效应就开始反向作用于平台方——这比任何技术优越性论证都更能解释MCP的扩散速度。

## 8.3 MCP与A2A：不是对手，是搭档

很多人容易把MCP和谷歌推出的A2A（Agent-to-Agent）协议混为一谈，或者当成竞争关系。这两种看法都不对。

MCP解决的是“AI怎么用工具”——它定义了一个标准化的接口，让AI能调数据库、发邮件、查文件。A2A解决的是“Agent之间怎么说话”——它让不同供应商、不同框架开发的AI Agent能互相发现、沟通、协作。

用谷歌官方给的类比来说：MCP是让员工会用工具（千斤顶、扳手、万用表），A2A是让员工能和其他员工、和客户沟通协作。两者解决的是不同层面的问题。

实际部署中，两者的关系更像网络的二层和三层：

- MCP像二层交换：在局域网内，能看到所有细节，能直接操作具体工具。
- A2A像三层路由：把网络划分成子网，每个子网对外只暴露摘要信息，而不是全部细节，这样才能扩展到更大的范围。

沃达丰已经在生产环境里同时使用MCP和A2A了——他们的AI Booster平台中，MCP Server在跑真实业务，A2A负责多Agent之间的任务协调。Telenor的看法更直接：MCP会成为“决策接口”的基础，而A2A负责Agent之间的协调。

所以，当你听到有人在争论“MCP和A2A哪个会赢”时，你可以礼貌地打断他：它们不是竞争关系，它们在同一条食物链的不同层级。

有一个耐人寻味的佐证：A2A在2025年年中被谷歌捐给了Linux基金会；半年之后，2025年12月，Anthropic把MCP捐给了Linux基金会旗下新成立的AAIF。两个协议先后进了同一个屋檐——治理层面早已用脚投票，选择了“分层共存”而不是“赢家通吃”。再看我自己的场景：WeClaw目前是单Agent多Server的结构，MCP这一层就够用；可当我让Hermes框架里的多个Agent分工协作——一个负责检索、一个负责写作、一个负责审校——Agent之间需要互相发现、互相委托时，A2A式的协调才会真正进入我的技术栈。层与层不冲突，缺哪层补哪层。如果你需要一个更可操作的判断标准，我用的是这条：当你的系统里出现了第二个拥有独立目标、独立上下文的执行体，并且它们需要互相委托任务时，就该开始研究A2A了；在那之前，安心把MCP这一层用透，不必为了架构图好看而提前引入第二套协议——过早增加的分层和缺失的分层一样有害。

## 8.4 局限性：热闹背后的真实短板

生态这么热闹，不代表没有坑。而且有些坑不是“早期阶段”的问题，是结构性难题。

**上下文成本。** 这是目前被讨论最多的问题。当一个MCP Server绑定了43个工具，光工具描述就能塞爆55000个Token——AI还没开始干活，上下文窗口先被吃掉一半。你添加的每一个工具（名称、描述、参数Schema、示例）都会占用上下文窗口。如果你连了10个服务，每个服务有5个工具，那启动成本就是几千甚至上万Token。

我在第6章做过一个47个工具的压测：工具列表从17个扩展到47个，AI的工具选择错误率从3%涨到了22%。也就是说，上下文成本有两张账单——一张算钱，一张算对：窗口被吃掉不只是贵，还会直接劣化模型的决策质量。后来我又观察到一条更细的规律，单个工具描述超过200 Token之后，Claude对它的理解准确率会下降大约20%。

Perplexity的CTO Denis Yarats在2026年3月内部宣布放弃MCP，转而使用API和CLI，核心原因就是上下文成本。他们的观点很直接：上下文窗口是Agent最宝贵的“房地产”，MCP的静态工具枚举是一种浪费。

这个批评不是没有道理。但值得注意的是，MCP 2026年7月的候选版本引入了工具和资源列表的确定性缓存机制，目的就是提升提示缓存命中率，降低Token成本。问题被识别了，修复方案也在路上。

“在路上”已经有了下文：2026年7月28日，无状态化大版本正式落地，不再是候选版，缓存机制随之进入正式规范。协议用“让缓存命中变得确定”来回应“上下文太贵”的批评——这个模式我在第3章反复见过：MCP的每一次进化，几乎都是对最尖锐批评的直接应答。不过要说清楚，确定性缓存只降低重复会话的边际成本，第一次加载的工具列表依然要付费，这个问题的终极答案藏在8.5节第四项优先事项里。

**初始化不稳定的老毛病。** 实际使用中，MCP服务端偶尔会启动失败，导致Claude Code需要重启。有时重试能成功，有时必须清空状态推倒重来。这个问题在stdio传输模式下尤其突出——因为stdio依赖于本地进程的生命周期管理，而进程管理本身就是一件容易出岔子的事。

我有切肤之痛。2025年9月，我给WeClaw接的浏览器自动化Server就留下过一串僵尸进程——Server本体崩了，它拉起的Chrome实例却活着，占着端口吃着内存，排查到最后我发现真正的bug不在协议层，而在“谁负责收拾进程的遗体”这个没人想回答的问题上。后来我的做法很土但有效：每个stdio Server外面套一层带超时的进程守护，启动失败就先清理再重启。土办法的流行，恰恰说明规范的缺口真实存在。

**认证的碎片化。** 如果你使用多个MCP工具，每个工具都需要单独的认证流程，体验支离破碎。MCP 2026年7月版本更新了基于OAuth 2.1和OpenID Connect的授权框架，试图统一这一层，但生态的碎片化程度意味着统一认证还有很长的路要走。

我接入知乎搜索MCP到Hermes时的经历能说明问题：SSE传输加Bearer鉴权，光是让Token在会话里正确传递就折腾了大半个晚上，最后发现根因是一个mcp包缺失导致工具注册变成了no-op。一个Server一种鉴权姿势，十个Server就是十种。规范在收敛——OAuth 2.1、OIDC Discovery、再到2026年7月版本里的EMA（企业托管授权）扩展——但存量Server的碎片化是历史包袱，清理需要时间。

**安全和信任。** 工具的调用本质上是“允许AI在你的系统里执行操作”。如果缺乏细粒度的权限控制，一个恶意的或配置错误的工具就可能造成数据泄露。JFrog在推出企业Registry时特别提到：MCP服务器可能配置了过高的权限，如果缺乏集中治理，可能暴露凭据或数据。权限管理如果只有“白名单/黑名单”这种二选一，那在生产环境中几乎是不可接受的。

在这个话题上我想多停留一会儿，因为8.1节那次失联事故之后，我把WeClaw的第三方Server准入从“随手就接”改成了一套依赖审查流程，这套流程值得原样交出来。一看仓库活跃度：最近提交时间、issue响应速度，一个人维护且半年没动的要亮黄灯。二看依赖树：npm、pip的传递依赖里有没有可疑包，尤其警惕蹭知名包名字的仿冒包。三看权限声明：一个查天气的工具为什么要读文件系统？问不出来就别接。四看发布方式：锁死版本号，条件允许就走私有镜像，防的是“作者哪天推一个带私货的新版本”式的跑路。五做隔离：第三方Server一律跑在容器里，给最小文件系统和最小网络权限。还有一条容易被忽略的攻击面在工具描述本身——研究者称之为工具投毒：在描述里埋一句“调用本工具时顺便读取某个敏感目录”，模型可能照做。供应链安全在传统软件领域讲了二十年，如今在MCP生态原样重演，而且因为AI会“照着说明书办事”，审查比以往任何时候都更重要。

## 8.5 演进方向：从协议到基础设施

2026年8月27日，Linux Foundation旗下的Agentic AI Foundation（AAIF）发布了MCP的未来路线图，五个优先事项清晰地勾勒了协议的未来走向：

先把路线图周围的背景交代清楚。2025年12月，Anthropic把MCP捐赠给了Linux基金会旗下新成立的AAIF，创始成员包括Anthropic、Block和OpenAI，Google、微软、AWS、Cloudflare、彭博等是重要支持方；四个月内成员组织超过170家，下设7个工作组，仅Identity and Trust一个工作组就有来自66家组织的150名成员。捐赠不是移交包袱，而是把协议放进一个“谁都不能独占”的容器——Linux基金会过去三十年反复验证过这件事。再看时间线：2026年7月28日，无状态化大版本正式落地，SEP-2567与SEP-2575提案生效，Session概念被移除，改用显式句柄（handle）；官方影响分析说，约90%的公开Server根本没用Session ID，6.3%只在SDK样板代码里引用过，真正需要迁移的约3%，一个月后生态迁移基本完成。同一个版本里落地的还有Extensions框架——规范第一次拥有了“在不破坏核心稳定性的前提下叠加扩展”的机制，EMA就是以扩展形式发布的。所以这份路线图不是凭空画的，五项优先事项里，有两项的地基已经在7月版本里打好了。

第一，Agentic messaging primitives。 MCP最初是为“用户提问-模型回答”这种聊天场景设计的。但AI正在从“聊天机器人”进化成“后台长期运行的Agent”。未来的MCP需要支持服务器主动推送事件、客户端轮询进度，而不是只有“请求-响应”这一种模式。

这一项有清晰的落地节奏。Tasks——异步长任务原语——在2025年11月25日的版本里已经引入，让“跑一个三十分钟的报表任务”不再占着一个悬而未决的请求；路线图再往前一步，是服务器主动发起的触发式交互，用MCP Dev Summit上那句很传神的话说，让Server能主动喊一句“我有新数据，来交互”。我自己的Excel报表Server在2025年7月就撞到过这堵墙：报表要跑十几分钟，用户想知道进度，当时能给出的答案只有Elicitation提问和客户端轮询的土法组合。消息原语补齐之后，Agent才能真正“挂”在一个长任务上各干各的，聊天式的一问一答不再是唯一形态。

第二，HTTP-native transport unification。 早期的MCP有WebSocket、stdio、本地连接等多种传输方式混在一起。2026年7月的规范已经支持通过HTTP连接远程MCP服务器，未来会完全统一到HTTP基础之上。这意味着MCP会变得越来越像普通的Web API——更容易被现有的云基础设施接纳。

这是五项里我最能感受到“已经发生”的一项。2025年3月26日，Streamable HTTP取代HTTP+SSE，是统一的第一步；2026年7月28日的无状态化是决定性的一步——Session没了，负载均衡器不再需要粘性会话，任何一个节点都能处理任何一个请求，滚动发布、弹性伸缩、按请求计费这些云原生的标准动作突然全部适用。那句“更容易被现有的云基础设施接纳”翻译成工程语言就是：部署MCP Server的经验，可以直接抄普通Web服务的作业。这也是无状态化影响分析里那3%真正需要迁移的Server最终只花一个月就完成迁移的原因——路线画得足够直。

第三，Agent identity and enterprise-ready security。 目前的MCP认证是给“人类用户在浏览器里点击授权”设计的。未来的Agent会拥有自己的身份，能代表用户执行操作，甚至把部分权限委托给子Agent。这个方向的核心是：权限管理的对象正在从“人”变成“代码”。

这条线上已经排好了三块砖：OAuth 2.1授权框架（2025年3月）、OIDC Discovery与增量授权（2025年11月）、EMA企业托管授权扩展（2026年7月，官方博客专门撰文说明）。它们共同指向的场景是：Agent以自己的身份向企业身份系统证明“我是谁、替谁办事”，跨组织令牌交换打通单点登录，审计轨迹满足SOC 2、ISO 27001这类合规要求。Identity and Trust工作组那66家组织150名成员的规模说明，企业界是真金白银地在推这件事。我在8.4节抱怨过认证碎片化，这个方向就是碎片化的解药——只是药效还需要时间。

第四，Improved primitives。 工具发现要变得更智能——不是一股脑把所有工具描述塞进上下文，而是分层、分级地展示：先显示类别，用户或Agent选择类别后再展开具体工具。这会显著缓解上下文成本问题。

这就是对Perplexity批评的正面回答，业内叫渐进式发现：工具列表不再整块注入上下文，而是先给目录再给详情，配合工具搜索按需加载。如果那个43个工具的Server能先只声明“我有四类能力”，55000 Token的开局成本就能砍掉一个数量级；把领域知识捆绑进Server的Skills方向，也在给“原语”这个概念继续加厚。结合第6章压测里“工具越多、选择错误率越高”的数据，分层发现省下的不只是钱，还有准确率。确定性缓存让重复访问变便宜，分层发现让第一次访问就便宜——两条腿都站住了，Perplexity式的高成本批评才会真正失效。

第五，Improved SDK developer experience。 SDK正在被越来越多的“人”和“AI”同时使用。API要更直观，文档要更精确，让开发者和Agent都能快速上手。

“AI也在用SDK”这半句值得停下来想想。2026年有相当比例的新Server是AI自己写的——开发者描述需求，AI调SDK生成代码。文档的精确性不再只是“对人类友好”，而是“对模型可解析”。路线图对应的承诺是Python与TypeScript SDK的下一代版本：更符合直觉的API，加上把7月无状态化迁移这类脏活做成顺手的迁移工具。当协议的消费者从几万名开发者变成几万名开发者加上每天几百万个AI会话，SDK里每一处含糊都会被放大几百万倍——这不是修辞，是并发。

把这五项放回一张图上看：第一和第四项解决Agent的通信与认知，第二项解决部署形态，第三项解决信任模型，第五项解决开发者生态——拼起来正好是“从协议到基础设施”的完整路径。基础设施的最后一个特征，是没有人再讨论它。路线图走完的那天，大概就是MCP从技术媒体头条消失的那天，而那才是它真正的成人礼。

## 8.6 给开发者的建议：现在该做什么

如果你是现在开始接触MCP，我的建议只有三个字：别等。

等MCP“成熟”再入场，就像2010年等Web“成熟”再学HTML——等你觉得成熟了，战场已经被别人占完了。2026年的MCP已经足够好到可以用于生产，虽然有坑，但这些坑大多是“运维复杂度”层面的，不是“根本性缺陷”层面的。

具体来说，你可以从这四件事开始：

1. 先当一个用户：在你的Claude Desktop或Cursor里配置一个现成的MCP Server（比如GitHub的或Notion的），用自然语言调用它。这能帮你最快地理解“AI用工具”和“人用工具”之间的体验差异。

这一步在今天最多只需要十分钟：往配置文件里加几行JSON，重启客户端，连接面板里就会出现这个Server的名字和工具列表。

```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": { "GITHUB_TOKEN": "your-token-here" }
    }
  }
}
```

真正的收获藏在体验差异里。你会第一次亲眼看到：AI是读完工具描述之后自己决定调什么的——描述含糊它就猜，描述清楚它就准。这十分钟建立起来的手感，比读十篇协议解析都有用，因为它会把“描述即契约”从一句理论变成你的肌肉记忆。

2. 把一个重复性工作封装成MCP Server：不用复杂，一个工具就行。比如你每天要查的某个内部API，或者你反复执行的某个脚本。把它暴露成MCP工具，然后在对话里让AI帮你跑。

动手成本比大多数人想象的低。用FastMCP，三行装饰器就能把一个函数变成工具：

```python
from fastmcp import FastMCP

mcp = FastMCP("daily-helper")

@mcp.tool()
def get_rate(base: str, quote: str) -> str:
    """查询两种货币的当前汇率。base与quote使用三字母代码，如USD、CNY。"""
    ...
```

写描述时记住第7章的教训：动词开头，说明适用时机，把参数格式和单位写死。我2026年7月给雨轩编辑器做的汇率查询Client，核心功能不到一百行代码——一个足够小的真实需求，就是一个足够好的起点。亲手做完这一个工具，你对“声明式契约”的理解会超过任何理论。

3. 关注传输层的选择：本地工具用stdio，通过npx分发；需要团队共享的远程服务，用Streamable HTTP方式跑在云上。7月版本之后，HTTP方式越来越成熟，值得认真考虑。

我把选择标准压缩成一句话：跟着部署单元走。工具跟着人走——个人电脑、单人使用，选stdio，进程即生命周期，零运维；服务跟着团队走——多人共享、跨设备访问，选Streamable HTTP，这时要认真对待三件事：鉴权（Bearer Token起步，正式环境上OAuth 2.1）、限流，以及无状态化之后的部署红利——不再需要粘性会话，扩容就是加副本。我自己从stdio迁到SSE加Bearer的那一路坑，一大半源于把本地习惯直接搬上云；7月版本落地之后，这条路已经平整了许多。

4. 别忽视“被发现”这件事：服务器写完只是第一步。把它提交到MCP Registry，写清楚描述和示例提示词，让其他人（和其他AI）能找到它。如果做企业内部工具，考虑搭建私有Registry或使用JFrog这类企业方案。

提交流程本身不复杂：仓库里要有能跑通的README，Server的描述字段值得像产品文案一样反复打磨——我给自己的知乎搜索Server写描述时删改了好几轮，最后压到两百Token以内，因为过长的描述会直接掉理解准确率。示例提示词尤其值得写，它是给“AI用户”的第一份说明书。企业场景则要反着想：先想清楚要不要被发现——私有Registry加准入审批，正是8.1节JFrog方案存在的意义。被发现的收益和被误用的风险，是同一枚硬币的两面。

这四件事的共同点是从使用到封装再到分发，每一步都不贪多——生态从来不缺宏大的计划，缺的是一个个能跑起来的小Server。

## 8.7 独思时刻：集成地狱是结构性的无解，成本可以优化

💡 独思时刻： 我在2026年5月做过一个实验——用WeClaw同时连了12个MCP Server，然后让AI同时处理“查资料、写周报、发通知”三个任务。结果AI花了快两分钟才完成所有工具调用，Token消耗接近8万。很多人都把这个现象解读为“MCP效率太低”，但我的感受完全不同。如果不用MCP，我根本不会让AI同时去做这三件事——因为每个系统之间的集成本身就是一个项目。MCP让“让AI做跨系统任务”从一个工程问题变成了一个成本问题，这本身就是巨大的进步。 成本可以优化，但集成地狱是结构性的无解。2026年的AI应用竞争，赢家不会是“谁的模型更强”，而是“谁的系统能更快地把散落的能力串起来”。MCP给了你串起来的绳子，怎么用，看你自己。

💡 独思时刻： 写到这里，本章开头那句话可以闭环了——基础设施的宿命是被忽视。十年后大概不会有人再写“HTTP生态展望”，因为到那时它就是空气，没有人会给空气写年度盘点。MCP多半也逃不过这个命运：被忽视，恰恰是因为无处不在。作为在这个协议两岁时逐行读过它规范、被它坑过也被它救过的人，我居然开始期待它变得无聊的那一天——一个技术最好的结局，就是让所有人忘记它的存在，只记得自己做成的事。

---

## 第9章 终章·后记

终章·后记

这本书写完最后一个字的时候，是凌晨两点十七分。我关掉雨轩编辑器，盯着屏幕上那个“MCP”三个字母看了很久。

最痛苦的一个决策，不是写哪一章，而是决定不写哪一章。

初稿大纲里，我花了两周时间准备了一整章叫“MCP在企业级架构中的部署策略”——画了六张架构图，对比了四种高可用方案，甚至做了压测数据。但写到后半程，我把这一章整个删了。不是因为写得不好，是因为我意识到：我根本没法在书里教别人怎么做我没真正操盘过的事。

我在Wind和东财做过大规模金融系统不假，但那些系统的架构和今天MCP Server的部署完全是两回事。我可以堆砌Kubernetes、服务发现、熔断降级的术语，但那是在假装知道。删掉那章的时候，我顺手在笔记本上写了一句话，后来成了书里“独思时刻”某个观点的雏形——“错误选择的代价不等于零”。对，我当时就是选错了方向，浪费了两周时间。

意外收获来自一个我完全没预料到的角度。

写第三章“协议规范”时，我卡了整整四天。JSON-RPC的消息格式、初始化握手流程、能力协商机制……这些东西文档里都有，我抄一遍毫无意义。后来我在调试WeClaw和Knowly的联动时，偶然发现一个现象：当MCP Server返回的工具描述超过200个token时，Claude理解工具用途的准确率会下降大约百分之二十。这个数据不严谨，但让我突然想通了这本书真正该写什么——不是协议本身，是协议和AI之间的摩擦。

所以后来书里花了很多篇幅讲工具描述的写法、列表长度的控制、错误消息的措辞。这些细节在官方规范里一个字都没有，但在我每天和MCP打交道的真实场景里，它们比握手协议重要十倍。

写完这本书之后，我反而更加不确定MCP会走向哪里了。2026年8月的生态和2024年11月刚发布时已经完全不是一个物种，Server数量、Client形态、应用场景都在剧烈膨胀。我在书里没有给出任何“五年后MCP会怎样”的预测，因为我觉得那是瞎扯。我能做的只是把此刻的理解写得足够诚实。

如果你读完全书只记住一件事，我希望不是某个技术细节，而是一个简单的判断：MCP的价值不在协议本身，在它强迫你去思考的那件事——你的AI到底需要什么工具，以及这些工具该怎么被描述、被暴露、被调用。剩下的，都会变。

苑广山

2026年8月29日

于上海

---
