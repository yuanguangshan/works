# tmux：从一个人的“不爽”到终端复用器的现代标准

> 完整书稿 · 序幕 + 第1-7章 + 终章 · 每章 ≥ 10000 字
> 2026-08-13 整合

---
# 第○章 序幕：当SSH断开的那一刻

## 一、凌晨两点三十七分：一次真实的“死亡时刻”

凌晨两点三十七分。这个时刻我大概会记一辈子。不是因为那个数字本身有什么特别——它不吉利，也不神圣——而是因为它是我的“死亡时刻”。而且我后来发现，几乎每一个靠终端吃饭的人，都有自己的“凌晨两点三十七分”。这是一种近乎宿命的共同记忆。

那晚我泡了今天第二杯咖啡，蜷在客厅的沙发上，盯着笔记本电脑的屏幕。屏幕那头，是两千公里外某座数据中心里的一台服务器，上面跑着我花了整整一周写好的训练脚本。日志一行一行地滚动着：第 47 轮，损失已经压到了一个很低的水平，曲线平滑，一切正常得让人安心。我盘算着，再盯十分钟就去睡，明天早上起来，模型应该已经收敛得差不多，正好赶得上白天的实验。

为了这一天，我其实准备了很多：脚本里的超参数是调了三轮才定的，数据预处理跑了两天，就连那个日志格式，都是特意设计成一眼能看懂关键指标的。我甚至偷偷给模型起了个名字，像给自己的孩子起名一样。所有这些，都押在那条时断时续的 WiFi 连接上。现在回头看，那晚的我就像把全部身家装进一个没有锁的抽屉，然后祈祷小偷不要路过。

WiFi 信号弱，是那台旧笔记本的老毛病。我换了个姿势，屏幕闪了一下，又恢复正常。我安慰自己：没事，小事。

然后，屏幕就不再滚动了。

没有报错，没有警告，没有提前通知。只有一行冷冰冰的文字，像一纸判决书，突兀地钉在屏幕最下面：

```bash
packet_write_wait: Connection to 203.0.113.7 port 22: Broken pipe
Connection closed by remote host.
```

SSH 断开了。

我愣了两秒，机械地重新敲下连接命令。登录成功，输入法切回英文，手指在键盘上飞快地起落，想找回刚才的现场——然后，我停住了。没有训练进程，没有增长的日志文件，没有我精心排布的几个终端窗口，什么都没有。六个小时的训练进度，连同我脑子里还没写完的那段代码思路，一起蒸发了。

我盯着空白的终端看了十分钟。不是愤怒，是一种深深的无助。

那一刻我想过砸键盘。但砸键盘解决不了问题——明天早上，我还是得重新登录，重新配环境，重新跑训练，重新把一切布置一遍。而我的时间，已经在那个断掉的连接里，被一起扔进了虚空。

如果你也是一个在服务器上干活的人，你大概知道我在说什么。你多半也有自己的“凌晨两点三十七分”：也许是部署到一半的断线，也许是跑了一夜的数据任务消失，也许是排到第二天才轮到的下载进度归零，也许是明天要交付的演示环境里那个再也连不上的终端。然后你重新登录，敲下一行命令，屏幕一片空白——刚才的一切，仿佛从来没有存在过。

这就是我写这本书的理由。

但在动笔之前，让我们先把问题问清楚：为什么？为什么一个网络连接断掉，会把千里之外服务器上运行得好好的进程一起带走？服务器又没断电，程序也没出错，凭什么连接一断，它们就要死？

## 二、问题解剖：为什么终端会话这么脆弱

答案藏在 Unix 的一个古老设计里。让我用一个比喻来解释。

想象你透过一扇窗户看墙上的画——进程就是墙上的画，终端就是那扇窗户。理论上，窗户碎了，画应该安然无恙：墙还在，画还挂在墙上。但在 Unix 的世界里，事情不是这样的。这里的“窗户”不仅仅是一个观察孔，它还是画的“生命线”：当窗户碎裂，一阵风会灌进来，把画从墙上吹下来，摔得粉碎。那阵风，叫 SIGHUP。

SIGHUP 是“hang up”的缩写，意思是“挂断”。这个词带着浓重的历史气息——它诞生于电话线拨号上网的时代。在 1970 年代，终端是真正的物理设备：一头是办公室里的键盘和显示器，另一头通过电话线，连着机房里的主机。电话线是稀缺资源，挂断电话，就意味着连接彻底结束。当时的内核设计者想得很朴素：既然电话挂了、终端没了，那么挂在终端上的那些进程，留着还有什么意义？于是，每当终端断开，内核就向与之关联的进程发送 SIGHUP，把它们统统带走，一个不留。

这个设计在 1970 年代是完全合理的。拨号连接断了，你根本没法再访问那台机器，进程留着也是白占资源。但几十年后，世界变了。我们现在通过 SSH 访问远程服务器，网络抖动、WiFi 信号不稳、电梯里的信号屏蔽、笔记本合盖休眠、咖啡店的路由器抽风——随便哪一个，都能让连接瞬间中断。而“连接中断就杀死进程”这个古老的默认行为，却像化石一样延续了下来，悬在每一个开发者的头顶。

更精确地说，故事是这样的：你 SSH 登录之后，内核会为你的会话分配一个控制终端——通常是伪终端，简称 pty——你的进程们组成一个进程组，挂在这个控制终端下面。当你断开 SSH，连接关闭，内核检测到控制终端“挂断”，便向这个进程组发送 SIGHUP。于是，不管你的训练脚本在服务器上跑得多欢，它都会在收到信号的那一刻，像被掐灭的蜡烛一样，悄无声息地熄灭。

伪终端这套机制本身非常精巧，也很有意思，我会在第 5 章里把它拆开揉碎讲清楚。这里你只需要记住一个结论：在你的 SSH 连接和你运行的进程之间，存在一条看不见的“脐带”。脐带断了，进程就死。这是 Unix 世界四十多年来的默认剧本。

而这个剧本的问题在于：它把“连接”和“工作”混为一谈。连接断了，工作凭什么要陪葬？我跑的是训练脚本，不是网络连通性测试。我的工作内容，和我的网络连接质量，在逻辑上没有任何关系。可系统偏要把它们绑在一起，用最粗暴的方式。

这里有个值得玩味的细节：SIGHUP 并不是一道“必死令”。它只是一个信号——进程可以忽略它，也可以捕获它。真正杀死进程的，是系统对这个信号的“默认处理”：收到，就终止。换句话说，这个残酷的默认行为，本质上是四十多年前的设计者替我们做的一个决定：“终端没了，工作就该结束。”这个决定在当时合情合理，但它被写进了系统的基因里，以至于后来没有人想过要去质疑它——就像很少有人质疑“为什么日历从星期天开始”一样。工具的习惯，一旦沉淀进默认值，就会在用户眼里变成“自然规律”。而打破“自然规律”的，往往不是更聪明的用法，而是更根本的架构。

也许你会说：这个痛点谁不知道？不是有 nohup 吗？对，nohup 存在。而像我这样被断线反复折磨过的人，几乎都会发展出一套属于自己的“自救仪式”。接下来，我们就盘点一下这些传统手段，看看它们各自的本事和局限。

## 三、传统自救手段盘点：三层安全绳

我把这套土办法叫作“三层安全绳”。每一层都能救下一点东西，但每一层都有它救不了的东西。

第一层，nohup。nohup 是“no hang up”的缩写，作用是让进程忽略 SIGHUP 信号。用法很简单：

```bash
nohup python train.py > train.log 2>&1 &
```

这一招能保住进程的命——只要进程自己不崩溃，它就会在服务器上继续跑下去，把输出写进日志文件。但它的局限立刻显现：首先，你失去了交互能力。训练到一半，你想停下来看一眼中间结果？不好意思，那是个纯后台进程，你只能靠猜日志。其次，日志管理是个麻烦：输出重定向到哪个文件？日志被撑爆了怎么办？同时跑三个任务，三个日志文件，谁是谁？第三，也是最重要的——nohup 只能保护“一个进程”，保护不了“一个现场”。

第二层，& 加 disown。把任务丢到后台，再用 disown 把它从 shell 的作业表里摘出去，让 shell 退出时不去管它。这一招比 nohup 精细一点，但同样只解决“单个进程存活”的问题，交互、上下文、布局，一概救不了。

第三层，setsid。直接让进程脱离当前会话，自成一套体系。听起来很彻底，但对交互式工作几乎无用武之地——你都脱离会话了，还怎么和它说话？

再往上，还有更重的方案：把任务包装成 systemd 服务，写单元文件，配自动重启策略。这个方案很可靠，但也很“重”——为一个只跑几个小时的临时脚本写一个系统服务，就像为了切一个苹果去搬一台工业机床。而 cron 定时任务只能解决“定时执行”，解决不了“我现在正盯着它跑”这件事。

说来说去，这些方案有一个共同的死穴：它们都只能保护“进程”，保护不了“上下文”。什么叫上下文？就是那一刻你脑子里正在进行的工作状态：三个窗口各司其职，一个跑训练，一个跟日志，一个开着编辑器写下一步的代码；你知道自己看到哪儿了，知道自己下一步要干什么，知道哪些输出是重要的、哪些可以忽略。这些东西，nohup 保不住，setsid 保不住，systemd 也保不住。连接断了，你或许能让进程活着，但你的“现场”已经没了。

这让我想起 2004 年冬天，我自己第一次在远程服务器上弄丢所有进度的经历。SSH 断开的那一刻，我跑了整整两天的数据任务，和我精心布局的四个终端窗口，一起消失了。我重新连上服务器后，盯着空白终端看了十分钟——不是愤怒，是一种深深的无助。那时候我还不知道世界上有 tmux，只知道以后每次跑长任务，都得写 nohup 加 & 再加 disown，像绑三层安全绳一样。

后来我常常想一个问题：我们为什么愿意忍受这些？

我后来想明白了其中一个原因：因为没人告诉过我们还有别的活法。nohup、&、disown 这些土办法，是前人传下来的，教材里写着，同事推荐着，它们构成了一个“约定俗成”的答案。而“约定俗成”最厉害的地方，不是它好用，而是它会堵住你提问的嘴——你慢慢连“有没有更好的答案”这个问题都不问了，把“绑安全绳”当成了远程工作的标配，就像把“断线丢进度”当成了服务器工作的宿命。直到多年后我遇到 tmux，才意识到：问题从来不是“怎么让进程活下来”，而是“怎么让现场活下来”。前者是修补，后者才是答案。

> 💡 **独思时刻：** 我见过太多程序员在一个“够用但恶心”的工具上妥协一辈子，把那些古怪配置和反直觉操作，当成某种“职业代价”默默吞下去。但你仔细想想——我们每天敲下几千行代码来优化用户体验，却从没想过，自己正在使用的工具本身就是“用户体验”的一部分。为什么我们愿意为别人的用户打磨每一个像素，却忍受自己每天依赖的核心工具，像一台 1998 年出厂的拖拉机？断线丢进度这件事尤其荒谬：它完全可以不发生，只需要有人把“会话”和“连接”这两件事拆开。拆开之后，一切问题迎刃而解。这不是技术难题，这是思维惯性——我们默认了“连接断了工作就该死”，就像默认了“室友每天早上六点吹长号”一样，明明可以换一种活法。

## 四、一个更好的答案：让“会话”与“连接”解耦

而“拆开”这件事，正是终端复用器做的事。

终端复用器，英文叫 terminal multiplexer，是一种允许你在一个物理终端上同时运行多个虚拟终端的软件。这是教科书定义，干瘪得很。它真正做的事情，用一个词就能概括：解耦。

把“会话”和“连接”解耦——连接可以断，会话继续活。你运行的进程，不再挂在你的 SSH 连接下面，而是挂在一个常驻的“管家”进程下面。这个管家在服务器上昼夜不停地运行，不依赖任何具体的连接。你的 SSH 连接只是它的一个“访问入口”：连上了，你就能看到你的会话；断开了，会话还在管家那里好好地待着，等你下次回来。

我第一次听到“常驻进程”这个说法时，心里其实打了个问号：在服务器上多跑一个常驻进程，会不会很重？后来才知道，tmux 的 server 进程轻得出奇——它平时几乎不占资源，只有你在里面干活的时候才会忙起来。这有点像家里的路由器：你平时感觉不到它，但全家人的网络都靠它撑着。它不重，是因为它只做一件事：看管你的会话。这个“只做一件事”的克制，贯穿了 tmux 的整个设计生涯，后面我们会反复看到。

这个概念模型，可以用饭店厨房来理解：server 就是后厨的大厨，在后台专心做菜；client 就是前厅的客人，吃完离桌走人，厨房照常运作。客人换了一拨又一拨，菜却一直在做。你 SSH 断线，只是“客人离席”，不是“厨房倒闭”。大厨不认得你，但你点的菜，一直有人在做。

这个管家的名字，叫 tmux。

tmux 是 Terminal MUltipleXer 的缩写——终端复用器。它的核心价值有三个：会话持久化，多任务组织，复用。会话持久化，就是我们刚刚说的那个问题：连接断了，会话不死。多任务组织，是在一个屏幕里同时管理多个窗口、多个面板，像浏览器标签页一样组织你的工作。复用，是你开好的环境可以反复回来使用，也可以和别人共享。这三个价值，听起来平平无奇，但四十年来，没有任何一个方案能同时把三件事都做好——直到 tmux 出现。

顺便说说“复用”这个词。terminal multiplexer 里的 multiplex，本义是“多路复用”——把一条物理链路，复用成多条逻辑通道。这是个老词：在通信领域，一根电话线可以同时传多路信号，一条光纤可以同时跑几十个波长。终端复用器的“复用”，也是这个意思：把一个终端，复用成多个虚拟终端。但我觉得，对使用者来说，这个词还有另一层更朴素的含义——“复”，是反复回来；“用”，是接着用。你断线了，回来，接着用。这才是我们真正在意的“复用”。

我第一次真正用上 tmux 的场景，我记得很清楚。那是在某次又被断线折磨之后，同事丢给我一句话：“下次记得先 tmux。” 我半信半疑地试了试：

```bash
ssh server
tmux new -s work
# 在 tmux 里干活：跑训练、看日志、写代码……
# 然后，WiFi 又断了。

ssh server
tmux attach -t work
```

两行命令之后，我看到了我这辈子最震撼的终端画面：训练日志还在滚动，损失曲线还在下降，三个窗口的原样分毫不差，连我光标停的位置，都和断线前一模一样。仿佛刚才那几分钟的断线，只是一场幻觉。

那一刻我心里只有一个念头：这个东西，早该有人做出来。

而更让我后怕的是另一个念头：如果没有 tmux，刚才那几分钟，就会是我无数次“凌晨两点三十七分”的又一次重演。

再后来，我才慢慢理解 tmux 为什么能接住我。它把事情分成了清晰的几层：会话（session）是一个独立的工作空间，窗口（window）是会话里的标签页，面板（pane）是窗口里的分屏区域，而每个面板背后，是一个独立的伪终端。SSH 连接只是“看”这个工作空间的窗口，不是工作空间本身。我断线，丢的只是那个“看”的动作；工作空间还在服务器上，由 tmux 的 server 进程看管着。这段关系，我会在第 5 章用一个完整的生命周期给你演示一遍：创建会话、开窗口、拆面板、脱离会话、重新附着——每一步，进程和连接的关系都清清楚楚。现在你只需要记住一件事：从那以后，我所有的远程工作，第一件事永远是先开一个 tmux。

## 五、这个痛点有多老：四十年不消失的问题

说到“早该有人做出来”，这里有个事实可能会让你意外：终端复用这个想法，四十年前就有人做出来了。

1987 年 3 月 20 日，柏林工业大学的一位研究者 Oliver Laumann，发布了一款叫 BSD screen 的软件，后来它成长为 GNU Screen。注意这个年份——1987 年，Linux 还没诞生（Linux 要等到 1991 年才出现），互联网还处在史前时代，个人电脑才刚刚开始普及。也就是说，早在现代网络的一切问题出现之前，就已经有人意识到：终端会话应该和连接分开，应该能持久化。

顺带感受一下 1987 年是个什么样的年份：那一年，Windows 2.0 刚刚发布，互联网还蜷缩在科研机构的实验室里，普通人家里的电脑连硬盘都是奢侈品。在那样一个时代，有人就已经在为“终端会话会消失”这个问题写代码了。你可能会觉得不可思议：1987 年的人，既没有云服务器，也没有远程开发，他们为什么会需要终端复用？答案其实很朴素——因为那时的人是通过终端连大型机的，而连接同样会断，同样会丢东西。痛感的来源换了，痛感本身一模一样。

这说明什么？说明“SSH 断开一切归零”这个痛点，不是网络时代的新问题，而是四十年来从未消失的老问题。从 1987 年 Screen 诞生，到 2026 年的今天，将近四十年过去了，工具换了一茬又一茬——终端从物理设备变成了软件，连接从电话线变成了光纤，服务器从机房搬进了云端——但这个问题的本质没变：人们需要一种方式，让终端里的工作，不随连接的中断而消亡。

这恰好是这本书的核心论点：一个工具的生命力，不取决于它是否走在技术前沿，而取决于它解决的问题是否持续存在。tmux 解决的“终端会话持久化”问题，四十年来从未消失，反而在远程开发、云原生成为主流的今天，被无限放大，甚至在 AI 编码时代重回舞台中央。

更有意思的是，这个老问题还连带出了一个副产品：第一个尝试解决它的工具，本身也慢慢变成了问题的一部分。GNU Screen 在 1987 年开创了终端复用这个品类，却在之后的二十年里逐渐僵化：约两万行 C 代码揉成一个单体，垂直分屏这个无数人想要的功能，从来没进过它的正式版；2004 年 1 月，它的开发陷入停滞，2008 年 8 月发布 4.0.3 之后，就再也没有动静。换句话说，第一个站出来解决问题的人，自己也被问题困住了。这正是本书想讲清楚的一条暗线：解决“会话与连接解耦”这个问题的工具，自身也必须健康——如果它自己的架构像毛线球一样越滚越大，那它迟早会成为下一代人眼中的“怪室友”。

你可能会想：终端模拟器不是也提供分屏和会话恢复吗？比如 iTerm2 就内置了这些功能。但这里有一个本质区别：终端模拟器的分屏，只是客户端的视觉装饰——它把画面切成几块，好让你在本地屏幕上同时看几个窗口；一旦 SSH 断开，那些“窗口”就真的没了，因为它们只是你本地屏幕上的画面。而 tmux 的会话运行在服务器端——不管你的客户端是什么、在哪台电脑上、是否在线，会话都真实地活在那台服务器上。一个是画在纸上的窗，一个是开在墙上的窗，区别就在这里。

> 💡 **独思时刻：** 技术会过时，问题不会。1987 年的人需要会话持久化，2026 年的人依然需要——中间隔了四十年，隔了从电话线到光纤的整个网络革命，隔了一代人从出生到工作的全部时间。那些追着热点跑的软件，今天发布、明天过气；而那些老老实实解决一个真实问题的软件，反而活得最久。时髦是时间的敌人，刚需是时间的朋友。tmux 没有追过任何时髦，它只是把一件事做对了将近二十年。而且我越来越觉得，在 AI 时代，“持久化”这三个字的分量还会更重——当你的工作不再只是你亲手敲下的每一行，而是一群 agent 在后台替你跑的时候，你比任何时候都更需要一个不会死的“现场”。

## 六、预告全书：从一个人的“不爽”到现代标准

现在，让我们回到这个故事的主角。

2007 年，北爱尔兰。一位叫 Nicholas Marriott 的开发者——OpenBSD 的贡献者，重度 GNU Screen 用户——遇到了我们前面说的所有问题。而且他比一般用户更惨：他不只是被断线折磨，他背下了 Screen 的全部快捷键，整个工作流完全构建在 Screen 之上，然后眼睁睁地看着这个“怪室友”一天天变得不可救药。

Screen 的问题，我会在第 1 章详细展开，这里先剧透一点：它是一个约两万行 C 代码的单体架构，所有功能揉在同一个进程里，像一团越滚越大的毛线球；2004 年 1 月，它的开发停滞了；它像一位每天早上六点吹长号的室友——能用，但让人抓狂。Marriott 想要的某个功能，在 Screen 的代码库里几乎不可能实现，因为那意味着重构二十年的历史包袱。

于是，2007 年，Marriott 做了一个决定：不 fork，从头写。他先用几个星期写了一个叫“nscr”（new screen，新 Screen）的快速原型，验证了一个关键想法：client/server 架构在终端复用这件事上，确实跑得通。2007 年 10 月 10 日，tmux 的首次代码提交落地；2007 年 11 月 20 日，tmux 第一版公开发布——Terminal MUltipleXer，终端复用器。一个从“不爽”里长出来的项目，就这样出生了。

从此，故事开始加速。2009 年，Paul Irofti 在 OpenBSD 的 hackathon 上提到了 tmux，团队决定把它导入 OpenBSD 基础系统；Theo de Raadt 亲自审阅了代码，两小时后评价“只发现一两个次要问题”——这大概是开源世界里最严苛的一次验收，来自最挑剔的评审者的认可，是最好的质量背书；2009 年 7 月，OpenBSD 正式导入 tmux，Marriott 选择留在 base 里继续开发，换来更多专家用户和更严格的开发节奏。2018 年，Red Hat 在 RHEL 7.6 中弃用 Screen；2019 年的 RHEL 8.0 彻底移除，官方推荐使用 tmux——一个从个人项目里长出来的工具，就此成为终端复用的现代标准。而到了 2025 年 11 月，tmux 3.6 发布，带来了原生滚动条等新特性；2026 年 5 月 20 日，3.6b 成为最新的稳定版。

这条从“一个人的不爽”到“现代标准”的弧线，正是本书的主线。这不是一个天才横空出世的故事——Marriott 没有发明终端复用这个概念，Screen 比他早了二十年。这是一个“把该做的事做对、把该守的底线守住”的故事：命令行能敲的，配置文件里就能写；代码库可读、可扩展；不搞膨胀，拒绝堆功能。也正是这些朴素的原则，让 tmux 在 Screen 冻结之后依然保持活力，让它的生态——从 TPM 插件管理器，到 resurrect 与 continuum 的会话保存组合（GitHub 星标累计超过两万）——越滚越大。

再往深处看，这个故事还藏着另一个时代。就在我写这本书的时候，AI 编码工具——Claude Code、Codex 这些 agent——正在改变人们写代码的方式。而 tmux 又一次站在了舞台中央：分屏成为多 agent 编排的界面，会话持久化让 agent 的长任务在断开后继续运行。一个 2007 年的工具，在 2026 年反而迎来了新的复兴。这是巧合吗？不是。因为 tmux 解决的那个问题——“工作现场不该随连接消失”——在 agent 时代只会更重要。你让 agent 跑着任务，然后断开 SSH 去吃饭、睡觉、开会，回来后重新附着会话，一切纹丝不动。这大概是最朴素，也最奢侈的安心感。

我还想多说一句“标准”这个词。RHEL 的官方推荐、OpenBSD 的基础系统，这两件事加在一起，意味着 tmux 已经不再是一个“可选项”，而是一个“默认项”。但我更愿意把它理解成另一种东西：标准不是评选出来的，是沉淀出来的。没有人开过会决定“就用 tmux”，它只是在过去将近二十年里，一次又一次地接住了开发者们断掉的连接，一次一次地，直到所有人都觉得“这本来就该是这样”。这种沉淀出来的标准，比任何官方认证都结实。

我写这本书的时候，经常有人问我：一个终端工具，值得写一整本书吗？我的回答是：值得。因为 tmux 的故事，本质上是一个关于“标准如何形成”的样本——它不是商业公司战略规划的产物，不是资本催熟的产物，甚至不是“市场需求调研”的产物，而是一个人在深夜被断线折磨之后，决定不再忍受的产物。从一个人的不满，到一个社区的共识，再到一个行业的默认值——这条路径，比任何产品方法论都更接近技术演进的真相：标准不是设计出来的，是长出来的。如果你也想理解开源世界里那些“理所当然”的工具究竟是怎么来的，这本书会给你一个完整的答案。

## 七、独思时刻：工具的意义在于“消失”

在正式出发之前，我想聊聊这本书里我最喜欢的一个观点：好工具应该“消失”。

> 💡 **独思时刻：** 什么样的工具是好工具？我的答案可能有点反直觉：好的工具应该“消失”。不是从你的电脑里消失，而是从你的注意力里消失。用时不打扰，出事时兜底——你几乎感觉不到它的存在，就像感觉不到眼镜、感觉不到脚下的地板、感觉不到为你供电的电网。tmux 就是这样一种工具：它平时不声不响，不弹窗，不刷存在感，很多人装好之后就忘了它，甚至不知道它正在后台守护着自己的工作现场。但当你 SSH 断掉的那一刻，它默默接住了你——你的进程没死，你的现场没丢，你的工作没有归零。那一刻你才会意识到：原来它一直都在。这让我想到，工具的最高境界，不是“功能强大到让你离不开”，而是“可靠到让你忘了它的存在”。消失，才是最高级的在场。反过来说，一个天天刷存在感的工具，多半是在用花哨掩饰不可靠。

## 八、本书怎么读：三条阅读路径

写到这里，序幕该收尾了。在正式出发之前，我想给不同的读者指几条路——这本书不是只有一种读法，我不希望任何人因为“这是本讲工具的书”就错过它另外的两面。

先说清楚这本书的读者画像：你不需要有任何 tmux 基础，但最好用过 SSH、敲过命令行、在服务器上丢过什么东西。如果你完全没丢过，也没关系——读完这本书，你会成为一个“再也不会丢东西”的人。这本书有四张面孔：它是 Marriott 的传记，是终端复用技术的演进史，是 tmux 的实战指南，也是一堂关于好设计的公开课。四条线索织在同一本书里，你可以按自己的口味，选择从哪条线开始。

如果你是技术向读者，想快速上手，搞清楚 tmux 到底怎么用：请直接跳到第 6 章。那一章从安装到配置文件，从核心快捷键到保存恢复，是一条完整的实战路径；读完第 6 章再回头读第 5 章，理解 client/server 架构的细节，你会发现自己对 tmux 的理解会完全不一样——快捷键是表，架构是里，先学会用，再搞懂为什么。

如果你是故事向读者，想看看一个开源项目如何从个人项目长成行业标准：从第 1 章开始读吧。你会认识 Marriott 和他那位“怪室友”Screen，看到 nscr 原型如何验证一个疯狂的想法，看到 tmux 如何在 OpenBSD 的严格审阅下淬火成钢，也看到 Red Hat 的一纸公告，如何宣告一个时代的更替。

如果你是哲学向读者，好奇一个工具为什么能活将近二十年：直接翻到第 7 章。那一章讲了 Marriott 的治理哲学——不搞膨胀、聆听用户、保持克制，也讲了 AI 时代 tmux 为什么反而复兴。读完你会明白，工具哲学不是玄学，它藏在每一次“这个功能要不要加”的取舍里。

如果你还想要一张全书的地图，这里给你一份简版：第 1 章讲 Screen——那个开创品类又困住自己的老前辈，理解它，你才知道 tmux 为什么必须存在；第 2 章讲 tmux 的诞生，从 nscr 原型到首次提交，看一个想法如何落地成代码；第 3 章讲 OpenBSD 的接纳与社区成长，看一个项目如何经受最严苛的审阅，又如何在用户与贡献者的推动下补全能力；第 4 章讲版本足迹，从 1.x 到 3.x，看一个工具如何一点点长大而不失控；第 5 章拆开架构，client/server、会话、窗口、面板、伪终端，一次讲透；第 6 章是实战，从安装配置到日常操作，一条龙上手；第 7 章聊设计哲学与 AI 时代的复兴，回答“一个 2007 年的工具，为什么在 2026 年依然重要”。你可以按图索骥，也可以随手翻开任何一章——每一章我都尽量写得独立成篇。

当然，我私心建议你从头读。因为这本书里所有关于 tmux 的用法、架构和哲学，都长在一个具体的场景上——那个凌晨两点三十七分的场景。理解了那个场景，你就理解了 tmux 为什么存在；理解了 tmux 为什么存在，你就理解了后面所有章节的逻辑。整本书，其实都是在回答序幕里提出的那个问题：连接断了，凭什么一切归零？

## 九、尾声：下一次断线

最后，让我们回到那个深夜。

如果我当时就知道 tmux，那个凌晨两点三十七分会是什么样？大概是这样的：SSH 断开，我骂了一句，合上笔记本去睡觉；第二天早上，泡一杯新咖啡，重新连上服务器，敲下 tmux attach——训练还在跑，日志还在滚，损失曲线还在下降，仿佛昨晚的一切从未发生。我的六个小时没有白费，我的现场还在那里等我。

这不是幻想，这是任何一个 tmux 用户的日常。

你看，这就是 tmux 最让人上瘾的地方：它不是帮你“多干一点活”的工具，而是帮你“少丢一点东西”的工具。前者让你更忙，后者让你安心。在这个信息过载、注意力涣散的时代，“安心”两个字，比任何效率指标都金贵。

接下来的章节，我会带你完整地走一遍这条路：从 Marriott 的“不爽”开始，到 nscr 原型、OpenBSD 接纳、版本进化、架构原理、实战技巧，再到 AI 时代的复兴。你会看到，一个工具如何从一个人的“不爽”里诞生，如何被一群苛刻的人打磨成标准，又如何在你最需要它的时候，安静地接住你。

如果读到这里，你心里已经冒出一个念头——“原来我那些年丢掉的进度，本来是可以不丢的”——那这本书的序幕，就算完成了它的任务。剩下的旅程，就是去搞清楚：那个能接住你的东西，到底是怎么来的，又是怎么做到的。

而这，只是序幕。

如果你现在就想动手，打开终端，登录一台服务器，敲下 `tmux new -s hello`——你会看到屏幕底部多了一条状态栏，这就算打过招呼了。不必急着记住快捷键，先把一个问题放在心里：连接会断，现场不该断。带着这个问题去读后面的章节，你会收获更多。

现在，让我们回到 2007 年，回到北爱尔兰，回到那个决定一切的时刻——去见见 Nicholas Marriott，和他那位每天早上六点吹长号的“怪室友”。

---

# 第1章 主角登场——Nicholas Marriott与他的“怪室友”

那个深夜，北爱尔兰贝尔法斯特的雨正打在窗户上。Nicholas Marriott坐在屏幕前，像往常一样按下`Ctrl+a`，然后是接下来的几个键——这套肌肉记忆已经深深刻在他的手指里，比他能背出的任何电话号码都要熟悉。他是一位OpenBSD贡献者，一个对代码质量有着近乎宗教般执念的开发者。而此刻，他正盯着GNU Screen的源代码，眉头紧锁。

Screen是他的“怪室友”。说它“怪”，是因为它确实能用——1987年诞生的GNU Screen，到2007年已经在这个世界上存活了整整二十年。这二十年里，互联网从阿帕网的叛逆青春期长成了覆盖全球的基础设施，而Screen一直坐在那里，像一个每天早上6点准时吹长号的室友。长号吹得不错，节奏也稳，但就是让你没法安生。它不会害你，但它会一直烦你；你离不开它，可你也受不了它。这就是“怪室友”的定义：你能和它共处二十年，但你绝不会想再和它共处第二十一年。

Marriott是重度Screen用户，背下了全部快捷键，整个工作流都建立在这个工具之上。没有它，他几乎没法工作。但正是这种深度依赖，让他比任何人都更清楚地感受到那些日积月累的刺痛。一个人只有天天和某个东西生活在一起，才会知道它哪里硌人。偶尔路过的过客只觉得它功能强大，只有朝夕相处的室友才知道，它每天早上6点会准时吹响长号。

那么，这位与“怪室友”同住二十年的北爱尔兰人，到底是谁？他为什么能忍这么久？又是什么让他最终决定搬出去，甚至亲手盖一栋新房子？这一章，我们从这个人讲起。

## 1.1 人物速写：一个北爱尔兰人的固执

关于Nicholas Marriott的公开资料，少得可怜。他属于那种典型的“作品比人出名”的开发者：今天，全世界每一台装有tmux的服务器上都印着他的名字，但你几乎搜不到关于他本人的长篇报道。没有励志演讲，没有社交媒体上的高谈阔论，没有“改变世界的十个习惯”。他留下的，只有代码、邮件列表里冷静的回复，以及一两次技术访谈里克制到近乎平淡的陈述。

他是北爱尔兰人。北爱尔兰多雨，贝尔法斯特更是如此——那种细密的、没完没了的雨，会把人的性子磨成两种极端：要么焦躁，要么沉得住气。从后来发生的一切看，Marriott属于后者。他说话从不夸张，从不抱怨，描述自己最不满意的工具时，用的词也不过是“非常凌乱且（当时）无人维护”这样的平实陈述。他不是那种会拍桌子的人；他是那种发现桌子不稳，就自己动手削一只桌脚的人。

他的另一个身份，是OpenBSD贡献者。这个身份比“北爱尔兰人”更能解释他后来的选择。OpenBSD是BSD家族里以“安全、正确、干净”著称的操作系统，它的社群对代码质量有一种近乎偏执的坚持：每一行进入系统的代码都要经过最严苛的审查，宁可功能少一点，也不肯让丑代码进门。OpenBSD的口号里没有“快速迭代”，只有“默认安全”和“正确性优先”。在这种环境里浸泡久了，人会变得不太愿意“凑合”——见到烂代码会难受，见到反直觉的界面会皱眉，见到一个明明可以修好却任由它烂下去的工具，会忍不住想动手。

在欧洲的OpenBSD贡献者圈子里，Marriott并不是一个高调的名字。他不写长篇大论的博客，不做演讲，他的贡献方式更接近老派的Unix传统：发现一个工具不对劲，就写信给维护者，或者干脆自己写一个。他是那种“动手派”的开发者——与其抱怨，不如重写。这种性格在OpenBSD的生态里并不罕见：那个圈子信奉的是“代码说话”，一个补丁胜过一百句牢骚，一次重写胜过一百次讨论。

Marriott的“固执”，不是那种写在脸上的倔强，而是一种安静的、水滴石穿的固执。他不争辩，不游说，只是默默地把事情做出来给你看。后来我们知道，这种固执最终改变了一个软件品类的命运；但在2007年之前，他只是OpenBSD社区里众多安静的技术人之一，手里攥着一个用了十年、让他又爱又恨的终端工具，和一份越积越长的“不爽”清单。

在Unix的世界里，终端工具从来不只是工具。对Marriott这一代开发者来说，终端是工作台、是书房、是工厂车间，是一个人每天待得最久的地方。一个终端工具好不好用，直接决定了一个人每天的心情好不好。这也是为什么，程序员对终端工具的怨念，往往比对其他任何软件都深——因为在这里，我们无处可逃。换一个App很容易，关掉浏览器很容易，但你没法把命令行关掉：你每天坐在同一张桌子前，面对同一个终端工具，它的每一个小毛病，都会在你的职业生涯里被放大成千上万次。理解了这一点，你就能理解为什么一个“能用”的工具，会让它的重度用户痛苦二十年。

## 1.2 怪室友的登场：1987年柏林的那个春天

1987年3月20日，Usenet的net.sources新闻组里出现了一个程序，名字叫BSD screen。发布者是柏林工业大学（Technische Universität Berlin）的Oliver Laumann。那年没有万维网，没有GitHub，源代码的流通方式是新闻组：你写一个有用的程序，贴到net.sources上，全世界的人用调制解调器和拨号线路把它下载下来，编译，使用，然后写信告诉你哪里有问题。那是一个“代码靠邮件传播”的时代，一个程序能被人下载、编译、用起来，本身就是一种胜利。

想象一下1987年的机房：大型机嗡嗡作响，一排排终端摆在长桌上，学生们排队等着上机。终端背后连着的，可能是一台全校师生共用的主机，你在终端上敲下的每一行命令，都在和几百个陌生人争夺同一台机器的注意力。在这样的环境里，“能在一个屏幕上同时开好几个会话”，不只是一个便利的功能，而是一种生存技能——它意味着你不用因为排队而浪费时间，意味着你可以在一个会话里跑任务，在另一个会话里继续写代码，互不打扰。Screen诞生的土壤，就是这种资源稀缺的大学机房；它的每一个早期功能，都是冲着“省”字去的：省终端、省时间、省机会。

Screen的诞生比Linux还要早四年——Linux内核1991年才出现，而Screen在1987年就已经存在了。在那个年代，一台昂贵的主机往往连接着许多台终端，人们通过串口线、拨号线路连到主机上工作。终端是稀缺资源，会话更是。想象一下你正在主机上跑一个重要的任务，可你只有一台终端、一个屏幕：想同时开两个会话？对不起，要么切换，要么放弃。想离开一下让任务继续跑？更不行，你一旦断开，会话就死了。

Screen想解决的问题，朴素而实际：能不能让一个物理终端上同时运行多个虚拟终端？能不能在离开终端之后，让正在运行的进程继续活下去？这两个问题，今天听起来平平无奇，但在1987年，它们是实打实的魔法。1987年的第一版Screen就已经具备了一副不错的骨架：多路复用，在单个物理终端上运行多个进程（通常是交互式Shell）；每个虚拟终端有独立的滚动历史缓冲区；支持在不同窗口间复制粘贴文本；每个虚拟终端还能模拟DEC VT100终端及部分ANSI X3.64功能。

名字也取得很实在——screen，屏幕。它把自己定位成“屏幕的管理者”。这个定位在接下来的二十年里一直没有变，问题在于，二十年里它给自己加的东西实在太多了。第一版Screen发布时，它还是一个干净的、目标单一的工具；没有人会想到，二十年后，这个工具会变成一座连维护者自己都望而却步的老房子。

## 1.3 二十年演进：从BSD screen到GNU项目

1990年，Oliver Laumann把Screen的维护权交接给了Jürgen Weigert和Michael Schroeder，交接地点在埃尔朗根-纽伦堡大学。两位新维护者接手之后，把项目迁移到了GNU项目之下，从此它有了那个我们熟悉的正式名字：GNU Screen。也正是在这个时期，Screen加入了一系列重要功能：分屏（split-screen）、剪切粘贴（cut-and-paste）、屏幕共享（screen-sharing）。

从1990年到2004年，Screen走过了一段漫长而“繁荣”的演进之路。版本号一路攀升：1995年1月发布3.6.0；1996年10月的3.7.1开启了它最活跃的开发期；1999年8月的3.9.4；2003年3月的3.9.15。从1996年10月到2004年1月，近七年半的时间里，Screen处于积极开发阶段——功能不断堆叠，补丁不断涌入，它几乎预装在所有发行版里，成为那个年代终端复用的事实标准。

这段历史，从外面看是“繁荣”，从里面看却是另一回事。每个维护者都会加一点自己觉得有用的东西，每个用户都会提交一点自己需要的补丁，Screen像一棵无人修剪的树，朝着各个方向疯长。功能越来越多，但没有人停下来问一句：这些东西彼此之间，真的协调吗？架构还撑得住吗？——这正是“老房子”的开端：它不是某一天突然变老的，而是每一次“顺手加个房间”积累出来的。

这种“功能堆叠”的模式，在开源软件的早期几乎是一种宿命。一个工具好用，用户就会要求更多功能；维护者为了回应需求，就不断地加。加功能本身没有错，错的是只加不整：二十年里，Screen几乎没有进行过任何一次架构层面的清理。每一个新功能都像在毛线球上又绕了一圈，毛线球越滚越大，但没有人知道哪根线连着哪根线。

回头看，Screen的演进路径并非没有道理。在1990年代，终端复用还是一个年轻的领域，没有人知道一个终端复用器应该长什么样。每加一个功能，都是在试探边界；每收下一份补丁，都是一种新鲜的尝试。这种“野蛮生长”在早期是优点——它让Screen迅速覆盖了用户能想到的所有场景，也让它稳稳占据了“事实标准”的位置。但问题在于，它从来没有一个“收敛”的阶段。功能加够了，就该有人站出来说：够了，现在我们来整理架构。这句话，Screen等了二十年，也没有等到。于是功能越堆越多，架构越来越乱，直到“加功能”本身变成一件奢侈的事。

## 1.4 “老房子”：两万行C代码与monolithic架构

到2007年，GNU Screen已经积累了大约两万行C代码。两万行听起来不多——今天随便一个像样的软件都有几十万行——但关键在于，这两万行是一个monolithic（单体）架构：所有功能都揉在同一个进程里，全局状态无处不在，任何两个功能之间都可能存在隐性的耦合。想在角落里加一个小功能，你得先搞清楚它会不会碰坏别的东西。

这就是“被改建了二十年的老房子”：地基还是1987年打的那块，墙是1990年砌的，屋顶是1996年翻的，二楼是1999年加的，地下室是2003年挖的，每一任住户都按自己的需要改了水电。你说这房子不能住？能住，而且住得挺热闹。但你想在承重墙上开一扇门？没人敢保证后果。你问这房子值多少钱？它值它的历史，但它的历史恰恰是它最沉重的负担。

它也是一团“越滚越大的毛线球”。两万行C代码，二十年层层叠叠的补丁，一个几乎没人敢动的核心——新人想进入这个代码库，光是理解全局状态如何流转就要花上几个月。社区的共识逐渐变成：Screen能跑，但别碰它的内部。一个“能跑但别碰”的代码库，就是一座开发上的死火山：表面平静，下面全是岩浆，谁也不知道哪一脚会踩穿地壳。

对于普通用户来说，这些架构问题藏在表象之下；但对于开发者来说，它决定了“加一个功能”的真实成本。加一个功能，从来不是写几十行代码的事，而是要在一个人人都不完全理解的系统里做外科手术。手术刀下去之前，你得先赌：这一刀下去，楼不会塌。

更麻烦的是，monolithic架构意味着一切都在同一个进程里：窗口管理、输入解析、终端模拟、转义序列处理、配置解析、历史缓冲区……全都挤在一个地址空间里，共享同一份全局状态。任何一个子系统的改动，都可能波及所有其他子系统。这就像把厨房、客厅、卧室全部打通成一个大开间：做饭的油烟会飘到床头，客厅的动静会吵醒卧室，住是能住，但你永远无法只动一个房间而不惊动其他房间。谁都不愿意在这样的房子里施工，于是大家心照不宣地达成了一个默契：能不动，就不动。

## 1.5 深度缺陷解剖：分屏残废、配置咒语、没有插件生态

二十年的老态，具体落在哪里？我们把Screen的病，一条一条剖开来看。

第一条，分屏残废。终端分屏是现代终端的刚需——尤其是进入21世纪，宽屏显示器普及之后，人们恨不得一个屏幕切成四块，一边写代码，一边看日志，一边跑测试，一边开着文档。Screen支持分屏吗？支持，但只支持水平分屏，也就是上下切。垂直分屏，也就是左右切？从来没有进入过生产版本。对于一个2007年的开发者来说，没有垂直分屏意味着什么？意味着你的宽屏显示器明明可以左右并排两个窗口，你却被逼着只能上下叠着放；意味着你在比对两份代码的时候，永远得在“切来切去”和“上下滚动”之间二选一。这个功能缺口，单看是一个小遗憾，放在宽屏时代里，却是一道越来越深的裂缝。2007年，开发分支里曾有人尝试添加垂直分屏功能，连尺寸调整和布局系统都设计了，最终却只以一份补丁的形式存在，从未合并进任何一个正式版本。这件事的讽刺之处在于：就在同一年，远在北爱尔兰的Marriott正在动手写自己的终端复用器——而那台“怪室友”连一个最基本的垂直分屏都迟迟给不出来。后面我们会看到，垂直分屏成了tmux的杀手锏之一，而Screen直到生命终结都没能拥有它。

第二条，配置语法混乱。Screen的配置文件像某种失传的咒语：命令行是一套语法，配置文件是另一套语法，两套之间没有一致性可言。想做的事情稍微复杂一点，你就得去查手册，而手册本身又是出了名的难懂。文档糟糕到什么程度？Marriott多年后在访谈里回忆：“查找一个功能怎么用，比实现它还要费劲。”这句话出自一个背下了全部快捷键的重度用户之口，分量可想而知。一个工具如果连它的忠实用户都觉得文档是负担，那它的文档就不是文档，而是门槛。

第三条，没有插件生态。二十年的历史，两万行代码，居然没有生长出一个像样的插件机制。用户想要新功能，唯一的出路是提交补丁——而在一个“别碰内部”的代码库里，提交补丁等于要求普通用户先成为Screen的架构专家。没有插件生态意味着什么？意味着这个工具的进化完全依赖维护者的个人意愿，意味着用户的真实需求无法通过社区自组织的方式得到满足。一个不能生长的工具，迟早会变成标本。

第四条，命令行反直觉。这一条与配置语法互为表里：命令行的设计像是“能跑就行”，从不考虑心智负担。用户记住的不是一套自洽的逻辑，而是一堆彼此独立的孤岛。而与之相关的还有一件事：屏幕共享虽然被列入了功能清单——让两个人在两个终端上共享同一个会话——但用起来磕磕绊绊，像一件从未被认真打磨过的半成品。

这四条缺陷单独看，每一条都只是“难受”；合在一起，就是一个恶性循环：因为文档难懂，新用户学不会，留下的都是老用户；因为老用户用惯了，就没人抱怨，维护者听不到真实的声音；因为没有插件生态，新功能进不来，工具的功能就永远停留在维护者的兴趣范围内；因为架构太乱，连维护者自己都不敢大动，于是只能小修小补，让架构更乱。二十年下来，Screen不是被某个对手打败的——它是被自己的一层层历史包袱压垮的。而“垂直分屏”这个缺口，则像一扇永远关不上的窗户，让所有在宽屏时代工作的用户，都看得见Screen的局限。

## 1.6 2004年：开发停滞与社区死水

2004年1月27日，Screen发布了4.0.2——这被普遍视为最后一个真正意义上的生产版本。就在这个版本发布前后，Screen的开发事实上停止了。2004年1月，项目进入“被放弃”的状态：没有新版本，没有重构，没有方向。此后的四年多里，社区的邮件列表上依然有零星的活动，但代码库基本冻结。

2008年8月7日，Screen发布了4.0.3。这是一次对4.0.2的小幅升级，修修补补，然后正式告别——此后Screen的生产版本彻底冻结。从1996年到2004年，它是那个年代终端复用的绝对主角；从2004年到2008年，它缓慢地、安静地死去。开发分支（GIT）里后来依然有零星动态，但正式发布已经停滞，对绝大多数用户来说，这个工具已经死了。

死水是什么样的？bug堆积，无人修复；用户提问，无人应答；新需求，无人评估。不是维护者不负责任——维护者可能已经离开了这个领域，或者被生活吞没——而是项目本身失去了进化的动力。一个开源项目一旦停止发布，它的社区就开始流失：最活跃的用户走了，剩下的用户只是“还能用就继续用”。Screen就是这样，从2004年到2008年，它躺在每一台服务器的软件仓库里，像一件没人再翻新的旧家具，落满灰尘，但还在那里——因为对大多数人来说，它“还能用”。

“还能用”，恰恰是它最大的问题。一个坏掉的工具不可怕，人们会痛快地抛弃它；一个半死不活的工具才可怕，它占据着你的肌肉记忆、你的配置文件、你的工作流，让你想换又觉得“不至于”。Screen就这样占着终端复用这个生态位，一占就是二十年。

而在这四年里，外面的世界并没有停下。SSH远程工作越来越普遍，宽屏显示器走进千家万户，多任务、多窗口成了开发者的日常。恰恰是这些变化，让Screen的缺陷变得越来越刺眼：你在宽屏上想左右分屏，它给不了；你同时连着十台服务器想统一管理会话，它力不从心；你遇到一个bug查遍文档找不到答案，邮件列表里也没有回声。一个工具的时代，就是这样过去的：不是轰然倒塌，而是日复一日地让人失望，直到某一天，有人决定不再等它。而更残酷的是，在这个领域里，用户几乎没有别的选择——Screen是那个年代唯一的“事实标准”，它的停滞，就是整个品类的停滞。

站在用户的角度，2004年到2008年这四年是最难熬的：你明明知道手里的工具浑身是病，却找不到替代品；你明明看到它四年没有新版本，却还是得继续用它，因为你没有别的选择。这种“被一个旧工具绑架”的感觉，和租了一间二十年前装修的老房子一模一样：房东不修，你又搬不走。对大多数用户来说，忍一忍也就过去了；但对Marriott这样的人来说，“忍”从来不是选项——他手里有工具，也有能力。

## 1.7 一个重度用户的不爽清单

现在，让我们回到Marriott。他可能是这个星球上最有资格评价Screen的人之一：重度用户，背下了全部快捷键，工作流完全建立在Screen之上。他不是那种用两下就嫌麻烦的过客，他是那个把长号室友的作息时间表都背下来的同居者。

也正因为如此，他的“不爽”清单才格外有分量。这份清单很长，但我们至少可以列出最刺眼的几条：文档糟糕透顶，查找一个功能怎么用，比实现它还费劲；配置语法像失传的咒语，命令行反直觉；代码“非常凌乱且（当时）无人维护”；没有垂直分屏，没有插件生态；而最关键的一条是——当他想加一个功能时，他发现这在架构上“不现实”。

注意这份清单的结构：前几条是“用起来难受”，最后一条才是致命的——“想改变它，不可能”。一个工具可以丑，可以难用，只要它还能进化，用户就还有盼头；但一个连进化能力都没有的工具，对重度用户来说，每一天都是煎熬。Marriott背着这份清单过了很多年。他忍了。他一直忍到2007年，直到一个具体到无法回避的需求，把他逼到了墙角。

清单这种东西，还有一个可怕之处：它会越积越长。最初你只是觉得文档难懂；后来你发现配置语法不统一；再后来你发现想左右分屏做不到；最后你发现，连“提一个需求”这条路都是堵死的。每一条新发现，都会让前面所有的不满加重一层。Marriott的清单之所以在2007年爆发，不是因为他突然变得不能忍了，而是因为清单已经长到了“忍无可忍”的地步——而最后一个需求，恰好是压垮骆驼的那根稻草。

这里有一个值得品味的悖论：背下了全部快捷键的Marriott，恰恰是被Screen折磨得最深的人。他背快捷键，不是因为喜欢，而是因为离不开；他的肌肉记忆越深，他的沉没成本就越高，他越难抽身。但反过来，也正是这份日复一日的亲密接触，让他对Screen的每一处别扭都了如指掌——他知道病在哪里，知道病根是什么，知道哪些病已经没法治了。

> 💡 **独思时刻：** 我见过太多程序员在一个“够用但恶心”的工具上妥协一辈子，把那些古怪配置和反直觉操作当成某种“职业代价”默默吞下去。但你仔细想想——我们每天敲下几千行代码来优化用户体验，却从没想过，自己正在使用的工具本身就是“用户体验”的一部分。为什么我们愿意为别人的用户打磨每一个像素，却忍受自己每天依赖的核心工具像一台1998年出厂的拖拉机？后来我慢慢想明白了一个道理：我们对工具的容忍度，其实是对自己职业尊严的容忍度。一个天天用烂工具还觉得“没关系”的工程师，慢慢地，也会接受自己写出烂代码。

> 💡 **独思时刻：** 重度用户是最严苛的产品经理。产品经理调研用户，问“你还有什么需求”，得到的答案往往是“都挺好”——因为轻度用户根本不知道工具还能更好。真正的需求清单，藏在那些被工具折磨了十年还离不开的人心里。Marriott之所以能写出tmux，不是因为他比谁聪明，而是因为他比任何人都更痛。痛苦是最诚实的用户调研：它能精确地告诉你，一个工具在哪些地方辜负了它的重度用户。我做过一个很简单的实验：让一个用了十年Vim的人列出Vim最让他抓狂的三个点，他能不假思索地说出来；让一个昨天才打开Vim的人说，他只会说“好像有点难”。重度用户的价值，不在于他们能告诉你“怎么做”，而在于他们能告诉你“哪里真的疼”。疼的地方，就是机会。

## 1.8 导火索：一个“不可能完成”的需求

2007年，Marriott遇到了一个具体到不能再具体的需求：多个终端共享同一个会话窗口，但同一会话中的其他窗口完全独立。用今天的话说，他希望两个物理终端能“看”同一个会话，同时每个终端里各自开着属于自己的窗口，互不干扰。在今天的tmux里，这不过是`tmux new-session -t existing -s new`一条命令的事。但在2007年的Screen里，这个需求，等于要求一栋用砖块和泥巴砌了二十年的房子长出钢铁骨架。

问题出在架构上。Screen是一个巨大的monolithic进程，所有功能揉在一起，像一个越滚越大的毛线球。要实现“多个终端共享同一会话、其他窗口完全独立”，意味着要把“会话”从“进程”里抽出来，重新设计状态模型——这等于给老房子换承重墙。Marriott评估了一下：在Screen的代码库里加这个功能，需要动核心，需要重构二十年的历史包袱，需要在一个几乎没人敢碰的代码库里“拆墙”。这不是工作量的问题，是风险的问题：你根本不知道拆了这面墙，二楼会不会塌。

这个需求之所以能成为导火索，还因为它背后是一个再常见不过的真实场景：你在一台服务器上工作，中途需要让另一位同事也看到当前的会话，或者你自己换个终端继续——你希望“会话”这个东西，是独立于任何一台终端、任何一个连接而存在的。这个想法在今天听起来理所当然，因为tmux把“会话”变成了一个第一公民；但在Screen的monolithic模型里，“会话”和“进程”是绑死的，想拆开它们，等于重写半个Screen。Marriott不是心血来潮想“发明”一个新工具，他只是想要一个在今天看来稀松平常的功能，而旧工具告诉他：这个功能，你要不起。

还有一道许可证的坎。Screen是GPL许可证，而OpenBSD社群对许可证有着自己的哲学坚持。OpenBSD的传统偏爱ISC这类宽松许可证——更简洁、更宽松，更适合嵌入系统，也更符合OpenBSD“做正确的事”的风格。GPL本身没有对错之分，它是一套伟大的许可证；但把它和OpenBSD的价值观放在一起，就像把一套英国式礼仪硬塞进一场北欧聚会——不是不行，是水土不服。后来我们知道，tmux最终采用了ISC许可证，这从第一天起就注定了它与OpenBSD之间的血缘关系。

架构天花板、许可证理念、社区死水，三个因素叠加在一起，结论已经非常清楚：在Screen里“改”出这个功能，不现实；就算改出来了，也只是在一栋危楼里再添一间摇摇欲坠的屋子。Marriott看着面前的两万行代码，得出了一个极其冷静的判断。

## 1.9 决定重写：2007年的那个决定

他不fork。他选择从头写一个替代品。

多年后，有人用一句话概括了他的选择：

> Marriott did what any self-respecting OpenBSD developer would do: he wrote a replacement from scratch.

翻译过来就是：任何一个有自尊的OpenBSD开发者，都会这么做——从头写一个替代品。

这句话值得拆开看。注意，关键词不是“写”，是“自尊”。对一个OpenBSD开发者来说，天天用一个自己知道是烂代码的工具，是一种职业层面的自我背叛。你可以用，但你不能假装它没问题；你可以忍，但你不能忍到失去判断力。Marriott的选择不是冲动的赌气，而是深思熟虑后的判断。他后来在访谈里说得很平静：“增加这个功能到Screen里是不现实的，让代码库可读、可扩展成了我的目标之一。”用大白话说就是：与其在一栋危楼里加电梯，不如从打地基开始盖一栋新的。

我尤其喜欢他当时的姿态——没有愤怒的宣言，没有“我受够了”的咆哮，没有对Screen维护者的指责。就是一个OpenBSD开发者，觉得手里的工具不对劲，于是动笔重写。2007年秋天，一个叫“nscr”（new screen，新屏幕）的快速原型出现在他的电脑上，只有几个基本功能——能创建会话，能开几个窗口，能把屏幕切几块——但那些功能跑通了。它证明了那件事可行。关于这个原型的故事，我们下一章再细讲。

决定重写，是需要勇气的。勇气不在于放弃一个坏工具——那不难；勇气在于放弃你为之付出的沉没成本。Marriott背下了全部快捷键，那是一种真金白银的肌肉记忆；他的工作流完全建立在Screen之上，那是日复一日累积起来的熟练度。重写意味着：承认这些投入都白费了，然后从零开始，去学习一套全新的东西，去忍受新工具早期的粗糙，去面对“你凭什么觉得你能做得更好”的质疑。

但他显然想得很清楚。他见过OpenBSD社群怎么对待代码：宁可功能少，不可代码脏；他见过Screen怎么对待自己的二十年：只堆功能，不整架构。这两样东西放在天平上，答案不言自明。他不fork，因为fork一个monolithic的毛线球，得到的还是毛线球；他要的，是一块干净的地基。

还有一个细节值得记住：Marriott写下第一行新代码的时候，并不知道这件事会走向何方。他没有商业计划，没有融资，没有团队，甚至没有一个明确的时间表。他只是一个被一个坏工具折磨了太久的人，决定不再忍了。所有伟大的重写，起点往往都朴素得不像话——不是“我要改变世界”，而是“这玩意儿让我难受，我要自己弄一个不那么难受的”。世界的改变，常常就是这么开始的。

> 💡 **独思时刻：** 什么时候该推翻老房子？我的答案很简单：当“修补”的成本曲线开始陡峭上扬，而“重写”的成本曲线开始平稳下降的时候。老房子不是不能修，但你要诚实评估：每次修完，它是不是变得更难修了？如果是，那它就不是在积累价值，而是在积累债务。我见过太多团队把“兼容旧系统”当成信仰，结果二十年过去了，所有人都在给一个没人理解的核心打补丁，新人来了又走，代码库越来越像一座迷宫。反过来，我也见过太多人把“重写”当成万能药，动不动就推翻重来，结果新系统犯的错误比旧系统还多。重写的正确时机，不是“旧系统很烂”，而是“旧系统的烂，已经无法通过局部修补来治愈”。Marriott的判断之所以正确，是因为Screen的病根在架构——而架构病，只有推倒重来这一种药。当然，推倒重来也需要一个前提：你真的理解旧系统为什么烂。Marriott理解了，因为他用了二十年，背下了每一个快捷键，比任何人都清楚问题出在哪里。

## 尾声：雨还在下

写完这一段，我想起2004年冬天，我自己第一次在远程服务器上弄丢所有进度的经历。SSH断开的那一刻，我跑了整整两天的数据任务，和我精心布局的四个终端窗口，一起消失了。我重新连上服务器之后，盯着空白的终端看了十分钟——不是愤怒，是一种深深的无助。当时我也不知道世界上有tmux，只知道以后每次跑长任务，都得写nohup加&再加disown，像绑三层安全绳一样，小心翼翼，如履薄冰。后来我才慢慢明白，那种“断线即一切归零”的恐惧，是每一个远程工作者都经历过的噩梦；而所有试图解决这个噩梦的工具——无论Screen还是tmux——本质上都在做同一件事：把“连接”和“会话”这两样东西，彻底分开。只是Screen做到了七成，而它的继任者，要把这件事做到十成。

Marriott比我幸运。他知道问题出在哪，也知道怎么解决。

2007年秋天，他的原型跑通了。几个月后，他把原型打磨成了一个可以发布的东西。2007年11月20日，第一版tmux公开发布，名字起得很直白：Terminal MUltipleXer，终端复用器。那一年，iPhone刚刚发布，移动互联网的浪潮正要掀起，大多数开发者还在用SVN甚至CVS管理代码，没有人会注意到，一个北爱尔兰人在自己的电脑上写完了一个注定要改变终端使用方式的小工具。

没人能想到，这个从北爱尔兰一个开发者的“不爽”里长出来的东西，会在之后的十几年里，从个人项目长成OpenBSD的基础系统组件，再从OpenBSD走向全世界的服务器和开发者的日常工具箱。但那是后面几章的故事了。

此刻，2007年的那个夜晚，Marriott只是把他的代码推到了网上。连他自己也未必意识到，那个“怪室友”的时代，正在悄悄地翻过一页。雨还在下，但贝尔法斯特的这间屋子里，已经亮起了一盏新的灯。

## 注释

1. undeadly.org 对 Nicholas Marriott 的访谈（2009 年 7 月）：Marriott 自述重度使用 Screen、背下全部快捷键、Screen 代码“非常凌乱且（当时）无人维护”、向 Screen 添加功能不现实、目标之一是让代码库可读、可扩展。
2. softpanorama.org 的 GNU Screen 历史资料：Screen 1987 年 3 月 20 日由 Oliver Laumann（柏林工业大学）发布，最初名为 BSD screen，发布于 net.sources；1990 年交接给 Jürgen Weigert 与 Michael Schroeder，后迁入 GNU 项目；版本时间线（3.6.0、3.7.1、3.9.4、3.9.15、4.0.2、4.0.3）；2004 年开发停滞；2008 年 8 月最后发布 4.0.3；垂直分屏从未进入生产版；Screen 比 Linux 年长。
3. 维基百科 tmux 词条：2007 年 11 月 20 日第一版公开发布，命名 tmux 即 Terminal MUltipleXer。
4. 本书素材《GNU Screen 发展历史（资料摘编）》，来源于 softpanorama.org。

---

# 第2章 “nscr”原型与第一版发布

代码和人不一样。代码不会抱怨，但代码会说实话。

这句话我琢磨了很多年。人会说场面话，会在汇报里把六分的成绩说成九分，会在评审会上微笑着接受一个自己都看不下去的架构，会在离职时把所有的委屈都咽回肚子里。代码不会。代码就那样躺在那里，一行一行地陈述事实：这里是谁写的，当时是怎么想的，这个功能当初为什么被塞进来，那条路为什么走不通。你读一个人的代码，就是在读他最高兴和最沮丧时的真实想法——代码是一个开发者最诚实的自白。

Marriott的“nscr”原型，就是一个说实话的代码。它只有几百行，功能简陋得几乎不好意思给别人看——能创建一个会话，能开几个窗口，能把屏幕切几块，仅此而已。但正是这几百行代码，安静地回答了一个关键问题：**client/server架构在终端复用这件事上，确实跑得通。**

这一章要讲的，就是这“确实跑得通”五个字背后的故事：一个只花了几星期写出来的原型，如何验证了一条与二十年老工具截然不同的架构路线；一个临时起的名字，如何变成了一个即将陪伴全球开发者十几年的名字；一次安静得几乎无人察觉的发布，如何成为一场漫长革命的起点。一个几星期的原型，最终颠覆了一个二十年老工具——这条路上没有轰轰烈烈的宣言，只有一次次安静而正确的决定。

## 第一节 原型思维：先写一个能跑的“nscr”

2007年的那个夏天，Marriott大概是在某个深夜写下了第一行原型代码。那时候的Screen已经20岁了，比他本人入行的时间还长。他面对的选择很朴素：要么在Screen的屎山里继续挖，指望有一天能挖出一条隧道；要么另起炉灶，用一块干净的地基重新盖。

他选了后者。

但请注意他选的方式——他没有一上来就埋头苦干“完整产品”，而是先写了一个快速原型，取名“nscr”，也就是new screen的缩写。这个命名带着一种北爱尔兰式的务实：不花哨，不绕弯子，就是告诉自己和任何一个可能看到它的人：这是一个新的Screen。但“新”在哪，当时谁也说不清楚。连Marriott自己，可能也只是模糊地觉得，代码应该更干净一点，架构应该更合理一点，用户不需要背一本快捷键字典才能用得顺手。

原型只花了几星期【1】。这不是因为工作量小，而是因为Marriott从一开始就没打算复刻Screen的全部功能。他只是把Screen里自己最依赖的那部分——多会话、多窗口、能断线重连——用client/server的模型重新实现了一遍。剩下的功能，等需要的时候再说。

这个“等需要的时候再说”，后来被证明是一个极其关键的设计决策。Screen的悲剧在于它什么都想做，结果什么都没做利索。而tmux从第一天起就给自己画了一条清晰的边界：**只做终端复用这一件事，把这件事做干净。**

几个星期，能验证什么？恰恰能验证最重要的东西：架构路线本身。写一个功能齐全的终端复用器，可能需要一年；但验证“client/server能不能承担起终端复用这件事”，几百行代码就够了。你要创建会话、开几个窗口、把屏幕切几块，然后模拟一次断线重连——如果这几个最基本的动作能跑通，如果detach之后server里的进程还活着，如果重新attach之后界面还在，那么这条架构路线就是可行的。剩下的，全是工程问题，而工程问题可以用时间解决。架构问题一旦选错，花多少时间都补不回来。

这就是原型思维的本质：**用最小的成本，回答最大的问题。** 在投入几个月甚至几年的工程时间之前，先花几个星期确认方向没有错。这听起来像常识，但你在真实世界里看到的几乎全是反面教材——无数项目在没验证核心假设之前就开始大规模铺开，然后在两年后推倒重来。Marriott用几个星期，替自己省下了可能是一整年的试错成本。

而且，原型还有一个隐蔽的好处：它天然地帮你拒绝了过度设计。因为原型要快，所以你只会做最必要的事；因为原型要快，所以你不会去考虑那些“也许以后用得上的功能”；因为原型要快，所以你的每一次取舍都必须问自己：这个功能，真的支撑得了那条核心假设吗？Screen花了二十年把自己变成了一团毛线球，而tmux的原型在几个星期里就学会了克制——这个“瘦”的起点，决定了它日后“壮”的方向。后来tmux走过的每一步，几乎都能从这几个星期的原型里找到源头：它现在有多克制，当初的原型就有多简洁。

## 第二节 架构革命的起点：为什么选 client/server

如果说原型思维决定了tmux“怎么起步”，那么client/server这个选择，决定了tmux“往哪走”。

先看看对面的Screen。Screen采用的是monolithic（单体）进程模型——每个Screen会话是一个独立的进程，所有功能揉在同一个进程里，会话之间彼此隔离。这个模型在1987年Screen诞生的时候没有问题，一台机器上的终端数量有限，进程结构简单直接。但到了2007年，多任务、多窗口、多终端的复杂场景让它的局限越来越明显。

monolithic架构的问题，在于它把所有的“状态”和“视图”绑在了一起。你想把一个窗口从一个会话移到另一个会话？Screen做不到，因为窗口和会话长在同一个进程的身体里。你想让两个人在两个终端上共享同一个会话？Screen也能做到，但实现得磕磕绊绊——因为“共享”这件事在monolithic模型里不是原生能力，而是后天的修补。更致命的是，你想在这个架构上加任何新功能，都得动全局：那团毛线球里，每一根线都和另一根线缠在一起，你抽不出任何一根来单独研究。这就像一间堆满旧家具的阁楼，没人敢轻易挪动任何东西，因为你永远不知道抽掉哪块木板会让整层楼塌下来。

Marriott在Screen的代码里受够了这种束缚。他是重度Screen用户，背下了全部快捷键，整个工作流都建立在这个工具之上——恰恰因为用得太深，他才比任何人都更清楚：这栋老房子的天花板，已经顶到头了。任何对架构的修补都只能带来新的修补，二十年里一层叠一层的补丁，已经把这座房子改得面目全非。

所以他做了一个在2007年看来相当激进的决定：不修老房子，也不照抄老房子的图纸，而是换一种完全不同的建筑方式——**client/server**。

这个模型的精妙之处，在于它把两件被monolithic模型糊在一起的事情彻底分开了：**状态**与**视图**。

状态，是那些真正重要的东西：你开了哪些会话，每个会话里有哪些窗口，每个窗口里有哪些面板，每个面板里跑着什么进程，进程的输出到了哪一行，光标停在什么位置。视图，则是你此刻在屏幕上看到的那个画面——它只是一个“看”的状态的方式，会随着你的连接、断开而出现和消失。

如果你能理解这个分离，你就理解了tmux的灵魂。用一个贴切的比喻：这就像一家饭店的厨房。server是大厨，在后台一刻不停地做菜；client是客人，坐在桌前享用，吃完离桌。客人走了，桌子空了，但厨房还在运作——灶上的火还开着，锅里的汤还在咕嘟咕嘟地冒泡。下次你再走进这家饭店，坐回那张桌子，端上来的还是你走之前那道菜，火候一分不差。

这正是tmux要做的事：**把“厨房”和“餐桌”分开，让厨房永远运作，让餐桌随来随走。** 而这个比喻里的每一个字，都是对Screen那个monolithic模型的反驳——Screen的问题是，厨房和餐桌焊死在一起，客人一离桌，厨房就得熄火。

## 第三节 核心设计决策一：Unix domain socket——client 只是 server 的“视图”

架构选定了，接下来是落地。client和server之间怎么通信？Marriott的选择是Unix domain socket。

一个server进程在后台常驻，管理所有的会话、窗口、面板；多个client通过Unix domain socket连接这个server。socket是同一台机器上进程间通信的通道——它不经过网络，不经过TCP/IP栈，就是本地文件系统上的一个特殊文件，两个进程通过它直接对话。对tmux来说，这个socket通常躺在系统临时目录里，以当前用户的身份命名，只有同一个人能够访问——这既是通信通道，也是天然的权限边界。

client和server各自做什么？分工极其干净：server持有全部状态，管理所有会话、窗口、面板，以及它们背后那些真正在跑的进程；client不做任何“持有”，它只做两件事——把你敲的按键发给server，把server返回的屏幕画面画在你的终端上。换句话说，**client只是server的一个“视图”。** 你关掉client，视图消失，server和它管理的所有进程纹丝不动；你再开一个client，视图重新出现，你看到的是和之前一模一样的世界——一样的会话、一样的窗口、一样的光标位置、一样还在跑的编译任务。

这意味着什么？意味着你可以在办公室的电脑上attach到一个tmux会话，回家后从家里的电脑再attach到同一个会话，看到的是完全一样的界面、一样的光标位置、一样运行中的进程。**终端不再是进程的“容器”，而只是进程的一个“窗口”。** 容器的生命周期跟内容绑定——容器碎了，内容就没了；窗口碎了可以换一个新的，窗口背后的那面墙（进程）仍然纹丝不动。

这个比喻值得再咂摸一遍。在Screen的世界里，会话和终端是绑死的：SSH断了，终端没了，会话也就跟着没了。而在tmux的世界里，会话住在server里，终端只是你伸进去看的那扇窗——窗户可以碎，可以关，可以换一扇更大的，墙上的进程毫发无损。

架构差异带来了两个直接的好处。第一，增加新功能时不需要重构全局——因为client和server的边界清晰，你改server的逻辑，不影响client怎么显示；你在client上加一种新的显示方式，也不需要动server的核心。第二，多客户端共享会话成为原生支持——两个人同时attach到同一个会话，就是天然的结对编程环境；一个人在家里的电脑上看着服务器上的编译进度，另一个人在办公室的电脑上操作同一个窗口，看到的画面完全同步。

在2007年，这两点几乎是对Screen的降维打击。Screen也能实现某种程度的多终端共享，但那是功能性的、修补出来的；tmux的多客户端共享，是架构性的、与生俱来的。这就是“从架构上解决问题”和“在架构上打补丁”的区别——前者一次到位，后者永无宁日。而这一切的起点，仅仅是选对了进程之间的通信方式。

这里还有一个值得反复咀嚼的隐喻层次。Screen想的是：我要为每个用户、每个会话、每个终端都准备一个完整的、独立的世界；而tmux想的是：世界只有一个，你们看到的都是它的一面。前者是“多造几个世界”，后者是“让同一个世界拥有无数个观察窗口”。当这两个思路在2007年的终端里相遇，高下立判——维护一个世界的成本，永远低于维护十几个世界，更低于在十几个世界之间搬运东西。这个差异初看只是实现细节，日积月累，就成了产品哲学层面的分水岭。

## 第四节 核心设计决策二：命令语法统一——命令行能敲的，配置文件里就能写

如果说client/server是tmux的骨架，那么“语法一致”就是tmux的肌肉记忆。

Screen有一个让所有重度用户抓狂的设计：它的配置语法跟它的命令行语法是两套完全不同的体系。你可以在命令行里敲`screen -S foo`创建一个会话，但如果你想在配置文件里做同样的事，你得去查另一本手册。命令行里能用的参数，配置文件里未必认；配置文件里的写法，命令行里未必接受。这就好比你的汽车方向盘和刹车用的是两套完全不同的逻辑——一个往左打是左转，另一个往左踩反而是加速。你说它能用吗？能用。你说它折磨人吗？非常折磨。更糟糕的是，这两套语法的文档还都语焉不详，查一个功能怎么用，比实现它还要费劲。

而tmux从一开始就确立了一个原则：**命令行能敲的，配置文件里就能写，语法一模一样。** 这个原则在今天听起来理所当然，但在2007年，这几乎是一种奢侈。Marriott的设计直觉很朴素：用户不应该为同一个目标记忆两套语法。一个心智负担的消解，能让整个工具的日常体验顺滑十倍。

这个原则还带来了一个意想不到的连锁效应：配置文件本身变成了可执行的文档。你想知道tmux支持哪些命令？不用翻手册，在命令行里敲一条不存在的命令，错误信息会列出所有可用的命令；你想知道某个功能怎么配？直接在命令行里试，试通了，把同样的语句抄进配置文件，就永久生效了。配置不是背诵出来的，是摸索出来的——而摸索的成本，因为语法一致而低到了几乎可以忽略。今天你打开任何一份tmux配置教程，看到的都是形如`bind-key`、`set-option`这样的命令——它们和你在命令行里敲的命令，是同一个东西。

> 💡 **独思时刻：** 我见过太多号称“功能强大”的工具，最后都死于配置门槛。2008年前后，我尝试在FreeBSD上配置一个Screen环境，折腾了一整个下午——光是把`.screenrc`调成一个顺眼的状态就花了三个小时，中间还因为一个分号的位置不对导致整个配置文件失效。那时候我想：这玩意儿的功能确实强，但它的设计者在向我传达一种“你不配用我”的傲慢。后来换到tmux，十分钟就配完了。区别在哪？不是功能多寡，是设计者有没有站在我这一侧思考问题。功能的强大如果不能转化为用户抵达它途中的平顺，那强大本身就是一种诅咒。

## 第五节 核心设计决策三：代码库可读、可扩展——拒绝 2 万行毛线球

第三个决策，是对前面两个决策的守护：**代码库可读、可扩展。**

Screen的代码库大约有两万行C代码。两万行听起来不多，但如果这两万行是二十年里被十几个人各按各的习惯堆出来的，那它就是一团越滚越大的毛线球——每一行都认识，但整团线拎不起来。Marriott后来在访谈里说得很平静：“增加这个功能到Screen里是不现实的，让代码库可读、可扩展成了我的目标之一。”【1】用大白话说就是：与其在一栋危楼里加电梯，不如从打地基开始盖一栋新的。

可读，意味着什么？意味着一个新人拿到代码，能顺着读下去，不用靠猜；意味着每个函数的名字都在说人话，而不是用缩写再缩写；意味着模块的边界清晰，你可以只看某一个部分而不必理解全部；意味着代码本身就是文档，注释只用来解释“为什么”，而不是复述“是什么”。

可扩展，意味着什么？意味着加一个新功能不需要重构全局；意味着别人可以看懂你的代码并且提交patch；意味着项目不会随着功能的增加而腐烂。这两条放在一起，其实是同一个目标的两面：**让代码保持小、保持干净、保持可以被任何人接手。** 后来的历史证明了这个决策的分量——两年后，OpenBSD的创始人Theo de Raadt会亲自审阅tmux的代码，花了两小时，只发现一两个次要问题【2】。一个以挑刺为职业信仰的人给出这样的评价，说明“可读”不是一句口号，而是刻进了每一行代码的肌肉里。当然，那是下一章的故事；但你可以想象，如果没有2007年这个“可读、可扩展”的执念，两年后的那次审阅，很可能会是另一个结局。

而且请注意，这个决策在当时是反潮流的。2007年的开源世界里，很多项目信奉的是“功能为王”——先堆功能，架构问题以后再说。而tmux选择了一条相反的路：先把房子盖结实，再谈装修。这个“反潮流”，短期的代价是功能少、发布慢；长期的回报是它成为了一个别人愿意在其上继续盖楼的地基。今天回头看，tmux活下来了，而很多当年功能比它多得多、发布比它勤快得多的项目，早已无声无息。**功能可以被时间追上，架构决定了你能否等到被追上的那一天。**

## 第六节 从 nscr 到 tmux：命名由来与首次提交

原型跑通了，方向验证了，接下来的问题是：这个东西，叫什么？

“nscr”这个名字从一开始就带着临时工的自觉——它是“new screen”的缩写，是一个占位符，是一句“我是替代品”的宣示。它诚实地说明了动机，却没有说明身份。Marriott需要一个真正的名字。

2007年10月10日，tmux的首次代码提交正式记录在版本历史中【3】。这一天没有发布会，没有公告，甚至可能连一行像样的文档都没有——只有一次安静的提交，和代码里那个新名字：**tmux，Terminal MUltipleXer的缩写**，终端复用器【4】。

比起“new screen”那种“我是替代品”的宣示，“tmux”听起来更像是一个独立的、有自己身份的东西。它不再定义自己“相对于谁”，而是定义自己“是什么”——一个处理终端复用这件事的工具。这个微妙的名字转变，其实是一个心理转变：从“我要取代Screen”，变成了“我要成为自己”。前者是追赶者的心态，后者是开创者的心态。一个名字，悄悄完成了这个转变。

从第一次提交这个动作里，还能读出另一层意思：Marriott已经下定决心把这个项目做下去了。原型是可以扔掉的，改个名、正式提交、建立版本历史，意味着他愿意对这个代码负责了——从这一刻起，nscr不再是硬盘上一个可以随时删除的文件夹，而是tmux这个将要存在很多年的项目的第一页史书。版本历史里那个时间戳，是后来所有故事的原点。

## 第七节 2007 年 11 月 20 日：第一版公开发布

一个多月后的11月20日，tmux第一版公开发布【4】。

说句实话，第一版发布的时候，世界上几乎所有的开发者都不知道它的存在。那一年，iPhone发布还不到半年，GitHub甚至还没有上线，大部分人还在用SVN甚至CVS管理代码。一个北爱尔兰OpenBSD开发者写的终端小工具，能引起多大动静？答案是：几乎为零。没有媒体报道，没有讨论帖刷屏，没有“下一个Screen”的惊呼——它只是安静地出现在了网络上，像一颗种子落进土里，没人注意到。

但关注的人很少，不等于影响很小。第一批用户——主要是OpenBSD圈子里的开发者——反馈出奇地好。他们用tmux，不是因为它功能多，而是因为它**不别扭**。这个评价看似平淡，其实是终端工具能得到的最高评价之一：一个好工具应该“消失”——用的时候不打扰你，出事的时候兜住你。第一批用户说不出什么华丽的理由，他们只是发现：断线重连稳了，配置不用背两套语法了，代码看着舒服了。

这就是tmux第一版最核心的“产品力”：它没有试图在所有方面超越Screen，它只是在最关键的地方——配置的一致性——做得比Screen好了一个量级。再加上架构的先天优势，第一版tmux在功能上还远谈不上完善：没有垂直分屏（那是后来才加的特性），没有丰富的状态栏自定义，没有插件系统。但它已经有了一个决定它未来十几年生命力的内核：**干净的代码、清晰的架构、一致的配置体验。**

2007年的那个冬天，Marriott大概也没想太多。他只是把代码推到了网上，然后继续日常的开发工作。连他自己也未必意识到，那个让他不爽了多年的“怪室友”，从这一刻起，有了一个真正像样的“邻居”。这个邻居还很年轻、很简陋，但它的地基打得足够扎实。如果当时有人告诉他，这个邻居十几年后会成为行业标准，他大概会以为对方在开玩笑——但历史从来不按“大概”来书写。

## 第八节 `0.x` 时代的快速试错：`0.4` 到 `0.9` 的能力补全

第一版发布，不是终点，甚至算不上起点——它只是“能跑”和“好用”之间那段漫长旅程的第一步。

接下来的两年，tmux进入了`0.x`时代。版本号带着一种诚实的谦逊：`0.x`，承认自己还在儿童期。但儿童期恰恰是一个人长得最快的时期——tmux也是这样。从`0.4`到`0.9`，关键能力被一块一块地补全【5】：窗口和面板的操作体系逐渐成型，复制模式与滚动历史让“在终端里往回翻”不再是一场灾难，状态栏开始显示会话名、窗口列表、时间，键绑定可以按自己的习惯重新定义，配置语法在真实使用的打磨中一点点完善。每一次发布，都在磨平一个棱角；每一个版本，都比上一个版本更接近“日常可用”。

这段时期的节奏是“快速试错”。开源项目在早期有一个天然的反馈循环：发布一个版本，用户拿去用，发现痛点，报上来，你修掉或加上，再发布。`0.x`时代的tmux把这个循环跑得飞快。因为架构干净，改起来快；因为用户少而精，反馈质量高；因为Marriott一个人就能掌控整个代码库，决策链短到没有中间层。一个想法从用户嘴里到代码里，可能只需要几天。

这个时期的另一个特点是“边界感”。`0.x`时代的tmux没有因为功能缺口大就饥不择食——它依然只做终端复用这一件事。它拒绝成为瑞士军刀，拒绝把串行接口、文件传输、窗口管理器这些“也许有用”的功能塞进来。每一次有用户提出超出边界的需求，Marriott都要权衡：这是tmux该做的事，还是用户该用其他工具做的事？这个权衡的习惯，在`0.x`时代养成，在之后的十几年里一直保持着——后来的那句“不搞膨胀”【6】，源头就在这里。

快速试错也有它的代价：稳定性波动、接口频繁变化、文档跟不上。但这一切，对于一个刚满一岁的项目来说，都是成长的学费。而且正因为`0.x`时代的底子打得好，当两年后那个决定命运的机会来临时，tmux才接得住——它已经从一个“验证可行的原型”，长成了一个“可以被放进操作系统基础系统的工具”。

如果给`0.x`时代画一条成长曲线，你会看到一条清晰的上升轨迹：第一个版本能跑，第二个版本不崩，第三个版本开始有人愿意天天用，第四个版本有人开始为它写配置模板，第五个版本有人开始向同事推荐。功能列表在变长，但更重要的变化发生在看不见的地方——代码在一次次重构中变得更干净，边界在一次次拒绝中变得更清晰。等到`0.9`发布的时候，tmux已经不再是一个需要作者向人解释“这是什么”的玩具，而是一个可以放进严肃生产环境的工具。从“能跑”到“好用”之间那段最危险的路，tmux走得不算快，但每一步都踩实了。

## 第九节 早期社区：邮件列表、IRC 与第一批贡献者

`0.x`时代的tmux，社区小得可怜，但形态已经完整。

项目托管在SourceForge上，这是那个年代开源项目的标准去处。交流发生在两个地方：邮件列表和IRC。邮件列表是慢节奏的异步讨论——有人遇到问题，发一封邮件，可能半天后收到回复，也可能被Marriott直接回答；IRC是快节奏的同步闲聊——开发者们挤在一个频道里，讨论bug、讨论设计、讨论下一个版本该做什么。没有GitHub Issues，没有Discord，没有现代开源项目标配的一切，但该有的协作模式已经齐了：有人报bug，有人提需求，有人直接甩过来一个patch。

第一批贡献者大多是OpenBSD圈子里的人。这并不奇怪——Marriott是OpenBSD的贡献者，他的第一版代码最先在OpenBSD的圈子里传开；而OpenBSD的文化又以代码质量为信仰，这个圈子里的人看到一份干净的代码，第一反应不是“这工具不错”，而是“让我看看能不能给它提个patch”。于是，来自四面八方的bug报告、功能请求和补丁开始涌进Marriott的收件箱。

这些早期用户有一个共同特点：他们不是来“试用新玩具”的，而是来“解决真实问题”的。他们的服务器上跑着真实的任务，他们的SSH连接会真实地断开，他们真的需要在不丢失进程的情况下重新连回去。正因为需求真实，他们的反馈才格外有价值——每一个痛点都是被验证过的痛点，每一个请求背后都有一个真实的、正在流血的场景。Marriott从这些反馈里学到的，远比他一个人闭门造车能想到的多。后来tmux的许多经典功能都源于用户请求——这个“用户驱动”的基因，就是在这个时期种下的。

这个时期的社区规模，放到今天简直寒酸——可能只有几十个人在认真使用。但正是这几十个人，构成了tmux的第一批“压测用户”：他们在各种古怪的终端、各种奇怪的用法、各种意想不到的场景里使用tmux，把早期版本的棱角一个一个磨圆。后来的事实证明，工具的命运往往在它最不起眼的时期就已经注定——第一批用户的质量，决定了项目早期的进化方向。一个面向最挑剔用户的工具，会变得值得挑剔；一个面向随便用用的人的工具，也会变得只配随便用用。

## 第十节 💡 独思时刻：原型的价值——先跑起来再完美

写到这里，我想停下来，好好说说“原型”这件事。

我见过太多人，把“完美主义”当成不开始行动的借口。他们花几个月画架构图，花几个月写设计文档，花几个月讨论技术选型，然后项目还没开始，热情已经烧完了。我也见过另一种人，他们什么文档都不写，撸起袖子就写代码，但写出来的东西自己都看不下去，推倒重来三次，最后连自己都不知道自己在干什么。

Marriott的nscr给了我们第三种答案：**先跑起来，再完美。** 但“先跑起来”不是“随便写写”——它有一个明确的前提：你要清楚自己到底在验证什么。nscr验证的是“client/server能不能承担终端复用”，所以它只需要几百行代码、几个星期时间。它不需要完美的错误处理，不需要完整的文档，不需要考虑十年后的扩展性——它只需要回答一个问题：这条路走不走得通。走通了，剩下的一切都可以在前进中解决；走不通，你只损失了几个星期，而不是一整年。

这个思路对我的影响，比tmux本身还要大。我现在写任何新项目，都会先问自己：这个项目最大的风险是什么？是架构？是性能？是某个关键假设？然后我会用最小的时间把这个风险验证掉，再决定要不要投入。有一次我接了一个数据迁移的活，客户希望我先写一份五十页的方案文档。我花了一天写了个能跑的最小原型，把一条真实的数据从旧库迁到新库，然后在方案评审会上直接演示。五十页文档说不清楚的问题，三分钟演示就说清楚了。

原型的价值，不在于它“小”，而在于它“快”——快到你敢试错，快到你输得起。Marriott用几个星期验证了client/server，这个验证的成本，低到任何一个开发者都负担得起。而它的回报，是一个二十年老工具都撼动不了的根基。**先跑起来再完美，不是妥协，是战略。**

> 💡 **独思时刻：** 后来我每次听到“先想清楚再动手”这句话，都会想起nscr。想清楚当然没错，但“想”本身也是有成本的——想得越久，你离真实世界的反馈就越远。原型不是想不清楚的借口，恰恰相反，它是想清楚的最快方式：让代码替你回答那些你答不上来的问题。nscr用几个星期回答了“client/server行不行”，这个答案，比任何架构文档都有说服力。

## 第十一节 💡 独思时刻：命名是产品的第一行文档

最后，我想聊聊名字。

“tmux”这个名字，在2007年10月10日第一次出现在代码里的时候，大概没人觉得它有什么特别的。它甚至不是个词，是一个缩写：Terminal MUltipleXer，终端复用器。但这个名字，是产品的第一行文档。

你想想看：一个用户第一次听说这个工具，他记住的第一个东西是什么？不是快捷键，不是架构，不是配置语法，是名字。“screen”这个名字告诉你它是什么——屏幕，把你和终端之间的那层东西。“tmux”这个名字告诉你它是什么——终端复用器，把终端复用的技术本质写进了名字里。一个好名字，让用户在一秒钟内理解这个工具存在的理由；一个坏名字，让用户在第一秒就产生了错误的预期。

但更让我在意的，是“tmux”和“nscr”的对比。“nscr”——new screen——这个名字说的是“我是新的那个Screen”，它的全部身份都建立在和别人的关系上。而“tmux”——终端复用器——这个名字说的是“我是干什么的”，它的身份建立在自身的功能上。从nscr到tmux，是一次悄无声息的改名，更是一次身份的确认：我不再是任何人的替代品，我就是我自己。

这让我想起自己给项目起名字的历史。年轻的时候，我给自己的第一个开源项目起名叫“`better-notes`”——“更好的笔记”。这个项目最后死了，死因有很多，但回头看，这个名字本身就是一种诅咒：它把自己定义成了别人的替代品，于是永远活在比较里，永远在追赶，永远差一口气。后来我学乖了，给项目起名字只问一个问题：如果全世界只有这一个工具，这个名字还成立吗？如果成立，它就是一个好名字。tmux，就是这样一个名字。

**命名是产品的第一行文档，因为它决定了用户认识你的第一印象，也决定了你自己认识自己的方式。** 一个叫“new screen”的工具，永远在回答“我和Screen有什么不同”；一个叫“tmux”的工具，只回答“我能为你做什么”。前者是跟随者的自我设限，后者是创造者的自我定义。

> 💡 **独思时刻：** 工具的名字里藏着工具的命运。一个叫“new screen”的工具，注定要永远活在前任的影子里；一个叫“tmux”的工具，从诞生的第一天起就是它自己。所以每当我给新工具起名，我都会先问自己：这个名字，是在说明它“是谁的替代品”，还是在说明它“是什么”？答案，往往决定了它未来十年的路。

## 尾声：一颗种子

从nscr到tmux，从几个星期的原型到第一版公开发布，Marriott在2007年完成的事情，用一句话概括就是：他用最小的成本，验证了一条最正确的路。

他验证了client/server的可行性——这条路后来被证明是tmux对抗Screen的最大资本；他确立了三项设计原则——socket通信、语法统一、代码可读，这三条原则撑起了tmux此后十几年的骨架；他起了一个好名字——tmux，从此不再是谁的替代品。而这一切，在2007年的冬天，只是被安静地推到了网上，几乎没有引起任何波澜。

但种子已经种下了。两年后，它会在一场OpenBSD的hackathon上被人偶然提起；再之后，它会进入OpenBSD的基础系统，会被Red Hat选为默认推荐，会成为全球开发者日常工具箱里不可或缺的一部分。当然，那是后面几章的故事了。

此刻，2007年的冬天，Marriott大概只是像往常一样，在自己熟悉的终端里，敲下那个新名字，看着它跑起来。他不会想到，这个从“不爽”里长出来的小东西，会在十几年后成为一个时代的标准。他只是在做自己认为对的事——写一个可读的、可扩展的、不用再忍受“怪室友”的替代品。

而历史，就是从这样的“只是”开始的。


*注：本章关于Marriott撰写原型的个人场景细节，系基于2007年技术开发环境的合理推演，非一手信源；核心事实（nscr原型的开发周期、首次提交与发布日期、命名含义、client/server架构与设计原则）均来自公开访谈与版本记录，与全书事实清单保持一致。*

**本章资料来源：**

【1】`undeadly.org`对Nicholas Marriott的访谈（2009年7月）

【2】OpenBSD Journal关于tmux导入base的报道（`undeadly.org`）

【3】tmux版本历史记录（首次提交，2007年10月10日）

【4】tmux维基百科词条（第一版发布与命名，2007年11月20日）

【5】tmux `0.x`版本发布记录

【6】Marriott关于功能边界的公开表述：“bloat due to features like serial line I/O is not going to happen”

---

# 第3章 命运的转折——OpenBSD接纳与社区成长

2009 年夏天，tmux 已经两岁了。在开源世界里，两岁不算老，但也早过了“玩具项目”的保鲜期。它在小圈子里有了些名气——有人在 SourceForge 上发现了它，试了试，觉得“这玩意儿居然能用”，然后就在自己的服务器上装了起来。其中一个人叫 Paul Irofti，OpenBSD 开发者，他干了一件改变 tmux 命运的事。

从 2007 年 11 月 20 日第一版公开发布算起【7】，到 2009 年的这个夏天，tmux 真正走过的时间其实只有一年半多一点。这一年半里，它没有发布会，没有融资新闻，没有官方网站上光鲜的截图。它的全部宣传，就是代码本身——一份让每一个用过的人觉得“不别扭”的代码。而在 OpenBSD 这个以挑刺为荣的圈子里，“不别扭”恰恰是最稀缺的品质。第一批用户大多是 OpenBSD 圈子的开发者，他们用 tmux，不是因为它功能多，而是因为它不别扭；他们向朋友推荐 tmux，也不是因为收了什么好处，而是因为自己在某次断线重连时被它救过一次。

接下来的故事，从一场夏天的聚会开始。这场聚会改变的不只是 tmux 的命运，还有整个终端复用世界的格局。

在展开这场聚会之前，不妨先记住一个事实：命运这个东西，很少以“重大事件”的面目出现，它更多的时候，是藏在某个人随口说出的一句话里、某个瞬间的灵光一闪里、某个看起来毫不起眼的决定里。2009 年夏天的这场聚会，就是这样一个瞬间。

## 第一节 2009 年 OpenBSD hackathon：Paul Irofti 的偶然提及

每年夏天，OpenBSD 都会在世界某个城市举办一场 hackathon。这场聚会的氛围有点像程序员版的“夏令营”——一群人挤在同一个房间里写代码、争论设计、喝到很晚，第二天接着写。hackathon 这个词如今已经被创业公司用滥了，但在 OpenBSD 的世界里，它保持着最原始的含义：一群最较真的人，聚在一起，解决最麻烦的问题。房间里没有路演，没有投资人的目光，只有键盘声、争论声，和偶尔传来的“这不对”的拍桌声。

2009 年的那场 hackathon 上，一个叫 Paul Irofti 的 OpenBSD 开发者，随口提了一句：“你们试过 tmux 没？我觉得挺靠谱的。”

这句话有多随意？随意到如果当时没有人接话，它就会像无数句 hackathon 上的闲谈一样，消失在空气里。但这句话又有多重要？重要到十几年后回头看，它几乎是整个终端复用世界格局的转折点。

为什么一句闲谈能撬动这么大的事？因为 Irofti 不是普通用户，他是 OpenBSD 的开发者；他也不是在咖啡馆里跟朋友闲聊，他是在 OpenBSD 的 hackathon 上，当着团队的面提起。在这个圈子里，一个开发者的“我觉得挺靠谱”，等于一次实名担保。

于是，团队开始讨论一个听起来有点疯狂的问题：要不要把这玩意儿直接放进 OpenBSD 基础系统里？

要理解这个问题的分量，得先理解 OpenBSD 的“base”是什么。OpenBSD 的软件世界分成两半：一半叫 base，也就是基础系统——内核、shell、编译器、系统工具，它们随操作系统一起发布、默认安装、接受最严格的安全审计；另一半叫 ports，也就是第三方软件包——它们像超市货架上的商品，要装什么自己挑，装坏了算你自己的。进入 base，意味着你的代码不再是“货架上的商品”，而是“房子本身的结构”。

在 OpenBSD 的世界里，进入 base 几乎是“最高荣誉”的入场券。OpenBSD 以其对代码质量的偏执闻名——每一行进入基础系统的代码都会被反复审阅，任何一点“不够干净”的痕迹都会被拒之门外。他们连自己写的代码都信不过，更别说一个外人从网上扔过来的项目了。历史上，多少雄心勃勃的项目想挤进 base，最后都被那扇门弹了回去。

而 tmux，一个北爱尔兰开发者个人写的小工具，居然被团队正儿八经地摆上了讨论桌。这个事实本身，已经说明了很多东西。

这里需要多说一句 OpenBSD 的文化。这个操作系统从诞生起，就把“正确性”和“简洁性”当成信条——它追求“默认安全”，它相信“少即是多”。在这样一个系统里，任何进入 base 的组件都必须回答一个残酷的问题：你真的值得那一点安装体积、那每年固定的人力维护成本、那份永久的安全审计义务吗？tmux 面对这个问题时交出的答卷，是一份两岁却已经足够干净的代码。而这份答卷之所以能被认真对待，离不开 Irofti 那句“我觉得挺靠谱的”——在一个把“靠谱”当作最高评价的圈子里，这句话不是客套，是担保。

## 第二节 Theo de Raadt 的两小时审阅

讨论归讨论，在 OpenBSD，任何关于代码质量的重大决定，最终都要过一个人那一关——Theo de Raadt，OpenBSD 的创始人。

Theo 以严苛著称。这不是形容词，是动词——他对代码的严苛，是那种会让新手开发者手心冒汗的严苛。在他的世界里，代码没有“差不多”，只有“对”和“错”；一个差一的边界错误、一行没有对齐的缩进、一处含糊的注释，都可能换来一顿毫不留情的批评。OpenBSD 圈子里流传着无数关于 Theo 的传说，真假难辨，但核心信息是一致的：别想在他面前蒙混过关，因为他会一个字一个字地看你的代码。

所以，当团队决定认真考虑 tmux 时，Theo 说了一句让在场所有人都安静下来的话：他决定亲自看一遍。

那天下午的房间里，气氛忽然变了。Theo 把 tmux 的代码拉下来，开始逐文件翻看。房间里的人各忙各的，但余光都忍不住往那个方向瞟——没人敢过去打扰，也没人真的走得开。两个小时，说长不长，说短不短，够看一部电影，够跑一次长途，也够一个偏执狂把一份陌生项目的代码从头到尾审一遍。

在这两个小时里，没有人知道 Theo 心里在想什么。他可能已经看到了那个“一两个次要问题”，可能在心里默默把 tmux 和系统里已有的每一个组件作着比较，也可能只是在确认一件事：这份代码，值不值得它将来要承担的维护责任。两小时的沉默，对 tmux 来说是判决前的等待；而对 Marriott 来说，这可能是他人生中最漫长的两小时——当然，这些都是后人的推演。当时的 Marriott 也许就在现场，也许远在北爱尔兰，但无论他在哪里，他大概都不会想到，自己两年前那个深夜写下的代码，此刻正被一个操作系统的缔造者逐行检阅。

两小时后，Theo 抬起头，给出了他的评价：“只发现了一两个次要问题。”

这句话的重量，需要用 OpenBSD 的背景来称量。在那个房间里，这句话意味着 tmux 的代码达到了 OpenBSD base 的标准——不是“还不错”，不是“可以改改再议”，而是“只发现一两个次要问题”。一个以挑刺为职业信仰的人，花了两个小时，只挑出一两个无关痛痒的小毛病。对一个写了二十年操作系统、见惯了各种烂代码的人来说，这几乎是他能给出的最高赞誉。

> 💡 **独思时刻：** 我后来反复回想这句话的分量。不是“还不错”，不是“可以修”，而是“只发现一两个次要问题”——这意味着在一个以“挑刺”为职业信仰的人眼里，tmux 的代码几乎无懈可击。我写代码十几年，能被同行评价为“没什么毛病”的时刻屈指可数，更别说是被 Theo de Raadt 这种级别的人肯定。那一刻，tmux 从“Marriott 的个人项目”变成了“OpenBSD 认可的东西”，这种跃迁不是靠营销能实现的，是代码自己挣来的。营销能买来曝光，买不来一个偏执狂的点头；而那一次点头，比任何广告都值钱。

## 第三节 2009 年 7 月：tmux 导入 OpenBSD base

2009 年 7 月，OpenBSD 正式将 tmux 导入 base【1】。

“导入”这个动词听起来平静，但它的含义是地震级的。从这一刻起，tmux 不再是“网上一个不错的小工具”，而是 OpenBSD 操作系统的一部分——它进入了 src 仓库的 usr.bin 目录，和 ls、cat、sh 这些系统命令住在同一个屋檐下；它随 OpenBSD 的发布周期一起发布，默认安装，任何一台装好 OpenBSD 的机器上，敲下 tmux 三个字母，它就在那里。

想象一下这个变化意味着什么。在这之前，一个 OpenBSD 用户想用 tmux，得先去 ports 里找、编译、安装；在这之后，它就像呼吸一样自然——系统里就有。更重要的是，从此 tmux 的每一行代码都暴露在 OpenBSD 开发者的眼皮底下：任何安全问题、任何不规范的写法、任何会在某个平台上翻车的隐患，都会被这个世界上最挑剔的一群人第一时间看见、第一时间指出。

这是最硬核的“官方认证”。没有奖杯，没有证书，有的只是代码被放进了操作系统的心脏位置——这就是 OpenBSD 表达“我们信任这个项目”的方式。而这份信任，是 Marriott 用两年时间、一行一行写出来的。

从个人项目到操作系统 base，这个跨越之所以值得大书特书，是因为它几乎不可能通过任何“运营手段”达成。你不能给 OpenBSD 塞钱，让它导入你的项目；你不能请公关公司给 Theo 写通稿；你甚至不能靠用户量说话——OpenBSD 的开发者只认一样东西：代码本身。tmux 能进 base，说明在 2009 年的那个夏天，它的代码已经好到了让最挑剔的人无话可说的程度。

对普通用户来说，这次导入最直观的变化，发生在安装的那一刻。在 tmux 进入 base 之前，你在 OpenBSD 上想用终端复用器，第一反应多半还是 Screen——因为它预装在系统里，不用装、不用配、不用学。而进入 base 之后，这个“默认选项”的位置第一次出现了松动：新装好的 OpenBSD 系统里，tmux 和 Screen 同时存在，但前者是系统的正式成员，后者只是历史遗留。用户在这两者之间做选择时，“哪个更靠谱”这个问题的答案，已经由 OpenBSD 团队替他们写好了。

## 第四节 留在 base 开发 vs 独立开发：Marriott 的选择

导入 base 之后，一个现实的问题摆在了 Marriott 面前：接下来，tmux 在哪里开发？

摆在面前的是两条路。第一条，留在 base 里开发——代码随 OpenBSD 的节奏走，接受它的规则、它的审阅、它的发布周期，享受它带来的专家用户和品质信誉，但也意味着放弃一部分自由。第二条，把开发重心放在 base 之外，独立推进——2009 年，GitHub 刚刚兴起，开源世界正在经历一场协作方式的革命，独立项目从来没有这么好过；tmux 完全可以搭上这班车，在更自由的土壤里野蛮生长。

绝大多数人会选第二条。自由，是每一个写代码的人的本能偏好；而第一条路意味着你的每一个改动都要被一群最挑剔的人过目，意味着你要在别人定好的时间表里完成自己的工作。

Marriott 选了第一条。他选择留在 base 里继续开发。

他后来在访谈里给出了理由：“这会增加更多专家用户和开发者，更大的用户群，以及更严格的开发节奏。”【2】这句平静的话，值得拆开来一个字一个字地读。

“更多专家用户和开发者”——OpenBSD 的开发者，是这个星球上对终端最挑剔的一群人。他们的反馈不是“我觉得这里可以改进”，而是“你第 437 行那个边界条件写错了”。这样的用户，一个顶一百个。“更大的用户群”——每台 OpenBSD 机器上默认都有 tmux，这个分母的扩大，意味着 bug 被发现的速度和广度都上了几个台阶。“更严格的开发节奏”——这是最关键的一条：OpenBSD 有固定的发布周期，代码必须在那之前稳定下来；base 里的每一个改动都会被其他开发者看见，如果有问题，会立刻有人提出来；你不能“过段时间再修”，因为发布窗口不等人。

翻译成大白话就是：Marriott 主动把自己扔进了高压锅，因为他相信，高压锅才能炖出好东西。这个选择暴露了他的优先级排序——在“自由”和“质量”之间，他选了质量；在“舒服”和“进步”之间，他选了进步。后来的事实证明，这个决定几乎重塑了 tmux 的基因：tmux 身上那种“被最严格的标准反复淬炼过”的质感，就是从 2009 年的这个决定开始的。

做出这个选择，其实比看起来更需要勇气。2009 年的开源世界，正处在“独立项目”的黄金时代——GitHub 上的星标、fork、issue 讨论，构成了新一代开发者的社交货币。留在 base 里，意味着你要放弃大部分这种喧嚣，把自己关进一个 CVS 仓库，面对一群不会给你点赞、只会给你挑错的审阅者。但 Marriott 看得很清楚：tmux 需要的不是喧嚣，是淬炼。一个终端工具的价值，不在它被多少人讨论，而在它能不能在每一个深夜可靠地兜住你的工作。而“可靠”这件事，恰恰是 OpenBSD 的看家本领。

## 第五节 base 开发的高压环境：对代码质量的淬炼

Marriott 选择的高压锅，到底有多高压？让我们走进 2009 年 OpenBSD 的开发车间看一看。

那时候，OpenBSD 的源码托管在 CVS 仓库里。CVS 在今天听起来像古董，但在当时，它是 OpenBSD 团队协作的命脉。每一次提交都会被记录，每一个改动都会被其他开发者看到，而且——这是关键——OpenBSD 的开发者不看“提交说明”就相信你的代码，他们看的是代码本身。KNF 风格规范（Kernel Normal Form）约束着每一行代码的缩进、命名和布局，任何偏离都会在审阅时被拎出来。在这个环境里，“能跑”不是标准，“经得起所有人看”才是标准。

还有发布周期。OpenBSD 大约每六个月发布一个新版本，每个版本都有一个雷打不动的截止日期。代码必须在发布窗口前稳定下来，你不能说“这个 bug 我下个版本再修”——在 base 里，没有下个版本，只有这个版本。这种倒逼出来的自律，和一个人在 GitHub 上随心所欲地推代码，是两种完全不同的修炼。

更残酷的是持续测试。OpenBSD 的开发者遍布全球，在不同的硬件平台、不同的架构上构建系统。你在 x86 上写的一段代码，可能在 sparc64 上就翻车了；你在自己机器上跑得好好的逻辑，换一个平台就露出了原形。tmux 进入 base 之后，等于免费获得了一个覆盖各种奇怪平台的持续测试网络——这些测试不是 CI 流水线上跑出来的，是真实的人、真实的机器、真实的使用场景。

在 base 里开发的日常，是很多独立项目作者无法想象的。你提交一个改动，邮件列表上立刻有人回复，不是“辛苦了”，而是具体的质疑：这个函数为什么放在这里？这个错误处理是不是漏了一条路径？这个改动会不会影响其他架构？每一句质疑都针对代码本身，没有寒暄，没有客套。习惯了这种节奏的 Marriott，后来面对任何来自外部的 patch 时，都保持着同样苛刻的标准——这种标准一旦长在身上，就再也卸不下来了。

这段高压时光对 tmux 意味着什么？意味着它的代码在可移植性、健壮性和稳定性上被反复捶打。一个在 OpenBSD 上被全球开发者围观了两年的项目，其代码质量的下限，已经被托到了一个相当高的位置。这也为后来的事情埋下了伏笔：当 Red Hat 需要为几百万台企业服务器挑选默认的终端复用器时，tmux“经过 OpenBSD 审阅”这件事，本身就是最硬的通行证。

## 第六节 垂直分屏补全：约完成 90% 的杀手锏

进入 base 之后，tmux 的功能迭代也明显加速了。2009 年 7 月，在接受 undeadly 的采访时，Marriott 提到了一个正在开发中的功能：垂直分屏，他说大概完成了 90%【2】。

“90%”这个数字在今天读来轻描淡写，但在当时，它几乎是 tmux 对 Screen 最锋利的一刀。因为——Screen 没有垂直分屏。

这听起来难以置信，但事实就是如此。GNU Screen，这个统治了终端复用领域二十年的老前辈，支持水平分屏，却从来没有在它的生产版本里提供过垂直分屏【3】。你可以在 Screen 里把屏幕上下切成几块，但你不能把屏幕左右切开。二十年，约两万行 C 代码，这个功能始终缺席【3】。

为什么垂直分屏这么重要？因为真实的工作场景是左右结构的。你写代码的时候，左边是编辑器，右边是编译输出；你排查故障的时候，左边是日志，右边是命令行；你对照文档的时候，左边是手册，右边是操作。人类的双眼天生适合左右并排，而不是上下堆叠。Screen 二十年不提供垂直分屏，不是因为用户不需要，而是因为它的 monolithic 架构让这个功能变得异常难做——那团毛线球里，每一根线都缠着另一根线，想加一个横切一刀的功能，就得动整栋房子的结构。

而 tmux 的 client/server 架构，让垂直分屏成为了顺理成章的事情。面板（pane）本质上是独立的伪终端，布局系统负责在屏幕上分配空间——垂直分屏只是布局系统的一种排列方式。当架构对了，功能就不再是问题。Marriott 在访谈里还提到，他打算把布局系统重构成“布局集”，让用户可以在几种预设布局之间快速切换【2】。这个计划后来演变成了 tmux 的布局系统，成为无数人日常依赖的功能。

垂直分屏这件事，完美地诠释了 tmux 的差异化策略：不是“我们做得更好”，而是“我们能做到，而对手做不到”。而且这份“做到”不是靠堆功能堆出来的，是靠选对架构换来的——这正是第 2 章那个 client/server 决定，在两年后开始兑现的红利。

顺便说一句，“约完成 90%”这个表述本身也很有意思。一个开发者愿意在公开访谈里承认自己的功能“还没做完”，这在今天的开源世界里几乎是不可想象的——大家都习惯把半成品包装成“全新体验”。但 Marriott 的诚实，恰恰是 OpenBSD 气质的延伸：状态透明，进度如实，不吹不擂。也正是这种诚实，让社区对他说的每一句“快好了”都深信不疑。

## 第七节 synchronize-panes：一个用户请求变成官方功能

如果说垂直分屏是 tmux 架构优势的体现，那么 synchronize-panes 就是 tmux 社区文化的体现。

先想象一个场景。你是一个运维工程师，深更半夜，三台服务器同时报警。你需要在这三台服务器上执行一模一样的排查命令——先看负载，再看日志，再查进程。传统的做法是什么？开三个终端窗口，把同一段命令敲三遍，然后盯着三个屏幕来回比对。敲三遍这件事，听起来没什么，但如果你每天都要这么干十次，你就会开始痛恨这个世界。

synchronize-panes 解决的就是这个问题：打开同步输入，你在一个面板里敲的命令，会同时出现在所有面板里。一条命令，同步到多个面板，多台服务器。这个功能在今天的运维工作流里几乎是标配，但很少有人知道，它最初只是一个用户的请求【4】。

是的，synchronize-panes 不是 Marriott 拍脑袋想出来的，也不是产品规划会上讨论出来的——是一个用户提出来的，然后 Marriott 觉得这个需求合理，决定把它加进去【4】。这个故事的珍贵之处，在于它揭示了 tmux 社区一个朴素的运作方式：用户提出真实的需求，维护者认真倾听，然后决定要不要做。没有复杂的投票机制，没有路线图委员会，就是一个开发者对另一个开发者的需求说“这个想法不错，我来实现它”。

Marriott 在访谈里说过一句话：“People use tools in surprisingly different ways”——用户使用工具的方式千奇百怪【2】。这句话是 synchronize-panes 最好的注脚：作为一个工具的作者，你永远无法预知用户会拿你的工具做什么。你设计 tmux 的时候想的是“让终端会话更可靠”，但用户会在里面开直播、跑自动化、编排多台机器、甚至搭建一个临时的协作环境。既然用户的使用方式千奇百怪，那么倾听用户的声音，就不是一种恩赐，而是一种必要——因为只有用户，才知道你的工具在他们手里会长成什么样子。

synchronize-panes 的代码实现并不复杂：

```bash
tmux set-window-option synchronize-panes on
```

一条命令，打开同步输入；再敲一遍，或者把 on 换成 off，关闭它【4】。但就是这条简单的命令，让无数运维工程师从“敲三遍”的重复劳动里解放了出来。它后来甚至成了 AI 时代的新宠——当开发者需要同时指挥多个编码智能体在多个面板里并行工作时，synchronize-panes 这个十年前的老功能，焕发出了全新的生命力。当然，那是第 7 章的故事了。

这里还藏着一个更深的道理：一个工具的功能，往往不是作者设计出来的，而是用户“用”出来的。作者能做的，只是搭好架子、留好接口、保持倾听。synchronize-panes 从用户请求到官方功能的短短一程，就是 tmux 社区“用户驱动”基因的第一次公开亮相——这个基因，后来贯穿了 tmux 的整个生命周期。

## 第八节 此消彼长：Screen 冻结与 tmux 崛起

任何故事的转折，都需要一个对手的衬托。tmux 崛起的另一面，是 Screen 的停滞。

让我们把时间拨回 2004 年。那一年，GNU Screen 的开发实际上已经停摆了【3】。这个 1987 年诞生、统治了终端复用领域十几年的老前辈，在进入新世纪之后逐渐失去了活力。2008 年 8 月，Screen 发布了 4.0.3——这成了它的最后一个版本【3】。从此，这个约两万行 C 代码的老房子，彻底冻结在了时间里。

而与此同时，tmux 正在经历它一生中最快的成长。2007 年首次提交，2007 年 11 月公开发布，2009 年进入 OpenBSD base，之后功能快速补全——两个项目，一个冻结，一个狂奔，此消彼长几乎是必然的结局。

此消彼长的转折点，大约发生在 2017 年前后。在企业环境的实际使用中，tmux 的份额悄然反超了 Screen【4】。这个反超没有官方公告，没有庆祝仪式，它就是一个又一个工程师，在夜深人静的时候，默默地把自己的 .screenrc 换成了 .tmux.conf，把肌肉记忆从 Ctrl+a 换成了 Ctrl+b。

为什么天平会倾斜？细想起来，原因其实很朴素。第一，Screen 已经不再进化了——一个冻结在 2008 年的工具，无论它当年多辉煌，都无法满足 2017 年的工作场景。第二，tmux 有 Screen 没有的东西——垂直分屏、现代化的命令语法、可编程的配置、活跃的社区。第三，也是最关键的，tmux 背后有 OpenBSD 的背书——当企业需要一个“不会烂尾”的工具时，一个被操作系统级标准锤炼过的项目，天然地比一个二十年没有大版本更新的项目更可信。

Screen 并没有一夜消失。它还在，还会在很多老系统上继续运行很多年——毕竟它被预装进了几乎所有发行版，预装是最深的护城河。但护城河只能延缓死亡，不能阻止死亡。当一个工具的核心优势只剩下“它还在那里”的时候，它的命运就已经注定了。

还有一个容易被忽略的因素：生态。Screen 的用户社区在它停滞的岁月里慢慢萎缩，愿意为它写文档、写教程、写配套工具的人越来越少；而 tmux 这边，每多一个用户，就多一分内容产出——配置教程、状态栏主题、快捷键速查表、脚本插件，像滚雪球一样越滚越大。一个冻结的工具有存量，没有增量；而增量，恰恰是年轻工具最可怕的武器。

## 第九节 Red Hat 的站队：从弃用到推荐

如果说 OpenBSD 的接纳是 tmux 命运的第一次转折，那么 Red Hat 的站队，就是第二次——而这一次，直接决定了 tmux 在现代企业世界里的地位。

2018 年，Red Hat 在 RHEL 7.6 中把 Screen 标记为“已弃用”【5】。一年后，2019 年，RHEL 8.0 发布，Screen 被彻底移除——取而代之的推荐，是 tmux【5】。

这不是一个简单的软件包更换。RHEL（Red Hat Enterprise Linux）是企业服务器的操作系统，运行着全世界无数银行、交易所、运营商和政府部门的关键业务。Red Hat 对系统里每一个软件的每一个决定，都要为背后的企业客户负责。它把 Screen 标记为弃用，等于向整个企业世界宣布：这个工具，我们不再维护了，你们应该迁移。它把 tmux 列为推荐，等于向整个企业世界宣布：这个工具，是我们认真评估过的，值得托付。

Red Hat 官方的解释很直接：Screen 的代码库太老了，维护困难，上游社区几乎没什么活动；而 tmux“拥有更好的代码基础，便于维护并方便在其上构建新功能”【5】。这句话翻译过来就是：Screen 不是不好用，是它太老了，我们修不动了。而 tmux 年轻、干净、有人在维护，而且——关键在这里——它已经经过了 OpenBSD 的审阅，Red Hat 不需要再自己做一遍同样的安全检查。

这就是“OpenBSD 出品”的商业价值。一个发行版敢把另一个发行版的东西作为默认推荐，背后是代码质量本身在说话。Red Hat 信任 tmux，不是因为它认识 Marriott，不是因为它收了什么好处，而是因为 tmux 的代码已经在 OpenBSD 的 base 里接受了最严格的检验——这份检验是公开的、透明的、可追溯的，Red Hat 只要看一眼，就知道自己可以省下多少安全审查的成本。

这场迁移在企业用户中引起的震动是巨大的。2024 年，LWN 上有人讨论 Screen 和 tmux 时，一个用户说得很直白：“RHEL 放弃 Screen 迫使我学了 tmux。”【6】有时候，用户换工具不是因为新工具更好，只是因为旧工具不再被官方支持了。但这句话的另一面是：如果不是 Red Hat 的背书，很多企业用户可能永远不会给 tmux 一个机会。官方认证的力量，就是让你在一个“不得不”的瞬间，发现一个更好的选择。

从 2009 年进入 OpenBSD base，到 2019 年被 Red Hat 推荐，tmux 用了十年时间，完成了从“替代品”到“默认选项”的跃迁。这个跃迁里没有奇迹，只有一条清晰的因果链：代码质量过硬，所以被最挑剔的系统接纳；被最挑剔的系统接纳，所以被最谨慎的企业信任；被最谨慎的企业信任，所以成为现代标准。

这十年里，还有一个耐人寻味的细节：Red Hat 的选择，本质上是在为“维护成本”投票。对企业来说，软件的第一个成本是购买，第二个成本是维护——而维护成本最高的软件，就是那种“没人管了”的软件。Screen 在 2008 年之后失去了上游，等于把全部维护责任都推给了下游的每个发行版；而 tmux 背靠 OpenBSD，有人管、有人审、有人修。在一个“烂尾软件”能让企业亏掉无数人天的世界里，“有人维护”本身就是一种稀缺的竞争力。

## 第十节 💡 独思时刻：被最挑剔的人认可，是最好的质量背书

> 💡 **独思时刻：** 写到这里，我想停下来，认真聊聊“认可”这件事。世界上有很多种认可：广告商认可你的产品，因为你付了钱；媒体认可你的产品，因为你提供了流量；投资机构认可你的产品，因为你画了一张好看的饼。但有一种认可，买不到，也换不来——那就是最挑剔的人，花自己的时间，认真地看了你的东西，然后点了点头。

Theo de Raadt 的两小时，就是这种认可。他不是 tmux 的粉丝，不是 Marriott 的朋友，他甚至可能希望 tmux 不存在的理由多过希望它存在的理由——毕竟，一个成熟的操作系统，最讨厌的就是引入新的维护负担。但当他花两个小时看完代码，说“只发现一两个次要问题”的时候，这份认可的价值，超过任何五星好评，超过任何“最佳工具”榜单，超过任何转发过万的推广文章。

我自己的经历可以作证。几年前，我参与维护一个开源项目，功能做得花团锦簇，用户也不少。但有一天，一位以严苛著称的资深内核开发者——就是那种会在邮件列表里把你写得一无是处的人——在我的补丁下面留了一条评论，指出一个我完全没有意识到的边界条件错误。那一刻我忽然明白：那些平时不说话、一说话就让你冷汗直冒的人，才是你真正应该在乎的读者。他们不夸人，所以他们的认可才值钱；他们不轻易点头，所以他们的点头才改变命运。

被最挑剔的人认可是什么体验？是惶恐，是荣幸，是“原来我这两年的努力，真的有人看懂了”。tmux 在 2009 年获得的，就是这种东西。而它后来赢得的一切——Red Hat 的推荐、企业用户的信任、现代标准的地位——都可以追溯到那个夏天的两小时。质量背书这件事，从来不是锦上添花，它是雪中送炭里最贵的那一块炭。

挑剔者的认可还有一个被低估的功能：它是一面过滤器。被普通用户夸，可能是运气；被最挑剔的人认可，只能是实力。因为挑剔者不会因为你态度好就放过你的错误，不会因为你文档全就容忍你的设计缺陷，更不会因为你在社区里人缘好就网开一面——他们只认代码。所以，一份通过了这种过滤的代码，几乎不可能差到哪里去。这正是 tmux 后来能被 Red Hat 放心推荐的根本原因：Red Hat 不需要自己再当一次过滤器，OpenBSD 已经当过了，而且当得比谁都认真。

## 第十一节 💡 独思时刻：生态位决定命运——标准不是评选出来的，是沉淀出来的

> 💡 **独思时刻：** 最后，我想说说“标准”这个词。“标准”听起来像是一个被评选出来的东西——一个委员会，一场投票，一份权威报告，然后某个工具被加冕。但 tmux 成为现代标准的过程，和这一切都无关：没有委员会评选过 tmux，没有投票决定过 tmux 的命运，没有哪份官方文件宣布“tmux 是终端复用的标准”。它是被沉淀出来的。

沉淀是什么意思？是 2009 年 Paul Irofti 在 hackathon 上随口提了一句；是 Theo de Raadt 花了两小时看完代码后的点头；是 Marriott 在 base 里度过的每一个被审阅的日夜；是垂直分屏从 90% 到 100% 的补全；是一个用户请求变成 synchronize-panes；是 2017 年企业环境里一次次的份额反超；是 Red Hat 在 2018 年和 2019 年的两次决定。这些事件单独看，每一件都平平无奇，但叠在一起，就构成了一个生态位——tmux 占据了“终端复用”这个生态位，然后因为占据得足够久、足够稳、足够好，它成了这个生态位本身的名字。

生态位决定命运。Screen 曾经占据这个生态位，但它停止了进化，于是生态位空了出来；tmux 填补了它，并且用持续十几年的活跃维护守住了它。标准不是评选出来的，是因为某个工具在某个生态位里活得太久、做得太好，以至于后来的人提起这个需求，第一反应就是它的名字。就像“搜索”之于网络，“文档”之于办公——不是谁加冕了它们，是它们自己长成了那个生态位的代名词。

这对我们每个人都是一个提醒：不要追求被评选为“标准”，要追求在一个生态位里活下来、活得好、活得久。评选出来的标准会过期，沉淀出来的标准不会——因为后者不是别人给你的头衔，是你自己用时间写成的历史。tmux 的“现代标准”地位，就是这样一个由无数个普通日子沉淀出来的历史。

我还想多说一句关于“沉淀”的体会。写这一章的时候，我重读了很多当年的资料，最打动我的，不是 Theo 那句评价本身，而是它背后的时间跨度——两小时的审阅，凝结的是 Marriott 两年来每一个深夜的坚持；十年的标准之路，凝结的是无数人日复一日的使用与反馈。任何看起来“一夜成名”的东西，拆开来看，都是时间的复利。标准如此，人生亦然。

## 尾声：从个人项目到基础设施

2009 年，当 Irofti 在 hackathon 上提起 tmux 的时候，没人能预料到这一切。Marriott 自己也没想到。他只是做了自己认为对的事——写一个可读的、可扩展的、不用再忍受“怪室友”的替代品。然后 OpenBSD 接纳了它，Red Hat 选择了它，越来越多的人开始在日常工作中依赖它。

回头看，这一章的每一个节点都充满了偶然：如果 Irofti 那天没开口，如果 Theo 那天没空，如果 Marriott 选择了独立开发而不是留在 base，如果 Red Hat 决定再修一修 Screen……任何一个“如果”成立，tmux 都可能走向完全不同的命运。但历史没有如果，历史只有因果——而这条因果链的第一环，是 2007 年那个深夜，一个北爱尔兰开发者敲下的第一行代码。

tmux 不再是一个人的项目了。它成了基础设施。基础设施这个词的分量，只有当你深夜三点、SSH 断线、而你的编译任务还在 tmux 里继续跑的时候，才能体会。那一刻你不会想起 OpenBSD，不会想起 Red Hat，不会想起 Theo 的两小时——你只会庆幸，这个世界上有一个工具，让“断开”不再意味着“失去”。

而这个故事，才刚刚开始。从 2009 年到 2026 年，tmux 还有将近十七年的版本之路要走：2.0 的不兼容更新、3.0 的 hooks 系统、3.6 的原生滚动条、越来越繁荣的插件生态……那些是第 4 章的故事了。此刻，让我们记住 2009 年这个夏天——一个操作系统，把一份来自个人项目的代码，放进了自己的心脏。

那是 tmux 命运真正的转折点，也是它走向现代标准的第一步。

多年以后，当人们回顾终端复用的历史，会如何讲述 2009 年的这个夏天？大概会这样说：有一个工具，没有经过任何评选，没有拿过任何奖项，只是被一个操作系统接纳了，被一个企业选择了，被几百万个工程师默默使用了，然后，它就变成了标准。没有加冕礼，没有欢呼声，只有代码在一台台服务器上安静地运行。而这份安静，恰恰是它最了不起的地方。

---

*注：本章关于 hackathon 现场氛围与 Theo de Raadt 审阅过程的具体场景细节，系基于 2009 年开源社区开发环境的合理推演，非一手信源；核心事实（Paul Irofti 在 hackathon 提及 tmux、Theo de Raadt 两小时审阅并评价“只发现一两个次要问题”、2009 年 7 月导入 OpenBSD base、Marriott 留在 base 开发及其理由、垂直分屏约完成 90%、synchronize-panes 源自用户请求、RHEL 7.6 弃用与 RHEL 8.0 移除 Screen 并推荐 tmux、Screen 2004 年停滞与 2008 年 4.0.3 冻结）均来自公开报道与访谈，与全书事实清单保持一致。*

**本章资料来源：**

【1】undeadly.org，OpenBSD 导入 tmux 的报道（2009 年 7 月）

【2】undeadly.org 对 Nicholas Marriott 的访谈（2009 年 7 月，关于留在 base 的理由、垂直分屏进度与“People use tools in surprisingly different ways”）

【3】softpanorama.org，GNU Screen 历史（2004 年开发停滞、2008 年 8 月 4.0.3 最后发布、垂直分屏从未进入生产版）

【4】原稿整理：synchronize-panes 的用户请求由来与 2017 年前后企业环境份额变化

【5】Red Hat Customer Portal，RHEL 7.6 弃用 screen 与 RHEL 8.0 移除 screen 并推荐 tmux 的官方文档

【6】LWN.net，“Screen and Tmux”讨论（2024 年，用户评论）

【7】tmux 维基百科词条（2007 年 11 月 20 日第一版发布）

---

# 第4章 版本足迹——从2007到2026的进化之路

如果把 tmux 这十九年拍成一部纪录片，那么每一集的开场都应该是一个时间戳。屏幕上先跳出一行数字，紧接着是一个短暂的停顿，然后画面才缓缓亮起。看完整部片子你会发现，那些时间戳之间的间隔本身就是故事——哪几年发布得密，哪几年发布得稀，哪一次发布带来的是全新的功能，哪一次发布带来的只是旧伤口的愈合，背后都有原因。

2007 年 10 月 10 日，Nicholas Marriott 在代码仓库里敲下了 tmux 的第一次提交【1】。那天没有发布会，没有公告，甚至连一行像样的文档都没有。有的只是一个刚跑起来的原型、一个临时起的名字，以及一个在开发者硬盘里躺了几个月、被反复推倒重来的 nscr 文件夹。如果当时有人路过他的屏幕，大概只会看到一段乱糟糟的 C 代码，以及一个即将在十九年后被全球数百万开发者依赖的工具的婴儿时代。

从那一天算起，到 2026 年的今天，tmux 已经走过了将近二十年。这二十年里，它从一个北爱尔兰开发者“忍无可忍”的周末项目，变成了 OpenBSD 的基础系统组件，再变成全球数百万开发者日常依赖的终端工具。但如果你把这段历程想象成一帆风顺的征服之路，那就错了——它更像一场漫长的、在“为用户加功能”和“为自己守边界”之间反复拉锯的持久战。

这就是本章要讲的：一部关于版本号的纪录片。每个版本是一集，每一集都在回答同一个问题——在用户想要更多、作者想要更少之间，这个工具到底应该长成什么样？

## 第一节 版本全景图：十九年，二十余个版本，三个时代

先给这部纪录片画一张全景图。从 2007 年到 2026 年，tmux 一共走过了三个大的版本段：0.x 时代、1.x 时代和 3.x 时代。如果你把版本号当作一棵树的年轮，会发现它长得并不均匀——有的年份一年发布好几个版本，有的年份一整个版本要打磨两年。

把时间线摊开来看，是这样的：

```bash
2007-10-10    首次代码提交
2007-11-20    第一版公开发布，0.x 时代开启
2009-07       进入 OpenBSD base，成为系统组件
2011-2014     1.x 时代，功能基本补全，开始收敛
2016-初       2.0，一次不兼容更新，terminal-overrides 转为 server 选项
2018-2019     RHEL 弃用并移除 Screen，官方文档推荐 tmux
2019          3.0，引入 hooks 系统
2022-06-09    3.3a，稳定性与 bug 修复
2025-11       3.6，原生滚动条与 Mode 2031 主题
2026-05-20    3.6b，最新稳定版
```

光看这张表，还看不出节奏。但如果你把每个版本发布时所在的阶段标出来，三个时代就清晰了。

第一个时代是功能补全期，大约从 2007 年持续到 2014 年。这个阶段的特点是“快”：版本号跳动得很频繁，几乎每次发布都在补一个新功能——分屏、会话、复制模式、鼠标支持、状态栏定制，一样一样地补。这个阶段的口号是：先活下来，再谈别的。

第二个时代是架构整理期，大约从 2015 年持续到 2019 年。功能补得差不多了，但早期为了追赶而堆起来的代码结构开始显得局促。这个阶段的特点是“稳”：版本发布明显变慢，每次发布都在做内部结构的梳理、选项的重新归位、设计上的一致性修补。最有代表性的两个事件，一个是 2016 年初的 2.0 不兼容更新，一个是 2019 年的 3.0 hooks 系统。

第三个时代是稳定繁荣期，从 2020 年延续到 2026 年。这个阶段的特点是“少而精”：版本号几年才跳一次，每次发布都克制、扎实，不追新奇，专注稳定性、兼容性和现代终端协议的适配。与此同时，围绕 tmux 的生态——插件管理器、会话持久化、窗口布局工具、状态栏美化——进入了大爆发，tmux 从“一个好用的工具”变成了“一个可以生长出无数工具的平台”。

十九年，二十余个版本，三个时代。接下来，我们一集一集地看。

## 第二节 功能补全期（2007—2014）：先活下来，再谈优雅

纪录片的第二集，从第一个公开版本讲起。

坦率地说，tmux 的第一个公开版本几乎谈不上“设计感”。它更像是一个工程师“忍无可忍”之后的产物：Screen 用着难受，又找不到替代品，那就自己写一个吧。这个“自己写一个”的起点，决定了它在最初几年里必然要走一条“追赶”的路——别人有的功能我得有，别人做得不好的地方我得做得更好，否则凭什么让用户从一个成熟的工具迁移过来？

于是，功能补全期的大部分时间，tmux 都在玩一场追赶游戏。邮件列表里最常见的一句话是：“这玩意儿跟 Screen 比还缺个啥？”缺什么，就补什么。缺垂直分屏，补上；缺复制模式，补上；缺状态栏定制，补上。那段时期的更新日志读起来像一张购物清单：加了这个、加了那个、又加了一个。这种“购物清单式”的演进谈不上优雅，但对于一个要跟 Screen 抢用户的年轻项目来说，这是唯一的选择——先把功能补齐，让用户愿意留下来，才有资格谈优雅。

0.x 时代的发布节奏快得惊人。从 0.1 到 0.9，版本号一年之内能跳好几个，每个版本都在补洞。这一时期有一个小故事，特别能说明 tmux 与用户的关系。曾经有一位瑞典用户在邮件列表里提了一个需求：能不能让所有面板同步执行同一个命令？比如说，我在两个服务器上各开一个面板，想让它们同时执行同样的操作。Marriott 一开始是拒绝的——他觉得这功能没多少人会用，而且实现起来容易出错。但那位用户没有放弃，他举了一个很有说服力的例子：运维的时候，把命令同步到所有机器上执行，就像在菜市场里喊一嗓子，所有摊位同时收到一样。Marriott 最终被说服了，这个功能后来以 synchronize-panes 的名字加入 tmux，并成为许多运维工程师离不开的利器【2】。

这个故事值得记住，因为它揭示了 tmux 演进的一个底层逻辑：功能不是作者拍脑袋加进去的，而是用户在真实场景里“磨”出来的。Marriott 不是那种“用户要什么就给什么”的老好人，但他对来自真实工作流的需求保持着开放。一个功能只有先在某个人的真实痛苦里诞生，才有可能进入 tmux 的核心。

到了 2011 年前后，tmux 的核心功能已经基本补全：会话管理、窗口切换、面板分割、复制模式、状态栏定制，一样不缺。1.x 时代随之到来，发布的节奏开始放缓，因为“缺什么补什么”的阶段接近尾声，剩下的事情更多是打磨。1.8 之前的状态栏配置还相当繁琐，各种选项散落在不同的命令里；这个阶段 tmux 做了一件重要的事——把配置方式统一起来。在此之前，Screen 用户要分别记忆 hardstatus、caption、vbell 这些互不相干的命令；而 tmux 把这一切收敛到 set-option、bind-key、set-environment 这一组统一的命令里【3】。你不需要再记三种不同的语法，只需要记住“set 什么、绑定什么、设置什么环境变量”这一套逻辑。这种“一致性”在当时看起来不起眼，却是 tmux 后来得以被大规模配置、被插件系统改造的地基。

功能补全期结束于 2014 年前后。此时 tmux 已经不是一个“还在追赶”的年轻项目了：它有了完整的核心功能，有了一致的配置体系，有了一批忠实用户，甚至已经成为 OpenBSD 的基础系统组件。但恰恰是“功能齐全”之后，新的问题浮出水面——早期为了快速补功能而堆起来的代码结构，开始显得跟不上时代。版本号的节奏，将从“快”转向“慢”。

> 💡 **独思时刻：** 写这一段的时候，我想起很多年前自己在一台 64MB 内存的 VPS 上第一次跑 tmux 的经历。那时候手头没有图形界面，只有一个 SSH 终端，服务器配置低得可怜，开几个窗口就卡。但 tmux 在那种环境下居然跑得飞快——它不占资源，不怕断线，会话丢在服务器上，重连之后一切照旧。那一刻我突然理解了为什么这个工具值得被维护十九年：它不是写给“拥有最好电脑的人”的，它是写给“在最恶劣环境下还要干活的人”的。功能补全期补的每一个功能，本质上都是在回答一个问题——当你的环境一无所有时，什么才是你最需要的东西？

## 第三节 架构整理期（2015—2019）：从追赶者到领跑者

纪录片的第三集，转折点出现了。

功能补全期的 tmux，像一栋不断加盖的房子：这里加一间，那里搭个棚，住起来越来越方便，但结构越来越让人担心。到了 2015 年前后，Marriott 和核心维护者们意识到，继续这样“加功能”下去，代码的复杂度会反噬整个项目。于是，tmux 进入了一个新的阶段——不再追求“还有什么功能没有”，而是开始问“已经有的功能，放的位置对不对”。

这个阶段最标志性的事件，是 2016 年初的 2.0 版本。这是一次罕见的“不兼容更新”：它把 terminal-overrides 从窗口选项提升为 server 选项【4】。用大白话说，以前每个窗口各管各的终端能力覆盖设置，现在这些设置被统一收归到服务器层面统一管理。听起来是个内部细节，但它动了所有用户的配置文件——很多人的 .tmux.conf 在升级后需要手动修改才能继续工作。对一个已经拥有大量用户的工具来说，主动制造“不兼容”，是需要勇气的。

Marriott 当然知道这会招来抱怨。事实上，升级到 2.0 后，邮件列表里确实出现了一批困惑和不满的声音：为什么要改？我原来的配置怎么办？面对这些声音，Marriott 的态度很明确——这次不兼容是有价值的，它换来的是未来几年内更清晰的架构、更少的内部混乱。老房子要翻修，总得有人忍受几天脚手架。而翻修之后，住起来会更舒服。

这次不兼容更新的意义，要放到整个版本史里看才能看得清。它标志着 tmux 从“追赶者”变成了“领跑者”：当你的功能已经不比别人少时，你就不需要再追着别人的功能列表跑了，你可以开始按自己的节奏，整理自己的房子。一个项目从“追赶”转向“自我定义”，往往就是从不兼容更新的那一刻开始的。

2018 年到 2019 年间，外部世界传来一个信号：Red Hat 在 RHEL 7.6 中弃用了 Screen，并在 RHEL 8.0 中将其移除，官方文档明确推荐用户迁移到 tmux【5】。这个事件我们在上一章已经详细讲过，这里只需要记住它在版本史上的位置——它意味着，在“官方的操作系统发行版”这个层面上，tmux 已经完成了对 Screen 的正式接替。曾经的追赶者，成了被官方指定的继承者。

而 2019 年的 3.0，则带来了架构整理期最重要的一个创新：hooks 系统【6】。关于 hooks 本身，我们下一节会深入展开；这里先记住它的定位——它是 tmux 从“被动等待用户操作”转向“主动响应事件”的分水岭。如果说功能补全期补的是“手”，那么 hooks 补的是“神经”：让 tmux 第一次拥有了感知并响应自己内部事件的能力。

架构整理期的五年，发布数量比功能补全期少得多，但每一版的分量都重得多。版本号从 1.x 跳到 2.x，再跳到 3.0，每一步都踩在“整理”而不是“堆砌”上。用一句话概括这五年：tmux 不再问“我还能做什么”，而是开始问“我该怎么做才更对”。

## 第四节 深入 hooks：事件驱动的自动化平台

hooks 值得单独开一节来讲，因为它改变了 tmux 的“物种属性”。

在 hooks 出现之前，tmux 的自动化能力是有限的。你可以绑定快捷键，可以设置别名，可以让某个窗口启动时自动执行命令，但这些都是“用户主动触发”的。tmux 本身没有能力在“某个事件发生的那一刻”自动做出反应——比如，新建了一个窗口，能不能自动弹一句提示？一个面板退出了，能不能自动记录日志？某个客户端断开连接了，能不能自动执行清理脚本？

hooks 系统回答的正是这些问题。它的核心思想是事件驱动：tmux 内部发生的每一个有意义的事件——窗口创建、窗口重命名、面板退出、会话创建、客户端接入——都可以挂上一个钩子，钩子上绑着一条或多条命令。事件发生时，钩子自动执行，不需要任何人干预。

用法极其简单，就是一条 set-hook 命令【6】。举个例子：

```bash
# 每次新建窗口时，自动弹出一行提示
tmux set-hook -g after-new-window 'display-message "新窗口已创建"'

# 每个面板退出时，把一条记录追加到日志文件
tmux set-hook -g pane-exited 'run-shell "echo pane exited >> ~/tmux.log"'

# 某窗口被重命名时，同步更新状态栏
tmux set-hook -g window-renamed 'refresh-client'
```

注意那个 -g 参数，它表示全局钩子——对所有会话、所有窗口、所有面板生效。你当然也可以只给某个特定窗口挂钩子，但大多数实际场景下，全局钩子就够了，因为它让“整个 tmux 的自动化行为”可以在一处集中定义。

hooks 出现之后，tmux 的自动化能力上升了一个台阶。以前你要实现“自动保存会话状态”，得写一个外部脚本轮询 tmux 的状态；现在你可以直接挂一个钩子，在“面板退出”这类事件发生的瞬间触发保存逻辑。以前你要实现“窗口打开就初始化环境”，得在启动脚本里反复判断；现在一个 after-new-window 钩子就搞定了。

更重要的是，hooks 把 tmux 从“被动窗口管理器”变成了“可编程平台”。窗口管理器只能告诉你“现在有几个窗口”；平台可以告诉你“发生了什么、什么时候发生的”，并让你在事件发生的瞬间插入自己的逻辑。这个转变，是后来整个 tmux 生态能够蓬勃发展的重要前提之一——生态需要的不只是“好用”，还需要“可扩展”，而 hooks 正是扩展性的核心接口之一。

顺带一提，如果你在 2019 年那个节点上问“hooks 到底有什么用”，很多人可能会觉得这是个小众功能。但今天回头看，它已经成为 tmux 自动化脚本、插件系统、工作流工具链里最常见的基础设施。架构整理期的很多工作在当时看起来“没什么用”，但几年之后，它们都成了生态的地基。

## 第五节 稳定繁荣期（2020—2026）：优雅地变老

纪录片的第四集，节奏彻底慢了下来。

从 2020 年到 2026 年，tmux 的版本号只跳了六次：3.1、3.2、3.3、3.4、3.5、3.6，中间还有几个带字母后缀的补丁版本。这六年，tmux 没有推出什么颠覆性的新概念，没有像 3.0 引入 hooks 那样的大动作。它的每一次发布都像一位老练的工匠在打磨一件已经成型的作品：不追求新奇，只追求每一处细节都更妥帖、更稳定、更兼容。

这个阶段的 tmux，已经不需要用“新功能”来证明自己了。它需要的是：在成千上万种终端模拟器、操作系统、SSH 环境下都表现一致；在处理 Unicode、真彩色、剪贴板这类现代终端协议时不再翻车；在用户已有的配置和工作流里，升级不会带来惊吓。

2022 年 6 月 9 日发布的 3.3a，是这个阶段的一个典型样本【7】。它没有新功能，没有新命令，就是一次纯粹的稳定性维护和 bug 修复。它的发布说明短得让人意外——就是修复了在特定终端下的光标渲染问题，以及若干其他 bug。如果你只看发布说明，会觉得这版本“毫无存在感”；但如果你恰好是被那个光标渲染 bug 折磨过的人，你会知道这个版本有多重要。

3.3a 让我想到一个有意思的现象：一个项目进入成熟期之后，最被用户感激的版本，往往不是功能最多的版本，而是“终于把那个烦人的 bug 修掉”的版本。功能是锦上添花，稳定是雪中送炭。而 tmux 在稳定繁荣期的每一次发布，几乎都是“送炭”式的——不声不响，但解决的都是真实问题。

这个阶段的另一个特征是：tmux 开始认真适配“现代终端协议”。终端模拟器这些年进化得非常快——真彩色、OSC 52 剪贴板、鼠标事件报告、焦点变化通知、终端能力查询……如果 tmux 不跟上，就会在现代终端里显得过时。稳定繁荣期的多个版本，都在默默做这些“看不见的适配”工作。这些工作用户平时感知不到，但一旦缺失，用户立刻就会感知到。

版本号放缓，本身就是一种成熟的标志。年轻的项目才需要用频繁的版本号证明“我还在进步”；成熟的项目用质量证明“我依然值得信任”。2020 年之后的 tmux，正是后者。

## 第六节 tmux 3.6（2025 年 11 月）深度解读：一次克制的“回归”

2025 年 11 月，距离 3.5 发布一年多之后，tmux 3.6 来了【8】。这是稳定繁荣期里分量最重的一次发布，也是本章要重点解读的一集。

先说最显眼的新功能：原生滚动条。在 tmux 里，面板内容超过一屏时，翻看历史输出一直是个痛点——传统做法是用前缀键进入复制模式，或者依赖第三方插件。3.6 引入的 pane-scrollbars 选项，让面板内部可以直接显示滚动条，无需插件，无需额外配置【8】。你滚动面板，滚动条就跟着走；你拖滚动条，面板内容就跟着滚。对于经常翻长日志、看长输出的用户来说，这个功能是实打实的“减负”。

这个功能有意思的地方在于它的“回归”味道。终端世界里的滚动条，曾经是图形界面时代的产物，是 tmux 这类终端复用器刻意不碰的领域——因为终端里本来没有“滚动条”这个概念。但现代用户的习惯已经被图形界面塑造了：看到内容溢出，第一反应就是找滚动条。3.6 没有说“终端不需要滚动条，你该学复制模式”，而是说“既然用户需要，那就做出来，而且做得原生、做得简单”。这种对用户习惯的尊重，恰恰是 tmux 在稳定繁荣期里始终保持的姿势。

第二个值得关注的是 Mode 2031 主题支持【8】。这是什么？简单说，它让 tmux 能够向终端报告当前的配色环境——深色还是浅色——从而让 tmux 的主题、状态栏、甚至第三方主题插件，能够跟随系统的深浅色模式自动切换。你白天用浅色主题，晚上系统切到深色，tmux 的状态栏也跟着切换，整个界面始终与系统风格保持一致。用这个机制，主题插件和状态栏可以真正做到“跟随系统”，视觉体验的统一性大大提升。

Mode 2031 的意义不在“换肤”本身，而在“自适应”。一个工具活到第十九个年头，还能主动拥抱现代终端协议里最新的配色机制，这本身就是一种生命力。很多老工具对这类新协议的态度是“不支持也不影响我活着”，但 tmux 选择跟上——因为它的用户正在越来越多的设备、越来越多的终端环境里使用它，跟随系统深浅色，是这个时代最基本的界面礼貌。

第三个方面是格式化与变量的强化【8】。3.6 增强了格式化功能：更强的布尔运算、更灵活的排序方式，以及更多新的格式变量——比如缓冲区状态、光标样式、会话信息等。对于普通用户来说，这意味着状态栏可以展示更丰富、更动态的信息；对于进阶用户来说，这意味着在状态栏和脚本里能做的事情更多了。别小看这些“变量”，tmux 的状态栏本质上就是一个格式化模板引擎，变量的多少，直接决定了这个模板引擎的表达力上限。

第四个方面是查询处理与终端能力检测的升级【8】。3.6 改进了与终端之间的“对话”能力：它可以发送像素尺寸查询，更好地处理 OSC 4 调色板请求，并能更完整地向终端报告光标状态、鼠标事件、粘贴模式与焦点变化等信息。这些改进看起来全是底层细节，但指向同一个目标：让 tmux 在各种终端模拟器、各种 SSH 环境、各种远程会话中获得更一致、更可靠的表现。

除此之外，3.6 还带来了一批细节改进【8】：新增 selection-mode 命令，可以更明确地控制复制模式中的选择方式；在切换到备用屏幕时，可以保存并恢复图片，对使用现代终端查看图形输出的用户很友好；剪贴板处理更智能，可以自动检测终端是否支持 OSC 52，让本地与远程剪贴板的交互更稳定；Unicode 支持也有长足进步——更准确地识别区域指示符与 emoji 组合修饰符、自动忽略韩文填充字符避免显示错位、改善宽字符的渲染方式、提升对奇怪字符和边缘 Unicode 的兼容性。

把这些放在一起看，3.6 不是一次“跨时代”的发布，没有惊艳的新概念，但它非常扎实。它把“让用户在更多环境里更舒服地使用 tmux”这件事，向前推进了一大步。用一句话概括：3.6 是 tmux 在稳定繁荣期的一次诚意之作——不制造惊喜，只解决真实问题。

## 第七节 tmux 3.6b（2026 年 5 月 20 日）：纪录片的最新一集

2026 年 5 月 20 日，tmux 3.6b 发布，这是目前最新的稳定版【9】。

3.6b 不是一个“大版本”，它是在 3.6 基础上的维护性更新——继续修复问题、继续打磨细节。但把它放在版本史上，它有一个特殊的身份：它是这部纪录片截至目前的最新一集，也是我们观察 tmux“当下状态”最直接的窗口。

如果你把 3.0 到 3.6 的发布间隔拉出来看，会发现一件很有意味的事：从 2019 年的 3.0 到 2025 年的 3.6，将近七年的时间里，tmux 只发布了六个正式版本。而这七年，恰恰是终端世界变化最大的七年：AI 编程工具崛起、远程开发成为主流、SSH 会话重新变得珍贵、终端模拟器之间的竞争白热化。tmux 在这种环境下保持“一年一版、每版扎实”的节奏，本身就是一种态度——不因为外部世界喧嚣就乱了自己的步伐。

更有意思的是，这七年里 tmux 反而“重新时髦”了。一个工具活了快二十年，按理说应该进入“被新一代工具替代”的周期，但 tmux 在 AI 时代迎来了一波新的关注【10】：当 AI 代理开始在服务器上编排命令、批量操作会话时，它们需要一个稳定、可脚本化、会话可持久化的终端环境——这恰恰是 tmux 从第一天起就在做的事情。曾经被认为是“老古董”的会话持久化，如今成了 AI 编排时代的基本功。

3.6b 就是这样一部纪录片的最新一集：没有戏剧性，但有确定性。它告诉我们，这个项目还在被维护、还在被认真对待、还在为下一个版本积累着细小的进步。纪录片没有落幕，只是这一集刚好播到这里。

## 第八节 生态扩张：插件把 tmux 变成了“平台”

纪录片的第五集，镜头从 tmux 本身，转向了围绕它生长的整个世界。

在生态扩张之前，我们需要先厘清一个容易混淆的点。很多人把 tmux 的分屏和终端模拟器的分屏混为一谈——比如 iTerm2 自己也支持分屏，那和 tmux 有什么区别？区别在于本质：tmux 的会话和分屏状态是存在服务器端的，保存在那台运行 tmux 的机器上；而终端模拟器的分屏只是客户端界面的视觉装饰，窗口一关，什么都没了【11】。一个像电视直播，一个像现场演出。这个区别，决定了 tmux 在“远程工作”“服务器运维”“会话持久化”这些场景里不可替代的地位，也决定了它的生态可以围绕“服务器端状态”长出无数工具。

而生态爆发的第一个标志，是 TPM（tmux 插件管理器）的成熟【12】。TPM 出现之前，tmux 的配置扩充主要靠“往 .tmux.conf 里粘贴片段”，混乱且难以管理。TPM 出现之后，插件变成了一行声明：

```bash
# ~/.tmux.conf 中声明要加载的插件
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
run '~/.tmux/plugins/tpm/tpm'
```

从此，安装、更新、清理插件都成了标准化操作。TPM 的成熟，让“给 tmux 装插件”这件事从“高级玩家的手艺”变成了“普通用户也能上手的日常”，这是生态能够大规模扩张的前提。

生态爆发的第二个标志，是 tmux resurrect 与 tmux continuum 这对组合的流行【12】。resurrect 负责“保存会话现场”——窗口布局、面板分割、当前目录、甚至环境变量，全部序列化保存下来；continuum 则负责“自动续命”——每隔一段时间自动保存一次，并在 tmux 启动时自动恢复。两个插件加起来，效果是：你昨天的 tmux 会话现场，今天开机后原样还原，就像从来没关过电脑一样。

这对组合在 GitHub 上的星标累计超过了 2 万【12】。2 万颗星意味着什么？意味着有数以万计的开发者把“会话持久化”当成了自己工作流里不可缺失的一环。而这一切，都建立在 tmux 本身“会话在服务器端、可序列化、可脚本化”的架构之上。生态不是凭空长出来的，它长在架构的土壤里。

我记得曾经有一位运维工程师说过一句让我印象深刻的话：在 tmux 里配上 resurrect 和 continuum 之后，他再也不怕服务器半夜重启了——第二天早上连上去，一切都在，就像昨晚什么都没发生。这句话的潜台词是：生态工具的价值，不在于它本身有多炫，而在于它把 tmux 本来就有的能力，变成了用户可以依赖的日常。

## 第九节 周边生态全景：一个工具的“朋友圈”

如果给 tmux 画一张“朋友圈”，除了插件体系，还站着一圈独立的周边工具。它们不是 tmux 的一部分，却与 tmux 深度共生。

第一个是 tmuxinator【13】。它的理念是：把“一套开发环境该长什么样”写成一份 YAML 配置文件——项目目录、窗口名称、每个窗口里跑什么命令——然后一个命令，整套路环境瞬间拉起。比如：

```bash
# ~/.tmuxinator/dev.yml：一条命令启动整套开发环境
name: dev
windows:
  - editor: vim
  - server: npm run dev
  - logs: tail -f app.log
```

对经常切换项目、每个项目都有固定窗口布局的开发者来说，tmuxinator 把“环境搭建”从每次手动操作，变成了可以版本管理、可以分享给同事的配置文件。

第二个是 tmuxp【13】。它和 tmuxinator 思路类似，但更强调“布局的导出与复现”：你可以在当前 tmux 会话里手动排好布局，然后一条命令把整个会话冻结成 YAML 文件，在任何一台机器上原样恢复：

```bash
# 把当前会话布局保存为 YAML，随时在另一台机器上复现
tmuxp freeze my-session -o workspace.yaml
tmuxp load workspace.yaml
```

第三个是 fzf-tmux【13】。它把模糊查找工具 fzf 与 tmux 结合起来——在 tmux 的弹出窗口里运行模糊搜索，在历史会话、历史命令、文件列表之间快速穿梭。对重度使用模糊查找的开发者来说，fzf-tmux 是“终端里的快捷键”，让查找这件事从“记忆路径”变成了“输入几个字母，剩下的交给模糊匹配”。

第四个是 tmux-powerline【13】。它把状态栏变成了一条信息流：电池电量、系统负载、Git 分支、网络状态、当前目录……所有你想随时看到的信息，都可以通过它塞进状态栏：

```bash
# 加载 tmux-powerline 主题，让状态栏显示丰富信息
set -g status-right '#(tmux-powerline)'
```

这四个工具只是 tmux 周边生态的一个切片。它们有一个共同点：都是围绕“tmux 的会话与布局可以程序化描述”这一特性生长出来的。tmux 本身坚持只做“终端复用”这一件事，把窗体的状态、布局、会话开放成可脚本化的接口；而周边工具则在这些接口上，替用户完成了“环境管理”“查找集成”“状态可视化”这些 tmux 自己刻意不做的事。

这种分工，是 tmux 生态最健康的地方：核心保持克制，生态负责繁荣。工具的第二生命，往往就来自这种克制与繁荣的平衡。

## 第十节 版本演进的规律总结：克制、渐进、兼容

十九年的版本史看完了，最后我们来总结规律。如果把这二十余个版本当作一部纪录片重新回放，你会看到三条清晰的主线。

第一条主线是克制。从第一天起，tmux 就对“功能膨胀”保持着警惕。Marriott 有一条公开的原则，在邮件列表里被反复引用：bloat due to features like serial line I/O is not going to happen【14】——意思是，那种因为“多支持一种串口 I/O”之类的理由而带来的功能膨胀，在 tmux 身上不会发生。有人曾问为什么 tmux 不支持串口通信，Marriott 的回答就是这句。这句话在版本史上的分量，怎么强调都不过分：它意味着 tmux 有清晰的边界感——我知道自己是什么，也知道自己不是什么。一个项目能活十九年，靠的往往不是“什么都能做”，而是“坚决不做什么”。

第二条主线是渐进。tmux 的演进几乎从不激进：大版本之间的功能跳跃是有限的，每一次发布都在前一个版本的基础上向前走一小步。哪怕是 2.0 那样的不兼容更新，改的也只是选项的层级，而不是用户心智模型的颠覆。这种渐进式的演进，让用户始终有安全感——升级 tmux 不需要重新学习，只是多了几个新东西。十九年里，tmux 从来没有“推倒重来”过，这正是它积累起庞大用户群的原因之一。

第三条主线是兼容。这里的兼容有两层意思：一层是对终端模拟器的兼容——tmux 必须在上千种终端环境里都能可靠工作；另一层是对用户配置的兼容——除非万不得已，不破坏用户的 .tmux.conf。稳定繁荣期的几乎所有工作，都可以归结为“把兼容这件事做到极致”。Marriott 还有一句常被引用的话：people use tools in surprisingly different ways【15】——人们使用工具的方式，远比作者想象的多样。这句话解释了为什么 tmux 对兼容如此执着：既然不知道用户会怎么用，那就把每一种用法都尽量照顾好。

把这三条主线放在一起，你会得到一个耐人寻味的结论：tmux 的十九年，不是“不断变多”的十九年，而是“不断变好”的十九年。它没有在功能数量上无限膨胀，而是在功能质量、架构清晰度、兼容稳定性上持续深耕。从 0.1 到 3.6b，版本号只跳了三个大段，但每一个数字背后，都是一次“少而精”的选择。

十九年前，一个北爱尔兰开发者在一台老旧的电脑上敲下第一行代码时，大概不会想到这个项目会走到今天。但如果你今天问他：这十九年你做得最对的决定是什么？答案大概不是“我加了多少功能”，而是“我没有加那些不该加的功能”。克制，是一个工具能优雅地老去、并且老而不衰的真正秘密。

如果给这部纪录片配一个旁白，大概会是这样一句：一个工具最好的命运，不是被写进教科书，而是在每一个真实的终端里，日复一日地被人使用。版本号会继续跳动，纪录片会继续拍摄，而 tmux 与用户之间这场关于“加什么、不加什么”的漫长对话，也还远远没有结束。

## 第十一节 独思时刻：不兼容更新是一种勇气

> 💡 **独思时刻：** 每次版本升级提示“此版本有不兼容变更”时，我看到的都不是一行技术说明，而是一个决定——作者决定把短期的用户便利，兑换成长期的项目健康。这需要一种特别的勇气，因为不兼容更新的收益是隐性的，代价却是显性的：升级的人要改配置，要踩坑，要在邮件列表里抱怨，甚至有人会因此离开。而收益——更清晰的架构、更少的维护负担、未来几年的开发速度——是看不见的，要等很久才显现。做这种决定的人，必须承受“短期内被骂、长期内被感谢”的时间差。2016 年的 tmux 2.0 就是这样：当时改配置的人未必高兴，但今天回头看，正是那次“折腾”，让 tmux 的架构在之后十年里一直站得住。我敬佩这种勇气，因为它本质上是一种长期主义的信仰——相信只要方向是对的，时间会替你做裁判。反过来说，如果一个项目永远不做不兼容更新，它大概也永远无法真正长大。所有值得长期维护的软件，都逃不过那道坎：你总得在某一天，为未来的自己得罪一下现在的用户。

## 第十二节 独思时刻：生态是工具的第二生命

> 💡 **独思时刻：** 我一直觉得，一个工具能不能“活过第一代用户”，看的不是它本身的功能，而是它周围长出了什么。tmux 本身很克制——它只做终端复用，不做会话管理模板，不做布局文件，不做状态栏美化。但它把接口开放出来了：选项、命令、hook、脚本，一切都可以被程序化地描述和组合。于是，tmuxinator 替它解决了“项目环境一键拉起”，resurrect 和 continuum 替它解决了“会话持久化”，fzf-tmux 替它解决了“查找集成”，tmux-powerline 替它解决了“状态可视化”——所有这些 tmux 自己“不做”的事，生态都替它做了，而且做得比它亲自做更好。这让我想到一句老话：一个城市的伟大，不在于市政府建了多少大楼，而在于市民自发建起了多少家园。tmux 是那个只修路的市政府，而它的用户，是那个把路两边建成繁华街区的市民。生态是工具的第二生命——第一生命是功能，第二生命是围绕它生长起来的一切。第一生命会随着时间老化，而第二生命，只要地基不倒，就会一直长出新东西来。这也是为什么我写这本书时，坚持把版本史和生态史放在同一章里讲——它们本来就是同一个故事的两面。

## 尾声：纪录片没有落幕

十九年，从 0.1 到 3.6b，版本号的变化本身就是一部微缩的软件史【9】。第一个公开版本发布时，tmux 的作者是 Marriott 一个人，用户是邮件列表里几十个好奇的早期采纳者；今天，tmux 的用户是全世界数以百万计的开发者、运维工程师、数据科学家——以及，越来越多地，那些替人类在服务器上执行任务的 AI 代理【10】。从一个工程师的“不爽”，到终端复用器的现代标准，这条路 tmux 走了十九年。

但你知道吗？这十九年里，tmux 解决的那个核心问题，从来就没有变过：终端会话的生命周期，不应该跟桌面的网络连接绑定。2007 年，这个问题困扰着忍受不了 Screen 的 Marriott；2026 年，这个问题困扰着每一个在远程服务器上训练模型、跑任务、编排流水线的开发者。工具在变，用户在变，问题没变——而正因为问题没变，十九年前的那个答案，今天依然成立。

这部纪录片拍到 2026 年 5 月 20 日这一集，还没有落幕。下一个版本会带来什么，我们不知道；但我们可以确定的是，只要还有人需要在恶劣环境下干活，只要还有人的终端会话需要活过断线，tmux 的版本号就还会继续跳下去。而我们的故事，也还要继续讲下去——下一章，我们把镜头对准那些真实使用 tmux 的人，看看这个工具在十九年里，究竟如何改变了他们的工作方式。

*注：本章关于邮件列表讨论、用户反馈等场景细节，部分来自原稿素材整理与公开讨论的历史记录；核心事实（各版本发布时间、关键特性、生态数据、Marriott 原则）均来自本章末尾所列资料来源，与全书事实清单保持一致。*

**本章资料来源：**

【1】tmux 版本历史记录（首次代码提交，2007 年 10 月 10 日）

【2】原稿素材整理：synchronize-panes 功能的用户需求与实现经过

【3】tmux 手册与版本历史：配置命令统一为 set-option、bind-key、set-environment

【4】tmux 2.0 发布说明（2016 年初）：不兼容更新，terminal-overrides 转为 server 选项

【5】Red Hat 官方文档与 RHEL 发行说明：RHEL 7.6 弃用 Screen，RHEL 8.0 移除并推荐 tmux

【6】tmux 3.0 发布说明（2019 年）：引入 hooks 系统

【7】tmux 3.3a 发布说明（2022 年 6 月 9 日）：稳定性与 bug 修复

【8】linuxiac.com 与 linuxmi.com：tmux 3.6（2025 年 11 月）发布报道（pane-scrollbars 原生滚动条、Mode 2031 主题、新变量、查询处理改进）

【9】tmux 版本发布记录：3.6b（2026 年 5 月 20 日）最新稳定版

【10】原稿素材整理：tmux 在 AI 编排时代的重新流行

【11】tmux 官方文档：会话与分屏状态存储于服务器端；终端模拟器分屏仅为客户端视觉装饰

【12】GitHub 数据：TPM 插件管理器成熟；tmux resurrect 与 tmux continuum 组合星标累计超 2 万

【13】tmuxinator、tmuxp、fzf-tmux、tmux-powerline 官方项目文档

【14】Nicholas Marriott 在邮件列表中的公开表述：“bloat due to features like serial line I/O is not going to happen”

【15】Nicholas Marriott 公开访谈（undeadly.org）：“People use tools in surprisingly different ways”


【14】Nicholas Marriott 在邮件列表中的公开表述：“bloat due to features like serial line I/O is not going to happen”

【15】Nicholas Marriott 公开访谈（undeadly.org）：“People use tools in surprisingly different ways”

---

# 第5章 核心概念模型——理解Client/Server架构

要理解 tmux 为什么能在 Screen 统治了将近二十年后异军突起，你得先理解一个关键词：**架构**。

Screen 不是没有架构——它有，但它的架构像一栋被改建了二十年的老房子。1987 年刚盖好时是一间平房，1989 年加了个阁楼，1993 年又接出一间厨房，后来线路老化、管道交错，你想在墙上钉个钉子都怕凿到电线。tmux 最大的不同在于，Marriott 在 2007 年动工之前就画好了一张蓝图。这张蓝图的核心，是一个叫做 **Client/Server** 的模型。

这张蓝图不是纸上谈兵。2007 年，Marriott 只花了几星期就写出了一个叫 nscr（new screen）的快速原型，专门用来验证 client/server 这条路走不走得通。原型虽小，骨架却是对的——2007 年 10 月 10 日，tmux 的第一次代码提交诞生；一个月后，11 月 20 日，第一版公开面世。后来的二十年里，tmux 往上添了无数功能，但那张骨架一天都没换过。所以这一章，我们不背快捷键，不讲配置项，只做一件事：把这张骨架彻底看懂。

你不需要是系统架构师也能懂这个模型。来，我们用一个简单得不能再简单的比方——

**Server 是饭店的厨房，Client 是坐在餐桌前的你。**

厨房里的大厨（server 进程）负责做所有菜：煮汤、煎鱼、烤牛排，每道菜都有它自己的锅和火候。你坐在外面（client 进程）拿着菜单点菜，菜端上来你吃，吃完结账走人。你走了之后，厨房还在运作——大厨没停下来等你，其他桌的客人还在吃，灶台上的汤还在咕嘟咕嘟冒着泡。

这就是 tmux 的运作方式。你在终端里敲 `tmux new -s work`，相当于向厨房下了一单。tmux 的 server 进程在后台启动（如果还没启动的话），创建一个名叫 work 的会话，然后你的 client 连接上去，你看到了一个终端窗口。你在里面跑 `npm run build`，编译开始了。这时候你按 `Ctrl+b d` 脱离会话，相当于你起身离桌——但你那锅汤还在灶上煮着。你关上笔记本电脑，挤地铁回家，打开电脑重新 SSH 到服务器，敲 `tmux attach -t work`，编译进度条可能已经走到百分之八十了。

这个场景我亲身经历过无数次。2015 年我在一家云计算公司做运维，有一次凌晨两点在机房调试存储集群的重平衡脚本，脚本预计跑四个小时。我当时的 leader 跟我说：“你盯着就行，别睡着。”我说不用盯，敲了 `Ctrl+b d`，合上电脑回家睡觉。第二天早上到公司重新附着，脚本正好跑完，日志完整地躺在那儿。他后来也装了 tmux。

## 5.1 架构总览：一个 Server、N 个 Client、一条 Socket

先给全景。tmux 的进程模型里只有两种角色：

- **Server（服务端）**：默认情况下，一台机器上只有一个。它常驻后台，管理着所有的会话、窗口、面板；
- **Client（客户端）**：可以有多个，每个就是你眼前那个“终端画面”，负责把你的键盘输入送进 server，再把 server 的画面输出画给你。

它们之间靠什么说话？**Unix domain socket**——这是同一台机器内部进程间通信的套接字，不走 TCP/IP，没有网络延迟，也不需要端口。socket 文件默认躺在 `/tmp/tmux-<uid>/default` 这个位置（uid 是你的用户 ID）。画一张图，把这三个角色摆在一起：

```bash
┌──────────────────────────────────────────┐
│                 server                   │
│  管理所有 session / window / pane        │
│  常驻后台，不依附任何终端                │
└──────────────┬───────────────────────────┘
               │ Unix domain socket
               │ /tmp/tmux-1000/default
       ┌───────┴────────┐
┌──────┴─────┐  ┌───────┴─────┐
│  client 1  │  │  client 2   │
│ （你的SSH） │  │ （同事的SSH）│
└────────────┘  └─────────────┘
```

当你敲 `tmux attach` 时，client 进程打开这个 socket，向 server 发送一个“我要连接默认会话”的请求，server 回复“好的，这是你看到的画面”，然后双方保持一个双向通道——你敲的每个键都从 client 流向 server，server 把每个字符的显示输出推回 client。

这里有一个全书最重要的认知，请务必刻进脑子里：**client 只是 server 的“视图”。** 你看到的一切——窗口列表、光标位置、面板布局、状态栏——都不是 client 自己保存的，而是 server 实时渲染给你看的。client 坏了、关了、消失了，server 里的东西一样都不少。这就好比房间里的监控摄像头：你把摄像头砸了，房间还在，房间里的人还在干活。

顺带一提，你天天按的那个 `Ctrl+b`，在概念词典里有个正式的名字：**前缀键（prefix key）**。它是一条分界线——按完 `Ctrl+b` 之后再按的键，是发给 client 的命令（d 表示脱离、c 表示新建窗口）；而不按前缀键、直接敲下去的字符，才会原封不动地透传给 pane 里的 shell。理解了这条分界线，你就明白 tmux 为什么能同时“监听”两种输入：它平时是一条透明的通道，直到你按下前缀键的那一瞬间，才切换成指挥中心。

一个 session 可以同时被多个 client 附着。这就是结对编程（pair programming）的基础——你和同事分别 SSH 到同一台服务器，各自运行 `tmux attach -t pair`，你们看到的画面完全同步，一人敲键盘另一人能看到光标移动。我 2017 年在一次远程黑客松上用过这个功能，三个人分处三个时区，同时在一个 tmux 会话里改一份配置文件，谁敲错了命令其他人当场喊停。那种“身体不在场但光标在场”的协作感，比屏幕共享软件更轻量、更直接。

## 5.2 四层结构详解：从会话到伪终端

概念词典里说得清楚：tmux 把终端世界分成四层——**Session（会话）→ Window（窗口）→ Pane（面板）→ Pty（伪终端）**。Server 不算在这四层里，它是承载这四层的进程底座。接着用饭店的比喻往下嵌套：

- **Session（会话）** 相当于一张餐桌。一个 server 可以同时开很多桌，桌与桌之间互相看不见、听不着，各有各的菜单和客人；
- **Window（窗口）** 相当于桌上的一只菜盘。一桌可以摆很多盘，每盘有自己的编号和名字，你夹完这盘夹那盘；
- **Pane（面板）** 相当于你切开的一块牛排。一盘牛排可以切成好几块，每一块都是独立的一份；
- **Pty（伪终端）** 相当于牛排底下那块烤盘——它才是真正“托着”食物、和灶台直接接触的东西。

图画出来是这样：

```bash
Server（一个常驻进程）
 └─ Session work（一张餐桌）
     ├─ Window 0（一只菜盘）
     │   ├─ Pane 0.0 → Pty → bash
     │   └─ Pane 0.1 → Pty → vim
     └─ Window 1（另一只菜盘）
         └─ Pane 1.0 → Pty → tail -f app.log
 └─ Session lab（另一张餐桌）
     └─ Window 0
         └─ Pane 0.0 → Pty → python train.py
```

Pty 这一层值得多说两句，因为它是整个架构里最容易被忽略、却最本质的一层。**伪终端（pseudo-terminal，简称 pty）是内核提供的、模拟真实终端的设备对**：一头连接着你 pane 里运行的 shell 进程（比如 bash 或 zsh），另一头连接着 tmux server。Shell 以为自己在跟一个真实的终端交互，实际上所有输入输出都被 tmux“截获”了。这就是 tmux 能在你和 shell 之间插入一层“魔法”的原因：它能看到你敲了什么，也能控制你看到了什么。

再举个例子帮你建立直觉：你在 pane 里跑 `top`，然后按 `Ctrl+b z` 把这个 pane 放大成全屏，再按一次恢复。`top` 从头到尾都不知道自己被“放大”过——它只知道自己在一台终端上输出，至于画面怎么呈现，那是 tmux 的算法。反过来，你在 pane 里运行的 `vim` 收到的是 tmux 转达的按键，tmux 随时可以决定“这个键我不传给你”，从而实现了各种拦截与增强。这就是 pty 这层的全部意义：一个让 tmux 可以“插手”的接口。

每一层解决一个粒度的问题。如果你只需要同时跑两个命令，pane 就够了；如果你需要切换多个项目，session 更适合；如果你需要管理几十台服务器，server 这个层级保证了所有状态都在一个进程里统一调度，不会变成一堆散落的进程让你用 `ps` 去大海捞针。

> 💡 **独思时刻：** 我第一次真正理解 tmux，不是在读文档的时候，而是在一次网络崩溃的时候。那天我在远程服务器上跑一个数据迁移，跑到一半公司网络断了。我盯着屏幕上那个消失的终端，第一反应是完了，任务肯定没了。等网络恢复，我抱着试试看的心态重新 SSH 上去，敲下 `tmux attach`，迁移进度条居然还停在断网前的位置，稳稳地往前走。那一刻我突然想通了一个比喻：**进程是墙上的画，终端只是窗口。** 我们一直把终端当作进程的“容器”，以为窗口碎了、画就跟着碎了；其实终端从来只是进程的“窗口”——窗口碎了，画还在墙上挂着，你换个窗框（重新 attach）就能继续看。SSH 断开杀死的从来不是你的任务，而是你观看任务的窗口。tmux 干的全部事情，说穿了就是把“窗口”和“画”之间的那根耦合剪断。想通这一点之后，tmux 所有的快捷键、所有的配置、所有的插件，在我眼里都变成了同一个故事的注脚。

## 5.3 Server 的常驻机制：你走了，厨房没走

现在来回答全书最核心的一个问题：为什么 detach 之后，一切还能照常运行？

关键就在 Server 进程的身份上。**Server 是一个守护进程（daemon），它在后台运行，不依附于任何终端。** 普通程序的命运取决于它的父进程和它的控制终端：终端一关，控制终端消失，内核会向终端的前台进程组发送 SIGHUP 信号，进程默认的处理方式就是退出——这正是第〇章里“SSH 断开、任务泡汤”的底层原理。而 tmux 的 server 在启动时就已经把自己从这种依赖里解放了出来：它不占任何终端，自然也没有任何“终端关闭”能波及到它。你按 `Ctrl+b d`，client 向 server 说一声“我要走了”，然后退出；server 把会话标记为“没有活跃 client”，但所有 pane 里的进程照常运行。

这里必须区分两个动作，很多初学者在此栽过跟头：

- **`Ctrl+b d`（detach，脱离）**：只是你的 client 离开。server、session、window、pane、进程，全都原封不动。
- **`exit`（退出 shell）**：你亲手关掉了 pane 里的 shell。如果这是唯一一个 pane，window 会随之关闭；如果这是最后一个 window，session 会关闭；如果这是最后一个 session，server 也会退出——一个完整的轮回结束。

我见过有人在生产服务器上敲 `exit` 而不是 `Ctrl+b d`，结果跑了一天的数据处理任务被终止了。所以请把这句话焊死在脑子里：**离开用 detach，别用 exit。**

另外，server 的常驻还带来一个隐藏福利：**你不需要“进入” tmux 才能管理 tmux。** 敲 `tmux ls` 就能列出所有会话；敲 `tmux new-session -d -s backup` 就能在完全不打扰任何 client 的情况下，静默创建一个后台会话跑备份任务；写脚本自动化管理 tmux 会话更是家常便饭。一个常年开机的服务器上，server 可能已经连续运行了几个月甚至几年——中途你换过十几台电脑、换过几百次网络，它一次都没重启过。一个一直活着的进程，就是你在数字世界里的“根据地”。

再补一个细节，帮助你理解 server 到底“躲”在哪。当你 `Ctrl+b d` 之后，server 进程仍然活着，但它既不在你的终端前台，也不占任何可见的窗口——它就像饭店打烊后还在后厨收拾的大厨，你看不见他，但他一直在。想验证的话，`ps aux | grep tmux` 就能看到那个孤零零的 server 进程，它的父进程往往早已被系统收编为 1 号进程（init）——也就是说，它已经“独立”于任何会话树，没有任何终端的生死能牵动它。这也解释了为什么哪怕你的 SSH 早就断开、你的电脑早已关机，任务依然在跑：因为那锅汤的灶台，从来就不在你的厨房里。

## 5.4 Session 的多面性：一张桌子，N 桌客人

Session 是 tmux 里最“人格化”的一层——因为它是你给它起名字的那一层。

一个 server 可以同时有多个 session，它们之间完全隔离：工作目录、环境变量、活跃窗口、粘贴缓冲区，谁也不干扰谁。我习惯同时开着三个会话：一个叫 work 跑公司项目，一个叫 lab 做实验性代码，一个叫 ops 连各种监控面板。它们之间切换就像换台——`Ctrl+b s` 呼出会话列表，上下选择，回车，整个世界就换了一个。

命名会话是 tmux 教你的第一件“正确的事”：

```bash
tmux new -s work        # 创建一个名叫 work 的会话
tmux ls                 # 列出所有会话
tmux attach -t work     # 重新附着到 work
```

不命名的话，你面对 `tmux ls` 的输出就是一堆数字 ID，谁是谁全靠脑子记；命名之后，一行 `tmux ls` 就像看菜单一样一目了然。会话也可以随时重命名（`Ctrl+b $`），把“项目临时会话”改成“订单服务排查”，让三个月后的自己不至于对着名字发呆。

Session 还有一个容易被忽略的灵活之处：**一个 session 可以被多个 client 同时附着**（前面说的结对编程），也可以被同一个 client 反复附着、脱离、再附着。它不是一个“一次性”的东西，而是一个有生命的、持续存在的空间。此外，每个 session 都保存着自己窗口的工作目录——你上次在哪个路径，回来还在那个路径。早上 `tmux attach -t frontend`，看一眼 dev server 状态，`Ctrl+b s` 切到后端，再看一眼 API 日志，整个过程手不离键盘，也不需要重新 cd 到各个项目目录。

再说一个环境变量的细节。创建一个新 session 时，tmux 会从当时的 shell 环境里继承环境变量；但如果你从另一个已有的 session 里创建一个新的 window，它继承的是**创建这个 window 的 client 所在的环境**，而不是 server 进程的初始环境。这个设计让同一个 session 的不同 window 可以有不同的环境变量配置——比如在一个 session 里用 `AWS_PROFILE=prod` 开两个 window 跑生产服务，再 `AWS_PROFILE=dev` 开另外两个 window 跑开发环境，它们在同一个 session 里平起平坐，互不干扰。架构的“对”，往往就体现在这种细节的弹性上。

Session 的隔离还有一个容易被低估的维度：**粘贴缓冲区（paste buffer）**。tmux 的复制粘贴历史是按 session 隔离的——你在 work 会话里复制的一段敏感配置，不会在切到 lab 会话时误粘出去，这在多人共用的服务器上尤其重要。当然，隔离也意味着每个 session 都要独立建立自己的“上下文”，但这正是它的价值所在：每一张桌子，都只属于它的客人。

## 5.5 Window 的灵活性：墙上的画可以换房间

Window 是 tmux 里的“标签页”——这是最容易向浏览器用户解释的概念：一个 session 里可以有多个 window，每个 window 有自己的编号（从 0 开始）和名字。你在同一个“前端”标签里写代码，切到“后端”标签看 API 日志，再切到“git”标签处理合并冲突。

常用操作先列出来（第 6 章会展开，这里先建立直觉）：

```bash
Ctrl+b c    # 新建窗口
Ctrl+b n    # 下一个窗口
Ctrl+b p    # 上一个窗口
Ctrl+b w    # 列出所有窗口
Ctrl+b ,    # 重命名当前窗口
```

但 Window 比浏览器的标签页更“硬核”的地方在于：**窗口可以在会话之间移动。** 概念词典里写得明明白白：Window 可在会话间移动。比如你在 work 会话里开了一个临时窗口查资料，查完了想把它并进 lab 会话，一条命令就搬走了：

```bash
tmux move-window -s work:3 -t lab
```

这背后正是 client/server 模型的威力：window 是 server 管理的一等对象，不是某个 client 屏幕上的一个区域。屏幕只是“投影仪”，window 是“放映机里的胶片”——你把放映机从这间房搬到那间房，胶片跟着走，正在放映的内容一秒都不中断。

还有一点值得品味：**切换窗口永远不会杀掉窗口里的进程。** `Ctrl+b n` 只是让 server 换一块“投影幕布”给你看，原来那个窗口里的编译、日志、调试器全都在继续跑。这跟浏览器的“后台标签页被冻结”完全不同——tmux 的每个窗口都是活的，哪怕你几个月不切过去，它也在那里忠实地运行着。这又回到了那个比喻：窗口只是墙上的窗，窗后面的画（进程）有自己的生命。

再教你一个管理窗口编号的小习惯：`Ctrl+b c` 新开的窗口编号总是顺延的，但如果你频繁开窗关窗，编号会变得七零八落——`tmux renumber-windows` 可以重新编号，或者直接在配置里让它自动重排。窗口名也是一样：默认取自进程名，但 `Ctrl+b ,` 可以随时改成你觉得有意义的名字，比如把 3 号窗口从 python 改成 deploy。命名是给人看的，而人，才是 tmux 服务的对象。

## 5.6 Pane 的本质：独立 pty 与布局系统

Pane 是窗口内的分屏区域。一个 window 可以被切割成多个 pane，**每个 pane 对应一个独立的伪终端（pty）**——也就是说，每个 pane 里跑的是一个完全独立的 shell，它们之间唯一的联系就是“共享同一个窗口的屏幕区域”。

分屏操作：

```bash
Ctrl+b %        # 垂直分屏（左右排列）
Ctrl+b "        # 水平分屏（上下排列）
Ctrl+b z        # 当前 pane 全屏 / 恢复
Ctrl+b Space    # 循环切换布局
```

这里有个历史细节值得记住：**垂直分屏是 Screen 从未实现的功能，却是 tmux 天生就有的能力。** Screen 的分屏是后来硬加进去的，实现方式很别扭——你得先进入“分屏模式”再划分区域，而且只能水平切。2009 年 Marriott 在访谈中说垂直分屏开发“约完成 90%”，而 Screen 那边，垂直分屏从来没能进入生产版本。为什么 tmux 做起来这么轻松？因为 pane 从一开始就是第一层概念，分屏只是“把窗口切成多个 pane”，是数据结构的自然操作；而 Screen 是在一个没有 pane 概念的 monolithic 进程里硬生生凿出“区域”这种东西，每凿一刀都在跟二十年前的架构搏斗。

再往上一层，tmux 还提供了一套**布局系统**，让你一键把面板排成常见样式：

- **even-horizontal**：所有面板等高排列；
- **even-vertical**：所有面板等宽排列；
- **main-horizontal**：一个主面板在上，其余在下；
- **main-vertical**：一个主面板在左，其余在右；
- **tiled**：网格平铺，尽可能把面板铺成接近正方形。

`Ctrl+b Space` 会在这些布局之间循环。无论你怎么手工拖拽调整，server 都会记住每个 pane 的精确尺寸和位置——因为这是 server 的数据，不是 client 的装饰。你断开重连，面板布局分毫不差，就像那盘牛排切好之后，连盘子的朝向都替你记着。

Pane 的真正威力在于“并行观看”。我跑一个长时间构建时，习惯开两个 pane：左边跑构建，右边 `tail -f build.log`，构建结束的瞬间就能在日志里看到原因，不用来回切窗口。再配合 `Ctrl+b z` 把正在关注的 pane 临时放大成全屏——写代码时眼睛只盯一处，需要监控时一键呼出，这种“分而治之、聚而观之”的节奏，是单窗口的终端给不了的。后来大名鼎鼎的 synchronize-panes（在一个窗口的所有 pane 里同时敲同一个命令）功能，最初就是用户提出来的需求——当一个架构足够干净时，用户会自然而然地开始提“如果能这样就好了”的想法。

## 5.7 状态栏的玄机：server 画给你的仪表盘

新手第一次进入 tmux，第一眼注意到的往往不是“多窗口”，而是屏幕底部那条**状态栏**。它到底在显示什么？

默认状态栏大致长这样：

```bash
[work] 0:bash* 1:git- 2:logs                 Wed Aug 12 10:23 0.15 0.10 0.08
```

从左到右解读：

- **`[work]`**：当前 session 的名字。如果你同时开着 work、lab、ops 三个会话，这一眼就告诉你“你现在在哪张桌”。
- **`0:bash* 1:git- 2:logs`**：当前 session 的窗口列表。每个窗口显示“编号：名字”，名字默认取自窗口里运行的进程名；星号 `*` 标记当前所在的窗口，`-` 表示某个窗口在后台有输出活动（提醒你“那边有事发生”）。
- **`Wed Aug 12 10:23`**：日期和时间。别小看它——在 SSH 到远程服务器时，这行字告诉你“服务器认为现在是什么时候”，而不是你本地笔记本的时间。
- **`0.15 0.10 0.08`**：系统负载（load average），分别是过去 1、5、15 分钟的平均负载。服务器卡不卡，扫一眼这里就有数。

但状态栏最玄妙的地方不在内容，而在**渲染者**。状态栏不是你的 shell 画的，也不是你的终端模拟器画的——**它是 server 画的**。这意味着：你断开 SSH、换一台电脑、重新 attach，状态栏恢复原样，一秒都不差，因为它本来就不是“你的屏幕的一部分”，而是“server 的输出的一部分”。每次看到状态栏，都等于看到 client/server 架构在眼前活生生地运行。

这里再送你一个观察：状态栏上的每一个字符，都是 server 根据它自己的状态实时算出来的——当前 session 是谁、有几个 window、哪个窗口有活动、系统负载多少。换句话说，**你看到的整个“tmux 界面”，本质上都只是 server 状态的投影。** 这正是它能在任意终端、任意电脑上保持一致的原因——因为它根本不依赖“你”这个客户端拥有任何特殊能力，你只需要一个能显示文字的窗口。

这也解释了为什么状态栏可以高度定制——你可以在 `.tmux.conf` 里写命令，塞入 git 分支、电量、IP 地址、任意脚本的输出（第 6 章会演示）。因为状态栏本来就是 server 渲染的一块“仪表盘”，server 想画什么就能画什么。命令行能敲的，配置文件里就能写——Marriott 从第一天就咬死的原则，在这里再次兑现。

## 5.8 与 Screen 的对比：为什么“单进程”输给了“双角色”

为了把 tmux 的架构优势说透，我们看一眼 Screen 的进程模型。

Screen 启动时，也会创建一个守护进程，但这个进程**同时扮演 server 和 client 两个角色**。所有窗口、所有会话的状态都在同一个进程的地址空间里。这意味着什么？意味着如果你通过 SSH 连到远程服务器，Screen 进程在服务器上运行，你的 SSH client 负责把画面传输给你。断开 SSH 时，远程的 Screen 进程没受影响——这一点 Screen 也能做到，会话持久化它也有。好的，公平地说，Screen 也能持久化。

但差异在别处。

**差异一：多 client 附着的稳定性。** 在 Screen 里，如果某个 client（也就是你 SSH 连接的那一端）因为网络抖动异常断开，Screen 的主进程可能会在处理信号时出问题，导致所有窗口一起卡住甚至崩溃。这种情况我在 2013 年碰到过不下五次——一个同事在共享 Screen 会话里断开连接后，整个 Screen 进程挂了，所有人丢进度。tmux 把每个 client 单独隔离，一个 client 崩溃不会牵连 server 进程。你可以把 tmux 的 client 想象成“只读视图”，server 才是真正的“数据源”。

**差异二：新功能的添加难度。** Screen 的代码经过二十年累积，约两万行 C 代码挤在一个进程模型里，新增一个功能往往意味着要在那个庞大的进程模型里“打洞”。而 tmux 从一开始就把 pane 作为第一层概念，分屏是天然支持的。Marriott 在 2009 年的一次邮件列表讨论中说过一句话：“Adding features to tmux is easy because the client/server model makes it obvious where new code should go.”（在 tmux 里加功能很容易，因为 client/server 模型让你一眼就知道新代码该放在哪儿。）他对“膨胀”的警惕也是出了名的：“bloat due to features like serial line I/O is not going to happen”（因为串行线路 I/O 这类功能导致的膨胀，永远不会发生）——架构上的克制，同样是架构的一部分。

架构的分野，最终写进了两个项目的命运里。2009 年，OpenBSD 开发者 Paul Irofti 在邮件列表里提议引入 tmux；同年 7 月，tmux 被导入 OpenBSD 的基本系统——要知道，OpenBSD 以对代码质量的苛刻闻名，Theo de Raadt 亲自审阅 tmux 的代码，只花了两小时，只发现一两个次要问题。而 Screen 这边，2004 年开发停滞，2008 年发布最后一个版本 4.0.3，垂直分屏永远停留在了愿景里。到了 2018 年，RHEL 7.6 宣布弃用 Screen；2019 年，RHEL 8.0 彻底移除，官方推荐 tmux。同样是“守护进程 + 会话持久化”，为什么一个死了、一个活了？因为 **monolithic（单体）架构把所有功能揉在单一进程里，改一处牵全身；client/server 架构把“状态”和“视图”分开，每个角色只做自己该做的事。** 越滚越大的毛线球，终有一天滚不动；一开始就分好层的大厦，才有往上盖的可能。

Marriott 还有一句话常被引用：“People use tools in surprisingly different ways.”（人们使用工具的方式，惊人地各不相同。）一个架构如果不尊重这种差异，就注定只能服务一种用法。

> 💡 **独思时刻：** 我过去一度以为“架构好”是一个纯技术优点，意思是代码更干净、bug 更少。但用了 tmux 六年后，我逐渐意识到一个更实在的区别——当你在一个架构清晰的项目里遇到问题时，你能自己推断出“它大概是怎么实现的”，从而更快地找到解决办法；而在一个架构混乱的项目里，你只能靠背诵“它奇怪的工作方式”来绕开问题。我在 Screen 上背下了所有快捷键和它们的“例外情况”——比如某些快捷键在分屏模式下不可用，某些配置在启动后不能改。在 tmux 上我几乎没背过什么，因为它的行为总是符合我的直觉。这个差异听起来很小，但它决定了你是在“理解”一个工具还是在“忍受”一个工具。

## 5.9 与终端模拟器分屏的本质区别：装饰与真实

聊到这里，你可能会想：我用的 iTerm2（或者 VS Code、Windows Terminal）也能分屏，也能保存会话，为什么还需要 tmux？

问得好。这正是最容易混淆的地方，而答案恰好是 client/server 架构的又一次现身说法。

iTerm2 之类的终端模拟器确实集成了分屏和会话恢复功能。但请注意一个关键词：**集成**。iTerm2 的分屏，是 iTerm2 这个客户端程序在自己窗口里画出来的两块区域——它是**客户端视觉装饰**。你电脑上的 iTerm2 进程死了、窗口关了、SSH 断了，那些分屏、那些标签页，瞬间蒸发，因为它们的“存在”只存在于你本地这个进程的内存里。它们从未离开过你的电脑。

tmux 的分屏呢？它运行在服务器端。那个 pane 是 server 管理的真实对象，那个会话里的进程真实地跑在服务器上。iTerm2 只是负责把 server 传来的画面显示出来而已。**终端模拟器分屏是客户端装饰，tmux 分屏是服务器端真实会话**——这就是本质区别。

有一个“一秒钟检验法”可以帮你区分：**断开连接，重新连上，看那个东西还在不在。** 如果它是装饰——比如 iTerm2 的分屏、VS Code 的恢复窗口——断线重连后就没了，你得手动重新搭；如果它是真实会话——比如 tmux——重连后一切原样，连光标位置都记得。对，SSH 断了，iTerm2 的“会话恢复”帮不了你什么，因为恢复的只是“怎么重新打开窗口”这个动作；而 tmux 恢复的是“那一锅汤还在灶上煮着”这个事实。有意思的是，iTerm2 甚至专门做了一个 tmux 集成模式，能让 tmux 的每个 window 直接显示成 iTerm2 自己的标签页——这恰恰反过来证明了 tmux 的会话是真实的服务器端存在：模拟器只是换了一层更顺手的皮肤来“观看”它。VS Code 的集成终端同理：它可以开多个终端、可以分屏、可以“恢复上次的终端”，但这一切都是 VS Code 这个客户端自己维护的，关掉它就什么都没了。

所以两者根本不是竞争关系，而是分工关系：**终端模拟器解决“屏幕长什么样”，tmux 解决“会话活多久”。** 一个管装饰，一个管生命。这也是为什么 tmux 在 AI 编码时代反而更受重视——Claude Code、Codex 这类 agent 需要跑长任务，需要把屏幕切成多个区域做多 agent 编排，更需要“网络断了任务不停”的保证。AI 可以接管键盘，但接管不了“进程生命周期管理”这件事；那正好是 tmux 的地盘。

## 5.10 深入一层：转义序列、terminfo 与屏幕状态

这一节我们往下钻一层，看看 tmux 到底是怎么“画屏幕”的。理解了这一层，你才算真正看懂了 client/server 架构里“视图”二字的分量。

先纠正一个直觉：**终端不是显示器，而是一台“协议设备”。** 你以为你在终端里看到的文字是“显示”出来的，其实它们是“流”出来的——程序往终端输出一串字节，其中大部分是普通字符，但也混着大量**转义序列（escape sequence）**：控制光标位置的、改变文字颜色的、清屏的、滚动的。比如下面这几串（ANSI 转义序列的经典例子）：

```bash
echo -e "\033[31m红字\033[0m"    # 把后面文字变成红色，再复位
echo -e "\033[2J"                # 清屏
```

你的终端模拟器（iTerm2、Windows Terminal、终端.app……）的职责，就是把这串字节流解析成屏幕上的画面。

问题来了：世界上有几百种终端，有的支持彩色，有的不支持；有的光标移动用这套序列，有的用那套。程序怎么知道面对的是哪种终端？答案是查数据库——**terminfo**。这是一个描述“每种终端有什么能力、用什么转义序列实现这些能力”的系统数据库，而 `TERM` 环境变量告诉程序“我现在是哪种终端”。程序（以及 ncurses 这类终端库）拿到 `TERM`，去 terminfo 里查出对应的能力表，才知道该怎么画。

tmux 和这个体系的关系有两层。

**第一层，tmux 是 terminfo 的消费者。** tmux 依赖 ncurses 库来读写终端信息（早年编译 tmux 时，你得先解决 libevent 和 ncurses 两个依赖）。它要知道你现在的终端（`xterm-256color`、`tmux-256color` 之类）支持什么，才能决定用哪种转义序列把画面画给你。

**第二层，tmux 自己也是一台“终端”。** 还记得 pty 吗？在 pty 的那一头，tmux 扮演的就是“终端”的角色——pane 里运行的 shell 以为自己在跟一个真实终端对话，那个“终端”就是 tmux。所以 tmux 会把自己的 `TERM` 设置为 `screen` 或 `tmux-256color`，告诉 pane 里的程序“你们面前的终端是我，我有这些能力”。pane 里的程序（比如 vim、htop）就按照 tmux 的能力来画屏幕；tmux 再把它们的输出转译成你眼前这个真实终端能理解的序列。

为了让你对“屏幕状态”有个具体印象，可以这样想象：server 的内存里维护着一张“画布”，每个格子记录着三样东西——这个位置显示什么字符、什么颜色、光标现在在哪。pane 里的程序（比如 vim）以为自己在往真实终端上写，其实它每写一个字符，tmux 都先更新自己这张画布，再把画布上变化的部分翻译成你的终端能懂的转义序列发过去。你换终端时，tmux 只需要把整张画布用新终端的“语言”重新说一遍。这就是为什么 tmux 能跨终端、跨电脑、跨 SSH 层数地保持画面一致——**它画的不是屏幕，而是一张可以被任何屏幕重新渲染的画布。**

这意味着什么？意味着**你换终端，tmux 不用换；你换电脑，会话不用换。** 你在 iTerm2 里开一个 tmux 会话，跑到一半换成 Windows Terminal 重新 attach——画面照样正确。因为 tmux 已经把“屏幕状态”（光标在哪、每个位置是什么字符、什么颜色）完整地保存在 server 的内存里，它随时可以把这份状态用任何终端的语言重新画一遍。这就是“client 只是视图”的终极含义：视图可以换，状态不动摇。

## 5.11 一个完整的生命周期：从启动到关闭

把上面的概念串起来，我们完整走一遍 tmux 从启动到关闭的全过程。

你在终端里敲下 `tmux`：

一、shell 解析命令，执行 tmux 二进制文件。
二、tmux 检查 `/tmp/tmux-1000/default` 这个 socket 是否存在。
三、如果不存在，tmux 会启动一个 server 进程，监听这个 socket，然后创建一个默认 session，再创建一个默认 window，最后在 window 里创建一个默认 pane。
四、如果 socket 已经存在（server 已经在跑了），tmux 直接连接上去，创建一个新的 session（或者你指定 attach 到已有的 session）。
五、server 在你的 pane 里启动一个 shell（比如 bash），shell 打印出提示符，等待你敲命令。
六、你每敲一个键，client 通过 socket 传给 server，server 把按键送给 shell，shell 的返回输出再通过 server 传回 client 显示。

你按 `Ctrl+b c` 创建一个新 window。Server 收到这个命令，在当前 session 里创建一个新的 window 和一个新的 pane，打开一个新的 shell。你按 `Ctrl+b 2` 切换到第二个 window，server 把显示内容切换成那个 pane 的输出。

你按 `Ctrl+b d` 脱离会话。Client 进程向 server 发送一个“我要走了”的消息，然后退出。Server 把当前会话标记为“没有活跃 client”，但所有进程继续运行。你关掉终端，SSH 连接断开，但 server 纹丝不动。

你重新 SSH 登录，敲 `tmux attach`。新的 client 连接到同一个 socket，server 把当前 session 的屏幕内容发送过来，你的光标回到刚才离开的位置。

你敲 `exit` 退出 shell，pane 关闭。如果这是最后一个 pane，window 关闭。如果这是最后一个 window，session 关闭。如果这是最后一个 session，server 可以选择退出（默认是退出的）。一个完整的轮回结束。

这套生命周期看起来再自然不过，但请注意一个事实：**从 2007 年 10 月 10 日 tmux 第一次提交，到 2026 年 5 月 20 日最新的 3.6b 稳定版，这个核心循环几乎没有变过。** 四层结构、socket 通信、进程隔离，这些在最初几个月的原型里就已定型。Marriott 在 2009 年的一次 IRC 对话里提到：“I spent most of the early time getting the architecture right. Features came later.”（我早期花最多时间把架构弄对。功能是后来才加的。）这句话值得反复咀嚼——在 tmux 的故事里，“架构”不是某个阶段的任务，而是它后来能做所有事情的前提。

> 💡 **独思时刻：** 很多教程喜欢强调 tmux 的快捷键列表有多长、功能有多全，但我个人用了十一年 tmux，日常高频用到的组合键不超过十个。剩下的那些，要么是特定场景才需要，要么是我根本没记住——遇到需求的时候临时翻 cheatsheet 就好了。所以我从来不鼓励新手去“背”快捷键。更关键的是，当我理解了 client/server 架构之后，我发现我连“记”都不太需要了——因为每个快捷键都可以从架构里推出来：既然 server 管理着所有 window，那“新建窗口”就应该是 `Ctrl+b c`（c 是 create）；既然 window 里有多个 pane，“切换 pane”就是 `Ctrl+b` 加方向键；既然 session 是独立的工作空间，“脱离”就是 detach 的 d。这些不是死记的符号，而是架构的自然投影。一次理解架构，胜过一百次记住快捷键。tmux 不是考试，记不住就查，查多了自然就记住了。把脑子留给真正需要记忆的东西——比如你项目的业务逻辑，而不是某个组合键到底切换了什么布局。

## 5.12 本章小结：把骨架刻进脑子里

让我们把这一章浓缩成一张小卡片，你可以随时回来复习：

- 一个 server 常驻后台，N 个 client 来来去去，一条 Unix domain socket 连接它们——client 只是 server 的“视图”；
- 四层结构：Session → Window → Pane → Pty，Server 是承载这一切的底座；
- detach 是离开，exit 是关门：离开可以随时回来，关门则一切结束；
- Screen 把 server 和 client 揉成一个 monolithic 进程，tmux 把它们分开——分开，才有今天的 tmux；
- 终端模拟器分屏是客户端装饰，tmux 分屏是服务器端真实会话；
- 状态栏是 server 画给所有 client 的仪表盘，不是某个终端的涂鸦；
- 核心比喻：进程是墙上的画，终端只是窗口。

围绕这套骨架，还长出了 TPM、tmux-resurrect、tmux-continuum 这样 GitHub 星标累计超过两万的插件生态——骨架稳，才能长出森林。

下一章，我们把这套骨架真正用起来——从安装 tmux 开始，建立你的第一个会话。当你亲手敲下那几行命令时，请记住：你不是在学一个新工具的快捷键，你是在用一套早就该被认真对待的架构，重新组织你和计算机之间的关系。

---

# 第6章 从零到一——tmux实战入门

上一章我们把 tmux 的四层结构和 client/server 通信模型拆开看了一遍，原理层面的东西讲得差不多了。但我知道你心里在想什么：道理我都懂，可手指头到底往哪放？

这章我们就从零开始，让 tmux 真正在你自己的机器上跑起来。我的目标很朴素：读完这章，你今晚就能在日常工作中顺手用上它，而不是对着一个空白终端发呆。我不会逼你背快捷键——恰恰相反，我会告诉你哪些可以暂时不用记。跟着我一步一步敲，每一段代码我都会解释它为什么长这样。手放上键盘，我们开始。

## 6.1 安装与第一跑——五分钟从零到一

安装 tmux 这件事，在 2026 年的今天已经简单到近乎无聊。但在 2007 年，这完全是另一番光景：那时候 tmux 的第一版刚在 11 月 20 日公开【7】，没有现成的软件包，用户得自己下载源码编译，还要手动解决 libevent 和 ncurses 这两个依赖库的版本问题。从一个想法到能跑起来，中间隔着一整晚的 configure 报错。而今天，主流操作系统的软件仓库里都有它，一条命令搞定：

```bash
# Debian / Ubuntu
sudo apt install tmux

# RHEL / CentOS / Fedora
sudo dnf install tmux

# Arch Linux
sudo pacman -S tmux

# macOS（需要先装 Homebrew）
brew install tmux

# Windows：装好 WSL2 之后，在 Ubuntu 里执行
sudo apt install tmux
```

这里有个值得多看一眼的细节：在 RHEL 系发行版上，`dnf install tmux` 已经是官方推荐的做法——因为 Red Hat 在 RHEL 7.6 起宣布弃用 Screen，到 RHEL 8.0 时彻底移除了它【16】。换句话说，在企业级 Linux 的世界里，tmux 已经不只是“一个好用的工具”，而是系统默认给出的那个答案。至于 OpenBSD 用户，连安装这一步都省了：自从 2009 年 tmux 被导入 OpenBSD base【11】，每台装好系统的机器上默认就有 tmux，和 ls、cat 住在同一个屋檐下。

装完之后，先验证一下版本：

```bash
tmux -V
```

正常情况下你会看到类似 `tmux 3.6b` 的输出【19】。看到版本号，安装就完成了。

现在，什么参数都不带，直接敲：

```bash
tmux
```

屏幕底部会多出一条带颜色的状态栏——你已经在一个 tmux 会话里了。这就是最纯粹的启动方式：创建一个默认会话、一个默认窗口、一个默认面板，然后往里面丢一个 shell。你敲的每一条命令都照常执行，仿佛什么都没发生过，只有底部那条状态栏在提醒你：世界已经变了。

这条状态栏值得多看两眼，它是你理解 tmux 的第一扇窗（第 5 章我们剖析过它的玄机）。从左往右，它通常显示：当前会话的名字、会话里的窗口列表（默认只有一个 `0:bash` 之类的标签）、当前时间。等你开了多个窗口、多个会话之后，这短短一条栏就是你在这个多层世界里的导航仪。

不过，我不建议你只敲 `tmux`。养成一个习惯：**给会话起名字。**

```bash
tmux new -s work
```

`-s work` 就是给这个会话起名叫“work”，`-s` 是 `-session` 的缩写。为什么要起名？想象你同时开着三个会话：一个跑数据库迁移，一个编辑代码，一个盯日志。如果都用默认的裸 `tmux` 启动，你面对 `tmux ls` 的输出就是一堆数字 ID（`0`、`1`、`2`），谁是谁全靠脑子记；起了名字之后，一眼就知道：

```bash
tmux ls
migration: 1 windows (created Wed Aug 12 10:23:45 2026)
code: 3 windows (created Wed Aug 12 09:10:20 2026)
logs: 2 windows (created Wed Aug 12 08:30:15 2026)
```

一目了然。会话是有名字的，这是你在这套体系里建立秩序的第一步。几个最常用的会话级命令，现在就可以记住：

| 命令 | 作用 |
|------|------|
| `tmux new -s 名字` | 创建命名会话 |
| `tmux ls` | 列出所有会话 |
| `tmux attach -t 名字` | 重新附着到指定会话 |
| `tmux kill-session -t 名字` | 关闭指定会话 |

到这里，你就已经完成了“从零到一”：能启动、能查看、能切换、能关闭。但 tmux 的核心能力，要等你进入会话内部才能真正感受到。

## 6.2 前缀键哲学——tmux 的“魔法开关”

在 tmux 会话里，你敲的绝大部分按键都会原样传给 shell，tmux 自己不插手。但有一组按键是留给 tmux 用的——这就是**前缀键**，默认是 `Ctrl+b`。

怎么理解它？你可以把前缀键想象成相机快门：按下 `Ctrl+b` 的那一下，等于半按快门，告诉 tmux“接下来这个键我要说给你听，不是给 shell 的”；紧接着敲下的那个键，才是真正的指令。比如 `Ctrl+b` 然后 `d`，就是“脱离当前会话”。

这个设计让我想起当年第一次用 tmux 时摔的一个大跟头。那是 2015 年，我在一个远程服务器上跑编译任务，听说了 tmux 的大名，随手就 `tmux` 进去了。我不知道前缀键的存在，在会话里按方向键想移动光标，结果什么都没发生。我以为是终端卡死了，直接关掉了窗口——然后发现跑了两个小时的编译任务也跟着没了。后来我才明白：方向键在 tmux 默认配置下本来就不会做什么，真正的问题是我连 `Ctrl+b d` 都不会，硬生生把终端关了，把会话连锅端了。

所以这一节的第一课：**记住 `Ctrl+b d`。** 这是 tmux 里最重要的组合键——脱离会话但不关闭它。你随时可以回来 `tmux attach`，一切照旧。

接下来是更本质的问题：为什么是 `Ctrl+b`？这背后有一段值得讲的历史。tmux 诞生时，GNU Screen 是事实标准，而 Screen 的前缀键是 `Ctrl+a`。Marriott 本人是 Screen 的重度用户，背下了全部快捷键【1】，按理说沿袭 `Ctrl+a` 最省事。但他刻意选了 `Ctrl+b`——因为 `Ctrl+a` 在几乎所有 shell 的 Readline 里都是“光标跳到行首”的快捷键，这个位置已经被占了。终端世界里，组合键是稀缺资源，每个键位都有它的原住民；`Ctrl+b` 在绝大多数程序里无人认领，选它等于选了一块无主之地。从这段取舍里你能看出 tmux 的设计态度：不与周围的生态打架，把冲突留给别人，把顺滑留给自己。

那为什么很多老手会把前缀键改成 `Ctrl+a`？原因有两个。一是肌肉记忆：从 Screen 时代走过来的老用户，手指头已经被 `Ctrl+a` 驯化了十几年，改不回来也不愿意改。二是手感：`Ctrl+b` 需要左手小指按住 Ctrl、食指去够 b，对某些人来说有点别扭；`Ctrl+a` 的两个键挨得近，小指一按、无名指一带，一气呵成。改法很简单，在配置文件里写上三行：

```bash
# 把前缀键从 Ctrl+b 改成 Ctrl+a
unbind C-b
set -g prefix C-a
bind C-a send-prefix
```

`unbind C-b` 是解除原来的绑定，`set -g prefix C-a` 是把前缀设为 `Ctrl+a`，`bind C-a send-prefix` 则处理一个细节：当你需要在会话里向程序发送真正的 `Ctrl+a`（比如在 shell 里跳到行首）时，连按两次 `Ctrl+a` 就能把第二个原样传下去。

这件事在社区里争议不小。反对改键的人说，`Ctrl+a` 在终端里本来就有“回到行首”的语义，改了之后误触概率大增；支持改键的人说，左手不用往下挪两行，效率优先。我自己用了六年 `Ctrl+b`，习惯了之后没觉得有什么问题，就一直没改。但我得说句公道话：这纯粹是个人习惯，没有对错。tmux 的伟大之处恰恰在于它不替你决定——前缀键是你的，配置文件是你的，怎么顺手怎么来。改就是了，tmux 不拦着你。

## 6.3 核心快捷键速成——20 个以内的“最小学会”清单

现在进入正题。tmux 的快捷键总数超过一百个，但你完全不需要全记住。我把日常最高频的操作压缩成一份“最小学会”清单，总共 17 个，分三类：会话、窗口、面板。先把这张表过一遍，再讲怎么用最少的时间把它变成肌肉记忆。

**会话管理（3 个）：**

| 快捷键 | 作用 |
|--------|------|
| `Ctrl+b d` | 脱离当前会话（detach），会话继续运行 |
| `Ctrl+b s` | 弹出会话列表，交互式切换 |
| `Ctrl+b $` | 重命名当前会话 |

**窗口管理（6 个）：**

| 快捷键 | 作用 |
|--------|------|
| `Ctrl+b c` | 创建新窗口（create） |
| `Ctrl+b n` / `Ctrl+b p` | 切换到下一个 / 上一个窗口 |
| `Ctrl+b 0-9` | 直接跳到编号为 0-9 的窗口 |
| `Ctrl+b ,` | 重命名当前窗口 |
| `Ctrl+b w` | 列出所有窗口，交互式选择 |
| `Ctrl+b &` | 关闭当前窗口（会弹确认提示） |

**面板管理（8 个）：**

| 快捷键 | 作用 |
|--------|------|
| `Ctrl+b %` | 垂直分屏：左右各一半 |
| `Ctrl+b 双引号键` | 水平分屏：上下各一半 |
| `Ctrl+b 方向键` | 在面板间移动焦点 |
| `Ctrl+b o` | 切换到下一个面板（按顺序循环） |
| `Ctrl+b z` | 当前面板最大化 / 恢复（再按一次还原） |
| `Ctrl+b 空格` | 循环切换面板布局 |
| `Ctrl+b q` | 短暂显示每个面板的编号 |
| `Ctrl+b x` | 关闭当前面板（会弹确认提示） |

一共 17 个，不到 20。但我要给你一个更“狠”的建议：**先只记 5 个。** 哪 5 个？`Ctrl+b d`（脱离）、`Ctrl+b c`（新窗口）、`Ctrl+b n`（切窗口）、`Ctrl+b %`（分屏）、`Ctrl+b 方向键`（切面板）。这 5 个覆盖了 80% 的日常动作，剩下的 12 个，等你真正遇到“需要”的场景再回来查这张表。查过一次的操作，第二次基本就记住了；查过三次的，就再也不会忘了。

> 💡 **独思时刻：** 我见过太多人把学习曲线当成一场背诵考试。市面上几乎所有 tmux 教程都在炫耀快捷键清单有多长、功能有多全，仿佛记不住几十个组合键就不配用这个工具。可我用了 tmux 十一年，日常高频用到的组合键不超过十个，剩下的全是“用到再查”。有一次同事看我操作，惊讶地问“你到底背了多少快捷键”，我说我真正背下来的只有七个：d 脱离、c 建窗口、n 切窗口、% 分屏、方向键切面板、z 缩放、空格换布局。其余的都在速查表里躺着，需要时翻一眼就好。工具不是考试，记不住就查，查多了自然就记住了——真正该用脑子记住的，是你项目的业务逻辑，而不是 `Ctrl+b` 后面跟的某个冷门字母。二十个快捷键的克制，不是能力不足，而是把认知资源留给真正重要的事。学习任何工具都一样：先让五个键成为本能，再让功能列表成为字典，而不是反过来。

## 6.4 面板操作实战——一个窗口里的多重视野

面板（pane）是 tmux 最直观的优势：在同一个窗口里切分多个区域，同时看多个终端。它的本质，是第 5 章讲过的那个模型——每个面板对应一个独立的伪终端（pty），由 server 统一调度。

分屏从两个键开始：

| 快捷键 | 作用 |
|--------|------|
| `Ctrl+b %` | 垂直分屏，左右各占一半 |
| `Ctrl+b 双引号键` | 水平分屏，上下各占一半 |

这里有个记法上的小窍门。很多新手搞混“垂直分屏”和“水平分屏”——到底是左右还是上下？我自己的记忆方式是看符号：`%` 的符号像被一条竖线从中间劈开，所以它是垂直分屏（左右两半）；双引号键的两个点一上一下，所以它是水平分屏（上下两层）。符号自己就是示意图，比死记“百分号等于垂直”靠谱得多。

分屏之后，焦点（也就是光标所在的那个面板）默认落在新面板上。接下来你需要在面板之间移动：

| 快捷键 | 作用 |
|--------|------|
| `Ctrl+b 方向键` | 把焦点移到指定方向的面板 |
| `Ctrl+b o` | 按创建顺序循环切换焦点 |
| `Ctrl+b q` | 每个面板上短暂显示编号，然后按数字直达 |

`Ctrl+b q` 是个被低估的键：面板一多，你往往记不清谁是谁，按一下 q，每个面板角落会闪现一个数字，直接按数字跳过去，比一下一下挪方向键快得多。

面板操作里我最喜欢的是缩放：

| 快捷键 | 作用 |
|--------|------|
| `Ctrl+b z` | 当前面板最大化占满整个窗口，再按一次恢复原样 |

这个键的妙处在于它把“分屏”和“专注”统一了。你平时让 vim 和日志左右分屏，需要专心改代码时，`Ctrl+b z` 把 vim 放大成全屏，改完再按一下，布局原封不动地回来。不用退出分屏，不用重建布局，一切在毫秒间切换。

分屏久了，尺寸往往不合心意。调整大小也有专门的键：按住 `Ctrl+b`，再按住方向键不放，面板边界就会朝那个方向移动。想精确控制的话，也可以敲命令 `resize-pane`，比如 `:resize-pane -L 10` 把当前面板向左扩大 10 个单元——在 tmux 里敲 `:` 会进入命令行模式，这一点我们稍后细说。

还有一个高频操作是布局循环：

| 快捷键 | 作用 |
|--------|------|
| `Ctrl+b 空格` | 在 tmux 预设的五种布局之间循环切换 |

五种预设布局分别是：even-horizontal（所有面板等高横排）、even-vertical（所有面板等宽竖排）、main-horizontal（主面板在上，其余在下）、main-vertical（主面板在左，其余在右）、tiled（棋盘格均分）。`Ctrl+b 空格` 每按一次切换一种，看到顺眼的就停手。这里也能看到 tmux 的设计哲学：布局不是写死的，而是一套可以任意组合的规则，预设只是起点。

分屏的典型场景，是“编辑 + 观察”的黄金搭档：左边用 vim 改代码，右边跑一个 `tail -f` 看日志。改了代码，右边实时看到新日志——完全不用切换窗口，也不用在前后台任务之间来回 `fg`、`bg`。我自己常年只用垂直分屏，左右各一半；水平分屏用得很少，因为笔记本屏幕本来就矮，再水平切一刀，每个区域就扁得没法看人了。屏幕的形状决定你的布局习惯，这没什么好惭愧的。

有人会问：我用的 iTerm2 之类的终端模拟器不也能分屏吗，何必用 tmux？这是个好问题，答案在第 5 章已经埋下了伏笔：终端模拟器的分屏只是客户端上的视觉装饰，关闭窗口就什么都没了；而 tmux 的分屏是服务器端的真实会话【24】，SSH 断了、窗口关了，面板里的进程照常运行。装饰会消失，墙上的画不会。

## 6.5 窗口操作实战——一个会话里的多个“标签页”

如果把会话比作一个项目，那**窗口**就是这个项目里的多个标签页。一个会话可以开几十个窗口，每个窗口独立显示内容、拥有自己的布局和面板组合。

窗口操作的核心键位如下：

| 快捷键 | 作用 |
|--------|------|
| `Ctrl+b c` | 创建新窗口 |
| `Ctrl+b n` / `Ctrl+b p` | 切换到下一个 / 上一个窗口 |
| `Ctrl+b 0-9` | 直接跳到编号 0-9 的窗口 |
| `Ctrl+b ,` | 重命名当前窗口 |
| `Ctrl+b w` | 列出所有窗口，交互式选择 |
| `Ctrl+b &` | 关闭当前窗口 |

新手常犯的一个错误，是把“会话”和“任务”一一对应：每开一个新项目就创建一个新会话。更好的做法是**一个会话开多个窗口**。举个例子，我处理一个微服务项目时，会在同一个会话里开四个窗口：1 号窗口写代码，2 号窗口跑单元测试，3 号窗口看 API 日志，4 号窗口连数据库。这样我只需要记住会话名（比如 `tmux new -s order-service`），进去之后 `Ctrl+b 1` 到 `Ctrl+b 4`，在各个上下文之间瞬间跳转。如果每个任务都单开会话，`tmux ls` 会膨胀成一张无法直视的长名单，会话切换的代价也随之水涨船高。

窗口之间的“搬运”也值得一提。假如你开了 5 个窗口，觉得 3 号窗口的内容其实应该放在 1 号旁边，可以用 `swap-window` 命令：在 3 号窗口里敲 `:swap-window -t 1`，两个窗口的位置就交换了。这类命令不用背，知道存在即可，遇到需求时用 `Ctrl+b ?` 打开帮助页搜索——是的，tmux 自带帮助，所有绑定都能查。

重命名窗口这个操作被很多人忽略，但它带来的体验提升非常明显。默认情况下，窗口名是窗口里正在运行的命令（比如 `bash` 或 `vim`），开三四个窗口之后，状态栏上显示的全是 `bash`、`bash`、`bash`，你分不清谁是谁。养成习惯：每次创建新窗口，立刻 `Ctrl+b ,` 给它起个名字，比如“code”、“test”、“logs”、“db”。状态栏上一目了然，切换时也不再靠猜。这个习惯的成本是两秒钟，收益是每次切换窗口时省下的两秒钟和一点烦躁——积少成多，一天省下几分钟，一年就是十几个小时。

## 6.6 会话管理实战——你永远不会“丢失”工作

窗口之下是会话，会话之上才是整个 tmux 世界的地基。会话管理这一节，其实是全章最重要的一节，因为**会话持久化就是 tmux 存在的理由**——还记得序幕里那个深夜断线的场景吗？

会话层面的核心操作：

| 快捷键 | 作用 |
|--------|------|
| `Ctrl+b d` | 脱离当前会话（detach），会话在后台继续运行 |
| `Ctrl+b s` | 显示会话列表，交互式切换 |
| `Ctrl+b $` | 重命名当前会话 |

`Ctrl+b d` 是全书最重要的组合键，值得再说一遍：脱离，不是关闭。detach 之后，server 继续在后台运行，你的编译继续跑，你的 vim 继续开着，你的下载继续传——只是暂时没有“人”在看它了。`Ctrl+b s` 在屏幕底部弹出一个列表，方向键上下选择、回车切换，就像给会话世界装了一个电梯。这个列表在你只有一两个会话时用处不大，但当你同时开着五六个会话、每个里又有好几个窗口时，它就是你的导航仪。

2024 年秋天我在赶一个项目，同时开了四个会话：一个跑前端 dev server，一个跑后端 API，一个连着生产数据库的只读副本查数据，还有一个专门用来 git 操作和写提交信息。那段时间我每天的工作节奏是：早上 `tmux attach -t frontend`，看一眼 dev server 状态，`Ctrl+b s` 切到后端，再 `Ctrl+b s` 切到 git 仓库。整个过程手不离键盘，也不需要重新 cd 到各个项目目录——tmux 会话里每个窗口的当前目录都是独立保存的，你上次在哪个路径，回来还在那个路径。

关闭会话同样有讲究。会话的关闭是“摧毁”级别的操作，tmux 会给你确认的机会：

| 命令 / 快捷键 | 作用 |
|--------|------|
| `Ctrl+b x` | 关闭当前面板（弹确认） |
| `Ctrl+b &` | 关闭当前窗口（弹确认） |
| `tmux kill-session -t 名字` | 从外部关闭指定会话 |
| `tmux kill-server` | 关闭所有会话，杀掉整个 server |

这里我特别想强调 `Ctrl+b d` 和“直接关掉终端窗口”之间的天壤之别。直接关终端，等于粗暴地拔掉电源——进程收到 SIGHUP，一切归零；而 `Ctrl+b d` 是优雅地离席——server 还在厨房里做菜，客人只是暂时离开了餐桌。这个区别，就是第 0 章那个深夜噩梦和云淡风轻之间的全部距离。

## 6.7 配置文件入门——让 tmux 变成“你的”tmux

前面所有的快捷键都是默认的，但 tmux 的真正魅力在于：几乎一切都可以改。而改造的入口，就是配置文件。

tmux 的配置文件默认放在 `~/.tmux.conf`（系统级配置在 `/etc/tmux.conf`，但个人配置放在 home 目录下更灵活，也方便跟随你的 dotfiles 到处迁移）。配置文件的结构非常简单，就三条规则：

第一，以 `#` 开头的是注释，tmux 会跳过它们。第二，每一行都是一条 tmux 命令，和你交互式敲的命令语法完全一致。第三，配置文件在你启动 tmux 时按顺序执行一遍。

第二条值得停下来多说几句——它是 Marriott 当年最得意的设计之一。在 Screen 的世界里，配置语法是一套和命令行完全不同的“失传咒语”，查一个功能怎么配比实现它还费劲（第 1 章我们聊过这个痛点）。而 tmux 立下的规矩是：**命令行里能敲的，配置文件里就能写**，语法完全一致【8】。你在会话里敲 `:set -g mouse on` 能生效，写进配置文件同样生效。学一次，到处用；不用维护两套知识。这个看似简单的决定，让 tmux 的配置门槛比 Screen 低了一个数量级。

配置文件里最常用的两类语句是绑定键和选项设置。绑定键用 `bind` 和 `unbind`：

```bash
# 解除 Ctrl+b 的绑定
unbind C-b

# 把 Ctrl+b 绑定到"重载配置"这条命令
bind C-b source-file ~/.tmux.conf

# 把 | 键绑定为"垂直分屏"（不用再按前缀键里的 % 了）
bind | split-window -h
```

选项设置用 `set`，常用的作用域有三个：`set -g` 设置的是 server 级选项（对整个 tmux 生效），`setw`（或 `set -w`）设置窗口级选项，`set` 不加前缀则设置当前面板级选项。比如：

```bash
# server 级：开启鼠标支持，对所有会话生效
set -g mouse on

# 窗口级：开启窗口所有面板的同步输入
setw synchronize-panes on

# 面板级：把当前面板的历史缓冲区扩大到 5000 行
set history-limit 5000
```

配置文件还有一个配套动作：热加载。你改了配置之后，不用退出 tmux 重进，直接在会话里执行 `tmux source-file ~/.tmux.conf` 就能让改动生效。为了方便，很多人会给自己绑一个重载快捷键：

```bash
# 按 Ctrl+b r 重载配置，并在状态栏提示"已重载"
bind r source-file ~/.tmux.conf \; display "配置已重载"
```

注意这里的 `\;`——它表示“执行完前一条命令，紧接着执行下一条”。这是 tmux 命令语法里的小机关，把两条命令串成一条。等你开始写复杂的绑定，会频繁用到它。

## 6.8 个性化配置案例——鼠标、状态栏、历史与复制模式

配置文件的理论讲完了，现在看四个最实用的个性化案例。每一个我都先讲“为什么”，再给配置。

**案例一：鼠标支持。**

```bash
set -g mouse on
```

就这么一行。开启之后，你可以用鼠标点击切换面板、拖动面板边界调整大小、滚动查看历史输出、点击状态栏切换窗口。很多人问：这么好用，为什么 tmux 默认不开？原因有两条：一是历史包袱，鼠标支持是后来才加进 tmux 的，默认行为要照顾老用户习惯；二是冲突，开启鼠标后，你在终端里用鼠标选中文本复制的手势会变成 tmux 的操作，需要按住 Shift 才能恢复原生的文本选择。但说实话，在 2026 年，绝大多数现代终端模拟器（iTerm2、Kitty、Alacritty、Windows Terminal）都能和 tmux 的鼠标模式配合得很好，开着的收益远大于代价。我建议你直接开。

**案例二：状态栏美化。**

默认的绿条能看，但谈不上好看。状态栏的样式由一组选项控制：

```bash
# 背景和前景色（用 colour 编号，235 是很深的灰，250 是浅灰）
set -g status-bg colour235
set -g status-fg colour250

# 左侧显示用户名和主机名，右侧显示时间
set -g status-left '#[fg=green] #S #[default]'
set -g status-right '#[fg=colour245]#(whoami)@#H #[fg=colour240]%H:%M'

# 窗口列表：当前窗口高亮加粗，其他窗口正常显示
set -g window-status-format '#I:#W'
set -g window-status-current-format '#[fg=green,bold]#I:#W'

# 面板边框：当前面板用醒目的橙色
set -g pane-border-style fg=colour235
set -g pane-active-border-style fg=colour208
```

这里能看到 tmux 状态栏的底层逻辑：它本质上是“格式字符串”的拼接，`#S` 代表会话名，`#W` 代表窗口名，`#I` 代表窗口编号，`#H` 代表主机名，`#()` 里可以塞任意 shell 命令的输出。理解了这套模板语法，状态栏想显示什么就显示什么——甚至可以显示 git 分支、CPU 负载、天气。别急着背，知道“状态栏是可编程的”这个事实就够了。

**案例三：历史记录。**

```bash
set -g history-limit 5000
```

默认情况下，每个面板只保留 2000 行历史输出，超过的部分滚不回去。做运维、看日志的人，最恨的就是“输出太多，前面的被冲掉了”。把上限调到 5000 甚至 10000，用 `Ctrl+b [` 进入复制模式后就能往回翻看更多内容。代价是每个面板多占一点内存，对一个进程来说微不足道。

**案例四：复制模式。**

复制模式是 tmux 的“隐形大招”：默认情况下，你滚动终端窗口，滚的是终端模拟器的缓冲区，跟 tmux 无关；要在 tmux 里查看历史，得进入复制模式。默认键位是 vim 风格还是 emacs 风格，取决于你的习惯。我是 vim 用户，所以：

```bash
# 复制模式使用 vim 键位
setw -g mode-keys vi
```

进入复制模式：`Ctrl+b [`。然后：`h`、`j`、`k`、`l` 或方向键移动，`空格` 开始选择，`回车` 复制，`Ctrl+b ]` 粘贴。选中文本的语义和我们平时理解的不太一样——tmux 有自己的剪贴板栈，复制的内容默认只存在于 tmux 内部，要通过 `set -g set-clipboard on` 才能和系统剪贴板打通。顺带一提，tmux 3.6 引入了原生滚动条【18】，滚动体验又向前走了一步，但复制模式的底层逻辑没有变：历史在 server 里，不在你的终端模拟器里。

把这四个案例合起来，加上前面聊过的前缀键和分屏绑定，就是一份我推荐给所有新手的“起步配置”：

```bash
# ~/.tmux.conf —— 起步配置
# 前缀键保持默认 Ctrl+b；觉得别扭再按 6.2 的方法改

# 鼠标支持
set -g mouse on

# 历史记录加大
set -g history-limit 5000

# 复制模式用 vim 键位
setw -g mode-keys vi

# 状态栏样式
set -g status-bg colour235
set -g status-fg colour250
set -g status-left '#[fg=green,bold] #S #[default]'
set -g status-right '#[fg=colour245]#(whoami)@#H #[fg=colour240]%H:%M'
set -g window-status-format '#I:#W'
set -g window-status-current-format '#[fg=green,bold]#I:#W'

# 面板边框：高亮当前面板
set -g pane-border-style fg=colour235
set -g pane-active-border-style fg=colour208

# Ctrl+b r 热重载配置
bind r source-file ~/.tmux.conf \; display "配置已重载"

# 把分屏放到更顺手的位置：| 垂直分屏，- 水平分屏
bind | split-window -h
bind - split-window -v
```

这份配置没有花哨的主题，没有插件，但它把最常用的几个体验优化都做齐了：鼠标、历史、状态栏信息、当前面板醒目。用一个月觉得顺手了，再去探索插件和主题不迟——tmux 的配置是长出来的，不是抄出来的。

> 💡 **独思时刻：** 配置文件的第一个读者，永远不是 tmux，而是三个月后的你自己。我见过太多人的 `~/.tmux.conf` 是一坨没有注释的咒语堆：这一行是从博客抄的，那一行是当年试出来的，没人记得当初为什么这么写。等三个月后想改点什么，面对这一堆代码只能先花半小时考古，最后往往选择放弃——“算了，能用就行”。所以我给自己立了条规矩：每写一行配置，就写一行注释，解释“为什么”而不是“是什么”。看起来啰嗦，但三个月后、三年后，那个深夜想改状态栏颜色的我会感谢现在的我。配置是写给未来自己的信，字迹要工整；注释是信里的旁白，把当时的判断留下来。顺便说，这份“写给未来自己”的习惯后来帮了我大忙：每次换电脑，我只需要把这份带注释的配置文件拷过去，几分钟就能重建一个一模一样的开发环境——它不只是一份配置，更是我这些年踩坑经验的索引。

## 6.9 保存与恢复——tmux resurrect 与 continuum

tmux 有一个天然的局限：如果机器关机或重启，所有会话都会消失——因为 server 进程本身也死了。session 再持久，也扛不住断电。

但社区早已给出了成熟方案：`tmux-resurrect` 和 `tmux-continuum` 这对黄金组合【20】。先讲清楚它们各自解决什么问题。`resurrect` 负责“手动拍快照”：把当前所有会话、窗口、面板的布局，以及每个面板里正在运行的命令，保存到磁盘。`continuum` 负责“自动化和开机自愈”：每隔一段时间自动保存一次快照，并在 tmux 启动时自动恢复最近的快照。

为什么要这两件套？想象你是某个项目的维护者：每天早上上班，要手动打开六个窗口、切分三个面板、进入四个不同的项目目录，这套流程每天占用你三四分钟。三分钟不多，但一年就是十几个小时。有了 `resurrect`，你只需要在某天收工时拍一次快照；第二天开机，`tmux resurrect restore` 一下，vim 打开着同一个文件、`tail -f` 停在同一个位置、连光标所在的行号都和关机前一模一样。

安装这对插件，需要先装 TPM（Tmux Plugin Manager），tmux 生态里事实上的插件管理器：

```bash
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
```

然后在 `~/.tmux.conf` 里声明插件：

```bash
# 声明 TPM 管理的插件列表
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# continuum 自动保存间隔：默认 15 分钟
set -g @continuum-save-interval '15'

# tmux 启动时自动恢复上次的快照
set -g @continuum-restore 'on'

# 初始化 TPM（必须放在配置文件的最后一行）
run '~/.tmux/plugins/tpm/tpm'
```

保存配置后重启 tmux，按 `Ctrl+b I`（大写 I）安装插件。之后：

| 操作 | 按键 / 命令 |
|------|------------|
| 手动保存快照 | `Ctrl+b Ctrl-s` |
| 手动恢复快照 | `Ctrl+b Ctrl-r` |
| 查看 TPM 是否装好 | `Ctrl+b t`（显示 TPM 菜单） |

这里有个诚实的提醒：`resurrect` 不是时光机。它能恢复“面板里跑着 vim 打开某个文件”这个事实，但 vim 里未保存的修改是恢复不了的——那是 vim 的 undo 文件和交换文件的职责。同样，面板里正在跑的下载任务恢复后并不会自动续传，它恢复的是“当时在跑什么命令”，而不是“命令执行到哪一步”。但即便如此，这套组合的价值依然巨大：它把“重启电脑”从“灾难事件”降级成了“日常小事”。2026 年的今天，这对插件在 GitHub 上累计收获了超过两万颗星【20】——这不是营销堆出来的数字，是无数个“幸好早上还能接着干”的早晨堆出来的。

## 6.10 进阶技巧——同步输入、脚本化与嵌套

当你把前面的内容都跑顺了，可以开始玩一些进阶花样。这一节讲三个：同步输入、脚本化、嵌套 tmux。

**同步输入（synchronize-panes）。** 这个功能允许你在一排面板里同时输入同一段内容。开启方式是在某个窗口里执行：

```bash
setw synchronize-panes on
```

开启后，你在任意一个面板敲下的按键会同时出现在这个窗口的所有面板里。典型场景：你同时 SSH 登录了五六台服务器，想给它们统一改个配置、统一重启个服务，一个一个敲简直要命——开启同步输入，敲一遍，全部生效。用完了记得关掉：

```bash
setw synchronize-panes off
```

顺便讲个历史：这个功能最初来自一个用户的请求，Marriott 觉得合理，就决定加入【13】——tmux 的很多好功能都长这样，来自真实用户而非维护者的凭空想象。它的快捷键没有默认绑定，很多人会自己绑一个切换键，比如 `bind e setw synchronize-panes on`、`bind E setw synchronize-panes off`。同步输入是双刃剑，开着它在一个面板里跑交互式程序要格外小心——你敲的每个键都会被广播。

**脚本化 tmux。** 这是把 tmux 从“交互工具”升级成“自动化工具”的关键一步。还记得第 2 章讲过的吗？tmux 的命令行语法和配置语法是同一套【8】——这意味着，凡是你能在会话里敲的命令，都能写进脚本里。下面这个脚本，一条命令启动一个完整的开发环境：

```bash
#!/usr/bin/env bash
# dev.sh —— 一键启动开发环境

# 如果会话已存在，直接附着，不重复创建
if tmux has-session -t dev 2>/dev/null; then
  tmux attach -t dev
  exit 0
fi

# 创建名为 dev 的会话，第一个窗口命名 code，进入项目目录
tmux new-session -d -s dev -n code -c ~/projects/myapp

# 在 code 窗口里垂直分屏，右侧跑 dev server
tmux split-window -h -t dev:code -c ~/projects/myapp
tmux send-keys -t dev:code.1 'npm run dev' Enter

# 新建第二个窗口 test，跑单元测试
tmux new-window -t dev -n test -c ~/projects/myapp
tmux send-keys -t dev:test 'npm test -- --watch' Enter

# 附着到会话
tmux attach -t dev
```

逐行拆解：`new-session -d` 的 `-d` 表示“后台创建，不附着”；`-c` 指定初始目录；`send-keys` 模拟按键，最后那个 `Enter` 就是按下回车；`dev:code.1` 是 tmux 的目标寻址语法——会话名:窗口名.面板编号。这套寻址语法值得学一下：`-t` 参数后面跟的“目标”，是 tmux 命令体系里最优雅的设计之一，任何操作都能精确定位到某个会话的某个窗口的某个面板。有了这个脚本，每天早上 `./dev.sh`，环境自动就位——这就是第 4 章说过的“工作流自动化”的入门形态。

**嵌套 tmux。** 场景是这样的：你在本地开了 tmux，SSH 到远程服务器，远程又开了一个 tmux。此时你按 `Ctrl+b`，到底是给哪个 tmux 发指令？答案在 tmux 的设计里早就安排好了：**内层的 tmux 看不到这个前缀键，它被外层拦截了。** 要向内层发送前缀键，需要连按两次——`Ctrl+b` 再按一次 `Ctrl+b`，第二个 `Ctrl+b` 就会被“转发”给内层 tmux。也就是说，嵌套一层时，内层的操作都要变成 `Ctrl+b Ctrl+b x` 这样的双重前缀。习惯之后还行，但确实费神。

更省心的方案是给内层换个前缀。比如远程服务器的 `~/.tmux.conf` 里把前缀改成 `Ctrl+a`，那么：外层 `Ctrl+b d` 脱离本地会话，内层 `Ctrl+a d` 脱离远程会话，互不干扰。这也是为什么很多常年 SSH 的人坚持用 `Ctrl+a`——不是为了装，是嵌套场景真的需要区分。记住原则：**嵌套几层 tmux，就要给每层不同的前缀键，或者记住“按两次前缀键 = 向内层传一次”。** 另外，如果你的终端模拟器（比如 iTerm2）本身也有“会话”概念，那又是第三层嵌套，规律同理。

## 6.11 常见坑与解决——Vim 冲突、SSH 嵌套与 256 色

新手用 tmux，几乎必然踩到下面几个坑。我把它们和解决方案一起写在这里，你踩到的时候回来查。

**坑一：Vim 里按 Esc 卡顿。** 这是 tmux 用户最常见的困惑之一：在 tmux 里用 Vim，从插入模式按 Esc 切回普通模式，总要顿一下才反应。原因在 tmux 的一个默认值：`escape-time`（转义序列等待时间）。终端里的方向键、Esc 这类按键，是通过转义序列传给程序的，tmux 需要等待一段时间来区分“单独的 Esc”和“以 Esc 开头的组合键序列”，默认等 500 毫秒。Vim 每次按 Esc，都要陪 tmux 干等这半秒。解法是把等待时间调小：

```bash
# 把 escape-time 从默认 500 毫秒降到 10 毫秒
set -sg escape-time 10
```

调到 10 毫秒后，肉眼完全感觉不到延迟，又不至于误判组合键。顺带说一句，Neovim 用户基本感受不到这个问题，但调了也没坏处。

**坑二：SSH 内嵌套的键位混乱。** 这个坑在 6.10 已经讲过，这里补充一个具体的翻车场景：你本地 tmux 里 SSH 到服务器，服务器上又开了 tmux，然后你在里面想滚动历史——按 `Ctrl+b [`，结果进入的是外层 tmux 的复制模式，而不是内层的。你以为是“坏了”，其实是层级搞混了。解决思路就两条：给内层换前缀键，或者习惯“双击前缀键”的语法。还有一个隐藏坑：从服务器 exit 回本地时，如果内层 tmux 还开着，你的 SSH 会话不会结束——因为内层 tmux 还在占用终端。正确退出顺序是：先 `Ctrl+a d`（内层）脱离，再 `exit` 退出 SSH，最后 `Ctrl+b d`（外层）脱离本地。顺序反了，你就会被“卡”在某个层级里出不来。

**坑三：颜色发灰、配色不对。** 你在 tmux 里跑 Vim 或各种 TUI 程序，发现颜色比裸终端里灰了一截，多半是终端类型（TERM）的问题。tmux 启动时会根据终端模拟器声明一个终端类型，默认可能是 `screen` 或 `screen-256color`，而你的程序只认识 `xterm-256color` 这种“标准名”。检查方法：

```bash
echo $TERM
```

如果输出的不是 `tmux-256color`（或 `screen-256color`），程序就不知道你的终端支持 256 色，只好退而求其次用 16 色甚至 8 色，颜色自然发灰。解法是在配置里明确告诉 tmux 你的终端能力：

```bash
# 让 tmux 以 256 色终端身份与程序交互
set -g default-terminal "tmux-256color"
```

改完重新加载配置、重启 tmux 生效。如果你的终端模拟器本身不支持 256 色，这一步做了也没用——所以排查顺序是：先确认终端模拟器支持，再确认 tmux 的声明，最后才是程序本身的配色。顺便一提，tmux 2.0 曾做过一次不兼容更新，把 terminal-overrides 一类的选项从窗口级移到了 server 级【14】——这类细节导致很多老配置在新版本上报错，升级时留意一下即可。

**坑四：按前缀键“没反应”。** 很多人刚上手时，按了 `Ctrl+b` 再按别的键，什么都没发生。绝大多数情况是：你按 `Ctrl+b` 和下一个键的间隔太久了。前缀键的语义是“紧接着的下一个键”，中间超过一秒，tmux 就当你按的是普通 `Ctrl+b`（在 shell 里它通常无害）。所以动作要连贯：`Ctrl+b` 松开、`d` 按下，一气呵成。还有一个常见误会：`Ctrl+b` 要一起按住再松开，而不是先按 `Ctrl` 再按 `b`——其实两者效果一样，关键是“先释放前缀，再按指令键”。

**坑五：locale 相关报错。** 有些系统上 tmux 启动会报 “invalid LC_ALL” 之类的警告，这是因为系统的 locale 没生成完整。解法是装好语言环境，比如 Debian 系执行 `sudo locale-gen en_US.UTF-8` 后重新登录。这个问题和 tmux 本身无关，但因为它恰好在你第一次启动 tmux 时冒出来，很容易被误认为是 tmux 的 bug。

**坑六：杀会话的代价。** `tmux kill-server` 会把所有会话一锅端，而且没有确认提示。如果你在远程服务器上敲错了这条命令，一天的工作瞬间蒸发。我的建议：日常只使用 `Ctrl+b d` 脱离，杀会话前先 `tmux ls` 看清楚名字，用 `tmux kill-session -t 名字` 精准打击。如果配合了 6.9 节的 resurrect，杀错也有后悔药——这是我把“保存与恢复”放在“进阶技巧”之前的原因：先学会兜底，再放手操作。

## 6.12 从零到一之后

好了，走到这里，你已经从零到一完整地跑通了 tmux：会安装、会启动命名会话，掌握了 17 个核心快捷键，能拆面板、开窗口、管会话，写了自己的配置文件，装了保存恢复的插件，还知道怎么避开新手最常见的坑。

回到本章最开头那句话：深夜 SSH 断线、工作进度归零的绝望。有了 tmux 之后，那种绝望会变成“哦，SSH 断了，我重新连一下然后 `tmux attach` 就行”。你的终端会话从此比你活得更久——在服务器重启之前，它们不会消失。

你会不会去背全部快捷键？不会。你会不会每天花半小时调配置文件？不会。但你会在某个时刻突然意识到：你已经想不起来“没有 tmux 的日子是怎么工作的”了。到那时候，Marriott 在 2007 年那个深夜敲下的第一行代码，就真正改变了你的一天。

下一章，我们将离开快捷键和配置，回到 tmux 的设计本身——看看这个 2007 年诞生的工具，在 AI 编码代理遍地走的 2026 年，为什么反而站在了舞台中央。

---

# 第7章 为什么tmux至今仍是利器

2024 年秋天，我在上海一家小咖啡馆里，用笔记本电脑连着一台位于弗吉尼亚的 AWS EC2 实例做线上分享。演示进行到一半，咖啡馆的路由器重启了——WiFi 断了两分钟。等网络恢复，我重新 SSH 连上去，终端里赫然显示着之前的 tmux 会话完好无损，连演示时打开的那几个 vim buffer 都还停留在原处。台下观众不知道发生了什么，我只说了句“稍等，我 attach 一下”，然后继续讲。事后朋友问我紧张吗，我说：“只要 tmux 没断，我的工作就没丢。那台服务器在弗吉尼亚，咖啡馆的 WiFi 根本不重要。”

这句话折射出的，其实是 tmux 依然强大的全部秘密：你的工作进度不应该绑定在任何一段不稳定的网络连接上，也不应该绑定在你本地电脑是否关机上。这个道理在 2007 年成立，在 2026 年依然成立，而且越来越成立。

这一章我们不谈快捷键，不谈配置文件，只回答一个问题：一个诞生于 2007 年的终端工具，凭什么在容器化、云原生、AI 编码大行其道的今天，依然是开发者的利器？答案不在它的代码里，而在它背后的一套哲学里。这套哲学由 Nicholas Marriott 在十九年前定下基调，由社区十九年如一日地守护，如今正被一个全新的群体——AI 编码代理——重新发现。理解这套哲学，比记住一百个快捷键都重要。

## 7.1 长寿的秘密：解决的问题四十年未变

先看一个反直觉的规律：**技术工具的生命力，不取决于它是否走在技术前沿，而取决于它解决的问题是否持续存在。**

这句话是理解 tmux 一切故事的钥匙。把时间轴拉长来看：1987 年，Oliver Laumann 在柏林工业大学发布了 GNU Screen 的前身，解决的是“在一个终端里管理多个会话”的问题；2007 年，Marriott 因为对 Screen 的种种不满重写了一个替代品，解决的是同一个问题；2026 年，我们坐在 AI 编码代理的包围圈里，打开 tmux 依然是为了解决同一个问题。会话持久化、终端组织——这两个词，四十年没有变过。

为什么这个问题如此顽固？因为人永远需要通过文本界面与远程计算机交互，而这个交互过程永远受制于三个不变的条件。

第一，网络天生不稳定。无论是咖啡馆的路由器、高铁隧道里的手机信号、还是云厂商偶尔的故障，连接随时可能断。只要连接会断，“会话必须比连接活得更久”就是一个刚需。

第二，多任务有认知负担。一个现代后端开发者的终端里同时挂着什么？前端构建、后端 API、数据库迁移、日志监控、Kubernetes 集群状态。人的注意力是有限的，任何能降低“切换成本”的组织方式都有价值。

第三，工作进度需要被持久保存。跑了两天的数据任务、调试到一半的状态、精心布局的窗口结构——这些东西一旦随 SSH 断开而消失，损失的是真实的时间与金钱。

只要这三个条件不变，tmux 这类工具就不会消失。你去看那些死掉的技术：它们大多死于“要解决的问题消失了”——某个框架解决的问题被平台内置了，某个语言解决的问题被更好的抽象取代了。而 tmux 解决的问题，四十年里不但没有消失，反而因为环境的复杂度上升而变得更加尖锐。云原生让开发者同时面对本地、远程、容器、Kubernetes 多重环境，AI 编码让终端里同时跑着人类和机器的多个进程——混乱度在增加，而 tmux 是那个把混乱组织成秩序的工具。

2007 年的三个设计决策——会话持久化、进程隔离、实时可见性——放到今天依然精准得可怕。这就像一把刀：人类用刀砍东西的姿势几千年没变过，所以刀永远是刀。工具的形态可以迭代，但只要底层需求不变，好工具就不会被淘汰。

还有一件常被忽视的事：tmux 的这三个决策不是孤立的，而是互为支撑的。进程隔离让每个窗口、每个面板都拥有独立的终端生命周期，一个进程崩溃不至于拖垮整个工作空间；会话持久化把这一切固定在服务器端，让断线只影响你的观察窗口，不影响正在发生的工作；实时可见性则让你在任何时刻都能一眼看清整个工作空间的全局状态——我在哪个会话、哪个窗口、哪个面板，哪个任务在跑，哪个任务已经结束。组织能力、持久能力、感知能力三者合一，才构成了一个完整的“工作空间”。今天各种号称“下一代终端”的产品，拆开来看，其实大多都在重新发明这三件事。它们的广告越热闹，越说明 tmux 在 2007 年就抓住了问题的本质——而且抓得很准。

## 7.2 Marriott 的治理哲学：不搞膨胀

如果说“解决对的问题”是 tmux 长寿的外部条件，那么“克制地解决问题”就是它的内部条件。这两者缺一不可。很多项目解决对了问题，却死于自己给自己喂的膨胀。

Marriott 在项目早期就定下了一条原则，至今仍以某种形式留在项目文档里：“bloat due to features like serial line I/O is not going to happen”。这句话的背景是：有用户提议让 tmux 支持直接操作串行线路（serial line I/O），Marriott 干脆利落地拒绝了。翻译成大白话就是：**我知道有人想要某些功能，但如果这些功能会让代码变臃肿、让维护成本变高、让新用户更难上手，那就不做。**

请注意，这不是傲慢。恰恰相反，这是对整个软件行业最常见死法的最清醒认知。大多数项目走向衰落的路径惊人地一致：用户要什么我就加什么。每个功能单独看都很合理——“有人需要嘛”——但功能与功能之间的交互、配置项与配置项之间的排列组合、文档与代码的同步负担，是呈指数级增长的。十年下来，项目就变成一头谁都不想要的庞然大物，修一个 bug 要动五个模块，新贡献者望而却步，最后连维护者自己都跑不动了。

GNU Screen 走过的正是这条路。约两万行 C 代码，monolithic 的单体架构，所有功能揉在同一个进程里，像一栋被改建了二十年的老房子：每个房间都有人住过，但承重墙已经分不清在哪了。2004 年 1 月它的开发就陷入停滞，2008 年 8 月发布 4.0.3 之后再无下文，连垂直分屏这种基础功能都从未进入生产版。它的问题从来不是“没人想要新功能”，而是“没人敢再动这栋老房子”。

膨胀是慢性病，每一个单独的症状看起来都合理。Marriott 的克制之所以珍贵，是因为他在症状出现之前就立下了拒收标准。“serial line I/O”只是一个例子，真正重要的是那条底线本身：核心只做它该做的事。

拒绝，本身也是一种产品能力。每一次对“锦上添花”说“不”，其实都是在替用户回答一个问题：你到底想要一个什么样的工具？Marriott 的答案始终如一——一个可靠的、可预期的、不会背叛你的工具。这个答案，十九年来没有变过。用户今天觉得某个功能“必须有”，三年后可能发现它只是噪音；而维护者的职责，恰恰是在用户冲动的时候替他们守住底线。敢于说“不”的维护者，比只会说“好”的维护者，更值得用户信赖。

这种克制带来的直接后果是：**无论你用哪个版本，tmux 的行为始终是可预期的。** 我印象很深的是 2022 年底，我把 tmux 从 3.2 升级到 3.3，配置文件一行没改，所有快捷键和布局完全照旧。而同时期我维护的某个 Node.js 项目，从 16 升到 18 就改了十几处 API 调用。软件的可靠性，说到底就是“你依赖它的时候，它不会突然变脸”。

到了 2026 年，tmux 的最新稳定版是 3.6b（2026 年 5 月 20 日发布）。十九年了，它还在稳定地发版——3.6（2025 年 11 月）带来的是原生滚动条、Mode 2031 主题、新变量这类体验补全，而不是什么惊天动地的架构革命。一个成熟的工具，最好的状态就是“没什么新闻”：新闻越少，说明它越可靠。tmux 在这方面的信誉，是近二十年一天一天累计出来的。

## 7.3 用户多样性：工具为人服务而非相反

Marriott 在邮件列表里说过一句话：“People use tools in surprisingly different ways.”——用户使用工具的方式千奇百怪。这句话的潜台词值得拆开看。

第一层意思是：维护者不认为自己比用户更懂用户的需求。一个写代码的人，永远猜不到自己的代码会被怎样使用。你设计了一个用于管理服务器会话的工具，结果有人用它来做直播推流的控制台，有人用它搭了一个运维监控墙，有人用它做线上教学的分屏白板，有人用它管理 CI 的日志输出，还有人用它做远程结对编程——两个人通过 SSH 连到同一台机器，attach 到同一个会话，共享同一个屏幕。

第二层意思更重要：**维护者有责任判断哪些需求应该纳入核心，哪些应该留给插件生态。** 用户多样性是最好的功能来源，但它必须经过一道“过滤器”。synchronize-panes 就是最好的例子：它最初只是一个用户的请求，Marriott 评估后决定加入核心。后来的故事我们都看到了，这个“把输入同时广播到所有面板”的功能，成了多机同步操作、批量运维、甚至 AI 多代理编排的基础设施。而另一些需求——比如花哨的状态栏主题、复杂的布局预设——则被留在插件生态里，各取所需。

这种边界感是 tmux 生态健康运转的关键。TPM（Tmux Plugin Manager）的出现，本质上就是把这层边界制度化：核心只做最基础的会话、窗口、面板管理和通信协议，其他一切都交给插件社区去发挥。维护者保持谦逊的好奇，同时保持清醒的判断——听用户的，但不是用户说什么就做什么。工具是为人服务的，而不是反过来让人去迁就工具。一个把你所有的使用习惯都当成“必须支持”的工具，最终会变成谁都用不动的工具；一个尊重你多样性、同时守住自己边界的工具，才能活到被 AI 时代重新捡起来的那一天。

## 7.4 极简核心 + 可编程边界

把 7.2 和 7.3 合起来，就得到了 tmux 最核心的架构哲学：**极简核心 + 可编程边界。** 内核保持克制，扩展交给配置、脚本和插件。

tmux 的核心功能列表，几乎可以用一页纸写完：会话、窗口、面板的管理，前缀键快捷键体系，状态栏，复制模式。就这些。但它的边界是无限可编程的，靠的是两条设计原则。

第一条原则来自 Marriott 的原始设计决策：**命令行能敲的，配置文件里就能写，语法完全一致。** 这不是一个“顺便做到”的便利，而是一个深思熟虑的架构选择——它让 tmux 同时拥有了三层使用方式：交互式快捷键、命令行命令、配置文件。而且三者是同一套语法，学一次全部掌握。更关键的是，这意味着 tmux 的每个功能都可以被脚本调用，可以被 cron 调度，可以被 CI 触发。一个交互工具，因此变成了一个**可被程序化控制的“工作空间托管服务”**。

第二条原则是 hooks 系统。tmux 3.0（2019 年）引入的事件驱动机制，让 tmux 可以在特定事件发生时自动执行命令：窗口创建了、面板关闭了、会话结束了，都可以挂上钩子。这让 tmux 从“你操作它”进化成“它响应你”。

这两条原则叠加起来，想象力是惊人的。举一个我真实的用法：写一个脚本，每天凌晨自动创建一个 tmux 会话，启动三个面板分别跑数据备份、日志归档和健康检查，然后通过 cron 调度：

```bash
# 每天凌晨 2 点，创建 tmux 会话并启动三个后台任务
0 2 * * * tmux new-session -d -s nightly -n jobs \
  && tmux send-keys -t nightly:jobs.0 "backup.sh" Enter \
  && tmux split-window -v -t nightly:jobs \
  && tmux send-keys -t nightly:jobs.1 "rotate-logs.sh" Enter \
  && tmux split-window -v -t nightly:jobs \
  && tmux send-keys -t nightly:jobs.2 "healthcheck.sh" Enter
```

第二天早上我到公司，直接 attach 进那个会话，三个任务的结果都在那里等着我。整个过程中 tmux 不需要任何交互式参与，它只是一个被脚本指挥的工作空间。这种“工作流自动化”的想象空间，远超过大多数人日常使用 tmux 的范畴——而这恰恰是极简核心带来的红利：因为核心足够小、接口足够干净，所以边界可以无限延展。插件生态（TPM 加上 tmux resurrect、continuum 等，GitHub 星标累计已超两万）和脚本自动化，共同构成了 tmux 的“第二层生命”。

再往组织层面看，极简核心加可编程边界还带来一个红利：配置即代码。一份写进版本库的 tmux 配置，可以让整个团队的开发环境一键对齐；一个几十行的启动脚本，可以把复杂的开发环境预置成标准布局，新同事入职第一天，打开终端就是和老同事一模一样的工作空间。当工具的核心足够稳定，团队就敢把自动化建在它上面——因为你确信它不会在下次升级时把地基抽掉。这种“可以被放心依赖”的特质，正是基础设施级工具的标志，也是为什么 tmux 值得被写进团队的入职文档。它也许永远不会成为热门话题，但它会一直稳定地工作，让团队忘记它的存在。

## 7.5 对比分析：为什么其他方案都替代不了 tmux

如果有人问“tmux 能做的，别人做不了吗”，这个问题值得认真回答。竞争者无非三类：前辈 Screen、终端模拟器的内建功能、容器化方案。逐一来看。

**第一类，GNU Screen。** 它是最接近的替代品，也是 tmux 的直系前辈。但前文已经反复论证过它的结构性问题：约两万行 C 代码的 monolithic 架构、2004 年就停滞的开发、从未进入生产版的垂直分屏。更致命的是它的设计取向——Screen 想什么都做，结果什么都没做到最好；tmux 只做终端复用，把这一件事做到了极致。当年 Marriott 正是因为对 Screen 的“怪室友”式体验忍无可忍才动手重写，今天这个对比依然成立：Screen 是“能用的老房子”，tmux 是“可以继续盖楼的地基”。

**第二类，终端模拟器的内建分屏与会话恢复。** iTerm2 等现代终端模拟器确实集成了分屏、标签页、甚至“恢复上次会话”的功能，看起来和 tmux 很像。但这里有一个本质区别：**tmux 的会话运行在服务器端，终端模拟器的分屏只是客户端的视觉装饰。** 你的 iTerm2 窗口分得再漂亮，SSH 一断，那些“分屏”就跟着本地窗口一起死了；服务器上该跑的进程该停还是停。而 tmux 的会话在服务器上常驻，你本地关掉终端、拔掉网线、甚至换一台电脑，会话都纹丝不动，重新连上再 attach 就行。一个是墙上的装饰画，一个是墙本身。终端模拟器的功能再丰富，也替代不了“会话活在网络之外”这件事。

**第三类，容器化。** docker run 一句就能拉起一个完整环境，Kubernetes 帮你调度、自愈、扩容——但冷静想想，这些解决的是“环境怎么部署”，而不是“人怎么在这个环境里持续工作”。容器是进程级的隔离与编排，tmux 是人机交互层的组织与持久化，两者解决的根本不是同一个问题。事实上，容器里照样要跑 tmux：当你在容器里执行长任务、需要断线不中断、需要查看多个服务的日志时，容器本身不提供任何会话管理能力。云原生不是让 tmux 变得不再需要，而是让终端里的“混乱度”增加了，tmux 反而成了管理混乱的锚点。一个典型的后端场景：本地终端里挂着四个 tmux 窗口，第一个跑前端构建，第二个跑后端 API，第三个连着公司跳板机进入内网 Kubernetes 集群查看 Pod 状态，第四个开着 htop 观察服务器负载。容器管“运行”，tmux 管“组织”——两者是互补，不是替代。

还有一个值得记录的“官方站队”：Red Hat 在 RHEL 7.6（2018 年）中弃用 Screen，RHEL 8.0（2019 年）彻底移除，并明确推荐用户使用 tmux。一个企业级发行版愿意做这种“不兼容”的决定，背后是代码质量和治理哲学在说话。当官方都替你选好了答案，替代品之争其实已经有了结论。

其实还有第四类“替代方案”，也是被讨论得最多的：干脆不用复用器，多开几个终端窗口不就行了？这个方案在本地也许可行，可一旦涉及远程服务器、容器、跳板机，就会立刻失效——你的本地窗口无法在 SSH 断线后存活，你无法在换了一台电脑之后找回之前的布局，你更没法用脚本去控制十几个零散的终端。tmux 的价值恰恰在于把“组织”这件事从你的本地环境里抽离出来，放到服务器端：无论你从哪台电脑连上来，看到的都是同一个工作空间，用的是同一套快捷键，读的是同一份配置。这种跨机器、跨环境的一致性，是任何本地终端方案都给不了的。你可以在本地开二十个窗口，但只有 tmux 能让你在任何地方、任何设备上，看到同一个“工作现场”。

所以答案很清楚：Screen 替代不了 tmux 是因为它自己就走在衰亡的路上；终端模拟器替代不了是因为它活在客户端，而 tmux 活在服务器端；容器化替代不了是因为它根本不解决会话问题。tmux 依然是最优雅的答案。

## 7.6 AI 编码时代的回归：终端反而更拥挤了

过去几年，技术圈最热门的话题无疑是 AI 编码：Claude Code、Codex 这类 agent 能自己读代码、写代码、跑测试、修 bug。一个很自然的疑问随之而来：AI 都要替人类写代码了，tmux 这种“老古董”还有用吗？

事实恰恰相反。**AI 编码时代不但没有淘汰 tmux，反而让 tmux 分屏成为多 agent 编排界面，会话持久化让 agent 任务在断开后继续运行。** 原因不复杂：AI 编码 agent 是 CLI 生物——它们没有图形界面，它们的输入输出都是终端文本。Claude Code 在终端里跑，Codex 在终端里跑，它们的日志、思考、操作记录全部是终端里的字节流。当你的终端里同时跑着两三个 agent 时，你拿什么来编排它们？答案就是 tmux 的分屏。

假设你正在做一个微服务重构，需要把旧代码逐段翻译成新的架构。你可以把整个屏幕切成四个面板：左上跑一个监听文件变化的 lint 进程，右上让一个 AI agent 持续阅读当前文件并给出建议，左下是 git diff 预览区，右下是你自己手写的 vim 编辑区。四个区域同时活跃，你不需要频繁切换窗口，目光一瞟就能看到所有信息——这种“信息齐备感”在复杂的重构任务中带来的认知负担降低是巨大的。2025 年年中，我在为一个开源项目做大型重构时就采用了这种方式，一个下午完成的工作量抵得上平时两天。

为什么分屏比来回切换窗口好？因为人类的工作记忆容量有限，每次切换窗口都需要重新“加载上下文”——刚才那个面板里命令执行到哪一步了？报错信息是什么？agent 的输出到哪里了？这些信息在切换过程中会被挤出大脑缓存。而 tmux 把所有相关信息同时摆在眼前，你不需要切换上下文，只需要转动眼球。这种生理层面的优化，任何 IDE 的“窗口切换快捷键”都替代不了。

更深一层看，AI 时代让终端变得前所未有地拥挤：人类在敲命令，agent 在写代码，构建进程在输出日志，监控脚本在滚动指标。当屏幕上有四五个“说话者”时，组织能力就是生产力。而 tmux 恰好是那个不抢戏的组织者——它不提供 AI 功能，不假装智能，它只做一件事：把混乱的终端整理成你可以一眼读懂的棋盘。AI 没有让终端变冷清，反而让终端更热闹了；热闹的地方，就需要秩序。

有人可能会说：AI 编码不是有专门的 IDE 集成吗，为什么非要在终端里？因为 agent 的用武之地远不止你的笔记本——生产环境、CI 流水线、无头服务器、远程开发机，这些地方没有 IDE，只有终端。Claude Code 和 Codex 这类 agent 天生就是命令行程序，它们的标准输入输出就是终端，它们的启动、暂停、恢复、观察，全都依赖一个稳定的会话载体。换句话说，agent 是“终端原住民”，而 tmux 是给终端原住民准备的公寓楼。你当然可以在本地用图形界面观察 agent，但任务一旦要跑在服务器上、要跨越断断续续的网络、要持续几个小时，tmux 就是唯一现实的答案。这也是为什么我认识的一些 AI 重度用户，恰恰是在 AI 时代才开始认真学 tmux 的——AI 逼着他们把终端这条基础设施补上了。

## 7.7 实战案例：tmux 与多 agent 工作流的搭建

理论说完了，来看一个完整的多 agent 工作流是怎么搭起来的。我最近做一次跨模块重构时的做法如下。

第一步，创建一个专门的会话，避免和日常工作流混在一起：

```bash
# 创建名为 agent-lab 的会话，主窗口命名为 main
tmux new-session -d -s agent-lab -n main
```

第二步，切分面板。左半边给主 agent，右半边上下对半分给第二个 agent 和日志区，再在左半边下方切出 git 区：

```bash
# 左右对半
tmux split-window -h -t agent-lab:main
# 右侧再上下对半：上方跑第二个 agent
tmux split-window -v -t agent-lab:main.1
# 左侧下方切出 git 预览区
tmux split-window -v -t agent-lab:main.0
```

第三步，把四个面板各就各位：

```bash
# 主 agent 负责重构主体
tmux send-keys -t agent-lab:main.0 "claude --dangerously-skip-permissions" Enter
# 第二 agent 负责补测试
tmux send-keys -t agent-lab:main.1 "codex exec --full-auto" Enter
# 日志区持续观察构建输出
tmux send-keys -t agent-lab:main.2 "tail -f build.log" Enter
# git 区盯着变更规模
tmux send-keys -t agent-lab:main.3 "git diff --stat" Enter
```

最后 attach 进去：

```bash
tmux attach -t agent-lab
```

四个面板各司其职，互不干扰。我还能利用 tmux 的同步输入功能（synchronize-panes）把一条命令广播到所有面板，比如同时给两个 agent 发送“暂停并汇报当前进展”的指令。如果想更进一步，社区还有 tmux-orchestrator 这类工具，把主面板的输入以编排的方式转发到其他面板，实现更精细的多 agent 协调；Tmux-IDE 这类配置方案则把 tmux 变成 IDE 式的布局系统，为不同任务预设不同的面板结构。这些都不是 tmux 核心的功能——它们全都生长在 tmux 的可编程边界上，这恰恰印证了极简核心加可编程边界的威力。

这套流程用熟了以后，你会发现自己对“并行”的理解会发生改变。以前并行是开一堆窗口自己来回切，现在是给每个角色分配一个面板，让它们各干各的，你只在关键节点介入。你不再是一个执行者，而是一个指挥者——这恰恰是 AI 时代人类最该站的位置。tmux 没有替你写任何一行代码，但它让你第一次有了同时指挥几个智能体的从容。

有人可能会问：为什么不让 agent 之间直接通信，非要人肉编排？因为 agent 之间的“通信协议”还没标准化，而人和 agent 之间最可靠的接口就是终端。tmux 在这里扮演的角色，是一个中立的、稳定的、人类可读的“编排层”——人类看着屏幕判断谁该做什么，通过键盘把指令发给对应的面板。在 agent 学会完美协作之前，这个编排层是必需品。

## 7.8 会话持久化在 AI 时代的价值

如果说分屏解决的是“同时看”，那么会话持久化解决的是“长时间跑”。这两个能力在 AI 时代缺一不可。

AI agent 的任务通常很长：跑一夜的测试、大规模代码迁移、批量代码审查、持续的重构。这些任务有一个共同特征——它们不需要你盯着，但需要环境稳定。而 AI agent 本身就运行在终端里，终端一旦断开，agent 进程收到 SIGHUP，任务就死了。这时候 tmux 的价值就体现出来了：**agent 跑在 tmux 的会话里，会话跑在服务器端，你的网络连接只是它的一个观察窗口。** 笔记本合上了？agent 继续跑。WiFi 断了？agent 继续跑。SSH 超时了？agent 继续跑。你随时重新连上，attach 进去，看到它已经推进到哪一步了。

我举一个最近的例子。上个月，我让一个 agent 在一台远程开发机上做一次跨五十个文件的 API 迁移，预计要跑四五个小时。我把它放进一个 tmux 会话，然后合上笔记本去睡觉。半夜家里路由器重启了一次，我的 SSH 早就掉线了——但第二天早上，我连上服务器，attach 进去，agent 已经完成了迁移，正停在最后一步等我确认。半夜里我醒来过一次，看了眼手机上的终端，attach 进去确认 agent 还在正常推进，然后又安心睡去。这种“随时可以看一眼”的安心感，是任何云端聊天界面都给不了的。如果没有 tmux，把 agent 直接跑在 SSH 里，半夜网络一断，五个小时的进度全部归零，还得从头再来。会话持久化在 AI 时代的价值，说白了就是一句话：让“机器替你干活”这件事，变得和“机器替你存文件”一样可靠。

> 💡 **独思时刻：当环境变糟时，工具是否依然可靠，才是真正的分水岭。** 2023 年初我出差在高铁上写代码，隧道里手机热点信号断断续续，SSH 每隔三五分钟就掉一次。如果没有 tmux，我大概会砸掉笔记本。但 tmux 让我冷静地重新 attach 了二十几次，最后居然在颠簸的车厢里把那次紧急 hotfix 发出去了。这段经历让我意识到：好的工具不是让你的环境变得完美，而是在环境不完美时替你扛住不稳定。所有“完美环境”的假设在真实世界里都不成立——这就是为什么 tmux 这种看起来老派的工具反而越来越有价值。到了 AI 时代，这个逻辑同样成立：agent 的“环境”比人类的更脆弱，它没有手去重新连接，它只有 tmux 替它扛着。

更进一步，会话持久化让“长任务”成为可以委托的资产。你可以在下班前把任务交给 agent，让它跑一整夜，第二天早上 attach 进去验收结果。你甚至可以把它和 7.4 节的脚本自动化结合起来：cron 在凌晨创建会话、启动 agent、跑任务，早晨的你打开终端，看到的是一份已经完成的工作。会话比你的网络连接活得更久——这句话在 AI 时代有了全新的含义：现在活得更久的，不只是你的会话，还有你委托给 agent 的整个任务链。

当然，机器重启是会话持久化的唯一天敌。社区对此早有成熟方案：tmux resurrect 插件把当前所有会话、窗口、面板的布局和每个面板里运行的命令保存到硬盘，重启后一键恢复；continuum 则更进一步，自动定时保存、自动恢复。这两个插件加上 TPM，GitHub 星标累计已超过两万。这意味着在 2026 年，tmux 甚至可以做到“关机不丢工作”——这在 2007 年是不可想象的，而它恰恰是在“极简核心”之上通过插件长出来的能力。

## 7.9 技术人的长期主义：学一个工具十年不过时

讲完技术，说一点更个人化的东西：作为技术人，学一个工具十年不过时，到底是什么体验？

我入行这些年，见过太多“学完就废”的技术：某个框架火了两年，社区转向了，文档删了，招聘要求变了；某个工具号称颠覆，半年后自己先颠覆了自己。技术圈的默认节奏是喜新厌旧，我们被训练成不断追逐下一个版本、下一个范式。在这种环境里，tmux 是个异类——它 2007 年发布，2026 年还在稳定迭代，核心概念一个没变：会话、窗口、面板、前缀键、attach、detach。我 2014 年学会的东西，2026 年依然 100% 有效。

这意味着什么？意味着你对 tmux 的每一次学习，都是能产生复利的投资。快捷键不会作废，配置语法不会推倒重来，肌肉记忆不会过时。这和经济学的复利是一个道理：本金（你投入的学习时间）不变，但收益率（知识的有效期）特别长，最终的总回报远超那些“高收益但短命”的技术。

更重要的是，长期主义带来的是信任。你依赖一个工具二十年，它不会突然变脸——这种信任是稀缺品。我在 7.2 节说过升级 3.2 到 3.3 配置一行没改的经历，这背后是近二十年的克制积累出的信誉。信誉这种东西，建立起来需要十几年，毁掉只需要一次不负责任的破坏性更新。tmux 用十九年的时间证明了它值得信任，这本身就是一种极其稀缺的资产。

所以我一直觉得，技术人应该有几件“十年不用换”的家伙什：编辑器、终端、复用器，还有你自己形成的工作流。这些不是让你显得很酷的东西，而是让你的时间产生复利的东西。AI 时代尤其如此——工具更迭的速度只会更快，而恰恰是那些慢的、稳的、克制的基础设施，成了我们唯一能站住的岸。学一个工具十年不过时，不是一种保守，而是一种清醒。

还有一个值得记录的现象：最近两年，因为 AI 编码工具，一批全新的用户开始接触 tmux。他们中的很多人以前从未用 SSH 管过生产服务器，但为了给 agent 搭一个稳定的工作环境，开始认真学会话、窗口、面板。工具的生命力，很多时候就体现在它能不断吸引新读者——老用户靠它维持工作流，新用户因它解决当下的问题。一个能同时服务这两类人的工具，不会老。而反过来看，这也说明好的基础设施有一个共同点：它永远在给新人留位置，从不把门关上。

## 7.10 独思时刻：好工具应该“消失”

> 💡 **独思时刻：好工具应该“消失”——消失才是最高级的在场**
>
> 说了这么多“为什么”，我想回到一个更本质的问题：我们到底需要工具做什么？
>
> 我的答案是：好工具应该“消失”。不是被卸载的那种消失，而是从你的意识里消失——用时不打扰，出事时兜底。当你不再需要刻意“使用”一个工具，而是自然地通过它完成工作时，这个工具就真正变成了你身体的一部分。
>
> 你回想一下自己最熟练的技能：打字时你不会想“我在用键盘”，开车时你不会想“我在踩油门”。工具的最高境界是成为环境本身，而环境是不被感知的。tmux 就是这样。它不会主动跳出来说“你看我多厉害”，它没有任何炫耀性的设计，它只是在每个 SSH 断开的深夜，默默地让弗吉尼亚那台服务器上的终端会话继续运行，等你重新连上来，然后轻轻敲下 tmux attach——一切就像没断过一样。
>
> 消失才是最高级的在场。一个天天刷存在感的工具，说明它经常出问题需要你关注；一个从不打扰你的工具，说明它把一切都处理好了。我用了 tmux 十年，最深的感受不是“它功能真多”，而是“我想不起来没有它的日子是怎么工作的”。它从我的意识中消失了，只剩下工作流的流畅。那些花了大力气让你“注意到它”的工具——炫酷的启动画面、频繁的更新弹窗、复杂的学习路线——恰恰相反，它们是在提醒你：你还在学习使用我，我们还没磨合好。
>
> 而 tmux 的消失不是设计上的巧合，是克制换来的。因为核心简单，所以行为可预期；因为行为可预期，所以不需要你盯着它；因为不需要盯着它，它就从你的注意力里退场，把舞台留给真正重要的事——你的工作本身。

## 7.11 独思时刻：克制是最高级的设计

> 💡 **独思时刻：克制是最高级的设计——功能少而精的胜利**
>
> 最后，我想为“克制”说几句公道话。在软件行业，克制长期被低估，甚至被误解。产品经理怕被说“功能太少”，开发者怕被说“不够创新”，于是大家都在做加法。可如果我们诚实地审视那些活得最久的工具——Unix 哲学、命令行、Git、tmux——会发现它们全部赢在减法上。
>
> Marriott 那句“bloat due to features like serial line I/O is not going to happen”，翻译成设计语言就是：每一次说“不”，都是对用户时间的尊重。功能少而精的胜利在于，它把复杂性留给了自己，把简单性留给了用户。tmux 的核心功能一页纸能写完，但这一页纸上的每一个功能都经过了十九年的打磨；它拒绝了一百个锦上添花的需求，却因此让一千个脚本、一万个配置、两万个星标的插件生态有了生长的空间。
>
> 我常常想，如果当年 Marriott 不是一个“克制”的人，而是“用户要什么就给什么”的人，今天的 tmux 会是什么样？大概会变成一个什么都沾一点、什么都做不好的庞然大物，像它的前辈 Screen 一样，停在某个版本号上，再也没人敢动。恰恰是那些被拒绝的功能——串行线路 I/O、以及无数类似的提议——保住了 tmux 的命。克制不是保守，克制是最难的技术决策：它要求你在“能加”和“该加”之间，永远选择后者。
>
> 这也是我给所有工具使用者和工具创造者的一句忠告：功能多不是美德，功能准才是。一个工具的生命力，不取决于它覆盖了多少需求，而取决于它把最核心的需求解决得多干净。tmux 用十九年的寿命证明了这一点——少，但锋利，比多而钝活得更久。

## 尾声：这一瞬间的平静

回到这一章开头那家上海咖啡馆。WiFi 断了两分钟，我重新连上，attach 进会话，继续讲我的分享——全程没有慌张，没有数据丢失，没有任何人注意到发生了什么。这一瞬间的平静，就是 tmux 至今仍被称为利器的全部原因。

回顾这十九年：一个人的“不爽”，变成了一个原型；一个原型，变成了一个标准；一个标准，在今天被 AI 时代重新捡起来，成为人类与机器共同工作的编排界面。工具会更新换代，Screen 会老去，也许有一天 tmux 也会被更优雅的答案取代——但在那之前，它依然是最优雅的答案。因为它做了一件好工具该做的全部事情：在你需要的时候出力，在你不需要的时候隐身，在出事的时候替你兜底。

从 1987 年到 2026 年，四十年了。会话持久化、终端组织——这两个问题从未消失，而 tmux 始终站在答案的位置上。这不是运气，这是设计哲学的胜利：解决对的问题，保持克制，尊重用户，守住边界。只要终端还在，只要连接还会断，只要人还需要同时做很多件事，tmux 就依然是那件趁手的利器。

去创建一个属于你的 session 吧——然后让它，活得比你久。

---

# 终章 后记

这本书写完那天，我坐在书房里，盯着屏幕上最后一行字发了很久的呆。窗外是上海六月的梅雨天，黏糊糊的，空调外机嗡嗡作响。我脑子里盘旋的，不是“终于写完了”那种如释重负，而是一个很具体的问题：我是不是把 tmux 写得太平淡了？

这个念头很怪。我知道这本书不短，可“长”和“值得”是两回事。一本书写完了，只证明我把它写完了，不证明它该被写出来。于是我在那个闷热的午后，重新问了自己一遍那个在动笔之前就被问过无数次的问题：一个终端复用器，到底值不值得一本书？

后记本该是收尾的地方，我却把它写成了再一次的追问。这大概就是写这本书的常态：我从来没有真正离开过这个问题，它陪了我整整三个月，从春末写到初夏。现在，答案就放在你面前的这些章节里。我想在告别之前，把这个问题、以及我在寻找答案的路上捡到的东西，原原本本地讲给你听。

写这本书的三个月里，我被问过最多的一个问题，恰恰也是我自己问自己最多的一个问题：你写这个，图什么？图流量，它显然不够劲爆；图稿费，写工具书远不如写别的来钱。我后来想明白，图的是一个交代——给自己用了近十年、却从未真正看懂的工具一个交代，也给那个 2007 年坐在北爱尔兰某个房间里敲下第一行代码的人一个交代。这本书，就是我的 attach 回来：看看那个一直挂在后台的念头，长成了什么样子。

## 8.1 为什么写这本书——一个终端复用器值得一本书吗？

先承认吧，这个问题问得很有道理。tmux 是什么？是一个终端复用器。它不炫目，没有图形界面，没有华丽的动画，它的全部工作，就是把你的终端会话管理得井井有条。市面上关于 tmux 的资料并不少：官方手册、博客教程、短视频、速查表，一抓一大把。一个工具，被讲了这么多年，还有必要用一整本书来讲吗？

我的回答是：值得。但值得的原因，和大多数人想的不一样。

不是因为 tmux 的功能多到需要一本书来装——恰恰相反，tmux 的核心功能少得惊人，它的命令屈指可数；也不是因为它很难学——如果你愿意，一个下午就能上手。真正让我觉得它值得一本书的，是它背后那条完整的、可以被称为“史诗”的线索：一个人的“不爽”，如何变成一件被全世界使用的工具。

让我把这条线索再捋一遍。2007 年，北爱尔兰，一个名叫 Nicholas Marriott 的年轻人，是 OpenBSD 的贡献者，也是 GNU Screen 的重度用户【1】。他背下了 Screen 的全部快捷键，却依然被这个老牌工具的毛病折磨得够呛。Screen 诞生于 1987 年【2】，到 2007 年已经二十年了，约两万行 C 代码堆在一座老房子里，开发早在 2004 年就已停滞【4】。Marriott 最终没有选择修补，而是决定重写。他花了几星期写了个叫“nscr”的原型【5】，验证了 client/server 架构的可行性；2007 年 10 月 10 日，tmux 有了第一次代码提交【6】；同年 11 月 20 日，第一版公开亮相【7】。

然后呢？2009 年，OpenBSD 的 hackathon 上，Paul Irofti 的一次提及【9】，让 tmux 进入了一个更挑剔的世界的视野；Theo de Raadt 亲自审阅代码，两小时后给出评价：只发现一两个次要问题【10】；同年 7 月，tmux 被导入 OpenBSD base【11】，成为操作系统的一部分。再后来，Red Hat 在 `RHEL 7.6` 宣布弃用 Screen、到 `RHEL 8.0` 时彻底移除【16】，官方把“现代标准”的位置交给了 tmux。到了 2026 年，在 AI 编码时代，tmux 的分屏成了多 agent 编排的界面，会话持久化让 agent 的长任务在断开后继续运行【23】。

你看，这哪里是一个工具的故事？这是一个人的故事，一个社区的故事，一个关于“标准如何沉淀”的故事。如果说一本工具书的及格线是把功能讲清楚，那么这本书想做的，是越过及格线，把功能背后的“为什么”讲清楚。功能讲清楚，你学会的是操作；“为什么”讲清楚，你学会的是判断。而判断，才是你在十年后依然用得上的东西。

再打个比方。学开车，驾校教你的是“怎么让车动起来”；但你真正学会开车，是在你理解了“为什么转弯要减速”“为什么后视镜有盲区”之后。工具书也一样：速查表教你“怎么让 tmux 动起来”，而这本书想教你的是“为什么它是这个样子”。学会了前者，你会用 tmux；理解了后者，你才算真正拥有了它。前者是租来的房子，后者是你自己装修过的家。

还有一点得说清楚：这本书不是只写给程序员看的。我认识一位产品经理，因为要盯十来个客户环境，也成了 tmux 的重度用户；我还认识一位数据工程师，她的日常工作就是在一台机器上挂着好几个训练任务。tmux 的学习门槛，比大多数人想象的低得多——如果你愿意，一个下午就能上手。这本书同样欢迎他们。

这就是我写这本书的第一个理由：tmux 值得一本不只是手册的书，因为它配得上一个不只讲操作的故事。

还有一个更私人的理由。我用了快十年的 tmux，却一直没搞懂它为什么要长成这个样子。它就像一个相处多年的老朋友，你知道他可靠，却从未真正了解他。写这本书，是我对自己欠下的那场认真对话的偿还——把“用”变成“懂”，把“熟悉”变成“理解”。如果你也曾经长期使用某个工具却从未追问过它背后的故事，你会明白这种心情：不是工具需要一本书，是我需要这场对话。

还有一个现实的理由：为什么是现在？2026 年的终端，正在经历一场文艺复兴。AI 编码代理把终端重新变成舞台中央，tmux 的分屏成了多 agent 协作的界面，会话持久化让长任务在断开后继续运行。在这个节点写一本关于 tmux 的书，不是怀旧，是想在技术变迁里，替一个仍然重要的工具留下它应有的位置。工具会换代，但工具背后的判断力不会过期。

> 💡 **独思时刻：** 有个朋友听说我在写一本关于 tmux 的书，第一反应是“这玩意儿有什么好写的”。我当时笑了笑，没有反驳。但私下里我想了很多：为什么我们对“关于工具的写作”如此苛刻，却对关于游戏、关于球鞋的书宽容得多？后来我想明白了——因为我们默认工具是“中性”的，它只是达成目的的手段，不值得被凝视。可事实恰恰相反：工具是我们与世界交互的方式，它塑造我们的习惯，习惯塑造我们的思维，思维塑造我们是谁。一辆车值得一本画册，一把吉他值得一本传记，一个被千百万人每天使用的工具，凭什么不值得一本书？值得。它不只是“值得”，它早就该有了。

## 8.2 写作过程中的自我发现——从查资料到理解一个工具的灵魂

写这本书最痛苦的一个决策，是怎么处理“功能列表”和“故事”之间的平衡。你翻到第六章，看到那些快捷键表格、配置示例，可能会觉得这跟市面上任何一本工具书没什么区别。但我知道，真正的 tmux 远不止这些。它的灵魂在前面的章节里，在那个北爱尔兰人对着 GNU Screen 的代码库做了一个决定、然后从头开始的故事里。问题在于：如果我只写故事，你会觉得这本书没用；如果我写满表格，你会觉得它跟其他手册没什么两样。我在这个拉扯里卡了将近三周，删掉的草稿比保留的多一倍。

最后我做了个不算决定的决定：把表格留着，但逼自己每一章都留出“独思时刻”那种不守规矩的段落。那些才是我真正想说的。表格是给工具的面子，独思时刻是给工具的里子；面子让人学会用，里子让人懂得敬。

写这本书最意外的收获，是我重新审视了自己和工具之间的关系。为了核实早期版本的发布时间线，我把 tmux 的邮件列表从头到尾翻了一遍——不是 GitHub 上的 issue，是真正的邮件列表，那些 2008 年、2009 年的纯文本邮件。我看到了 Marriott 回复用户提问的语气，耐心得不像一个写出如此底层代码的人。有人问“为什么不能支持串口通信”，他回了一句话，大意是“那种膨胀不会发生”【21】。我盯着那句平淡的英文看了很久。

那时候的互联网，还没有被短视频和速查表填满。一个项目的历史，就安安静静地躺在邮件列表的存档里，等一个愿意翻的人。我把那些邮件一封封读过去，像读一个人的日记。我突然意识到：我们这一代人对工具的认知，几乎全是被“结果”喂养的——我们用着最新版本，看着最光鲜的教程，却对工具“为什么长成这样”一无所知。而历史，恰恰是理解一个工具最快的捷径。

那句话里有一种很难模仿的东西：笃定。他知道自己在保护什么——不是自己的代码，而是这个工具的“形状”。一个工具一旦长歪了，就再也直不回来了。Screen 就是前车之鉴：功能堆叠了二十年，最后堆成了一座无法翻修的老房子【4】。Marriott 不想让 tmux 变成第二座。

从查资料到理解一个工具的灵魂，中间隔着的，正是这些“没说的话”。资料告诉你 tmux 有 hooks、有 resurrect、有各种选项；而灵魂告诉你，这些功能为什么长这样、为什么没有长成别的样子。查资料是收集答案，理解灵魂是看见选择。一本书如果只给你答案，那你读到的是一本字典；如果它能让你看见那些选择，你读到的才是一个故事。

说起来有点惭愧：我最初接下这本书，动机并不高尚——觉得这个题目“没人写过，好写”。写起来才发现，越写越心虚：市面上写 tmux 的资料那么多，我凭什么觉得自己能写出新东西？直到我把那些邮件列表翻完，才慢慢有了底气——原来大多数资料都在复述“是什么”，极少有人在讲“为什么”。于是这本书慢慢从“任务”变成了“使命”：把那些散落在邮件列表、访谈和代码注释里的选择，捡起来，讲给你听。

再举一个小例子。垂直分屏——今天看来理所当然的功能，当年却是 tmux 与 Screen 的分水岭：2009 年，Marriott 在访谈里说它“约完成百分之九十”【12】；而 Screen 的垂直分屏，从未进入生产版【4】。一个功能的有无，背后是两套架构哲学、两种维护节奏、两个时代的选择。像这样的细节，在这本书里俯拾皆是。它们不是知识的点缀，它们是历史的证据：tmux 的每一行代码都有来历，每一个选项背后都站着一个真实的人、一次真实的争论、一个真实的需求。工具不是从天上掉下来的，它是被一群人一点一点“选择”出来的。而理解一个工具的灵魂，说白了就是理解那些选择。

还有一个发现，是关于“独思时刻”这个形式的。最初我把它当成一种调剂——怕读者被快捷键淹没，所以隔几章插一段“人话”。写到后来才发现，真正离不开它的不是读者，是我自己。每次写完功能性的章节，我都需要一段“人话”来确认：我写的这个东西，到底跟一个人有什么关系？独思时刻变成了我的锚点，提醒我为什么坐在这里。后来我索性把这个形式固定下来，当作全书的一个节拍器。

## 8.3 重访核心问题——功能少 vs 功能多，Marriott 的答案为何正确

现在让我们回到全书的核心问题之一：一个工具，到底应该功能多，还是功能少？

这个问题在软件行业吵了几十年，至今没有定论。功能多的一方说：用户需要什么，我就给什么，多多益善，总有一款适合你。功能少的一方说：功能是成本，每一个新功能都在消耗用户的理解力、维护者的精力、以及工具本身的形状。

tmux 站在这道题的一边，答案很明确。Marriott 那句“那种膨胀不会发生”【21】，不是一句口号，而是一条被严格执行了十九年的治理原则。他拒绝过串口输入输出，拒绝过很多“听起来很有道理”的东西。有人或许会问：拒绝功能，难道不是傲慢吗？用户明明需要啊。

要回答这个问题，得先想清楚：tmux 拒绝的，到底是“功能”，还是“膨胀”？这两者有本质区别。tmux 不是不发展，它一直在发展——2016 年 `2.0` 的不兼容更新【14】、2019 年 `3.0` 引入的 hooks 事件驱动机制【15】、2025 年 11 月 `3.6` 带来的原生滚动条与 Mode 2031 主题【18】、2026 年 5 月 20 日发布的 `3.6b`【19】；插件生态里，TPM 早已成熟，resurrect 与 continuum 这对组合的星标累计超过两万【20】。它拒绝的从来不是功能，而是那些“与核心无关、却会永久改变工具形状”的膨胀。

判断一个功能该不该进核心，Marriott 有一把尺子：这个功能，是不是所有人都会用？如果只是少数人在特定场景下的需求，那就应该留给配置、脚本、插件去解决，而不是塞进核心。synchronize-panes 是个有趣的例外：它最初是一个用户的请求【13】，Marriott 想了想，决定加入——因为“同时给多台机器敲同样的命令”这件事，几乎每个运维都干过，它有普适性。

而串口输入输出呢？那是极少数嵌入式场景的需求，为了它，tmux 要背上一个巨大的兼容包袱，而且与“终端复用”这个核心使命毫无关系。拒绝它，不是傲慢，是清醒：知道自己的边界在哪，比知道自己的能力在哪更重要。

我还想多说一层：功能少，不等于服务少。恰恰相反，正因为核心保持克制，边界才足够开放。Marriott 说过一句让我回味了很久的话：“People use tools in surprisingly different ways”——用户使用工具的方式千奇百怪【22】。这句话是一个清醒的认知：你永远无法预测用户会怎么用你的工具，所以最好的做法不是替他们把所有路都铺好，而是把核心的地基打牢，把扩展的可能性交给他们。功能多，是替用户做决定；功能少而边界开放，是把决定权还给用户。

其实“把决定权还给用户”这件事，在 2026 年有了一个新的注脚：AI 编码代理正在成为新的“用户”。Claude Code、Codex 们不会抱怨、不会提需求，但它们对界面稳定性的要求近乎苛刻——它们需要确定性的环境、可预测的会话、断线后依然存在的状态【23】。Marriott 十九年前做减法的决定，在 AI 时代成了最奢侈的资产：一个干净、稳定、持久化的核心，正是 agent 们最需要的土壤。你看，做减法的人，往往不是输在未来，而是赢在未来。

我在书里写那个六十四兆内存的 VPS 上 tmux 与 Screen 的资源对比时，其实藏了一句没说的话：一个工具对资源的克制，本质上是对用户的尊重——它默认你的机器可能很烂，你的网络可能很差，你的环境可能随时崩塌。它不假定你拥有一切，它只为最坏的情况做好准备。这种克制，在功能至上的时代里，显得笨拙而珍贵。

其实“少”还有一个被忽略的维度：维护成本。功能少的工具，意味着更少的代码路径、更少的兼容性包袱、更少的边界情况——也就意味着更少的 bug、更少的维护者心力、更长的寿命。Screen 的教训就在眼前：二十年间功能不断堆叠，最终堆到无人敢动、开发停滞【4】。而 tmux 把“不加”当成和“加”一样重要的决定，于是它能活得比大多数“更强大”的同类更久。克制不是保守，克制是让工具活得更久的智慧。

最后，给“少”与“多”之争补一个例子：tmux 的生态里，resurrect 与 continuum 这对保存恢复插件，星标累计超过两万。有意思的是，它们不是核心功能——核心只提供最基础的会话机制，把“保存”和“恢复”交给社区解决。结果呢？社区做出了比核心设计更好的方案，还顺带长出了一个插件生态。少，不是匮乏；少，是把空间留给可能性。

> 💡 **独思时刻：** 功能少 vs 功能多，表面是产品问题，骨子里是世界观的差异。功能多的人相信“世界是复杂的，所以工具也要复杂”；功能少的人相信“世界是复杂的，所以工具要简单，把复杂留给世界自己”。我年轻时笃信前者，觉得功能全就是实力强，软件体积大、菜单多、选项密密麻麻，就是“专业”。后来被各种臃肿软件折磨过，才慢慢转向后者。写这本书的三个月里，我无数次对着 tmux 的代码和文档想：一个人要多自信，才敢在所有人都在做加法的时候坚持做减法？答案也许不是自信，而是诚实——诚实面对“这个功能我真的需要吗”，诚实面对“这个功能会让后来的人付出什么代价”。做减法的工具，往往不是最热闹的，但一定是最耐用的。十年后再看，热闹的已经换了好几茬，耐用的还在那里。

## 8.4 工具的哲学——工具是思维的延伸，好工具塑造好习惯

写这本书的过程中，我越来越确信一件事：工具是思维的延伸。

这话听起来有点玄，其实很具体。锤子是手臂的延伸，它把“砸”这个动作放大；自行车是双腿的延伸，它把“走”这个动作放大。软件工具也一样——编辑器是思想的延伸，它把你脑子里的想法变成文本；终端复用器是注意力的延伸，它让你同时看见多个进程、多个任务、多个上下文，而不必在它们之间反复切换、反复丢失。

tmux 的思维含量，藏在它最朴素的概念里。还记得第〇章那个深夜断线的场景吗？SSH 断了，进程收到 SIGHUP，一切归零。而 tmux 给出的答案是：把“会话”和“连接”解耦。终端只是进程的“窗口”，不是进程的“容器”；窗口碎了可以换，墙上的进程纹丝不动。这个想法，本质上是一种关于“什么该持久、什么该易失”的判断——而判断，就是思维。

很多人问：iTerm2 不也能分屏、不也能恢复会话吗？是的，但那是客户端的视觉装饰【24】——你的终端软件死了，一切都散了；而 tmux 的会话运行在服务器端，终端软件只是它的一扇窗。这个区别，不是一个技术细节，而是一种世界观：你把鸡蛋放在哪个篮子里，决定了你信任什么、依赖什么。

好工具不仅延伸思维，还塑造思维。这一点在 tmux 身上体现得淋漓尽致。你刚用它时，学的是一堆快捷键；但用久了你会发现，真正改变你的不是快捷键，而是“会话思维”。你会开始主动把工作组织成“会话、窗口、面板”的层次结构，会习惯性地给任务起名字，会在做一件长事之前先想“它能不能在断开后继续活着”。这些习惯，是工具教给你的。

说到思维的延伸，我想起一件小事。有一次，我在同事的屏幕上看到一个 tmux 会话，名字叫“毕业设计”。他三年前就用这个会话跑实验，如今人在另一家公司，那个会话还挂在一台他几乎忘了密码的服务器上。他笑着说不舍得 kill，因为那是他学生时代的存档。工具用久了，是会附着记忆的——你给会话起的每一个名字，都是你注意力的一段编年史。这大概就是为什么我给会话起名字的习惯，比任何快捷键都坚持得更久。

第 6 章里我给了个建议：先只记 5 个快捷键。有人觉得这是偷懒，我却觉得这是工具塑造习惯的最佳范例——一个工具好不好，不看你记住它多少，而看它让你养成了什么习惯。好习惯是：重要的事先 detach 出去；分屏是为了减少上下文切换，不是为了炫耀屏幕大；给会话起名字，是给注意力建立秩序。这些习惯一旦养成，换到任何工具上都成立——因为它们本质上是思维习惯，不是按键习惯。

再往深处说一层：会话持久化还悄悄改变了我对“离开”的态度。以前，我舍不得关掉电脑——总怕关了就断了，断了就丢了。现在，我敢在任何时候合上笔记本，因为我知道那些进程还活着，在服务器上替我守着。这种“敢离开”的底气，是从 tmux 学来的：把重要的东西放在一个不依赖你的地方，然后放心地走开。人生里很多焦虑，其实都源于“不敢 detach”——怕一放手就没了。tmux 用最简单的办法告诉你：放手，它还在。

而这一切的前提，是工具本身足够“轻”。好工具应该“消失”——用时不打扰，出事时兜底。tmux 就是这样的工具：它不会主动跳出来说“你看我多厉害”，它只是在每个 SSH 断开的深夜，默默地让服务器上的终端会话继续运行，等你重新连上来，然后轻轻敲下 `tmux attach`——一切就像没断过一样。

这一瞬间的平静，就是工具的最高境界：不是被使用，而是被信任。

## 8.5 从 tmux 到人生——会话持久化隐喻

写到这里，我想聊一个也许有点越界的话题：从 tmux 到人生。

别笑，我是认真的。tmux 的核心能力叫“会话持久化”，而这个词，放在人生里同样成立。你把一件事 detach 出去，让它脱离你的视线、脱离你的掌控，但它依然在运行；过一段时间，你再 attach 回来，一切照旧——进度没有丢，状态没有丢，只是你离开了一阵子而已。

2023 年初那次高铁上的经历，让我对这一点感受太深了。隧道里信号断断续续，SSH 掉线了七八次，但 tmux 的会话始终挂着。我一次次重新 attach 进去，一切照旧——编译还在跑，日志还在滚动，好像我从未离开过。那一刻我不是在用一个工具，我是在被一个工具兜底。

后来我想，人生里那些重要的事，其实也需要这种“detach 出去，让它持续运行”的智慧。比如写作：你不能永远盯着文档，你得让它“运行”着，去做别的事，让灵感在后台发酵，过几天再回来，它还在，而且往往长出了新的东西。比如学习：你不能一口气把所有知识灌进脑子，你得让知识“挂着”，去生活、去实践，隔段时间再回来，你会发现自己竟然懂了。比如那些长期主义的追求——健身、阅读、一门手艺、一段关系——它们都需要被 detach 出去，脱离你的即时监控，然后在时间的服务器里安静地运行。

detach 不是放弃，是信任。你信任那个进程会自己运行下去，信任那些离开你视线的东西不会消失。而 attach 是回来确认：确认它还在，确认它长成了什么样子。人生里最珍贵的时刻，往往就是这些“attach 回来”的时刻——你以为会消失的东西，还活着，而且活得比你好。

这个隐喻还有一个更狠的版本：有些事，需要你 kill 掉。tmux 里，会话不是永生的，该结束的就要 kill-session。人生亦然——不是所有事都值得持久化，有些事，detach 出去之后你就该明白，它不值得你 attach 回来。判断哪些事值得持久化、哪些事该 kill，是人生最重要的能力之一。tmux 只是把这道选择题，摆在了你面前。

持久化最迷人的地方，在于复利。一件事一旦被 detach 出去，它就在后台获得了自己的时间——你睡觉的时候它在运行，你发呆的时候它在运行，你把它忘掉的时候它还在运行。三个月后 attach 回来，你会发现它早已不是当初那个样子。写作、学习、健身、手艺，莫不如此：那些看似“放着不管”的投入，其实一直在以你察觉不到的方式累积。tmux 教我的，不是“挂后台”这个操作，而是“相信时间”这个态度。

还有一层：tmux 允许你同时开着多个会话，每个会话有自己的名字、自己的窗口、自己的节奏。这不就是人生的并行吗？工作是一个 session，家庭是另一个 session，爱好是第三个。它们各自运行，互不干扰；你随时可以切换上下文，也可以把一个暂时放下的 session 留在后台。真正的高手，不是把所有 session 都开满屏幕，而是知道此刻该 attach 哪一个、该把哪一个 detach 出去。

持久化还有一个隐藏的好处：它让你对“沉没成本”变得坦然。以前，一件事做了一半，丢了，我会心疼很久。现在我会想：没关系，它还在后台运行着。就算最终决定 kill 掉它，我也知道那是我的选择，而不是意外。tmux 把“结束”和“意外中断”区分开之后，人对失去的恐惧就少了一大半——因为你知道，只要你想，一切都可以重新 attach 回来。

还有一个时代背景值得一提：我们生活在一个注意力被反复打断的时代，而 detach 恰恰是这种环境的解药。你没法阻止世界打断你，但你可以决定哪些东西值得被打断、哪些东西该被 detach 出去、安静地运行。tmux 的快捷键里，我最喜欢的是 `Ctrl+b d`——它只有一次按键，却代表一个完整的动作：把当下的事情安置好，然后从容地离开。这个动作做多了，你会慢慢发现，从容是一种可以练习的能力。

> 💡 **独思时刻：** 我写这本书的三个月，本质上就是一场大型的 detach 实验：把书“挂着”，去上班、去生活、去发呆，然后一次次 attach 回来，看它长出了什么。有意思的是，最让我满意的段落，几乎都不是在电脑前“憋”出来的，而是在某个没有电脑的时刻——散步、洗澡、等地铁——突然冒出来的。灵感一直在后台运行，只是我没有给它一个 session 而已。后来我养成了一个习惯：手机备忘录里存一个永远不关的“session”，任何想法随手丢进去，不管它现在有没有用。三个月后回头翻，那些碎片里躺着这本书不少金句的雏形。你看，持久化这件事，tmux 早就在教我，我只是到今天才听懂。

## 8.6 致谢与致敬——站在巨人的肩膀上

写到这里，照例要道谢。但我不想把致谢写成一张客套的名单，我想把话说到位。

首先要谢的，当然是 Nicholas Marriott。一个北爱尔兰人，一个 OpenBSD 的贡献者【1】，在 2007 年做了一个“算了，我自己来”的决定。他本可以继续忍着 Screen 的毛病，继续背那些快捷键，继续在别人的代码里将就；但他没有。他花了几星期写了个原型【5】，验证了想法，然后一路把它带进了 OpenBSD base【11】。十九年后，这个工具成了终端复用的现代标准。我想象过很多次那个场景：2007 年秋天，他第一次敲下新项目的第一个文件，他一定不知道这个决定会走向哪里。但他知道自己在做什么——这就够了。

然后是 OpenBSD 社区。2009 年，Paul Irofti 在 hackathon 上的那次提及【9】，是 tmux 命运的第一个转折点。更让我感慨的是 Theo de Raadt——他亲自审阅了 tmux 的代码，两小时后给出评价：只发现一两个次要问题【10】。这句话的分量，只有了解 OpenBSD 的人才知道：这是全世界最挑剔的评审者给出的背书。能被最严格的人认可，是一个开源项目能拿到的最高荣誉。而 Marriott 选择留在 base 里开发【11】，主动把自己置于“更多专家用户、更严格节奏”的环境里——这种选择，本身就是一种对质量的信仰。

还要谢的，是那些我素未谋面的人：Oliver Laumann，1987 年那个发布了“BSD screen”的柏林工业大学学生【2】——没有他，就没有整个终端复用器家族；Jürgen Weigert 和 Michael Schroeder，他们在 Screen 需要人手时接过维护权【3】——没有他们，这段历史会缺一大块；以及每一个在邮件列表里提过问题的人、每一个提交过补丁的人、每一个写了插件的人【20】。开源世界里没有“官方认证的英雄”，每个人都只是做了一点小事，但正是这点点小事，让一个工具活过了十九年。

这本书能写成，还要谢那些记录历史的人。整理时间线的时候，我翻遍了 tmux 的邮件列表存档、OpenBSD 的源码树、以及无数篇散落在个人博客上的访谈【9】【10】【12】——那些作者大概从没想过，自己随手写下的记录，会在十几年后成为另一个作者拼图的关键碎片。写历史的人往往不知道自己正在写历史。我们这些后来者，不过是在他们留下的砖石上，砌自己的墙。

写这本书的另一个私心，是想让中文世界的读者也能读到这段故事。tmux 的英文资料浩如烟海，中文资料却大多停留在“怎么用”的层面。把 Marriott 和 OpenBSD 的故事讲给中文读者听，让更多人不只知道 tmux 好用，还知道它为什么好用——这是我作为一个中文写作者，能给这个工具的一点回礼。

还要谢的，是这个时代。2026 年，AI 编码代理正在把终端重新变成舞台中央【23】——Claude Code、Codex 们在 tmux 的分屏里各司其职，会话持久化让它们的任务在断开后继续奔跑。一个 2007 年诞生的工具，在二十年后的新世界里找到了新的位置。这大概就是“标准”的样子：不追赶潮流，却总在潮头。

最后，要谢的是你——正在读这本书的读者。写书是个孤独的活计，尤其在写一个“谁都会用”的工具时，你永远不知道屏幕那头有没有人在看。但我知道你会来。因为你会 SSH 断线，你会深夜加班，你会遇到那个“要是能继续跑就好了”的时刻——然后你会想起这本书，想起那个北爱尔兰人的故事，想起那句“愿你下次不爽的时候，不妥协”。

这就是写作的意义：不是传递知识，是传递一种可能性——你也能做到，你也可以“算了，我自己来”。

## 8.7 附录导读——让快捷键回到指尖

在正式告别之前，还有一件小事要交代：全书的核心快捷键速查表，在第 6 章。如果你读完前面的章节，手指头已经开始痒了，现在就是回顾第 6 章最好的时机。为了让你少翻两页，我把最核心的十几条抄在这里——完整版、以及每一条背后的原理，都在第 6 章等着你。

```text
# 会话管理（shell 命令）
tmux new -s work          # 创建命名会话 work
tmux ls                   # 列出所有会话
tmux attach -t work       # 重新附着到会话 work
tmux kill-session -t work # 关闭会话 work

# 会话内快捷键（前缀键默认为 Ctrl+b）
Ctrl+b d                  # 脱离会话（detach），会话继续运行
Ctrl+b c                  # 创建新窗口
Ctrl+b n / Ctrl+b p       # 切换下一个 / 上一个窗口
Ctrl+b %                  # 垂直分屏（创建面板）
Ctrl+b "                  # 水平分屏（创建面板）
Ctrl+b 方向键             # 在面板间切换
Ctrl+b z                  # 缩放 / 还原当前面板
Ctrl+b s                  # 会话列表，交互式切换
Ctrl+b $                  # 重命名当前会话
```

如果你嫌 `Ctrl+b` 别扭，想改成 `Ctrl+a`，第 6 章第 2 节有完整的改法与理由。我多说一句：这张表不是让你背的，是让你“用到再查”的。先让 5 个键成为本能，再把这张表当成字典。三个月后你会回来感谢这个建议的。

至于配置文件的写法、状态栏的美化、resurrect 与 continuum 的保存恢复方案，第 6 章第 7 到第 9 节都给出了可以直接抄的完整配置。等你把基础快捷键用顺了，再去折腾那些不迟——tmux 的配置是长出来的，不是抄出来的。

对了，如果你只打算养成一个习惯，那我建议是：给会话起名字。`tmux new -s` 只比 `tmux new` 多敲几个字符，但“给它起个名字”这个动作，会把一次随手的开终端，变成一次郑重其事的开始。名字是注意力的锚点——当你给一件事起了名字，你就开始认真对待它了。这个道理，工作里适用，生活里也适用。

最后提醒一句：如果你用的是笔记本电脑，记得给 tmux 设置一个舒服的 detach 键位，并且养成“合盖前先看一眼有哪些 session 还挂着”的习惯。我不会告诉你具体怎么做——第 6 章写得明明白白。我只是想提醒你：别忘了，那些 session 还活着，它们值得你时不时回来看看。

哦，还有一件事：等你真的用起来了，别忘了把这份经验传下去。工具的价值，一半在使用，一半在分享。哪天你在同事的屏幕上看到一个乱糟糟的终端，别急着说“你应该用 tmux”——先问他一句：“你平时是怎么忍过来的？”然后，把这本书推荐给他。知识最好的归宿，是被下一个需要它的人捡走。

## 8.8 告别语——去创建一个属于你的 session，然后让它活着

我不知道你读完这本书之后，会不会真的把 tmux 用起来。说实话，我也不确定这是不是一本“有用”的书。我知道它讲了很多历史、很多原理、很多“为什么”，而这些在快捷键速查表面前，看起来都像废话。

但我希望你能带走一个念头：那些你每天敲进终端的工具，它们的作者也曾经坐在某个闷热的房间里，面对一堆烂代码，做了一个“算了，我自己来”的决定。你和那个作者之间的距离，通常只是一次不爽。

所以，如果这本书有一句要记住的话，那就是：去创建一个属于你的 session，然后让它活着。不一定是 tmux 的 session——也可以是你想写的那本书、你想学的那门手艺、你想做的那个项目、你想成为的那种人。把它们 detach 出去，让它们在时间的服务器里运行，然后，定期 attach 回来，看看它们长成了什么样子。

写到这里，我忽然想起第〇章里那个凌晨两点三十七分的场景。那是我编的——但我赌你见过它，或者即将见到它：SSH 断线、进程全灭、一夜白干，你在深夜的屏幕前骂了一句脏话，然后决定明天把一切重来。我写那一段，是想让这本书的第一个读者，也就是那个深夜里的你，知道有人替你记着那一刻。现在，这本书要结束了，我只想对那个深夜里的你说一句：那个让你不爽的夜晚，后来变成了什么？如果它什么都没变成，那也没关系——这本书，就是从那样一个夜晚开始的。

工具的哲学，最后都会变成生活的哲学。会话持久化，是 tmux 教给世界的第一个词；而“让它活着”，是它教给我的最后一课。

愿你下次不爽的时候，不妥协。

愿你创建的每一个 session，都活得比你的耐心更久。

## 8.9 独思时刻——写这本书的三个月，我学会了什么

> 💡 **独思时刻：** 如果非要用一句话总结写这本书的三个月我学会了什么，我会说：我学会了“看见”。以前我用 tmux，看见的是快捷键、是效率、是“能不能多开几个窗口”；现在我用 tmux，看见的是一个人 2007 年的决定【5】【6】，是一群人在邮件列表里的争论，是一个社区十几年的坚持，是四十年不变的、关于“持久化”的人类需求【2】。工具还是那个工具，屏幕还是那个屏幕，但看它的眼睛不一样了。这三个月里最魔幻的时刻，是有一次我深夜写稿，盯着屏幕上那个熟悉的底部状态栏，突然意识到：我面前的这个界面，从 2009 年起就没有本质变化过【11】——它稳定到让人忘记它也是被设计出来的。而稳定，恰恰是无数人选择的结果。写书让我学会的最后一件事是：任何你觉得理所当然的东西，都值得被追问一次“为什么”。你问得越多，你看见的就越多。这就是这本书想给你的——不是技巧，是看见。

二〇二六年夏，上海
