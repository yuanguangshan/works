# 《命令之下》

> 作者:广山哥  
> 完成时间:2026-09-02

---

## 序言

# 序言

先做一个实验：随手打开你正在用的终端，不管它是 macOS 的 Terminal、iTerm2，还是 Linux 下的 Alacritty、Kitty，然后想一想——你现在使用的输入协议，和五十年前坐在一台电传打字机前面的操作员，有多大的区别？

答案令人不安：几乎没有。你敲下的字符仍然一行一行地走，回车仍然既是“回到行首”又是“换到下一行”的两个动作的合体，退格键在内核里仍叫 ERASE，Tab 键补全背后站着一九八〇年就定型的协议，Ctrl+C 仍然是把一个字节（0x03）交给内核、由内核翻译成信号。屏幕早就从纸换成了像素，键盘早就从机械换成了电容，但这两样设备之间的对话规则，几乎原封不动地用了半个多世纪。

软件世界里，没有第二个人机界面享受过这种待遇。图形界面换了好几代范式，网页从静态读到单页再到现在的一切皆应用，手机交互从按键到多点触控再到手势——它们的祖先早就被埋掉了。只有终端，它的每一层历史都还活着，都在被使用，都在给新来的地层当下垫。

这本书想干的，就是把这套层层叠加的地层挖开给你看。

## 一门考古学

我把这门手艺叫工程考古学。它的方法很简单：遇到任何一个“天经地义”，先问它从哪来。

终端的世界里，天经地义的东西特别多。为什么终端默认八十列？因为一九二八年 IBM 打孔卡一列就是八十个孔位。为什么回车和换行是两个字符？因为打字机上这是字车回到最左和纸卷上滚一格的两个机械动作。为什么有个 terminfo 数据库？因为七〇年代每个厂商的终端方言都不一样，vi 的作者受不了一把一个终端写一套代码。为什么 Ctrl+A 是跳到行首、Ctrl+E 是跳到行尾？因为 GNU Readline 的作者决定给用户提供 Emacs 的肌肉记忆。为什么今天的 AI 编程助手（Claude Code 们）依然要靠 PTY 跑命令、靠退出码判断成败？因为——这一层你读完第六章就会明白——一个程序如果不假装自己在跟终端说话，它就没法跟任何东西说话。

这些问题的答案串起来，就是本书的九章，也就是终端世界的九层地层：电传打字机的物理遗产（TTY）、控制语法的确立（ANSI）、方言词典（Terminfo）、行编辑的搬家（Readline）、全屏科学（Curses）、远程的基石（PTY）、参数解析的三种哲学（Cobra/Clap/Click）、现代 TUI 四重奏（Ratatui/Bubble Tea/Textual/Ink）、以及最后加盖的新地层（AI 对话循环）。

九层地层，每一层都没有替换掉下面那层，只是把它包起来、垫起来、然后自己变成新的地基。这就是本书的核心论点：终端没有被现代化，它只是被考古化了。

## 和《终端觉醒》的关系

这本书有一个姊妹篇，《终端觉醒》。那本书自上而下：从 AI 时代的终端应用讲起，看 TUI 框架如何渲染、AI 如何改变终端的使用方式。这一本自下而上：从打字机讲到内核，看一切终端应用脚下的大地是怎么一层一层夯出来的。

两本书在第八章、第九章碰头：现代 TUI 框架和 AI CLI，既是《终端觉醒》的主角，也是本书地层的最上层。如果你两本都读，会看到同一批素材的两种讲法——一种讲它们怎么用，一种讲它们为什么长这样。

## 几句方法论的交代

本书的历史部分用“我们”，那是考古队员的集体口吻；实战部分用“我”，那些是在我自己的项目里真实发生的事——wsstunnel 在只有 HTTP 出站的沙箱里反向拉起真终端的整个历程，我在第六章原原本本讲给你听，包括那份四百六十六行修复的翻车记。

每章的最后一节固定叫地层学笔记：这一层的东西，教给了今天的我们什么。写完九个笔记你会发现，它们其实说的是同一件事——兼容是互联网最被低估的美德。没有五十年不破坏兼容的耐心，就没有今天一切终端创新的舞台；那些嘲笑终端老古董的人没有意识到，老古董之所以老，是因为它从来没有被超越过，只是被包围了。

书中所有机制性叙述，我都尽量配上了可以直接在你自己机器上跑的验证命令：infocmp、bind -p、stty -a、otool。考古学最怕的是只讲故事不见实物，所以这些命令就是本书的实物标本——欢迎你亲手挖一铲子。

## 致谢

感谢 wsstunnel、yuangs CLI、Tmux-FSM 和 DeepSeek Harness 的源码，它们是本书第九章之前所有实战素材的出产地；感谢那些几十年如一日维护 ncurses 和 terminfo 数据库的人——他们维护的是互联网最早、也最长寿的公共品；也感谢《一个人的军团》和《边缘王国》，这是基建三部曲的第二部，上一步我们铺好了网络的大地，这一步向下挖开了交互的地层。

第三部《推理工厂》会钻得更深：深到一块 GPU 里面，看大模型的推理工厂如何把电变成字。

现在，深呼吸。我们要往下挖了。

广山哥
二〇二六年九月，于听雨轩

---

## 目录

- 序言
- 第1章 TTY 的物理起源:我们至今住在打字机的影子里
- 第2章 ANSI 转义序列:统治五十年的隐秘语法
- 第3章 Terminfo 与 Termcap:终端能力数据库的四十年战争
- 第4章 行编辑的搬家:从内核到 Readline
- 第5章 Curses 与全屏:一门前科学的诞生
- 第6章 PTY 的魔法:一切远程终端的内核基石
- 第7章 参数解析的取舍:Cobra、Clap、Click 与帮助文本的 UX 学
- 第8章 TUI 四重奏:四种实现，一种野心
- 第9章 AI 时代的 CLI:古典循环的加盖工程

---

## 第1章 TTY 的物理起源:我们至今住在打字机的影子里

# 第 1 章 TTY 的物理起源:我们至今住在打字机的影子里

## 1.1 一台机器的孤儿:Teletype 如何工作

每一门考古学都得先回答同一个问题:最底下那一层是什么。对终端这门学问来说，答案不是屏幕，不是键盘，甚至不是计算机本身——是一台会打字的电报机。把地层剖面翻到二十世纪六十年代，我们看见的第一件展品，是一台会尖叫、会漏油、每秒只能吐出十个字符的机电怪物:Teletype 电传打字机。它不是为计算机发明的，却在无意之间成了计算机的第一张嘴与第一只耳朵;今天你在终端里敲下的每一个字符，走的依然是它当年铺好的管道。本书的全部故事，都从这台机器开始。

先看它从哪儿来。公司史的几个年份值得刻在地层柱上:1902 年，前身 Morkrum-Kleinschmidt 公司成立，隶属西部电气（Western Electric）体系;1904 年，”Teletype“作为商标被注册，”teletypewriter“一词正式出现;1928 年，公司干脆随商标更名为 Teletype Corporation，总部设在美国伊利诺伊州的斯科基;1930 年被 AT&T 收购，仍作为独立实体运营;1982 年，在始于 1974 年的反垄断诉讼之后，业务被剥离为 AT&T Teletype;1990 年，这条产品线最终关停解散（素材:materials/ch01_补充_teletype.md）。将近九十年，这家公司只认真做了一件事:把文字变成电信号送出去，再把电信号变回文字打出来。”Teletype“后来从一个注册商标演变为电传打字机的通称——商标被地层吸收成品类名，这是商业史能给一家公司的最高敬意。

机器的工作原理，可以当作”打字机与电报机的合体“来理解。按下按键，机器把字符编码成电信号送上串行线路，同时在本地把字符打印出来;线路另一端，收到的信号驱动打印机构，把字符打在连续的卷纸上。它是双向设备:既能发，也能收;在一条线路的两端各放一台，人就能与远方的人——或者远方的计算机——笔谈。它的编码学血缘来自更早的机械电报:1870 年代的博多码（Baudot），五位;后来定型为 1924 年的 ITA2。电传打字机扛着这套五单位码跑了半个世纪，直到 ASCII 来接班（素材:materials/ch01_补充_teletype.md）。

展厅中央，是 1963 年问世的 Model 33。它最初为美国海军设计，后来作为商用产品面市，是最早采用新定案的 ASCII 编码的设备之一;因为便宜、皮实、跟计算机合得来，它成了 1960 到 1970 年代最普及的电传打字机——到 1975 年，仅 32 型与 33 型的累计产量就超过 50 万台，生产线一直开到 1981 年。型号后缀是一门小分类学:ASR（Automatic Send and Receive，自动收发）带键盘与打印机，侧板里还内置纸带阅读器与穿孔器;KSR（Keyboard Send and Receive）有键盘与打印机，没有纸带设备;RO（Receive Only）只剩打印机，负责单向接收（素材:materials/ch01_补充_teletype.md）。

规格表比任何形容词都有说服力。速度:110 波特，折合每秒 10 个字符;接口早期用 20 毫安电流环，RS-232 电压接口是后来的事。打印:圆柱形字轮，每英寸 10 字符，每行 72 字符，纸是 8.44 英寸宽的卷纸。做工:整机几乎纯机械，键盘、打印、读写头全靠机电式编解码，电子部分极少，因此噪音惊人，也娇贵得有道理——手册列出了约 500 个需要加油或上脂的部位。份量:连支架约 75 磅。价格:1974 年约 755 至 1220 美元，走 DEC 配货加安装约 1850 美元，维护合同每月约 37 美元。还有一桩著名的不便:Model 33 只认大写字母，35 型同样只有大写，要等到 37 型，机器才终于认识小写（素材:materials/ch01_补充_teletype.md）。

ASR 型号的灵魂藏在侧板里:纸带。1 英寸宽、经油浸处理以润滑穿孔机构的 8 级纸带，每英寸穿 10 个字节;纸带阅读与穿孔的速度同样是每秒 10 字符。在没有软盘的年代，纸带就是程序存储介质兼数据交换媒介——程序被打成纸带保存，再从纸带读回内存。慢到什么程度?从纸带读入 PDP-8 的 2000 字内存，大约要 7 分钟，所以高速读带机与磁盘成了当年的热门选配。ASR 里的 A（Automatic）指的就是这桩本事:从纸带自动发送;KSR 只能靠人手，一下一下地打（素材:materials/ch01_补充_teletype.md）。

我们叫它”一台机器的孤儿“，说的是它的身世:电传打字机是电报时代的孩子，被计算机时代领养，又反过来给领养它的文明立了规矩。它出生时不知道自己会成为千万人面对计算机的唯一界面;设计它的工程师更不会想到，”tty“这三个字母会成为它最小的墓志铭，刻进此后每一台计算机的设备目录。接下来的三节，我们把它打在纸上的几个物理事实——宽度、回车与换行、设备身份——逐一拍成化石，看看这些化石是如何活过一百年的。

在成为计算机终端之前，电传打字机早已是电报网络的标准装备:电报网、新闻通讯社、无线电传，都靠它分发稿件与收发订单。计算机选中它，不是因为它先进，而是因为它便宜、皮实、现货供应——一款量产了几十万台的成熟机电设备，天然是当时把文字送进计算机、又从计算机取出来的最廉价方案（素材:materials/ch01_补充_teletype.md）。更重要的是，它带来了计算史的一次范式切换:打孔卡时代，人与机器的关系是批处理——提交一叠卡，等一个晚上;电传打字机时代，人与机器第一次可以对话——你敲一个字符，机器回一行结果。交互式计算不是被某台巨型机发明的，是被这台每秒 10 字符的打字机一口一口喂大的。

再补一段操作现场的复原，帮今天的读者建立体感。你要用点力气按下按键，圆柱形字轮转到对应位置，打印机构砸在纸上，一声脆响;机器以每秒 10 个字符的恒定速度吐字，快不了，也慢不下来。它只认大写，于是早期系统的通信里遍地是大写字母——不是审美，是机器的限制（素材:materials/ch01_补充_teletype.md）。而当你想保存一段程序，就要把它穿成纸带;想把它交给隔壁的机器，就把纸带塞进阅读器。今天我们双击一个文件的时间，当年是一盘纸带的物理搬运。

## 1.2 80 列、CR、LF:三个物理约束的一百年

终端为什么是 80 列?这个问题听起来像在问”人为什么有十根手指“——天经地义，无需解释。但地层学不接受”天经地义“这种答案。80 这个数字，CR 与 LF 这两个控制码，各自都是一次物理妥协留下的化石;把它们并排放好，就是一部微缩的人机输入输出史。本节我们把三块化石逐一从土里起出来，再看它们如何在屏幕时代继续发光。

80 列的祖先不是显示器，是打孔卡。1890 年，赫尔曼·霍尔瑞斯为美国人口普查发明打孔卡机，当时的卡是 45 列、圆孔;1928 年，IBM 推出 80 列矩形孔打孔卡，后来成为 IBM 的标准卡——每张卡固定 80 列，每列存一个字符，一张卡正好一行。因为 IBM 垄断了数据处理市场，80 列成了事实标准，程序员写下的一”行“，物理上就是一张卡（素材:materials/ch01_补充_80col.md）。CRT 终端登场时，首要用途是替代打孔卡做数据录入，于是屏幕宽度照抄卡片:1965 年的 IBM 2260、1971 年的 IBM 3270，都刻意把屏幕设计成 80 列宽，好让一行屏幕正好等于一张卡。这一遗产走得更远:今天 xterm 与 bash 的默认窗口仍是 80 列，而电子邮件格式标准 RFC 5322 规定每行不超过 78 个字符——80 列减去 CR 与 LF 各占的一位，正是电传打字机一行的余量。

至于”24 行“以及 80×24 这个组合本身，则是市场标准化的结果，不是技术必然。1970 年代中期，终端尺寸五花八门:31×11、42×24、50×20、52×48、81×38、100×50，技术根本约束不了它。1971 年的 IBM 3270 用 80×24 屏占据了约一半的终端市场份额，迫使所有竞争者跟进;1978 年 8 月，DEC 的 VT100（前身 VT52）采用 80×24，卖出超过 100 万台，并把 ANSI 转义序列顺手带成了行业标准;1981 年的 IBM PC 比标准多加一行，变成 80×25——今天你打开终端模拟器看到的默认窗口，多半还是这两个尺寸之一（素材:materials/ch01_补充_80col.md）。行式打印机则走了另一条路:1960 年代 IBM 1403 打印机留下的 132 列标准，至今还活在某些日志与报表系统的输出宽度里。

再看字符编码这一层。ASCII 的正式身份是 ASA X3.4-1963，1963 年 6 月获批;从 1960 年 10 月 6 日委员会首次开会算起，这份标准被吵了将近三年。主持委员会的是 Teletype 公司的 John Auwaerter——电传打字机厂商坐在了方向盘上，这个细节解释了为什么 ASCII 里处处是电传机的痕迹。委员会最终选择 7 位编码:放弃 8 位，为了省传输成本;放弃移位方案，因为错一位就全盘皆乱。128 个码位里，前 32 个全部划给控制字符，CR 与 LF 就住在其中。它的更早祖脉，是博多码到 1924 年 ITA2 这条机械电报的旧血缘（素材:materials/ch01_补充_80col.md）。

CR 和 LF 则是从打字机机体上直接拆下来的两个动作。打字机上，装纸的部件叫字车（carriage）;按回车杆，字车弹回最左端的第 1 列——这就是 CR（Carriage Return，0x0D），把打印头送回行首。LF（Line Feed，0x0A）只让滚筒把纸前进一行，字车原地不动。在真正的打字机上，要完成一次换行，你必须同时做两件事:把字车拉回左边，把纸卷上去一行。电传打字机忠实地继承了这套机械语义，把两个动作做成两个独立的键、两个独立的控制码;Model 33（1963 至 1981 年生产）完整保留了这对兄弟的分工——CR 只回列首，LF 只进一行（素材:materials/ch01_补充_80col.md）。

两个动作分开，后患一百年。文本文件里”换一行“到底怎么表示，至今没有统一答案:Windows 用 CRLF，忠于打字机”回车加换行“的两步动作;Unix 只保留 LF，把”进到下一行“当作完整换行;老 Mac 一度只用 CR。同一份源码在两家系统间来回搬运，换行符就换一次阵营。用 od 看一眼文件底层，差异一目了然:

```
$ printf 'a\r\nb\n' | od -c
0000000    a  \r  \n   b  \n
0000005
```

三种选择没有谁更正确——它们是三种不同的机械记忆:一种记得纸，一种只记得行，一种只记得列（素材:materials/ch01_补充_80col.md）。下一节我们要看，当 Unix 决定迎娶这台机器时，这些物理习惯如何被原封不动地写进操作系统的户口本。

把 110 波特这个数字拆开，能看清当年每一比特的斤两。串行线路上，一个字符被打包成一帧:起始位、数据位、校验位、停止位，合计约 11 位;110 波特除以 11，正好是每秒 10 个字符——规格表上的两个数字，其实是同一笔账。委员会坚持 7 位编码，也是这笔账:每多一位，全线设备的成本与误码率都要重算;而前 32 个码位划给控制字符，等于把线路上的机械动作——回车、换行、响铃、停带——也纳入了编码。ASCII 不只是一张字符表，它是一台机电设备的操作手册。它的谱系上承博多码与 1924 年的 ITA2，旁支里还躺着 1956 年的 FIELDATA 与 1963 年的 EBCDIC（素材:materials/ch01_补充_80col.md）。

大写限制还有一层深远后果:早期系统的命令、文件名与报文，普遍以大写书写和传输;小写要等到 Model 37 一代设备普及，才真正进入日常（素材:materials/ch01_补充_teletype.md）。更重要的，是“行”这个单位的凝固:纸是按行走字的，字车是按行回去的，于是从电传打字机往下，人与计算机的对话单位默认是一行。这一行，将在下一节成为内核的责任田。

CR 与 LF 也没有躺进博物馆。你在命令行里看到的进度条与百分比原地刷新，靠的就是单独输出 CR——把光标拉回行首重画一行，而不把纸卷上去;转换换行符的 dos2unix 与 unix2dos 至今是装机常备;连 C 标准库都保留了文本模式这个概念，专门负责在 
 与 CRLF 之间做翻译;Python 打开文件时的 newline 参数，管的也是这桩百年旧案。一百年过去，这两个控制码还在每天上班，只是字车换成了光标，再没有人听见回位的那声弹簧响。

## 1.3 Unix 的婚姻:一切皆文件，终端也不例外

二十世纪六十年代末，贝尔实验室的一群人给 PDP 系列小型机写操作系统，摆在桌上的终端就是 Teletype——最早那批 Unix，就是对着 Model 33 这类打印终端开发出来的，后来才换上支持大小写的 Model 37。这桩婚姻的门第并不般配:一边是追求优雅抽象的新操作系统，一边是每秒 10 个字符、只认大写、靠电机打字的铁疙瘩。但 Unix 做出了一个影响至今的决定:不把终端当异类，把它当成一个文件（素材:materials/ch01_补充_teletype.md）。

”一切皆文件“是 Unix 最著名的口号，而终端是这句话最硬核的证明。终端在文件系统里有自己的设备文件，名字就叫 tty——Teletype 的缩写。打开它、读它、写它，与读写一个普通文件没有区别;于是重定向与管道这些机制天然对终端生效。你在 shell 里敲 tty 命令，它回答 /dev/pts/3，本质上是在告诉你:你的键盘与屏幕，此刻对应着文件树上的这个节点。物理机器消失了，只剩下文件——抽象对实体的胜利，很少有这么彻底的。

```
$ tty
/dev/pts/3
$ echo hi > /dev/pts/3
hi
```

婚姻的日常细节里全是这台机器的影子。Kernighan 在回忆录《Unix: A History and a Memoir》里写道，在 Model 33 上打字需要相当大的力气，打印也慢——这被公认为 cp、rm、ls 这些命令只有两个字母的原因之一。每个多余的字母都是额外的击键、额外的机械动作、额外的时间;节俭不是 Unix 的美学偏好，是被打字速度逼出来的肌肉记忆。1971 年，雷·汤姆林森（Ray Tomlinson）在 Model 33 ASR 的键盘上选中 @ 符号作电子邮件地址的分隔符，理由朴素:这是键盘上罕见的、人名里用不到的字符。你今天发出的每一封邮件，地址里那个 @ 的位置，都是那台机器的键盘布局决定的（素材:materials/ch01_补充_teletype.md）。

但把终端抽象成文件，立刻撞上一个难题:这台机器太笨了。它没有缓冲，没有编辑能力;你按一下退格，它就老老实实把控制码发给计算机——纸上留下一个墨块，什么也没删掉。谁来当编辑器?Unix 的答案是内核。硬件终端太笨，行编辑只能由内核来做，这一决定直接塑造了 TTY 子系统的形状:内核里专门有一层，叫线路规程（line discipline），默认实现是 n_tty，它替所有进程完成行编辑（素材:materials/raw_readline_pty.md）。到 1979 年的第七版 Unix，线路规程作为独立的一层被明确划分出来;此后五十年，这层设计原封不动地沿用至今。

线路规程的规范模式（canonical mode）由 termios 里 c_lflag 的 ICANON 位控制。置位时，内核的接收路径替你干活:ERASE 删除行内前一个字符，KILL 删掉整行，WERASE 删除前一个单词，直到遇见行结束符，read（） 才返回;VEOF（Ctrl-D）在行首按下时返回 0，表示文件结束;ISIG 让 Ctrl-C 不再是普通字符，而是变成 SIGINT 信号;ECHO 由内核直接把你的输入回显到屏幕。规范模式还有一个上限:一行的规范长度 MAX_CANON 是 255 字节，读缓冲 N_TTY_BUF_SIZE 是 4096 字节。内核只替你管一行——这就是”行编辑“这个名字的来历，也是第四章 readline 登场之前，全世界行编辑所在的位置（素材:materials/raw_readline_pty.md）。

与规范模式相对的是非规范模式:ICANON 关闭后，内核不做编辑、不回显，read（） 的返回时机由 VMIN 与 VTIME 两个参数控制;cfmakeraw（） 是一步进入这种裸模式的便捷封装。我在 wsstunnel 里第一次真正敬重这套开关:远端拉起 bash -i 之后，如果不把终端切到非规范模式，我敲的每个字符都要等内核攒满一行才交给 bash，交互体验碎得没法用;把 ICANON 关掉，字符立刻逐个可达，交互才像交互。你按下一个键到屏幕出现字符的完整旅程，我们留到第六章逐站拆解，但它的起点与终点，都是这一层——内核里的线路规程（素材:materials/raw_readline_pty.md）。

这桩婚姻还有一条容易被忽略的条款:控制终端。进程 fork 时，子进程继承父进程的控制终端——你从哪个终端登录，你的 shell 以及它派生的所有进程，就都认这个 tty 为自己的控制终端;Ctrl-C 按下，内核把 SIGINT 送给这个终端的前台进程组，而不是随便哪个进程。登录、作业控制、信号路由，全部生长在“终端是文件”这个决定之上（素材:materials/raw_readline_pty.md）。我们今天在 tmux 里开十个窗格、在 ssh 里挂起一个会话，管理的在内核眼里都是这条条款的具体化:谁认领了你的终端，谁就有权给你发信号。

线路规程还是一个插槽，而不是一根焊死的电路。内核支持多种规程:默认的 N_TTY 负责行编辑，后来又有 N_PPP 一类，让点对点协议复用同一条串行通道（素材:materials/raw_readline_pty.md）。这个设计的妙处几十年后才显影:为电传打字机留的插槽，先后插过拨号上网的 PPP 与各式虚拟串口——硬件换了四代，插槽还是那个插槽。这是地层学的又一个例证:好的抽象不会过时，它只是不断被新硬件重新实现。

把本节的几位主角请进一台真实终端看一眼（stty 输出节选、注释为本书所加）:

```
$ stty -a
speed 38400 baud; rows 24; columns 80; line = 0;
icanon iexten echo echoe isig        # 行编辑在此:ICANON 与 ISIG
onlcr icrnl                          # 输出 LF 变 CRLF;输入 CR 变 LF
```

设备文件还藏着一个漂亮的自指:一个进程想找到自己的终端，不必记住任何设备名，打开 /dev/tty 即可——这个名字永远指向调用者自己的控制终端。于是 shell、编辑器、一切前台程序，都能用同一个名字谈论“我的终端”，无论它物理上是串口另一端的 Model 33，还是窗口系统里的 /dev/pts/3（素材:materials/raw_readline_pty.md）。这是文件抽象的第二次胜利:设备可以被替换、被虚拟化、被远程化，而名字不动。半个世纪里，终端硬件从电机打字机换成玻璃屏幕，再换成网页与容器，程序嘴里念的还是这三个字母。

本章最后留一枚化石给你:终端突然冻住，敲什么都不动，像死机了一样。这不是故障，是 Model 33 的遗物——ASCII 码 17（Ctrl-Q）与 19（Ctrl-S）在电传打字机时代被硬件分配为 XON 与 XOFF，即流量控制的开启与关闭;今天你的终端模拟器默认还认这两个码，解除的方法是把 Ctrl-Q 按回去（素材:materials/ch01_补充_teletype.md）。五十多年过去，我们还在用给纸带机停机的手势，让屏幕假装一台打印机。玻璃没有改写契约——契约只是换了个房间，继续生效。

## 1.4 玻璃电传打字机:屏幕假装自己是纸

CRT 终端出现时，没有人问”屏幕应该长什么样“，因为答案早已被给出:屏幕应该长得像一台打字机打出来的纸。玻璃电传打字机（glass TTY）——用屏幕替代纸张、但完整保留打字机行为模型的终端（概念见 BRIEF.md 概念词典）——不是一个将就的过渡方案，而是整个终端世界的开国宪法。我们今天说的”终端模拟器“，模拟的对象不是某台具体的机器，而是这台被玻璃化的打字机。

替换纸的第一步甚至不是屏幕:是点阵打印终端，比如 DECwriter II——它还在打纸，只是打字机换成了点阵头;第二步才是 CRT。1965 年的 IBM 2260 与 1971 年的 IBM 3270 把屏幕做成 80 列，3270 凭约一半的市场份额把 80×24 钉成事实标准;1970 年代中期，终端尺寸一度百花齐放，31×11 到 100×50 什么都有;1978 年 8 月，DEC VT100 以 80×24 的规格、超过百万台的销量，连同 ANSI 转义序列一道，终结了这场尺寸战国（素材:materials/ch01_补充_80col.md）。请注意这里的考古学顺序:先是纸，再是模仿纸的玻璃，最后才是承认自己是玻璃的玻璃。

屏幕为什么要假装自己是纸?三个理由，一个比一个硬。第一，软件已经写好了:成千上万行针对电传打字机的程序，只会往串行线路上写字节流，只会期待 CR 与 LF 的语义;新终端若不肯扮演打印机，一个旧程序都跑不了。第二，操作系统已经定型:内核的线路规程替你攒行、回显、发信号，整套机制都建立在”输出设备是一台顺序打印的机器“这个前提上。第三，物理上可行:屏幕天然支持随机访问，想画哪里画哪里，模拟一台只会从左到右、自上而下打印的机器毫无难度。兼容是单向门:玻璃可以假装纸，纸永远无法假装玻璃。

于是屏幕继承了纸的全部契约。光标就是打印头，永远只往前走;回车把光标送回第 1 列，换行把画面向上卷一行——卷屏（scroll）这个词，就是纸卷的卷。行缓冲契约原样保留:内核照样一行一行地攒你的输入，行编辑照样只能编辑当前行——尽管玻璃明明可以擦掉任何位置的字符。上一节那个 Ctrl-S 冻屏，在玻璃上继续生效，理由荒诞而合理:打印机可以被命令停机，所以屏幕也必须可以被命令停机。而当程序需要打破纸的隐喻、在屏幕上任意定位光标时，它必须发送额外的转义序列——那是玻璃才有的特权，也是第二章的主角。

这场假装的规模超出了所有人的预期。1981 年的 IBM PC 把字符屏幕做成 80×25，比电传时代的标准多出一行，这一行之差沿用至今;今天你打开任何一个终端模拟器，它默认窗口的祖先谱系清晰可考:打孔卡定的宽，3270 与 VT100 定的高，Model 33 定的回车与换行，内核线路规程定的行缓冲。1984 年问世的 xterm，把”模拟一台终端“做成了 Unix 世界长盛不衰的软件品类——而它模拟的对象，自始至终是纸的隐喻。

我在 wsstunnel 的调试里真切摸到过”假装“的边界:远端容器里的 bash 经 pty 接上 WebSocket 之后，日志里的行尾全是 \r\n——因为 pty 的线路规程默认开着 ONLCR，把程序输出的 LF 翻译成 CRLF 再发出去，像极了一台尽职的电传机在替纸完成换行的两个动作。把 termios 里相应的开关关掉，输出才变回裸的 LF。那一刻我意识到，终端模拟器模拟的从来不是”屏幕“，而是”电传打字机连着打印机“的整套物理流程;我关掉的不是几个标志位，是一台并不存在的机器（素材:materials/raw_readline_pty.md）。

玻璃电传打字机，是本书”活着的考古层“这一总纲的第一处实证:屏幕没有取代打字机，只是加盖在打字机的行为模型之上;CRT 没有取代纸，只是学会了假装成纸。这场假装持续了半个世纪，直到今天，你的终端模拟器在每次启动时都会重新承诺一遍:我会表现得像一台顺从的打印机。这份承诺书有正式文本，叫 termios;它的签名，是以 ESC 开头的转义序列——那是下一章的故事。

补一句 VT 系列的家谱。VT100 的前身 VT52 还不会说 ANSI，说的是 DEC 自家的方言;VT100 改用 ANSI 转义序列，等于把公共标准装进自家机身，再靠百万台销量让标准成为事实（素材:materials/ch01_补充_80col.md）。从此终端市场进入方言井喷的时代:每家厂商在标准之外私加能力，程序要动用这些能力，就得先知道对面是哪块玻璃——这个“先问对面是谁”的难题，将由第三章的 termcap 与 terminfo 接手。

纸的隐喻连声音都一并继承。ASCII 控制字符里的 BEL（0x07），对应电传打字机上真实的铃铛——线路那头要提醒人注意，机器就敲一下铃;今天你的终端“叮”一声，是那颗铃铛的电子转世。而纸本身也没有消失:屏幕上的字会滚掉，纸上的不会，于是现代终端模拟器都有一条叫 scrollback 的回滚缓冲区——本质是给玻璃配了一卷虚拟的纸，让你往回翻看滚出屏幕的行。硬件死了，隐喻负责善后。

直到最近几年，一批打着“重新发明终端”旗号的新产品出现，它们的共同动作耐人寻味:第一步依然是接入一个 pty，继续履行“表现得像一台打印机”的古老承诺，然后才在壳外添加自己的图层。半个世纪的假装，已经从技术约束升格为入行仪式——你可以不继承纸的历史，但你绕不开纸的契约。顺带一提，全屏应用并非假装的终结者:vim、htop 这类程序只是更主动的表演者，用转义序列指挥玻璃，演完再把舞台还给打印机模式。

## 1.5 地层学笔记:物理约束如何变成文化基因

每一章的地层学笔记回答同一个问题:这一层给今天留下了什么。第 1 章的答案最重，因为它是所有上层地层的地基。本节只有一个主题:物理约束不会随物理消失——它们先变成默认值，再变成习惯，再变成礼法，最后变成我们眼中的”常识“。化石不在博物馆里，化石在你的日常里。

第一组化石与宽度有关。电子邮件格式标准 RFC 5322 规定每行不超过 78 字符——80 列减去 CR 与 LF 各一位，恰是电传打字机一行的余量;man 手册页与大量 README 至今按 80 列折行;Python 官方风格指南 PEP 8 把代码行宽限制在 79 字符——80 列减 1，给光标留一个身位;git 社区流行的提交说明规范，正文折行于 72 列——这个数字与 Model 33 每行 72 字符的物理宽度严丝合缝。我们不必声称 git 规范的作者在向电传机致敬，72 在这里多半是排版可读性的经验值;但一个经验值恰好落在一台 1963 年机器的物理宽度上，这种巧合本身就是地层连续性的显影（素材:materials/ch01_补充_80col.md）。

第二组化石与 CR/LF 有关。HTTP 与 SMTP 的协议文本至今要求以 CRLF 结束每一行——万维网与电子邮件的报文格式，仍在执行打字机的两步换行动作;git diff 里偶尔冒出的 ^M，是 CR 在向你招手;同一个仓库在 Windows 与 Linux 之间来回检出，换行符自动变形，于是有了 .gitattributes 里的 eol 约定与 dos2unix 这类专门工具。三种换行并存六十年，没有任何一方打算投降——因为每一种都是某个庞大生态的兼容契约（素材:materials/ch01_补充_80col.md）。

第三组化石刻在你的手上。cp、rm、ls 的两字母身材，是 Model 33 打字费力的直系后代;Ctrl-S 冻住终端，是电传机流量控制的幽灵;Ctrl-D 表示输入结束，可以一路追溯到纸带时代;甚至你在终端里感觉到的”流畅“，也部分归功于当年为每秒 10 个字符练出的输出节俭——在 110 波特的线路上，verbose 是奢侈，terse 是被速度训练出来的品味。肌肉记忆的寿命，比任何一款硬件都长（素材:materials/ch01_补充_teletype.md）。

把三组化石排成一条规律:约束的继承是三级火箭。第一级，物理约束产生技术决定——纸宽 80 列，屏幕便 80 列;第二级，技术决定沉淀为默认行为——80 列成了出厂设置;第三级，默认行为升格为文化规范——代码 79 字符、邮件 78 字符、提交说明 72 字符。三级火箭烧完，最初的物理理由已无人记得，但推力还在。这就是本书反复回到的观点:终端没有被现代化，只是被考古化;每一层都没有被替换，只是在上面加盖。

有人会问:这些遗产值得供着吗?为什么不趁早清场，设计一个不带历史包袱的现代终端?本章的历史已经给出了答案的一半:清场的成本不在新系统内部，而在它与所有旧系统的边界上。玻璃 TTY 若不假装成纸，成千上万行旧代码立刻作废;Unix 若不把终端当文件，管道与重定向的全部哲学都要重写。五十年不破坏兼容，才换来今天一切终端创新的舞台——你可以在任何一个现代终端模拟器里跑 1970 年代的程序，这种事在图形界面世界是不可想象的。兼容是互联网最被低估的美德，而这条美德的第一次大规模实践，就是把一台电传打字机供进了操作系统内核。

下一章，我们将看到同一逻辑的第二次上演:为了赋予玻璃玻璃才有的能力——随机定位光标、上色、擦除——人们发明了以 ESC 开头的转义序列;而转义序列一旦成为契约，就再没有人能推翻它。ESC[ 转义序列五十年的统治史，不过是 Model 33 统治史的续集。我们给五十岁的协议发新工牌，却从不敢没收它的工位——这就是终端世界的地层学，也是这本考古报告的全部方法论。

还有一组更隐蔽的化石:行，作为信息的最小单位。日志系统按行 grep，配置文件按行解析，HTTP 的报头以行为单位，readline 一次读一行——行，是纸的原子，被所有上层沿用至今。你的终端模拟器窗口被拉大时，COLUMNS 环境变量随之变化，而它的出厂值仍是 80;大批 CLI 工具在探测不到宽度时，兜底假设也是 80 列。这个数字已经不需要任何理由，它自己是自己的理由——这正是文化基因的定义:不再需要论证的默认值。

本章的三件展品，值得在离馆前最后看一眼:一台 Model 33，教会我们速度与沉默——短命令与简洁的输出;一张打孔卡，教会我们宽度——80 列;一块玻璃，教会我们假装——行为模型比硬件长寿。地层学笔记的方法论也在这三件展品里:当你遇到一条反直觉的约定，不要急着嘲笑它，先去找它的物理祖先，你多半会找到一台机器、一种介质、一次妥协。终端技术的每一层，都是某台已经停产的机器留下的活着遗嘱;把遗嘱读懂，才知道哪部分可以动、哪部分动不得。这是本书所有章节的共同读法，也是我们对“过时”一词的持续反驳:它们不是过时，只是在服役。

第四组化石最安静:名字。/dev/tty、/dev/pts、tty 命令、用 Ctrl-Alt-F 呼出的虚拟控制台——这些名字里都住着 Teletype 这家公司的注册商标;图形编辑器里那个内嵌终端，本质仍是一块新玻璃继续假扮旧纸。商标法的保护期早就过了，商标却比公司多活了几十年，还顺便活成了操作系统一个子系统的名字。命名是最便宜也最顽固的考古现场:机器可以报废，名字会自己找新的宿主。下一次你在终端里敲下 tty，不妨把它当成一次叩访地层——三个字母之下，压着电报网、打孔卡、字车、铃铛，和一整个机电时代。

---

## 第2章 ANSI 转义序列:统治五十年的隐秘语法

# 第 2 章 ANSI 转义序列:统治五十年的隐秘语法

如果只能挑一门语言来证明“兼容是最被低估的美德”，它不会是 COBOL，也不会是 SQL，而是一门连名字都常被念错的小语言：ANSI 转义序列。它没有运行时，没有安装包，没有版本协商，部署渠道是每一台终端——从 1978 年的 VT100 到你此刻正在用的终端模拟器，五十年来，它以 ESC[ 开头、以最终字节收尾，悄悄统治了人类与计算机对话的那一小块矩形玻璃。这一章我们解剖这门世界上部署量最大、废弃率最低的 DSL：它从哪里来，语法长什么样，标准如何三头并进，以及我为什么在 2020 年代还在亲手写它。

## 2.1 ESC 的诞生:从控制字符到控制序列

故事要从一个 byte 的极限讲起。1963 年，美国标准协会公布 ASCII，给 128 个码位排好座次，其中 33 个码位不对应任何可打印字形——前 32 个，加上末尾的 DEL——它们是控制字符：BEL（0x07）让电传打字机的铃铛真的响一声，CR（0x0D）让打印头回到行首，LF（0x0A）让纸卷上滚一行，BS（0x08）让打印头倒退一格。这是终端交互的第一代命令语言：一个控制字符一个字节，一个字节一个动作，没有参数，没有结构，也不需要参数——一台打字机要做的动作，本来就不超过一巴掌。

问题出在屏幕上。第 1 章我们看过，玻璃电传打字机（glass TTY）用阴极射线管替换了纸张，却保留了打字机的行为模型；但屏幕很快展示出纸不具备的能力：光标可以在整个画面上任意移动，任意位置的字符可以反显、加亮、擦除，整屏可以瞬间重画。这些动作在 33 个控制字符里找不到容身之处。给每个新动作分一个新码位？7 位编码的余量已经见底，何况每改一个码位，全世界的键盘、线路和软件都要重新对表。控制字符这门语言只有名词、没有语法：它列举得出原子动作，却表达不了参数——“把光标移到第 10 行第 20 列”这种带坐标的句子，一个字节无论如何装不下。

出路是转义。ESC（0x1B，十进制 27）在 ASCII 里的本职工作是换挡：它宣告“接下来的字节暂时改变含义”。1974 年，ANSI 的 X3.41 标准把这件事制度化——转义序列以 ESC 开头，后接若干字节，组成一条可以任意延长的命令。这是 7 位编码世界里最优雅的扩展机制：不占用任何已有码位，不改任何硬件，在协议之外再搭一层协议。我们不妨说，ESC 是终端世界的左括号——从此控制字符退化成单词，转义序列长成了句子，而句子，是可以带参数的。

这个设计还有一个深远后果，值得单独记一笔：控制信息与数据共用同一条通道，术语叫带内信令。它让 1970 年代的硬件省掉了独立的控制线路，却给未来埋下一根刺——任何一段数据流，只要里面恰好出现 0x1B 这个字节，就会被终端当成命令执行。二进制文件过不了终端，cat 一下图片文件屏幕就疯狂，根子都在这里。几十年里，终端工程师围绕“如何区分数据与控制”打满了补丁：二进制要先编码再过终端，控制序列自己要转义才能当数据用。带内控制是这门语言的基因，也是它一生的过敏源。

更麻烦的是，这门语言没有开小灶的余地。ASCII 的控制字符集中在 0x00 到 0x1F 的 C0 区，外加 127 号的 DEL；8 位字符集后来又在 0x80 到 0x9F 补出一排 C1 控制区。可两排加起来，能分的“单词”也就六十多个，还要留出一部分给传输与格式控制。屏幕想要的动词清单——定位、清屏、反显、滚动区域、键盘重定义——是按百计的。词汇量的鸿沟，靠加码位永远填不上；填上它的，是语法。

历史现场在 1974 年前后的 DEC。VT52 是玻璃 TTY 时代的名机，配了一套自己的私有控制语言：ESC A 让光标上移，ESC B 下移，ESC Y 行 列 把光标定位到指定坐标（行、列都以空格为原点），ESC J 清掉右下方的屏幕。它的序列没有参数分隔符，没有统一的收尾字节，每增加一个功能就多一个字母代号——好用，畅销，但它是 DEC 的方言，别的终端一个字也听不懂。为每种终端各写一份翻译表，正是第 3 章 terminfo 登场的伏笔。

另一个常被忽略的地层细节是键盘。1970 年代中期，Lear Siegler 的 ADM-3A 终端风靡一时，它把 ESC 键放在今天 Tab 键的位置，又在 H、J、K、L 四个键帽上刻了方向箭头——因为它没有独立的方向键。几年后，Bill Joy 在伯克利写 vi，光标移动键就顺理成章选了这四个自带箭头刻字的键。今天亿万开发者肌肉记忆里的 hjkl，不是深思熟虑的人体工学，而是一台终端的键盘布局。终端史里的许多“标准”，追到源头都只是一次将就的物理安排。

分水岭出现在 1978 年。两年前的 1976 年 9 月，欧洲计算机制造商协会（ECMA）发布 ECMA-48 第一版，与美国国家标准学会的 X3L2 委员会协作起草；美国的草案随后成形。1978 年 8 月，DEC 发布 VT100——这家以私有方言闻名的公司，做出了终端地层里最关键的一次决定：不发明新语言，直接说即将诞生的国家标准的话。控制序列以 ESC[ 两个字节开头，参数用分号分隔，功能由一个最终字节收尾。VT100 成为史上最畅销的终端之一，也让 ESC[ 这个尚未正式出版的语法，一夜之间有了几十万台硬件背书。等到 1979 年 ANSI X3.64 正式出版，它的处境不是“颁布”，而是“追认”。

VT100 的产品定义本身也值得一看：它没有追逐最强性能，而是把“标准的完整性”做成了卖点——一套指令覆盖光标定位、字符属性、滚动区域、键盘重定义，选配 Advanced Video 之后还能反显与下划线。DEC 又做了一件影响深远的事：公开完整文档，允许兼容厂商按图施工。于是 VT100 成了被克隆最多的终端，克隆大军反过来又把它的语法推向全球。把“兼容”第一次做成一门生意的，正是这台机器。

回头看，VT100 押对的不只是标准，还有“兼容”这个概念本身。此后四十余年，几乎每一台新终端、每一个终端模拟器上市时都要回答同一个问题：你和 VT100 像不像？答案直接决定你能运行多少现成软件。今天键盘上那颗 ESC 键，就是 27 号控制字符的活化石——它从打字机的换挡键，变成终端的命令前缀，再变成 vim 用户的肌肉记忆，三层含义叠在同一块键帽上，像三层地层。而字符串里八进制的 \033 与十六进制的 \x1b，是同一个字符在两种进制下的名字：十进制 27，八进制 033，十六进制 1B——三种写法，指向同一个五十年不倒的开关。

## 2.2 CSI 语法解剖:ESC[31m 只是冰山一角

先给语法。ECMA-48 把控制序列定义成一台小型语法机器：CSI P…P I…I F。CSI 是引导符（Control Sequence Introducer），7 位世界写作 ESC[，即 0x1B、0x5B 两个字节；8 位世界的 C1 控制区里还有一个等价的单字节 0x9B，今天几乎绝迹，但在标准里坐着一把交椅。P 是参数字节，取值 0x30 到 0x3F——数字 0 到 9、分号，外加 < = > ? 四个符号；I 是中间字节，取值 0x20 到 0x2F，用于给功能分组，实践中极少露面；F 是最终字节，取值 0x40 到 0x7E，一个字母或符号，宣告这条序列到底是干什么的。参数可以整体省略，缺省值由功能自行定义；多个参数用分号串联，顺序写死在标准里。

把 ESC[31m 放上解剖台数一数，一共五个字节：0x1B 宣告“接下来是命令”，0x5B 把它归入 CSI 家族，0x33 与 0x31 两个数字拼出参数 31，最后的 0x6D——字母 m——按下“字符属性”这个功能键。没有包头，没有校验，没有长度字段，五个字节说完一切。对比今天任何一个 RPC 协议动辄数十字节的开销，这门语言的节俭近乎苦行；而它换来的是，在 9600 波特的串口线上也能流畅地全屏重绘。1978 年的带宽账本，写进了语法的每一个字节。

最终字节是这门语言的“动词表”，摘录如下：

| 最终字节 | 功能 | 例句 |
| --- | --- | --- |
| A、B、C、D | 光标上、下、右、左移 n 格 | ESC[2A 上移两行 |
| H 与 f | 绝对定位，行在前、列在后，从 1 数起 | ESC[10;20H |
| J | 清屏：0 从光标到屏底、1 从屏顶到光标、2 整屏 | ESC[2J |
| K | 清行，三种范围同上 | ESC[K |
| m | SGR，字符属性 | ESC[31m 红色 |
| h 与 l | 设置与复位模式 | ESC[?25l 隐藏光标 |

一段最小演示，可以直接在你的终端里跑：

```bash
printf '\033[2J\033[H'                           # 清屏,光标送回左上角
printf '\033[31m错误\033[0m:配置文件不存在\n'      # 红色报错,随即复位
printf '\033[10;20H在第 10 行第 20 列落笔\n'       # 绝对定位,1 起算
printf '\033[1;4;36m加粗、下划线、青色\033[0m\n'   # 参数可以叠加
```

SGR（Select Graphic Rendition）是最终字节 m 的家族，也是今天曝光率最高的一支：0 复位一切，1 加粗，2 暗淡，3 斜体，4 下划线，7 反显，9 删除线；30 到 37 是八种前景色，90 到 97 是同色系的高亮版，40 到 47 与 100 到 107 如法炮制给背景。ESC[0m 是整门语言里最著名的句子：无论前面叠了多少属性，一条命令打回原形——所有着色程序的输出都以它收尾；忘了写它的人，会让半屏文字陪着报错一起变红。

SGR 的参数表里还藏着一个精巧的设计模式：几乎每个开属性都有对应的关属性——1 开加粗，22 关；4 开下划线，24 关；7 开反显，27 关；3 开斜体，23 关。开与关成对出现，样式就能局部叠加，不必动辄全局复位；0 号复位是核武器，22、24、27 是手术刀。今天 CSS 用 font-weight 与 text-decoration 这类“属性名加值”解决同一个问题，而 1976 年的答案只用了两排连续整数。语法俭省到吝啬，语义的完备性却一点不少——这是标准作者们的天赋。

颜色有自己的扩展史。1990 年代，16 色开始不够用；1999 年前后，xterm 把色板扩到 256 色：ESC[38;5;Nm 选中第 N 号前景色，背景换成 48;5;N。色板的编排不是随机的：0 到 15 沿用 16 色，16 到 231 是一座 6×6×6 的 RGB 立方体（索引 = 16 + 36r + 6g + b），232 到 255 是 24 级灰阶。再往后是真彩色：ESC[38;2;R;G;Bm 直接指定三个通道，每个通道 0 到 255，合计 1600 万色。ISO 8613-6 曾为真彩色规定过冒号分隔的书写形式，语法更严谨，但跑遍世界的却是分号写法——事实标准又一次压过文书标准，这笔账 2.3 接着算。

```bash
printf '\033[38;5;208m256 色板 208 号:橙\033[0m\n'
printf '\033[38;2;255;165;0m真彩色 rgb(255,165,0):同一种橙,更准\033[0m\n'
```

不过“只出不进”也并不绝对。标准里藏着一个最初的反向通道：设备状态报告，应用发出 ESC[6n 询问“光标在哪”，终端把当前行列号当作一次普通的键盘输入，从 stdin 灌回来。vim 这类程序至今用它探测终端——发一句询问，看对面回不回答、回答得像不像自己。这是终端世界最早的能力协商：应用提问，终端回答，答案走的是键盘的门。第 3 章 terminfo 的能力数据库，以及现代终端的种种查询协议，都是这条小路的拓宽版。

另一个值得点名的是备用屏幕缓冲。ESC[?1049h 让终端切换到一块独立画布，vim、htop、less 都在这里作业；退出时 ESC[?1049l 切回主屏，你 shell 里滚了半天的历史记录原封不动，全屏程序来过又像没来过。这个功能不在 ECMA-48 的正文里，是 DEC 的私加楼层，却成了今天的默认礼节：全屏程序不留痕，是终端用户与工具之间无言的契约。私用序列承担公共礼仪——这是“后门变高速公路”最日常的注脚。

冰山在水下的部分，比露出水面的大得多。SGR 只是 m 字节的一个家族；OSC（以 ESC] 开头）是另一支，负责窗口标题、剪贴板、进度上报这类“操作系统级”事务；此外还有光标形状、滚动区域、备用屏幕缓冲。但真正值得划重点的，是这门语言的整体气质：没有握手，没有版本协商，没有返回值，也不保证送达——应用把字节流推进 stdout，解析器在屏幕那头自行取舍，听懂的执行，听不懂的丢弃。解析器就是整个 API。也正因为它对对面只有一条要求——“住着一个能听懂大部分话的解析器”——它才能从串口线一路跑到浏览器，五十年不换台词。

还有一个工程真相，藏在“解析器就是整个 API”这句话背后：序列并不保证一次到齐。应用一次 write 的字节，经过管道、伪终端、WebSocket 的层层缓冲，完全可能把 ESC、[、3、1、m 拆成两半送达——解析器必须是一台带记忆的状态机，字节来了先存着，凑齐才执行。每一款终端模拟器的核心，都是这样一台状态机；每一款 TUI 框架的输出端，都默认对面是这么一台状态机。我们习以为常的“终端显示”，实际上是两台状态机隔着一条不可靠的字节河，跳了一支默契了五十年的双人舞。

## 2.3 标准的战争:ECMA-48、ANSI X3.64 与 ISO 6429

把标准谱系摊在桌面上，它比大多数软件的版本史还整齐：

| 年份 | 标准 | 身份 |
| --- | --- | --- |
| 1974 | ANSI X3.41 | 转义序列机制的总框架 |
| 1976 | ECMA-48 第一版 | 欧洲版本，与 ANSI 协作起草 |
| 1979 | ANSI X3.64 | 美国版本，与 ECMA-48 第二版实质等同 |
| 1981 | FIPS 86 | 美国联邦信息处理标准 |
| 1983 | ISO 6429 第一版 | 升格为国际标准 |
| 1991 | ECMA-48 第五版 | 现行文本 |
| 1992 | ISO/IEC 6429 | 与第五版对齐的国际标准 |

同一份技术文本，三块不同的牌子：日内瓦的 ECMA、纽约的 ANSI、国际标准化组织 ISO，先后各盖一次章。1979 年 8 月的 ECMA-48 第二版与 ANSI X3.64-1979 实质逐字相同，1983 年 ISO 6429 把它升格为国际标准，1991 年的第五版与 1992 年的 ISO/IEC 6429 再次合流。所以这场名为“标准的战争”的战役从头到尾没开过一枪：三大机构是联姻，不是混战；三十年间文本只做增量修订，核心语法从第一版起就没动过，也从来没有出现过第二个竞争标准。真正的战争发生在另一条战线上——标准文本与私有扩展之间。

顺带修正一个流行误会：ECMA-48 并不是“ANSI 颜色码标准”那么简单——它的正文定义了约一百七十项控制功能，从光标移动、字符集指派、行距字距到双倍高度行，远不止颜色与清屏；绝大多数终端至今只实现了其中一小角。换句话说，我们今天使用的不是 ECMA-48 的全部，而是它被五十年实战反复锤炼出的最小子集。一个标准能活五十年，恰恰因为它敢于定义得比实现多——多余的部分沉在水下，等后来的人需要时再浮上来。

ECMA-48 里埋着一个决定成败的细节：最终字节 0x70 到 0x7E 被显式保留给私用；参数区的 ? 前缀可以开启厂商私有模式。换句话说，标准亲手给厂商开了后门。后门很快变成高速公路：DEC 的 ESC[?25l 隐藏光标、ESC[?1049h 切换备用屏幕缓冲，被 1984 年问世的 xterm 一并继承；xterm 自己继续加盖——OSC 0 设置窗口标题，1999 年前后加入 256 色板，其后是鼠标上报协议、真彩色、超链接。xterm 从 1984 年服役到今天，它的 ctlseqs 文档比任何官方标准都厚，事实上成了终端扩展的“非官方标准部”。今天每一款新终端的文档都要补一句“兼容 xterm”，就像三十年前它们都要补一句“兼容 VT100”。

8 位形式 0x9B 的死亡则是一堂编码课。ECMA-48 为 C1 控制区设计了单字节 CSI，设想 8 位环境下序列更省一个字节；但 UTF-8 问世后，0x80 到 0x9F 这些字节在多字节序列里只作为后续字节出现，任何解析器都不敢再把一个裸的 0x9B 当成 CSI——除非它确定流不是 UTF-8。于是 7 位的 ESC[ 两字节形式赢下了全部现实世界，8 位形式只活在教科书里。这场胜负没有裁判：UTF-8 从侧翼路过，顺手终结了半个标准。标准的生死，有时候与终端产业毫无关系。

命名战争的余波同样值得看一眼。IBM PC 时代，DOS 的终端驱动叫 ANSI.SYS，要在 CONFIG.SYS 里挂载才能让程序输出彩色——“ANSI”这个美国国标的名字，就这样借 IBM 的销售渠道传遍千家万户；欧洲人起草的 ECMA-48、国际化的 ISO 6429 反而少有人念得出口。更有趣的是微软的迟到：Windows 的控制台长期不认这些序列，直到 2014 年前后才在 Windows 10 里补上虚拟终端处理层，2018 年推出 ConPTY 让老序列在新架构下继续服役，2019 年的 Windows Terminal 上市时，它的核心仍然是一个 VT100 语法的解析器。三十年里最热衷“推倒重来”的公司，最后也排队来交兼容税。

兼容税还有一个具体形状，叫测试成本。1980 年代起，终端与模拟器的兼容性有了专门的考试：vttest，一套专门给 VT100 家族出题的考卷，从光标移动到滚屏、从字符集到私用模式，逐项挑错。此后每一款新模拟器上市，都要先过一遍旧考卷；每一道做错的题，都是一次召回。新玩家总以为终端的门槛是性能，做下去才发现门槛是历史——性能可以用钱堆，历史只能一行一行赔。

工程考古到这里，值得立三块碑，告诉后来人去哪里核对原典：其一，ECMA-48 第五版的 PDF 在 ECMA 官网免费公开，是唯一权威且免费的总纲；其二，vt100.net 保存着 DEC 的原始手册，VT100 用户指南至今可读，读完你会明白“文档即规范”的年代标准是怎么写的；其三，xterm 的 ctlseqs，私用扩展的活体目录，终端工程师真正人手一份的案头书。三者叠起来，恰好是这个地层的地质报告——官修的、厂商的、社区的，三层沉积清晰可辨。

这一层教给我们三件事。第一，标准的胜出不靠完美，靠时机与采用——VT100 在标准正式出版前一年先行背书，硬件销量替草案投了票；第二，留白即生命线，私用区让每个厂商都能加盖，不必等委员会开会，协议因此内建了进化机制；第三，事实标准大于文书标准，xterm 的扩展没有一张 ISO 证书，却写进了每一台终端；ISO 8613-6 端正的冒号写法，输给了野路子的分号写法。标准赢的方式，是让所有人都以差不多的方式违规。

## 2.4 我的实战:手写转义序列的三个场景

我的三个场景都来自自己的项目：wsstunnel 的窗口同步与输出清洗、yuangs CLI 的行擦除重绘、launch-tui.sh 的终端准备。它们共同的背景是：这门语言五十年没换过台词，但每一行实战代码都要自己摸清边界——文档只告诉你序列长什么样，不告诉你它什么时候会背叛你。

场景一：窗口同步。wsstunnel 把远端 bash 的伪终端输出经 WebSocket 推给浏览器；浏览器窗口一变大小，前端发来一条形如 `__RESIZE:30,120` 的文本消息，我在这头把它翻译成内核调用：

```python
def _set_winsize(fd: int, rows: int, cols: int) -> None:
    """设置伪终端窗口大小。"""
    winsize = struct.pack("HHHH", rows, cols, 0, 0)
    fcntl.ioctl(fd, termios.TIOCSWINSZ, winsize)
```

四个无符号短整数按 C 结构体的布局打包，一条 ioctl 写进内核；内核随即向前台进程组广播 SIGWINCH，vim、htop 这些全屏程序这才知道“世界变宽了”，下一帧按新坐标系重画。窗口尺寸本身不是转义序列，却是所有转义序列的坐标系——行列号全部相对于它。我在这里踩过的坑是忘了同步初始尺寸：远端 bash 以为屏幕还是 80 列，前端窗口明明开着 200 列，ls 的输出全被拦腰折断。序列一个字节都没写错，错的是坐标系。另一处细节在消息入口：客户端往伪终端写入 `\x03\r`——Ctrl-C 加回车——先把远端 bash 输入缓冲里残留的半条命令清干净，再继续打字。两个最原始的控制字符，干了最现代的活：给一条穿越了 WebSocket 的会话，补一个“清场”动作。

场景一的另一半是输出清洗。浏览器里除了真终端视图，我还要喂一个纯文本视图，给审计日志与微信推送用；伪终端的原始输出里全是转义字节，得先剥干净：

```python
_ANSI_RE = re.compile(
    r"\x1b\[[0-9;]*[a-zA-Z]"      # CSI:参数字节与最终字节
    r"|\x1b\][^\x07]*\x07"        # OSC:窗口标题等,以 BEL 收尾
    r"|\x1b[()][AB012]"           # 字符集切换,如 ESC ( B
    r"|\x1b\[[?][0-9;]*[a-zA-Z]"  # DEC 私有模式:? 前缀
)
```

这段正则就是 2.2 那张语法表的代码化：参数、最终字节、OSC 家族、字符集切换、私有前缀，一个家族一条分支。测试里存着一条真实的彩色输出作样本：

```python
ansi_data = b"\x1b[01;32mfile.txt\x1b[0m\r\ndata\r\n"
# 剥离后,文本模式前端收到 "file.txt\r\ndata\r\n"
```

清洗只做一次，所有文本模式前端共享结果。这既算工程优化，也算对 1978 年语法的致敬——能被一行正则吃干净的协议，才养得起这样的偷懒。

有人会问：为什么不干脆把转义序列解析成结构化数据，还原出颜色与位置？我认真评估过这条路线，最后放弃了——因为解析等于再造一台终端模拟器。SGR 的颜色跨行持久，光标状态、滚动区域、字符集彼此纠缠，任何“轻量解析”都会在某个 htop 的输出上现出原形。剥离是唯一诚实的有损变换：我不要语义，只要文本。这也算这个地层留给后人的纪律——面对一门有状态的语言，要么完整实现它，要么完整放弃它，半吊子解析是最贵的选项。

场景二：yuangs CLI 的行擦除重绘。渲染器要把流式输出的 JSON 原文重写成 markdown，技法是教科书级的：回车回到行首，ESC[K 擦掉本行；ESC[A 上移一行，再擦；从最后一行擦回第一行，再整块重画：

```javascript
process.stdout.write('\r\x1b[K');              // 回到行首,擦掉当前行
for (let i = 0; i < lineCount - 1; i++) {
    process.stdout.write('\x1b[A\x1b[K');      // 上移一行,再擦
}
process.stdout.write(this.prefix + rendered + '\n');
```

第一次上线就撞上了边界：内容超过一屏时，顶部已经滚出视口，ESC[A 上移到顶就停，擦不到滚走的行，硬擦会在屏幕上留下擦不干净的鬼影。最终的方案加了一道判断——内容不超一屏才整块重绘，超了就老实换行收尾，保留已滚出的内容。这里还藏着第三个坑：ESC[A 数的是逻辑行，而一条超长的 JSON 会被终端软换行成好几个视觉行，擦除的步数必须按视觉行数重新计算，不能偷懒数换行符。这不是转义序列的缺陷，是它的物理本性：光标能在视口里跳舞，却跳不出滚动缓冲区，也分不清逻辑行与视觉行。场景二还有两道保险：输出不是 TTY（管道、CI 日志）时，着色与重绘路径整体旁路，退回纯文本；宽度取不到时回退 80 列——第 1 章那台打字机的幽灵，又一次按时上班。

wsstunnel 里还有一处与 OSC 有关的妥协，值得记录在案。我很想让服务端自动知道远端 bash 的当前目录——现代终端约定里有现成的句子：OSC 7，shell 每次打印提示符时向终端上报一次当前路径。但 bash 默认不发这条序列，要让它开口，得请用户改自己的提示符配置；对一个追求零配置可用的隧道工具来说，这个前提不成立。最后的方案很笨：客户端在输入流里嗅探 cd 命令，自己维护一份目录状态。用 1978 年的语言说完了所有漂亮话之后，工程上真正管用的，往往是这种一点就通的笨办法。

场景三：launch-tui.sh 的终端准备。这个脚本一行转义序列都不输出，却是我手写序列的工具链的前置条件：它以 set -euo pipefail 起手，检查 web 端口是否已被占用、管理 pid 文件、注入凭证、把 TUI 与 web 服务的会话根对齐，确保 TUI 每次都生在“真终端加就绪环境”里。它教我的事可以压成一句话：手写转义序列之前，先确认自己住在终端里——isTTY、TERM、窗口尺寸，三件事任何一件不成立，再漂亮的序列也只是发给虚空的字节。顺带一提，Python 生态里人尽皆知的 click 库，它的 clear 函数用的也是 1978 年的老句子：

```python
echo("\033[2J\033[1;1H", nl=False)   # 清屏,光标归位;出自 click 的 termui.py
```

五十年过去，“清屏加归位”还是这两句连用——最新框架与最老协议之间，常常只隔着一层薄薄的封装。

## 2.5 地层学笔记:为什么五十年无人推翻它

先摆一个容易被忽略的事实：以 ESC[ 开头的这门小语言，很可能是世界上部署量最大的 DSL——不是之一。它没有运行时，没有安装包，没有应用商店，部署渠道是每一台终端模拟器、每一条 ssh 会话、每一段 CI 日志、每一次 docker attach，部署量只能以十亿计。它同时是废弃率最低的 DSL:1978 年写下的 printf '\033[2J'，今天在任何一家操作系统的任何一款终端里照跑不误；与它同时代诞生的无数“先进”协议——各代 RPC 框架、各代消息总线、各代 GUI 工具包——坟头草已经换了几茬。

为什么五十年无人推翻它？三个原因咬合在一起，缺一不可。

第一是网络效应，而且比通常说的更物质。不只是“人会使用”构成网络，“已经写出来的字节流”也构成网络：五十年来全世界的脚本、日志、Man page、git diff、构建系统输出，本身就是一座用转义序列写成的语料库，而且规模只随时间膨胀。任何新语法想取而代之，先要让这座语料库整体失效——等于要求全世界的存量输出同时报废。没有竞争者付得起这笔账，所以每一代“终端革命”的最后，都变成了在旧语法上加新功能。

第二是测试语料的复利。新终端模拟器的第一张工牌从来不是性能榜，而是兼容性：vttest 这样的测试套件要过，vim、htop、tmux 这些金丝雀应用的逐格表现要对。每通过一轮验收，旧语法就往下多沉一层地基；每一次验收失败，都会被记作新模拟器的丑闻。兼容在这里不是美德宣言，是准入考试。

第三，也是我认为最根本的一条：协议内建了加盖机制。私用最终字节、? 前缀私有模式、OSC 的自由字符串，让扩展不需要任何委员会批准。kitty、iTerm2、Windows Terminal、Warp 各自在私用区加盖自己的楼层，没有一家需要先推翻一楼。一门允许私加词根的语言，永远不会因为词库不够而死亡。所谓兼容税，收的从来不是“不许变”的税，而是“新增请走旧语法”的税——税率低到没有人想逃。微软的故事是这条税则最好的注脚：连那家最热衷推倒重来的公司，最终也选择把 1978 年的语法装进 2018 年的新内核。

把它的同行拉出来比一比，更能看清位置：正则表达式、SQL、cron 表达式、Makefile，都是活了几十年的 DSL，但没有一个像它这样——每个使用现场都独占一块屏幕，每次敲击键盘都在生产新语料，而且五十年间从没有出现过“2.0 版本”。SQL 有 NoSQL 来搅局，正则有结构化数据来分流，而转义序列的继任者至今不敢宣布存在。一门语言的统治力，不看它的语法多精巧，看它的继任者在哪里。

第 8 章我们会正面迎接这个地层的现代形态：Ratatui 的双缓冲 diff 逐格比较，只把变化单元格对应的 ANSI 序列发给终端；Textual 把每行合成不可变的 Strip 做行级 diff；Ink 用 React Fiber 协调器决定哪几行需要重写。四个范式吵得不可开交，输出却惊人一致——全是 CSI。现代 TUI 框架本质上是一门老语言的编译器：它们不是掘墓人，是这门语言最高产的当代作者。第 9 章的 AI CLI 同理：Claude Code 在伪终端里跑命令、用颜色渲染 diff、靠 ANSI 重绘对话气泡，大模型脚下踩的，还是这台五十年前的打字机。

这段历史给我最大的提醒是：最好的标准，不是设计得最完备的，而是把“未定义”认真留给未来的。ECMA-48 的作者们在 1976 年留出了参数语法与私用区，等于替五十年后的 256 色、真彩色、鼠标、超链接预留了语法空位——他们不可能预见这些功能，但他们预见了自己看不见未来。层层加盖之所以可能，是因为第一层就没打算住满。相比之下，那些规定到每个字节的“完备”标准，大多死在了第二版之前。

所以，下一次当你的终端整屏变红、光标听话地跳到第 10 行第 20 列、橙色进度条以 256 色板第 208 号颜色往前爬时，不妨停半秒：你正在读的，是一门比 Linux 老、比万维网老的 DSL 的最新一期出版物。我们给五十岁的协议发新工牌，一发就是三十年——它一天都没有失业。

---

## 第3章 Terminfo 与 Termcap:终端能力数据库的四十年战争

# 第 3 章 Terminfo 与 Termcap：终端能力数据库的四十年战争

如果说第二章的 ANSI 转义序列统一的是终端世界的语法，那么本章的主角解决的是词汇：同一个意图，不同终端要用不同的字节序列去表达。把词汇差异做成一张可查询的表——这项发明名叫 termcap 与 terminfo。它是软件史上最早、也最成功的驱动程序模型，而围绕它的格式之争，足足打了四十年。

## 3.1 混乱的巴别塔：每个终端一种方言

第二章读起来容易让人产生一个错觉：终端世界似乎从来都归 ANSI 管辖。我们得先把时钟拨回去——ECMA-48 标准到 1976 年才定稿，而它被厂商们当作一纸参考文件的岁月还要漫长得多。在 1970 年代的绝大多数时间里，机房是一座货真价实的巴别塔：每一台终端都说着自己的方言，同一句“把光标移到第 10 行第 20 列”，换一台机器就要换一种说法。

方言为什么能多到这种地步？答案埋在地层深处。第一章说过，Teletype 电传打字机根本不需要光标——它只有打印头，只会打字、回车、换行，所以所有 Teletype 后裔共享一套极小的词汇表，并无分歧。玻璃 TTY 换上了屏幕，行为模型却还是打字机：新行从底部推上来，旧行向上滚走，你永远回不到上一行改一个字。真正的分水岭是带寻址光标的 CRT 终端：屏幕第一次变成一块可以随机访问的二维画布。而“如何访问画布”这个全新的问题，没有任何标准给出答案——厂商们各显神通，方言大爆炸由此开始。

何况这场爆炸的燃料十分充足。终端是外设，不是平台：没有哪家厂商有动力去兼容别家；市场高度碎片化，新型号一年一茬，采购按报价单做决定，用户机房里什么型号都有。更麻烦的是，各家终端的“智能程度”本身参差不齐——打字机型、纯滚动型、光标寻址型、图形存储管型，能力集合从根上就不同。方言的差异不只是“同一个意思有不同的说法”，很多时候是“你根本没有这个意思可说”：这台有反白，那台有下划线，第三台连光标都移不动。差异在两个维度上同时裂开，一个是词汇，一个是词汇量。

方言之间有多荒谬？我们看同一件事——把光标移到第 10 行第 20 列——在三种当年最流行的终端上的说法：

```
VT52       ESC Y 行列两字节   # 行、列各加 32 偏移后当普通字符发出
ADM-3A     ESC = 行字节 列字节 # 等号打头,后面跟行列
Hazeltine  ~ 加一串私有码      # 前导符根本不是 ESC,是波浪号
```

VT52 要求行列值加 32 之后当作普通字符发出去；ADM-3A 用等号打头；Hazeltine 最离谱——它压根不用 ESC，前导符是波浪号，大概是全世界唯一靠“~”说话的终端。而这还只是“光标移动”一项能力。清屏、下划线、反白、滚屏、功能键定义，每一项在三四十家厂商那里都有三四十种说法。第二章的 ANSI 统一了语法——控制序列长什么样；但每台终端的词汇表——哪个序列干什么活——依然各行其是。语法统一了，方言照旧。

这份方言名录有多长？我们数一数那些年喊得出名字的机型：Datapoint 3300 是玻璃 TTY 的先驱之一，HP 2640 系列带着一排谁也不一样的功能键，Tektronix 4010 系列干脆拿示波管当屏幕，ADDS、Soroc、Wyse 各占一个方言岛。每一家都坚信自己的设计更合理，每一家都给世界添了一种新说法。机房管理员的应对办法原始而有效：在墙上贴一张手抄对照表，写明哪台机器用哪些键干什么事，新来的程序员先背三天再上岗。那张纸，就是 termcap 之前的人肉数据库——词典尚未发明，词条全靠师徒相传。

应用软件的世界因此陷入组合爆炸。当年的全屏编辑器几乎都是为某一种终端而生的：康奈尔大学的 Fred 认死了自家机房里的几种型号，兰德公司的 Rand 编辑器绑定自家终端。想支持一种新终端，就得拿到源码、读懂它的输出代码、手工加一个分支再重新发版。编辑器数量乘以终端型号，每一格组合都是一次重复劳动；硬件在百花齐放，软件在到处救火。那个年代甚至没有“移植”这个词，只有“再写一遍”。更残酷的是，这种劳动毫无沉淀：为 A 编辑器适配过 Hazeltine 的经验，对 B 编辑器毫无价值，每个应用都在独自翻越同一座山。

1976 年前后，伯克利的研究生 Bill Joy 决定动手写自己的全屏编辑器，就是后来的 ex 与 vi。他面对的机房与别处并无不同：ADM-3A、Datamedia、Hazeltine，各说各话；远程用户拨号进来，带来的终端型号只会更杂。 vi 不可能像前辈们那样认死一种终端，它必须回答一个此前没人认真回答过的问题：在我运行的那一刻，我面前这台机器是谁？它会说哪些词？

答案后来成为软件史上最重要的设计转折之一：把差异从代码里搬出去，搬进数据。程序不再硬编码“这台终端怎么清屏”，而是去问一张表：清屏，怎么说？终端的特性从此有了一个沿用至今的名字——能力。能力分三类：布尔能力回答“有没有”，比如能不能在屏幕中间插一行；数字能力回答“多大”，比如几行几列；字符串能力回答“怎么说”，比如清屏该发哪串字节。终端型号作为查表键，由 TERM 环境变量携带进每一个进程；而那张表，就是下一节的主角——软件史上第一本终端方言词典。

这本词典的生命力，我在写作本章时亲手指过一遍：今天打开这台 Mac 的 /usr/share/terminfo——termcap 的直系后裔——七十四号十六进制目录里，tek4014、tab132 这些七〇年代终端的条目仍在册；七十六号目录里，vt100、v3220、viewpoint 与新终端比邻而居。巴别塔早就塌了，但塔里每一种语言的词条，都还收在这部词典的某一页上。而词典的第一版，出自 Bill Joy 之手。

## 3.2 termcap：Bill Joy 的文本数据库

历史现场在 1977 年 9 月的伯克利。Bill Joy 为他的终端问题写下了第一份答案，当时它叫 ttycap。今天我们还能在 ncurses 附带的 termcap 手册页里读到这段官方家谱：1978 年 3 月，ttycap 随 1BSD 发布——那是伯克利 Unix 的第一卷发行版，同一卷里躺着 ex 与它的全屏视图 vi。值得注意的是，那一天的四个函数名就已定型：tgetent 负责载入条目，tgetflag 问布尔能力，tgetnum 问数字能力，tgetstr 问字符串能力。四十多年过去，这四件套一个字都没改过。

这份代码的改名史，本身就是一部微缩 Unix 史。2BSD（1979 年 5 月）里它叫 termlib，加入了 tgoto——给参数化能力填入行号列号，以及 tputs——按终端的延迟要求把字符串送出去；3BSD（1979 年末）补上了手册页与测试程序；4BSD（1980 年 11 月）正式定名 termcap。此后它的接口冻结成化石级别：原封不动穿过 4.3BSD（1986 年 6 月），连 1994 年 6 月 4.4BSD-Lite 的大重构都没舍得动它。一套 API 服役近五十年而无需改版，原因只有一个——它抽象对了东西。查表这个动作，和表格用什么格式存，本来就是两件事；前者从此定终身，后者留给后人折腾。

termcap 的本体是单文件文本库，通常躺在 /etc/termcap。每个终端一个条目：第一行是名字段，主名、别名、人话描述用竖线隔开；随后是用冒号分隔的能力字段，能力码一律两个字母。我们看一份简化过的 vt100 条目：

```
vt100|vt100-am|dec vt100 (带高级视频选项):\
	:am:co#80:li#24:\
	:cm=\E[%i%d;%dH:cl=\E[2J:ho=\E[H:
```

第一行是三名一体的名字段；am 是布尔能力，声明这台终端到行尾会自动换行；co#80 与 li#24 是数字能力，80 列 24 行；cm 是字符串能力，cursor motion，光标移动的字节序列，其中 \E 是 ESC 的转义写法，%i 与 %d 标出了将来要填入行列的位置；cl 是清屏，ho 是光标归位。两个字母的缩写体系直到今天仍在服役——你在本章后文会一次次撞见 cm、cl、cup 这些词。

条目一多，词典自己长出了继承机制：tc= 字段。 vt100 的种种变种条目只需写自己与父条目的差异，末尾补一个 tc=vt100，其余字段在装载时从父条目自动补齐。用今天的话说，这是“差异继承”——面向对象语言的类继承、CSS 的层叠覆盖，思想上都是它的晚辈。差异永远比全量小，继承让词典的增长速度从组合级压回了线性级。它长这样：

```
vt102|dec vt102:tc=vt100:
```

一行即一条完整条目：vt102 的全部词汇，除声明自身之外，都从 vt100 处继承。当然，这份继承相当朴素——tc= 只认单个父条目，没有多继承，展开的先后次序还各有各的实现细节；后世无数配置系统踩过的坑，它当年一个不落地都踩过。但方向是对的：差异永远比全量小，把差异写成一行，是数据库对维护者的第一份体贴。

程序这一侧的用法干净利落：

```
char *cm, *area;
tgetent(area_buffer, getenv("TERM"));   /* 按查表键载入条目 */
cm = tgetstr("cm", &area);              /* 问表:光标移动怎么说 */
tputs(tgoto(cm, 20, 10), 1, putchar);   /* 填参数,输出字节串 */
```

编译完成的程序不再包含任何具体终端的知识——知识全在 /etc/termcap 里。换一台终端，零重新编译。在 1978 年，这套东西的优点朴素得动人：它是纯文本，人可以读，可以用 grep 追查，可以打印出来钉在机房墙上；它只有一个文件，拷贝即部署；它没有守护进程，没有配置服务，启动路径上没有任何魔法。这是一份可以随磁带寄往全世界、也能当着值班员的面朗读的数据库。

这里必须给查表键 TERM 单独记一笔。它由谁设置？拨号时代的 login 与 telnetd 替你填，今天的终端模拟器替你填——你面前坐着哪种终端，程序自己永远无从得知，必须由环境告诉它。TERM 的取值还发展出一套小型命名学：主名之外允许连字符后缀，标注同一终端的能力子集，vt100-am 表示自动换行型号，vt100-nam 表示不换行，后缀 rv 表示反白。查表键里携带家族信息，这个习惯一直活到今天——前文那台 Mac 的目录里就躺着 tab132-rv、tab132-w-rv 这样的条目，而 xterm-256color 这个名字本身就是同一个句式：主名 xterm，后缀 256color。一枚环境变量，半部命名学。

但纯文本的两条软肋，很快就在规模面前显形。第一条是性能：程序启动时要从头扫描整个文件，逐条目比对名字，直到撞见自己的查表键。条目上百之后，这份“人类可读”的文件成了程序眼里的长跑赛道——编辑器是一天开几十次的东西，每次启动都跑一趟全程，谁也受不了。第二条是表达力：文本字段很难表达“带运算的输出”。光标移动的参数填法，靠 tgoto 在库里硬编码几种已知模式去猜；各家对参数写法的理解开始漂移，解析器只好越写越宽容，而转义规则（冒号、反斜杠、续行符）坑洼密布，大文件里藏着几十年没人发现的手误。词典越长越厚，查词典的成本反而成了主要矛盾。为了抢救它，系统管理员各显神通：把常用条目挪到文件开头，把没人用的机型整段删除，/etc/termcap 成了每台机器手工修剪的盆栽——而修剪本身，又成了新的维护灾难。

termcap 证明了两件事：差异可以声明，查表可以统一 API。它同时留下两个悬而未决的问题：词典大了怎么查得快？能力多了、复杂了怎么描述得动？下一层地层来自贝尔实验室，答案带着一股编译器的味道。

## 3.3 terminfo：Mark Horton 的编译式升级

历史现场移到了新泽西。1981 至 1982 年，AT&T 贝尔实验室的工程师 Mark Horton——今天以 Mary Ann Horton 之名生活——着手正面解决 termcap 的两大软肋，新方案取名 terminfo，terminal information，终端信息库。与上一层的温和加盖不同，这是一次推倒重来：数据库格式、参数表达、访问 API，全部换新。

这段历史里有一枚独特的时代注脚。Horton 是 AT&T 雇员，按当时的雇佣政策，她写下的代码归公司所有——她不能把它公开发布。1982 年 7 月 10 日，她在网上张贴了 terminfo 完成的公告，并在当年的 USENIX 会议上宣布了这项工作。公告列出的优点，今天读来仍是一份教科书级的需求清单：编译好的数据库让程序启动飞快；描述源文件反而比 termcap 更可读；能力数量更多；参数机制被彻底泛化——词典从此能说“向前移动 35 格”这样的完整句子，而不必连喊 35 次单步口令；访问 API 也不再需要先复制再解析的笨拙手法。

API 的新意值得单独看一眼。termcap 四件套要求程序自备缓冲区、自己照看返回值，一次问答横跨多次调用；terminfo 把整套动作收成两步——setupterm 装载条目，tigetflag、tigetnum、tigetstr 三条问询各司其职。接口从手工作坊变成了柜台，这层改动不出彩，却决定了数据库能否被成千上万的程序放心使用。

于是历史开了一个玩笑：最先进的实现被锁进了公司保险柜，公众拿不到。补救来自一位志愿者——程序员 Pavel Curtis 写了公有领域实现 pcurses，随 4.2BSD（1982 年 12 月）发布，在 AT&T 之外的世界成了事实标准。Horton 本人在多年后公开确认过这段公案：他的版本是自由软件，成了 AT&T 之外所有地方的事实标准；而正主版本最终随 System V Release 4 与 Solaris 走进历史。正版被锁，盗火版通行——而后者，正是下一代 ncurses 的直系血脉。

正史的节点随后依次落定：terminfo 进入 UNIX System V Release 2（1984），很快成为 System V 世界描述终端的首选；System V Release 3（1987）带来重写的 curses 库，以及那个关键的编译器 tic——1986 年末问世，负责把人类可读的 terminfo 源文件编译成二进制数据库，安装到 /usr/lib/terminfo。至此战线分明：BSD 阵营抱着 termcap 文本库，System V 阵营换装 terminfo 二进制库，两边格式不同、API 不同、互不兼容。所谓四十年战争，打的正是这对双生标准的并行岁月。

战争的物质证据，我在这台 Mac 上亲手解剖了一件。file 命令的鉴定结论言简意赅：

```
$ file /usr/share/terminfo/78/xterm-256color
Compiled terminfo entry "xterm-256color"
$ xxd /usr/share/terminfo/78/xterm-256color | head -2
00000000: 1a01 2500 2600 0f00 9d01 c405 7874 6572  ..%.&.......xter
```

开头两个字节 1a 01，按小端读作 0x011A——terminfo 的魔数，八进制写法 0432，与 System V 时代完全一致。魔数之后紧排五个区段长度：名字区 37 字节、38 个布尔能力、15 个数字能力、413 个字符串偏移、1476 字节的字符串表。整份条目 3462 字节，程序装载时按偏移直接索引，零解析、零扫描——这就是“编译式”三个字的全部含义。顺带一提，目录名 78 是查表键首字母 x 的十六进制：当年没有好用的文件索引，编译器干脆把哈希桶铺成了文件系统的目录树，用路径当索引。

编译之前的源文件长什么样？infocmp 反编译出来的，正是 tic 吃进去的源格式：

```
xterm-256color|xterm with 256 colors,
	colors#256, cols#80, it#8, lines#24, pairs#32767,
	am, bce, ccc, km, mc5i, mir, msgr, npc, xenl,
	cup=\E[%i%p1%d;%p2%dH, bel=^G, cr=^M, cud1=^J,
```

三类能力一目了然：井号跟数字是数字能力，裸单词是布尔能力，等号跟字节串是字符串能力。源文件人类可读、可以 diff、可以进版本库；编译产物把它压成偏移表，专供机器快速索引。人类一份，机器一份，各读各的——源码与二进制的分工，和编程语言一模一样，只是这里的编译发生在 1980 年代初，产出的是一本词典。

顺带把二进制布局说全。布尔能力每项一字节，紧跟名字区；数字能力每项一个短整数，colors#256 与 pairs#32767 就住在这一区——32767 恰好是 16 位有符号整数的上限，这个数字本身就是格式年代的化石。字符串不直接存值，只存 413 个两字节偏移，指向那 1476 字节的字符串表，同一份字符串可被多处引用。1982 年为串行线路与几兆赫兹的机器设计的存储结构，今天从固态硬盘上读，一个字节都不用改。

terminfo 的灵魂是参数化字符串。本机的 terminfo 手册页开宗明义：需要参数的能力，用内嵌类 printf 转义的参数化字符串描述；参数机制由一台栈机驱动——%p 把参数推入栈，%d 把栈顶按十进制打印。我们用 infocmp 反编译本机 xterm-256color 的光标定位能力：

```
cup=\E[%i%p1%d;%p2%dH
```

翻译成人话：输出 ESC[；把第 1 个参数推栈打印——但先用 %i 给所有参数整体加一，因为终端的行列从 1 数起，而程序习惯从 0 数起；然后分号；再打印第 2 个参数；最后 H 收尾。实测一遍：

```
$ tput cup 10 20 | cat -v
^[[11;21H
```

10 和 20 进去，11 和 21 出来——0 基到 1 基的换算由 %i 在数据库内部完成。程序只表达意图，方言翻译发生在词典里，这是 termcap 时代定下的契约，terminfo 把它执行得更彻底。

这台栈机比看上去更强。手册记载它有字符常量、整数常量、求字符串长度、加减乘除与取模，乃至条件分支——这不是修辞。我反编译本机条目的真彩色定义能力 initc，里面赫然写着 %{255}%*%{1000}%/：把 0 到 255 的颜色分量乘以 255、除以 1000，换算成老式终端要求的千分刻度。运算逻辑写在数据文件里，由运行时库解释执行。今天的工程师管这叫“配置即代码”，terminfo 在 1982 年就这么干了。

还有一层更古老的幽灵，埋在字符串末尾。本机条目里有这么一段：

```
flash=\E[?5h$<100/>\E[?5l
```

flash 是可视响铃——用屏幕闪烁代替蜂鸣。$<100/> 的意思是：发完前半段之后，至少填充等待 100 毫秒。手册的 Delays and Padding 一节解释了动机：许多老终端（包括 VT100）不支持 XON/XOFF 流控，光标移动这类动作期间终端跟不上主机速度，必须塞入填充字符给它消化；xon 布尔能力可以声明“我有流控，免了填充”，pb 能力声明“低于某个波特率才需要填充”。词典不仅要描述“说什么”，还要描述“等多久”——第一章那台机械打字机的物理惯性，在这里以毫秒为单位留下最后一笔手迹。

性能靠编译，表达力靠栈机，物理兼容靠填充延迟，再加 tic 与 infocmp 这对编译反编译工具链——termcap 的两大软肋至此全部补齐。但战争下半场从来不是格式之争，而是阵地维护：谁来保证上千种终端的描述永远有人写、永远写得对？这一问，把下一节的主角请了出来。

## 3.4 数据库的活体维护：Thomas Dickey 与 ncurses 时代

1993 年，Zeyd Ben-Halim 与 Eric S. Raymond 以 pcurses 为底本重写了 curses，新库取名 ncurses——new curses。此后它交到 Thomas Dickey 手中，由他维护至今，版本号以日期为后缀推进：我在本机跑 infocmp -V，得到 ncurses 6.0.20150808，后半段就是发布日期。 ncurses 做了一件比写好库更了不起的事：它同时收编了两条战线——既讲 terminfo，也认 termcap。四十年战争的真正结局不是谁吃掉谁，而是新司令部把两本词典放上了同一个书架。Linux 世界从诞生之日起就内置 ncurses 与它的 terminfo 数据库，macOS 与各路 BSD 同样如此，本书第五章的主角，脚下踩的正是这一层。

“数据库”这个词容易让人想到出土即封存的文物，terminfo 恰恰相反——它是一座仍在生长的生物圈。我在本机数了一遍：/usr/share/terminfo 下有 42 个十六进制命名的子目录，从 31 一路排到 7a，共计 2684 个条目。每一种曾经存在过的终端，只要有人为它写过条目，它就还在册。

翻翻名录，能看到一整部二十世纪的终端与操作系统编年史：

```
74/tek4014    Tektronix 存储管终端,1970 年代工程师的画板
74/tandem6510 Tandem 小型机的终端
76/v3220      七〇年代 CRT 终端
68/hurd       GNU Hurd 控制台
62/beterm     BeOS 的终端——那个 1990 年代的理想主义系统
6b/kterm      日文 X 世界的 kterm
6c/linux      Linux 控制台自己
```

Tektronix 4014 与 BeOS 的终端睡在同一层地层里——这种跨界同居大约只有 terminfo 办得到：它可能是唯一一份同时登记过硬件终端、操作系统控制台与终端模拟器的数据库。每一枚条目都是一个物种的化石，而化石名录本身仍在增补。

增补的主战场在 ncurses 上游。全部条目收在一份随库发布的源文件总账里，新终端模拟器、新能力、老条目的勘误，都先落进这份总账，再由 tic 铺成各系统的目录树；Thomas Dickey 数十年如一日地收条目、修条目、答复报告，数据库的版本号也因此跟着库一起以日期推进。一本词典要活，靠的不是封存，而是收发室里常年有人值班。

维护这座活体的工具链三十年没换过名字：tic 负责把源条目编译入库，infocmp 负责把二进制条目反编译回源，toe 负责列出在册终端。实测一把：

```
$ toe | head -3
xterm-256color	xterm with 256 colors
xterm     	xterm terminal emulator (X Window System)
xterm-new 	modern xterm terminal emulator
$ infocmp -V
ncurses 6.0.20150808
```

toe 只报出三条，不是这台机器只认三种终端，而是我当前环境的搜索路径先指向了一本小词典——这正是本节我最想给你看的活体证据。这台 Mac 的环境变量里赫然写着：

```
TERMINFO_DIRS=/Applications/iTerm.app/Contents/Resources/terminfo:/usr/share/terminfo
```

终端模拟器 iTerm 把三个条目（xterm、xterm-256color、xterm-new）预先编译好，直接打进应用包，启动时再把自己的数据库目录插到搜索路径最前面。换句话说，一个 2020 年代的 GUI 应用发布新终端支持的方式，与 1984 年 System V 的约定完全一致：写源条目，tic 编译，放进十六进制目录树，登记进搜索路径。infocmp 反编译时也诚实地报出了出处：

```
#	Reconstructed via infocmp from file:
#	/Applications/iTerm.app/Contents/Resources/terminfo/78/xterm-256color
```

四十多年过去，新终端“登记入册”的仪式一步未改。登记的内容也在生长：infocmp 展开 xterm-256color，colors#256 声明 256 色，pairs#32767 声明颜色对上限，几十个 k 开头的键位能力把 Shift、Ctrl、Alt 组合键的编码逐一登记，initc 里还内嵌着上一节那个 RGB 换算小程序。条目像年轮，一层层能力按年代叠上去——二十一世纪的图形终端，仍在用 1982 年设计的字段格式说话。

说完词典，说查词典的人。我在写作本章的沙箱里跑 echo $TERM，得到的不是 xterm-256color，而是 dumb——一枚被刻意保留的活化石。dumb 条目短得像一句判词：

```
$ infocmp dumb
dumb|80-column dumb tty,
	am,
	cols#80,
	bel=^G, cr=^M, cud1=^J, ind=^J,
```

自动换行，80 列，会响铃，会回车，会向下走一行——第一章那台电传打字机的直系幽灵，四十多年后仍然只有这几句话，连“清屏”都说不出口。此时 tput cup 10 20 输出空串，tput colors 返回 -1：词典说你不会，你就什么都不是。失败模式同样由查表决定，这是驱动程序模型最铁面无私的一面。顺带一提，连“这台机器没你的登记”也是一种登记：条目缺失时，curses 应用会在启动时直接拒绝运行——宁可不开工，也不去猜。猜测才是兼容的大敌。而我把查表键换成 xterm-256color，同一台机器、同一个 tput 程序，世界立刻换了一种语言：

```
$ TERM=xterm-256color tput cup 10 20 | cat -v
^[[11;21H
$ TERM=xterm-256color tput setaf 1 | cat -v
^[[31m
$ TERM=xterm-256color tput clear | cat -v
^[[H^[[2J
```

程序表达意图，词典负责方言——1978 年定下的契约，今天仍在严格履行。

活体数据库也有活体才有的麻烦。最经典的当代事故：你 ssh 登上一台多年前的服务器，打开 htop，屏幕上砸出一句 Error opening terminal——远端机器的词典里，没有你本地查表键的那个词。解法古朴得可爱：把本机的 terminfo 条目文件拷过去，塞进远端的 ~/.terminfo 目录，搜索路径会自动收留它。知识不同步，系统就失聪——四十年战争的最新战场，已经从机房搬到了云上；而把两千多个条目一代代维护到今天的人，值得被写进正史。

老手们为此积累了一整套求生技艺。登老主机前先把查表键降级，TERM=vt100 几乎永远在册，代价是丢掉颜色与现代键位；本地调试时用 TERMINFO 变量把搜索路径整个换掉；再讲究一点的，随身带一份自己的 ~/.terminfo，走到哪把词典带到哪。这些技艺没有一条写进任何标准，却在全球运维圈的口耳相传里活了几十年——约定俗成，往往是比标准文档更强的标准。

## 3.5 地层学笔记：能力协商的现代回声

把本章压缩到只剩骨架，是四步：承认差异是常态；把差异声明成数据；程序运行时按查表键取用；对上层暴露统一 API。任何系统走完这四步，就是一套驱动程序模型——我们今天说硬件驱动、说字体回退、说编解码器探测，指的都是同一副骨架。按这个定义，termcap 在 1977 年就把驱动程序模型做了出来，做在了用户态，做给了一种当时没人看得上的外设。这是本章最想留在你脑子里的一句话：terminfo 与 termcap 是软件史上最早、也最成功的驱动程序模型，它比这个词成为大众词汇早了将近二十年。

它最字面的后代，是操作系统内核里的硬件驱动表：Linux 按 vendor ID 与 device ID 查表匹配 PCI 驱动，按描述符查表匹配 USB 驱动——查表键、能力表、统一接口，三位一体，与 terminfo 的结构逐字段同构。图形栈里 OpenGL 的扩展注册表、多媒体框架里的 codec 探测，都是同一思想的复刻：先问“你会什么”，再决定“怎么使唤你”。

Web 把这套思想变成了几代工程师的日常词汇：feature detection，特性检测。浏览器不会因为你自报家门是最新版本就相信你，JavaScript 要逐项探测能力；caniuse 之类的支持矩阵网站，就是全人类共建的 termcap；CSS 的 @supports 让样式表自己查表，库里社区维护的探测代码，正是当年 tgetflag 的直译。现代前端框架层层叠叠的兼容层，本质上就是给 DOM 与 CSSOM 各配了一本方言词典。

HTTP 把协商做成了协议本身：每个请求带着 Accept 头声明自己能接受哪些格式，服务器按声明说话；数据库驱动暴露元数据接口，让程序在运行时询问对端的方言支持到哪一步；CSS 字体栈里的回退列表，是一份排好序的能力检索清单。能力协商这件事，一旦开始寻找就会发现它无所不在——因为它不是某个领域的技巧，而是处理差异的通用形状。

再往远处望，MIME 类型表决定一个文件该用什么方式打开，/etc/services 登记端口与协议的对应，Unicode 的字符属性数据库告诉排版引擎每个码点是不是数字、能不能断行。它们与 terminfo 是同一个家族：把世界的不一致，登记成一张可查询的表，让上层代码只面对一致性。差异是数据，一致是 API——这个句式可以套用在今天绝大多数“跨平台”承诺上，而所有承诺的祖师爷都在 /usr/share/terminfo 里。

终端自己也在给老模型发新工牌。TERM 这枚查表键五十年没换过，但它早已不够用：256 色、真彩色、鼠标上报协议、花式下划线，这些新能力挤不进 1982 年的字段表。行业的做法不是推翻，而是加盖——我在这台机器的环境变量里看到 COLORTERM=truecolor 与 TERM_FEATURES 两枚新工牌，前者向程序宣告真彩色，后者是 iTerm 附加的能力位串；应用程序的惯例是先查 TERM，再逐个核对补充变量，逐级协商。 xterm 甚至提供了用转义序列反查能力条目的通道，让词典查询可以穿越 ssh 链路实时完成。每一项，都是 1978 年那句“你是谁，你会什么”的现代问法。新工牌从不撕旧工牌，这是终端世界不成文的宪法。

第八章的四重奏——Ratatui、Bubble Tea、Textual、Ink——无论范式多么新潮，启动时做的第一件事仍然是读 TERM、查词典、问颜色；它们的后端抽象，不过是把 1982 年的查表动作包进了新语言的外壳。第五章的 curses 与本章的 terminfo 在它们脚下原样服役，没有一行底层代码需要为新时代重写。地层学在此刻显出它最动人的形状：不是标本室，而是活地基。

作为地层，这一层教给今天三件事。其一，把差异做成数据，而不是代码——代码改不动，数据随时可以追加；2684 个条目里任何一个被修好，所有程序立刻受益，这是数据与代码分离能给出的最大红利。其二，格式可以升级，查表键不能换——termcap 到 terminfo，连格式带 API 全部换代成功，靠的是 TERM 这枚钥匙从未动摇；一切兼容工程的第一戒律，是别动钥匙。其三，让数据库活着——写实现的聪明人固然重要，但四十年如一日维护条目、回答邮件、收录新终端的人，才是词典真正的心脏； ncurses 活到今天，一半功劳属于实现，另一半属于那本永远有人续写的词典。

第九章我们会看到，AI 编码助手在接管终端之前，做的第一件事仍然是探测：你的终端是什么，支持多少色，能不能滚动，会不会吞掉转义序列。那套探测代码无论写得多现代，每一行都在替 1977 年伯克利机房里的某份文本文件传话。巴别塔从未建成，但词典让我们彼此听懂。这是 terminfo 与 termcap 留给全部软件的遗产，也是本书反复回扣的论点在硬件时代最清晰的印证：兼容是美德——而维护兼容的人，是美德的守夜人。

---

## 第4章 行编辑的搬家:从内核到 Readline

# 第 4 章 行编辑的搬家：从内核到 Readline

> 本章关键词：ICANON、n_tty、rl_prep_terminal、keymap、libedit。一句话论点：行编辑生在内核，长在库，老在兼容里——1989 年那次搬家至今没有结束，余震以 Ctrl+R 的形式，定期抵达你的指尖。

## 4.1 内核里的行编辑：n_tty 与 ICANON

1960 年代的终端是一台打字机，这是本章一切讨论的物理起点。Teletype 没有屏幕、没有显存、没有光标坐标——纸是唯一的状态。你敲下一个键，打印头立即落下；敲错了，墨已经渗进纤维，没有任何电子手段可以“撤回”。在这样的硬件上，“编辑”这个概念根本无处安放：终端自身不可能提供它，能提供它的只有离用户最近、又有计算能力的那一层软件。早期 Unix 里，那一层就是内核。

所以“行编辑”（line editing）这个词，最初不是一种功能命名，而是一次户口登记：它登记在内核 TTY 子系统的名下。我们今天打开 Linux 源码树的 drivers/tty/ 目录，还能看到这套三层结构原封不动地趴在原地：最上面是 tty core（tty_io.c 与 tty_ioctl.c），负责设备节点与数据流的公共管理；中间是线路规程（line discipline），默认实现叫 n_tty，这一层才是行编辑的真正执行者；最下面是 tty driver，面向具体硬件——串口 UART 是它，伪终端的 pty.c 也是它，第六章的主角此刻已经埋好了伏笔。

线路规程的规范模式（canonical mode）由 termios 里 c_lflag 的 ICANON 位控制。ICANON 置位时，n_tty 在接收路径上提供一组最小动词表：ERASE 删除行内前一个字符，传统绑定在 Ctrl-H 或 DEL；KILL 删除整行，通常是 Ctrl-U；WERASE 删除前一个单词，是 Ctrl-W；read() 系统调用要等行结束符才返回，行没敲完，进程就一直在睡；VEOF（Ctrl-D）在行首出现时让 read 返回 0——这就是我们在键盘上“键入 EOF”的来历；ISIG 置位时，Ctrl-C、Ctrl-Z、Ctrl-\ 不再是普通字符，而是被翻译成 SIGINT、SIGTSTP、SIGQUIT；ECHO 则让内核亲手把输入回显到屏幕。这些控制字符登记在 termios 的 c_cc 数组里，stty 命令可以随手改写——键盘语义在当时是可配置的，这件事本身就是时代的证词。

规范模式还有一块常被忽略的天花板：MAX_CANON。规范行的上限是 255 字节，内核读缓冲 N_TTY_BUF_SIZE 则是 4096 字节。255 这个数字带着浓重的 1970 年代口气——那是一个“一行大概不会超过一张穿孔卡片”的年代，没人想过有人会在终端里写散文。

我在本机的伪终端里跑了一次 stty，看到的正是这套规范模式的活体：

```
$ script -q /dev/null stty -a
speed 9600 baud; 0 rows; 0 columns;
lflags: icanon isig iexten echo echoe -echok echoke -echonl echoctl
iflags: -istrip icrnl -inlcr -igncr ixon -ixoff ixany imaxbel -iutf8
oflags: opost onlcr -oxtabs -onocr -onlret
cchars: discard = ^O; dsusp = ^Y; eof = ^D; eol = <undef>;
        erase = ^?; intr = ^C; kill = ^U; lnext = ^V;
        min = 1; quit = ^\; reprint = ^R; start = ^Q; status = ^T;
        stop = ^S; susp = ^Z; time = 0; werase = ^W;
```

BSD 风格的输出把 Linux 手册里的 ICANON 写成小写 icanon，ISIG 写成 isig，ECHO 写成 echo，而 echoe 正是 ERASE 的“抹除显示”选项——按一次退格，内核连屏幕上的字符一起替你擦掉。请顺便注意 icrnl：回车（CR）被翻译成换行（LF），这个翻译也发生在线路规程里，第一章那对纠缠百年的 CR 与 LF，就住在这条标志位上。

同一份输出的下半段，登记着这套动词表的键帽分配：eof 绑在 ^D，erase 绑在 ^?，kill 绑在 ^U，werase 绑在 ^W，intr 绑在 ^C，末尾还附赠 min = 1 与 time = 0——非规范模式那对通行证参数，此刻正以默认值待命。这份 cchars 名单，就是 c_cc 数组的人事档案：前文那串抽象的大写控制符，在每一台正在工作的终端上都有具体键帽与之对应。我们在此立下本书的一条方法论：凡是怀疑某层地层是否还活着，就去它的工作现场点名。stty 点名的结果是，1970 年代那支编辑队伍今天一个不缺，全部在编。

那么，为什么说内核只能做“一行”的编辑？因为内核没有屏幕，只有字节流。它不知道光标停在屏幕第几行第几列——屏幕坐标是终端与应用程序之间的事，内核只认字符序列。ERASE 只能删行尾字符，因为对一台打字机而言，已经打出来的字是物理事实，内核能做的只是把回退与抹除的指令原样转发；它不可能支持“把光标移到行中间插入一个字符”这种动词，除非它开始假装自己知道一块根本不属于自己的屏幕。规范模式的终点是一次 read() 整行交付：行编辑的一切痕迹在换行符出现的瞬间被抹平，进程拿到的永远只是成品。内核是流水线，不是工作台。

这套动词表的生理构造也值得看一眼。ERASE 抹除一个字符时，内核向终端发回的不是什么清除指令，而是“退格、空格、再退格”的三连击——先把光标挪回去，再用空格把字盖掉，再退回来待命。这套笨拙的手法是为纸张设计的：纸上的墨擦不掉，只能盖住。玻璃终端完全可以发明真正的删除指令，但没有人发明，因为所有应用已经依赖“盖住”这个语义生活了十几年。地层学在这里露出它最固执的一面：底层早已迁去屏幕，上层的语义还住在纸上。

要打破这个模型，第一步是把 ICANON 关掉。非规范模式下，n_tty 不再编辑、不再按行缓冲、也可以不再回显，read() 的返回时机改由 VMIN 与 VTIME 两个参数控制——VMIN 是“至少读到几个字节才返回”，VTIME 是“最多等多久”。而 cfmakeraw 这个便捷函数，能把 ICANON、ECHO、ISIG 等一整组标志一次性清干净，一步进入裸模式。请记住这扇门：1989 年搬走行编辑的那个库，就是从这里进去的；第五章 curses 与第六章一切 PTY 故事，也都从这扇门进。

VMIN 与 VTIME 的组合值得单独一提，因为它们是用户态接管时代的通行证。VMIN 大于零、VTIME 为零，是攒够若干字节再交付的批量模式；VMIN 为零、VTIME 大于零，是定时器式的等待；两者皆零，读立即返回，用于非阻塞探测；而本机这份输出里的 min = 1、time = 0，意味着每按一个键立即交付一个字节——这正是行编辑库想要的形态，也解释了为什么行编辑库接管终端的第一件事总是改写这两个值。内核并不反对这次离家出走，它只是把门留着，顺手把 ICANON 那盏灯一直亮着。

## 4.2 搬家的契机：为什么行编辑必须离开内核

把 4.1 节那组动词摆成一张表，你会发现规范模式的全部表达力只有四个半动作：删一个字符、删一个词、删一行、结束本行，外加半个——回显。不能左移光标，不能在行中间插入，不能调出上一条命令，更不会有补全。为什么？三个原因，一个比一个硬。

第一，内核不知道应用语义。WERASE 删除“前一个单词”，可“单词”的边界因应用而异：shell 里空格分隔，编辑器里还有标点与括号，数据库客户端里也许是逗号，内核不可能为每个应用的方言各备一本词典。第二，历史必须跨进程存活。上一条命令是上一个进程敲下的，而内核的行缓冲随 read() 交付而清空，没有任何机制把它留给下一个进程——历史的产权天然属于用户态。第三，也是最根本的一条：Unix 内核的传统是策略中立。字节流是机制，编辑体验是策略；把策略塞进内核，等于把一种意见写成法律。

但用户的手指不会等内核修宪。1970 年代末，屏幕终端普及，方向键长上了键盘；玻璃打字机们保持着打字机的行为模型，人却已经开始想动光标了。第一批离开规范模式的，正是最需要编辑的那批程序：全屏编辑器 Emacs 在用户态自己接管终端；1983 年，David Korn 的 ksh 把 vi 与 emacs 两套行编辑直接做进 shell 本体；前后脚，tcsh 给 csh 加上了 Tenex 风格的补全。行编辑离开内核的尝试早已四处冒火，只是每一处都要重新发明一遍——那时没有库，只有各家自建的小作坊。

这段前史值得多看两眼，因为它说明搬家并非 readline 的独创，而是 readline 把一场自发运动整编成了正规军。ksh 的 vi 模式让用户按 ESC 进入命令态，用 h 与 l 左右移动光标，把整座 vi 的肌肉记忆压缩进一行；emacs 模式则把 Ctrl-A、Ctrl-E、Ctrl-K 分别交给行首、行尾与删至行尾。tcsh 让 Tab 有了第二份工作——不再只是缩进，而是询问。这些实现各自精彩，也各自孤独：键位互不相认，历史格式互不相通，换个 shell 就得重学一遍手指。散装行编辑的时代缺的不是技术，而是一份所有应用都肯签字的公共契约。

把这个局面终结的是 GNU 项目。1987 年，Brian Fox 开始为 FSF 编写自由软件世界的 shell；1989 年，bash 首次发布，而它内部那套行编辑代码很快有了自己的户口。Bash 1.02 的 ChangeLog 里留有一条直接证据：

```
New directory: LIB contains readline and history stuff,
and is Make-able on its own.
```

readline 出生时是 bash 源码树里的一个 LIB 目录，随后被拆出去单独抚养：1990 年起由 Chet Ramey 接手维护，一养就是三十多年；1994 年，readline 2.0 首次作为独立 GNU 软件包分发。刚出生就被同行引用的另一个证据来自竞品：zsh 1.0（1990 年）直接借用了 bash 的 readline 代码，直到 1991 年的 2.0 才换成自己的编辑器——一个库刚满月就被对手拿去用，这在任何时代都是罕见的出生证明。我们把这条时间线钉在这里：1979 年，规范模式在第七版 Unix 里定形；1983 年，ksh 与 tcsh 在各自进程里私设行编辑；1989 年，readline 随 bash 诞生；1994 年，readline 独立成库。政变用了十五年，谈判桌换了四张。

那么，为什么搬家必须发生在用户态，而不是给内核升级一套更好的编辑器？答案还是那三条，只是此刻换成了正面的说法。演进速度：内核的版本周期以年计，编辑体验的迭代以月计——历史搜索、补全、宏，每一项都要快速试错；库可以发版，内核不能。通用性：bash 需要，gdb 需要，ftp 需要，Python 解释器也需要；一处实现、处处链接，这正是库的定义。以及那条最容易被忽略的——控制权。行编辑是用户与程序对话的最前线，把它放在用户态，等于承认每个应用都有权决定自己的对话规则。这个决定有多深远，要到第九章 AI 把对话循环搬进终端时才完全显形：连 AI CLI 的输入框，都默认继承这套编辑规则。

还有一层动机容易被略过：FSF 要的不只是一个好用的 shell，而是一套人人可自由使用、修改、再分发的软件栈。行编辑在 1980 年代是各家 shell 的私产——ksh 起初是专有软件，csh 的授权也另有考量——GNU 需要一个把编辑、历史、补全装进同一间屋子的自由库，让 gdb、ftp 与未来的每个交互程序都不必从零再造一遍输入。于是 readline 从出生第一天起就被规划为公共设施：它不是 bash 的附庸，而是 bash 递交给整个用户态的一份提案。这场政变与商业并购最大的不同在于，政变者上台后颁布的第一条法令，是开放。

我们再从用户的视角掂量这次搬家的分量。搬家之前，行编辑是终端的方言：ksh 用户与 csh 用户无法交流手指，换一台机器、换一个 shell，手感推倒重来；搬家之后，行编辑成了普通话——一套动词、一套历史语义、一个 Tab 的约定，跨 shell、跨程序、跨机器通用。1989 年之后学终端的一代人已经无法想象另一个世界：他们以为 Ctrl+A 是终端的天性，不知道那是某个库在某年某月的一次立法。把方言升格为普通话，才是库政变真正改写的东西；政变最深的痕迹从来不在宫墙之上，而在每个人的手指里。

政变成功的标志不是炮声，而是遗忘。今天几乎没有终端用户知道行编辑曾在内核上班；n_tty 还在岗，但它服务的前台已经悄悄换成了 cat、login 与一切没有链接 readline 的程序。所以每一次 Ctrl+R 都不是怀旧，而是那次搬家的余震——震源在 1989 年，震中在你的指尖。

## 4.3 readline 解剖：rl_prep_terminal 与两种 keymap

readline 干的第一件事，是把 4.1 节末尾那扇门推开。接管仪式的标准名称叫 rl_prep_terminal：先用 tcgetattr 把当前 termios 完整存档，然后清掉 ICANON 与 ECHO——注意是“近似裸模式”而非一刀切，信号处理通常保留，否则 Ctrl-C 会失去杀伤力；接着把 VMIN 设为 1、VTIME 设为 0，让 read 变成“来一个字节交一个字节”的逐字读取。从此键盘上的每个键帽都失去了内核代理，以字节形态直接抵达 readline 的编辑循环。

有接管就有还政。rl_deprep_terminal 负责把存档的 termios 原样恢复，而恢复的时机是一门手艺：执行外部命令前必须回到规范模式，否则 ls 们会看到一块被 readline 弄乱的终端。bash 在你按下回车、命令真正执行前的那一瞬间，总是先还政于内核——你从没注意到 ls 的输出曾被谁弄乱过，恰恰说明这套交接仪式三十年没失过手。

还有一层结构常被低估：readline 并不完全依赖 terminfo。为了在史前终端与最小环境里也能工作，它自带了一套终端能力管理的简化机制，在需要处才与 termcap、terminfo 会合。这是一种务实的双重国籍——库要搬家，就得自带家具；凡是把自己的生存寄托在别家地基上的库，都要回答同一个问题：目标环境比预期更穷的时候怎么办？readline 的答案是自带一个最小可用层，把优雅降级做成了默认行为。这与其说是实现技巧，不如说是一种被历史反复验证的生存策略，后来的每一代终端库都继承了这份谨慎。

readline 的第二重解剖结构是键位表（keymap）。它内置两大派系：emacs 与 vi，由 editing-mode 一锤定音。我在本机 bash 里跑 bind -v，读到的是 emacs 派当值：

```
$ bind -v | grep -E "editing-mode|completion"
set editing-mode emacs
set completion-ignore-case off
set completion-query-items 100
set page-completions on
```

而 bind -p 打出的键位表，是 readline 至今活着的直接证据：

```
"\C-g": abort
"\C-j": accept-line
"\C-m": accept-line
"\C-b": backward-char
"\e[D": backward-char
"\C-h": backward-delete-char
"\C-?": backward-delete-char
"\eb": backward-word
```

这张表值得逐行读。\C-b 把 Ctrl-B 绑到 backward-char；紧随其后的 \e[D 把左方向键的 ESC 序列也绑到同一个函数——终端方言这个第三章的老毛病，readline 在自己肚子里又消化了一遍：同一个动词，对应键盘方言的若干别名。再看中间两行：C-j 与 C-m，也就是换行与回车，都只是 accept-line 的普通绑定。这两行有哲学含量：在 readline 的世界里没有任何键是神圣的，连回车都只是键位表里的一行配置。编辑界面从“内核的物理规定”变成了“一张可改写的表”——这是搬家之后最直接的收获，也是今天一切可定制输入法与键位方案的合法祖先。

顺带记一条我在写作环境里的实地观察：当 bash 的标准输入没有接在终端上时，bind 会警告“line editing not enabled”，但 bind -p 照样把整张键表打印给你。键表是数据，编辑是行为；行为需要一块真终端，数据却随进程到处走。户口册比人活得久，这句话在软件里同样成立。

vi 派的存在提醒我们另一种世界观。emacs 模式里，编辑是一条从头画到尾的单行道，组合键随时待命；vi 模式里，一行之内有两个王朝——按 ESC 进入命令王朝，h、j、k、l 成为光标的手脚，i 与 a 负责打回插入王朝。同一个库把两套互相冲突的肌肉记忆都合法化，靠的就是键位表整体切换：换王朝，不换宫殿。而键位表本身也可以续写：用户在 inputrc 里写一行映射，bash 里用 bind 现场改写，bind -x 甚至能把某个组合键直接绑给一条外部命令。再配上宏——把一串按键录制成一个名字，一次触发，整段重放——readline 的行编辑就不是一组散兵游勇，而是一支带编制的部队。

历史是 readline 的第二件嫁妆。编辑缓冲之外，readline 维护一条历史链表，会话结束写入历史文件，下次会话读回。在此之上是历史展开：!! 重放上一条命令，!$ 取上一条的最后一段，^旧^新 做一次快速替换。还有那记重拳——Ctrl+R 的增量反向搜索：键入即搜，边搜边显，命中即回放。今天“按上键调出历史”“按 Ctrl+R 找那条删掉的命令”的肌肉记忆，根系全在这里；第九章我们会看到，AI CLI 要进入终端，同样要从历史与补全这两条协议过境。

Ctrl+R 的机制也值得一提，因为它把“搜索”这个现代交互动词第一次带进了终端：按键后进入增量搜索态，每敲一个字符，历史链表就反向过滤一次，命中即回显，再按一次跳向更早的命中，按 ESC 或回车接受。注意这套交互没有动用任何新发明——它全部由既有的键位表与既有一行显示完成，连“搜索态”也只是 keymap 换了张当前表。一个新功能若能完全用旧机制表达，它就天然免于兼容性审查——这是 readline 三十年不破的直接原因，也是它最值得今天的框架作者抄走的一页设计笔记。

第三件嫁妆是补全。Tab 触发，readline 调用一个由应用注册的候选生成函数——rl_completion_entry_function，应用由此把“我知道哪些合法答案”注入行编辑；候选的展示、分页与列宽由一组 completion-* 变量控制，本机 bind -v 里那一排 completion-ignore-case、completion-query-items 就是它们。再往上一层，可编程补全把候选生成权下放给 shell 函数，git 与 systemctl 的智能补全都由此而来。“编辑器出题、应用答题”这条协议，是 readline 最深远的设计：它让行编辑第一次成为可扩展的公共设施，也为三十年后的语义补全留好了插座。

本机 bind -v 里那排 completion-* 变量，是这套协议的体验细目：completion-ignore-case 决定补全是否宽容大小写；completion-query-items 决定候选多到多少时先问一句“是否全部显示”；page-completions 决定长列表是否翻页；completion-display-width 决定候选列的对齐宽度。连“候选太多”这种尴尬时刻都有正式编制处理，可见补全在 readline 的世界里不是事后贴上去的贴纸，而是与键位表同龄的原住民。从 bash 的可编程补全到 AI 的语义补全，三十年间所有新意都发生在协议答题的一侧，出题的一侧纹丝未动。

最后看一眼它的 API 之小，小到令人尊敬：

```
#include <readline/readline.h>
#include <readline/history.h>
char *line = readline("guest@shell> ");
add_history(line);
```

一行取行，一行入库。五十年基建，API 面就这么宽——抽象对了东西的库，从来不靠面积取胜。

## 4.4 libedit 与兼容的烟火气

readline 是自由软件阵营里姿态最强硬的库之一：它刻意选择 GPL，而非 GNU 家族库更常用的 LGPL。LGPL 允许专有软件以链接方式使用库，GPL 不行——链接 readline 并分发的程序，整个程序必须以 GPL 兼容许可证发布。这是 FSF 的公开策略，不是历史误会；一份库的许可证，本身就是一份立场声明。代价很快显形：1992 年，Common Lisp 实现 CLISP 因为用了 readline，被要求整个转为 GPL，项目一度陷入分裂。而墙不止存在，还会加高：readline 到 2.0（1994 年）用 GPL-1，2.1（1997 年）到 5.2（2006 年）是 GPL-2，6.0（2009 年）起改为 GPL-3——这一改，把那些仅兼容 GPL-2 的项目挡在了新版本之外。用一个行编辑库，居然要先研究许可证的编年史，这在软件史上并不多见。

我们不妨把许可证也当地层来读。GPL 的选择不是 readline 的一时兴起，而是 FSF 整个战略的组成部分：让最常用的基础设施去做传教士，任何想链接它并分发的专有软件，都得在牌桌上亮出自己的许可证底牌。理解了这一点，也就理解了 libedit 为什么偏偏诞生在 BSD 阵营——那是一个把“随便你怎么用”写进许可证文本的世界，与 copyleft 的世界互为对照组。两块大陆之间没有战争，只有一座桥；桥的名字叫 API 兼容层，桥的规格书叫 readline.h。桥上不收通行费，但过桥的车辆必须符合桥的规格。

有墙就有门。BSD 阵营递进来的梯子叫 libedit：前身是 4.4BSD 时代的 editline，由 NetBSD 的 Christos Zoulas 长期维护，BSD 许可证，随便链、随便改、随便商用。libedit 的杀手锏不是功能超越，而是兼容：它提供一个 readline.h 兼容头文件，针对 GNU readline 写的程序往往只需改一行编译选项就能换马。于是生态形成了稳定分工——FreeBSD 与 NetBSD 默认启用非 GPL 的 readline 模拟；MariaDB 与 PHP 在编译期提供 GNU readline 与 libedit 二选一；Python 的 readline 模块同样可以链接其中任何一个，从而不让整个解释器背负 GPL 义务；Debian 甚至专门提供 libeditreadline-dev 包，包描述直言 readline 6 改约 GPL-3 之后仅兼容 GPL-2 的程序已无退路，editline 就是那条退路本身。兼容层在这里扮演的角色，与其说是技术组件，不如说是外交豁免。

但 API 兼容从来不是行为兼容。libedit 的原生 API 是 el_gets、el_parse 一族，readline 接口只是架在上面的一层垫片；历史展开的细节差异、键绑定名的出入、边缘行为的不一致，让“换一个实现”这件事永远带着烟火气。macOS 用户最有体感：系统自带的 readline 实为 libedit，Python 若链着它，交互式历史搜索的某些手感就与 Linux 上不同——“同一条命令，Mac 的 Python 按键不对劲”是圈内流传多年的小悬案，病根就在这层兼容的接缝里（此为圈内常识，具体症状随版本各异，我们只作现象记录）。构建系统里那些探测 readline 还是 libedit 的宏，本质上是两套许可证之间的外交文书。还有一个单向阀值得单独记牢：BSD 许可的代码可以并入 GPL 项目，反之不行——宽松可以流向强制，强制无法退回宽松。这决定了两个阵营三十年间只有单向的人口流动。

兼容层真正的成本藏在行为差异的缝合处。历史展开的边界条件在两个实现里并不完全一致；同一个绑定名，在一边是内建动词，在另一边要靠垫片翻译一次；el_gets 返回之后对终端状态的清理顺序，也与 GNU 实现有微妙出入。这些差异单看都无伤大雅，叠起来就成了用户体验里那种说不清道不明的“手感不对”。于是软件工程里多了一种独特的工种：在两个实现之间维护等价性的人。他们不生产新功能，只消灭惊讶——而在终端这个最讲先例的世界里，消灭惊讶恰恰是最古老的美德。

这段恩怨的考古学价值，在于它证明了一件事：接口比实现活得久。当两种立场敌对、许可证互斥的实现共同实现同一个 API 时，这个 API 就脱离双方，自立为地层——readline 从此不再只是一个库的名字，而是一套行为契约的名字：编辑动词、历史链、补全协议、宏机制。这与第三章 terminfo 的故事同构：政治解决不了的分歧，由接口出面调解；方言解决不了的分歧，由词典出面调解。所谓“兼容的烟火气”，是地层学的日常景象：上面的房子照盖，下面的暗火不灭——而恰恰是这股不灭的烟火，证明这一层至今活着。

## 4.5 我的实战：yuangs CLI 的交互输入设计

最后轮到我这层薄薄的新地层。yuangs 是我用 TypeScript 写的命令行工具，跑在 Node.js 上；它没有链接 GNU readline——Node 进程里也确实链不上——它用的是 Node 标准库的 readline 模块。这不是遗憾，而是本章论点最好的注脚：1989 年从内核搬出的行编辑，今天在每一种语言里都被重新实现了一遍，Node 的 readline 就是第三代翻版。打开 cli.ts，import 区就是一张 readline 概念的遗产清单：

```typescript
import { getAllCommands, getCommandSubcommands, getCommandDescription,
    installBashCompletion, installZshCompletion, complete, setProgramInstance }
    from './core/completion';
import { getMacros, saveMacro, runMacro } from './core/macros';
import { getCommandHistory } from './utils/history';
import { confirm } from './utils/confirm';
```

历史、宏、补全、确认门——readline 发明的四件套一件不缺，只是各自长成了模块，散落在 src 目录里。

交互输入的第一课，藏在 confirm 的三种模式里。utils/confirm.ts 的逻辑分三岔：环境变量 YUANGS_NO_CONFIRM 置位时自动放行，服务 CI 与自动化脚本；stdin 不是 TTY 时自动放行并打一行灰色警告——管道里的程序等不来一个永远不会出现的回车，必须立刻放行，宁可放错，不可挂死；只有确认自己活在交互终端里，才弹出行编辑提问，以黄色提示等待 y/N。创建 readline 接口时，有一行注释值得抄进任何教材：

```typescript
const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout,
    terminal: false, // 禁用终端特性以避免与外部 readline 接口冲突
});
```

terminal 一路关掉的是内层的回显与编辑特性——yuangs 常被跑在 tmux、ssh 与各类 PTY 包装器里，外层已经有人在做行编辑与回显，内层再开一套就会出现双重回显与按键丢失。这行注释是拿真金白银的 bug 换来的：谁接管终端，谁负责恢复；没接管的，老实排队。

从治理的视角看，confirm 这扇门是 yuangs 人机分工的枢纽。危险动作——改文件、跑外部命令、动仓库——必须过这道确认门，门楣上写着默认 NO；而三种模式让同一条规则在三个世界里自洽：交互终端里人说了算，CI 里策略说了算，管道里契约说了算。这也是第九章主题在本书前半段的第一次亮相：当 AI 或自动化拿到执行权，确认门就是权限的物理形态。行编辑管的是人怎么把命令敲进来，确认门管的是命令凭什么能出去——一进一出，正好是终端治理的两道关卡，而它们脚下的地基，一块叫行规程，一块叫退出码。

第二课是历史与补全的产品化。utils/history.ts 把命令史存进 ~/.yuangs_cmd_history.json：每次记录去重、上限一千条——应用自己管自己的历史，不指望 shell 替它记账。补全走协议路线：installBashCompletion 与 installZshCompletion 把补全脚本装进用户的 shell；用户敲 Tab 时，shell 把光标处的词经 complete 入口传回 yuangs，resolver 再从命令树里算出候选。core/completion/path.ts 里的 resolvePathSuggestions 则是路径补全的最小实现——readdirSync 读目录、按前缀过滤、目录补斜杠，十行上下，却正是 rl_completion_entry_function 在 Node 世界的直系后代。至于 core/git/InteractiveReview.ts 里那套追问循环（评审之后继续追问修复方案），是对“提示符即 UI”最朴素的践行：每一次提问，都是一次微型的交互设计。

这套设计的妙处在分工：yuangs 不必自带行编辑界面，就能坐享用户 shell 里现成的编辑能力——bash 的 Ctrl+R 依然可用，因为补全发生在答题一侧，不动出题者的一根手指。安装即集成，协议即接口，这与 readline 当年向应用开放补全入口是同一场交易的续签：三十年前，是编辑器邀请应用来答题；三十年后，是应用把答题机器人派驻进编辑器。

把这些年踩过的坑收敛起来，我的自研 REPL 五原则如下。其一，TTY 是契约：process.stdin.isTTY 为假时必须给出非交互通路，自动放行好过永久挂起。其二，不与外层 readline 抢终端：能交给终端行规程的交给行规程，能交给 shell 补全协议的交给 shell，内层只做必须内层做的事，并且用完即还。其三，历史与补全是产品资产：自己存、自定义格式、经安装协议进入用户的 shell，而不是等用户背熟你的每一根旗标。其四，肌肉记忆具有法律地位：Ctrl+A、Ctrl+E 属于默认供给，谁缺席谁违规；Ctrl+R 这类高阶动词若不实现，要在文档里声明你实现到了哪一层——用户可以接受功能少，不能接受静默降级。其五，危险动作默认 NO：确认门的默认值必须落在安全一侧，逃生门必须显式命名。五条背后是同一个态度：提示符是产品的脸，而脸的肌肉记忆是几十年养成的，别自作主张整容。

至于提示符即 UI，我在 yuangs 里得到的教训具体而微。提示符必须与正文有视觉区隔，让人一眼认出“该我了”；默认值必须安全，y/N 的大写 N 要永远待在括号里的安全一侧；提问文案必须把代价写清楚，而不是用一句“确定吗”把思考责任推回给用户。这些细节没有一条会出现在功能清单里，却决定了一个工具像不像可以托付的同事。终端的美学是复利：每一次微小的体贴都会被手指记住，每一次微小的惊吓也会。

### 地层学笔记

这场搬家的余震，值得在章末清点一次。n_tty 还在内核里上班，只是业务变了：它不再服务交互前线，而是服务 cat、login 与一切没有链接行编辑库的程序——内核那间行编辑办公室没有关门，改成了档案馆，而且档案馆每天开门。readline 的真正遗产不是那几万行 C 代码，而是一份行为契约：一组编辑动词、一条历史链、一个补全协议、一套宏机制；它胜利的方式，是被每一种语言重新实现一遍——node:readline、Python 的绑定、Rust 的 rustyline、Go 的各家 liner——库以接口的形式赢得了战争。所以，下一次你在任意 shell 里按下 Ctrl+R，请记得那不只是在搜索一条历史，那是 1989 年政变的余震，穿过三十多年的地层，抵达你的指尖。内核守住 ICANON，1979 年的程序今天还能跑；readline 守住 emacs 与 vi 两种 keymap，1989 年的手指今天还好使。没有一层死掉，每层都是新一层的地基——全书想说的那句话，在行编辑这一层，长的是这个样子。

---

## 第5章 Curses 与全屏:一门前科学的诞生

# 第 5 章 Curses 与全屏:一门前科学的诞生

> 本章地层:curses/ncurses（1980 至今，虚拟屏幕与最小更新）|上承:第 3 章 terminfo 方言词典、第 4 章行编辑搬家|下启:第 6 章 PTY、第 8 章 TUI 四重奏

## 5.1 全屏的问题:移动光标太贵了

在前四章的地层剖面上，我们已经看清了三件事:TTY 的物理躯体来自打字机，ANSI 转义序列给屏幕装上了“可寻址”的语法，terminfo 把几百种终端方言编成了词典。但是请把视线从“一个字符怎么走”抬起来，看整个屏幕——你会发现一个悬而未决的大问题:行编辑只管一行，那全屏怎么办?谁能在 24 行 80 列的玻璃上，把字符画到任何一个格子里，还不把串口逼疯?

这个问题在 1970 年代末是一个真实的物理问题，不是一个工程洁癖问题。我们要算一笔账。当时的终端通过串口连主机，常见的速率是 9600 波特;按每字符 10 位（1 起始位、8 数据位、1 停止位）计算，每秒大约只能送出 960 个字节。一整屏是 24 行乘 80 列，即 1920 个字符位;哪怕只算字符本身，把整屏重画一遍也要两秒钟。两秒钟是什么概念?足够一个玩家在 Rogue 的地牢里被一只怪物追上两步。而如果只更新玩家移动的那一个格子，所需字节是十位数——上千倍的差距。所以“全屏程序”能否成立，取决于一个看似琐碎的能力:能不能只把变化的部分送出去。我们把这个问题称为全屏的原罪——移动光标太贵了，贵到必须有一门科学来管。

这里还有一个更深的物理约束，常被忽略:终端不会向你汇报它显示了什么。VT100 的屏幕是一块单向的玻璃，主机可以往里写字符，却读不出玻璃上现在有什么。也就是说，任何“最小更新”的算法都无法向终端本身求证，只能在软件里自己记一本账:上一帧我发了什么，屏幕现在就应该是什么。这本账，是后面一切故事的地基。

历史现场到了。1980 年前后，在加州大学伯克利分校，一位叫 Ken Arnold 的学生写成了这样一套 C 例程库:它让程序可以“光标寻址”——把一个字符放到屏幕的指定位置。Rogue 的共同作者 Glenn Wichman 在《A Brief History of Rogue》里回忆道，当年伯克利有个叫 Ken Arnold 的学生，“把一套允许程序做光标寻址的例程打包成库……虽然没有真正的图形，但你已经可以用字母、数字和符号去模拟画面了。这套例程库叫作 curses”。这个名字是一记标准的伯克利式冷幽默:它是 cursor optimization（光标优化）的缩写，同时也是英语里“诅咒”的意思——每个在串口上跟光标搏斗过的程序员，都懂这份双重含义。

curses 并非凭空而来。它的血统可以直接追溯到 Bill Joy 的 vi:Joy 在写 vi 时发明了 termcap 能力数据库（那是第三章的主角），并写下了 vi 的整屏更新逻辑;Joy 与 Arnold 一起把这些 termcap 函数从 vi 里拆出来变成独立库，Arnold 又把 vi 的全屏更新代码（插删行、插删字符的部分除外）提炼、泛化，做成了可复用的 curses。换句话说，curses 不是发明了“优化输出”，而是把 vi 里那个最难的、只服务于一台编辑器的算法，解放成了所有程序共用的地基。这是 Unix 世界里一次教科书式的“从应用到基础设施”的搬家——与第四章 readline 把行编辑从内核搬回用户态，恰好是同一个时代精神的两条路径。

而让 curses 名扬天下的，是一款游戏。1980 年，圣克鲁兹加州大学的 Michael Toy 与 Glenn Wichman 拿到了这套流通中的库，Toy 提议用它做一个“每玩一次就重新生成一座地牢”的冒险游戏，Wichman 为它起名 Rogue。Toy 后来转学到伯克利，与 Arnold 会合，Rogue 从此由两人继续开发。1982 年夏天，Toy 和 Arnold 在波士顿 USENIX 会议上宣读了论文《Rogue:Where It Has Been, Why It Was There, And Why It Shouldn't Have Been There In The First Place》;1983 年 8 月，4.2BSD 发行，Rogue 随发行磁带一起出厂。Wichman 写道:“BSD 的 4.2 版收录了 Rogue——忽然之间，全世界的大学计算机上都有了这个游戏。”游戏是钩子，curses 是钩子上挂着的货:全世界的机房在一夜之间发现，原来写全屏程序可以不关心终端是谁。此后三年，Rogue 成了大学校园最流行的游戏，而 curses 顺流而下，成了 Unix 上全屏程序的标准答案。

这笔传播学的账，值得再翻两页。第一个细节是“闭源的善意悖论”:Toy 与 Arnold 在 USENIX 论文之后拒绝分享 Rogue 的源码，这激起了 MIT 一群人的斗志——Jay Fenlason 等人受那场报告启发，决定自己动手写一个同类游戏，这就是 Hack，后来 NetHack 的直系祖先。一部作品因为闭源而催生了另一部作品，地牢爬行游戏的家谱就这样被一笔“不给看”续写了几十年。第二个细节是作者的去向:Arnold 在 1985 年从伯克利拿到计算机科学学士学位后加入 Sun Microsystems，后来成为 Java 平台早期的核心人物之一，与 James Gosling 合著了《The Java Programming Language》。写出 curses 的人去做了 Java，这并非改行，而是同一条路线的延长线——curses 本来就是软件史上最早的“跨平台层”之一，只不过它跨越的不是处理器架构或操作系统，而是几百种终端的方言。第三个细节是时代背景:在 curses 之前，vi 与 emacs 已经证明了全屏编辑是可能的，但每一个全屏程序都在各写各的光标移动代码，优化技巧散落在无数份私有的源码里，像手工作坊里不外传的模具。curses 的历史功绩不在于发明了这些技巧——其中最难的部分来自 vi——而在于把它们从“每家各扫门前雪”的手艺，变成了全体程序共享的公共设施。从这一天起，写全屏程序的人才第一次可以假装世界上只有一种终端。

下面这两段伪代码，是“全屏原罪”最直观的证词。第一段是暴力重画——玻璃 TTY 时代的下意识动作:

```text
# 暴力重画:全屏刷一遍
输出 clear=\E[H\E[2J          # 7 字节,光标归位加清屏
逐行输出 24 行 × 80 列        # 1920 字节
合计约 1927 字节               # 9600bps 串口上约 2 秒
```

第二段是 curses 视角下的同一个世界——Rogue 的玩家向左走了一格，其余 1919 个字符位纹丝不动:

```text
# curses 最小更新:只动一个格子
^H                            # 1 字节,光标左移一格
@                             # 1 字节,重写玩家符号
合计 2 字节                    # 约毫秒级,肉眼即"瞬间"
```

两秒对两毫秒，这就是 curses 存在的全部理由。请注意，这里没有任何一个字节是“聪明”出来的——聪明的是分层:程序只声明“世界变成了什么样”，翻译成“最少字节流”的脏活累活全部交给库。库的名字起得像个玩笑，干的却是编译器的活:它是历史上第一个面向显示的编译器，源语言是“逻辑屏幕状态”，目标语言是“某台具体终端的字节序列”，而它的优化器，我们留到 5.3 再解剖。ncurses 五十年不衰的第一性原理，此刻已经埋下:它抽象的从来不是终端，而是“人与全屏交互的最小公倍数”。

## 5.2 虚拟屏幕的形而上学:WINDOW 与两阶段刷新

如果只用一句话概括 curses 的哲学，那就是:程序描述“应该是什么”，终端呈现“实际是什么”，而在两者之间，必须竖起一面镜子。curses 把这面镜子拆成了三块屏幕，这是本章机制解剖的核心，也是它五十年后仍被 Rust 与 Go 社区照抄的蓝图。

第一块屏幕叫 WINDOW。它是库在你的进程内存里开出的一张二维字符矩阵，每个格子是带属性的字符单元（字符本身加上颜色、粗体等修饰）。程序对屏幕的一切想象——画边框、写文字、改颜色——都只发生在这张内存矩阵上，一动也不动到真实终端。initscr() 建立会话时，库会送你一块全屏大小的默认 WINDOW，名字叫 stdscr;需要更多时，newwin() 可以开出独立的窗口，subwin() 与 derwin() 可以开出子窗口——子窗口的精妙在于它不复制字符存储，而是与父窗口共享同一批单元，像同一份地籍册上的两块责任田。还有一位更狂想的成员叫 pad:它是一块“比屏幕大得多”的虚拟屏，可以是任意宽高，从不整体落地，只在你调用 prefresh 时把某个可视矩形贴到屏幕上。Rogue 那座比显示器大得多的地牢、后来一切滚动视窗的祖先，都住在这个 pad 里。虚拟屏幕的“形而上学”四个字并非修辞:WINDOW 是理念世界，终端是现象世界，程序是柏拉图，库是那个负责把理念投影到现象上、还要投影得又快又省的匠人。

第二块与第三块屏幕藏在库的内部，构成著名的两阶段刷新。第一阶段，wnoutrefresh(WINDOW):把一块或几块 WINDOW 的内容做投影与合成，抄写到库内的“虚拟屏幕” newscr 上——注意这一步只改内存，一个字节都不发给终端。第二阶段，doupdate():让 newscr 与 curscr 相互对照。curscr 是第三块屏幕，它是“物理屏幕当前内容的软件副本”——还记得 5.1 里那块单向玻璃吗?终端不会告诉你它显示着什么，所以库必须自己记账:curscr 记录的就是“我上次发出去的最后一帧”。doupdate 逐格比较 newscr 与 curscr，把差异翻译成一条经 terminfo 优化的最小字节流，发给终端;发完之后，curscr 被更新为新帧，账本合拢。于是任何一次画面更新，在 curses 内部的完整旅程都是:WINDOW（你的逻辑）→ newscr（合成后的下一帧）→ curscr（上一帧的账）→ 终端（玻璃）。最短的 C 程序可以证明这个分层的存在:

```c
#include <curses.h>

int main(void) {
    initscr();                    /* 建立 stdscr 与内部三屏 */
    box(stdscr, 0, 0);            /* 画边框:只写在内存矩阵里 */
    mvwaddstr(stdscr, 1, 2, "hello, curses");
    wnoutrefresh(stdscr);         /* 第一阶段:合成到 newscr,不发字节 */
    doupdate();                   /* 第二阶段:对照 curscr,输出最小差异 */
    getch();                      /* 等一个键 */
    endwin();                     /* 拆除会话,恢复终端 */
    return 0;
}
```

为什么一定要拆成两个阶段，而不是一个“画完即刷”?第一层原因是原子性。真实程序的画面是多个窗口合成的:htop 的表头、进程列表、底部的功能键提示条，是三块 WINDOW 贴在同一块玻璃上。如果每块窗口各自直接驱动终端，用户就会看到画面被一块一块“补”出来，中间态全部裸奔——这在串口时代是两秒的尴尬，在今天是掉帧。两阶段刷新保证:先在 newscr 上把所有窗口合成完毕，再一次性对比、一次性输出，玻璃上只会出现“上一帧”与“下一帧”，永远没有中间态。第二层原因是性能批处理。合成阶段把分散在多块 WINDOW 里的变化归拢成一帧，优化阶段才有机会做全局决策——这一行要整体重写还是插删?这两段光标移动谁更便宜?全局信息只有在一帧齐备时才存在。我们可以说，wnoutrefresh 是“提交”，doupdate 是“编译并发布”，两次提交之间，你可以摆弄任意多块窗口。

curscr 这个名字值得单独凭吊。它就是软件史早期最朴素的双缓冲:一块给眼睛看的玻璃，一块给程序记账的影子。这个思想后来有了许多学名——shadow buffer、帧缓冲、离屏渲染、双缓冲 diff——但骨架从未变过:凡是“输出介质昂贵且不可回读”的地方，就必须在软件里保留一份上一帧的副本。1980 年的 curses 在 24 乘 80 的字符矩阵上押中了这条公理，2010 年代的浏览器合成器与 2020 年代的 Ratatui 只是换了几种口音把它再念一遍。这也是我在自己的项目里反复复用的思想:凡是往窄通道里推帧的程序，我做的第一件事永远是开一块“上一帧”的内存账本——因为通道两端，永远隔着一层你读不回来的玻璃。

顺着账本的隐喻，还能看清几个工程细节的必然性。newscr 与 curscr 的命名就直白得像账房先生的自报家门:new screen（下一帧的工作台）与 current screen（玻璃现在的样子），两本账一新一旧，doupdate 做的就是新旧对账。既然账本是“猜”出来的，就有猜错的时候——屏幕尺寸变了、备用屏切换了、信号把程序挂起又恢复了，账面与玻璃对不上怎么办?curses 的答案是允许你掀桌重来:clearok() 声明下次刷新强制整屏重画，把 curscr 重新初始化为空白；而 refresh 在怀疑账目时也会主动重放。这不是什么高深的容错理论，只是记账人的朴素美德:账对不上，就重新盘点。输入侧则有一个对称的小兄弟:功能键方言。xterm 的上箭头发 \EOA，另一些终端发 \E[A，同样的“向上”却有着不同的字节拼法。keypad() 打开后，curses 查询 terminfo 里每种功能键的词条，把杂多的字节序列翻译成统一的 KEY_UP、KEY_DOWN 之类的逻辑键名——这正是 terminfo 的“能力协商”思想在输入侧的复刻。输出的 curscr 与输入的 keymap 合在一起，构成了 curses 世界观的完整闭环:凡是玻璃两端不确定的东西，都要在软件里立一份账；账立起来了，程序才有资格假装世界是简单的。

## 5.3 优化输出:terminfo 驱动的最小更新

现在我们打开 curses 的机房，看那台“面向显示的编译器”怎么编译。它的优化问题可以严格陈述为一道字节经济学题:已知一张价目表（terminfo），上面每种操作都标着字节价格;已知上一帧（curscr）与下一帧（newscr）的逐格差异;求一条总花费最小的输出序列，使玻璃从上一帧变成下一帧。程序不需要懂这道题，库替它解——而价目表，正是第三章那位主角 terminfo。

先看价目表的真实长相。下面是在我这台机器（一台 Arm 架构的 Mac mini）上用 infocmp 取出的 xterm-256color 条目的关键能力，原样引用:

```console
$ infocmp xterm-256color | grep -E "cup|el=|ed=|dl=|ich=|dch=|smcup|rmcup"
	cup=\E[%i%p1%d;%p2%dH, ed=\E[J, el=\E[K,
	dl=\E[%p1%dM, dch=\E[%p1%dP, ich=\E[%p1%d@,
	smcup=\E[?1049h, rmcup=\E[?1049l,
```

这张表里每一个条目都是一种操作及其单价:el=\E[K,3 字节，擦到行尾;ed=\E[J,3 字节，擦到屏尾;cup=\E[%i%p1%d;%p2%dH，按参数位数约 8 字节，光标绝对定位;dl=\E[%p1%dM，滚动删除一行;ich 与 dch，插入与删除字符。所谓优化，本质就是在这些单价之间做选择。几个经典法则值得逐条咀嚼。法则一:能不移动就不移动。扫描一行时，如果下一个字符恰好接在当前光标位置之后，直接书写即可，移动费用为零。法则二:短途回退用退格。光标只需左移一两格时，一个 ^H（1 字节）比一次 cup（约 8 字节）便宜八倍;若新内容恰是旧行向后延伸的部分，连退格都省了。法则三:长途移动用 cup，短途重复用相对移动。法则四:区域性变化优先考虑插删。如果一行文字中间删了一个字符，可以发一次 dch 把右侧字符整体左移，代价 3 字节上下;如果整页内容都要上滚一行，发一次滚动序列远比重画 1920 格便宜。法则五，也是最容易被现代程序员忽略的一条:以上所有决策都依赖价目表——在一条不认识 dl 序列的终端上，“删一行”的最优解就是笨拙的重画;价目表越贫瘠，编译出的字节流越保守。这就是“terminfo 驱动”四个字的分量。

坐标细节也有戏。注意 cup 模板里的 %i:它把参数自增一。我在本机用 tput 实测，让 curses 的祖先告诉你第 10 行第 20 列怎么表示:

```console
$ TERM=xterm-256color tput cup 10 20 | od -c
0000000  033   [   1   1   ;   2   1   H
```

你传入 10 和 20，终端收到的是 11 和 21——玻璃上的坐标系从 1 数起，而程序员的矩阵从 0 数起，%i 就是两个世界的海关。这类“小到不值得写进论文”的翻译，terminfo 为每种终端各存了一份;ncurses 手里握着的，是几大 Unix 系统数十年累计的几千种终端的价目表全集。

再看实现一侧。公开的源码资料（本章机制专题的现搜通道三次故障未能落盘，以下依据素材与 ncurses 公开源码结构，读者可对照 ncurses 源码的 tty/update.c 与 hashmap.c 验证）显示，ncurses 为 doupdate 准备了行级哈希表:把每一行内容散列成指纹，两帧对照时先比指纹，指纹相同的行整行跳过，只有哈希碰撞或指纹不同的行才逐格细查。这把逐格比较从“行数乘列数”的暴力扫描，降为“变化行数乘行宽”。优化的对象也随之清晰:窗口再多、逻辑再乱，doupdate 的开销只与“真正变了的东西”成正比——这句朴素的话，四十年后会被 Rust 社区用另一种语法再说一遍，此为后话。

价目表上还有两页历史，值得单独翻给大家看。第一页叫 padding，是比优化更古老的焦虑:早期的终端消化一条序列需要时间，如果主机发得太快，慢终端会丢字节，所以 termcap 时代就规定在特定序列后面垫上若干填充字符（常见的是 NUL），给终端留出喘息时间——慢，在这个行当里从来都是一等公民，快才是后来学会的。第二页叫交替屏，就是取证清单里的 smcup=\E[?1049h 与 rmcup=\E[?1049l:程序启动时切换到一块独立的备用屏幕，退出时整块还原成你进入前的主屏——vim、htop 退出之后终端原样如初，仿佛什么都没发生过，靠的就是这对序列；而在 curses 的账本模型里，这意味着 curscr 整本翻页重记。至于 doupdate 内部的决策顺序，把它翻译成编译器的行话毫不违和:先试“什么都不做”（没变化就一个字节不发），再试“便宜的小动作”（退格、直接书写），然后是“区域操作”（el、ed、dch、dl 这类按需擦拭与插删），最后才轮到整段重写——这与编译器先做窥孔优化、实在不行才换算法的思路同构，区别只是它的目标机器是一块玻璃，指令集是 terminfo 里那一小卷词条。

说到我的实战。wsstunnel 的反向终端跑在 WebSocket 上，那是一条比 9600 波特串口体面不了多少的窄通道，而且每个字节的延迟都被公网 RTT 放大。我在处理窗口同步时遵循的第一戒律，就是从这一节抄来的:永远不要发送一个可以推导出来的字节。做法并无新意——本地保留上一帧，发送前做 diff，只把变化段连同其寻址信息推过隧道;对方端按同样规则重放。在 5G 与光纤的时代重读 1980 年的 curses 源码，你会恍惚觉得串口从未消失，只是改名为“上行带宽”。launch-tui.sh 里还有一条反向的教训:当 TERM 未设或设成了 dumb 时，任何依赖价目表的“聪明”都会瞬间退化为哑流——infocmp dumb 显示这本方言词典薄得可怜，只有 bel、cr、cud1、ind 几个词条和 cols#80 一项声明。价目表缺失时，优雅降级而不是崩溃，是 curses 用四十年教给所有人的事故预案。

## 5.4 ncurses 的现代服役:htop/vim/tmux 底下都是它

把谱系钉牢，我们才能理解眼前这台机器。BSD curses 之后，Bell Labs 的 Mark Horton 在 System V 里重写了它，绑定 terminfo，并加入颜色、多窗口与后来的 panels、menu、form 扩展库，这版规范后被 X/Open 收编为 XSI curses 标准。1982 年前后，Pavel Curtis 写了自由的克隆版 pcurses;1993 年 11 月，Zeyd Ben-Halim 把 pcurses 打磨重发，更名为 ncurses（new curses），版本号 1.8.1;Eric S. Raymond 随后接手了一段，1996 年起维护权交给 Thomas Dickey——他此后近三十年同时看护 ncurses 与 terminfo 数据库，第三章那位“数据库的活体维护者”正是此人。ncurses 的发布节奏至今稳健:6.0 于 2015 年，6.5 于 2024 年 4 月 27 日。这不是活化石的喘息，是地基的正常施工。

空谈无益，直接解剖我正在打字的这台机器。在一台 2025 年的 Mac mini 上:

```console
$ which htop vim tmux
/opt/homebrew/bin/htop
/usr/bin/vim
/opt/homebrew/bin/tmux
$ otool -L $(which tmux) | grep -i ncurses
	/opt/homebrew/opt/ncurses/lib/libncursesw.6.dylib (compatibility version 6.0.0, current version 6.0.0)
$ otool -L /opt/homebrew/bin/htop | grep -i ncurses
	/opt/homebrew/opt/ncurses/lib/libncursesw.6.dylib (compatibility version 6.0.0, current version 6.0.0)
$ otool -L /usr/bin/vim | grep -i ncurses
	/usr/lib/libncurses.5.4.dylib (compatibility version 5.4.0, current version 5.4.0)
$ otool -L /usr/bin/top | grep -i ncurses
	/usr/lib/libncurses.5.4.dylib (compatibility version 5.4.0, current version 5.4.0)
```

请细看这份清单，它是一张地层剖面图。tmux 与 htop 链接的是 Homebrew 的 libncursesw.6.dylib,ncurses6-config 报告版本 6.5.20240427——与上游发布日期严丝合缝;而 macOS 自带的 /usr/bin/vim 与 /usr/bin/top 链接的是系统私藏的 libncurses.5.4.dylib。5.4，那是 2004 年发布的版本。也就是说，这台机器上同时住着两代 ncurses，相差二十一年，各伺其主，互不打扰。系统没有把 5.4 删除，也没有把 4.2BSD 以来任何一层铲平——这正是本书总纲的活体证据:每一层都没有被替换，只是在上面加盖。词典也一样:/usr/share/terminfo 下我数出 2684 个编译好的条目，Homebrew 的 ncurses 又带着 1833 个，一台家用电脑上躺着四千多种终端方言的价目表，而其中绝大多数终端已经停产三十年——词典从不退货。

更有意思的是这三个程序的“租楼姿势”并不相同。htop 住在 ncurses 的一楼——窗口层:它的表头、进程表、计量条是真真切切的 WINDOW，由 wnoutrefresh 与 doupdate 统一发布。vim 与 tmux 则主要租用地下室——terminfo 词典层:vim 自带一套整屏更新算法，只向 ncurses 查询每种终端的能力与序列;tmux 更进一步，它在 pty 主端那侧自己维护着一块虚拟屏幕，对每个窗格的内容做逐帧对照，把最小差异写给真实终端——翻翻 tmux 源码里的 screen.c 你会发现，它等于把 curses 的三屏模型又实现了一遍，因为 tmux 的本职就是“对内当终端，对外当程序”，它必须同时扮演 5.2 讲过的两块屏幕。所以准确地说，htop 是 ncurses 的用户，vim 与 tmux 是 ncurses 的客户，而 tmux 还是 curses 思想的同门再传——同一门科学，在协议栈的不同楼层各开了一家分店。

这栋楼的住户名单比直觉长得多。ncurses 早就成了各语言生态的公共地板:Python 标准库自带的 curses 模块，底层绑定的就是 ncurses——无数运维脚本、安装器与老派控制台界面都是它的租客；Perl 与 Ruby 的 curses 绑定同出一门；再往应用层数，dialog 这类对话框生成器、mc 这样的老牌文件管理器、诸多发行版的安装器与包管理界面，脚下踩的都是同一块楼板。名单里还有一个常被误认的类别:替代品。S-Lang 是自己的屏幕库，PDCurses 是 curses 在 DOS 与 Windows 界面的移植——但它们各自守住一方水土，谁也没有成为“下一个 curses”。这件事本身就值得深思:四十多年里，全屏幕库这个岗位上并不缺少挑战者，缺少的是换人的理由。ncurses 的不可替代性不在功能多寡——论视觉表现力，它早就被 Web 甩开几个时代——而在于它把三件最难协调的事一次做齐了:一本覆盖几千种终端的活词典、一套经过半个世纪终端实机校准的优化算法、一份稳定到应用代码可以躺平二十年的 API。三者缺一，就只是又一个库；三者齐备，才是基础设施。后来者可以绕开它的一楼（窗口层），却始终绕不开它的地下室（terminfo 词典）与地基（diff 思想）——这两样，恰好是 curses 在 1980 年就抽象对的东西。

顺手交代一次诚实的取证过程。动笔前我按图索骥去翻了广山哥项目的 Tmux-FSM 仓库里的 build_workflow_analysis.md，通读后发现它取证的是另一件事:那份文档记录的是 compile.sh 把 TypeScript 与 AssemblyScript 源码经 webpack 捆绑、再由 vsce 打包成 .vsix 的 VSCode 扩展构建管线，与 curses 的渲染机制无关，本章不作引用;tmux 与 ncurses 的血缘关系，以上面 otool 的输出为准。这段废话留在正文里，是想示范本书的态度:引用必须先被证伪过一次。

取证的手段还可以再放大一号。与其只看三个明星程序，不如清点全楼门禁:把 /opt/homebrew/bin 下两百多个二进制逐个做 otool 检查，直接链接 ncurses 的有二十一个——htop 与 tmux 不出所料，但名单里还有 bash、telnet、watch、gettext 全家，甚至 lame 这样的 MP3 编码器；系统目录 /usr/bin 下，cal、less、ex、mg、clear、infocmp 也赫然在列。最动人的是 cal:这个连一屏都撑不满的小日历，多半只是为了把今天的日期标成反白，就动用了一整个 ncurses。也就是说，在一台普通家用电脑上，每天有几十个程序出入这栋楼的门厅，而绝大多数用户终其一生不会念出 ncurses 这个名字。基础设施的最高荣誉是匿名性:本章的标题里写着它，命令行的日常里却从不需要提到它——所有该说的，它都替程序对终端说完了。

最后看两件“ncurses 没有停在 1980”的证据。其一是宽字符:今天的 ncurses 以 libncursesw 形态发布，cchar_t 允许一个格子容纳多字节复合字符，UTF-8 的中文、日文与 emoji 才能在终端里各就其位——本项目书稿能在终端编辑器里排版，底层正是这套机制。其二是鼠标:ncurses 提供 mousemask 与 getmouse，把 xterm 发来的鼠标序列解码成结构化事件，htop 里点一下列名就能排序——1980 年的 Ken 当然没见过鼠标，但 1990 年代的库给了玻璃一只耳朵。抽象对了的东西，是可以慢慢加盖的;ncurses 五十年不衰的另一半原因就在这里:它不但抽象对了，还活着，还在长。

## 5.5 地层学笔记:curses 教给现代 TUI 的三件事

考古进行到第五层，把刷子放下，清点这层出土的文物。curses 与它的现代继承者 ncurses 五十年不衰，在软件行业这是个近乎反常的岁数——比它年轻的图形库换了多少茬，比它响亮的框架死了多少代。它活下来的原因，值得单独列一个清单，而且这个清单的每一项，都将在第八章的 TUI 四重奏里重新登场。

第一件事:diff 思想。curscr 那本“上一帧账簿”是 curses 真正的不朽文物——保留上一帧，只输出变化，这句话今天有个更时髦的名字:双缓冲 diff。第八章的四位主角，无一例外全部踩在这块基石上:Rust 的 Ratatui 把字符矩阵叫作 Buffer，显式提供 diff，逐格比较后只产出变化单元格，新版本甚至进一步拆成迭代式的 BufferDiff 与 CellDiffOption;Python 的 Textual 用 Compositor 把界面合成为一行行 Strip，做行级 diff，未变的行直接跳过;JS 生态的 Ink 借用 React Fiber 的协调机制算出变化子树，再用 LogUpdate 式输出重写那几行。范式各不相同——立即模式、消息循环、CSS 加 DOM、Fiber 协调——但渲染的最深处都是同一个动作:拿下一帧与上一帧对照，把差额变成字节。8.1 节我们会把这些血脉一次理清，此处只需记住:2005 年以后出现的每一个 TUI 框架，其渲染核心都是 wnoutrefresh 与 doupdate 的转世灵童，连变量名都懒得换。

第二件事:能力协商。curses 的第二大发明，是把“程序懂终端”变成了“程序问终端”。程序只声明意图（把光标移到第十行第二十列），价目表负责翻译成方言（cup 还是别的什么写法），而价目表可以整表更换——换一台终端，换一份 terminfo，程序一行不改。这个“意图声明、运行时查表”的结构，是软件史上最早、最成功的能力协商，第三章已经给它写过完整传记，这里要补的是它的后世:第八章里，四大框架全都继承了这个结构，只是价目表从磁盘上的编译条目，变成了运行时的一串探测——COLORTERM 是否声明真彩色，TERM 以什么前缀开头，终端应答 DA 与 XTGETTCAP 报出什么身份;再往前追，Wayland 与 DRM 的能力查询、Web 前端的 feature detection，骨子里都是同一个动作。抽象出一层“能力语言”，让两端各自报数，永远好过让某一方赌另一方的长相。这套方法论，第八章讲选型时还要用到。

第三件事:抽象边界。必须诚实地记录 curses 的边界画在哪里:它抽象的是输出的方言，没有抽象输入的演进。键盘功能键它管，但后来的括号粘贴模式、Kitty 键盘协议、真彩色 SGR 扩展、图片协议，全部生长在 curses 的墙外，各家框架与终端模拟器自行协商。这个“没管住”恰恰是最有教育意义的教材:一个抽象的价值不在于包住一切，而在于划清哪些差异不值得被应用看见（几百种终端的寻址语法），哪些差异必须留在边界之外（输入协议仍在演化的战场）。第八章的 Ratatui 给出了后世的答案——Backend trait:把“最终怎么写字节”留成一个可替换的接口，默认实现直写 ANSI 序列，绕开 curses 的窗口层，却完整保留 curses 的 diff 心法。加盖还是绕行，判断标准只有一个:旧抽象是否还抽象对了东西。curses 的窗口层在部分新框架里被绕开了，但它的两阶段刷新与能力协商至今无人绕开——因为没人抽象得比它更对。

如果要用一把更锋利的尺子来量这第三件事，不妨用这个问题:当一种新终端出现时，你要改几处代码?curses 时代的答案是:应用一行不改，改的是词典——给 terminfo 加一个新条目，所有程序立刻受益；现代 TUI 框架的答案是:渲染层改一个 Backend 实现，上层 diff 逻辑照旧。两个时代给出的是同一个答案的两种写法，这个答案就是好抽象的通用形状:把变化关进一个小房间里，让其余的一切对变化视而不见。反面的形状我们也见过:把终端型号硬编码进应用的程序，每一次终端换代都是一次灭门。所以第八章考察四重奏时，我们会反复回到这把尺子——Ratatui 的 Backend trait 量出来几分，Textual 的驱动层量出来几分，Ink 的渲染器又量出来几分。第七章看参数解析，第八章看 TUI 框架，用的其实都是同一副考古学的眼镜:不看它今天长什么样，看它的地层构造能否承受下一次加盖。

于是我们可以给这一层写下结案陈词。1980 年，伯克利的一间机房里，一个学生为了同学的游戏能跑在多种终端上，把“人与全屏交互”这件事拆成了最小公倍数:一块必须自己记账的玻璃，一张必须按方言翻译的价目表，一套永远寻找最短字节流的算法。四十五年过去，玻璃换成了 4K 显示器，价目表搬进了内存，算法被 Rust 和 Go 重新发表了一遍，但最小公倍数一个因数都没变。我们今天给 ncurses 发新工牌，不是出于敬老，而是因为岗位还在:只要还有人在终端里看进度条、表格与图表，wnoutrefresh 与 doupdate 就仍在服役。地层之上还会加盖——第八章的四重奏已经在楼上开工了——但请记住楼板是谁浇的。

---

## 第6章 PTY 的魔法:一切远程终端的内核基石

# 第 6 章 PTY 的魔法:一切远程终端的内核基石

前五章里，我们对面的终端始终是真的：一根串行电缆、一块玻璃屏幕、一个内核里的行编辑器。这一章我们把道具掀开——因为从 1983 年起，绝大多数“终端”都不再是设备，而是一对互相假装的文件：主端冒充键盘，从端冒充屏幕，合起来叫伪终端（pty）。没有它，SSH 只是字节搬运工，Docker attach 打不开一个交互 shell，tmux 无处安放它的会话，终端模拟器连 bash 都请不动。而我把这对设备推到了它最极端的用法：在一个只允许 HTTP 代理出站的沙箱牢笼里，反推一个货真价实的真终端。这一章是全书的地理中枢——一个按键从指尖到屏幕的完整旅程，把前五章的每一层串成一条河。

## 6.1 为什么需要假的终端

先看一个今天的工程师人人都撞过、却很少有人追问的现场。你在终端里敲 `ls`，目录名是带颜色的；你写 `ls | cat`，颜色消失了。你直接跑 bash，它给你提示符、给你历史、给你 Ctrl+A；你写 `bash < script.sh`，它突然变得寡言，退回 1970 年代的沉默。`passwd` 在管道里干脆拒绝工作，直说“必须从终端读密码”。`vim` 和 `top` 更直接：没有终端，压根不起。这些行为差异的开关，是 C 库里的一个提问——`isatty(0)`：标准输入是一个终端吗？半个世纪以来，几乎每一个交互式程序都在启动时问这句话，并根据答案切换自己的人格。而“终端”这个身份，由谁颁发？

硬件时代的答案很自然。1970 年代，终端是一台设备，插在串口上，串口在 Unix 里是 `/dev/ttyS0` 这样的设备文件。程序读它，读到的是键盘；程序写它，写出来的是屏幕；内核的 TTY 子系统（第一章的老朋友）替双方当中间人。可到了玻璃终端和终端模拟器的年代，答案突然悬空了：xterm 只是一个用户态进程，它手里没有串口，没有设备文件，它凭什么给 bash 颁发“终端”的身份？远程登录年代，问题更尖锐：网线那头的 sshd，键盘和屏幕远在几千公里之外，可远端的 bash 坚持认为，自己必须和一个终端说话才肯好好干活。两类场景撞在同一个死结上：应用只认“我在和终端说话”，而终端这个东西，物理上已经不存在了。

解开死结的思路，是软件史上最优雅的一次冒名顶替。既然应用要的只是一个“符合终端接口的东西”，那就造一对互相假装的设备：主端（master）负责冒充键盘——谁往主端写字节，效果等同“用户按了键”；从端（slave）负责冒充屏幕——从端被打开时，它的一切行为都和一个真终端设备无法区分，`isatty` 对它说真话，行编辑、回显、信号，一样不少。两端在内核里背对背连着：写进主端的数据像按键一样流向从端，从端写出的数据像屏幕输出一样出现在主端。xterm 拿着主端，扮演“人”；bash 打开从端，以为自己在和硬件促膝长谈。应用一行代码都不用改——因为对应用来说，什么都没变。这正是全书反复回扣的那个论点最纯粹的一次显形：兼容不是没人想换，是换了没人看得出来。

这套魔法不是 Linux 的发明，它的地层比很多人以为的深得多。给伪终端修族谱，几个年份值得刻上化石：1967 年，DEC 的 PDP-6 分时系统里就有了伪终端的雏形，后来的 TENEX 等系统各自长出了类似机制——需求来自批处理作业想“假装有人在打字”。1970 年代末，RAND 公司与 BBN 给第六版 Unix 打补丁，加上了 pty 驱动，动机是 ARPA 网上的远程访问——网络一进来，“设备在本地”这个假设就塌了，伪终端是第一批补丁（素材:materials/ch06_补充.md）。1983 年是它的寒武纪大爆发之年：一方面，贝尔实验室的第八版 Unix 在 Dennis Ritchie 的流（stream）机制论文里正式描述了现代伪终端；另一方面，4.2BSD 把它带进了主流，接口是 `/dev/ptyXY` 与 `/dev/ttyXY` 这一对对预先造好的设备。1980 年代，AT&T System V 把伪终端装进 STREAMS 框架，并贡献了那个影响至今的多路复用器 `/dev/ptmx`。1998 年，这套 API 以 Unix 98 标准的名义统一天下，`posix_openpt()` 成了正规入口。2018 年，连最固执的 Windows 都补上了这一课，叫 ConPTY——伪终端熬死了所有试图取代它的方案，连不肯收编它的阵营最后也主动来报到。

我们把“伪”字掰开看。伪终端的“伪”，伪造的从来不是行为，只是材质——从端对应用的承诺是完整复刻一台硬件终端的接口语义，包括那些最刁钻的细节：Ctrl+C 必须变成信号而不是字符，Backspace 必须能抹掉行内的字，窗口尺寸变化必须能被应用感知。承诺兑现的方式藏在内核里，这正是下一节要解剖的东西。而在进入内核之前，值得先记住本节的一句话结论：伪终端是一对互相假装的设备，主端冒充键盘，从端冒充屏幕；它是“接口与实现分离”在内核层的一次教科书演出——应用的幻觉，由内核负责维持。SSH、终端模拟器、Docker、tmux，后面出场的每一位，买的都是这份幻觉。

这份幻觉在日常生活里到处留痕，只是我们很少把它们归案。sudo 敢于开口要密码，git 知道给分支名上色，安装器敢画出会动的进度条，都是因为 `isatty` 的答案为真；反过来，把输出接进管道，它们立刻收敛成安静的数据加工者——进度条消失，颜色消失，人机对话的化妆全部卸掉。由此得到一条可操作的推论：每个 CLI 都有两副人格，调试它时若想看到“终端模式”的那一副，就给它配一个假终端——`ssh -t` 强制分配一个，或者用 `script` 凭空造一个。假终端在这里已经不是权宜之计，而是一件日常工具。

还有一批用户，是伪终端的发明者们没有预料到的：自动化。要在无人值守的机器上跑一个“非终端不工作”的交互安装器，要在测试里程序化地与一个只肯跟终端说话的老程序周旋，要在沙箱里替人类敲键盘——四十年间，需求侧的名单越拉越长，而供给侧始终只有一样东西：一对互相假装的设备。所以我们把本节收在第二句话结论上：假终端从来不是为了“假”而造，它是为了“终端接口所代表的那份承诺”而造——承诺一旦立住，新的世界就会自动来认领它。

## 6.2 内核解剖:ptmx、pts/N 与 line discipline 的复用

伪终端在内核里的户口，经历过一次搬家，搬家前后的两套门牌至今仍在教科书里打架，我们把两派都看清。老派是 BSD 风格：系统启动时就预创建好两百多对设备，主端叫 `/dev/ptyXY`，从端叫 `/dev/ttyXY`，X 的取值范围是 p 到 z 再加 a 到 e，Y 是 0 到 9 加 a 到 f。程序要用伪终端，只能拿着这些名字挨个 `open()` 试探——打开失败就换下一对，找到空闲的为止。这个设计的毛病一目了然：数量封顶、查找靠撞、两个进程同时试探还会撞车（竞态条件）。Linux 把它供在 `CONFIG_LEGACY_PTYS` 的牌位后面，自 2.6.4 起标记废弃，如今默认关闭——这是整个 TTY 地层里少见的、真正被填埋的一层，填埋它是因为新派确实全面胜出。

新派叫 UNIX 98 风格，今天所有现代程序走的都是这条路。它把“分配一个伪终端”压缩成打开一个文件：`/dev/ptmx`——pseudo terminal multiplexer，伪终端多路复用器。这是一个 clone 设备：每次 `open()` 它，内核就动态造出一对全新的主从两端，主端是你手里这个文件描述符，从端则以 `/dev/pts/N` 的面目出现在 devpts 虚拟文件系统里，N 由内核按需分配。标准调用序列五步，配套的辅助函数把它们封装成 `openpty()` 与 `forkpty()` 两个趁手的工具：

```c
int mfd = posix_openpt(O_RDWR | O_NOCTTY);  /* 打开 /dev/ptmx,得主端 */
grantpt(mfd);                               /* 设置从端属主与权限 */
unlockpt(mfd);                              /* 解锁从端,允许打开 */
char name[64]; ptsname(mfd, name, 64);      /* 问出从端名:/dev/pts/3 */
int sfd = open(name, O_RDWR);               /* 打开从端 */
```

数量上限写在 `/proc/sys/kernel/pty/max` 里，可以在线调整——伪终端从此成了内核里一种随取随用的弹性资源。这个“打开即分配”的模型眼熟吗？它和第 3 章里 terminfo 用编译数据库替代逐文件扫描是同一种智慧：把最笨的那层（遍历、试探、竞争）沉下去，把干净的接口浮上来。

解剖从端这一侧，会发现一件本节最重要的事：伪终端不是一根“加了壳的管道”，它把整个 TTY 子系统的语义原封不动地复用了。内核源码 `drivers/tty/pty.c` 里，一对主从共享同一个 `struct tty_struct`，两端各持引用、背对背交换数据。数据通路是：主端写入 → TTY 核心（tty_io.c）→ 线路规程（n_tty）→ 从端读队列 → 应用 `read()` 返回。请注意中间那一站——线路规程。第一章和第四章我们见过它的全部本领：规范模式下做行编辑（ERASE、KILL、WERASE、VEOF），ISIG 位开着就把特定字节翻译成信号，ECHO 位开着就替应用回显。这些逻辑在伪终端上照常运转，分毫不差——写进主端的一个 Ctrl+C（字节 0x03）会被线路规程当场拦截，转成 SIGINT，投递给从端一侧的前台进程组，而根本不会作为字符进入应用的输入缓冲。这就是那句“在主端写入 Ctrl-C，从端连接的进程真的会收到 SIGINT”的机制底细：信号不是 shell 转发的，不是 SSH 转发的，是内核线路规程在数据路径上顺手完成的。为了验证这个机制，不妨看这个十行的实验——用 `pty.fork()` 一步到位（它替子进程完成 setsid，并把从端设为控制终端，这正是“成为前台”的全部手续），再从主端写进一个 0x03：

```python
import os, pty
pid, mfd = pty.fork()   # 子进程:setsid,并把从端设为控制终端
if pid == 0:
    os.execvp("sleep", ["sleep", "30"])   # 子进程:一个无辜的 sleep
os.write(mfd, b"\x03")  # 主端写进一个 Ctrl+C 字节
_, status = os.waitpid(pid, 0)
print(os.WIFSIGNALED(status), os.WTERMSIG(status))
# 输出:True 2 —— sleep 死于 SIGINT,而不是读到字符 0x03
```

运行它，`sleep` 会当场死于 SIGINT——bash 里那个家喻户晓的“Ctrl+C 停止当前程序”，底层就是这一条数据路径。顺带说清一个容易误会的细节：线路规程把 0x03 认出来并转成信号，前提是这次投递有明确的收件人——从端一侧的前台进程组。这个“收件地址”是会话与控制终端的登记手续（子进程 setsid 之后再让从端成为控制终端）办下来的，办了手续，信号才送得到；这也是为什么自己手写 pty 代码时，漏掉这一步会让 Ctrl+C 集体失灵。伪终端的主端上没有任何特殊的“信号 API”，信号是字符穿过线路规程时的副产品。

最后看主端的一个专属机关：TIOCPKT，包模式（packet mode）。主端支持一组从端没有的 ioctl，其中最有用的是把数据流升级成“带信封”的：应用在主端上开启 TIOCPKT 之后，每次读主端，第一个字节不再是终端数据，而是一枚控制标志——标志位会告诉你“对端发生过输出冲刷”“终端设置变化了”“窗口尺寸变了”等事件，真正的数据跟在信封后面。这个设计是为 SSH 这类远程终端量身定做的：没有它，sshd 只能靠轮询 ioctl 才能发现对端的终端状态变了；有了它，状态变化和数据共享一条流、一次 `read()` 全知道。窗口尺寸同步、Ctrl+C 的即时送达，这些远程终端体验的关键细节，都靠这枚小小的信封字节。我们不妨给本节收个束：从端是一个尽职的演员，把 tty 设备的一生演绎得滴水不漏；主端是剧场后门，握有它的人扮演上帝——投喂按键，收割屏幕，还能隔着信封知道舞台上的风吹草动。下一节，我们跟着一个按键，把这条通路完整走一遍。

新派的世界观落在文件系统上，值得亲眼一看。devpts 文件系统挂载在 `/dev/pts` 之后，这个目录就是全体从端的点名册：每开一个终端标签页、每起一个 tmux 面板、每 `ssh` 进一台机器，目录里就多出一个以数字命名的文件；会话退出，文件消失。当前用量与上限写在 `/proc/sys/kernel/pty/nr` 与 `/proc/sys/kernel/pty/max`，前者实时跳动，后者给全系统的伪终端总数封顶。老牌的 `who` 命令列出的每一个登录会话，末尾那列正是从端名——所谓“在线用户”，在内核眼里不过是一张 pts 设备的租借清单。设备文件即会话，目录即世界，这是 Unix“一切皆文件”最整齐的一次落地。

最后回答一个工程上的常见疑问：既然主从两端就是一条双向字节流，为什么不用现成的 socketpair 或管道，非要劳驾内核造一对设备？因为 pty 随身带着三件管道给不了的行李。第一件是身份：`isatty` 对管道说假话，应用会当场切换人格，交互能力全部下岗。第二件是加工：管道里的字节原样直达，而 pty 的字节要过线路规程这道关——行编辑、回显、信号翻译，全在这一站。第三件是状态：窗口尺寸、前台进程组、终端设置，这些“只有终端才有”的属性，管道无处存放。三者合起来就是一句话：伪终端与管道的差别从来不是性能，而是语义——你要交付一个终端，就必须造一个终端。

## 6.3 一个按键的完整旅程（全书地理中枢）

每一章我们都站在某一层地层上向两端张望；这一节，我们把所有地层钉在一张剖面图上，然后跟着一次真实的按键——比如按下回车前的最后一个字母——从指尖走到像素，一站不停。先看全程地图：

```
键盘
 │  物理按键 → 操作系统键盘事件
 ▼
终端模拟器(xterm/iTerm/Windows Terminal)
 │  把按键翻译成字节流,写入主端 fd
 ▼
/dev/ptmx(pty 主端)
 │  内核 tty 核心(tty_io.c)
 ▼
线路规程 n_tty(行编辑/回显/信号)
 │
 ▼
/dev/pts/N(pty 从端)
 │  应用 read() 醒来
 ▼
bash + readline(处理/编辑)
 │  fork/exec 命令,输出 write 回从端
 ▼
从端 → 线路规程(输出加工)→ 主端
 ▼
终端模拟器读主端 → 解析 ANSI → 屏幕发光
```

第零站在键盘上。按下一个键，操作系统的输入子系统收到的是一次物理事件：键位扫描码加上修饰键状态，而不是字符。“a” 是一个事件，Ctrl 加 “a” 是另一个事件，↑ 方向键又是一个。把这些事件翻译成什么字节流，是下一站的决定。

这条路上有一个著名的堵车现场，值得提前插叙：粘贴。一次粘贴五百字节，对模拟器来说等于五百次按键被一次性塞进主端；而第三站无论按哪种宪法办事，都扛不住这种洪水——规范模式下行缓冲只按“行”供货，换行一出现半截内容就提前交付；应用接管后的非规范模式更是一字节一交付。于是 REPL 把没读完的行当场执行，vim 给每一行自作聪明的缩进，历史上一整类“粘贴进终端就出事”的事故都发源于此。现代的解法依然是我们第二章的老朋友：xterm 私加扩展，发明“括号粘贴模式”——进入这种模式时，模拟器把粘贴内容用一对专用转义序列包裹起来，明确告诉应用“这是一次粘贴，别当按键处理”。一行内核代码都没改，问题在协议层解决，各应用选择性加入。这是带内信令的又一次胜利，也是兼容税的又一次收缴。

第一站，终端模拟器。这里是全书反复出现却最常被认错身份的一个角色：它不是显示器驱动，而是一个持有 pty 主端的普通用户态进程。把按键翻译成字节的是它——普通字符按 UTF-8 编码，“a” 变成单字节 0x61；方向键翻译成三字节转义序列 ESC [ A，这正是第二章那门五十年不倒的隐秘语法在输入方向的现身；Ctrl+C 翻译成单字节 0x03。翻译完毕，它 `write()` 自己的主端文件描述符——注意，它写的不是什么特殊设备，就是一个当年从 `/dev/ptmx` 打开的 fd。同一根 fd 还有第二个用途：模拟器不停地 `read()` 它，把从端那边流出来的字节画到屏幕上。所以模拟器与 pty 的关系是双向独占：按键进主端，屏幕出主端，主端是它通向内核世界的唯一孔道。

第二站，内核 TTY 核心。字节穿过系统调用边界，落进 `drivers/tty/tty_io.c` 的缓冲与队列管理——这一层不懂编辑、不懂信号，只负责把字节搬运给挂在从端 tty 结构上的线路规程，并管好流控与唤醒。

第三站，线路规程 n_tty，整个旅程的灵魂站。字节在这里接受“这算不算一个命令”的审问，审问规则由 termios 标志位决定。如果应用还没接管（规范模式，ICANON 开着）：回车之前的字节都攒在行缓冲里，Backspace（ERASE）能抹掉行内前一个字符，Ctrl+U（KILL）抹掉整行，Ctrl+W（WERASE）抹掉前一个词，Ctrl+D（VEOF）提前交付缓冲；行缓冲的规范上限是 MAX_CANON 的 255 字节，实际受 4096 字节的 N_TTY_BUF_SIZE 读缓冲约束。与此同时，ECHO 位让内核不等应用出手就把字符回显出去——你敲一个字母屏幕上立刻出现一个字母，这件事是内核干的，不是 bash 干的。最关键的是 ISIG：0x03 一出现，内核当场把它从数据流里抽走，生成 SIGINT 投递给从端一侧的前台进程组——上一节我们刚刚看过这颗字节的完整命运。如果应用已经接管（第四章的剧情：readline 调用 rl_prep_terminal 把终端切进非规范模式），n_tty 立刻收起全部手艺，一字不改、一码不发地把字节原样上缴，read 何时返回改由 VMIN 与 VTIME 两个参数说了算。同一站，两种宪法。

第四站，从端 `/dev/pts/N`。经线路规程加工后的字节排进从端读队列，而 bash 的文件描述符 0、1、2 全都指向它——这是 fork 时继承来的家产，同时也是这个会话的控制终端，作业控制、前台进程组，登记的都在这里。bash 的 `read()` 从睡梦中醒来，拿到字节。

第五站，bash 与 readline。`isatty` 早已确认自己面对终端，提示符与 readline 上岗。行编辑发生在用户态：Ctrl+A 跳行首，Ctrl+R 翻历史，Tab 触发补全——第四章的那场“库政变”每时每刻都在上演，内核里的行编辑老匠人被旁路，用户态库接管了与手指的对话。

第六站，处理与写回。回车落下，readline 把整行交给 bash，解析、展开、`fork/exec`。假设执行的是 `ls`，新进程的输出 `write()` 到自己的标准输出——还是那个 `/dev/pts/N`。注意输出方向也要过一次线路规程：OPOST 开着时，ONLCR 把每一个 `\n` 翻译成 `\r\n` 再发往主端。第一章那对纠缠了一个世纪的回车与换行，就埋伏在这半站路里——为什么 Unix 程序只写 `\n` 而屏幕永不叠印？因为内核替它补了 CR。

第七站，回到模拟器。主端 `read()` 收到字节流，按第二章那门语法逐条解析 ANSI 序列，再凭自己被设定的能力清单驱动像素发光——第三章 terminfo 的功课在本站之前已经做完：告诉 vim 你的屏幕会什么。一个字母的旅程至此走完：八站，穿越了进程边界与内核边界各一次，历时通常不足一毫秒。

这就是本书的地理中枢：第一章的 CR/LF 埋伏在第六站，第二章的 ESC[ 出现在第一站与第七站，第三章的能力协商在第五站与第七站之间，第四章的 readline 占据第五站，而内核行编辑的老家正是第三站。一次普通按键，踏过全书每一章的地表。别忘了那条捷径：同样这颗 0x03，从第一站到第三站就被内核拦下，根本到不了第五站——这条捷径正是下一节所有故事的地基，因为当键盘远在几千公里之外，远程终端的全部工作，就是把第一站到第三站这条路原样延长。

## 6.4 wsstunnel:在牢笼里反向拉起一个真终端

现在换我来讲。wsstunnel 是我自己的项目，Python 写的，PyPI 上叫 wsstunnel，核心两千七百行上下，它做的事一句话可以说完：在一个只允许 HTTP 代理出站的受限容器里，凭空拉起一个真终端，再把终端的两端经 WebSocket 接到远方。缘起写在 README 里，我照实复述：那段时间我被沙箱环境折磨得够呛，容器说回收就回收，远程 SSH 总是不通。能想到的工具全试了一遍，cloudflared、frp、wireguard，要么配置复杂到令人头秃，要么在“只允许 HTTP 代理出站”的铁壁前直接哑火。折腾到半夜，我突然想明白了：既然现成的路都走不通，何不自己开一条？转机是一个几乎被忽略的环境变量：`echo $http_proxy` 有值——平台为了让自己的 AI 助手能拉代码，留了一个 HTTP 代理出口。这是他们给自己留的门，我决定沿着这门走进去。

技术路线在概念词典里叫反向 PTY：在目标进程内拉起真终端，并经既有通道反推 IO。方向是反的——不是终端模拟器持有主端、服务器里跑着应用，而是远方的浏览器想当“人”，牢笼里的进程替它把从端置办好。但无论方向怎么反，内核这一层的戏码与 6.1 节一模一样：必须让 bash 真的相信自己面对一台终端。这是整个项目最要害的一个判断：如果用 `subprocess.PIPE` 给 bash 喂命令（网上多数“反弹 shell”教程的做法），bash 的 `isatty` 会立刻穿帮——它关掉提示符，关掉行编辑，vim、top、htop 全部罢工，你拿到的是一根管道，不是终端。所以 wsstunnel 默认模式的核心三行，就是 6.2 节那套标准流程的 Python 版，摘自 `wsstunnel/client.py` 的 `_run_pty_mode`：

```python
master_fd, slave_fd = pty.openpty()
cols, rows = os.get_terminal_size()
_set_winsize(master_fd, rows, cols)
shell_proc = subprocess.Popen(
    [shell, "-i"],                 # bash -i:交互模式,画出提示符
    stdin=slave_fd,                # 三个标准流全部接到从端
    stdout=slave_fd,
    stderr=slave_fd,
    pass_fds=(master_fd,),
    preexec_fn=os.setsid,          # bash 自立门户,成为新会话的组长
)
os.close(slave_fd)                 # 从端使命完成,父进程只留主端
```

`pty.openpty()` 一行，等价于 6.2 节那五步 C 代码；`os.setsid` 让 bash 成为自己的会话组长，这是信号投递的登记手续；随后父进程关掉从端，只攥着主端——它此刻扮演的角色，和 xterm 在 6.3 节第一站的身位完全相同：投喂按键，收割屏幕。收割是独立线程干的，代码朴素到近乎透明：`select.select([mfd], [], [], 0.5)` 盯住主端，有货就 `os.read(mfd, 65536)`，然后 `ws.send_binary(data)` 把 bash 的“屏幕”原样推上 WebSocket；另一侧主循环收到的每一个二进制帧，`os.write(master_fd, msg)` 写进主端当按键。6.3 节的八站旅程没有被打破，只是第一站被搬进了浏览器，第七站被搬进了 VPS，中间两站换成了电文——内核里的第三站与第四站，浑然不觉。

被搬走的第一站留下两个必须补上的职责。其一是窗口同步：浏览器窗口千奇百怪，而内核从端里躺着一个默认 24 行 80 列的窗口尺寸，vim 一开就错位，htop 一跑就截断。解决办法是把第一站的 ioctl 也搬过来——前端每次变化发来 `__RESIZE:rows,cols` 文本帧，客户端解包后照章办事：

```python
def _set_winsize(fd: int, rows: int, cols: int) -> None:
    winsize = struct.pack("HHHH", rows, cols, 0, 0)
    fcntl.ioctl(fd, termios.TIOCSWINSZ, winsize)
```

ioctl 落下的那一刻，内核更新从端的窗口尺寸，并给前台进程组发去 SIGWINCH，正在跑的 vim 会自己重排——这正是 6.2 节 TIOCPKT 信封里“窗口变了”那枚标志位的另一半。其二，牢笼里的命运比机房里的 xterm 漂泊得多：沙箱要回收，网络要抖动，连接会死。wsstunnel 为此修了四层保活，层层独立、层层兜底——WebSocket 协议层禁用库内置的 ping，与应用层心跳互斥；应用层每 30 秒一发 `__PING__`、一回 `__PONG__`；连接断了走指数退避重连，5 秒起步，封顶 5 分钟，连上即归零；shell 本身崩了就在原连接里重生，最多五次。心跳线程的核心与文档里写的一致：

```python
def _heartbeat(ws, reconnect_event):
    while not reconnect_event.is_set():
        try:
            ws.send("__PING__")
            time.sleep(30)
        except Exception:
            reconnect_event.set()   # 心跳失手,唤醒主线程重连
            break
```

而隧道本身走的是协议走私：客户端调用 `ws.connect(server_url, http_proxy_host=proxy_host, http_proxy_port=proxy_port)`，websocket-client 库便向代理发出 RFC 7231 的 CONNECT 请求，代理回一句 200 Connection established 之后，这条连接就成了盲眼隧道——TLS 握手、WebSocket 升级、pty 的字节流，全部从缓存网页的那扇门里过。从防火墙的日志看，这是一次再普通不过的 HTTPS 代理访问；从牢笼里看，一个完整的交互式终端正在出门。

牢笼之外的中继端也没闲着。一个 relay 可以同时挂多个容器：每个 client 报上名字注册（命令行的 name 选项），前端 `LIST` 一声，全体后端连同各自的模式（pty 或 pipe）与在线时长列队报到；`USE 名字` 切换默认目标，`@名字 命令` 定向投递，输出自动挂上归属标签——一套最小的集群路由，代码不到百行。链路开销交给 WebSocket 的 permessage-deflate 压缩，TUI 界面满是重复的空格与边框字符，压缩比高得惊人。工程上还留了一条退路：打开 no_pty 开关即切回管道模式，照顾那些造不出伪终端的老旧环境——用 6.2 节的话说，就是交付不了“真终端”时，至少把通道保住。而浏览器里的那一端，是 xterm.js：它读 WebSocket 字节流、解析 ANSI、把字符画上画布，恰好是 6.3 节第一站加第七站的全套职责——所谓“第一站搬进浏览器”，搬的就是这个库。

这个项目还有一个诚实的附录，我把《生产升级翻车记》留在 docs 里：一次“看看哪里还有优化空间”的日常 review，我写了条探针脚本对着真实中继进程逐帧喂消息，实锤了十二处问题——命令黑名单声明了却从未被调用、文件角色认证通过却因 fall-through 转发不出去、广播循环遍历时集合被并发修改当场炸掉——四百六十六行净改动修完，测试从 131 涨到 176 个，全绿，发版升级，然后我亲手把生产打挂了十分钟：服务器上的 websockets 库是 12.0，走的是 legacy 实现，而我本地 16.0，全绿的前提是测试环境等于生产环境。翻车记里最扎心的一处与我刚写进本章的道理遥相呼应：探针跑 PTY 用例时报 `out of pty devices`——伪终端是内核资源，也有耗尽的一天。修复那晚我学到的最值钱的一条是：删除“未使用”的代码前，先问一句作者为什么写它——那段被当死代码清掉的版本探测，是一个没写完的 TODO，它当晚就在生产上等着我。至于反向 PTY 这个词本身，6.5 节的家族合影会告诉你，它并不是发明，只是把 pty 的两端拆得更开了一点。

## 6.5 PTY 家族合影:Docker attach、tmux、script、expect

合影之前先报人数：这一节出场的每一位，用的都是同一个原语——打开 `/dev/ptmx`，握住主端，把从端让给某个无辜的进程。不同的只是“谁扮演人”“从端让给谁”。我们按身位逐个点名。

第一类，终端模拟器，xterm、iTerm2、Windows Terminal、Alacritty。它是 6.3 节的第一站，家喻户晓到反而没人记得它是个 pty 程序：进程启动时打开主端，fork 出登录 shell 挂上从端，此后一生只做两件事——把你的按键写进主端，把主端流出的字节画上屏幕。它没有内核特权，不碰显卡驱动，它就是一对设备文件的使用者。

第二类，SSH。sshd 在服务端持有主端，把从端让给登录 shell；网线在此刻被抽象成一根延长线——你笔记本上的终端模拟器写主端，字节经加密隧道落到 sshd 手里，sshd 替你写进另一台机器上的主端。两台机器、两对 pty，接力维持一场幻觉。`ssh -t` 强制分配终端，`ssh -T` 明确不要，而 `ssh host ls` 这种带直接命令的调用默认不分配——因为你大概率要的是管道语义，输出还要进下游的脚本。SSH 把“终端还是管道”这个上古身份问题，原样递交给了每一个远程用户。

第三类，容器。`docker exec -it` 里的 `-t`，做的就是打开主端这件事：让容器里的 bash 住进一个从端，提示符、行编辑、Ctrl+C 一样不少；去掉 `-t`，你面对的是管道，交互程序集体罢工——第 6.1 节开头那一串现象会在容器里原样重演。`docker attach` 的同理与差异都值得记一笔：它接到的是容器主进程的 stdio，主进程当年有没有被分配 pty（-t），决定你此刻面对的是终端还是管道；Kubernetes 的 `kubectl exec -it` 同门同法。容器时代人人都会敲的那对字母 -it，全称是 interactive 加 tty——伪终端的名字，就藏在每次排障的第一条命令里。

第四类，终端复用器，tmux 与 screen。它们把“谁扮演人”这个角色彻底做成了可插拔：tmux 服务端为每个面板持有主端，shell 住从端；你 attach 时，客户端与 tmux 之间再架一条通道，把你的键盘与那对 pty 的主端接通；你 detach 时，键盘与屏幕双双离场，而 pty 对原封不动，会话、正在跑的 vim、正在滚的日志，全都活在 tmux 手里那半截主端上。“断开连接程序不死”这个改变了几代人工作方式的特性，实现上只是一句陈述：把主端的持有权从“人”手里挪到“中间人”手里。screen 比它年长，1987 年问世，还兼职管理调制解调器拨入的老差事。

第五类，记录与自动化。`script` 把一次会话的所有进出原样抄录成 typescript 文件，它的工作原理就是开一对 pty 从中截流；工程师更常用它的是另一面——`script -qec 命令 /dev/null` 凭空造一个终端，骗过那些“没有 tty 就罢工”的程序，最经典的应用是让 sudo、ssh 在无终端的脚本环境里肯跟你对话。`expect` 则走得更远：主端在它手里不是记录仪，而是方向盘——它按剧本往主端写“密码”，从主端读提示，把一场人机对话完整地程序化。1990 年代 Don Libes 写下它时给出的理由今天依然成立：有些程序天生只肯跟终端说话，那就在终端这一层跟它们谈。测试自动化、设备群控、装机脚本，expect 的后世子孙都还在这条路上——Python 的 pexpect 把它包成了库函数，而今天 AI agent 操控终端时“发命令、读回显、按需补发 Ctrl+C”的整套手法，血缘一眼可辨：expect 当年写下的对话剧本，只是换了个主角继续上演。

合影里还有两个新面孔，值得单独按快门。一个是我自己的实验场 Tmux-FSM：Go 写的 tmux 键位状态机，把前缀键、层模式、超时回落、与 Neovim 的模式同步做成一套 YAML 可配置的有限状态机，再用 CRDT 做多机状态同步的试验。它给我的启发很直接：当 tmux 把主端攥在手里之后，pty 之上依然有空地——键位语义、状态层、同步层，都可以在这块空地上继续加盖，这正是“考古层”定义本身的一次现场验证。另一个面孔属于这个十年：AI Agent 的沙箱终端。让智能体跑一条交互式命令、进一个 Python REPL 调试、应付一次 sudo 密码提问，工程上的标准答案依旧是 pty——agent harness 拉起从端、握住主端，在 6.3 节那张地图上，它只是最新的一位“第一站扮演者”。第九章我们会正面遇上它，眼下只记一笔：AI 没有换掉这个地基，它直接住了进来。

### 地层学笔记

伪终端教给我们的，是“兼容”这个美德最纯粹的样子。从 1983 年到今天，tty 设备的真实载体换了四次——硬件终端、终端模拟器、网络对端、容器与云端沙箱——而应用的感知是零：没有人改过一行代码来迎接任何一次迁移。靠的不是运气，是内核五十年来持续兑付的一个承诺：从端的接口语义，与当年那台串口上的电传打字机逐字节一致。冒名顶替能四十年不穿帮，靠的是接口的诚实。反向 PTY 不过是这套哲学的极端推论：既然主端只是“另一端的进程”，那这一端放在哪里都行——放在浏览器里，放在 VPS 上，放在一个只肯放行 HTTP 代理的牢笼之外。wsstunnel 没有发明任何新东西，它只是把 6.3 节那张地图的第一站和第七站拔起来，插到了墙外。给今天的工程师留下三条可操作的笔记：第一，每个 CLI 都有两副面孔，tty 一副、管道一副，测试要两副都跑——`isatty` 的分支和正文的分支一样值得覆盖；第二，伪终端是内核的有限资源，压测、探针、容器密度都要给 `/proc/sys/kernel/pty/max` 留出余量，`out of pty devices` 不是传说；第三，想清楚自己要终端还是要管道，再决定要不要那对字母 -it——伪装的终端解决不了管道的问题，畅通的管道也演不了终端。每一台远程终端都是一场精心策划的冒名顶替，而这场戏能连演四十年，是因为台下每一位观众——从 bash 到 vim 到 Claude Code——都只认接口，不认脸。

---

## 第7章 参数解析的取舍:Cobra、Clap、Click 与帮助文本的 UX 学

# 第 7 章 参数解析的取舍:Cobra、Clap、Click 与帮助文本的 UX 学

命令行工具给用户的第一印象，不是它的功能，而是它敲下 help 之后的那一屏输出。用户在决定是否信任一个 CLI 之前，几乎总会先敲一遍 help——这是工具与用户之间的第一次对话，而对话的质量在那一刻已经决定。这一章，我们把参数解析当作一个考古地层来挖:最底下是 C 语言 argc/argv 的原始荒原，中间是 getopt 与 POSIX 语法约定形成的沉积层，最上面是三个语言生态各自长出的声明式解析器。挖到最后一层我们会发现一个略带反讽的事实:这些解析器真正的产品不是解析本身，而是帮助文本的排版。

## 7.1 argv 的原始时代:getopt 与手撕参数

每一个故事都有一个最底层的地层。参数解析的地层，是 C 语言的 main 函数签名:

```c
int main(int argc, char *argv[])
```

操作系统把命令行拆成一串字符串递给程序:argc 是 token 个数，argv[0] 是程序自己的名字，从 argv[1] 开始才是用户敲下的参数。这里的关键在于:操作系统只负责拆词，不负责解释语义。-v 是什么意思、-o 后面要不要跟文件名、两个连字符之后还算不算选项——这些问题操作系统一概不管。Unix 把语法的解释权完全下放给了每个程序自己。

这个决定在 1970 年代制造了一场混乱。我们回看那个时代的源码，会发现几乎每个 Unix 工具的 main 函数开头，都蹲着一段手写的参数循环:一个 while，一串 strcmp，再加几个全局变量。每个程序都有自己的方言——有的用 -l 表示长格式，有的用 -long，有的要求选项与参数之间必须有空格，有的又接受粘连写法。用户面对的不是一套语法，而是一百套互相矛盾的语法。所谓“手撕参数”，撕的不是字符串，是用户的心智模型。

手撕时代的遗迹至今可见:许多老工具的 usage 字符串写得像摩斯电码——方括号表示可选，竖线表示互斥，三个点表示可重复。这套元符号不是任何委员会的发明，是无数 main 函数在报错信息里反复书写之后沉淀出来的习惯。用户学会读 usage，本质上是在学一门为每个程序重新发明一次的小语言;而 getopt 的历史功绩，正是把这门小语言从一百种方言收敛成了一种。

秩序出现在 1980 年前后的贝尔实验室。getopt(3) 作为标准 C 函数诞生，它定义了短选项的核心语法:以单个 - 开头的 token 是选项;选项名是单个字母或数字;无参选项可以捆绑，-abc 等价于 -a -b -c;选项与它的参数可以分开写也可以粘连，-o foo 与 -ofoo 等价。1985 年，它的作者在达拉斯的 UNIFORUM 大会上把这份实现以公共领域发布——在讲究所有权的 Unix 圈，这是少见的一次慷慨，也让 getopt 得以被此后每一个 Unix 变体收录，连 shell 后来都在 SVR3 里内置了配套的 getopts 命令供脚本使用。

getopt 的 API 本身就带着那个时代的指纹。它不用结构体，不用回调，而是用几个全局变量完成一次状态机循环:

```c
int opt;
while ((opt = getopt(argc, argv, "vo:")) != -1) {
    switch (opt) {
        case 'v': verbose = 1;      break;
        case 'o': outfile = optarg; break;
        default:  fprintf(stderr, "usage: prog [-v] [-o file]\n");
                  return 1;
    }
}
/* 循环结束后,argv[optind] 起才是位置参数 */
```

optstring 里每个字母是一个选项，带冒号表示要吃参数;optarg 指向当前选项的参数值;optind 记录扫描进度。我第一次读这个 API 时愣了一下:全局变量?但转念一想，这正是那个年代的正确抽象——程序是单线程的，进程就是全局命名空间，简单粗暴，但它把“扫描 argv”这件每个项目都要重做一遍的重复劳动，第一次标准化了。

getopt 的家族史也值得一记。它太小、太有用，以至于每个平台都想自己写一份:BSD 有 BSD getopt，GNU 有 GNU getopt，glibc 里还有能吃长选项的 getopt_long，不少嵌入式工具链则带着一份私改版。版本一多，行为就开始分叉:GNU 版支持选项与操作数互相穿插，解析时会悄悄重排 argv，把非选项参数挪到最后;POSIX 版遇到第一个非选项就停下;对未知选项与缺失参数，各家返回的错误旗标也不尽相同。于是 1990 年代流行的可移植做法干脆而粗暴:把一份 getopt.c 随源码一起分发，宁可自带行李，也不赌目标平台的方言。一个百行小函数，活成了便携行李，这大概是它作者没有料到的职业生涯。

还有一个小角色不该漏掉:optind 的语义。扫描开始时它是 1，每消费一个 token 加一;循环结束后，argv[optind] 到 argv[argc-1] 就是剩下的位置参数。听起来平常，但正是这个游标让 getopt 与后续的手工处理可以无缝接力——它没有吞掉 argv，只是在上面夹了个书签。好的解析器都这样做:解析与执行分离，书签留给用户。今天所有框架把剩余参数原样递给你的机制，都是这个书签的直系后代。

真正的立法是 1992 年。POSIX.2 把几十年积累的惯例写成《Utility Syntax Guidelines》，十几条约定第一次有了法典地位:选项以单个 - 开头;选项名为单个字母数字字符;无参选项可捆绑;单个 - 视为普通操作数，惯例上表示标准输入或标准输出;选项可以以任意顺序出现、可以重复。其中最要紧的是第 10 条（Guideline 10）——它把双横线定为“选项结束符”:第一个双横线之后的所有参数一律视为操作数，哪怕以 - 开头。这条约定解决了一个经典难题:如何删除一个名叫 -f 的文件?答案是:

```
rm -- -f
```

一个纯粹由标点构成的语法契约，就此有了法典效力。

同一时期，GNU 项目在 POSIX 之上加盖了一层:getopt_long。长选项以双横线开头，由字母数字与连字符组成，形态如下:

```
--verbose         双横线加完整单词
--name=value      参数用等号传递
--verb            无歧义缩写，等价于完整拼写
```

等号传参让“可选参数”第一次有了体面的写法;GNU 还支持无歧义缩写——只要前缀不与其他选项冲突，verbose 就可以只敲到 verb。注意 GNU 的做法:它没有推翻短选项，而是让长选项与短选项共存于同一个解析器，-v 与长写的 verbose 可以指向同一个变量。旧地层没有被铲掉，新地层直接压上去——又一次兼容式加盖。

这层地层的惊人之处在于它的寿命。今天 Rust 的 clap、Go 的 Cobra、Node 的 Commander，全部默认实现这套约定:单横线短选项、双横线长选项、双横线结束符、捆绑与粘连。三个语言生态的哲学再怎么分歧，底下垫的都是 1980 年那台状态机。getopt 是所有现代解析器的公理——公理不需要优雅，只需要没有人敢反对。

## 7.2 声明式革命:三个语言三种哲学

时间推进到 2010 年代，CLI 迎来了自己的大爆炸。Kubernetes、Docker、GitHub CLI 把命令行工具从“一个小工具”推向“一个平台”，子命令动辄上百个，补全、嵌套帮助、配置管理成了标配。手撕 argv 与裸 getopt 都不够用了——我们需要的不再是一个扫描循环，而是一整套“如何描述一个命令”的语言。三个语言生态，几乎同时给出了三种答案，而三种答案的分歧，本质上是三种编程哲学的分歧。

Go 的答案是 Cobra，作者是 Steve Francia（网名 spf13），他一手维护着 Go 的 CLI 帝国:Cobra 负责命令框架，Viper 负责多层配置合并，pflag 是 POSIX 风格的 flag 库，cobra-cli 是脚手架。Cobra 的哲学是树:一个 CLI 就是一棵命令树，每个节点是一个 Command 结构体，同时承担命令路由与执行两个职责。它把命令行语法总结成一个公式:

```
APPNAME COMMAND ARG --FLAG
kubectl  get    pods   --namespace=default
```

程序名、动作、名词、修饰语，逐词对应。Command 结构体里，Use 是命令名，Short 与 Long 是两级帮助文本，RunE 是执行函数，Args 是参数校验器;AddCommand 把子命令挂到父节点上，实现上不过是设置 parent 指针、向子命令切片追加。执行时，ExecuteC 先递归回到根节点，再用 Find 沿 argv 逐层向下匹配，命中后依次走过 PersistentPreRunE、PreRunE、RunE、PostRunE 的钩子链——全局前置逻辑（比如加载配置）与局部执行被干净地分层。kubectl、docker、GitHub CLI、hugo、etcd 全部站在 Cobra 上，它是 Go 生态当之无愧的事实标准。

Cobra 还顺手解决了补全问题，方案是三层架构:ValidArgs 静态声明候选词;ValidArgsFunction 是动态回调，可以实时查询 API Server 或文件系统;GenBashCompletionV2 负责生成 bash/zsh/fish/powershell 的补全脚本。最有意思的是它的运行时协议:生成的脚本里并不包含任何候选词，只注册一个补全函数——用户按下 Tab 时，shell 反过来调用你的二进制，带着一个隐藏子命令 __complete 与已输入的参数，由程序在命令树上实时算出候选词和 ShellCompDirective 指令，再交还 shell 展开。补全是活的。这个设计，我们稍后会在一个意想不到的地方与它重逢。

Rust 的答案是 clap，作者是 Kevin K（网名 kbknapp），2015 年发布首个版本，灵感来自 C 与 Python 的 argparse 传统;v2 加入 YAML 定义，v3 重写解析核心，v4（2022 年）把 derive 宏确立为主力接口。clap 的哲学是类型:命令行结构不该用命令式代码描述，该用类型声明描述。你定义一个 struct，给字段标上 #[arg(short, long)]，编译期的过程宏读取字段名、字段类型与文档注释，生成整套 Builder API 调用代码。最漂亮的是类型即行为:bool 字段自动变成开关，Option<T> 变成可选参数，Vec<T> 变成可重复参数，u8 加上 action = Count 变成 -vvv 式的计数;文档注释 /// Load config from file 直接成为 help 输出里那一行说明。Parser、Args、Subcommand、ValueEnum 四个派生宏分别负责顶层入口、可复用参数组、子命令与枚举取值。在 clap 的世界里，解析器是编译器附赠的赠品，类型错误在编译期就死掉了，运行时零声明成本。

Python 的答案是 Click（2014 年，作者 Armin Ronacher，也是 Flask 的作者)，哲学是函数:函数即命令。@click.command() 把一个普通函数包装成 Command 对象，函数体存为 callback;@click.option() 把选项注册进该命令的 params 列表;@click.group() 定义命令组，而 group 的 command 装饰器创建子命令后调用的正是 add_command——仔细看，这与 Cobra 的 AddCommand 是同构操作，只是从方法调用变成了装饰器语法糖。Click 的独门武器是上下文:@click.pass_context 把 ctx 对象传进函数，父命令把解析结果放进 ctx.obj，子命令随手可取——全局选项只解析一次，整棵命令树共享一份状态。

Node 生态给出的是第四种活法:Commander.js。它资历比 Cobra 还老，后来 Cobra 的设计也明确参考过它;它不发明新哲学，而是把链式声明磨到顺手——program.command() 返回一个 Command 对象，description、option、action 一路点下去，声明读完，命令树也建好了。我的 yuangs CLI 就是它的用户。四个生态、四种口味，但真正耐人寻味的不是它们的不同，而是它们的相同:没有谁试图重新发明短选项语法，没有谁敢让帮助文本变成可选的装饰，也没有谁不把补全当回事。解析器这个物种演化了四十年，分化出的所有分支都长在同一副骨骼上。

选型时把三家摊开对比，骨架差异一目了然:

| 维度 | Cobra（Go） | clap（Rust） | Click（Python） |
|---|---|---|---|
| 核心抽象 | 命令树 | 类型 | 函数 |
| 声明方式 | 结构体加方法调用 | derive 过程宏 | 装饰器 |
| 帮助文本来源 | Short 与 Long 字段 | 文档注释 | help 参数 |
| 补全方案 | 生成脚本加隐藏命令 | 内置生成 | 生态插件 |
| 生态代表 | kubectl、docker、hugo | bat、ripgrep | Flask、black、httpie |

这张表没有“谁最好”的答案——三个生态的答案其实是被各自语言的编译模型决定的:Go 的惯例是组合与方法，就长出运行时建树;Rust 有过程宏，就把声明挪到编译期;Python 一切皆对象，装饰器便是天生的声明位。技术选型的深层逻辑，往往在语言的地基里就已经写好了。

回头看，三场革命的真正纲领其实是一句话:单一事实来源。命令的名字、参数的类型、帮助的措辞、补全的候选、错误的建议，全部从同一份声明派生——Cobra 的 Command 结构体如此，clap 的 struct 如此，Click 的装饰器堆栈也如此。声明式之前，这些信息散落在解析代码、帮助函数、补全脚本三处，彼此靠注释和默契保持一致;声明式之后，它们成了同一份数据的四种视图。这既是工程胜利，也是 UX 胜利:视图同步了，体验才可能一致。

三种哲学，树、类型、函数，把同一个问题切成了三块:Cobra 优化的是命令的组织，Clap 优化的是参数的正确，Click 优化的是代码的简短。但它们在底层全然一致——全都遵守 getopt 与 POSIX 约定，全都自动生成帮助文本，全都内置补全方案。革命党人换了三拨，宪法还是 1980 年那部。

## 7.3 帮助文本即产品:UX 的第一公里

现在可以摆出本章的核心论点了:三个解析器真正的产品不是解析——解析是一个已经被解决四十年的旧问题——而是帮助文本。想想用户与一个陌生 CLI 的第一次接触:不是读文档（没有人在安装软件之前读文档），不是读源码，而是敲一遍 help 旗标。这一屏输出决定了用户是留下来还是转身卸载。CLI 的第一 UI 是 help 输出的排版——这不是修辞，是用户行为统计。

帮助文本首先是活的文档。传统文档最大的原罪是失同步:代码改了，README 忘了改，用户按文档操作报错，信任当场归零。而 help 输出由解析器从声明直接生成——Cobra 的 Short 与 Long、clap 的文档注释、Click 的 help 参数——声明与展示是同一份数据，结构上不可能失同步。我在维护自己的 CLI 时最深的体会之一:凡是手写两次的信息，一定会不一致;帮助文本把“写两次”压缩成“写一次”，这是它作为文档形态的根本竞争力。

其次是信息架构。成熟的帮助文本有自己的版面学:第一行是 Usage，用法公式的具体化，一眼给出这个命令吃几个位置参数;然后是命令分组，GitHub CLI 把上百个子命令按主题分区，分区之间的空行就是版面的段落;再往下是示例块，一个好示例胜过十行说明文字;最后是 Global Options 与本命令 Options 的分区——全局选项和局部选项混排，是新手最常迷失的地方。所有现代解析器默认两列排版:选项列与说明列对齐，并自动按终端宽度折行。80 列的幽灵在这里再度显形——帮助文本的版面宽度至今跟着终端走，1960 年代打字机的纸张宽度，仍在给 2020 年代的 UI 排版让路。

错误提示是帮助文本的暗面，却更能看出一个工具的用心。用户敲错命令时，差的 CLI 打印 usage 然后退出，好的 CLI 会追问一句:Did you mean this?Cobra 按编辑距离（Levenshtein distance）计算相似度，把最接近的候选命令列出来;clap 内置同样的 did you mean 机制，敲错长选项时给出建议。这一句话的实现成本极低，体验收益极高——它把报错从句号变成了逗号，用户的操作流没有被截断，而是在原地获得了一个修正选项。我的 errors.ts 里也为此留了一个 suggestions 字段，错误对象自带建议数组——同一个思路在自己代码里的落点。

错误提示值得再多说两句，因为它是比 help 更前置的第一公里:用户撞上报错的瞬间，往往连 help 都没想过要敲。好的错误信息有三段式结构——发生了什么（unknown command）、为什么（多半是拼写）、下一步怎么办（给出最接近的候选）。这与产品设计里“可操作的反馈”是同一件事:只宣布失败的系统是把责任推回给用户，给出下一步的系统才是在与用户合作。Cobra 在命令没找到时会列出全部近似项，clap 会为每个拼错的旗标注明不存在但存在相似的——这类细节在 issue 区攒下的好感，最终都会变成项目的星标。

帮助文本与 man page 的分工也值得说清。man 是 1971 年与 Unix 同龄的发明，是完整的参考手册——但它按手册的体例组织，新手想找“最常见的三个用法”得通读全文。help 旗标则是速查卡:要点式、带示例、与当前命令的上下文精确对应。两层之间的缝隙催生了 tldr 们的兴起——社区维护的“带例子的简化手册”，专门服务“我只想知道这个命令最常见的那三种用法”的场景。我的一线经验是:现代 CLI 普遍把 man 置于次要地位，把 help 做到极致，因为前者容易失同步，后者由代码生成、永远精确。文档的下沉是一条清晰的演化线:man（参考）到 info（超文本，失败了）到 help 旗标（速查）再到 tldr（示例库），每往下一层，就离“用户此刻的具体困惑”更近一步。

帮助文本还有自己的分层学。成熟的 CLI 会区分求助的深度:敲主命令的 help 看总览，命令名加 help 看这一条命令的详解，敲一个手册子命令（git 的做法）进入完整文档。三层内容严格递增，而不是同一份文本的三个拷贝。GNU 工具链还试过一个更极端的层次:info 手册，想用超文本链接取代 man 的翻页——结果连它自己的导航都要先学十分钟，成了文档 UX 史上有名的一次失败。它的失败恰好反证了帮助文本的第一原则:用户已经在困惑里了，别再让他学一套新的导航系统。

排版细节里藏着工具的段位。其一，选项排序:按字母序稳定可预测，但会埋没高频项;按重要性排序贴心，但每次新增选项都要人为维护一次顺序——主流解析器多半选了默认字母序、允许手工干预的折中。其二，占位符的写法:大写的 PORT、FILE 是 Unix 传统的“这里填什么”信号，沿用至今。其三，颜色的节制:现代 CLI 会在帮助文本里给命令名与旗标上色，但成熟产品都很克制——帮助文本要能通过管道喂给 grep，颜色必须能一键关掉。这最后一条，正是 7.5 兼容税的又一处伏笔。

最后是可测试性。把 help 的输出快照进版本库做测试，是 CLI 项目的常见实践:帮助文本变了，测试就红，评审时一眼可见。这等于给 UI 写了大纲——帮助文本从此不是装饰，而是接口契约的一部分。当帮助文本成为契约，排版就不再是美学问题，而是 API 设计问题。这就是为什么我说三个解析器真正的产品是帮助文本:解析只发生在进程生命周期里的一瞬间，而帮助文本面向的是用户生命周期的一生。

## 7.4 我的实战:yuangs CLI 的命令治理

轮到我自己了。yuangs 是我的个人命令行工具，TypeScript 写成，选型时我没有自研解析器，而是选了 Commander.js v13——Node 生态里与 Cobra 血缘最近的框架（Commander 的资历其实比 Cobra 更老，Cobra 的设计明确参考过它）。这个选型背后是一个朴素的判断:命令治理的难点从来不在解析，在组织。

我的命令组织是双轨制。打开 src/cli.ts，能看到 12 个内联注册的 .command()——ai、list、history、macros、completion 这些门面命令，体量小、调用频率高，直接写在主文件里;同一个文件的底部，则是一排 register 调用——capability、registry、explain、replay、skills、preferences、config、ssh、router、git，十个领域模块各自向 program 注册自己的子命令树，src/commands/ 目录下共 39 个 TypeScript 文件。为什么分两轨?门面命令是入口的入口，要求一眼可见、极少改动;领域命令各有子命令、选项与内部状态，全塞进主文件会把 cli.ts 变成几千行的怪物。命令治理的第一课与代码治理同构:按变化频率分文件，而不是按字母序堆文件。

文档侧，docs/ 目录下恰好 48 篇 markdown，与命令体系一一对应——每条产品线（能力、注册表、回放、技能、SSH）都有独立的文档目录，文档树就是命令树的镜像。我给自己立的规矩是:一个子命令如果没有对应的文档与帮助文本，它就不该存在。这不是洁癖，是把上一节“帮助文本即产品”的论点落到仓库结构上——help 旗标面向用户此刻的三秒钟，docs 面向用户三天后的深度使用，两层必须同构，否则任何一层失真都会被另一层立刻暴露。

docs 目录下还藏着治理的时序:48 篇文档不是一次写完的，是跟着命令一起长出来的——每新增一条产品线，先建文档目录，再写注册函数，最后才写实现。这个顺序反过来就是灾难:实现一旦先行，文档就永远排不上优先级，因为“能跑”永远比“能读”显得紧急。我还给补全装了一层项目语境:补全函数会向上逐级查找 package.json 或 .git 来定位项目根，候选词因此知道用户正站在哪个仓库里——从静态名单到上下文感知，这是补全质量的一条分水岭。

历史命令的交互是另一处小实验:history 子命令列出最近执行过的 AI 问答，允许按序号选择重放，但重放前必须过一遍 confirm 确认门;再加一个 last 简写旗标可以一键执行上一条，同样要确认。设计动机很直接:重放的是历史命令，可历史里的上下文早已变化，那个确认键是用户唯一的安全带。这类设计在 9.4 章还会展开——到那里问题会升级成:当 AI 有了执行权，确认门应该设在解析器的哪一层。

补全协议是本章最让我会心一笑的部分。yuangs 里有一个隐藏命令 _complete，接收 JSON 编码的 argv 数组（words）与光标所在词的序号（current）两个隐藏参数，调用 complete() 计算后把候选词逐个输出到 stdout——安装补全时，写进 shell 配置的脚本只负责一件事:把 Tab 键转成一次这样的进程调用。写完这个协议，我后来翻 Cobra 的源码才发现，它的 __complete 与我的 _complete 几乎逐字同构:shell 触发、隐藏命令接收 JSON 化的 argv、程序在命令树上计算、stdout 返回候选词。两个素不相识的作者，在两个语言生态里，收敛到了同一个协议——这不是相互抄袭，而是这个问题只剩这一个合理解法:补全必须由程序自己算，因为只有程序自己知道当前上下文里哪些候选词合法。

配置优先级是命令治理的另一根支柱。ConfigService.ts 里有一行注释写明合并顺序:defaults < project < user < env < overrides——内置默认值、项目配置、用户全局配置（~/.yuangs.json）、环境变量、命令行覆盖，五层依次盖过。比顺序更关键的是 sourceMap:每个顶层字段都记录自己的来源（built-in / user-global / project / env / command-override），getSourceInfo() 让“这个值是从哪一层来的”变成一个可查询的问题。我坚持要这层溯源，因为配置系统的 bug 十有八九不是值算错了，而是“不知道哪一层覆盖了哪一层”——让优先级可解释，是给三个月后的自己留证据链。这正是行业公理“CLI > 环境变量 > 文件 > 默认值”的个人实现，只是比公理多了一层项目级配置、多了一层来源追踪。

还有两个与确认门有关的细节，算是参数解析在治理向 CLI 里的延伸。其一是 TokenPolicy:它把上下文预算分成四层治理——估算用量不超过模型窗口的 70% 直接放行，70% 到 80% 放行但预警，80% 到 100% 必须用户确认，超过 100% 强制阻断;而且估算走延迟加载，先算文件元数据、不读全文，避免“为了判断超不超预算，先把所有文件塞进内存”的自我悖反。其二是 confirm():三种模式——设了 YUANGS_NO_CONFIRM=1 就自动放行，服务 CI 与自动化脚本;非 TTY 环境（管道、重定向）自动放行并打印警告;只有交互式终端才走 y/N 提示。人类确认门的边界必须显式设计，否则自动化场景会卡死在等一个永远不会来的回车上。最后一个彩蛋:源码里有一个 YUANGS_UNLOCK 隐藏开关，注释里明写“不写文档、不进帮助文本”，实现还得防御 JavaScript 的真值陷阱——字符串 '0' 在 JS 里是真值，开关必须显式枚举 '1'、'true'、'yes'、'on' 才算打开。帮助文本有负空间，负空间同样需要治理——哪些选项不出现，和哪些选项出现，是同一门学问。

## 7.5 地层学笔记:解析器的“兼容税”

参数解析层的考古价值，在于它是全书所有地层里“税感”最重的一层。终端兼容税通常按年计息，解析器的兼容税却按选项计息——每一笔都白纸黑字，每一笔都躲不掉。

第一笔税是选项冻结。一个选项一旦发布，它就是公共 API 的一部分，而且是世界上最小的那种 API:一个字母。1980 年 getopt 定下的短选项约定，2025 年的解析器仍要照单全收;-l、-v、-h、-o 各自代表什么，在无数脚本里已经焊死;POSIX Guideline 10 给双横线赋予的“选项结束符”地位，五十年来无人敢动。删除一个选项的代价，不是发一条 changelog，而是打破全世界所有引用它的 shell 脚本。所以负责任的 CLI 不删选项，只废弃:先在帮助文本里标注 deprecated，再让旧选项悄悄转发到新实现，等上几个大版本，才敢让它退场。选项的墓志铭通常比用户的任期还长。

废弃的仪式感也有最佳实践:先让旧选项继续工作但打印一条警告，警告里写明替代者;几个版本后，警告升级为拒绝执行，错误信息里仍保留旧选项的名字与指引;再等上足够多个发布周期，才真正移除。很多老选项在大型 CLI 里存活至今不是怠惰，而是礼貌——POSIX 至今保留着 1970 年代的约定，无数 shell 脚本里还躺着上世纪的写法。税额最重的从来不是维护旧选项，而是记住为什么不能删它。

第二笔税是语义与输出的冻结。选项能改什么、不能改什么，边界在管道文化里被划得极清:脚本能 grep 你的 stdout，所以 stdout 的格式是契约;CI 在 if 里判断你的退出码，所以退出码是契约;错误信息会被人贴进 issue，所以它也是半个契约。这也是为什么成熟的 CLI 宁可新增 json 这类结构化输出的长选项，也绝不“顺手优化”人类可读输出的版式——版式是给眼睛的，契约是给机器的，两者动一个都要还债。

第三笔税是配置优先级的冻结。“命令行参数 > 环境变量 > 配置文件 > 默认值”这个顺序，是几十年实践沉淀下来的行业公理，也是十二因子应用方法论的一部分:命令行最明确所以最高，环境变量次之，文件再次，默认值兜底。谁把这个顺序反过来，谁的迁移指南就会变成道歉信。我在 yuangs 里实现的五层（defaults < project < user < env < overrides）是这条公理的细化版——多出来的项目级一层，对应的是“仓库约定优先于个人偏好”的工程共识;多出来的溯源机制，则是为了回答公理本身不回答的问题:冲突发生时，谁赢了，为什么。公理负责稳定，溯源负责可解释，两者缺一不可。

这笔公理还有一个隐藏条款:合并的粒度也是契约。深层配置是整块覆盖还是逐字段合并，两种流派各有信众——整块覆盖可预测但啰嗦，逐字段合并贴心但难排查。我的 ConfigService 选了递归深合并，这才不得不把来源证据链补上;而“用户只写了半份配置文件，剩下半份由默认值补齐”这个行为，一旦有人依赖，就再也改不掉了。兼容税的征收范围比大多数人以为的要大得多:不止选项名，连配置合并的语义、报错的措辞、退出码的编排，全都在税单上。

大版本也要交税。clap v4 借 2022 年那次改版做了大量破坏性变更，换来的是漫长的迁移指南与弃用周期——社区一边骂一边迁，因为 v3 与 v4 的 API 差异确实换来了更干净的类型推导;而 Cobra 至今保持 v1 不动大版本号，不升大版本本身就是一种兼容承诺。两种策略，交的是同一笔税，一个一次性预缴，一个分期永续。

最后回到帮助文本。help 输出是考古层的地表:用户看到的是排版与措辞，底下压着的是 POSIX 语法指南、GNU 长选项约定与 getopt 状态机。你在 help 输出里看到的每一个对齐、每一个双横线拼写、每一个 Did you mean，背后都是五十年的约定在托底。参数解析是小事里的大学问——它小到每个程序员都觉得自己随手能写一个，大到三个语言生态用三种哲学各自演化了一整个框架，最终却谁也没能绕开同一套 1980 年的语法公理。兼容在这里不是包袱，是地基:正因为选项被冻结在原地，第 9 章里那个即将接管终端的 AI Agent，才敢放心地把命令行当作自己的执行器——它调用的每一个子命令、解析的每一个 flag，都是半个世纪里从未背叛过调用者的老契约。

---

## 第8章 TUI 四重奏:四种实现，一种野心

# 第 8 章 TUI 四重奏:四种实现，一种野心

考古学里有一种地层现象叫岩脉侵入:岩浆并不推翻上覆的沉积岩，而是沿着裂缝向上灌注，在每一层里留下自己的纹理。本章要解剖的四条岩脉——Rust 的 Ratatui、Go 的 Bubble Tea、Python 的 Textual、TypeScript 的 Ink——就是四组活体样本。它们的语言、出身、哲学互不相同:一个信奉立即模式加双缓冲 diff，一个把 Elm 消息循环整个搬过来，一个在终端里自建了一台 CSS 加 DOM 的浏览器，一个干脆让 React 的协调器跑在终端上。范式之争吵得不可开交，但把四个项目挖到最深处，会发现它们踩在同一块楼板上:ANSI 转义序列与 diff 渲染。第 5 章埋下的三条线索——diff 思想、能力协商、抽象边界——本章逐一接住，先在 8.1 节把公共地基铺开，再到 8.2 与 8.3 节拆 Ratatui 与 Bubble Tea，8.4 节看通往 Web 的两条路，最后在 8.5 节给出一张可操作的选型决策树，并摊开我自己工作台上那套 TUI 编排的实证。

## 8.1 渲染的公共地基:从 curses 到双缓冲 diff

终端是一台奇怪的显示器:它没有图层，没有脏矩形，没有“把某个控件重绘一下”这种图元，它认识的动词全部写在第 2 章那张表里——定位、擦除、覆盖、变色。对全屏应用来说，这是刀耕火种:想改一处内容，要么把光标移过去就地覆盖，要么把受影响的区域整段重写;而串口时代的物理约束给每一个字节都标了价，全屏重画的账单没人付得起。第 5 章我们看过 curses 的答案:程序在虚拟屏幕里随意涂改，wnoutrefresh 把改动誊进 curscr 那本“上一帧账簿”，doupdate 拿着账簿算出最小差额，翻译成当前终端的方言。1980 年的这套流程，今天有一个更通用的名字:双缓冲 diff。

把 curses 的做法抽掉语言外壳，剩下与具体库无关的四步。第一，保留上一帧——内存里始终躺着一份“屏幕现在的样子”;第二，在内存里构造下一帧，随便折腾，没人看见;第三，逐格比较两帧，产出差额集合;第四，把差额翻译成 ANSI 字节流写出去。这是典型的空间换时间:内存翻了一倍，换来两样东西——串口时代省下的带宽，与图形时代依然要命的“不闪”。全屏重画之所以丑，是因为人眼会捕捉清屏与重写之间的空窗;diff 渲染让屏幕上绝大多数格子根本不动，闪烁就失去了来源。

不妨把这套机制放到一块三行高的玩具屏幕上看一眼。假设上一帧与下一帧只有一处不同——第一行的数字从 42 变成 43:

```
上一帧         下一帧         差额输出
CPU 42%        CPU 43%       ESC[2;5H     ←光标移到第一行第五列
MEM 61%        MEM 61%       43           ←覆盖两个字符
NET  0%        NET  0%       (其余两行:一个字节都不发)
```

三行屏幕，只发出两小段序列——这就是 diff 渲染的全部秘密，也是从 1980 年的 Rogue 到 2026 年的 AI 终端共用的那本账。图形界面让“逐像素重绘”成为理所当然，终端世界从来没有这份奢侈:它的带宽账本从 9600 波特一路记到今天，只是把“贵”的单位从秒换成了毫秒。

本章的四位主角，底座全是这四步，差别只在第三步的比较粒度:

| 框架 | 上一帧存在哪里 | diff 的粒度 | 差额如何变成字节 |
| --- | --- | --- | --- |
| Ratatui | Terminal 内的 previous Buffer | Cell 级（字符+前景+背景+修饰） | Backend 把变化格写成 ANSI |
| Bubble Tea | 渲染器手里的上一次输出 | 行级（字符串逐行比较） | 变化的行整行重写 |
| Textual | Compositor 的 Strip 缓存 | 行级（Strip 可哈希，比较哈希） | 变化的行经 Driver 写出 |
| Ink | React 提交后的输出树 | 子树级（Fiber 协调算出） | 变化子树经静态区外的擦写输出 |

这张表值得多看两眼。粒度从 Cell 到子树，一个比一个粗，也一个比一个贴近各自的编程模型:Rust 的控制力允许它一格一格抠;Go 的 Bubble Tea 让 View 每帧全量产出一整个字符串，把 diff 的脏活留给内置渲染器——渲染器在写入前做行级比较，没变的行原样跳过;Textual 干脆把“一行”做成不可变且可哈希的对象，变更检测退化成比较两个整数;Ink 最省事，把比较整个外包给 React Fiber——调和器算出哪些组件变了，Ink 只负责把变化的部分擦写在终端上。四个语言社区，用各自的脾气，把 1980 年的同一个动作重新实现了四遍。

第二件出土文物是能力协商。第 5 章说 curses 的聪明在于“程序只声明意图，价目表负责翻译”;四重奏继承了结构，却把价目表从磁盘上的 terminfo 编译条目，换成了运行时的一串问询:TERM 以什么前缀开头，COLORTERM 是否声明了真彩色，应答 DA 报出的终端身份是什么，同步输出、Kitty 键盘协议、XTGETTCAP 这些新能力对方认不认。协商的本质没变——两端先对暗号，再决定说什么话;变的是对暗号的方式:从启动时查表，变成对话式报数。一次完整的握手往往只要几个来回:应用发一组探测序列，终端逐条应答;问背景色，答深浅;问真彩，答有无——深色终端配浅色主题、浅色终端配深色主题这类自适应，在四重奏里是出厂标配。价目表的形状变了，做生意的规矩没变，第 3 章那个“换一台终端，换一份词典”的古老承诺，今天依然在兑现。

第三件是抽象边界。四个框架没有一个把 ESC[31m 硬编码进业务代码——所有 ANSI 的产出都被关在最深处的一层接口里:Ratatui 的 Backend trait（默认 Crossterm，可换 Termion、Termwiz，测试时换 TestBackend），Textual 的 Driver，Bubble Tea 的 renderer，Ink 的输出层。这个设计的红利在测试上兑现得最彻底:TestBackend 把终端换成内存里的 Buffer，断言“屏幕上应该是什么”不再需要一台真终端，TUI 的单元测试第一次可以跑进持续集成。第 5 章那句“旧抽象是否还抽象对了东西”，在这里可以给出中期评分:diff 抽象对了，能力协商抽象对了，“写字节”这个边界也划对了。

于是本章的方法论可以立起来了:往下挖，四个框架全到这一层的地基为止，再往下就是本书第二、三、五层的老地层——CSI 序列、能力数据库、两阶段刷新;往上挖，才是范式分化的地面向。地质学里，岩脉的成分由岩浆房决定，不由裂缝决定;本章的裂缝是四种语言社区各自的焦虑，岩浆房则是同一座——把 Web 二十年积累的开发体验，搬运到一块字符玻璃上。搬运路线不同，货物却惊人地一致:组件化、声明式、可测试。四条岩脉在地下汇合，地表上的四种长相，不过是同一次侵入的四个出口。接下来三节，我们逐一剖开这四条岩脉:它们如何在同一块地基上，盖出四种完全不同的房子。

## 8.2 Ratatui:立即模式的极简主义

家谱要先交代。2016 年前后，Rust 工程师 Florian Dehau 为了一个音频可视化的 side project 写了一个库，叫 tui-rs——它在 Rust 生态里服役多年，养出了第一批 Rust 终端应用，但 2021 年之后维护节奏放缓。2022 年年中，社区从 tui-rs 的主干上分叉出 Ratatui，由维护组集体接管。这次分叉是开源史的标准剧本——项目没死，只是换了户籍;而 Ratatui 也没辜负接盘的人:到 2026 年，它是两千多个 crate 的依赖对象，Netflix 的 bpftop、Amazon 的 Q CLI、Rust 生态最有名的 git 客户端 gitui，底下都是它（检索数据，2026 年年中星数约 2.2 万，是四者中起步最晚、增长最快的一个）。

Ratatui 的范式叫立即模式，这个词从游戏引擎的 Immediate Mode GUI 一脉借来，意思极其简单:每一帧，你把整个界面从零重画一遍。没有组件树，没有保留的控件对象，没有“更新某个按钮的文字”这种动词——你只回答一个问题:这一帧，屏幕上有什么?至于上一帧画了什么、这一帧与上一帧差在哪，你统统不用管，那是框架的事。这个哲学把“界面”从一系列需要维护生命周期的对象，退化成一个纯函数:状态与屏幕尺寸进去，这一帧的完整画面出来。代价是每一帧都要重新构造整幅画面，收益是心智模型简单到难以出错——状态与画面永远一致，因为不存在“忘了重画的那一块”。

具体到实现，Ratatui 的渲染管线是一条流水线:Terminal 内部维护 current 与 previous 两个 Buffer，Buffer 是 Cell 组成的二维网格;每个 Cell 四个字段——symbol（符号，通常是单个字符）、前景色、背景色、修饰符（粗体、斜体、下划线）。一帧开始，框架创建 Frame，你的代码调用 widget 的 render 方法，把各种组件画进 Buffer;一帧结束，Buffer::diff 把 current 与 previous 逐格比较，只把变化的 Cell 交给 Backend，Backend 把它们翻译成 ANSI 序列写出。注意这个结构的含义:diff 的对象不是“你的组件”，而是最终的字符格——不管业务逻辑多复杂，到了渲染层，一切都被压平成一张字符表格。Web 前端的协调器在组件树上做文章，Ratatui 直接把组件树砍了。

这套机制的观感值得一写:一个每秒刷新的时钟，画面上只有一个数字在跳，Ratatui 每帧产出的差额就是一两个 Cell，开销小到可以忽略;用户那一侧看到的，是永远稳定、永不闪烁的画面。至于框架不管的那些事，一个像样的 Ratatui 应用通常长这样:你写一个 App 结构体持有全部状态，on_key 与 on_tick 两个方法负责改状态，主循环里轮询事件、调用方法、重画——事件循环、状态容器、组件的持有方式，全是你自己的手笔。这份自由在成熟项目里长成了各种形状:有人把 Elm 架构移植过去，有人用 actor 模型分发事件，有人干脆手写状态机。框架不规定，社区就各奔东西——这是立即模式的自由，也是它的税。

一个最小的绘制闭包长这样:

```rust
terminal.draw(|frame| {
    let chunks = Layout::vertical([
        Constraint::Length(1),  // 标题栏:占一行
        Constraint::Min(0),     // 内容区:拿走剩下的全部
        Constraint::Length(3),  // 状态栏:三行,给边框留位
    ]).split(frame.area());
    frame.render_widget(Paragraph::new(title_text), chunks[0]);
    frame.render_stateful_widget(table, chunks[1], &mut table_state);
})?;
```

闭包里没有 begin 与 end，没有 add 与 remove——你把这一帧该有的东西挨个画上去，函数返回，Ratatui 去算差额、写终端。

Backend trait 值得单独一列，因为它是第 5 章第三件事——抽象边界——的标准答卷。trait 把“把 Cell 变成字节”留成可替换的接口:生产环境默认 Crossterm，也可选 Termion 或 Termwiz;测试环境换 TestBackend——它在内存里模拟一台终端，渲染与输出全部可断言。我看它的官方示例时印象最深的一点是:大量“渲染结果”就是拿 TestBackend 的内存截图与期望的字符画逐行比对——TUI 测试第一次长得像快照测试，而且不需要 X11，不需要伪终端，持续集成里裸跑。

布局是 Ratatui 的暗器。它既没有手算坐标，也没有照抄 flexbox，而是埋了一个约束求解器——Cassowary（Rust 生态由 kasuari crate 提供，这个算法是 1990 年代为图形界面约束求解而生的一门老学问）。你用 Constraint 声明愿望:Length(1) 要一行，Percentage(30) 要三成，Min(10) 至少十行，Ratio 与 Fill 处理比例与填满;再用 Flex 声明剩余空间的分法——靠左、居中、靠右、两侧留白、均匀分布。愿望彼此冲突时，求解器按约束的强弱找一组“最不委屈”的解。举个真实的分栏:顶部工具条要一行，中间列表至少十行，底部状态栏三行，剩余空间全给列表——写成几条约束扔给求解器，窗口拉宽拉窄它都给出一组合理解;换作手算坐标，每一次窗口缩放都是一次新的 bug。终端分栏，被形式化成一个带优先级的线性约束优化问题——四重奏里最像“真排版引擎”的部件，偏偏藏在最朴素的框架里。

2025 年 12 月的 v0.30 被官方称为“史上最大发布”:支持 no_std（渲染核心可以跑进嵌入式设备）、workspace 模块化拆分、新增 ratatui::run() 一行起屏;渲染侧则把 diff 这门手艺继续往深里打磨——引入迭代式 BufferDiff（逐格产出更新，不必一次性算完整个矩阵）与 CellDiffOption（让 widget 声明某些格子不参与 diff，解决超链接转义序列与宽字符带来的幂等性难题）。四十年前的 diff 思想，在这里仍然有工程前沿可挖。而它的哲学从头到尾没变过:状态管理、事件循环、组件复用，框架一概不管。极简主义的极简，是责任范围的极简——我只做最后一步“把字符表格变成字节”，其余的，你自己搭。这句免责声明，就是 8.5 节选型时它身上最重的那个砝码。

## 8.3 Bubble Tea:Elm 架构的 Go 表达

2012 年，Evan Czaplicki 发表关于 Elm 语言的论文，随语言一起提出了一套架构:Model-Update-View，单向数据流。状态只存在 Model 里;界面上发生的任何事——按键、点击、时钟、网络应答——都变成一条消息（Msg）送进 Update;Update 纯函数式地返回新状态;View 把状态渲染成界面。数据只朝一个方向流，不存在双向绑定，不存在“谁改了状态”的悬案。这套架构后来被 Redux 抄了作业，一度统治整个前端;2020 年，Charmbracelet 团队把它搬进 Go 的终端，起了个俏皮的名字 Bubble Tea。它 2024 年才摸到 1.0，2026 年 2 月 23 日发布 v2.0——诞生六年来第一次破坏性变更，这种两年才敢破一次兼容的克制，本身就是终端文化的美德（检索数据:v2 发布时星数约 4.4 万，四者中最高;公开统计里建在它上面的项目超过一万八千个）。

Bubble Tea 的全部 API 哲学，浓缩在一个接口里。你实现一个 Model，提供三件事:Init，启动时要做什么;Update，收到消息怎么办;View，界面长什么样。Update 的签名是整个框架的灵魂:Update(msg) 返回一对值——新 Model，外加一个 tea.Cmd。Cmd 是副作用:发起一个 HTTP 请求、读一个文件、等一个计时器。关键在执行方式:框架把它扔进独立的 goroutine 里跑，完成后把结果包装成一条新的 Msg，从 channel 送回主循环。

这个设计精确地骑在 Go 的语言模型上。状态更新是单线程的:所有 Msg 进同一条队列，Update 一次只处理一条，Model 不需要任何锁——消息循环保证了状态安全。副作用是并发的:goroutine 天生就是干这个的;tea.Batch 把一串命令并行发射，tea.Sequence 把它们串成队列。于是“单线程状态加并发副作用”这个在别的语言里要自己造的架构，在 Go 里是三行 API 的事。我把这看作四重奏里范式与语言咬合最紧的一对齿轮:Elm 的架构是语言无关的，但它的 Go 表达，几乎像是专门为 goroutine 预约的。

这套架构还有一个常被低估的红利:可测试性。Update 是纯函数——给定 Model 与 Msg，答案唯一;测试一段交互逻辑，不需要伪终端，不需要真渲染，构造一条消息喂进去，断言出来的 Model 即可。再进一步，把消息流录下来回放，就是 Elm 社区玩了几十年的时间旅行调试——在 Bubble Tea 里同样成立。前端世界为这套架构积累了十几年方法论，Go 社区拿来就用，一分学费不交。

一段典型的 Update 长这样:

```go
func (m model) Update(msg tea.Msg) (tea.Model, tea.Cmd) {
    switch msg := msg.(type) {
    case tea.KeyMsg:
        if msg.String() == "ctrl+c" {
            return m, tea.Quit
        }
    case tickMsg:             // 从 goroutine 回流的定时消息
        m.count++
        return m, tickCmd()   // 再派一个副作用,循环往复
    }
    return m, nil
}
```

渲染上，Bubble Tea 走的是“View 全量产出”路线:View() 把整个界面渲染成一个字符串，内置渲染器在写入前做行级比较，没变的行跳过，变了的重写;alt-screen 模式一键切进第二块屏幕;v2 又把整帧写入包进同步输出区间（终端世界的 DEC 2026 模式），压掉高刷终端上的撕裂，检索数据称刷新性能因此有数倍提升。有意思的是样式不归框架管:边框、对齐、颜色、圆角，全部由姊妹库 Lip Gloss 负责——你甚至可以只用 Lip Gloss 不用 Bubble Tea，给一条传统 CLI 穿衣服。生态全家桶还有 Bubbles（现成组件:进度条、文本框、spinner）与 BubbleZone（鼠标分区）;最值得单独一提的是 Wish——一个 SSH 服务器库，把你的 TUI 直接挂在 sshd 后面:用户 ssh 上来，看到的界面就是你的应用。第 6 章那对主从设备在这里完成了一次华丽转身:PTY 不再只是承载本地终端，而是承载一个远程交付的产品。第 6 章写过我在牢笼里反推终端的 wsstunnel;Wish 是同一个原语的商业镜像——不是把 IO 推出去，而是把整个界面搬进来，让 SSH 成为 TUI 的分发渠道。基础设施五十年没换，用法却可以翻新，这是兼容税付得值的一次典型回本。

Bubble Tea 的工程气质与 Ratatui 恰好互补:它管得多——事件循环、并发、生命周期、重绘，你全不用碰;它也管得少——布局没有声明式系统，复杂界面靠字符串拼接与手工换行，写过大界面的人都体会过那种“用 printf 摆棋盘”的辛酸。架构清晰、上手飞快、并发好写，代价是界面复杂度的天花板来得早。8.5 节的决策树里，这将是它与 Ratatui、Textual 分道扬镳的那条线:如果你的界面是“一条消息流加几个控件”，它是最快的路;如果要摆一张会重排的仪表盘，就得掂量掂量了。

## 8.4 Textual 与 Ink:两条通往 Web 的路

四重奏共同的野心，是把 Web 时代的开发体验搬进终端;本节的两位把野心写在了脸上。路有两条:一条是自己造——在终端里建一整套 Web 的等价物，DOM、CSS、布局引擎、事件循环，全部重新发明;另一条是直接借——把 Web 的引擎原样搬进终端，让 React 与 flexbox 以为自己在写网页。前一条路上站着 Python 的 Textual，后一条路上站着 TypeScript 的 Ink。

Textual 的作者 Will McGugan 是 Python 生态里最著名的终端库 Rich 的作者:Rich 负责把“内容”画漂亮，Textual 则想把“应用”做完整。它的组件树就是一棵类 DOM 树:App 之下挂 widget，widget 用 compose() 方法声明自己的子组件，层级、类型、id、class，一一对应 HTML 的心智。样式是一门方言齐全的 CSS——Textual CSS，SCSS 风格的子集，支持 dock、grid（grid-columns 里可以写 auto 与 1fr）、flex、padding、border;选择器支持 #id、.class、> 子元素。你写一个样式文件，布局随改随生效，不用动一行 Python——这句话在 Web 前端是日常，在终端圈是科幻。

渲染管线是 Textual 的独门手艺。每个 widget 的 render() 产出 Rich renderable;Compositor 把所有 widget 的输出合成为一行行 Strip——每行一个 Strip，内部是带样式的 segment 序列;点睛的一手是 Strip 被做成不可变且可哈希:行内容一旦算出哈希，变更检测就退化成比较两个整数，哈希没变的行直接跳过。行级 diff 本身不稀奇，curses 时代就有;稀奇的是拿“不可变性”给 diff 打的这份工——函数式社区最爱的武器，被调来优化终端重绘。再往下，Driver 把变化的行翻译成 ANSI，写出去。

组件仓库也按 Web 的口味备货:数据表格、树视图、Markdown 渲染器、标签页、输入框与开关，官方内置四十多个;滚动、焦点、快捷键全部内建。有人估算过 Textual 的体量——布局、样式、渲染、事件加在一起，已经是一个小型窗口子系统的规模，“终端里的浏览器”不是比喻，是工程预算。这也解释了它为什么需要一个公司来养:免费的开源里，藏着一个昂贵的野心;而它敢做终端加浏览器双端的底气也在于此——反正整台浏览器都是自己造的，换一种运行时把它跑起来，只是加一个出口。

运行时压在 asyncio 上:每个 widget 都是一个 MessagePump，按键、点击、定时器，一切皆消息，按队列串行处理;长任务必须用 run_worker 丢进后台——谁敢在消息循环里睡一觉，整个界面跟着卡。数据流有一条成文的约定:Attributes down、messages up——父组件通过设置属性向下传值，子组件通过发消息向上汇报，与 React 的 props down、events up 隔空握手。现状值得如实记录:2026 年 5 月的 v8.2.7 还在加入 Kitty 键盘协议支持，发布节奏从没慢下来;但母公司 Textualize 已于 2025 年 5 月关停，Will 以开源项目的方式继续维护（检索数据:星数约 3.6 万）。它还有一张别人没有的牌:textual serve，同一套代码既跑在终端里，也跑在浏览器里（WebAssembly 编译）——四者中唯一的终端加浏览器双端方案。

Ink 选了另一条路:不造引擎，直接搬。它实现了一个自定义的 React renderer——对 React 而言，Ink 只是又一个“渲染目标”，与 ReactDOM 平级;React Fiber 的协调器照常运转，它以为自己调和的是 DOM 节点，实际产出的是终端布局。布局计算外包给 Yoga——Meta 开源的 flexbox 引擎，React Native 同款;display 那一套原封不动地跑在终端上。你用 JSX 写界面，useState 与 useEffect 照用，npm 生态照用——一个前端工程师从网页切到终端，学习成本接近于零。这是“把 Web 开发体验搬进终端”这个野心最彻底的一次兑现:搬的不是体验，是引擎本身。

Ink 的词汇表也刻意做得极小:核心组件只有一个 Box——一个 flexbox 容器，方向、边距、对齐全靠属性声明;文本与颜色交给几个基础元素。用它搭界面，像回到 div 加 span 的远古前端，但心智模型一寸没变——变的只是渲染目标。

Ink 的渲染策略是一种立场:静态渲染加 LogUpdate 式输出。CLI 的历史输出——日志、命令结果——进入一次性静态区，写完就留在滚动缓冲里供用户回看;只有活动区（spinner、进度条、交互表单）被反复擦写。这决定了 Ink 的甜区不是全屏应用，而是行内应用——恰恰是 AI 时代最重要的 CLI 形态:一条对话流，历史不可篡改，活动区持续刷新。这个设计还有一层工程理由——终端的滚动缓冲是用户的私有领地，对话记录必须能往上翻、能复制、能检索;把历史输出钉死在静态区，是 Ink 对“终端是文档”这一古典传统的致敬。全屏 TUI 反而做不到:第二块屏幕里的内容，滚轮是翻不到的。事实也确实如此:Claude Code、Gemini CLI、GitHub Copilot CLI，这一代 AI CLI 的渲染层几乎清一色建在 Ink 上，Shopify CLI 与 Cloudflare Wrangler 这类云厂商工具也在用户名单里（检索数据:v7 发布于 2026 年 4 月，要求 Node 22 与 React 19;周下载两三百万;星数约 3.9 万）。但要如实记录风险:作者 vadimdemedes 已进入维护模式——大功能不再开发，社区 PR 仍在合入，项目未废弃。《终端觉醒》第 7 章把 Ink 的渲染引擎拆到了字节层，两本书在此互文并会师于同一个结论:Ink 底下没有新协议，还是 ANSI 与重绘。

两条路，一个方向。Textual 自建，换来完全可控与双端交付，代价是一个团队要维护一整台“终端里的浏览器”——公司为它成立，又为它关停;Ink 借宿，换来零学习成本与整个 npm 生态，代价是把命运绑在宿主的版本表上——React 升级，Ink 跟着升级，宿主打喷嚏，渲染层感冒。范式无高下，选型见真章——这正是下一节要做的算术题。

## 8.5 选型决策树与我的实践

范式之争没有裁判，选型问题可以有。我把四个维度摆开:语言、生态、布局复杂度、学习曲线，然后先交底:大多数时候你没有第一维的自由——TUI 框架绑死语言，Rust 项目不会为了 Textual 换 Python。所以决策树的根不是“哪个框架好”，而是“我正在用什么语言”:Rust 选 Ratatui，Go 选 Bubble Tea，Python 选 Textual，TypeScript 与 JavaScript 选 Ink。语言定了，后面三维才真正开始比。把四维压短一点就是四句话:先认语言，再看布局，量力学习，核对生态。选型失误的代价并不对称——语言选错返工最贵，布局估错最常见，学习曲线只疼一阵，生态风险却在产品长寿时才爆雷;所以顺序重要:先排除结构性错误，再打磨偏好。

第二维是布局复杂度，这是四个框架分化的主轴。界面上只有“一行提示加几个控件”——进度条、确认框、单选菜单——四个都胜任，选最顺手的;要摆表格、分栏、做仪表盘，Ratatui 的 Cassowary 与 Textual 的 CSS 是正解，Bubble Tea 的字符串拼接会开始还债;要响应式重排——窗口一变，复杂层级跟着重排——Textual 的 CSS 引擎最接近这个需求的原生形态;要行内流式输出——AI 对话、构建日志，历史不可改、活动区持续刷新——Ink 的静态渲染就是为这个场景设计的，Bubble Tea 的行内模式是 Go 生态的对应物。一句话概括:全屏重布局看 Ratatui 与 Textual，行内流式看 Ink，Bubble Tea 居中。

第三维是学习曲线，定价规则很简单:按你已经会的东西收费。会 React 的前端工程师写 Ink，一下午出活;写过 Redux 或 Elm 的，Bubble Tea 的 Update 循环一眼即懂;写过 CSS 的，Textual 几乎没有新概念;什么都不沾边的后端工程师，Ratatui 反而最诚实——它不假装帮你管状态，你从第一天就知道自己要搭什么:事件循环、状态容器、组件函数，搭完这些，Ratatui 给你的是最硬的渲染底座。反过来说，四者中最危险的错觉是“不用学 Web 也能白嫖 Textual 与 Ink”——它们的抽象全部来自 Web，选择器、协调、flexbox 这些概念不是可以绕开的实现细节，而是使用它们的母语。

第四维是生态与可持续性，这里必须如实引用 2026 年年中的检索数据，包括风险项。Bubble Tea:Charm 公司商业维护，组件库最全，v2 刚发，四者中最稳;Ratatui:增长最快，no_std 把它送进嵌入式，维护组集体共治，单点风险低;Textual:项目本身高频迭代，但商业公司 Textualize 已于 2025 年 5 月关停——选它等于签字接受纯社区维护，生命周期长的产品要多想一步;Ink:维护模式，新功能别指望，但作为稳定渲染层反而可靠——AI CLI 们要的恰恰是“不变”。这一维没有普适答案，只有与你产品寿命的匹配度。压成一张速查表:

| 你的处境 | 我的选择 |
| --- | --- |
| Rust，要性能与控制力，愿意自搭状态与事件 | Ratatui |
| Go，要快速做出漂亮的交互式 CLI | Bubble Tea |
| Python，要复杂响应式界面，或终端加浏览器双端 | Textual |
| TypeScript，前端团队，行内流式的 AI CLI | Ink |

其实还有第五维，表上写不下:交付形态。界面要发给异地的运维同事，挂在 sshd 后面当远程工具——Bubble Tea 的 Wish 现成;要一份代码同时服务终端与浏览器——textual serve 现成;要嵌进既有 Node 工具链、与 AI CLI 共存——Ink 是主场。四重奏各自攒下的场外招，经常在选型表的最后一行起决定作用。

然后是我自己的桌子。我日常的 AI 工作台入口是一个 175 行的 shell 编排脚本 launch-tui.sh:它先确认配套的 web 服务（默认 3080 端口）在监听，不在就后台拉起;再找到当前工作区最近的一份会话;最后启动 TUI（一个 Node 程序）带 resume 参数续接会话——TUI 与 web 读写同一份会话存储，我在终端里聊，在浏览器里翻，读的是同一本账。脚本里甚至有一段注释专门解释为什么结尾不能用 exec——exec 会替换掉 shell 进程，退出时的清理函数就再也不触发;编排层的每一行，都在为“两个前端共享一本账”这件事服务。它的界面库不是四重奏里的任何一家，而是 TypeScript 生态一个体量不大的极简库 pi-tui。选它的理由恰好是本章的道理:我把它的渲染源码读过一遍，核心就是行级差分——每一帧与上一帧逐行比较，记下 firstChanged 与 lastChanged，只重写这一段;首帧全量，宽度变化才整屏重画（因为换行规则全变了）;整帧写入包在同步输出序列（\x1b[?2026h 与 \x1b[?2026l）之间，压掉高刷终端上的撕裂。我逐条对过:diff 思想，有;能力协商，有——它探测 OSC 11 应答里的背景色与配色方案，再决定用什么颜色说话;抽象边界，有——组件接口与终端写入分层。第 5 章埋的三件事，在一个四重奏之外的库里一件不缺。这不是巧合，这是地基的证明:任何认真做渲染的终端程序，最后都会自己长出这三件器官。

同一个脚本里还留着一条教训。我曾为“web 与 TUI 并发写同一会话”自研过一个协调器:启动时复制会话副本，TUI 退出时合并回去。2026 年 8 月我把它停用了——它对 zstd 压缩帧做“找下一个魔数”式的朴素扫描，帧边界误判，合并副本时丢数据，会话日志出现大面积空洞;而官方持久层早已内置 O_EXCL 文件锁，并发互斥本来就有保证。编排层想帮忙，结果帮了倒忙——别替已经存在的锁再发明一把锁。另一个注脚来自 ~/ygs/Tmux-FSM，我那个用 CRDT 同步 tmux 状态的实验:项目里的界面层只是一个桩——那台机器的主要矛盾是状态同步，不是渲染。TUI 项目做到后头你总会发现:渲染是最先被解决的部分，状态才是真正难缠的。

### 地层学笔记

第 5 章埋下的三条线索，本章全部出土，清点如下。第一件，diff 思想:四重奏的粒度有 Cell、有行、有子树，最深处却是同一个动作——拿下一帧对照上一帧，把差额变成字节。wnoutrefresh 与 doupdate 的直系后裔分布在四个语言、四个代码库、四套范式里，连 TestBackend 那台“内存终端”都是 curscr 账簿的转世。第二件，能力协商:价目表从磁盘上的 terminfo 条目搬进了运行时——COLORTERM 报真彩，DA 应答报身份，XTGETTCAP 代答能力，同步输出模式报“我会防撕裂”;协商没有消失，只是从启动时查表，变成了终端与应用的对话式报数。第三件，抽象边界:Backend trait、Driver、renderer、输出层——四家不约而同把“最后怎么写字节”关进小房间;边界划对了，红利用测试结算——终端可以被内存假货替换，TUI 测试从此跑进持续集成。

分歧是真实的:立即模式、Elm 消息循环、CSS 加 DOM、React Fiber，四种范式决定你的代码长四种样子，而且互不兼容——这是楼上的争吵;共同也是真实的:向下，全部归结为 ANSI 转义序列;向内，全部归结为 diff 渲染——这是楼下的地基。四重奏的野心是把 Web 时代的开发体验搬进终端;野心基本实现了，实现的姿势却很诚实:楼上盖了四栋风格迥异的房子，楼板还是 1980 年代 curses 浇好的那一块，再往下的地基，打得更早。我们又一次给五十岁的协议发新工牌——四个框架，四种工种，工号却都注册在 ANSI 与 diff 名下。地下没有新大陆，只有新的盖法:这句话，就是考古层的定义，也是下一章的入口——当 AI 接管终端时，它脚下踩着的，依然是这几层。

---

## 第9章 AI 时代的 CLI:古典循环的加盖工程

# 第 9 章 AI 时代的 CLI:古典循环的加盖工程

## 9.1 古典循环的三击键:命令、执行、输出

挖掘进行到最后一层之前，先把脚手架在原地搭好。这九章里我们一路向下:看过打字机如何定义 80 列，看过 ESC 三个字母如何统治屏幕，看过词典如何收录两千种终端方言，看过行编辑如何在内核与用户态之间搬了一次家，看过 curses 如何把全屏变成一门科学，看过 pty 如何让远程成为可能，看过解析器与 TUI 框架如何在同一块地基上盖出不同的楼。现在，该给这座建筑请来最新的住客了。但在开门迎客之前，我们要先丈量它脚下的楼板——那个五十年没变过的“命令-执行-输出”古典循环，本章全部故事的承重结构。

它的契约朴素到几乎不值得写进文档:你敲一条命令，按一次回车;shell 替你 fork 一个进程，把可执行文件装进内存，把三个文件描述符——标准输入、标准输出、标准错误——摆到它面前;进程把文本写给输出与错误流，退出时留下一个 0 到 255 之间的整数，0 表示成功，非零表示各种死法;然后 shell 打出下一个提示符，等你再来一遍。命令、执行、输出——三次击键，一次授权，一张不撒谎的收据。1971 年的第一版 Unix 手册写的是这个契约，1989 年的 bash 是，2026 年的 zsh 还是。

这套契约的可怕之处在于可组合性。管道把上一条命令的输出焊接到下一条命令的输入，退出码配合条件连接构成分支微积分:

```bash
grep -l TODO src/ | xargs wc -l   # 管道:前者的 stdout 就是后者的 stdin
set -euo pipefail                 # 脚本开头:任何一环失败,全链止损
```

小命令各做一件小事，文本是它们的通用语言，退出码是它们的公共语法:找到了还是没找到、跑完了还是中途暴毙、有产出还是空手而归，每一步的成败都被折算成一个整数，供下一步决策。第二级的 grep 永远不必知道第一级的 grep 是谁，它们只凭 stdout 与退出码这两个五十年不变的接口缔约——第七章讲过的 Unix 哲学，物理基础就是这个契约足够硬。

契约里最常被忽视的条款，是退出码的语法。它只有八位，却有一整套约定俗成的档位：0 表示成功，1 表示一般失败，2 多用于用法错误，127 表示命令没找到，130 是被 Ctrl-C 打断——后者按 128 加信号编号的规则折算，上游死于断管时，下游还能收到 141。手册未必写全，但四十年下来，脚本、监控与流水线全都默认这套方言。更值得记录的是契约的继承：cron 半夜跑的批处理、持续集成里上千条命令的接力、容器里健康检查的探针，判断成败的唯一依据仍是这个整数。古典循环从来不只是交互界面的契约，它是机器对机器的通用外交语言——AI 进入终端，本质上是第三种对话者接过了一套现成的外交礼仪。

历史现场我们早已看过，这里只立一块碑:1979 年 Bourne 写下 sh，1989 年 GNU 项目发布 bash，POSIX 随后把 shell 语法与退出码语义写进标准，此后近五十年，标准没有破坏过一次兼容。1980 年代的脚本今天仍能运行，不是奇迹，是契约的力量;它也是全书总纲的又一次验证——每一层都没有被替换，只是在上面加盖。

为什么终章还要先花一节重讲这个循环?因为 AI 接管终端的全部可能性，就藏在契约的两个性质里。第一，它足够硬。输入是精确的字节，输出是精确的字节，成败是精确的整数，没有任何含糊的中间态;这对概率系统是极珍贵的品质——模型再怎么幻觉，最后也要落成一条字节精确的命令与一个整数精确的退出码。第二，它足够老。老意味着语料海量、语义稳定、文档自解释:man page 写了五十年，邮件列表与问答社区攒了三十年，退出码的约定从 Unix 第一版沿用至今，任何语言模型都读过它们。硬，给了 AI 可靠的落脚点;老，给了 AI 充分的教材。掘墓人会同时毁掉这两样东西——这正是本章核心论点得以成立的物理基础。

我自己被这个循环教育过一次。yuangs CLI 里挂着 18 个工具，某天晚上我逐个盘点它们的动作面，想看看哪些可以合并;盘完发现，绝大多数工具归约到底就是三件事:跑一条命令、读一段输出、看一个退出码。读文件是 cat 的变体，搜索是 grep 的变体，git 操作是 git 的变体，连“执行一段 AI 生成的代码”，终究也要在 shell 里落地成一次 fork。那一刻我意识到，所谓 agent，不过是把古典循环包进了一个更大的外层循环——这个判断的解剖学证据，9.3 节展开。

那么，AI 是从哪扇门进来的?不是对话框，对话框是后来的事。它进来的第一扇合法的门，是 1970 年代就预留好的:补全——你按 Tab，机器猜你想说什么。终端第一次把“意图”交给机器，比聊天机器人早了四十多年;而那扇门上的锁孔形状，决定了后来一切权力设计的走向。下一节，我们去看看这扇门。

## 9.2 补全协议:AI 进入终端的第一条合法通道

补全的祖制写在 shell 的内建机制里。bash 的补全是一份真正的协议:脚本用 complete 注册一个补全函数，用户按下 Tab 时，shell 把已输入的词数组与光标所在的词序号喂给这个函数;函数查自己的账本，把候选写进返回数组;shell 接管剩下的——展开、展示、续写光标之后的内容。zsh 走得更远，它的补全系统 compsys 是一整套带类型描述的语言，连“这个候选是文件名还是主机名”都能声明。注意这个结构的要害:一端输入不完整的文本，另一端返回结构化的候选列表——一问一答，壳内协商;机器参与构造命令，但不按下回车。

```bash
# bash 补全协议的最小标本
_yg() {
  local cur="${COMP_WORDS[COMP_CWORD]}"
  COMPREPLY=( $(compgen -W "ask git token policy" -- "$cur") )
}
complete -F _yg yg
```

2000 年前后，bash-completion 项目把这份协议变成了公共词典:上万条命令各有一份补全函数，谁安装谁受益。我们在这里又一次遇见老朋友——词典。一份补全 spec 声明的是“这个命令接受什么子命令与什么参数”，与 terminfo 声明“这个终端接受什么控制序列”在结构上同构:把对端的能力写成数据，运行时查表，新增一条命令只需新增一份 spec，shell 一行不改。第三章讲过的能力协商思想，在命令这一侧长出了第二株分蘖。

真正的变局发生在 2020 年前后。Fig 的思路是把补全搬出终端:它向用户的 shell 注入一段集成脚本，借 preexec 与 precmd 这类钩子掌握输入时机，再在屏幕上用一个 GUI 浮层把候选渲染成 IDE 风格的下拉框，每个候选附带说明文档——语义补全，不再只是文件名的展开;它的 spec 仓库覆盖数百条主流 CLI，由社区共同维护。2023 年，Amazon 收下 Fig，这套资产后来演进为 Amazon Q 的命令行形态。Warp 则激进得多:它不外挂，直接换掉终端模拟器本身——Rust 加 GPU 渲染，把每条命令的输入与输出固化成一个个“块”（block），块可点击、可复制、可回放;2023 年前后 AI 能力进入产品，后来长成完整的 agent 模式，自然语言可以直达命令与计划。同期的 Starship 用一份配置统一所有 shell 的提示符。这一批工具各有野心，但底下压着同一条底线:谁都没有动 shell 的内核语法，谁都没有破坏古典循环。Warp 的界面再炫，渲染输出的底层依然是一台服从 VT100 遗训的玻璃 TTY;Fig 的浮层再现代，也必须借 shell 的钩子才知道你敲了什么。第二章说过，新终端模拟器有它的“旧义务”——这一义务在 AI 时代一分未减。

这条赛道上的选手名单还在变长。fzf 用一条管道把模糊搜索做成了万能候选器：候选列表从 stdin 进，被选中的那一项从 stdout 出——补全协议的形状，换了一副极简的骨架，就撑起了历史搜索、文件跳转、分支切换无数场景。carapace 用一个单独的二进制给几十种 shell 供给同一套补全，把“词典的编译器”从壳里抽出来共享。fish 走得更远也更朴素：它会去解析命令的 man page，自动生成补全脚本——文档即词典的源代码，这大概是“语料自解释”最具体的一次兑现。把这条线索连起来看，会发现变量只有一个：候选列表如何产生、如何展示、由谁渲染，一直在变；而“shell 问、程序答、人拍板”的三角结构，从 1970 年代到 2020 年代，一个顶点都没有移动过。

协议化与壳外挂，是 AI 进入终端的两条路线，值得各给一句判词。协议化（complete 与返回数组一族）是宪法修正案:改的是解释，不改文本，成本最低，寿命最长;壳外挂（Fig 与 Warp 一族）是违章加盖:好看好用，但每一层都欠着旧墙的债——shell 升级一次，集成脚本就要跟着修一遍。两条路线我自己都走过:给 yuangs CLI 生成过 bash 与 zsh 的补全脚本，48 篇文档里的命令树，一半的日常寿命就落在补全那几百行上;编排脚本这边，则用环境变量与子命令替补全划界。两条路走完，会得出一个安静的结论:补全协议真正的遗产不是省键，而是授权边界的先例——机器可以替你构造命令，但回车永远归你。Tab 与 Enter 的分工，是终端世界里最早的人机权力协议。

2020 年代中期，这条协议长出了新枝节。Claude Code 的斜杠命令与 @ 文件引用，本质上是把补全协议升级成了“意图补全”:你敲斜杠，它列出技能;你敲 @，它列出上下文文件;你敲到一半，它列出历史。输入框还是 readline 祖传的那一行，候选还是结构化列表，只是词典的编写者从命令作者换成了模型。补全就这样完成了它的历史使命:在对话框诞生之前二十年，它已经让终端用户习惯了“机器猜、人裁决”的分工。所以当 AI 第一次提出“我想跑这条命令”时，人们接受得如此自然——因为这个手势，他们已经做了四十年。

## 9.3 对话循环解剖:Claude Code 与 agent harness 的终端学

先立证据，再画解剖图。2026 年 3 月，Claude Code 的源码快照被公开研究，社区拆解出一张五层剖面:交互层是深度定制的 Ink 引擎驱动的 REPL;编排层负责多轮对话、会话持久化与成本追踪;核心循环是发请求、拿响应、执行工具、再循环的 agentic loop，拆解者按快照里的文件名称之为 query（） 循环;工具层把读写文件与执行命令收敛为统一的 Tool 抽象;通信层负责流式 API。要交代的是出处成色:Claude Code 本身并未整体开源，本节涉及它的架构事实，均以这场公开拆解为据——这是本书的老规矩，引用先要交代出土层位。

解剖的第一刀落在循环本身。一次用户输入，不是一次模型请求，而是一段可持续多轮的循环:模型回话里若带着 tool_use，运行时就执行对应工具，把 tool_result 作为一条新消息追加进上下文，再次请求模型;循环往复，直到模型不再索要工具、用户中断或预算耗尽。消息序列就是协议——tool_use 与 tool_result 既是控制流，也是状态机日志;会话的事实来源被做成追加式 JSONL 日志，UUID 与 parentUuid 链支撑恢复、压缩与回退，UI 与 SDK 事件统统只是这份日志的投影。工具也不是一个函数，而是一个同时覆盖四层的运行时对象:给模型看的提示词、真正的执行体、权限声明、给用户看的渲染器，各是一段代码，各司其职。

第二刀落在本地。我的实证基地是 deepseek-harness（dsh），一个开源 agent harness，README 的自我介绍只有一句纲领:一切都是插件。动手写这节之前，我在 dsh 仓库里 grep 过 queryLoop，零命中——如实记录:dsh 的循环实名 agent-loop，以 ctx.agentLoop 挂在上下文里;本节的循环解剖，以它的文档与脚本为证。它的 architecture.md 里有一句值得刻碑的话:产品的每个部分都是插件，包括模型适配器、工具注册表、会话日志，连 agent loop 本身也是插件，一切皆可从配置替换。core 子系统文档把一轮对话拆成六步流水线:agent-loop 认领排队的 prompt;在 append-only 的 SessionEvent 日志上开启一轮;经 system-prompt 组装请求前缀、从日志推导历史;经 LLM 接缝流式接收模型响应;经工具注册表分发工具调用;把每一条模型可见的事实回写日志，再让下一步从日志派生。扩展插件只依赖 agent 接口而不依赖 agent-loop 实现——循环本身是可替换件。两套系统，名字各异，形状相同:认领、组词、流式、分发、回写。

第三刀落在终端学上:对话循环究竟靠什么跑命令、靠什么读结果。答案是毫不浪漫的三件套。跑命令靠 PTY——dsh 的 terminal-bash 包，源码注释明写着消费 node-pty 解码出的数据块，受控 bash 就是通过它拉起的;Claude Code 的 Bash 工具同样把命令送进本地 shell。读结果要洗——dsh 的 TerminalSanitizer 流式剥掉 CSI 与 OSC 转义序列，只把可打印文本送进上下文;受控 bash 每次打提示符前先发一个私有标记（OSC 133 家族的 shell integration 协议），清洗器靠这个标记知道“提示符出现了，命令结束了”;注释里写得很诚实:全量终端模拟被刻意推迟，普通行输出加私有标记，就是当前契约。看懂这个设计的瞬间你会笑出声:AI 读终端之前，要先把五十年的 ANSI 积累洗掉——它不直接看屏幕，它看的是洗净的文本，像考古学家刷掉浮土才看壁画。而判断成败靠退出码:命令跑完，零与非零决定 agent 下一步是继续、重试还是道歉。PTY、ANSI、退出码——第六章、第二章与 9.1 节的三件文物，在同一张解剖图上重新出镜。

我的实战标本是 launch-tui.sh，175 行 bash，把 dsh 的 TUI 与 web 拧在同一个会话根上。它做的事朴素到近乎啰嗦:先用 lsof 探 3080 端口，没监听才后台拉起 web，PID 写进文件，启动结果按时间戳追加进状态日志;再把调用者所在目录 sed 成会话目录名，找出最新 session，把它作为恢复参数传给 TUI，让上一次的对话原地续上;退出时靠 EXIT trap 清扫。它身上最有教育意义的是一段 2026 年 8 月的注释:曾自研过一个 Copy-on-Write 协调器，给 TUI 复制会话副本再合并回去，后来弃用——因为“找下一个 magic”式的帧扫描会误判边界，弄出大面积 seq gap、损坏会话日志;而官方持久化已内置 O_EXCL 锁，并发写天然互斥，自研纯属画蛇添足。旧实现没有删除，被保留成 DSH_LEGACY_TUI_COORDINATOR 开关与新行为并存——又一次，旧地层以开关的形态活着。脚本结尾还有一行更细的忠告:不能用 exec 启动 TUI，否则 exec 会替换 shell 进程，EXIT trap 永远不触发，副本合并就丢了。这就是 AI 时代的终端学:聪明的对话悬在头顶，脚下全是这些笨拙而可靠的 bash 惯例——对话再聪明，也得有人保证会话不丢、trap 不失效。

还有一处私人注脚。launch-tui.sh 拉起的，是 dsh 仓库里 apps/repl 那个 TUI——我自己日日使用的对话循环界面。翻它的源码目录，会看到一群朴素到可爱的文件名：状态栏、成本估算、模糊搜索、宏、剪贴板贴图，甚至还有一只名为鲸鱼娘的桌宠，安静趴在终端角落。有人觉得这是不务正业，我倒觉得这是终端学的正业：对话循环再新，它的家仍然是一台玻璃 TTY——状态栏要逐格刷新，桌宠要挑未变化的单元格去画，成本估算要读会话日志的追加流。第九层地层的住户，过的仍然是 curses 与 ANSI 教下来的日子，只是房租里多了一只宠物。

配置这一层同样有实证。~/.dsh/settings.yaml 里的 provider 格局长这样:不写内联密钥，全部用 apiKeyEnv 指向环境变量名，密钥只存在于进程环境;每个 provider 声明 api 类型与 baseURL，有的指公网，有的指向本机回环的代理端口;每个模型条目带着 contextWindow 这类能力字段，而 yaml 顶部有一行 agent-default-model 充当缺省项。我把这份数据读成表之后，几乎要给它起名叫 terminfo:把对端（这次是模型）能做什么声明成数据，运行时查表翻译，换供应商不改代码——第三章的能力协商，五十年后在终端侧的 AI 配置里原样还魂，连“默认条目”的思路都与 TERM 如出一辙。

于是对话循环的解剖收拢成一句话:它没有发明任何新的 IO 原语。所谓对话，只是把“命令-执行-输出”包进一个外层循环，循环的每一圈，都是一次标准的古典循环;外层真正新增的只有两样东西——意图的解释权，与执行的否决权。解释权已经看完，否决权长在哪里，是下一节的事。

## 9.4 治理 CLI:当 AI 有了执行权

古典循环里，授权从来不是问题，因为授权与执行是同一次击键:你打的每个字符都出自你手，回车就是签字。对话循环打破了这个同一性——提出命令的手与按下回车的手分开了，模型只出意图，击键的可以是运行时。于是五十年不需要专门设计的“授权”，忽然成为新地层里最厚的一层沉积。治理 CLI，指的就是这一层:当 AI 有了执行权，谁来批、怎么批、批了怎么记账。

先看行业侧的解剖。Claude Code 的权限系统不是几个弹窗函数，而是一条独立的策略流水线，一次工具调用要过五到七道工序:工具自检 validateInput;PreToolUse 钩子——用户自己写的 shell 脚本，可以批准、拒绝、甚至修改工具入参;权限规则集，放行、拒绝、必问三态，按会话、项目、用户、策略、命令行参数五级来源分层;交互确认弹窗，批准一次、总是批准、拒绝;最后是工具自身的权限校验，管路径与域名这些细节。整条流水线 fail-closed:任何一步失败，结论就是拒绝。设计里最见功力的是顺序——危险的全跳过模式并不排在最前面:显式拒绝、内容级必问、版本库目录与 shell 配置文件的敏感检查，全部排在它之前生效，“为了省事开着全跳过”不会让安全边界整体蒸发。

模式谱系也在演化:手动确认、自动接受编辑、只读计划、危险跳过、静默拒绝，各占一档;2026 年加入的 auto 模式，让安全分类器代替人工——白名单工具走快路径，其余进两级分类器，先快判，再链式思考降误报。它有一条值得全文抄录的安全设计:分类器只看 tool_use 结构化块，不看 assistant 文本。理由冷峻得漂亮:防止模型用一段声情并茂的文字诱导分类器——对社交工程攻击的防御边界，画在了数据结构上。再配一个熔断器:连续 3 次或累计 20 次被拒，交互模式回退人工，无头模式直接终止会话——拒绝必须能止住循环，否则“拒绝”很快沦为背景噪音。无头与后台 agent 拿不到弹窗，处理方式是宁可不动:遇到需要审批的能力直接拒绝。非交互模式的边界，就是这样用“不做”画出来的。

再看我这边的治理。姊妹篇《一个人的军团》把 yuangs CLI 的权力设计哲学讲透了，这里只借事实、不重复叙事:五因子风险打分把每张提案单拆成基础操作性质 0.25、破坏性 0.30、路径敏感度 0.20、上下文 0.15、复杂度 0.10，加权出 0 到 100 的原始分，再按 35、60、80、90 四条线分级放行;写入类工具无条件至少 human-review 的那行源码注释，优先于任何分数;TokenPolicy 把估算与支付拆成两个动作，预算的 70%、80%、100% 三道门依次警告、确认、阻断;八条 non-goals 的第一条，就是不支持自治执行。本节要补的是一个终端学视角:这些制度没有一样发明了新的 IO 原语，它们全部长在终端的旧器官上。确认弹窗占住的是 readline 祖传的那一行，你按下的 y 是 1971 年以来同一次授权击键;彩色 diff 预览是第二章的 SGR 序列;审计录像用的是 asciinema 的 .cast 格式——终端逐字节回放，连“谁在什么时刻被哪条规则拦下”都能打进时间轴;而拒绝的最终语义，是退出码。治理是把旧原语组织成宪法，不是铸造新原语。

两套系统隔空相望，共享同一个判断:推理权与执行权必须分离，且分离点必须落在人类手里。它们还共享同一个底层依赖:退出码、stderr、管道这些五十年前的发明，恰好是今天 AI 安全的全部地基。这个依赖值得再展开一步——正因为古典循环足够硬，AI 的每次越权才可审计:一条命令、一个退出码、一段错误输出，证据链完整;假如终端的成败语义含糊、输出是自由文本，越权将无从谈起。换句话说，治理的可行性，是旧地层的硬度赏赐的。

还有一个容易被忽略的对称:人这一侧的确认疲劳，与机器那一侧的熔断器，是同一枚硬币的两面。姊妹篇算过账——摩擦是一次性的、可预期的、按次计费的，事故是偶发的、不可预期的、按整机计费的;Claude Code 的熔断器算的是另一笔账——拒绝若不能终止循环，循环就会把拒绝磨成习惯。两边加起来，给所有治理 CLI 立了一条公理:确认必须昂贵到不能被廉价签发，又必须便宜到不会被人性绕过。道高一尺的办法不是把门加高，而是把门的账目做清楚:谁批的、凭什么、被拒了几次，全部落盘，随时可回放。

值得把非交互这条边界再描一遍，因为它最容易被热情冲垮。管道模式、定时任务、后台代理，共同点是拿不到那一次人类击键；合格的做法不是降级弹窗或悄悄跳过确认，而是拒绝执行需要确认的动作，把决定权原样退回。配套的纪律是审计先于行动：执行前落盘提案，执行后落盘退出码与输出摘要，中间任何一次人工介入都带时间戳。这些记录选择的载体，恰好是终端自己的格式——终端录像能逐字节回放当时的屏幕，退出码表能还原每一步的成败，连“当时的提示符上写了什么”都能复原。治理的可信度不取决于承诺多严，取决于事后能不能把现场原样搬回来；而终端，恰好是软件世界里少数天生自带行车记录仪的现场。

这一层沉积至今没有停止生长:分类器、信任度衰减、浏览器里的全屏治理控制台，都是最近一年的新土。但无论它长多厚，地质学家一眼就能认出它的基岩——回车键。只要“批准”这个动作还需要一次人类击键，终端就还是人的终端。这是本章全部技术叙事的伦理底线，也是终节那句结论的伏笔。

## 9.5 地层学终章:兼容是互联网最被低估的美德

全书的挖掘到此结束。合上笔记本之前，按惯例绘制整座遗址的总剖面——九层地层，一层不缺，全部仍在服役。自上而下，最新地层居顶，最老地层居底，像一根标准的地质柱状图:

```
┌─ 第 9 层  AI 对话层(2020s)──────────────── 意图、对话、确认;Claude Code、dsh、Warp Agent
│           脚下踩着:第 7 层跑命令、第 3 层画 diff、第 5 层收输入、第 1 层的回车签字
├─ 第 8 层  现代 CLI/TUI 框架(2010s)──────── Cobra、Clap、Click;Ratatui、Bubble Tea、Textual、Ink
│           脚下踩着:ANSI 序列、terminfo 词典、curses 的 diff 心法、PTY
├─ 第 7 层  PTY(1970s 至今)───────────────── 主端冒充键盘,从端冒充屏幕;SSH、tmux、Docker attach
├─ 第 6 层  curses(1980)──────────────────── 虚拟屏幕加优化输出;htop、vim、tmux 的公共地板
├─ 第 5 层  readline(1989)────────────────── 行编辑搬回用户态;Ctrl+A/E/R 的肌肉记忆
├─ 第 4 层  terminfo(1981)────────────────── 终端能力数据库;两千种方言的价目表
├─ 第 3 层  ANSI 转义序列(1976/1978)───────── ESC[ 控制语法;ECMA-48 与 VT100 的遗产
├─ 第 2 层  玻璃 TTY(1970s)───────────────── 屏幕假装纸;VT 系列
└─ 第 1 层  Teletype(1900s 至 1960s)──────── 80 列、CR/LF、纸带;Model 33 ASR
```

图下的解说只需要一句话:这张图里没有一层是化石。第 9 层的每一次工具调用，编译到底都是第 7 层的一次 PTY 会话;每一个彩色 diff，都是第 3 层的一串 SGR 序列;每一次补全与确认，踩着第 5 层的行编辑与第 1 层的回车;第 8 层的四重奏无论范式多新，渲染最深处仍是第 6 层的 diff 心法。九层同时在线，五十年零返工——这个成绩在软件行业找不到第二个案例。

然后回答全书最后一个问题:为什么 AI 是加盖，不是掘墓。给三个论证。技术论证:概率系统的下游必须是确定性接口。模型的输出再飘忽，最终都要落成一条字节精确的命令、一个整数精确的退出码;上游越自由，下游越要硬——古典循环的硬度，是 AI 敢撒开手的先决条件，9.3 节的解剖已经证明，对话循环连读终端输出都要先洗净成纯文本，才能交给概率的头脑。语料论证:AI 之所以“会”用命令行，恰恰因为命令行五十年没变。海量且语义稳定的 man page、脚本、退出码约定与管道习惯，是每一代语言模型最可靠的教材;假如 2000 年有人成功发明了推翻 shell 的“终端 2.0”，2020 年代的模型就要面对两套割裂的语料，谁都学不明白。兼容护住了语料库，等于护住了 AI 的整套教科书;掘墓的第一个代价，是烧掉自己的图书馆。治理论证:审计与确认长在旧器官上。退出码、错误流、逐字节录像、一行提示符——治理 CLI 的每一件武器都是旧地层出产的;新地层的安全，靠旧地层的硬度兜底。掘掉旧地层，新地层悬空。

这本书的第 1 章叫“打字机的影子”。九层挖完，可以给那个影子量一次最新的尺寸了。第 1 章说过:80 列是纸的宽度，CR 与 LF 是打印头的两个动作，回车键是字车归位杆。现在看第 9 层。AI 的回答以“打字机效果”逐字浮现——流式输出在复刻电传打字机的机械节奏，我们今天管这叫体验，它的原型是 1963 年那台机器上缓缓吐纸的打印机。diff 的排版仍被 80 列的幽灵管着，终端再宽，AI 生成的代码与文档仍默认在古老宽度内折行。CR 与 LF 的百年之争，仍会在 AI 生成的跨平台脚本里上演。而回车键，完成了它的第三次身份转变:在打字机上它是字车归位，在终端上它是执行，在对话循环里它是“我批准”的签字键——同一个物理动作，六十年来授权了人间每一次执行，从编译一个内核，到删除一个目录，到允许 AI 改写你的配置文件。第 1 章说，我们至今住在打字机的影子里;走完九层才发现，这影子在 AI 时代非但没有变淡，反而投得最深:最先进的地层，保留着最古老的姿势。

给下一个五十年留三句话，权作墓志铭。第一句:不要问哪一层会被淘汰，要问哪一层还在被引用。淘汰是技术史最无聊的问题，引用才是——ncurses 还在被链接器点名，ANSI 还在被 Ink 引擎书写，退出码还在被 agent 阅读，这些层就还活着。第二句:抽象对了的东西值得加盖，抽象错了的东西值得绕行。curses 的窗口层可以被 Backend trait 绕开，它的 diff 心法与能力协商无人绕开;判断标准从来不是新旧，而是是否还抽象对了东西。第三句:地层之上只能加盖，因为地层的本质，是被反复引用过的信任。五十年不破坏兼容，才让最聪明的后来者敢直接站上最老的楼板;每一次兼容的让步，都是给未来的一次存款。

有人会问：有没有第 10 层？大概率有，而且桩基已经打下。dsh 的仓库里已经能看到 goal、plan、workflow 这些包名，行业侧“目标驱动”的提法也开始流行——当对话循环足够成熟，下一层自然会以“意图层之上再抽象一层”的方式长出来：人宣布目标，系统自行拆解、执行、汇报。但请注意它踩的位置：再高的目标层，拆到底仍要生成 tool_use、过权限门、拿退出码、把结果回写日志——第 9 层的对话与确认，就是它的 terminfo 与 pty。地层学的规律从来如此：新的一层不会取消脚下的层，只会把它们压得更深、用得更狠。本书书名《命令之下》，指的正是这件事：无论智能拔得多高，命令始终在下面托着。

兼容是互联网最被低估的美德——这句话从第 1 章的序言走到第 9 章的终章，现在是它退休的时刻，也是它上岗的时刻。说它被低估，是因为它从不出现在任何发布会的高光页:没有一场主题演讲的标题是“我们这次什么都没改坏”;它只出现在事故复盘里、迁移文档里、凌晨三点的回滚命令里。说它是美德，是因为它是一种利他的克制:每一代工程师都忍住了推倒重来的诱惑，把地基留给下一代来说他们才能听懂的新话。AI 是这套美德最新的受益者，也可能是最大的受益者——它是站在九层楼板上说话的房客，而它的房租，是承认脚下每一层楼的合法性。

最后一组镜头，留给博物馆闭馆前的广播。1963 年的 Model 33 ASR 还在展厅里通电，纸带的孔洞依然规整;1978 年的 VT100 屏幕上，管理员周期性地运行着 top;1989 年的 readline 在每一台 Linux 的 bash 里打卡上班;1980 年的 curses 在 htop 的进程表下支撑着进度条;pty 在每一个 SSH 会话里假装着键盘与屏幕。而在它们之上，2026 年的对话循环里，一个模型刚读完一段洗净的命令输出，正要提出下一条命令——它提交提案的姿势，与六十年前打字员敲下回车的姿势，是同一个姿势。地层学的第一定律是:没有人能推翻地层，只能成为地层。我们给五十岁的协议发新工牌，它们接过工牌，继续干活——在命令之下，在智能之下，在一切之下。

---
