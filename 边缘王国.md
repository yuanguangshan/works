---
sync_time: 2026-09-02 10:02:13
source: clipboard
content_hash: 6473a82a329a000cfb840b10661dfc1131bbd0fd358c0386cfff65778a7cb530
---

# 《边缘王国》

> 作者:广山哥  
> 完成时间:2026-09-02

---

## 序言

# 序言

这本书开始于一个很小的瞬间：某天我在另一台电脑上贴出一张图，链接是 https：//pic.want.biz/xxx.png，顺手发给朋友。他回了一句：“你这图床哪买的？”

我愣了一下。买的？不是买的。那张图片躺在 R2 的一个桶里，桶挂在我自己的域名 want.biz 下面，请求被 Cloudflare 的边缘节点接住，离他最近的那一台把图递给了他。整条链路上没有任何一台机器是我买的、租的、看得见摸得着的——但那张图，确确实实是从“我的基础设施”里发出去的。

十年前，一个个人开发者说“我的服务器”，指的是他在某个 IDC 租的一台虚拟主机，一年几百块，宕机要开工单，备份靠自己，离开机房五十毫秒都算运气好。今天，我说的“我的基础设施”，是分布在全球三百多个城市的计算节点、一个 SQLite 兼容的边缘数据库、一个不收出站流量费的对象存储、一张我自己域名下的全球分发网络。它们加起来，大部分月份的花费是零。

这本书想讲清楚的就是这件事：一个普通人，是如何在没有机房、没有运维团队、没有预算的情况下，合法地拥有一张全球网络的。以及更重要的——这件事的边界在哪里，代价是什么，什么时候它会反咬你一口。

## 这本书不是什么

先说清楚它不是什么。

它不是 Cloudflare 产品手册的改写。官方文档写得比我全面得多，而且永远比我新。如果你想查“某个配置项怎么填”，请直接去读文档，这本书帮不上你。

它也不是一本鼓吹“上云”或“all in 边缘”的布道书。恰恰相反，这本书里你会反复看到我对平台保持警惕的部分：第七章讲域名过期是一套多么精密的没收程序，第九章讲免费额度的收紧就是免费层住户的“涨价”，第八章甚至专门论证了为什么你的核心资产应该留在你自己的 NAS 上，而不是任何一个云上。

它是一份实践志。所有章节背后都有真实的项目：knasync 是我自研的跨机消息队列，九行 wrangler 配置起家，长在 D1 上；want.biz 的五个子域各司其职；pic.want.biz 后面是 R2；api.yuangs.cc 后面是我的一揽子个人服务。书中引用的配置、表结构、脚本，都来自这些项目活着的源码，而不是为了这本书临时摆拍的示例。

## 全书的地图

九章文字，大致分三段。

第一段是“领地”（第一章到第三章）：先算清这张全球地契值多少、怎么领（第一章），然后解剖两块最核心的地基——Worker 的 V8 isolate 之轻（第二章）与 D1 的单写多读之辩（第三章）。读完后你应该能回答：为什么函数比进程更适合个人，以及 D1 到底放弃了什么。

第二段是“流动”（第四章到第五章）：数据怎么存（R2 与零出站革命）、消息怎么走（knasync 与队列哲学）。这两章的核心是一句我越写越信的话——队列的本质，是把时间从通信里解绑。

第三段是“治理”（第六章到第九章）：AI Gateway 作为边缘的安检门，域名作为数字不动产的运维实录，本地优先与边缘优先的划界原则，最后用一整章摊开成本账单——精算师视角收束全书，因为成本是这个王国唯一不会说谎的语言。

## 一个反共识的立场

市面上讲边缘计算的文字，大多沿着一条隐含的叙事：边缘是云的延伸，是大厂把算力铺到你家门口的恩赐。这本书的立场恰恰相反。

大厂讲边缘，是为了卖更多云；个人开发者用边缘，是为了把“全球网络能力”从买不起的奢侈品，变成按量付费、甚至长期免费的日用品。这本质上是主权的重新分配——过去只有企业预算才配拥有的能力，现在一张个人信用卡就能领地。Cloudflare 是这个时代个人开发者能拿到的、性价比最高的“全球基建券”，这是我全书的核心论点，但请注意，领了地契不等于高枕无忧：平台随时可能修改规则，免费额度就是货币政策的雏形，所以第九章的退出演练不是悲观，而是主权的完整定义——能离开，才谈得上留下。

## 写作方法：工程考古学与精算师视角

这本书的两条方法线索，提前交代。

其一是工程考古学：每讲一个服务，先问它从哪里来、替代了什么、放弃了什么，再看它今天长什么样。Isolate 为什么比容器轻？因为隔离从操作系统层下沉到了内存层。D1 为什么敢做单写多读？因为它用 Durable Object 的全局唯一性取消了一整类分布式冲突。知道一个东西是从哪来的，才知道它会往哪去、什么时候不可靠。

其二是精算师视角：每个关键服务都附一张数字卡片——免费额度、单价、限额、退出的成本。数字全部来自官方价目表与我的素材档案，并注明“以现行价目为准”，因为云上的价格比天气变得还快。唯一需要坦白的是：我自己的账单长期运行在免费层内，这既是本书的幸运，也是本书的论点。

## 致谢

感谢 knasync、knowly、podcast 这些项目的源码——它们是本书最诚实的素材库；感谢《一个人的军团》，那本书里的九个作品是踩在这张边缘网络上的军团，这本书写的是他们脚下的大地；也感谢深夜里那些报 400 的接口——mimo 渠道那一夜让我真正理解了“接口方言”这个词的分量。

这本书是“基建三部曲”的第一部：网络侧的大地铺完之后，第二部《命令之下》向下挖终端交互的九层考古地层，第三部《推理工厂》走进大模型推理的厂房，看一块 GPU 如何把 token 变成钱。

边缘的大地说完了。愿你也早日领到属于自己的那张地契。

广山哥
二〇二六年九月，于听雨轩

---

## 目录

- 序言
- 第1章 地契:从租服务器到买全球网络
- 第2章 Worker 即文明:函数比进程更适合个人开发者
- 第3章 D1 的野望:SQLite 如何在边缘成为全球数据库
- 第4章 R2 与带宽联盟:出站流量费归零之后
- 第5章 队列即时间机器:knasync 与人不在场的世界
- 第6章 AI Gateway:边缘的安检门
- 第7章 域名即主权:want.biz 运维实录
- 第8章 本地优先与边缘优先:knowly 与 knasync 的分界
- 第9章 成本账单:跑一个边缘军团到底花多少钱

---

## 第1章 地契:从租服务器到买全球网络

# 第 1 章 地契:从租服务器到买全球网络

## 1.1 一张账单引出的王国

一本讲基建的书从一张账单开场，听起来有点寒酸。但工程师的世界里，账单比宣言诚实:宣言可以堆满形容词，账单上的每一个数字都是有人真金白银付过的。所以全书的第一件事，不是定义边缘计算，而是把账单摊开。

每年春天我都做一次年度对账，把名下所有与“网络存在”有关的支出列进一张表。2026 年那张表出来之后，我自己愣了几秒:支撑着几个每天真实接流量的项目——图床、队列、行情源、模型网关——的年度固定支出，只有一行，域名的年费。其余的一切，计算、对象存储、数据库、队列、网关，全部躺在免费额度与按量计费的缝隙里，以几块几毛的方式存在。它们有一个共同的特征:不续费就会消失的，只有域名;其余部分，账单跟着用量走，用量趋近于零，账单就跟着趋近于零。

十年前不是这个局面。我租的第一台 VPS，月付几美元，换来的是某家机房里一个固定位置上的虚拟机:一串写死的 IP，一块共享的 CPU，一块不大的磁盘。它在物理世界里的坐标是确定的——某国的某个州的某个机房，机架的第几层。我的用户如果恰好离那台机器近，那是运气;离得远，那是宿命。为了弥补宿命，就得再租一台放在另一个大洲的机器，配负载均衡，买一家 CDN，为证书单独付费，再写监控脚本盯着它们谁都别死。那时候，“拥有全球能力”这句话对个人开发者来说是一种修辞，不是事实。

把时间轴拉长，这张账单其实演进过四代。拨号上网的年代，个人网站住在虚拟主机里，按月付费，配额以 MB 计;VPS 年代，账单变成主机、备份、CDN、证书、监控五项之和，每一项都要单独去谈;云函数年代，账单开始按调用次数计，但全球分发仍是需要加价采购的奢侈品;到了边缘年代，全球部署第一次不再是加购项，而是默认值。四代账单摆在一起看，趋势不是简单的变便宜，而是计费单位在持续变小:从按月租一台机器，到按毫秒租一段计算，再到按请求租一张网络的入口。计费单位越小，浪费越少，个人开发者与巨头之间在成本结构上的差距也就越小——这是日用品化真正的杠杆。

对照一下今天。今天我把一段不到一千行的代码推上 Cloudflare，它就同时活在全球三百多座城市里。按官方在 2026 年公布的数据，这张网络覆盖 330 多座城市、125 个以上的国家和地区，对外互联容量跨过 500 Tbps，与超过 13000 个网络对等互联，全球 95% 的互联网用户距离最近的一个数据中心不超过 50 毫秒（materials/ch01_补充.md）。这些数字在 2010 年前后，只出现在 CDN 厂商给大企业定制的合同附件里;今天，它们是一张免费账号的默认配置。注册那一刻，你什么都没买，却什么都能用——更准确的说法是:你买到了使用权，而且单价低到可以忽略不计。

本书的命题就藏在这个落差里。我的说法是:个人开发者与“全球网络能力”的关系，在这十年里悄悄完成了一次从租赁到持有的转变。租赁的心态是怕——怕涨价、怕下线、怕迁移;持有的心态是敢——敢在上面盖房子，敢让它接真实流量，敢把别的东西的依赖建在它上面。所谓从租服务器到买全球网络，租的是位置，买的是能力;租的是一台机器，买的是一张网。

我把它叫作地契。这是个刻意挑选的旧词。地契的意义从来不在那张纸，而在于它把一片公共的、无主的、随时可能被收回的荒地，变成明确的、可依赖的、可以做长期规划的领地。Cloudflare 账号之于今天的个人开发者，就像地契之于有产者:want.biz 是我的庄园，它下面的每一条 DNS 记录是地契上的界碑;界碑之内，Workers 是我的农具，R2 是我的谷仓，D1 是我的账房，队列是我的驿道。这本书要讲的，就是这块地怎么领、地上的庄稼怎么种、每年要为它交多少租，以及——最重要的——这块地和别人的地有什么不一样。

租约和地契的区别，比字面上更深。租约保护的是使用，地契保护的是处置:租约到期，装修带不走;地契在手，拆了重盖也没人拦。工程世界的对应物是迁移自由与重构自由。这十年里，我把同一批工具从虚拟主机搬到 VPS，再搬到边缘，每次搬家都在确认同一件事:搬家成本决定你对平台的谈判地位，而决定搬家成本的，是你签下的那纸契约究竟是租约还是地契。这本书把注押在后一种上，第九章会做一次完整的退出演练来验证这个押注——如果那天演练失败，全书论点作废，我也会如实写进那一章。

还要交代一句:为什么是 Cloudflare，而不是任何别家。答案不浪漫，是精算意义上的:Workers 免费层每天十万个请求，R2 免费层含 10GB 存储，D1 免费层每天 500 万行读（BRIEF 关键事实清单）。域名之外，我的年账单几乎为零。对一个独立开发者来说，这是当下能买到的、性价比最高的一张全球基建券。至于性能之争、开发者体验之争，在“免费”与“全球”同时成立这个事实面前，都只是次要条款。

本章是全书总起。后面四节依次是:画地图，王国的四权（1.2）;验家产，want.biz 上的五块招牌（1.3）;立论点，边缘为什么不是云的延伸而是主权的再分配（1.4）;约读法，每一章都有一张账单（1.5）。如果你只打算读一章，请读这章;如果你打算读完全书，那么这章是所有账单的第一页。

## 1.2 王国的四权:计算、存储、数据、网络

租服务器时代的第一课，是一台机器什么都得会。VPS 卖给你的其实是“四权的混居”:CPU 是计算权，磁盘是存储权，自己装上去的 MySQL 是数据权，一个公网 IP 加域名解析是网络权。四权长在同一台机器上，也就死在同一台机器上——磁盘满了，计算跟着卡;机器宕了，网络一起断。那个时代买的是整体，维护的也是整体:打系统补丁、配防火墙、装证书、写备份脚本、盯监控面板，五件事一件都不能少，少一件，凌晨三点的电话就会响。

边缘王国做的第一件事，是把四权拆开，各自定价，按量发放。这不是几个产品的罗列，而是所有权结构的变化:你可以只取你用得上的那几权，并且用多少付多少。下面逐权过堂，每一权都先讲它的物理前世，再讲我拿它做了什么。

第一权，计算，Workers。它的前世值得记一笔:2017 年 Cloudflare 发了一篇题为《Cloud Computing without Containers》的工程博客，核心决定是用 V8 的 isolate 而不是容器来跑用户代码。isolate 是 V8 引擎里的隔离上下文，单个操作系统进程可以容纳几百到几千个;冷启动约 5 毫秒，每个 isolate 的额外内存约 3MB。对照组是传统容器化进程 500 毫秒到 10 秒的启动时间，以及空跑一个 Node Lambda 约 35MB 的内存占用（materials/raw_worker.md）。发布时这套模型铺在 155 个数据中心，官方的表述是它能以好于亚马逊单个数据中心部署的经济性，把代码同时放到所有节点上;到 2026 年，节点数是数百座城市（materials/raw_worker.md、materials/ch01_补充.md）。个人开发者不必吃透全部细节，只需记住结论:全球部署的边际成本，第一次降到了零附近。另一个条款同样要紧:isolate 内存占用小，平台可以只在实际执行代码时计费，等待 I/O 的时间不收钱（materials/raw_worker.md）。对低流量的个人项目，这是决定性的差异——一天几百个请求，月末账单四舍五入等于零。

第二权，存储，R2。它改写的只有一个条款:出站流量费归零。S3 的存储单价是每月每 GB 0.023 美元，R2 是 0.015 美元，差距不算惊人;真正悬殊的在流量——R2 不收出站流量费，而且没有配比之类的隐藏条件（materials/raw_r2.md）。写操作按 Class A 计价，4.50 美元一百万次。对个人项目，这意味着成本模型从“不敢被下载”改写成“欢迎被下载”:图床、播客分发这类读多写少的业务，第一次可以不计后果地公开。顺带分清一个容易混淆的对手:市面上也有打着零出站旗号的存储服务，但常带配比条件——存多少 TB 才免多少流量，本质是把两件商品捆在一起卖;R2 的零出站是无条件的（materials/raw_r2.md）。无条件与有条件的差别，就是公理与促销的差别:前者可以写进架构假设，后者只能写进风险清单。

第三权，数据，D1。可以叫它“能联网的 SQLite”:底层就是 SQLite，2024 年 4 月正式 GA。每个数据库在后台由一个 Durable Object 承载，借它的全局唯一性换来干净的隔离语义;带 Time Travel 时间点恢复，付费计划保留 30 天，免费 7 天;2025 年 4 月读复制进入公开 Beta，在六大区域提供异步副本（materials/raw_d1.md）。我在 knasync 里给它起了个绑定名 DB，代码里 env.DB 三个字母，背后就是一个全球可达的数据库——这件事在租服务器时代等价于“自己搭一套主从复制”，是全职 SRE 的活儿。计费与限额也顺手记下:按行读、行写、存储三项计费，单库免费层 500MB、付费 10GB，2025 年 7 月起账户总存储上限从 250GB 提到 1TB，不用时可以缩到零（materials/raw_d1.md）。翻译一下:一个人的数据量级，免费层就装得下大半，而不用的时候，它一分钱不花。

第四权，网络，DNS、证书与分发。域名解析是四权里最古老也最基础的一权，它是界碑的刻字权:一条记录写下去，全世界才知道你的门牌。边缘证书自动化让 HTTPS 变成空气一样的存在，不再是要单独采购的商品;而那张网络本身——95% 的用户在 50 毫秒以内（materials/ch01_补充.md）——是前三种权得以兑现的承载面。这张网的肌肉来自经年累月的物理投资:与超过 13000 个网络对等互联，互联容量在 2026 年 4 月跨过 500 Tbps;它的生长速度一度夸张到在 24 天里新增 31 座城市（materials/ch01_补充.md）。个人买不起这样的网，但可以按请求租下它的入口——四权里，网络权是最没法自建的一权，也是最先被日用品化的一权。

把四权的今昔并排写成一张对照表，大概是这样:计算，从整月租一颗 CPU，到按毫秒租一个 isolate;存储，从整块租一块磁盘，到按 GB 月租一只桶;数据，从自己安装并调教一套数据库，到一个三字母的绑定;网络，从带宽、证书、CDN 三笔账，到解析与证书随域名自带。对照表的每一行都在说同一句话:买的东西变小了，能做的事情没有变小。

四权拆分还有一个常被忽略的副产品:风险也跟着拆散了。VPS 时代，单点故障是全权沦陷——磁盘一坏，四权同灭;边缘王国里，计算在几百座城市各自为政，个别节点出问题会被路由绕开，数据还有 Time Travel 可以回到过去某个时刻（materials/raw_d1.md）。单点变成多点的那一刻，凌晨三点的电话变成了早上起来看一眼日志。这不是乐观主义，是拓扑结构的礼物。

四权拆开之后，VPS 时代的运维清单发生了有趣的坍缩。打补丁消失了，因为运行时由平台统一更新;装证书消失了，自动化接管;备份简化成一条恢复命令;监控的对象从“机器活着吗”变成“函数跑得对不对”。清单上剩下的，是真正属于应用逻辑的部分。这就是按量付费的日用品化:全球网络能力从一件买不起的资产，变成一张与电费水费同构的账单。而账单，既是下一节的主角，也是贯穿全书的线索——先把家产验完，再看账单怎么读。

## 1.3 五块招牌:want.biz 上的庄园

验明家产的方式，不是看宣传页，而是看配置文件。宣传页可以说谎，wrangler.toml 不会。我的跨机消息队列项目 knasync，部署描述总共九行:

```toml
name = "knasync"
main = "src/index.js"
compatibility_date = "2024-01-01"

# D1 数据库绑定
[[d1_databases]]
binding = "DB"
database_name = "knasync"
database_id = "6ba67ac5-6dae-4efb-9836-6aec45304db6"
```

（~/ygs/knasync/wrangler.toml）

逐行考古。第一行 name，是这块地在平台户籍系统里的登记名。第二行 main 指向单文件入口 src/index.js——整个项目的接入层，996 行，HTTP 请求与 WebSocket 通道都从这里进。第三行 compatibility_date 是一枚时间胶囊:平台在演进，接口在变，这个日期把我的项目冻结在 2024 年 1 月 1 日那天的行为规范上，从此平台的未来与我无关，除非我主动改日期。最后四行是全文件的点睛之笔:[[d1_databases]] 声明了一个 D1 绑定，绑定名 DB，database_id 指向那个以 6ba67ac5 开头的全球唯一数据库实例。从这四行起，env.DB 在代码里只是一个普通的对象属性，背后却是一处全球可达的存储。一个真实项目的边缘配置长什么样?就是这样:短到可以背下来。配置的长度与架构的复杂度无关，与取舍的清晰度有关——这是我后来反复验证的经验。

这份九行的配置值得与它的前世对读。VPS 时代的同等内容是一叠文件:nginx 的 server 块、systemd 的 unit 文件、证书续期的定时任务、数据库的配置、防火墙规则，外加一份手写的部署文档，防止自己忘了哪一步。九行对一叠文件——体量差不只是省事，它意味着出错面的塌缩:没有步骤，就没有忘掉的步骤;没有状态，就没有漂移的状态。这是边缘配置的美学，也是它安全性的来源。

庄园的地不止这一块。want.biz 之下挂着五块招牌，在《一个人的军团》里它们被归入“私有基建”词条，这里正式验明正身。

第一块，pic。pic.want.biz 是 R2 图床的公开链接域。它存在的理由是一次教训:GUI 渲染 Markdown 图片时只接受 http/https 的完整链接，相对路径会被降级成纯文字——边缘上的对象，必须配一个公网域名才算数（BRIEF 关键事实清单）。没有 pic 之前，我的图躺在桶里;有了 pic，图才算发布。一个字节的差别，隔着“存储”与“分发”两条国界。pic 的链路是双向的:入站由上传接口把对象写进 R2，出站由这个公开域把图供给博客、笔记与 GUI 渲染;播客成片、封面与订阅源走的也是同一条 R2 出口（《一个人的军团》第一章的记录）。一只桶，养活了图床与播客两条生产线——零出站费在其中的作用，第四章细算。

第二块，upload。upload.want.biz 是 knowly 的上传入口，是私有数据从本地世界进入边缘世界的口岸。口岸的品德是天生的薄:验货、放行、登记，不做加工。凡是想在口岸上加工资的冲动，最后都会变成事故，这是后话。knowly 的本地优先哲学是第八章的主角，upload 只是它在边缘的影子:私有数据落 NAS，需要公开分发的部分才过这道口岸——分界线画在泄露成本上，不画在访问频率上。

第三块，fm。fm.want.biz 是我的行情站，自建的行情与新闻数据源。它在军团里的角色是矿场:播客工厂的 fetcher 每天从这里拉原材料，交给大模型写稿——那档开播以来累计一千多期的财经早餐，第一道工序就是访问它（《一个人的军团》第七章）。它的缘起在军团书里有一段好玩的记载:我把行情站的页面文本直接贴进需求文档，只写了一句要求——自动生成财经早餐，git 仓库的第一条提交就叫“自动生成播客”（《一个人的军团》第七章）。一个数据源的最终命运，常常是被另一个更大的自动化吞进肚子。

第四块，q。q.want.biz 供期货行情:六大期货交易所的数据面板从它取数，带多轮重试与最后成功快照，断网也有东西可看（《一个人的军团》第十章）。fm 与 q 一静一动，同是矿场，出口不同:fm 供新闻与日度行情，q 供盘面的实时读数——两块招牌把“数据源”这件事切成了刀工清楚的两大块。

第五块，poe。poe.want.biz 是模型网关的公网门面，播客写稿的弹药线从这里出库。它背后是统一网关的 providers 与 models 映射，加一家供应商零代码;智谱渠道失效当天实测切到小米 MIMO，前线没有停（《一个人的军团》第十章、BRIEF 渠道实战词条）。它出库的每一发弹药还要过安检:23 条脱敏规则加 5 类高危直接拦下，不信任调用方，只信任机制（《一个人的军团》第十章）。第六章会专门解剖这扇门。

五块招牌背后，站着的是四权:pic 踩着 R2 的零出站;fm 与 q 的数据在 D1 与缓存之间流转;poe 站在 Workers 与网关的组合上;upload 是边缘世界与本地世界的海关。庄园不大，但一个独立开发者需要的全部公网门脸，它都齐了。五块招牌的分工背后有一条隐含的纪律:每块招牌只做一件事，招牌之间用最简单的协议说话——HTTP 与队列。这让任何一块招牌都可以单独重写、单独下线、单独搬家，而庄园的门脸不变。微服务架构师会说这就是单一职责原则，我的说法更朴素:一个人的系统，每块招牌都必须小到我一个人修得动。

顺带一提，knasync 的家底也在同一块地上:schema.sql 里四张表，queue、submitted、results、consumed，其中 submitted 表直接把 content 字段设为主键（~/ygs/knasync/schema.sql）——消息天然去重，同一批货不会被登记两次。这个细节眼下只需眼熟，第五章讲幂等时，它会回来领赏。再看 queue 表:自增 id、content 正文、queue_type 队型、created_at 时间戳，外加一个按队型与时间建的联合索引（~/ygs/knasync/schema.sql 首段）——一张为顺序扫描而生的表，朴素得像一句谚语。仓库里还留着一扇特殊的门:mcp 目录下的 server.py 把队列能力封装成 MCP 接口，专供 AI 军团传令调用（~/ygs/knasync/mcp/）——庄园的门脸里，有一块牌子是写给机器看的。

## 1.4 边缘不是云的延伸，是主权的再分配

行业里讲边缘计算，主流叙事是“云的延伸”:云的半径缩短，计算从区域中心下沉到城市，延迟降低，体验变好。按这个叙事，边缘是云厂商的又一次扩张，卖点是更多的云。我对这个叙事没有意见，但必须指出，它对个人开发者是不完整的，甚至有误导性——因为在“延伸”的受益者排序里，个人从来排在末位。过去十年真正发生的另一些事，是边缘完成了一次主权的再分配:把原本只属于企业的能力，重新分给了个人。分三步论证。

第一步，看能力所有权的变迁。2010 年前后，一个应用要做到“全球低延迟”，需要的清单是:多家 CDN 合同、跨区域机房、负载均衡、全球健康检查，以及一支随时值班的队伍。这份清单的价格标签，天然把个人开发者排除在外——不是个人不想要，是个人买不起。2017 年，isolate 模型登场，官方的表述是它能以好于亚马逊单个数据中心部署的经济性，把代码同时铺到 155 个数据中心（materials/raw_worker.md）;到 2026 年，这个数字长成 330 多座城市、500 Tbps 的互联容量（materials/ch01_补充.md）。能力本身没有变，变的是能力的准入门槛:从企业预算，降到零。门槛降到零的这件事，不该叫延伸，该叫分配。这张网的来路也值得一提:2010 年它起步于帕洛阿尔托的一间办公室，当时只接了一家转接服务商;2018 年，它用 24 天新增 31 座城市;2023 年突破 300 城;今天它服务着全球 20% 以上的网站（materials/ch01_补充.md）。一家公司用十六年把“全球”做成了日用品，个人要做的不是感叹，而是把这张日用品化的网接进自己的项目。

第二步，看免费额度的经济学。Workers 免费层每天 10 万次请求，R2 免费层 10GB 存储，D1 免费层每天 500 万行读（BRIEF 成本锚点）。有人说这是施舍，我不同意。免费层是商业策略:它是获客漏斗的宽口，平台赌的是你的项目长大后会走进付费层;你赌的是，在项目长大之前，你的成本是零。双方各取所需，这是交易，不是恩惠。看清这一点的意义在于心态:把免费额度当收入来源一样精打细算的人，和把免费额度当天上掉的馅饼的人，五年之后的架构能力会差出一个量级。前者成了精算师，后者一直是租客。举一个我自己的例子。这部书稿的每一章都是几千字，配图走 pic，草稿同步走 knowly 与 R2，播客分发走 R2 与订阅源——这些动作一个月的总开销，长期落在免费额度之内;真正需要付钱的，只有域名。账单上最贵的一行其实是确定性，不是流量:我花钱买的是“这个域名的解析永远指向我”这件事，其余的一切，平台用免费额度换我的长期驻留，双方各取所需。

第三步，看退出自由——这才是“持有”的试金石。租用时代，你在服务商那里其实拿不走任何东西:关机那天，数据、配置、流量历史一起蒸发。而在边缘王国里，D1 的底层是 SQLite，数据随时可以导出成一个文件;R2 兼容 S3 API，官方还提供迁移工具 Super Slurper，进得来也出得去（materials/raw_r2.md）;DNS 更是随时可改 NS 记录，把整个域名搬去别家。第九章我会真的演练一遍:把 D1 导出成 SQLite 文件，把 R2 的对象整体搬到别的兼容端点，把 DNS 切到别家——演完你会发现，搬家的箱子只有三只，而且都不重。搬家自由，是有产者与佃户的本质区别:佃户不是不能走，是走了就一无所有;有产者搬家，家具跟着走。一个系统能不能算“我的”，不看它在谁的机房里，看它离场时要不要留下一只手。

其实还有第四步，发生在开发体验上。租服务器时代，上线是一个仪式:买机器、装环境、配域名、调证书，一条龙跑完，一天就没了。今天，knasync 仓库里的 deploy.sh 一共十步:环境检查、登录确认、建库、生成配置、灌入表结构、设置密钥、部署，最后对 health、pull、submit 三个端点自动验收（~/ygs/knasync/deploy.sh）。一个人，一条命令，几分钟，代码就活在全球几百座城市里。当“发布到全球”的动作成本低于“搬进一间新公寓”，能力的所有权才算真正落到个人手里——工具的民主化，是主权的日常形态。

当然，清醒和热情必须同时在场。主权是有边界的:isolate 可能被随时逐出，全局状态不可靠（materials/raw_worker.md）;平台的配额与行为规范随时可能调整;域名一旦忘了续费，王国一夜回到解放前——第七章我会讲一次差点失去一切的续费惊魂。承认平台即房东，然后把对房东的依赖压到最低，这才是“边缘主权”的完整含义:不是幻觉里的绝对所有，而是精心谈判过的、随时可以携带的所有权。

再分配不等于平分。个人的边缘王国再大，也大不过平台的调度权:哪座城市的节点承接你的请求、哪次冷启动发生在哪台机器、哪个 isolate 被逐出，全都不由你决定。但这是可以接受的不对称，就像有产者也不决定节气与天气。主权的关键从来不是控制一切，而是握住最要害的那几样:域名解析指向哪里，数据能不能带走，账单会不会失控。这三样在边缘王国里都攥在自己手里，其余的，可以放心交给平台。

所以我的结论是:对大厂，边缘是云的延伸;对个人，边缘是主权的再分配。前者是他们的商业叙事，后者是我们的生存现实。这本书从头到尾站在后一边。至于平台为什么愿意做这门生意，第九章的免费额度经济学里有半个答案;另外半个，藏在这样一个事实里:个人项目长大后的每一步，几乎都还走在这张网上。

## 1.5 本书的读法:每一章都有一张账单

一本技术书最容易被原谅的毛病，是只讲“怎么用”，不讲“它从哪来”和“它放弃了什么”。只讲怎么用的书，读完你会照做;来历与取舍缺席，你就永远只能在别人的架构里做客人。所以我为本书立下两条方法纪律:工程考古学，与精算师视角。

工程考古学，是说每讲一个服务，先做三层考古。第一层，读它的前世:它替代了什么，它出现之前的默认方案是什么。Workers 替代的是容器与进程，Queues 替代的是“人必须在线等结果”的同步调用，R2 替代的是那道出站流量税。第二层，读它的取舍:它放弃了什么。Workers 只能跑 JavaScript 与 WebAssembly，装不进任何编译好的二进制（materials/raw_worker.md）;Queues 只承诺 at-least-once 投递，放弃精确一次;D1 保持单写者，放弃多主写入。取舍清单比功能清单诚实——功能是营销，取舍是性格。第三层，读它的现在:源码、配置与账单。本书的所有关键论断，尽量落在三样可检验的东西上:wrangler.toml 的行，schema.sql 的表，价目表上的数字。

方法之外，也声明本书不写什么。它不写企业级的多区域容灾，不写千节点集群的运维史诗，不写把简单事情说复杂的架构图——那些舞台属于团队，而这本书的预设读者，是一个人加一只终端。一个人不意味着只能做小东西，只意味着所有环节的复杂度都必须由一颗脑袋扛得住;这恰恰是最严格的架构约束，也是最好的架构训练。后文的每一个设计决定，都会用同一把尺子丈量:一颗脑袋能不能扛住，一张账单能不能养起。

精算师视角，是第二纪律:把每个服务的免费额度与计价单位当账本算。每章的收束处，我会把当章的服务过一遍价格:单价多少，免费额度多少，我的项目一个月真实用了多少。数字本身不浪漫，但数字是个人开发者唯一不会被误导的语言。到第九章，所有账单合并成年终总账——那也是全书论点“从租到有”的最终核算。价格会变，所以书中涉及价格的表述一律以现行价目表为准;但“把定价翻译成自己项目的单位成本”这个习惯，比任何具体数字都长寿。精算师的具体动作只有三个:把单价除以自己的用量，把免费额度当存款，把涨价预案写进架构。第三章的单库 10GB 上限、第四章的每百万次写 4.5 美元、第五章的 25GB 积压上限，都会在各自章节里换算成 knasync 的真实单位成本（分别见 materials/raw_d1.md、raw_r2.md、raw_queues_ai.md）。

按这两条纪律，全书九章的路这样走。第二章解剖 Workers，把 isolate 放在手术灯下:函数为什么比进程轻，5 毫秒与 3MB 意味着什么，以及它为此付出的代价——JS 与 WASM 之墙、全局状态的不可靠。第三章讲 D1，SQLite 如何在边缘长成全球数据库，单写者换来了什么、又堵死了什么。第四章讲 R2，零出站费如何改写个人项目的成本模型，pic.want.biz 就是新模型的产物。第五章讲队列——我愿意叫它时间机器:knasync 如何让消息在人睡觉时依然抵达，at-least-once、死信队列与幂等键如何搭成可靠性三角。第六章讲 AI Gateway——安检门:缓存、限流、预算、脱敏如何从应用代码下沉到代理层，包括智谱渠道失效当夜切换小米 MIMO 的实战。第七章讲域名，个人在数字世界里唯一真正“拥有”的不动产，界碑学与一次差点丢掉一切的续费惊魂。第八章讲本地优先与边缘优先的分界:knowly 与 knasync 各守哪边，数据按泄露成本如何分级分封。第九章摊开全部账单，算清“一个人的全球基建”的年租，并做一次退出演练。

每章的内部结构也是统一的，全书共用一个叙事范式:边缘哲学开场，然后是这个服务的物理前世——它替代了什么，接着是我的项目如何用它，附代码与配置实录，然后是踩坑与代价，最后回扣总纲收束。结构的一致不是偷懒，而是让你在任何一章都能预测下一节会出现什么，把注意力省下来，全部交给论证本身。

与《一个人的军团》的关系，也在此交代。那边是作战志:一支由 Agent 组成的军团怎么点兵、怎么协同、怎么层层兜底;这边是后勤基建志:军团脚下的大地是怎么铺成的。写两本书的是同一个我，广山哥。两书共享同一个叙事宇宙——knasync、want.biz、R2 图床、NAS 都会在两边出现，但视角不同，互为注脚，不重复叙事;军团书第一章的“共享基建”一节，是两本书之间的暗号，读没读过那边，都不影响这边。

最后是给你的三条读法。看数字:书里每个关键论断都带着出处与数字，来源写在括号里，你可以核对，也欢迎核对。看源码:所有项目实录来自真实仓库，配置短到可以背下来——配置的短，本身就是选型的结果。看账单:每章的价格都注明以现行价目表为准，云的价目表比天气变得还快，请把注意力放在算法而不是数值上。

这三条读法指向同一种读者:不满足于把服务当魔法、想看清齿轮的人。如果你恰好是这样的人，这本书会像一份装修日记——每一笔开销有收据，每一面墙有图纸，每一处妥协有原因。也顺便说不适合谁:想找速查手册的人会嫌它啰嗦，想看神话的人会嫌它抠门;它的理想读者，是想亲手把自己的日子过成系统的人。

下一章，我们直接上解剖台:把一个 Worker 函数放进 V8 的手术灯下，看 isolate 到底是什么，以及“函数比进程更适合个人开发者”为什么不是口号，而是一道可以当场验算的算术题。

---

## 第2章 Worker 即文明:函数比进程更适合个人开发者

# 第 2 章 Worker 即文明:函数比进程更适合个人开发者

上一章末尾我把话说到一半:要把一个 Worker 函数放进 V8 的手术灯下。这一章兑现。先立论点——Workers 用 V8 isolate 把多租户隔离从操作系统层下沉到了内存层，这一下沉把冷启动压到约 5 毫秒、把每实例额外内存压到约 3MB，两件事合起来，让“把代码部署到全球数百座城市”第一次成为个人开发者的默认选项，而不是企业客户的特权（materials/raw_worker.md）。下面五节依次是:五十年的进程重量史，它回答的其实只有一个问题（2.1）;isolate 的解剖图，单进程里如何住下数千个房客（2.2）;三个“没有”构成的加速账（2.3）;我自己队列服务的入口，为什么薄到只有一个函数（2.4）;以及一份诚实的代价清单——这台机器不让你做什么（2.5）。

## 2.1 从 CGI 到 Serverless:五十年只为回答一个问题

标题里的“五十年”取的是个约数:从 Unix 分时系统让一台机器同时伺候多个用户算起，差不多;严格从 1993 年 cgi-bin 写进 NCSA httpd 算起，是三十多年。从哪头算不重要，重要的是这几十年来，Web 后端的一切技术演进，翻来覆去只在回答同一个问题:一次 HTTP 请求抵达之后，由谁、在哪、以多大的固定代价，执行“回答它”的那段代码。这一节把这部历史快速过一遍，不是为了怀旧，而是因为不看清进程模型的重量，就无法理解 isolate 的轻到底轻在哪。

1993 年的答案叫 CGI，公共网关接口。它的机制朴素到近乎天真:每来一个请求，Web 服务器就 fork 出一个全新的操作系统进程，去加载一个 Perl 脚本或者 shell 脚本;脚本执行完毕，把输出吐回给请求方，进程随之消亡，一命一还。它的美德是彻底:进程的生与死和请求的生与死严格同步，内存账目清爽，绝不拖泥带水。它的代价同样彻底:fork 一次进程，操作系统要复制页表、拷贝运行时映像、重新初始化解释器——这些成本与“访客到底想要什么”毫无关系，却要每个请求全额支付一次。一台服务器能同时伺候多少访客，不取决于带宽有多宽，取决于进程有多重。

**进程的重量清单（来源:materials/raw_worker.md）**
- 空跑一个 Node 进程，内存约 35MB;
- 一个容器，内存通常上百 MB;
- 容器化进程的冷启动，约 500ms 至 10s;
- 操作系统的一次上下文切换，开销可高达约 100µs。

拿这张清单换算一下:一台 8GB 内存的机器，光是把 Node 运行时摆满，就只够伺候两百来个并发访客;而在高并发之下，还有相当一部分 CPU 周期不花在运行用户代码上，而花在进程之间“换人”上。这就是进程模型五十年来一直背着的东西:隔离是真的，重也是真的。

但先别急着嘲笑它。进程模型有一个直到今天都无法完全替代的美德:隔离由操作系统背书。两个进程的内存互不可见，一个进程崩溃不连累邻居，这套保证被五十年的内核工程反复锤打，是计算世界里信用等级最高的承诺之一。整个九十年代和本世纪初的服务器技术史，本质上是在“隔离的信用”与“进程的重量”之间反复讨价还价:FastCGI 让进程常驻下来，一个进程循环处理成百上千个请求，不再一请求一死;mod_perl 和 mod_php 更激进，干脆把解释器塞进 Web 服务器进程内部，连进程间通信都省了;Java 的 Servlet 容器用线程池让一个 JVM 里的上百个线程分摊同一份运行时。路线各有不同，方向只有一个——把“执行一次代码”的固定成本往下压。

再往后是基础设施的两代更替。虚拟机把物理机切成多台逻辑机，隔离升级到硬件层;2013 年 Docker 把容器变成标准件，打镜像、起容器、扔容器成为动词;2014 年 AWS Lambda 把“函数”变成计费单位，Serverless 这个词从此登上台面，隔离粒度一路从物理机、操作系统、进程，细化到了函数。但要看清一个事实:哪怕到了 Lambda 这里，底下执行的仍然是容器化进程。每收到额外的并发请求，平台还是要冷启动一个新的容器，启动时间就在上面那张清单里——500ms 到 10s。函数是轻了，托举函数的容器没有轻。

这就是问题的关节所在。上面所有方案，不管包装成什么，共享同一个前提:隔离这件事，必须由操作系统层来提供。进程也好，容器也好，虚拟机也好，墙全部砌在内核那一层。而墙是有厚度的:内存是墙的砖，启动时间是墙的砂浆，上下文切换是墙上巡逻的卫兵。想住进墙里，就得支付整面墙的成本;想给全球用户服务，就得按城市复制整栋楼。在墙面预算面前，个人开发者永远是等外公民。

有意思的是，这道题的另一份答案早就写好了，就摆在每个人的桌面上。浏览器每天都在干同一件事:同时运行成百上千段互不信任的代码——你永远不知道下一个点开的网页是新闻还是陷阱，而 Chrome 的选择，是让这些代码跑在同一个 V8 引擎管理的隔离上下文里，一份运行时，成千上万个沙箱，彼此看不见对方的内存。V8 是天生为多租户设计的:它这辈子干的就是这件事（materials/raw_worker.md）。2017 年，Cloudflare 发表了那篇后来被反复引用的工程博客《Cloud Computing without Containers》，标题本身就是宣言:把浏览器的答案搬上服务器，不再用容器承载多租户。配套的开源运行时，后来定名 workerd;每一座边缘数据中心都运行着一份运行时实例，一个实例里同时住着成千上万个用户应用。

于是五十年兜了一圈，问题被压缩成一句话:请求来了，我的代码能不能像函数一样被调用，而不是像进程一样被诞生?CGI 时代，一次请求要陪一个进程走完整的一生;Serverless 时代，一次请求要陪一个容器完成一次启动;而 Workers 的回答是——什么都不要诞生，调用本身就可以是无痛的。对个人开发者的全部意义也藏在这里:当“执行一次代码”的固定成本趋近于零，部署到三百座城市就不再是一次运维远征，而只是一次代码推送。接下来两节，我们把这台机器拆开，先看引擎舱，再算加速账。

## 2.2 Isolate 解剖:单进程里的数千个房客

isolate 的正式定义很短:它是 V8 的隔离上下文，里面装着一段代码可以访问的全部变量，以及一个保证安全的执行环境。Cloudflare 文档把它比作“为你的函数准备的沙箱”（materials/raw_worker.md）。要紧的是它在系统结构里所处的位置:它不是一个进程，不是一个线程，也不是一个容器——它是 V8 引擎在一台已有进程内部开辟出来的一块带围栏的内存，连同围栏内的代码能看到的一切。打开一台边缘机器的引擎舱，从外到内是这样的:操作系统与 Linux 内核;里面运行着若干份 workerd 运行时进程;每个进程内部，并排住着几百到几千个 isolate;每个 isolate 里面，是你的脚本、你的变量、你的事件循环。

**密度卡片（来源:materials/raw_worker.md）**
- 单个操作系统进程:可容纳数百到数千个 isolate，来自不同客户、互不信任的代码共存于同一进程;
- 传统方案的同机密度:通常只有几十个进程或容器，受内存与进程数限制;
- 隔离方式:每个 isolate 的内存彼此封闭，任何 isolate 都无法读到另一个 isolate 的内存。

这就是本章论点里那句话的含义:多租户隔离从操作系统层下沉到了内存层。过去几十年，两家公司的代码要在同一台机器上共存，唯一可信的办法是给他们两个进程、两个容器、两台虚拟机;isolate 说，在内存里画一道围栏就够了——前提是，画围栏的那支笔要足够可信。这既是激进的，也是有恃无恐的:V8 是 Chromium 和 Node.js 共用的同一个引擎，是被安全测试得最充分的软件之一，Google 用十几年、累计几十亿次真实网页加载，给它做了一场无法复制的公开测试。

先看执行模型，因为它决定了写代码的方式。每个 Worker 默认实现一个 fetch（） 处理器;请求抵达任意一座数据中心，运行时就调用对应 isolate 里的 fetch（）。一个 Worker 实例用单线程事件循环处理多个并发请求:await 等待 I/O 的空档，事件循环转去伺候别的请求，空档结束再回来。两个请求可能被路由到同一个实例，也可能落到不同实例——这件事对写代码的人有一个直接后果:官方明确建议，不要在全局作用域里存可变状态。isolate 也不承诺长寿:内存紧张、脚本行为可疑、资源超限，运行时都可能把它逐出后重建。这两条是理解 2.4 与 2.5 的钥匙，先在这里立此存照。

接下来必须回答一个质疑:墙画在内存里，这么薄的墙，还配叫墙吗?Cloudflare 的回答是:薄墙之外，还有三道。四层防御全部来自官方的《Security model》文档（经由 materials/raw_worker.md），一层一层看。

第一层，V8 isolate 本身。内存级隔离，这是主要的安全边界，整座建筑的承重墙。它保证的是:房客与房客之间，谁也看不见谁的账本。

第二层，进程级的 cordons。这个词借自法语，本义是防疫封锁线。每台机器上运行着多个完整的运行时进程，Worker 按信任级别分组:免费版用户的代码，不会被安排与企业客户的代码同住一个进程——万一 V8 哪天爆出一个零日漏洞，攻击者也不能借一道墙洞把整栋楼一锅端。正在调试中的 Worker、CPU 消耗异常的 Worker，会被动态调度到独立进程里隔离观察。像酒店把装修楼层和营业楼层分开，免得一间房走水，烧掉整栋楼。

第三层，OS 沙箱。Linux namespaces 加 seccomp——听上去耳熟，这正是容器技术的地基，但 Workers 的配置比普通容器引擎苛刻得多:完全空白的文件系统（空的 mount namespace）、禁止一切文件系统相关的系统调用、禁止直接网络访问。这里藏着 isolate 模型独有的巧思:普通容器在启动之后，还得靠 exec（） 从磁盘加载程序，所以文件系统必须一直保持可用;而 Workers 的客户程序不是本地二进制，是 JS 和 WASM，代码加载完毕之后，文件系统就没有存在的必要了——进程可以把自己脚下的地板抽掉。容器做不了这件事，因为它还要站在地板上。

第四层，API 设计。运行时根本不暴露文件系统 API——没有 API，就没有访问，这比“授权之后再访问”干净得多;网络只能走 HTTP;isolate 与本地 supervisor 之间的通信走 UNIX domain socket，协议用的是 Cap'n Proto，一种能力导向的 RPC。四层看完，你会发现安全设计有一条贯穿的哲学:与其事后审计，不如让危险的事情在物理上无法发生。

这一节还差最后一块，也是整套设计里最“外科手术”的部分:Spectre 对策。Spectre 一类侧信道攻击的原理，是诱骗受害代码在微架构上留下时间痕迹，再用高精度计时器把痕迹读出来——窃密者要的从来不是数据本身，而是数据留下的影子。Workers 的对策因此全部围绕时间展开:Date.now（） 在代码执行期间被锁定，一次执行之内返回值不再变化;引擎不提供其他计时器;禁止多线程，不发给攻击者制造并发时差的并发原语;再配上动态进程隔离与周期性的整内存洗牌，让跨 isolate 的侧信道几乎无处落脚。时间是这栋楼里唯一的货币，而房东把它收走了。

按惯例补一条诚实条款:这套体系的安全性高度依赖 V8。V8 的攻击面其实比虚拟机更大——它要解析执行来自全网的不受信代码，而虚拟机的边界有硬件辅助。Cloudflare 的赌注是:Google 对 V8 的持续投入，加上上面四层防御，足以把风险压进可接受区间;作为回报，他们换来进程隔离方案十倍以上的密度（进程隔离的 CPU 成本是共享进程的 10 倍以上，materials/raw_worker.md）。信任链从内核转移到了一个 JS 引擎上，这笔交易的得与失，2.5 节接着算。

小结一下这栋楼的住户守则:数千个房客同住，每个人的记忆彼此不可见（内存隔离）;整栋楼分区管理，生人与熟客不同层（cordons）;地基按最严标准浇筑，楼板随时可以抽掉（namespaces 加 seccomp）;楼里连一把能开门的钥匙都不发（无文件系统 API）;连时钟都统一锁死（Spectre 对策）。解剖到此为止，下一节算钱:这套结构到底替住户省下了什么。

## 2.3 为什么快:没有冷启动、没有上下文切换、没有重复运行时

工程上的“快”从来不是一个孤立数字，而是一串被删除的步骤。Workers 的速度可以概括为三个“没有”:没有冷启动、没有上下文切换、没有重复运行时。这一节把三个“没有”逐一摊开，算清这张加速账，顺便回答一个更实际的问题:这一切落在个人开发者的账单上，是什么形状。

先看总账，数字卡片摆出来，全部来自 Cloudflare 官方文档与《Cloud Computing without Containers》一文（经由 materials/raw_worker.md）:

**加速账总卡（来源:materials/raw_worker.md）**
- 冷启动:isolate 约 5ms，容器化进程约 500ms-10s;
- 启动相对速度:约为启动一个 Node 进程的 100 倍;
- 每实例额外内存:约 3MB，对照空跑一个 Node Lambda 的约 35MB，低一个数量级;
- 上下文切换:操作系统一次切换可高达约 100µs;isolate 之间无 OS 级上下文切换;
- 同机密度:单进程数千个 isolate，对照传统方案几十个进程或容器。

第一个没有:没有“为新函数启动一个进程”这个步骤。传统 serverless 每收到额外的并发请求，就要冷启动一个新的容器化进程，500ms 到 10s 的等待就花在这里;而 isolate 不是在“新进程”里创建的，是在已经运行起来的运行时进程内部直接创建的，只需约 5 毫秒。官方原文是:This model eliminates the cold starts of the virtual machine model。这句话翻译过来平平无奇，放在架构图上看才见分量:虚拟机模型里，计算的计量单位是“一台机器的一生”;isolate 模型里，计量单位是“一次调用”。计量单位缩小了三个数量级，一切下游的成本公式随之改写。

第二个没有:没有上下文切换。操作系统在多个进程之间切换，要把上一个进程的状态移出、下一个进程的状态移入，一次可高达约 100µs。单次看微不足道，乘上并发数与切换频率，就是一台服务器相当比例的 CPU 周期——花在“换人”而不是“干活”上。isolate 的世界只有一个进程，一切切换发生在 V8 内部，内核完全不介入，机器几乎把所有 CPU 周期都花在运行客户代码上。用我的话说:这栋楼里换房间不经过大门，走廊都省了。

第三个没有:没有重复运行时。Node 和 Python 这类运行时，生来就不是为多租户设计的——每个函数都要自带一份完整运行时，一个空跑的 Node Lambda 占约 35MB;Workers 让同一进程里的所有 isolate 共享同一份 V8，运行时成本只付一次，之后每个脚本的额外开销降到约 3MB。别小看这一个数量级:内存往往是运行代码最大的成本项，把最大成本项压低十倍，成本模型和速度模型就一起被改写了。这也是为什么 Workers 可以做到“只在真正执行代码的瞬间计费”，等待 I/O 的时间几乎不占资源——你的代码不在的时候，那 3MB 也随时可以被回收。

三个“没有”叠在一起，结论反而反直觉:没有虚拟机、没有容器的代码，比任何其他云计算形态都更接近裸机。中间层越少，离硬件越近。V8 自身还是一台被极端优化的机器:Ignition 解释器负责快速起步，TurboFan 优化编译器负责把热点代码编译到接近原生的速度，再加上内联缓存与高效的垃圾回收——这些都是十几场大规模工程竞赛的沉淀。作为对照，如果放弃共享进程、退回每租户一个进程的隔离，CPU 成本会是共享方案的 10 倍以上（materials/raw_worker.md）。轻，不是省略了安全，而是把安全的实现方式换成了更便宜的。

这套密度换来的不只是快，还有“分布”的资格。上一章提过的那笔账值得在这里再算一遍:官方测算，用 isolate 模型把代码同时部署到 155 个数据中心（当年数字，如今是数百座城市），经济性好于在亚马逊的单一数据中心部署一次（materials/raw_worker.md）。这句话对大厂是成本优化，对个人开发者是资格改判——“网络就是计算机”不再是口号，而是你按量付费的账单科目:你交的是一个函数的钱，拿到的是一张全球网络的门票。

最后落到精算师视角。以下是 2026 年的额度快照，以现行价目表为准（来源:materials/ch02_补充.md）:

**免费账单卡片（来源:materials/ch02_补充.md）**
- 每日请求数:免费版 10 万次，UTC 零点重置，超出后返回 Error 1027;
- CPU 时间:免费版每次调用上限 10ms——注意计的是 CPU 时间而非墙钟时间，等待网络 I/O 不计入，真正执行代码才计时;
- 平台抽样均值:每个请求约消耗 2.2ms CPU，多数轻量场景足够;
- 付费 Standard 档:每月 5 美元，含 1000 万次请求与 3000 万毫秒 CPU 时间，超出部分每百万次请求加 0.30 美元、每百万毫秒 CPU 加 0.02 美元;
- 出站流量:不收费，这条对个人项目的意义，第 4 章展开。

“CPU 时间不等于墙钟时间”这一条，值得单独停一停。一个典型的队列请求，真正的计算只有几毫秒，剩下的几百毫秒都在等 D1 返回——等待是免费的，思考才收费。这个计费哲学和 isolate 的物理结构完全一致:你占用的从来不是一台机器，而是共享引擎里的几 MB 内存与几毫秒周期;你不用的时候，它们立刻归别人用。这就是按量付费能压到“几块几毛”粒度的物理基础。

所以，5ms 与 3MB 不是性能杂志上的装饰数字，而是一条资格线。冷启动以百毫秒和秒计的世界里，你要为一个函数常备一栋房子，常备就要预算，预算就要运维，运维就要一个人假装是一个团队;冷启动以毫秒计的世界里，函数随请求而生、随响应而散，不需要任何人守夜。全球部署第一次成为个人的默认选项，不是因为我变强了，而是因为“每次执行”的重力变小了。下一节，我用自己队列服务的真实源码，验证这份轻盈落在工程上是什么形状。

## 2.4 我的第一个 Worker:knasync 的入口为什么这么薄

理论讲完，上解剖台。我的队列服务 knasync——军团书里那对哨兵与传令兵的底座，手机、NAS、各台机器之间的消息都从它过——就是一个 Workers 项目。它的全部服务端代码，是一个文件:~/ygs/knasync/src/index.js，不到一千行。没有框架，没有路由库，没有依赖，npm install 都不需要。这一节就用这份真实源码，回答一个问题:一个 Workers 项目的入口，为什么可以、也应该这么薄。

先看全部的部署配置。wrangler.toml，照抄如下:

```toml
name = "knasync"
main = "src/index.js"
compatibility_date = "2024-01-01"

# D1 数据库绑定
[[d1_databases]]
binding = "DB"
database_name = "knasync"
database_id = "6ba67ac5-6dae-4efb-9836-6aec45304db6"
```

五行声明:项目叫什么，入口在哪，兼容到哪个平台版本，绑一个 D1 数据库，绑定名叫 DB。没有端口，没有主机，没有进程数，没有内存参数——2.2 节解剖过的那台机器，不向你开放任何一台机器的配置项，因为你的代码根本不独占任何一台机器。

再看入口本身。src/index.js 的第一行到最后一行，结构是这样的:

```js
export default {
    async fetch(request, env) {
        const url = new URL(request.url);
        const SECRET_KEY = env.SECRET_KEY;
        const QUEUE_ZHIHU = 'zhihu';
        const QUEUE_WECHAT = 'wechat';
        const QUEUE_GENERAL = 'general';
        const MAX_QUEUE_SIZE = 50;
        const DEDUP_WINDOW_SEC = 30;
        // ...
        return new Response('Not Found', { status: 404 });
    }
};
```

一个默认导出的对象，一个 async fetch 函数，传进来一个 request 和一个 env，返回一个 Response。这就是入口的全部仪式。没有 main 函数，没有端口监听，没有启动日志，没有生命周期钩子——因为平台调用的是一个函数，不是启动一个程序。2.1 节那个五十年的问题，在代码形状上直接显形:我的项目不是一个“程序”，是无数个“函数调用”的集合，每次请求就是一个调用。

注意一个容易被忽略的细节:上面这些常量，MAX_QUEUE_SIZE 等于 50、DEDUP_WINDOW_SEC 等于 30，全部定义在 fetch 函数内部，而不是模块顶层。这不是随手，是顺从——顺从 2.2 节立此存照的那两条:请求可能落在任意实例上，isolate 随时可能被逐出。既然如此，任何不属于“某一次请求”的状态都不值得放进内存;把常量写在函数里，每次调用重新求值，冷启动成本约等于零，任何一个实例上的任何一次执行都得到完全相同的结果。薄入口的第一层含义在这里:顶层不持有任何会变的东西。

那状态在哪?在 D1 里。knasync 有四张表——queue、submitted、consumed、results，分别管待处理消息、提交窗口去重、消费永久去重、结果广播。Worker 本身是个无记忆的转换器:请求进，查写 D1，响应出。以 /submit 的去重检查为例:

```js
// ① 永久去重 —— 已被消费者成功处理,永远不再入队
const consumed = await env.DB.prepare(
    'SELECT 1 FROM consumed WHERE content = ?'
).bind(content).first();
if (consumed) {
    return new Response(`Duplicate ignored (${queueType})`, { status: 200 });
}
```

内存里不留任何账本，每次都问数据库。于是“isolate 被逐出”这件事对业务的影响是精确的零:哪个实例接的请求、实例活多久、半夜重建多少次，全部无关紧要。很多人以为无状态是一种架构洁癖，在 Workers 上它是物理现实——你的房东随时可能把你搬去另一层楼，你最好根本没有家具。

入口里唯一称得上“安全工程”的一段，是鉴权。受保护的路径统一检查 X-Auth-Key 请求头或 key 查询参数，而比较密钥用的是手写的常数时间比较:

```js
function safeCompare(a, b) {
    if (typeof a !== 'string' || typeof b !== 'string') return false;
    if (a.length !== b.length) return false;
    let result = 0;
    for (let i = 0; i < a.length; i++) {
        result |= a.charCodeAt(i) ^ b.charCodeAt(i);
    }
    return result === 0;
}
```

逐字符异或、累积、最后只看结果是否为零——无论密钥错在第几位，比较耗时都相同，不给时序侧信道留口子。写这段的时候我恰好想起 2.2 节的 Spectre 对策:平台在引擎层把时钟锁死，我在应用层把比较时间抹平，两端做的是同一件事——不让攻击者从时间里读出信息。一栋楼的安全，靠的是房东和每个房客各守各的那一层。

整份入口的路由，是一串 if 语句:/submit 提交并分流，/pull 拉取并清空，/peek 只读查看，/push 回传结果，/results 游标读取，/clear 清空，/publish 统一发布，外加一个 /health。健康检查值得单独看一眼:

```js
if (url.pathname === '/health') {
    try {
        await env.DB.prepare('SELECT 1').first();
        return jsonResponse({ ok: true, ts: Date.now(), db: 'connected' });
    } catch (e) {
        return jsonResponse({ ok: false, ts: Date.now(), db: 'error' }, { status: 500 });
    }
}
```

它检查的不是“进程还活着吗”——进程不重要，平台会保证有进程;它检查的是“状态还在吗”:对 D1 发一个 SELECT 1，能应答，服务就是健康的。在无状态架构里，健康检查的对象从进程变成了数据库，这个细节比任何架构图都直白。

最后交代一处工程考古。我给 knasync 的定义一直是跨机消息队列，军团书里提过它的完整形态是 Workers 加 D1;而打开今天的源码，还能看到设计演化的痕迹——/results 用自增 id 做游标，注释里写着旧版用秒级时间戳、会在同一秒内碰撞丢条目，于是迁移到了严格递增的 id。设计改过版，语义收敛过，但入口的形状从第一版到现在没有变过:一个 fetch 函数，薄得像一张便签。这就是薄入口的第二层含义:计量单位变了。进程时代的部署，部署的是“一个程序的一生”;Worker 时代的部署，部署的是“一个函数的每一次调用”。程序需要照看，调用只需要正确。

顺带把账算清:这个入口每天替我转发几百到几千次消息，加上调试与其他流量，离每天 10 万次的免费额度还很远（materials/ch02_补充.md）;每次调用的真实 CPU 时间按平台抽样均值约 2.2ms 估算，远碰不到 10ms 的上限。一个服务，零服务器，零月费——2.3 节那张资格线，在我自己的项目上，是这么兑现的。

## 2.5 代价清单:JS/WASM 之墙与全局状态的不可靠

写到这里，再不说这台机器的坏话，就不诚实了。每一种计算模型都靠“放弃什么”来换取它擅长的东西:进程放弃密度换隔离，容器放弃启动速度换便携，isolate 放弃的东西同样明确。这一节列一份完整的代价清单——每一项都来自官方文档与设计事实（经由 materials/raw_worker.md 与 materials/ch02_补充.md），然后回答那个真正重要的问题:这份清单，为什么对个人开发者来说是一桩可以签字的交易。

代价一，也是最常被提起的一条:只能跑 JavaScript 和 WASM。这是 isolate 模型最大的缺点，没有之一。你不能把一个编译好的二进制扔上来——任何依赖本地可执行文件的东西，都被墙挡在外面;旧的二进制服务如果不重新编译，就无法迁进来。可编译到 WASM 的语言（Rust、Go、C 的部分生态）是翻墙的通道，但“重新编译”三个字本身就是一次工程冒险:依赖、构建链、平台差异，每一样都可能让你在墙根下耗掉一个周末。这面墙有个名字，就叫 JS/WASM 之墙。墙外住着的居民可以列一串:系统级工具、依赖线程的数值计算、需要任意本地文件的守护进程、以及一切历史遗留的二进制资产。

代价二:全局状态不可靠。这一条 2.2 与 2.4 已经反复出现，这里把它说透。isolate 可能被随时逐出——内存紧张、脚本行为可疑、资源超限，任何一条都能触发重建;两个先后抵达的请求，可能落在不同实例上。因此任何写在全局作用域里的可变状态，都是一个随时会蒸发的事实。但我想换一个角度说:这不是缺陷，这是契约。平台把数千实例的密度和毫秒级调度白送给你，对价是你不许在任何一台机器上留下“必须被记住的事”。 knasync 把四张表全部放进 D1，就是把这份契约变成了设计纪律——状态住在该住的地方，计算随处可生。从这个意义上说，isolate 不是禁止你存状态，而是逼你把状态存对地方。

代价三:CPU 时间上限。免费版每次调用 10ms CPU（materials/ch02_补充.md），超出即被中断，返回 Error 1102。要紧的是理解“CPU 时间”四个字:等待网络 I/O 不算，只有真正执行代码才计时——所以纯转发、队列读写、轻量改写都安然无恙;但服务端渲染、大负载体积的解析、重计算，会在毫秒级的世界里撞墙。付费档默认放宽到 30 秒，上限 5 分钟，但那是另一个价位的故事。此外还有几条配套的边界:免费版单个请求的子请求上限 50 个，脚本体积上限 3MB（与每实例 3MB 内存是两个不相干的 3MB，纯属巧合），waitUntil 允许响应返回后再续 30 秒——超过这个窗口的后台工作，不属于这里。

代价四:单线程。一个 Worker 实例是一个单线程事件循环，没有传统意义上的多线程，没有共享内存的并发原语。它的双重身份在 2.2 节已经露过面:既是 Spectre 防御的一部分（不给攻击者并发时差），也是计算模型的限制（没有核上的并行）。在 Workers 上，并行的唯一形态是等待中的并发——十个 await 同时挂起，事件循环在它们之间穿梭;而不是十条线程分食八个核。多数 Web 场景的瓶颈在 I/O，这够用;但你的问题若真是 CPU 密集，这面墙和代价一是同一面。

代价五:时间被锁死。Date.now（） 在一次执行之内返回值不变，引擎不提供其他计时器。这条 Spectre 对策落到应用层，是一些具体的别扭:knasync 的 /health 回传的 ts 只是个近似时刻，精确到毫秒毫无意义;你想知道“这段代码跑了多久”，在本机上随便测，在边缘上没有表可看;任何依赖高精度时间的逻辑——限速器、平滑抖动、超时细分——都得靠外部时间源或者干脆重新设计。我在写队列去重时用的是秒级时间戳，当时只觉得够用，后来才明白边缘上秒级几乎是唯一的诚实粒度。

代价六，也是最结构性的:安全信任链的转移。2.2 节说过，V8 的攻击面其实比虚拟机更大，整套体系的安全性高度依赖一个 JS 引擎的工程质量，以及房东的多层防御。选 isolate，本质上是把“我信任操作系统内核”改写成“我信任 Google 对 V8 的持续投入，加上 Cloudflare 的 cordons、seccomp 与时钟封锁”。这不是一笔一次性买卖，是一份长期订阅的信任;它是否划算，取决于两家公司未来十年的工程投入。个人开发者未必有能力审计这条信任链，但至少应该知道自己签的是什么。

清单列完，该做裁决了:为什么我认为这份代价对个人开发者是可以接受的?逐条对账。代价一，墙外的居民——二进制、线程、本地文件——恰好是边缘上最不该出现的东西:边缘节点的美德是高密度与无状态，一个带着本地文件系统欲望的程序，本来就是走错了门。代价二，全局状态不可靠，反而治好了我的老毛病:进程时代我总忍不住在内存里缓存点什么，然后在深夜被告警叫起来收拾缓存不一致的残局;逐出机制替我把这条路堵死了，knasync 从第一版起就没有一行全局可变状态，也从未在这类问题上醒过夜。代价三与代价四，对一个胶水层服务——转发、排队、改写、鉴权——是纸面上的限制，我的真实用量离上限差着两个数量级。代价五，时钟锁死，实际影响仅限于让我改用秒级语义，而秒级语义对一台“时间机器”恰好够用。代价六，信任链转移——这一条我确实无从审计，只能用最原始的方式兜底:数据在 D1 里有备份，架构不依赖任何单一平台特性，退出通道时刻保持畅通。这恰好也是第 9 章退出演练的主题。

所以我的结论是:isolate 模型放弃的，几乎全是个人开发者本来就没有的东西——遗留二进制、多机运维预算、以及为一个函数付一栋房子月租的必要;它换来的，是个人开发者从来不敢想的东西——数千实例的密度、5 毫秒的冷启动、按调用计费的账单，以及“代码推上去，全球就都有了”的默认现实。函数比进程更适合个人开发者，不是因为函数时髦，而是因为账算得过来。

最后回扣全书的总纲。五十年的进程史，把“执行一次代码”变成了一件隆重的事:租机房、装机、配网、守护进程，每一步都是对“重”的妥协;而 5ms 与 3MB 这两个数字，终结了这场隆重。从租到有——服务器时代，全球部署是一次远征;Worker 时代，它是一条推送命令。这道算术题做完，还有一个悬而未决的问题:无状态的计算四面开花，那状态住哪?答案在脚下——下一章，D1:SQLite 如何在边缘成为全球数据库。

---

## 第3章 D1 的野望:SQLite 如何在边缘成为全球数据库

# 第 3 章 D1 的野望：SQLite 如何在边缘成为全球数据库

## 3.1 世界上最被低估的数据库，上了世界的网络

凌晨两点，我在家里敲下一条 INSERT 语句，把一条待办消息塞进我的队列。下一刻，这条消息就躺进了一个 SQLite 数据文件的某一页里——那个文件的主副本由平台自动选址，安置在全球某间我从未踏足的机房，而它的几个只读副本，正安静地分布在另一批城市里。我没有为这个场景申请任何资源，没有配置主从，没有研究过半同步复制的参数，我只写了一行最普通的 SQL。

这件事值得停顿一下。因为就在十年前，我为了给一个日访问量不足一千的小站配一个 MySQL，花掉了整个周末：装系统、调缓冲池、配主从、写备份脚本、演练故障切换，最后还因为一个日志格式的坑丢了半天的数据。而今天，同一个诉求的实现方式，是在 wrangler.toml 里粘贴几行配置，然后忘掉它的存在。数据库从一个需要伺候的宠物，变成了一张写着我名字的地契。

要掂出这件事的分量，得先弄清 SQLite 是个什么东西。在数据库的世界里，SQLite 是一个异类：它不是运行在服务器上的服务，而是一个嵌进进程里的 C 语言库；它的全部世界就是一个文件；它没有用户名密码，没有网络监听，没有配置向导，甚至没有许可证——它被贡献给了公有领域。但恰恰是这个“不联网的数据库”，成了人类历史上部署量最大的数据库引擎：每一部手机里都有它，每一个主流浏览器里都有它，几乎所有操作系统和主流应用软件里都有它。它以另一种方式统治了世界——不是靠数据中心，而是靠无处不在的嵌入。

SQLite 的哲学是“小即是美”：整库就是一个文件，复制就是复制文件，备份就是备份文件，事务靠一个精心设计的日志结构保证。这个哲学让它赢得了嵌入式世界，也让它在服务器时代长期被视作玩具——“SQLite 不支持网络、不支持并发写、撑不起生产系统”，几乎是行业的条件反射。于是出现了一个耐人寻味的错位：世界上安装量最大的数据库，恰恰被世界上最庞大的服务器集群拒之门外。

Cloudflare 在 2024 年 4 月 1 日把 D1 推向正式可用（英文 General Availability，简称 GA），做的事情就是给这个被低估的引擎接上世界的网络（来源：materials/raw_d1.md）。此前的故事线是：2022 年底内部 Alpha，2023 年 10 月公开 Beta，期间整个存储后端推倒重来，2024 年春天正式开门迎客。而 D1 的产品定义里有一句话，特别值得逐字读——它不是“改造过的分布式 SQLite”，而是“SQLite 之上叠加了一层分布式服务”：SQL 方言、类型系统、JSON 函数、全文搜索，全部原封不动来自 SQLite；“全球可达”这件事，则交给 Cloudflare 的网络去解决。

这个定义精确地划出了一条工程哲学的分界线。通用分布式数据库的思路是改造数据库本体：把存储引擎拆成多副本共识组，把事务协议换成 Raft 或者 Paxos，用复杂的机制去模拟“单机数据库”的语义。D1 的思路反着来：数据库本体一个字都不改，SQLite 该是什么样就是什么样；所有分布式问题都在服务层解决——唯一性、路由、复制、一致性，由平台负责。用户面对的始终是那个熟悉的 SQLite，而复杂度被压进了平台的地基里。

我在这张地基上拥有的地产，有一个精确的身份编号。打开 knasync 项目的 wrangler.toml，会看到这样几行：

```toml
name = "knasync"
main = "src/index.js"
compatibility_date = "2024-01-01"

# D1 数据库绑定
[[d1_databases]]
binding = "DB"
database_name = "knasync"
database_id = "6ba67ac5-6dae-4efb-9836-6aec45304db6"
```

（来源：~/ygs/knasync/wrangler.toml）这串以 6ba67ac5 开头的 UUID，就是我那张全球地契的身份证号：它指向的数据库主实例由平台选址托管，读副本随流量在六大区域自动启停，账单按行读、行写和存储逐项计算，不使用不计费。绑定名叫 DB 的含义是，我的 Worker 代码里所有对 env.DB 的调用，都会被平台路由到这个编号指向的数据库——“路由到哪”这个曾需要运维工程师整夜操心的问题，如今是平台契约里的一行声明。

价钱更是把“个体开发者”四个字直接当成了定价对象。免费层每天给 500 万次行读取、10 万次行写入，账户总存储 5GB；付费层的口径是每月 250 亿行读取、5000 万行写入起步，超出部分按每百万行读取 0.001 美元、每百万行写入 1 美元计，存储每 GB 每月 0.75 美元，单库上限 10GB（来源：materials/ch03_补充.md，价格随时间调整，以现行价目表为准）。knasync 全天的消息流量，连免费额度的零头都用不完。十年前个人开发者租不起的“生产级数据库”，今天被折算成一个每天自动清零、用不完就作废的日用品额度——这正是全书总纲里“按量付费的日用品化”在数据这一权上的落地。

这就是本章要展开的野望：把世界上最被低估的数据库，架到世界上最靠近用户的网络上。接下来三节，我会拆开这台机器看它的传动结构——一个数据库如何只保留一个可写副本还能全球服务，1978 年的 Lamport 时间戳如何在新世纪还魂，以及我那份 31 行的 schema 如何住进这套系统。最后一节，我们诚实地谈谈它放弃了什么——因为任何一笔划算的交易，都要先看清卖方收走了什么。

## 3.2 一个数据库一个 Durable Object：全局唯一性的智慧

D1 最反直觉的一个设计决定，藏在官方架构文档的第一段里：每一个 D1 数据库，在后台都由单个 Durable Object 承载（来源：materials/raw_d1.md）。要理解这句话为什么是整台机器的传动轴，得先补一课 Durable Object 是什么——它是 Cloudflare 平台上的一个强保证原语：一个全局唯一的名字，对应全网唯一一个活动实例；实例内部单线程，按到达顺序逐个处理请求；全世界的请求只要冲着这个名字来，最终都会被路由到同一个实例面前。名字的唯一性不是约定，是平台的物理承诺。

D1 把这个承诺整个搬给了数据库：一个数据库，一个 Durable Object，一个活动副本。所有写查询先被路由到这个对象，对象内部住着那个 SQLite 文件，文件在同一时刻只被一个执行流触碰。于是 SQLite 原生的快照隔离（Snapshot Isolation）不加改造就成立了——任何时刻只有一个写入者在同一份数据上按顺序执行事务，不存在两个副本同时接受写入然后争吵谁的版本算数的局面，自然也不需要为“谁写的算数”准备共识协议和仲裁法庭。通用分布式数据库里最凶险的那部分复杂度，在 D1 这里被一个“只此一家”的拓扑直接取消了。

随之而来的是一套极其诚实的排队规则。数据库是单线程的，一次处理一个查询，请求多了先排队，队列也满了就返回 overloaded，对应 HTTP 429（来源：materials/raw_d1.md）。第一次看到这个行为的工程师多半会皱眉——“数据库居然会把我的查询扔回来”。但换个角度看，这是平台罕见地拒绝说谎：它不假装无限并发，不悄悄降级，而是明明白白告诉你“我在忙，等一等或者重试”。在第二章我们见过 Workers 对全局状态的诚实，这里 D1 把同一种诚实用在了容量上。

单写者的另一个红利藏在日志里。D1 的存储基于预写日志（英文 Write-Ahead Log，简称 WAL）：所有变更先顺序追加进日志流，再择机合并到数据页。因为整条日志流被完整保留，恢复就成了重放——“时间旅行”不再依赖每天凌晨一次的定时快照，而是可以按分钟回放日志，精确回到过去某一个提交点（来源：materials/raw_d1.md）。这就是 D1 的 Time Travel：付费版保留 30 天，免费版 7 天，每库每 10 分钟最多恢复 10 次。对比传统数据库的备份哲学——快照与快照之间的数据在故障中蒸发——这个“分钟级回放”对一个个人项目来说，几乎是超规格的后悔药：我误删一张表，最坏也只丢几分钟，而且恢复操作本身免费。

当然，天下没有白来的异地容灾。D1 要求每次写入都持久化到多个地理位置才返回成功，这是写入比读取慢的根本原因（来源：materials/raw_d1.md）。但请把这句话反过来看：一个免费额度内的边缘数据库，默认就替你做了跨地域的多点持久化——十年前这叫“两地三中心”，是要写进招标文件的词，如今是账单里一行小字的计价依据。顺带一提，这套存储后端是 2023 年 5 月重写过的，吞吐量比 Alpha 时期的版本提升约 20 倍，写入快约 6.8 到 11 倍（来源：materials/raw_d1.md）；D1 敢把 SQLite 摆上全球网络的底气，一半来自这个底座。

还有一个容易被忽略的细节：D1 的查询并不是在某个神秘的数据库机房里执行的，它就在 Workers 平台内运行，受 Workers 的 CPU 与内存限制约束（来源：materials/raw_d1.md）。于是第二章讲过的那套 isolate 逻辑在这里继续生效：单次 Worker 调用内的查询数量有上限，付费版一千条、免费版五十条；每张表最多一百列，单个字符串或二进制值最大 2MB，一条 SQL 语句最长 100KB，一次查询最多绑定 100 个参数（来源：materials/raw_d1.md）。这些数字拼在一起，勾勒出 D1 的体格——它是一个长在无服务器函数肚子里的数据库：函数能活多久，事务就能活多久；函数的边界，就是数据库操作的边界。理解了这一点，很多用法就顺理成章：它天然适合与 Worker 同生共死的短事务，而不适合放进去跑半小时的长查询。

现在可以回答这一节的标题了：全局唯一性的智慧，究竟智慧在哪？分布式系统几十年来的执念，是让数据的每一份副本都尽量“平等”——多主写入、冲突合并、最终一致，用算法的复杂度去换地理上的自由。D1 的回答近乎固执：不。写只有一个地方能发生，其余一切围绕这个事实来设计。这不是偷懒，而是对 SQLite 事务模型的忠实——SQLite 的快照隔离和它的锁机制，从第一天起就是按“单写者”假设设计的，D1 没有歪曲它，而是给了它一个全球唯一、永不下岗的执行者。

这套智慧在我的 knasync 里有一个具体的落点。我的去重表用消息内容本身作主键——“插入即去重”，同一条消息重复提交，第二次插入会被主键约束直接拒绝。这个简洁语义成立的前提，恰恰是单一写入者：所有插入排队经过同一个执行流，谁先谁后有全局公认的次序，主键冲突才有唯一裁判。假如存在两个可写副本同时接受同一条消息，仲裁“哪次插入是第一次”就会立刻退化成分布式一致性难题，我就得引入冲突时间戳、意向唯一性之类的补丁。D1 用拓扑结构把这一整类问题从我的架构里删掉了。而同一个事实的另一面——唯一的那支笔同时也是瓶颈——留到 3.5 再摊开。

## 3.3 读复制与 Sessions API：Lamport 时间戳在边缘的还魂

单写者解决了正确性，却解决不了地理：让悉尼的用户每次读数据都飞到美国东海岸，延迟会替你做出否决。D1 的解法是 2025 年 4 月 10 日进入公开 Beta 的读复制（来源：materials/raw_d1.md）：主库之外，平台在六大区域自动创建只读副本——ENAM、WNAM、WEUR、EEUR、APAC、OC，覆盖北美东西两岸、欧洲东西两部、亚太与大洋洲。副本由 Cloudflare 根据流量自动创建和停用，不需要人工配置，不额外收费，也不使存储成本翻倍。写查询会被自动转发回主库，读查询就近由副本服务——读可以无限铺开，写永远只有一个入口。

但异步复制立刻带来那个经典难题：副本可能滞后。用户在手机上发了一条消息，三秒后切换到另一台设备查看——请求落在一个还没追上进度的副本上，消息消失了。这不是数据丢了，是“读己之写”这个最基本的直觉在分布式环境下失效了。数据库此刻像个疑心病患者：你写进去的东西，它偶尔假装不记得。

D1 给出的答案，是分布式系统历史上最优雅的一篇论文在边缘的还魂。1978 年，莱斯利·兰波特发表《Time, Clocks, and the Ordering of Events in a Distributed System》，核心洞见是：分布式系统里真正重要的不是物理时钟，而是事件的先后次序——“先于”关系由本机事件序列与消息传递共同定义，据此可以为全系统构造一种逻辑时钟，给事件排出一个所有人认可的全序。D1 的 Sessions API 用的正是这套思想：每次查询都会得到一个书签（bookmark），它本质上是数据库某个已提交状态的 Lamport 时间戳；下一次查询带上这个书签，副本就会等待自己的状态追平书签所标记的那个时点，再把结果返回给你（来源：materials/raw_d1.md）。副本滞后没有消失，但滞后被关进了“你指定的那条因果线”里——你读到的，至少不落后于你自己上一次写。

有了书签，顺序一致性（Sequential Consistency）在边缘兑现为四个具体性质：单调读——一旦你读到某个状态，就不会再读到更旧的状态；单调写——你自己的多次写入按你发起的次序生效；写跟随读——你先读到某数据再写入，后来的读者能看到你写的版本；以及最重要的读己之写——你写的值，你自己下一次一定读得到（来源：materials/raw_d1.md）。注意这不是线性一致，它不承诺全网所有人在同一瞬间看到同一个值；它承诺的是“每个参与者的因果线内部不出现时光倒流”。对绝大多数应用来说，这个承诺刚好够用，而且价格便宜得多。

Sessions API 的使用方式有三种起步姿态，对应三种信任等级：withSession() 不设约束，第一查询可以去主库或任意副本，适合不要求最新数据的场景；withSession(“first-primary”) 强制主库，适合库存、余额这种绝不能容忍陈旧读的状态；withSession(bookmark) 从上次的书签继续，适合跨多个 HTTP 请求保持同一条因果线——浏览器会话、多步操作流（来源：materials/raw_d1.md）。有一个当前的限制要记录在案：Sessions API 仅通过 Worker 绑定可用，REST API 尚不支持。换句话说，想要边缘的一致性红利，你的应用得先搬到边缘上住——这既是限制，也是平台在悄悄划定它的引力范围。

2025 年的几次版本演进，能让这幅图景更完整。5 月 30 日起，REST API 的鉴权改在离用户最近的边缘节点完成，端到端延迟降低了 50 到 500 毫秒，非美区用户受益最大（来源：materials/raw_d1.md）；9 月 11 日起，只读查询获得自动重试——最多两次，且平台保证重试不产生副作用，失败即回滚（来源：materials/raw_d1.md）。前者是把控制面也铺向边缘，后者是把瞬态故障的善后从应用代码里拿走。两条更新单看都不起眼，合起来却是一个明确的信号：读复制时代的 D1，正在把“边缘读、就近认证、自动容错”从可选项打磨成默认值。

这套机制对 knasync 意味着什么？我的队列是多机协同的：一台机器入队，另一台机器消费。目前的规模下我靠幂等表兜底——消息带内容主键，重复投递会被拒收，副本滞后最多让我晚几秒看到新消息，不影响正确性。但如果哪天我要做“发送后立刻在另一端确认可见”的交互，书签就是现成的答案：入队时拿到那次写返回的书签，把它随 WebSocket 的会话状态带在身边，重连之后从书签处续读，因果线就从未断过。我还没有写下这段代码，但知道它躺在平台里随时可用——这是“平台的深度”另一种形式的便宜：你不必今天用，但你确信它在那里。

真正让我喜欢这个设计的是它的诚实。D1 没有假装副本不滞后，没有偷偷把所有读都路由回主库来伪装强一致，而是把“你到底要多新”做成了一个显式参数，交还给开发者。一致性从一个被隐藏的承诺，变成了一个可以被选择的选项——1978 年的那套逻辑时钟，穿越四十八年，最终以一行函数参数的形态降落在离用户最近的机房里。哲学史管这叫复归，工程史管这叫抽象的胜利。

## 3.4 knasync 的 schema：队列数据如何住进 D1

谈完传动结构，该看庄稼了。我的跨机消息队列 knasync，全部持久状态浓缩在一份 31 行的 schema.sql 里——没有 ORM，没有迁移框架，一个 SQL 文件就是整个数据字典。先把原文完整抄在这里，再用它当标本，看队列数据如何住进 D1（来源：~/ygs/knasync/schema.sql）：

```sql
CREATE TABLE IF NOT EXISTS queue (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  content TEXT NOT NULL,
  queue_type TEXT NOT NULL,
  created_at INTEGER NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_queue_type_created
ON queue(queue_type, created_at);

CREATE TABLE IF NOT EXISTS submitted (
  content TEXT PRIMARY KEY,
  last_seen_at INTEGER NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_submitted_last_seen
ON submitted(last_seen_at);

CREATE TABLE IF NOT EXISTS results (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  content TEXT NOT NULL,
  created_at INTEGER NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_results_created
ON results(created_at);

CREATE TABLE IF NOT EXISTS consumed (
  content TEXT PRIMARY KEY,
  consumed_at INTEGER NOT NULL
);
```

四张表，对应队列生命周期的四个状态。queue 是待投递的消息本体：id 是自增主键，content 是消息内容，queue_type 区分队列类型——微信推送、剪贴板同步、多机协同指令各走各的通道，created_at 用整数时间戳记录入队时刻。submitted 是“已接收登记表”：消息到达后先在这里挂个号。results 存放消费之后的产出。consumed 是“已消费登记表”：处理完毕的消息在这里销账。一个消息从我的一台机器出发，到在另一台机器上落地，正好走过这四个格子。

把四张表串起来看，一次完整的消息旅程是这样的：发送端把内容递进来，Worker 先查一眼 consumed——凡已消费过的内容，直接拒绝再次入队；查一眼 submitted——看这条内容是否登记在案；然后往 queue 插入待投递记录，再往 submitted 登记——登记那条语句带着 ON CONFLICT 冲突处理，写的是“存在则更新最近见到时间”，重复到达不报错，只刷新 last_seen_at（来源：~/ygs/knasync/src/index.js）。消费端按 queue_type 与 created_at 从 queue 里取出属于自己通道的消息，处理成功后把产出写进 results、在 consumed 里销账，最后按类型删掉 queue 里的原记录。半程有登记，终点有销账，任何一台机器在任何时刻断线，重启后都能从表里认出哪些是见过的、哪些是没做完的。

真正值得放大端详的，是 submitted 和 consumed 的主键选择：content TEXT PRIMARY KEY——消息内容本身就是主键。主键约束写进表定义，幂等就被压到了存储层：同一条内容无论被投递多少次，在登记表里永远只能占一行，这是数据库物理层面给出的承诺，不依赖任何应用层代码的自觉。而 src/index.js 对这个承诺的兑现方式，正好构成两道闸：登记走“存在则刷新时间”的 upsert，重复消息安静地折叠成一行；入队前先查 consumed，已消费过的内容在门口就被拒收。一条消息从重复投递到重复消费，两道闸都压在主键上。这是队列工程里“幂等键用作数据库主键”原则的最小实现，而它的可靠性根基正是 3.2 讲过的单一写入者——所有插入排队经过同一个执行流，主键冲突才有唯一的裁判。假如存在两个可写副本同时接受同一条消息，仲裁“哪次插入是第一次”就会立刻退化成分布式一致性难题，我就得引入冲突时间戳、意向唯一性之类的补丁。D1 用拓扑结构把这一整类问题从我的架构里删掉了。而同一个事实的另一面——唯一的那支笔同时也是瓶颈——留到 3.5 再摊开。第五章讲队列可靠性时，还会回到这个三角：at-least-once 投递、重试、幂等，缺一不可。

四个索引，每一个都对着一个具体的查询姿势。idx_queue_type_created 是（queue_type，created_at）的复合索引，对应消费端最频繁的动作——按类型拉取队列、按时间排序出货；复合索引的列序是讲究的：先等值过滤类型，再按时间排序，最左前缀匹配让这个查询只扫描该类型那一小段有序区间，而不是全表。idx_submitted_last_seen 服务清理——登记表不能无限膨胀，靠 last_seen_at 找出陈旧记录批量删除。idx_results_created 服务回看——按时间倒序取最近结果。D1 的行读取计费按扫描行数计算，不是按返回行数（来源：materials/ch03_补充.md）：一条无索引的查询，扫过的每一行都算一次行读，哪怕它一行都没返回。所以这四个索引不是性能洁癖，是账单防线——每一次“按类型取消息”都从全表扫描降级为区间扫描，免费额度由此多出数量级的余量。

索引也不是白建的。行写入的计数规则是：写一行数据、涉及索引列则表和索引各计一次行写（来源：materials/ch03_补充.md）。往 queue 插一条消息，实际消耗两次行写——表一次，复合索引一次；往 submitted 登记一条，同样两次。这是精算师视角下的真实成本：索引是拿写入额度换读取额度的交易，建在 WHERE 和 ORDER BY 里出现过的列上才划算，建在从不查询的列上就是白白双倍烧钱。我的四张表六个对象，没有一个索引是装饰。

还有一个不起眼但有意为之的选择：queue 和 results 用了 AUTOINCREMENT。在 SQLite 里，INTEGER PRIMARY KEY 本身就是行号（rowid）的别名，插入时自动取最大值加一；而加上 AUTOINCREMENT 关键字后，引擎会额外维护一张内部表，保证 id 严格递增、被删除的最大 id 不会被复用。代价是每次插入多一次内部表的读写。对许多场景这是浪费，对消息队列却是刚需：id 的递增次序就是消息的入队次序，消费端按 id 排序拿到的就是全序——队列最需要的恰恰是这个不会复用、不会回退的序号。31 行里的每一个关键词，都有它的岗位。

最后谈谈数据节制。D1 单库上限 10GB（付费）、免费层 500MB，账户总存储 2025 年 7 月起提升到 1TB（来源：materials/raw_d1.md）。这个数字对大厂是笑话，对个人是天花板和地板的合一：它逼着你让每一行都值得存在。knasync 的每条记录都是短文本加整数，消息消费后从 queue 迁入 results，登记表靠 last_seen 定期清扫，库的体积常年维持在一个可以整库导出、一眼看穿的量级。小库不只是省存储费——行读取按扫描行计费，库小扫描就快，额度就耐花。数据的节制最终会变成额度的自由，这是“10GB 限额下的数据节制”最实在的注脚。

精算师视角还必须记下 2026 年的一个变化：自 9 月 1 日起，D1 开始强制执行免费层的每日限额，超额之后查询会直接失败，直到次日凌晨零点（UTC 时间）自动重置，或者升级到付费计划（来源：materials/ch03_补充.md）。此前的超限更多表现为限流与提醒，此后变成了硬闸门。对个人开发者来说，这是个值得写进运维手册的日期：消费脚本的每一次全表扫描、监控程序的每一次无节制轮询，从那天起都有了真实的失败风险。我的应对答案就写在 schema 里——复合索引让每次轮询都走区间扫描，登记表按时间戳定期清扫让库保持小体积，扫描行数被压到最低，免费额度自然耐花。配额从来不是道德问题，是工程问题，而工程问题的答案，通常早就藏在表结构里。

## 3.5 它放弃了什么：写扩散与热点的边缘真相

工程考古学的规矩是：每讲一个服务，先问它放弃了什么。D1 的交易清单背面，同样写满了条款，而且比正面更能看出 Cloudflare 的取舍哲学。

第一项放弃，是并发写。所有写查询涌向同一个 Durable Object，在单线程里逐条排队执行；高峰期先排队，队列满了直接返回 HTTP 429 overloaded（来源：materials/raw_d1.md）。这意味着写热点是 D1 的天然禁区：秒杀扣库存、热文点赞计数、直播弹幕洪峰——凡是“全球同时抢着写同一行”的场景，都会被那支唯一的笔卡住咽喉。雪上加霜的是每次写入都要等跨地域多点持久化完成，写延迟里含着地理的距离。通用分布式数据库用多主写入和分片水平扩展把写吞吐摊到全世界，D1 明确地不玩这个游戏：读可以铺满六大区域，写永远只有一个入口在排队。

第二项放弃，是容量与负载类型的野心。单库 10GB 的上限（免费层 500MB）、账户总量 1TB，决定了它装不下数据仓库级别的野心；而行读取按扫描行计费的规则，让分析型的大范围扫描既慢又昂贵——扫一亿行和返回一行，在账单上是两种人生。它也不是天然的时序库和搜索引擎，全文搜索虽能借 SQLite 的扩展凑合，但那是方言的边角而非产品的承诺。D1 是一个为“事务型小而坚”设计的库：状态、元数据、队列、配置、关系——凡是“需要正确性大于需要吞吐”的数据，都是它的主场。

第三项放弃最彻底：多主写入这个通用分布式数据库的核心卖点，D1 直接不要了。读复制是异步、单向、只读的，不存在“两个区域同时可写”的选项；2025 年 11 月新增的 Jurisdiction 能力，允许创建库时把数据固定在欧盟或特定合规区域内运行与存储（来源：materials/raw_d1.md），但那是合规向的收窄，不是能力向的扩宽。Cloudflare 用一个产品决定宣告：对它的目标用户而言，跨区可写带来的冲突仲裁复杂度，远大于它换来的地理自由。这个判断对大厂未必成立，对个人开发者几乎无懈可击——我的队列从不需要在东京和法兰克福同时写入同一行，我需要的是两边都能立刻读到。

把这些放弃摊开，什么时候不该用 D1 就清楚了。写并发高、热点集中的业务，别用它硬扛——热点计数器可以交给 Durable Object 自己，那本来就是个天然的原子计数器；单库会超过 10GB 的重数据，大对象进 R2、D1 里只存指针，各归其位；复杂分析和大规模聚合，用定时任务把数据导出到对象存储离线算，别在生产库上扫全表；需要多区域可写或多云部署的，D1 从第一天就不是为这个设计的。反过来，凡是“读多写少、状态明确、规模可控”的场景——内容元数据、用户会话、配置中心、消息队列的持久层——D1 就是给独立开发者的量身款。

那么这笔交易到底划不划算？回到账本。通用分布式数据库把全部复杂度卖给每一个用户：共识协议的运维、分片的再均衡、副本滞后的调试、多主冲突的仲裁，无论你的项目是一万人的银行还是一个人的剪贴板，账单里都含着这些。D1 做的事是把其中个人用户用不到的九成收回平台，只把顺序一致性和一个书签递到你手上——然后免费额度每天早上自动续满。至于那个单写者瓶颈，knasync 每天几十条消息的流量距离 429 的天花板还有几个数量级的余量，这个余量本身就是“个人项目”与“全球基建”体量差的注脚：我为全球可达性付费，但只按一个人的用量结账。

还有一条隐形的退路值得点名。D1 的本体是 SQLite，这给退出成本画了一条别的数据库很难给出的下限：整个库就是一份标准 SQL 文件，随时可以导出，搬进任何一台装了 SQLite 的机器——包括我 NAS 上那一台。第 9 章做退出演练时会亲手验证这条路径，这里先记下结论：选择一个本体开放的服务，等于把“后悔”的期权也写进了合同。边缘不是牢笼，前提是你入住的那天，就看清了门在哪里。

于是回到本章的论点。D1 不是“分布式的 SQLite”，而是“SQLite 之上的分布式服务层”——它没有把 SQLite 改造成它不是的东西，而是在它四周搭起了一层服务：一个 Durable Object 保住它的单写者纯洁性，WAL 与 Time Travel 把后悔药做成分钟级，读复制把读铺向六大区域，Sessions API 用 Lamport 时间戳守住每条因果线。它放弃写扩散与全球可写，换来了个人开发者第一次买得起的全球数据库。从租到有的故事讲到数据这一权，就是这句话：十年前我租的是别人的一张表，今天我持有的是一个被驯服的全球数据库——读可以无限铺开，写永远在一个地方安静排队。这排队的秩序感，恰是个体开发者买得起的秩序。而秩序之外那些不需要正确性的庞然大物——图片、音频、备份，该往哪里放，是下一章 R2 的事了。

---

## 第4章 R2 与带宽联盟:出站流量费归零之后

# 第 4 章 R2 与带宽联盟：出站流量费归零之后

## 4.1 带宽税简史：云存储最赚钱的一行

我有一个每月例行的小仪式：把上一份云账单摊开，一行一行地看。这个习惯是被教训养出来的——几年前某个月，我的云存储账单里，存储费那一栏只有几毛钱，而“出站流量”那一栏的数字是它的几十倍。那一刻我才看清一个事实：在对象存储这门生意里，存储从来不是主角，流量才是。云厂商最赚钱的一行不在容量，在带宽——我私下管它叫带宽税。

先考古它的前世。带宽税也不是云时代的发明：租 VPS 的年代，带宽按 Mbps 计价，5Mbps 与 100Mbps 的月租可以差出十倍，本质上你买的是“马路的宽度”。2006 年 S3 上线，把计价单位从马路宽度换成了过路费——数据放进来不要钱，被读取、被下载才按 GB 收钱。这门生意的账本由三笔钱构成：存储费是房租，按 GB-月计；请求费是门铃，按次数计；出站流量费是关税，数据想离开机房，每一 GB 都要交税。素材档案（raw_r2.md）记着 2026 年的现行价：出口到互联网，前 10TB 每 GB 0.09 美元，用量越大阶梯越低，降到 150TB 以上的 0.05 美元，每月只有前 100GB 免费。顺带把阶梯记全，免得读者自己去翻：10 至 50TB 是 0.085 美元，50 至 150TB 是 0.07 美元。数字需要翻译成体感：一个 1TB 的数据集，只要被完整下载一次，出站那一行就是约 92 美元——比把它存上五年的房租还贵。

我第一次真正为这行税心碎，是在自建图床的年月：一台小 VPS，装着 nginx，月流量池 1TB。某天一张 2MB 的原图被转上热榜，流量池一夜见底，整站图片变裂图，加购带宽的钱比服务器本身还贵。那时我就该读懂的信号是：带宽在云的账本里从来不是成本项，是利润项——存储亏本引流，流量负责赚钱。看懂这个结构，再看后来者的登场才有意思：它不是把流量卖得更便宜，而是把这一行从账单上直接划掉。

这行税还有一个阴险的性质：非线性。平时的出站曲线平得像心电图上的直线，免费额度或流量池看起来绰绰有余；直到某个文件被聚合器抓走、某篇笔记被推上首页，曲线一夜竖起，账单跟着竖起。带宽税平时不存在，爆时才致命，而爆的时刻，恰恰是项目最成功的时刻。价目表上写着用量越大单价越低，体感却是越成功越疼——阶梯递减，递不过指数级的流量。

还有一个常被忽略的角色：镜像与备份。带宽税年代，许多人连异地备份都懒得做——拉一次全量就是一次出站，备份自己也要交税。零出站之后，异地拉取成了零成本动作，备份的心理障碍被拆除。这一条会在第八章“outbox 与本地的尊严”里再用一次，此处先记下：有些免费，改变的不是支出，是习惯。

这笔税最精妙的地方在于不对称：入站免费，出站收费。数据进得来，出不去，行业给这种现象起了个名字，叫数据引力（data gravitation）——数据像有质量一样，把周边的计算、缓存与应用统统吸到自己身边，因为“把计算搬到数据旁边”永远比“把数据搬到计算旁边”便宜。引力越强，迁移越贵：搬家成本等于数据总量乘以出站单价，1TB 就是 92 美元起步。于是“多云架构”在存储层常年停留在幻灯片上；于是那些需要把数据从中心拉到各处边缘节点的分发架构，每一次分发都在向存储方交税。云存储最赚钱的一行，不是因为世界存了太多东西，而是因为世界读了太多东西——容量是一次性付的，读取是永续付的；前者是资产，后者是现金流。商业史上所有最好的生意，都长在现金流那一栏。

云厂商并非看不见这一行的争议。整个 2010 年代，S3 的存储单价降了好几轮，出站费却岿然不动——降价降在不疼的地方，是因为出站费早已不只是收入，而是护城河：它把“数据迁出”的价格牌立在每一家潜在竞争者的门口，也让每一个想在多云之间调度数据的客户先算一笔搬家税。到后来，连“换一家云”这个念头本身，都要先被出站费计算一遍可行性。锁定从来不需要写进合同条款，一张价目表就够了。

把时间线再拉长一点，对带宽税的反抗早有伏笔。CDN 行业的老兵都记得，Cloudflare 当年在 CDN 市场起家的打法，正是把同行按 GB 计费的带宽改成套餐内不限量——流量免费，按请求与增值服务收钱。存储层的零出站，可以看作同一哲学的续写：先让搬运免费，再在计算与安全上做增值。历史在这里押了韵：先免费的是路，后收费的是路上的服务区。

带宽税最深的影响不在账单，在设计直觉。用惯了 S3 的开发者会不知不觉养成一套防御性架构：图片先压缩再缩略，大文件能不给直链就不给，下载接口加限流加防盗链，能走 API 分发的绝不交付原始文件——因为每一次下载都是敞开的账本。我给早年的项目写过下载限速，写的时候理直气壮，回头看那不是架构，是穷出来的应激反应。独立开发者的项目最怕的从来不是没人用，而是突然被很多人用：爆款意味着出站曲线变成悬崖，意味着月底那张账单可能比整年的域名费还贵。“不敢被下载”——这五个字，就是带宽税写给个人项目的判词。

所以第一次读到 R2 的定价页，我的震动不在便宜，在结构：它把惩罚成功的那一项从公式里删掉了。图床、播客、手绘卡片，都只是这次结构变化的下游结果。在正式对账之前，先把 R2 的身世补一笔：它出生在 2022 年，公开测试那天的新闻标题里，零出站费是唯一的主角——一家公司把“删掉一行收费”当成了发布会的全部。要看懂这件事有多反常，得先把新旧两本账逐行对一遍。

## 4.2 零出站的真伪：R2 与 S3 的逐项对账

精算师看见“零出站”三个字，第一反应不该是欢呼，而是追问：这笔钱去哪了。商业世界里的免费只有三种解释：补贴期钓鱼、拿数据变现，或者它本来就不靠这笔钱活。R2 属于第三种的嫌疑最大——Cloudflare 的主收入在网络与算力，存储对它而言是让流量“有事可做”的地基，不是独立的利润中心；更别说出站费这堵墙恰好挡在客户与它全家桶之间，拆墙对它有直接的商业动机。动机不能当证据，证据在价目表里。下面这张对账表，来自素材档案（raw_r2.md）对两家官网 2026 年现行价的摘录（us-east-1 参考价，写作时请以现行价目表为准）：

| 项目 | Cloudflare R2 | AWS S3 |
|---|---|---|
| 存储 | $0.015/GB-月 | $0.023/GB-月 |
| A 类操作（写入、列举） | $4.50/百万次 | 约 $5.00/百万次 |
| B 类操作（读取） | $0.36/百万次 | 约 $0.40/百万次 |
| 出站到互联网 | 免费，无条件 | $0.09/GB 起阶梯计价 |

第一行就值得盯三秒：存储单价，R2 比 S3 便宜约三成五。很多人以为 R2 的优势全在零出站，其实它在存储维度也更便宜——这排除了“用存储费补贴流量费”的障眼法，它是两个维度同时降价。操作费也略低一线，真正的分水岭只在最后一行：一边是零，一边是每 GB 九美分。阶梯只意味着量大时的折扣，不改变任何结构性结论：折扣再深，乘上一个不断增长的出站量，仍是一条陡峭的曲线；而零，乘以任何流量，还是零。

免费额度的对比更能说明定位差异。R2 每月送 10GB 存储、100 万次 A 类操作、1000 万次 B 类操作，长期有效，不设到期日；S3 的免费层是 5GB 存储，只给 12 个月——那是试用期，不是户口。把免费额度翻译成个人项目的语言，更能看出定位：10GB 存储按平均一张 200KB 的图算，能放五万张；100 万次 A 类操作，哪怕一天上传三千张也用不完；1000 万次 B 类操作折下来，日均三十多万次图片请求，对绝大多数独立项目是天文数字。换句话说，R2 的免费层不是阉割版，是完整版；S3 的免费层是体验装。前者假设你会留下来，后者假设你会长大——长大之后，两套账单结构就是完全不同的逻辑。

“零出站”的成色还得专门验一遍，因为市面上有过先例：Wasabi 同样宣传免费出站，却附带出口比例限制——出站量不得超过存储量级，超了照样收费，业内称之为伪免费。R2 的零出站没有配比、没有阶梯、没有隐藏检索费：无论从 S3 API、Workers API 还是公网域名拉数据，一律零美元。素材档案的原话是“真的且无条件”。对精算师来说，这五个字比任何发布会都值钱——它意味着你可以把“被下载”当作常态来设计，而不必在任何一行代码里防御流量突刺。

对账还要对到计费单位这种细节。存储按 GB-月计，官方口径是计费周期内每天峰值的平均；删除免费，意味着整理桶、清理旧文件不需要看账单下菜；返回 401 的未授权请求不计费，意味着扫描器扫你的桶也不产生费用。这些细节单独看都是零钱，合起来决定你敢不敢把日常运维动作做勤快——计费粒度越粗，人越不敢动；计费粒度越细，人越从容。“长期有效”这四个字，还值得单独鞠一躬：云行业的免费额度大多像超市试吃，限定时长，赌你尝完就不好意思空手走；R2 把 10GB 与两类操作次数写成长期条款，意味着一个项目可以从第一行代码活到最后一行日志都待在免费区——我的图床用了这么久，存储费至今是零，这不是薅羊毛，是条款本来就允许。

把三个典型工作量摆上桌（素材档案里的第三方测算，均为月成本）。场景 A，1TB 存储、每月 500GB 出站的中等流量：R2 约 $16.5，S3 约 $69.5。场景 B，10TB 存储、每月 10TB 出站的图床型配比：R2 约 $160，S3 约 $1138，差出七倍。场景 C，100TB 存储、每月 200TB 出站的内容分发型：R2 约 $1600，S3 超过 $18000。规律清晰：出口占比越高，R2 优势越大；存储与出站一比一时，S3 账单里有九成是带宽税。官方计算器给过同量级的示例：同样的存储量级，R2 每月 $173.25，S3 要 $868.85，每月省 $886。

不过对个人项目要说句实话：上面这些数字常常都趋近于零。pic.want.biz 这样的图床，存储几十 GB、月出站百 GB 上下，在 R2 的免费额度之内，在 S3 的免费边缘，两边都死不了。那么 R2 对个人开发者的真实价值是什么？我的答案是：它消灭的不是账单，是心理税。旧模型下，把公开链接发给陌生人之前，你要先在心里过一遍最坏情况；新模型下，最坏情况是零。敢被转发的链接，才会被转发；敢被爬的图床，才会进搜索引擎。成本结构的改变，最终改变的是分享的胆量。顺带一提，2026 年的 R2 还长出了 Data Catalog（Iceberg 数据目录）与 R2 SQL 分析引擎，存储之外另有新账本，此处按下不表。

对账不能只挑对自己有利的行。S3 仍有三样 R2 短期给不了：更细的存储分层（Glacier 归档、智能分层），深度绑定 AWS 生态的事件体系（Lambda 触发、Athena 查询），更成熟的合规组件（WORM 对象锁定、CloudTrail 审计）。所以结论要克制：冷数据沉底的工作负载，S3 的生态仍然值钱；公开内容的分发，R2 近乎碾压。对账的最终结论只有一句：存储更便宜、操作更便宜、出站归零——R2 在所有会随增长变贵的维度上都便宜，这才是它敢把免费写进合同的底气。写书的人有义务提醒一句：所有对账都依赖时点价格，贵与便宜会漂移，结构不会漂移——零出站是结构，折扣是价格。

## 4.3 pic.want.biz：一张图片的边缘之旅

先看一段流水线，再讲道理。我在终端里敲一句话，LLM 把它扩写成标题、正文与金句，免费图片接口按提示画一张手绘插画，Pillow 把它们排版成一张 1080×1440 的成卡；最后两步发生在两秒之内——上传，出链接。这张卡片从我笔记本到“全球任何一部手机可访问”，中间没有任何一台由我维护的服务器。这条路径的名字，叫 pic.want.biz。

图床的前世并不体面。自建年代，一台小 VPS 加 nginx，带宽按月配给，一张原图上了热榜就能让整站变裂图；三方图床要么压缩你的画质，要么某天悄悄关门，留你全站的图片集体失忆。对象存储加 CDN 是正解，但 S3 时代的正解是按出站收费的，图床于是从顺手的事，变成需要精算的事。R2 之后，图床被简化成三件事：一个桶、一个自定义域、一条命令。

配置考古先来。rclone 的配置文件里，我的 R2 远端长这样：

```
[r2]
type = s3
provider = Cloudflare
region = auto
endpoint = https://<账户标识>.r2.cloudflarestorage.com
acl = public-read
```

type = s3 说明 rclone 把 R2 当成一个 S3 兼容远端来驱动——素材档案里那句“API 层完全兼容 S3”的红利，在工具链层面就是零改造：AWS SDK、boto3、s3cmd、rclone，换凭据换 endpoint 就能搬家。region = auto 是 R2 特有的写法，它不要求你选地理区域，填 auto 即可；个人开发者第一次发现自己不必回答“数据放在哪个区”这种大厂才需要回答的问题。acl = public-read 则是本节的主角：公开桶。

上传只要一条命令：rclone copy 文件名 r2:yuangs。桶名 yuangs。公开链接形如 https://pic.want.biz/文件名——桶绑定了 want.biz 下的自定义域 pic。R2 默认提供一个 r2.dev 开发域，调试方便；正式分发我更愿意绑自定义域，流量走 Cloudflare 自己的网络，证书与缓存都是现成的。自定义域的前提是域名托管在 Cloudflare——第七章会讲，DNS 记录是地契上的界碑，图床只是在这块界碑上挂了块门牌。

再沿着一张图片被点开的方向走一遍。读者在千里之外点下链接，DNS 把 pic.want.biz 解析到离他最近的边缘节点；节点上有缓存，直接回图，这一次请求甚至不会碰到桶；缓存未命中，才回源到 R2 把对象取走——而回源也不产生出站费，因为桶和边缘节点在同一个网络里，联盟内部结算为零。这就是本章标题里“带宽联盟”四个字的字面含义：存储与分发同门，过路费在内部就免了。传统架构里，CDN 回源到中心存储的那一段是纯出站流量，每一 GB 都在计费；同门之后，这一段的价格直接消失。

桶也不必全裸地暴露在公网上。公开桶前面可以站一个 Worker：想加防盗链、想加访问统计、想把某些路径重定向，都在边缘函数里写几行——这是 R2 与 Workers 同门的又一层红利，存储与计算之间同样没有出站费。第一章点过名的五块招牌里，pic 管图床、upload 管上传，fm、q、poe 各司其职，它们与桶的协作都发生在这一层。第 2 章讲 isolate，第 5 章讲队列，而桶，是它们共同的仓库。

这条链路上我交过一次学费。在 Web 控制台里预览 Markdown 文档时，图片若写成相对路径，渲染结果里图片的位置全部降级为纯文字；把地址换成 http/https 开头的绝对 URL，图片立刻回来。原因朴素得近乎残酷：渲染器运行在云端，它只认公网可达的地址；相对路径是一种本地幻觉，它假设“文件就在旁边”，而边缘没有旁边。这个教训还有一个推论：别指望边缘上的任何东西知道你的本地目录结构——边缘的世界观里只有 URL。它后来被我写进使用守则：凡要进边缘的资产，先给它一个公网域名；存储是免费的，门牌必须自己挂。学费单还有一条副产品规矩：每新接一个能渲染 Markdown 的工具，第一件事就是把图床链接粘进去试一遍——工具的脾气各不相同，但脾气都偏向绝对地址。

桶用久了，会长出一套命名纪律。文件名就是 URL，就是对外契约：中文文件名进链接前要 URL 编码，text2card 脚本里那句 urllib.parse.quote 就是为此存在；日期前缀方便按时间翻找；对象一旦发布就不再改名——改内容等于换文件名，旧链接永远指向旧对象。对象存储没有“覆盖”的心智负担，只有“新增”的简单。还有一件被完全隐藏的劳动：证书。自定义域挂在 Cloudflare 上，HTTPS 证书自动签发、自动续期，我从未为 pic.want.biz 的证书写过一行配置。HTTP 时代自建图床，证书是年度考试；边缘时代，证书是空气。这件事与零出站同构：最好的配置，是你感觉不到的配置。

把手绘卡片的最后两步贴出来（摘自 text2card 脚本）：

```
subprocess.run(["rclone", "copy", card_path, "r2:yuangs/handdrawn/"], check=True)
url = f"https://pic.want.biz/handdrawn/{urllib.parse.quote(fname)}"
```

一条 rclone copy 把成卡推进桶里的 handdrawn 目录，链接拼出来直接交给微信发送接口。同一个桶，养活了军团所有的公开资产：handdrawn/ 下是卡片，根目录躺着每一期播客的 MP3，还有播客封面 podcast.jpeg。桶里的目录就是文件夹，路径就是 API。零出站之后，对象存储的哲学才真正成立：对象不可变，链接可外传，流量不心疼。“欢迎被下载”不是口号，是 pic.want.biz 每天发生的事实。

## 4.4 高写入的陷阱：Class A 操作费的精算

零出站容易滋生“R2 不要钱”的错觉，精算师的工作就是把错觉拆开。R2 的账单有三行：存储、A 类操作、B 类操作（低频桶另有检索费）。出站归零只划掉了流量那一行，操作那一行还在，而且对特定工作负载会成为大头——素材档案里官方与第三方的共同提醒是：高写入量（每月数亿次）的场景下，操作费将成为大头，官方给出的对策是批量写入与预签名直传。

先讲操作分类学（摘自补充档案 ch04_补充.md）。R2 把 API 操作分成两档：A 类是改变状态的操作，PutObject、CopyObject、ListObjects、ListBuckets，以及分片上传的每一个环节——CreateMultipartUpload、UploadPart、CompleteMultipartUpload，连列举分片都算 A；B 类是读取状态的操作，GetObject、HeadObject、HeadBucket 之类。价格上，A 类 $4.50 每百万次，B 类 $0.36 每百万次，后者只有前者的百分之八。价目表里也有两处免单：删除类操作（DeleteObject、AbortMultipartUpload 等）不收钱，返回 401 的未授权请求不计费。

免费层每月送 100 万次 A 类、1000 万次 B 类。对读多写少的图床，这是深海：一张图片被看一万次，只消耗一万次 B 类，免费额度够几十张热门图被读一整年。但对写密集的工作负载，A 类就是浅滩：每次上传、每次列举、每次分片，都在消耗同一个小池塘。

A 类与 B 类的对照，值得做成一张数字卡片放在这里（价格与额度来自素材档案与补充档案，2026 年标准存储口径）：

| 操作类别 | 典型操作 | 单价 | 免费额度 |
|---|---|---|---|
| A 类（写与列举） | PutObject、ListObjects、UploadPart | $4.50/百万次 | 100 万次/月 |
| B 类（读） | GetObject、HeadObject | $0.36/百万次 | 1000 万次/月 |
| 删除类 | DeleteObject、AbortMultipartUpload | 免费 | 不限 |

读这张卡片的正确姿势是看倍数：A 类是 B 类的十二倍半，删除是零。一个桶的性格，取决于它的读写比；一张账单的性格，取决于你把哪种读写比交给它。

不过先把恐慌掐灭在算术里。把“操作费成为大头”翻译成次数：要疼，得先穿越 100 万次的免费额度；要“成为大头”，按素材档案的口径是每月数亿次。每月数亿次是什么概念？单设备每五秒写一次，一天一万七千次，得有六千台设备同时往一个桶里写，才够得着数亿次。个人项目几乎永远到不了疼的区间——所以这个陷阱的正确名字是规模陷阱，不是入门陷阱。精算它，不是为了焦虑，是为了知道自己的项目离悬崖还有多远，以及规模化那天该先改哪一行代码。

低频访问桶的三个暗坑也记在案。单价便宜到 $0.01 每 GB-月，但 A 类操作费翻倍（$9.00 每百万次），每次读取另收 $0.01 每 GB 的检索费，并且有 30 天最短存储时长——存 5 天就删，照样按 30 天计。补充档案里官方的例子：1000 个 1GB 对象存满一个月、每个被读 1000 次，标准桶约 $14.85；同样这批对象若走低频桶、存 5 天就删，因最短时长仍按整月计，约 $29.90。便宜的门口，常站着更贵的账单。

我自己离陷阱最近的时刻，在播客同步脚本里。每档节目产出后，同步脚本执行 rclone copy 把整个输出目录推上 r2:yuangs——rclone 的工作方式决定了它要先列举两端、比对差异、再逐个上传；列举是 A 类，上传是 A 类，分片上传还把一个文件拆成多次 A 类。一天多档节目，一月上百次同步，单看每一次都微不足道，累加就是每月上万次 A 类。它目前仍稳稳落在 100 万次的免费额度里，但精算师的习惯让我把它写进了监控清单：免费额度不是护城河，是缓冲垫。

同步策略本身也有讲究。rclone 有 copy 与 sync 两个动词：sync 会把远端对齐成本地，多余对象一律删除；copy 只增不删。归档型桶我一律用 copy——远端是只进不出的仓库，误操作删掉的母带没有后悔药；真要清理，走生命周期规则，让规则按对象年龄自动过期，存储与列举一起瘦下来。素材档案里提到 R2 支持生命周期规则，这是我给规模化预留的阀门。

更有意思的坑在 Go 重构版里。输出目录正被 TTS 引擎写入时，rclone 偶发报“文件正在被更新、md5 不一致”的瞬态失败（原文 file being updated / md5 differ），一次同步就此夭折。解法是给这条命令单独加大重试（摘自 podcast 项目的 sync.go）：

```
{"copy", s.outputDir, "r2:yuangs", "--verbose", "--retries", "3", "--retries-sleep", "2s"},
```

重试买回了可靠性，却悄悄抬高了成本——每一次重试都是新一轮 A 类操作。同一段代码里的注释更值得抄在这里：rclone 失败（已重试）仅告警、不阻断全流程，因为“近几日偶发，不应算整档失败”。这是人不在场世界的典型取舍：宁可少传一个文件，也不能让整条流水线为它停下。可靠性与成本坐在同一副跷跷板上，舵握在重试参数手里；而我的舵法是精准加固——只给最常翻车的输出目录调高重试，其余保持默认，让可靠性只在需要的地方花钱。

最后把素材档案里的减负手段翻译成个人项目语言。批量写入：把 N 个小对象合并成一次 Put，操作次数从 N 降到 1；Worker 聚合：让边缘函数攒一批再落盘；客户端防抖：别在用户每次点按的瞬间都写一次；预签名直传：让客户端拿着限时凭证直传 R2，不过你的服务器。官方与第三方的测算认为，这几招合计可降七成到九成八的操作量。我的两个桶都遵循同一纪律：写入合并，读取放行。精算的最后一道工序是记账：Cloudflare 控制台里有按天的操作量曲线，我给自己定了每月一次的人工巡检——曲线平滑说明同步节奏健康，出现尖峰多半是某天反复重试或脚本跑重了。个人基建没有 SRE 团队，曲线就是体检报告，每月花五分钟读一遍，比出了事再翻日志便宜得多。陷阱从不否定零出站的价值，它只是提醒：免费的那一行最擅长让你忘记要钱的那一行，精算师的目光要落在账单的每一行上。顺带记一个反向教训：省操作的极限是省到不可维护——把上千个小对象合并成一个大文件，操作费归零，读取方却要整包下载，省下的 A 类钱会加倍还进 B 类与带宽。合并粒度要贴着读取模式设计，这是精算师与守财奴的区别。

## 4.5 播客与图床：军团的内容分发为什么都长在 R2 上

《一个人的军团》写过：这支军团由一个人加一群 AI 组成，日夜生产内容——播客、卡片、图、文章。军团书讲的是兵，本书讲的是粮道；而军团所有公开内容的粮道，都长在同一块地皮上：R2 的桶 r2:yuangs。这不是精打细算后的巧合，而是成本模型改变之后的必然——当分发的边际成本归零，所有公开资产自然向同一块免租的地皮聚拢，就像商铺天然扎在免租金的步行街上。

以播客工厂为例。广山之巅——我的播客工厂——已累积超过一千期，每期从 TTS 产出到全球可订阅，靠一个四步同步脚本（骨架摘自 podcast 项目的 sync.sh）：

```
# [0/4] 归档旧文件到 NAS
# [1/4] 同步到 R2(核心存储)
rclone copy "$OUTPUT_DIR" "r2:yuangs" --verbose
rclone copy "$RSS_XML" "r2:yuangs" --verbose
rclone copy "$INDEX_HTML" "r2:yuangs" --verbose
# [2/4] 同步到 iCloud(移动端预览)  [3/4] 更新 GitHub RSS
```

四步各司其职：NAS 收母带，管长期归档；R2 收成品，管全球分发；iCloud 管我自己的手机；GitHub RSS 管订阅发现。三条 rclone copy 往同一个桶里推进三样东西：MP3 音频、rss.xml 订阅源、index.html 索引页。播客客户端拿到订阅地址后，音频的公开链接就是 https://pic.want.biz/ 加上 URL 编码后的文件名，封面固定为 https://pic.want.biz/podcast.jpeg——订阅、收听、封面，一个桶全包。图床与播客共用一块地皮，不是节省，是同构：它们都是公开资产的分发，都欢迎被下载。

四步脚本的节奏值得单写一笔。仅财务早餐一个栏目，项目目录里就躺着早、午、下午、晚一天四档的成稿，再加上其他栏目，同步脚本一天要跑好几轮。凌晨与清晨的定时任务在没有人在场的时候，把新的一期推进桶里——这正是军团书里“时间机器”的另一个侧面：队列让消息在人睡觉时抵达，定时任务让内容在人睡觉时发行。听众未必知道广山哥是谁，但知道一天有四档财务早餐。发行被压缩成一条 crontab，是零出站时代才有的奢侈：反正发出去也不要钱，那就多发几班。

按素材档案场景 A 的口径做个个人样本估算：1TB 存储、每月 500GB 出站，R2 约 $16.5，S3 约 $69.5。音频恰恰是出口与存储比极高的品类——每一期都要被成百上千个客户端完整下载。假设某档节目走红，每天一万次完整下载、单期 10MB，每月出站约 3TB：S3 那一行就是两百多美元起步，R2 是零。再对照场景 B（存储与出站一比一的图床型配比）的第三方测算，S3 约 $1138 的月账单，R2 只要约 $160。对千期规模的播客工厂，这省下的不是零钱——按场景 A 的差价，每月省出的五十多美元够付好几年的域名年费。R2 把“千期音频全部挂在公网供任意下载”从一个需要勇气的决定，变成一个不需要做的决定。

有人会问：iCloud 和 GitHub 的落点是不是多余？不多余，它们各管一层风险。R2 管世界的可达性，iCloud 管我口袋里的可达性，GitHub 的 RSS 管订阅发现的可达性，NAS 管十年后的可达性。平台会变，格式会过时，域名理论上也有意外，把同一份成品铺在四个落点上，任何一个单点失效都不影响军团第二天开张。冗余在架构课上是贬义词，在一个人的基建里是生存策略——毕竟这条流水线的运维部门只有我一个人。

桶的命名空间里还有一层组织学：handdrawn/ 是卡片，根目录是音频，rss.xml 与 index.html 是门面。目录即服务，前缀即部门——一个人的军团用一棵对象树管理全部内容资产。对象存储最被低估的能力不是存，而是组织：它连内容的文件系统都一起免了邮费。

零出站的另一个赠品，是迁入也自由。官方迁移工具 Super Slurper 免费使用，只按它对目标桶产生的 A 类操作收费；素材档案里那张回本表值得背下来：从 S3 搬 1TB，一次性出口费约 $92，此后每月省约 $187，约 15 天回本；10TB 约 5 天回本，50TB 约 8 天。另有 Sippy 按需热迁移——对象被首次请求时才从 S3 后台搬来。配合 S3 兼容 API，从 R2 搬走同样只是改 endpoint 的事：进可攻，退可跑，这正是第九章退出成本演练想要的状态。锁定装置被拆掉之后，留在 R2 成了一个可以随时反悔的选择——而可以随时反悔的选择，才是真正的选择。

军团的分发长在 R2 上，母带却不长在这里。原始音频、过程稿、中间产物，归档在 NAS——那是本地优先的地盘，泄露成本高的东西不出家门；见得了天日的成品才推上桶，走边缘优先的路线。同一份内容住两个地方，一个负责保险，一个负责广播；这条分界线的完整画法，第八章用“泄露成本坐标系”展开，此处先立此存照。千期之后还有一个现实问题：桶会长大。音频按期累积，封面与索引页也在累积；好在存储本身便宜（$0.015 每 GB-月），热区交给生命周期规则，旧档在 NAS 与 R2 各留一份。图床与播客共用一个桶的另一个好处是心智成本低——我只需要记住一个域名、一个桶名、一条命令，军团所有公开资产的地址都在同一个命名空间里。

回到本章论点。带宽税年代，云存储的成本模型是“不敢被下载”，架构师的每个决定都在给流量设卡；R2 把出站那一行划掉之后，模型改写成“欢迎被下载”，pic.want.biz、千期播客、手绘卡片，全都长成了从前不敢长的样子。这也是全书总纲的又一次应验：从租到有——我租不起流量，但买得起一块不收过路费的地；按量付费的日用品化，在这一章的形态是，下载这件事从要看账单的奢侈品，变成了不要钱的自来水；成本即哲学——账单上被划掉的那一行，最终变成架构里被放开的那一类本能。凌晨三点，定时任务把新的一期推进桶里，人的不在场不妨碍世界的收听；出站流量费归零之后，被下载不再是要防御的风险，而是值得庆祝的事件。

---

## 第5章 队列即时间机器:knasync 与人不在场的世界

# 第 5 章 队列即时间机器：knasync 与人不在场的世界

## 5.1 人不在场的问题：RPC 与队列的哲学分野

一切通信方式，都在回答同一个问题：通信的双方，必须同时活着吗？

本地函数调用从不为这个问题操心。调用方把参数递过去，跳进函数体，拿回返回值，整个过程发生在同一个进程、同一份内存、同一条时间轴上——调用方与被调用方是同一个程序的两半，它们共享的不只是数据，还有“现在”。RPC（远程过程调用）把这层默契拉长到网络的尺度，却没有解除时间的绑定：客户端发出请求然后阻塞，服务端必须在此刻活着、醒着、接得住；任何一方缺席，这次调用当场失败。为了对抗这种脆弱，工程师发明了超时、重连、退避、熔断，一整套词汇描写的其实是同一件事——如何应付“对方恰好不在场”。同步调用是一通电话：铃响的时候你必须在线，否则就是未接来电，而未接来电在工程里的名字，叫报错。

队列的回答朴素得多：别打电话，留言。消息写进队列的那一刻，通信就已经完成——完成的是“托付”，不是“送达”。生产者发完就走，消费者随后来取，两端不必认识，不必同时在线，甚至不必知道对方的存在。《一个人的军团》里我把 knasync 叫作军团的驿道，驿道的本质正是这个：驿卒不等你，驿站不歇业，信搁在驿站的架子上，什么时候有人来取，什么时候才算走完最后一程。队列把“现在必须”改写成“最终一定”，把时间从通信里解绑——这就是我给它起的哲学别名：时间机器。消息从现在寄出，在未来被领走，中间那段没人值守的时间，由队列替你保管。

这套哲学在工业界的标准答案是 Cloudflare Queues，本章讲原理时拿它当参照系。它的概念模型只有四件东西：队列本身是自动伸缩的缓冲区，消息写入成功就不会丢，只有被成功处理后才删除；生产者绑定队列调用 send() 发布消息，一个队列可以挂多个生产者；消费者负责取货，可以是推送式的 Consumer Worker（队列有消息就自动触发），也可以是 HTTP Pull 消费者（任何能发 HTTP 请求的服务主动来拉）；消息本体是任意可序列化的对象。还有一条容易忽略的规矩：每个队列只能有一个消费者——这不是缺陷，是为了保证投递语义的纯粹。

> **数字卡片**|Cloudflare Queues 四概念：队列、生产者、消费者、消息；每队列仅一个消费者；消息写入成功不丢失，处理成功才删除；不保证按发布顺序投递（来源：materials/raw_queues_ai.md）

那我为什么还要自研 knasync，而不直接用 Queues?三个理由，都很个人。第一，消费者的身份不纯：我的消费者是 Chrome 扩展、NAS 上的守护进程、手机快捷指令、命令行工具，它们不是 Worker，也未必愿意为拉消息接入一套新的签名流程；knasync 的消费者只需要会发一个带 X-Auth-Key 的 GET 请求。第二，仓库已经在手里：第 3 章讲过，D1 就是边缘上的 SQLite，四张表就能安放队列的全部语义，与其引入新组件，不如让数据住在已有的地皮上。第三是精算师的私心：D1 免费层每天 500 万行读，Workers 免费版每天 10 万次请求，我的消息量级一辈子摸不到收费线。Queues 的概念我全部借来用，实现却按一个人的尺度重新丈量——这也是全书反复出现的姿势：大厂的产品定义词汇表，个人开发者在词汇表里挑性价比最高的那几个词。

人不在场，是这套架构真正的服务对象。晚上十一点半，我在手机上看到一篇文章，顺手丢进队列；处理它的程序跑在另一台机器上，此刻没人盯着我，也没人盯着它。它半夜两点来取件，四点把成品放回广播架，早上七点我醒来，承诺已经兑现。整个过程中没有任何人保持清醒，也没有任何一个环节要求两端同时在线。队列即时间机器——它让“信守承诺”这件事第一次不依赖任何人的在场。这正是本章要拆开的机器，而它的第一块地层，埋在一个浏览器扩展的仓库里。

再把“解绑”说得更技术一点，时间从通信里解绑，解开的其实是三种时间差。第一种是生产与消费的时间差：我在地铁上提交，程序在家里消费，两端的在线时刻不必重合，队列替两个无法见面的时刻保管信件。第二种是人与机器的时间差：人按作息生活，机器按负载排班，队列是两者之间的时间汇率兑换口，一边的空闲可以存进去，另一边的忙碌可以取出来。第三种是机器与机器的时间差：两台机器的负载曲线、网络质量、时钟节奏全不一样，队列让它们各自按自己的节拍跳舞，却合着同一首曲子。同步调用要求三种时间差同时归零，这是奢侈的要求；队列把它们统一放宽为“最终对齐”。放宽不是免费，消息要在队列里住上一段时间——住多久、住得安不安全、住到没人认领怎么办，这三个问题分别是后面三节的主题。

还有两条 Queues 的规矩值得单独一看。一条是每个队列只设一个消费者，它听起来像限制，其实是纪律：两个消费者抢同一条队列，重复与丢失都会从缝隙里长出来，保证投递语义的前提是别让语义先被竞争污染。另一条是不保证按发布顺序投递，它听起来像缺陷，其实是诚实：全球几百个边缘节点各自收信、各自发车，不存在一把全局的排队尺。承认这两条再设计系统，期望就不会写在错误的地方——顺序重要，就把序号写进消息里由消费者排序；顺序不重要，就彻底别操心。

## 5.2 knasync 的原型演化：从 worker-fixed.js 到正式版

工程考古学讲究先看地层。knasync 的最底层不在 knasync 仓库里，而在 ~/ygs/zhihu_extension/worker-fixed.js——一个浏览器扩展的仓库，263 行，一个完整的 Worker。文件名里那四个字母 fixed 像工地上的临时铭牌：它不是图纸上的第一版，是改到能跑、来不及改名的那一版。它的出生动机很具体：知乎助手扩展需要一个云上的落脚点，手机上看到好内容，提交上去；家里电脑上的扩展来领任务。生产者是手机，消费者是浏览器扩展，中间那个既不在手机里也不在电脑里的东西，就是这份 263 行的文件。

原型的骨架到今天都没换过，值得原文抄录（出处：~/ygs/zhihu_extension/worker-fixed.js）:

```js
const QUEUE_ZHIHU = 'zhihu';
const QUEUE_GENERAL = 'general';
const MAX_QUEUE_SIZE = 50;
const DEDUP_WINDOW_SEC = 300;
```

两个队列、五十条容量、三百秒去重窗口，四个常量就是整个队列的宪法。POST /submit 把内容塞进 D1 的 queue 表，GET /pull 把内容取走并删掉，GET /results 让其他客户端用时间戳当游标围观处理结果。没有框架，没有依赖，连包管理器都省了——平台给什么就用什么，这是从扩展时代一路带过来的家法。

正式版有自己的名字和生日。git 首提交在 2026 年 5 月 14 日，提交信息只有一句：init: add project with API documentation。仓库取名 knasync，wrangler.toml 一共九行，原文如下：

```toml
name = "knasync"
main = "src/index.js"
compatibility_date = "2024-01-01"

[[d1_databases]]
binding = "DB"
database_name = "knasync"
database_id = "6ba67ac5-6dae-4efb-9836-6aec45304db6"
```

入口是 src/index.js，如今 996 行，依然单文件、零外部依赖；D1 绑定名就叫 DB，直白得像往墙上钉钉子。第 2 章讲过，isolate 可能被随时逐出，全局状态不可靠，所以入口必须薄：fetch handler 只做路由和鉴权，所有状态全部下沉到 D1——Worker 是没有记忆的信使，D1 才是唯一记得事的那一方。

于是整个系统分成清晰的三层。入口层是 src/index.js 的路由表：submit 入队，pull 取件，peek 只读围观，push 回写结果，results 游标广播，publish 统一发布。存储层是 schema.sql 的四张表：queue 存待办，submitted 存近期提交，results 存成品，consumed 存已消费的凭据。通道层是最容易被误解的一层，值得诚实交代：knasync 没有 WebSocket。消息系统在很多人的想象里等于长连接推送，我也认真掂量过这条路——Workers 上的 WebSocket 连接必须有 Durable Object 承载状态，等于为了“实时”两个字引入一整层新的有状态组件，复杂度上一个数量级。而队列的本职本来就是解绑时间，消费方晚一分钟取件毫无损失。所以通道层的最终形态是：消费者按自己的节奏轮询，间隔默认六十秒，一个环境变量的事——wechat-queue-poller 脚本里那行 POLL_INTERVAL 等于 60，就是通道层的全部节奏。时间机器不赶时间。

从原型到正式版，变化都记在 git 的提交史里，每一笔对应一次真实的需求或真实的翻车：队列从两个变三个，新增 wechat，微信链路从此有了专属车道；去重窗口从三百秒压到三十秒，防抖要挡的是几秒内的手抖，五分钟的封锁会连正当的重试一起挡在门外；新增 consumed 永久去重表，消费确认过的内容永远不再入队；/results 的游标从秒级时间戳改成自增 id，因为同一秒写入的两条结果会撞车丢数据，这是一次真实的 bug 修复；/push 增加 ack-only 模式，只确认、不广播；新增 /peek 只读视图，浏览器地址栏直接看队列水位；鉴权补上查询参数写法，因为浏览器发不了自定义请求头。队列数量、去重窗口、游标类型、确认语义——四个变量全部动过，骨架却一根没换。

最能说明这次演化分寸感的是兼容。正式版连 queue_ 前缀的旧写法都留着，pull 的参数写成 queue_zhihu 也能认出来，代码注释里明写“兼容带前缀的命名”。原型和正式版说的是同一套协议，老消费者不换一行代码就能继续过日子。《一个人的军团》里我写过“新版本向后兼容旧习惯”，说的就是这段代码。考古挖到这里可以看清一件事：所谓演化，不是推倒重来，是同一根脊柱上长出新器官。

四张表值得逐一点名，队列的全部语义都住在里面。queue 表是正厅：id 自增，content 存消息本体，queue_type 存车道，created_at 存进门时刻，索引按“车道加时间”组合建好，取件永远是按时间升序的查询——先来先服务，这条队列是严格的 FIFO，公平得像银行叫号。submitted 表是门口的登记簿，记住每个刚进门的名字和时刻，窗口期内同名者请勿重复排队。results 表是成品货架，同样以自增 id 排序，后来的成品永远排在货架末尾，游标沿着货架编号推进。consumed 表是出站口盖章的本子，盖过章的名字不再二次放行。四张表、三个索引、零存储过程，队列的全部机构一目了然。

还有一个不起眼的端点叫 /health，执行一句 SELECT 1 检查数据库连接——全部运维动作浓缩成一件事：确认账房先生还醒着。回头看这份地层剖面，还能读出一个组织的生长方式：原型那四个常量里，只有 MAX_QUEUE_SIZE 纹丝未动，五十条的上限从扩展时代一路沿用到今天。容量克制是最长寿的品质——功能会进化，节制不会。

## 5.3 可靠性三角：at-least-once、重试与死信

讲完骨架，该讲承诺了。队列对使用者许下的诺言，拆开是三段论：第一，消息会不会丢；第二，处理失败了怎么办；第三，救不回来的消息去哪里。三段各有一件对应物：投递保证、重试机制、死信队列。三者合围才是完整的可靠性，缺任何一边，承诺都漏风。参照系 Queues 在这三段上给出的参数，值得整版抄录。

> **数字卡片**|Cloudflare Queues 可靠性参数：默认 at-least-once 投递；max_retries 默认 3 次，可配置，上限 100 次；重试耗尽进死信队列 DLQ，未配置 DLQ 则永久删除；消息保留期 60 秒至 14 天，免费版固定 24 小时；单队列积压上限 25GB；批内默认全有或全无重试，可用 ack()/retry() 逐条确认（来源：materials/raw_queues_ai.md）

先说 at-least-once。这个词组的直译是“至少一次”，完整的表述是：一条消息可能送达一次，也可能送达多次，但绝不静默消失。为什么工业界的默认值是“宁可重复，不可丢失”?因为丢失和重复的代价不对称：丢失是无声的，没人知道少了什么；重复是有声的，系统至少看得见它发生了两次。看不见的故障无法修复，看得见的故障才有得谈。于是可靠性工程把球踢给了下一环——你既然保证会重复，重复就必须无害。这个伏笔，5.4 收网。

重试是三角形的第二条边。max_retries 默认 3，不是技术常数，是一种概率判断：绝大多数故障是暂时性的——网络抖一下，下游打个嗝，三秒钟后自己好了，重试一次就能过；三次还不过，大概率不是抖动，是病。上限 100 次则是给执拗的人留的：有些下游就是要停机十分钟，你算准了，就让它多试几轮。Queues 还允许把重试的粒度交给消费者：批量投递时默认整批共进退，批内一条失败，整批重来；精细的消费者可以用 ack() 与 retry() 逐条表态，成熟的留，没熟的退回。全有或全无是省心的默认，逐条确认是负责任的选项，两者之间隔着运维的心智余额。

第三条边最冷门也最必要：死信队列。重试次数耗尽之后，消息总要有个去处——没有 DLQ，它就被永久删除，连一声告别都没有；配了 DLQ，它被移进另一条队列封存，像医院的病历室，不埋葬，留着解剖。DLQ 是系统的自白书：它明明白白告诉你，我尽力了，这几条我治不好。一个人的系统没有值班的运维，DLQ 的意义因此加倍——它是少数会主动向你汇报失败的地方。看 DLQ 是纪律；不看，它就只是一份没人翻的病历。

现在轮到诚实环节：knasync 的 /pull 是拉取即删除——先按时间升序取出，紧接着删除，两步之间数据已经不在队列里。这是教科书定义的 at-most-once，比 at-least-once 更激进。敢这么写，是因为个人系统的消息有一个企业系统没有的性质：可再生产。队列里跑的绝大多数是一个 URL、一段文本，丢了重发一遍，成本是零；真正不可再生产的产物走 results 广播加游标，那是另一本“不重不漏”的账。再看消费者一侧的纪律：扩展领到任务做不动，会重新推回队列，指数退避，五秒、十秒、二十秒、三十秒地往后挪，遭遇风控就整队暂停两小时——把失败处理得体面的，从来都是消费者自己的规矩（军团书第 10 章的原话，这里照抄）。代码不长，值得看一眼（出处：~/ygs/knasync/src/index.js）:

```js
const result = await env.DB.prepare(`
    SELECT content FROM queue WHERE queue_type = ? ORDER BY created_at ASC
`).bind(requestedQueue).all();
if (!result.results || result.results.length === 0) {
    return new Response(null, { status: 204 });
}
await env.DB.prepare('DELETE FROM queue WHERE queue_type = ?').bind(requestedQueue).run();
```

knasync 也有自己的死信，只是没有仪式。每个队列五十条上限，超出自动删除最旧，删除就是死因，没有 DLQ 收容。那谁来兜底？我的答案是 /peek——只读视图，浏览器地址栏输入地址就能看到三条队列的实时水位。每天路过看一眼：队列平坦，风平浪静；队列堆积，消费者出了事。一个人的系统里，DLQ 可以省，“有人看”这一环省不掉；省不掉的那个人，就是我。

最后把可靠性翻译成账单，这是精算师的本能。

> **数字卡片**|Cloudflare Queues 计价：免费版每天 1 万次操作；付费版每月含 100 万次，超出 $0.40/百万次；1 次操作等于 64KB 数据的一次写入、读取或删除；投递一条消息通常消耗 3 次操作（写、读、删各一）；每次重试额外 1 次读取；写入 DLQ 额外 1 次写入；无出站流量费（来源：materials/ch05_补充.md，搜自官方定价页）

这张卡片读出的潜台词是：可靠性是有单价的。每多重试一次，账单多一行读取；每条死信进病历室，再记一笔写入。免费额度一天一万次操作，按一条消息三次操作折算，每天大约养得起三千条消息——对个人绰绰有余，但数字就摆在那里。可靠性三角的另一面，是三角形的价格；这件事，云厂商的定价页比任何架构文档都诚实。

数字卡片里有两行参数值得单独咀嚼。第一行是消息保留期，60 秒到 14 天，免费版固定 24 小时。这个参数画出“最终一定”的边界：队列承诺的是最终送达，不是永久保管；保留期一过，无人认领的消息就到期离场。所以“最终”是有限期的最终，时间机器的车票印着有效期，这是使用队列的人必须刻在脑子里的条款。好在我的消费者最勤快的以秒计，最懒的也不过一分钟一轮，离撞线还差着几个数量级。第二行是积压上限，单队列 25GB。积压不是存款，是债务：队列越堆越长，说明出水口堵了；堆到上限，新消息进不来，那才是真正的灾难。盯积压因此是队列运维的第一课——错误日志记录的是已经发作的病，积压曲线预告的是正在发生的病，水位比日志更早知道系统不舒服。

三角形还缺一条边的注脚：这套保障的适用范围。at-least-once 保障的是“投递到消费者手里”，不保障“消费者一定处理成功”——处理是消费者的事，队列只负责把消息递到、递够次数、递不掉的收进死信。把这三件事的边界画清楚，就不会出现“队列没告诉我消息丢了”式的错怪；一个人的系统里，错怪自己的基础设施，是最冤枉的一种浪费时间。

## 5.4 幂等：时间机器的保险单

时间机器有一个没法掩饰的副作用：它可能把同一封信送两次。5.3 说过，at-least-once 的默认里藏着重复的必然；于是幂等登场——它不是一个优化技巧，它是时间机器的保险单。定义朴素得近乎数学：同一个操作，执行一次和执行一百次，系统的最终状态相同。保险单的逻辑也一样：它不阻止事故发生，它让事故不再致命。

Queues 的官方文档在这件事上毫不含糊，原话的中文整理是：若业务不允许重复，建议在写入队列时生成唯一 ID，并用作数据库主键或幂等键去重。注意这个建议的分量——它没有说“加一个去重模块”，它说的是把唯一 ID 直接造成数据库主键。主键是数据库里最硬的约束：同样的键插两次，第二次必然失败，数据库用最古老的方式替你守住底线。

knasync 的 schema 把这条建议执行得比官方示例还彻底。schema.sql 全文四张表，其中两张的去重字段直接就是主键（出处：~/ygs/knasync/schema.sql）:

```sql
CREATE TABLE IF NOT EXISTS submitted (
  content TEXT PRIMARY KEY,
  last_seen_at INTEGER NOT NULL
);

CREATE TABLE IF NOT EXISTS consumed (
  content TEXT PRIMARY KEY,
  consumed_at INTEGER NOT NULL
);
```

内容即幂等键，幂等键即主键——消息文本本身就是那把唯一的钥匙，不需要额外生成 ID，不需要额外哈希，存储引擎亲自把关。这不是巧合，就是 Queues 文档那句话在一个人的系统里的原样落地。

两张主键表各管一段时间，合起来是两级去重。第一级在 submitted:三十秒滑动窗口，同一内容三十秒内重复提交直接拒收，注释里写明用途——防快捷指令连发。第二级在 consumed:永久去重，凡是被消费者确认处理过的内容，永远不再入队，记录保留三十天后清理，防止表无限膨胀。两级分工清楚：窗口去重管手抖，永久去重管重复投递。git 提交 054b8ba 的信息把这层设计压缩成一行：去重优化——30s 窗口防抖 + consumed 永久去重。动机在军团书里写过：地铁里信号抽风，快捷指令重发了三遍，队列里只排一单。实现如下（出处：~/ygs/knasync/src/index.js）:

```js
// ① 永久去重 —— 已被消费者成功处理,永远不再入队
const consumed = await env.DB.prepare(
    'SELECT 1 FROM consumed WHERE content = ?'
).bind(content).first();
if (consumed) {
    return new Response(`Duplicate ignored (${queueType})`, { status: 200 });
}
// ② 窗口去重 —— 30 秒内同一内容重复提交(防快捷指令连发)
```

消费者的确认如何写回？答案是 /push 端点的 original 字段——这是 knasync 版的显式确认。消费者处理完，把原始内容作为 original 传回来，Worker 执行一句：

```js
await env.DB.prepare(`
    INSERT INTO consumed (content, consumed_at) VALUES (?, ?)
    ON CONFLICT(content) DO NOTHING
`).bind(original, now).run();
```

ON CONFLICT DO NOTHING 这四个词，就是幂等的全部修养：确认动作本身也幂等，确认一百次和确认一次效果相同。这一层还有个 ack-only 模式——消费者本地已经处理过、只差告诉服务器标记一下，就只传 original 不传 content，结果不进广播、只记凭据。这个模式是 git 提交 52ad517 加上的；紧随其后的 1c2dfdb 修了一个真实的 bug:JSON 解析失败的兜底逻辑把 ack-only 请求整个当成纯文本内容，写进了 results 广播表——确认通道和数据通道混了线。修复后的 readContent 里躺着一行注释，大意为 ack-only 请求不应把整个请求体当内容写入 results。保险单上涂改过的地方，往往就是未来出险的地方。

游标的幂等是另一段考古。早期 /results 用秒级时间戳当游标，git 提交 bc18045 修的正是它：同一秒写入的两条结果，时间戳相同，游标一跳就吞掉一条——广播“不重不漏”的承诺在细节上破了洞。改成自增 id 之后，严格递增，游标语义从此是数学事实，而不是美好愿望。对照 Queues 批内全有或全无的重试：一次 /pull 领走整个数组，要么都领走，要么都别动，不存在领一半——个人尺度的批和工业尺度的批，共享同一种干脆。

收个束。幂等不是技术洁癖，是一种预先的豁达：先承认重复必然发生——时间机器会抖，信使会重访，这是 at-least-once 世界的天气；再让重复无害——主键挡住重复入库，确认表挡住重复投递，游标挡住重复广播。人不在场的时候，没人当场判断“这条是不是处理过了”，所以判断必须提前写进结构里。保险单最贵的部分从来不是保费，是签合同那天就得想清楚所有出险方式——幂等键，就是那天签下的名字。

可能有人问：为什么不学官方建议生成 UUID，偏拿内容本身当主键？答案藏在个人系统的消息形态里——我的消息绝大多数是 URL 和短文本，天然自带唯一性，同一篇文章的链接就是同一个任务。内容寻址还有一层额外的好处：去重发生在入库之前，重复消息连队列都进不去，而不是进了队列再被消费者丢弃——把无益的东西挡在门外，好过放进屋里再扫地。当然这个设计有边界：它默认“内容相同即任务相同”。将来若出现同一内容对应两个不同任务的需求，再补一层显式的任务 ID 也来得及——架构跟着需求长，不跟着想象长。

消费者一侧同样有幂等的功课。同一条消息被投递两次，意味着解读分析跑两遍、微信推送发两条，重复的代价由下游承受。所以微信链路的轮询脚本把拉到的消息先写进本地的 messages.jsonl 台账，每条带一个 analyzed 标记，分析循环只认未分析的条目（出处：~/.pi/agent/skills/wechat-queue-poller/scripts/poller.py）——这层本地台账与云端的 consumed 表互为表里：一个管“云端不再投递”，一个管“本地不再执行”，两头都上锁，重复才真正无害。保险单从来不止一张，可靠的系统让它们互相背书。

## 5.5 一个人的分布式系统：多机协同实录

把前四节叠起来，就能看清 knasync 真正的形态：一个人的分布式系统。生产者住在手机、浏览器扩展、命令行和 MCP 工具里；消费者住在两台常开的主机、NAS 上的守护进程、Chrome 扩展里；中间人只有一个——Cloudflare 边缘上一个 996 行的 Worker，和它背后那个小小的 D1。没有哪台机器认识全部的谁，每一端只认识一个地址和一把 X-Auth-Key。分布式系统教科书里的角色表，在一个人的尺度上全员到齐，只是工资单上只有一个名字。

实录第一幕：手机剪贴板到家里电脑。手机装一个快捷指令，复制即触发，POST /submit 把内容丢上驿道；到家打开电脑，GET /pull 取走全部存货。mcp/server.py 里的配置只有两个环境变量——KNASYNC_BASE_URL 指向 api.want.biz，KNASYNC_KEY 是那把钥匙。两端隔着一整座城市，通信却像共用一张桌子。

实录第二幕：知乎派单。/zhihu 命令把链接提交给 /submit，knasync 认出知乎 URL 模式，自动分进 zhihu 队列，Chrome 扩展领走；地铁里手抖重发三遍，两级去重只放一单进来——5.4 的保险单在真实生活里的样子。驿道上跑的每一单都有凭据，凭据在 consumed 表里躺三十天，任何时候追问“这条处理过没有”，都有案可查。

实录第三幕，也是 knasync 最像传令兵的一幕：微信链路。wechat 队列守在云端，本机的 wechat-queue-poller 守护进程按默认六十秒的节奏轮询（出处：~/.pi/agent/skills/wechat-queue-poller/scripts/poller.py）:

```python
POLL_INTERVAL = int(os.environ.get("POLL_INTERVAL", "60"))  # 秒
url = f"{KNASYNC_BASE}/pull?queue={QUEUE_NAME}"
```

拉到消息，解读分析，再把成品经 api.yuangs.cc 的 weixinpush 通道推进我的微信。《一个人的军团》第 10 章里，哨兵 wxwatcher 盯着文件系统的动静对人报信；这一章的 knasync 是驿道上的传令兵，在机器与机器之间递军报——哨兵对人，传令兵对机器，感知不留死角，情报不误时限。军团书里算过一笔账：军团八成的异步故事，都发生在这条驿道上。

实录第四幕：队列长成了发布总线。sourcepack 加上推送参数，把代码快照推到 knasync，另一台机器上的模型前来取件分析；/publish 统一端点把一篇文章同时发往 IMA 知识库、博客和 NAS 播客生产线。队列起初只是中转剪贴板，长着长着就成了所有“从这台到那台”的必经之路——基建就是这样长出来的：不是规划出来的，是被每一次顺手的使用投票出来的。

这套东西按教科书的说法要回答 CAP，一个人的版本这样答：一致性，用幂等键答，重复投递无痛苦——5.4 的遗产；可用性，用 D1 的单写者简单性答，不要分布式共识，要一个说话算数的账房——第 3 章的遗产；分区容忍，用队列的本职答，任何一台机器离线，消息原地等待，上线再取——本章的遗产。至于削峰，Queues 的官方资料里有个漂亮案例：突发流量先压进队列，消费者批量写库，P99 延迟从数百毫秒降到几十毫秒（来源：materials/raw_queues_ai.md）。我的版本同构：微信推送接口有速率脾气，灵感偏在半夜扎堆，队列就是中间那个蓄水池——洪峰进池子，出水口按自己的节奏放水。

账单收尾。knasync 每月的现金成本约等于零：Workers 免费版每天十万次请求，D1 免费层每天五百万行读，我的全部消息流量在这两个数字面前像雨点掉进湖里（成本锚点来源：BRIEF.md 关键事实清单）。对照官方 Queues 免费层每天一万次操作——自建反而更阔绰，因为 D1 把行读的免费额度给到了 Queues 五百倍的位置。这是精算师视角的又一次胜利：不是省，是结构性便宜。当然要补一句清醒话：便宜的前提是量小；哪天驿道上的消息以十万计，该买的票还得买，精算师不赌运气。

回到总纲，收束这一章。队列的本质是把时间从通信里解绑：at-least-once 承认世界会重复，重试承认世界会失败，死信承认有些事救不回来，幂等保证这些承认不会演变成灾难。四件事合在一起，就是那台时间机器的全部机械结构——它让一个人的系统在人睡觉时依然信守承诺。军团书里我说过，这套基建存在的意义是让我在不确定的世界里睡得着觉；写完这一章，这句话可以再精确一点：睡得着觉的底气，一半来自哨兵会在出事时叫醒你，另一半来自传令兵在你睡着时，照样把每一封信送到。晚安，时间机器；明早的收件箱，拜托了。

协同的另一半是失败路径，这里也有一份实录。某晚消费者主机重启，队列里积了一小摞消息，没有任何告警——因为没有失败发生，消息只是在驿站过了一夜。第二天主机醒来，一轮拉取全部取走，顺序、内容、去重凭据一样不缺。这件事放在同步架构里是一次事故，放在队列架构里是一段注释。互为犄角的本质在这时显形：扩展不在线，任务在队列里等；主机不在线，消息在 D1 里等；甚至边缘节点抖一下，客户端重发一遍就是全部的恢复手段——每一处缺席都有下一处兜底，链路不断，只是慢一点。

至于驿道的未来，清单很短：多消费者竞争领件、按优先级插队、失败重试的退避计时器——每一项在工业队列里都是标配，在这里都还没被需要。个人基建的克制正在于此：不为想象中的规模预付今天的复杂度，等需求真的敲门，再决定开不开门。队列如此，全书反复出现的所有基建判断，皆如此。

---

## 第6章 AI Gateway:边缘的安检门

# 第 6 章 AI Gateway:边缘的安检门

## 6.1 请求到达模型之前，能发生多少事

机场的安检门是整座航站楼唯一的入口。无论你飞往东京还是法兰克福，无论你坐头等舱还是经济舱，行李都要过同一台 X 光机，人都要走过同一道门。门后，世界才分岔出各自的航线。把这张图平移到 AI 的世界:模型是境外的目的地，请求是旅客，而在请求出发与模型接单之间，也应该有一道门。这道门在最近两年有了正式的产品名，叫 AI Gateway;在我的概念词典里，它有个更冷的别名——安检门，请求到达模型之前的最后一道关卡。

按本书的习惯，先做工程考古，问它从哪来。这个角色在 Web 时代叫 API 网关:nginx 做反向代理，Kong 与 Envoy 做路由和鉴权，大厂在微服务架构里把它们用到极致。到了大模型时代，每家有点规模的公司都发现老网关不够用了——AI 流量有自己的脾气:一次调用可能要几万 token，一次故障要靠切换供应商来救，一笔账单要按用户、按团队、按项目拆开算。于是各家内部都长出了专门的“大模型网关”，缓存、限流、预算、审计一个不少。这些东西过去是大厂的专利，个人开发者连见到的机会都没有。Cloudflare AI Gateway 做的事情，本质上是把这个大厂标配搬到了边缘网络上，再把它免费化。

先说清楚它的解剖结构。官方文档对它的定义是:一个 AI 运维（AI Ops）控制平面，本质是位于你的服务与推理供应商之间的正向代理（forward proxy），上游接 OpenAI、Anthropic、Workers AI 等 20 多家供应商（见 materials/raw_queues_ai.md）。注意“正向代理”这个词——它不是替服务器挡流量的反向代理，而是替你的请求出门办事的中间人:你把请求交给它，它替你转发、记账、拦截、缓存，再把响应带回来。一次请求的旅程只有四步:应用向你的网关地址发一个 POST 请求;网关执行规则，缓存命中就直接返回，没命中就转发给供应商;返回途中记录日志与指标;最后把响应交还给你。因为它提供 OpenAI 兼容端点，切换模型或供应商通常只需要改一行代码——改的是那个 URL，不是你的业务逻辑。

这里藏着本章真正的论点:横切关注点的下沉。缓存、限流、重试、成本控制、安全、可观测性，这六件事在软件工程里被称为横切关注点——它们不属于任何业务函数，却渗透在每一次调用里。传统写法是每个应用自己扛:自己在代码里写重试循环，自己拼缓存键，自己算 token 成本，自己往日志里塞密钥脱敏的正则。我见过太多项目，业务代码五百行，这些“配套”代码三千行。大厂的答案从来不是把配套写得更漂亮，而是整体下沉到网关层，让应用代码只剩业务。AI Gateway 把这个下沉第一次带给了个人:你不需要写任何代码，把 base_url 从供应商官方地址换成你的网关地址，六件套全部到位。

下沉之后得到的东西，官方称之为控制平面，这个词值得停留一会儿。数据平面管的是“把请求送出去”，控制平面管的是“看清并指挥所有请求”:实时指标里躺着请求数、token 消耗、成本、错误率、缓存命中率，可以用 GraphQL API 查询，也可以用 Logpush 推送到外部仓库长期保存。这意味着你第一次有能力回答“昨天我的 AI 开销花在哪个模型上、哪个用户的成本最高、缓存帮我省了多少钱”这类问题，而不必在各个供应商的账单页之间来回对表。更妙的是 BYOK（bring your own key）设计:各家供应商的密钥加密存储在 Cloudflare,集中管理，轮换密钥不需要改一行代码。个人开发者的密钥通常散落在环境变量、配置文件和半打脚本里，谁也说不清某把 key 到底用在了几处;把钥匙串交给门房统一保管，这本身就是一种治理。还有 Custom Costs,允许用协商价覆盖默认定价，让网关算出来的成本跟真实账单对得上。可观测、密钥管理、成本核算——控制平面的三件套，恰恰是个人开发者过去“知道自己没有，却不知道从哪儿补”的能力。

我自己没有直接把所有流量接到 Cloudflare 的网关上，而是先用一个更土的办法体会了“网关”这个角色的分量。打开我机器上的~/.dsh/settings.yaml，providers 节里躺着八个渠道条目:opencode-go、xiaomi、meta、google、weclaw、ccswitch、tencent、nvidia。每个条目里最关键的字段有两个:baseURL 决定流量走哪扇门，api 字段决定说哪种方言。仅在我的配置里，api 字段就有三种取值——openai-completions、openai-responses、anthropic-messages，对应三套不同的请求报文格式;而 baseURL 一半是公网地址（opencode.ai、generativelanguage.googleapis.com，以及我自己挂在自己域名下的 wx.want.biz），一半干脆是 127.0.0.1 上的本地端口。换句话说，这个 YAML 文件本身就是我的一座手工安检门:我自己当安检员，手工决定每位旅客走哪条通道、说哪种话。它的问题也显而易见——安检规则只活在我脑子里，渠道失效要靠人肉切换，预算没有闸门，日志没有落盘。这些缺口，恰好是 AI Gateway 逐条补上的东西，本章后面几节会逐一对照。

最后谈钱，因为这本书承诺过每一章都要有一张账单。根据我在写作本章时的检索结果（materials/ch06_补充.md，数据以官方现行价目表为准），AI Gateway 的核心功能——仪表盘分析、缓存、速率限制、DLP 扫描——对所有套餐免费;网关代理本身不按请求收取任何费用，你对上游供应商仍按原价付费，Cloudflare 不加价。免费版的边界在别处:每个账户最多建 10 个网关（付费版 20 个），全账户所有网关合计 10 万条日志存储（付费版是每个网关 1000 万条），单条日志超过 10 MB 不存储，日志写入速率上限每网关每秒 500 条。对一个月调用量以千计的个人开发者来说，这些数字的意思就是:随便用。安检门不收过路费，它真正卖给你的，是治理权——一个人也配拥有治理权，这在十年前是不可想象的一句话。

## 6.2 缓存的物理学:SHA-256 键与 90% 延迟削减

先把结论放在开头:AI Gateway 的缓存能把命中请求的延迟最高削减 90%，并且命中时 token 消耗为零——一次几十毫秒的边缘响应，替代一次数秒的模型推理（见 materials/raw_queues_ai.md）。对一个每天要跟模型说几千句话的开发者来说，这是本章所有特性里最像“物理学”的一个:它不靠玄学，靠一条严格的守恒律。

这条守恒律叫缓存键。AI Gateway 判断“两个请求是否相同”的方式，是把五样东西拼接起来做一次 SHA-256 哈希:供应商、接口路径、模型名、你的认证头、完整的请求体。五要素缺一不可，哈希结果作为缓存条目的主键。这意味着它做的是精确匹配，而且是诚实到近乎苛刻的精确——请求体里任何一个字节的差异，一个标点、一个空格、一次参数微调，都会生成一把全新的钥匙，对应一个全新的缓存条目。它不猜测两句话“意思差不多”，不做语义相似度，不玩向量检索。这既是它的缺点，也是它最值得信赖的品质:命中率可以精确预测，绝不会把你以为该命中的请求张冠李戴到另一条响应上。

调试它只需要盯一个响应头:cf-aig-cache-status。第一次请求，值是 MISS，请求穿过网关到达模型，响应被网关复制一份存进边缘缓存;同样的请求再发一次，值变成 HIT，响应直接从离你最近的边缘节点吐回来。我在验证这类机制时的习惯动作是连发两次 curl，然后 grep 这个头——就像用温度计确认暖气片真的热了。还有三个请求头提供单请求粒度的控制:cf-aig-skip-cache 强制跳过缓存回源拿新响应;cf-aig-cache-ttl 自定义这一条缓存的存活时间，最小 60 秒，最大 1 个月;cf-aig-cache-key 则是逃生门，允许你手工指定缓存键，把“响应等价”的请求归成一组共享缓存——比如十个用户问同一个知识库的同一个问题，请求体里的系统提示词完全一致，只有元数据不同，你就可以把键固定在问题本身上。

值得强调的是，缓存默认是关闭的，需要在控制台或 API 里显式打开并设置默认 TTL。这个默认值选得很诚实:缓存在 AI 场景里不是免费的午餐，存错了东西比不存更糟。哪些请求不该缓存?第一类是采样温度大于零的创意生成——你让模型写十版文案，结果十版一模一样，缓存的确定性摧毁了随机的价值;第二类是时效性数据，问股价、问天气、问“今天几号”，60 秒的 TTL 之外都是谎言;第三类是携带会话上下文的多轮对话，每一轮请求体都在变，SHA-256 的守恒律会诚实地告诉你全 MISS;第四类是 RAG 检索增强场景，召回的上下文稍一变动，整串缓存全部作废。反过来，什么时候该开?文档问答、代码模板、系统提示词固定的批量处理任务——这些场景里同一个问题会被问一千遍，每一遍都值得省下几秒钟和几万 token。

TTL 的跨度值得单独咀嚼:从 60 秒到 1 个月，横跨四个数量级。60 秒是给“热搜类”问题的体面——足够扛住短时间内的重复查询，又不至于把过时答案端给用户;1 个月是给稳定知识的——一份 API 文档的解释，一个月内大概率不会变。配合每请求 25 MB 的可缓存上限（来自写作本章时的检索，ch06_补充.md），这个缓存系统的设计哲学是:把“知识的新鲜度”交给调用者声明，而不是平台武断规定。

五要素里最容易被忽视的是第四项——认证头。把供应商的 Authorization 令牌混进缓存键，意味着换一把 key 就换了一整片缓存空间:同一句话，用 A 账号的 key 问过，再用 B 账号的 key 问，依然 MISS。这个设计初看浪费，实则是租户隔离的守门线——它从机制上杜绝了“用别人的缓存白嫖响应”的套利空间，也顺带保证计费与缓存永远对得上账。说到账，缓存还是成本核算里的乘数:命中一次省下的不只是几百毫秒，是整整一次上游计费。原型开发阶段反复调提示词，同一个请求一天发几十遍，如果没开缓存，烧掉的全是真金白银的 token;开了缓存，只有第一次回源计费，后面几十遍全是边缘上的几十毫秒。对持精算师视角的个人开发者来说，这几乎等于把开发迭代的价格砍到了地板——而付出的代价，只是出门前勾选一个复选框。

我在自己的链路里给这套机制找了个对位物。wx.want.biz 上挂着我自建的轻量网关，其中一类请求是高频重复的意图识别——同样的指令模板，一天被命中几百次。这类流量如果走 SHA-256 键的边缘缓存，命中率几乎是百分之百，因为请求体逐字节相同。而另一类流量，比如多轮 agent 对话，缓存对它毫无意义。这就是缓存的物理学本质:它不能创造信息，只能节省重复计算;请求的熵越高，缓存越无用武之地。工程师要做的不是“打开缓存”这个开关，而是分清楚自己流量里的哪些部分是低熵的重复劳动。90% 这个数字动人，但它只属于那些值得被缓存的请求——其余的，还是要老老实实过门，去境外见模型。

## 6.3 路由与降级:mimo 渠道的 400 之夜

这一节讲一个我亲身经历的夜晚。那晚智谱渠道失效——不是我代码的问题，是上游渠道本身挂了，请求发出去石沉大海。按我的预案，我把默认模型切到 opencode go 套餐下的 mimo-v2.5，小米的模型，想着大厂套餐里的模型总该稳。结果第一发请求就吃了一个上游 400。

400 这个状态码值得多说两句。它和超时、连接拒绝完全不同:请求成功地穿过了网络、通过了鉴权、到达了上游服务器，然后被对方明明白白地拒绝——不是“我联系不上模型”，而是“我不想接你这张单子”。排查的方向因此完全不同:不是查网络，而是查方言。我把同一个渠道下的其他模型试了一遍，meta 条目下的模型走 openai-responses 接口一切正常，唯独 mimo-v2.5 报 400。问题定位了:mimo-v2.5 在 opencode go 套餐里不支持 OpenAI 的 Responses 接口，也就是那条以 /responses 结尾的 API 路径;它只认更老也更普及的 chat/completions 接口。我把请求改走 chat/completions，响应正常返回。为了让这个教训不再依赖我的记忆，我在~/.dsh/settings.yaml 里为它单独立了一个 provider，名字就叫 xiaomi:baseURL 与 opencode-go 完全相同，都是 opencode.ai/zen/go/v1，唯一的区别是它只挂 mimo-v2.5 一个模型，配置里明确声明走 openai-completions 方言。同一个 baseURL 在配置文件里出现两次，看起来重复，实则是一次事故的纪念碑——渠道与接口方言是两个独立维度，组合起来才是完整的路由决策。

顺带把两种方言的差异说透，因为这是排查 400 时的关键知识。chat/completions 是 OpenAI 最早普及的接口，请求与响应都是直白的 JSON;responses 接口是后来推出的新形态，把多轮状态、工具调用、流式事件组织成另一套结构，连流式传输的事件类型都不一样。对模型方来说，支持新方言意味着适配新的请求解析与事件协议，不少模型便只实现了老接口。于是 AI 链路的故障天然分成两类:渠道失效是“门牌被人摘了”，换一家供应商就能解决;方言不匹配是“你说的话对方听不懂”，换模型或换接口路径才能解决。那晚我一开始把两者混在一起排查，先怀疑渠道又怀疑额度，浪费了不少时间，最后才意识到请求根本没进模型的理解范围，而是死在了协议翻译那一层。排查 AI 链路故障时，先问“请求到达了哪一层被拒”，再问“这一层期待什么方言”，比盲目重试有效得多。后来我把这套判断写进了渠道预案的第一行:先分层，再动手。

现在把这一晚的手工操作翻译成 AI Gateway 的产品语言。它的动态路由功能允许你用可视化界面或 JSON 配置编排请求的流转，发布新版本即时生效、随时回滚，不用改一行应用代码。路由由节点组成:Start 入口;Conditional 节点基于请求体、Header 或元数据做条件分支，比如按请求里声明的接口类型或模型名分流;Model 节点调用某个具体的供应商与模型;Rate Limit 与 Budget Limit 节点按密钥、按周期限流限预算，超限自动切换备用路径;End 收尾返回（见 materials/raw_queues_ai.md）。另有 Percentage 节点按概率分流，专门用于灰度发布与 A/B 测试;而 Fallback 机制是整个体系的保命符——主供应商不可用或超限时，自动切到备用供应商。

对照着看，我那一晚干的事，恰好是一条路由的全部动作:智谱渠道失效，是一次需要 Fallback 的故障;发现 mimo-v2.5 不认 /responses，是一次需要 Conditional 分支的方言判断;改走 chat/completions，是选定了 Model 节点;为它建立独立的 xiaomi 条目，是把这条路由固化成配置版本。区别只在于，我用人工神经网络——也就是我这颗脑袋——在深夜里实时演算了一遍，而 AI Gateway 把同样的演算固化成了机器执行的路由表，还附赠即时回滚。我的手工路由有单点故障:故障点就是我的睡眠。凌晨三点渠道再挂，没人切流量;而网关的路由节点不睡觉。

这一夜给我留下了比故障排除更深的教训:接口方言才是网关要抽象的真正对象。初学者以为网关管的是“哪家供应商”，其实供应商只是表象，底层是请求报文的格式之争——OpenAI 的 chat/completions、更新式的 responses、Anthropic 的 messages，三套方言字段名不同、流式协议不同、工具调用格式不同。渠道可以随时失效、模型可以随时下架，但方言的变化慢得多。一个好的网关层，把“模型名”从应用代码里彻底驱逐出去，应用只说一种方言，门后谁接单、说什么方言，全部由路由节点翻译。大厂把“渠道降级”这件脏活产品化了，而每个用过多家模型 API 的个人开发者，其实都在自己身上重演过这条产品线的诞生史——只是多数人没意识到，自己在深夜里手工扮演的，正是一台 AI Gateway。

## 6.4 Spend Limits 与 TokenPolicy:大厂网关与个人预算门的同构

预算控制这件事，大厂和个人开发者的诉求惊人地一致，只是计量单位不同。先看大厂的做法:AI Gateway 的 Spend Limits 功能，可以按模型、按供应商、按自定义维度（比如用户、团队、项目）跟踪美元支出，超预算自动阻断请求（见 materials/raw_queues_ai.md）。它的核心动作是“事前”——每一笔请求在发出之前先算账，算完发现这单会撞破预算线，直接拦截，而不是等月底账单出来再追悔莫及。与之配套的是 Rate Limiting，用滑动窗口或固定窗口限制请求频率，防止滥用与配额被一夜打穿。两者构成预算门的两根门柱:一个管钱，一个管次数。

再看我自己写的一个小东西。npm_yuangs 是我的一个命令行工具库，里面有个策略模块叫 DefaultTokenPolicy，文件只有一百三十五行，却是整个工具里我最得意的部件之一。它管的是另一类预算:上下文预算。每次请求发出前，它先估算本次要发送的内容折合多少 token，再除以模型上下文窗口的容量，得出一个占用率，然后按四级分区决策（源码在 npm_yuangs/dist/policy/token/DefaultTokenPolicy.js）:占用率不超过 70%，安全区，直接放行;70% 到 80%，预警区，放行但记录警告;80% 到 100%，警告区，必须经用户确认才能继续，同时给出三个出口——继续执行、切换到长文本模型、对管道输入做头尾采样;超过 100%，阻断区，强制拦截，必须缩减内容或换模型，没有商量余地。

把这两套系统摆在一起，同构关系一眼可见。它们都在门的位置执行——不是事后审计，而是请求生命周期里最靠前的那道关卡;它们都是“估算、比较、决策”三段式;它们都区分“可放行的警告”与“不可商量的阻断”;它们都为阻断准备了逃生门。Spend Limits 超限时，动态路由可以自动切到 fallback 供应商或更便宜的模型;TokenPolicy 阻断时，返回的动作列表里有切换模型、自动采样、终止任务三选一。你甚至可以说，我的四级分区就是 Spend Limits 的百分位刻度版——大厂按美元记账，我按容量记账;大厂怕的是钱包失血，我怕的是上下文爆炸导致请求被上游直接拒绝，白白烧掉一轮已经计费的输入。

这套四级策略在实际运行里还有一个值得说的细节:它对“管道输入”这类不可控流量做了特殊设计。命令行工具经常要把一个几千行的日志文件整个塞给模型，占用率瞬间爆表;硬阻断会让人恼火，于是警告区给出的动作里专门有一项“自动采样”——只保留文件的头尾，中间大段机械重复的内容直接丢弃，模型要的往往是格式与边界，不是每一行。这个动作把“阻断”从一堵墙变成了一次协商:系统不替你做主，但递给你几个选项——确认继续、切换长文本模型、压缩输入、终止任务，选哪个是你的自由。事后想想，AI Gateway 的超限切换其实是同一思想的平台版:预算打穿时，门不是冷冰冰地拒绝，而是按你配置好的路由把你引向 fallback 模型。预算门与路由表在产品设计里本来就是一对孪生件，一个决定“还能不能过”，另一个决定“从哪条道过”。

差异同样有教育意义。Spend Limits 的计量对象是钱，因为大厂场景里 token 是真实美元，一个员工跑飞了一个循环任务，烧掉的是公司账上的真金白银;TokenPolicy 的计量对象是 token 占用率，因为个人开发者的硬约束往往不是钱，而是模型窗口——超了就是 400 错误，一分钱不少花，事还没办成。另一个差异在维度:Spend Limits 支持按用户、团队、项目拆分预算，这是多租户的思维;个人工具只需要一把全局的闸。但别小看这把小闸，它是“成本即哲学”这条总纲在代码里的落点——免费额度时代，个人开发者烧不起的是沉默成本:那些没有闸门、悄悄重试、越烧越多的失控循环。

回到 AI Gateway 的账单视角，把免费额度当账本算一算。核心功能免费、代理不限量、推理原价透传，意味着装这道预算门本身零成本;真正要精算的是日志——免费版全账户合计 10 万条日志存储，写满之后要么自动删除旧日志，要么停止保存新日志（检索结果见 materials/ch06_补充.md）。10 万条是什么概念?如果你的应用每天产生三百条日志，这个池子够你用一年;如果是灰度期间每天一万条，十天就满。顺带记一笔导出的价格:Logpush 属于 Workers 付费版能力，配额为每月 1000 万次请求，超出部分按每百万次加收 0.05 美元计费（检索结果见 materials/ch06_补充.md）。这是全书反复出现的定价形状:单价低到几乎可以忽略，却逼着你先想清楚自己的流量形状，再决定要不要为便利付钱。所以合理的姿势是把日志当环形缓冲区用，重要的结论实时导走，网关里只留滚动窗口。这一算让我确认了一件事:预算门的价值不在于帮你省钱——缓存替你省的那个部分更大——而在于把“花钱”从一个被动接受的账单，变成一个主动声明的契约。你在门口写下一个数字，门替你守住它。个人开发者第一次可以用声明式的语言管理自己的 AI 开销，就像十年前用声明式语言管理服务器一样。

## 6.5 安检门的边界:DLP、Guardrails 与“先脱敏再发模型”

安检门最重的职责不是省钱，是拦危险品。AI Gateway 在这个方向上有两件武器。一件是 Guardrails:用 Workers AI 的 llama-guard-3-8b 模型对提示词与响应做实时内容审核，检测并拦截有害内容，按 Workers AI 的 token 计费。另一件是 DLP（数据防泄漏）:扫描提示词与响应中的个人身份信息、财务数据、医疗数据，对标 GDPR、HIPAA、PCI DSS 这类合规框架;免费版只内置两个预设配置——金融信息与社保或身份证号，完整的 DLP 能力需要 Zero Trust 订阅（检索结果见 materials/ch06_补充.md）。

巧的是，我自己已经先造过一道土安检门。poeapi_go 是我自建的 LLM 网关，Go 语言写的，五十四个源文件;其中 internal/sanitizer/ 目录下的 sanitizer.go 不到三百一十行，装着整个系统的安全内核。它的设计遵循三原则:先阻断、再脱敏、再落盘。调用链在 handlers 包里清晰可见:chat_handler.go 第 65 行，请求进来先过 DetectCritical，命中高危内容直接拒绝，连脱敏的机会都不给;通过阻断检查后，第 78 行调用 SanitizeMessages 遍历请求里的全部消息，逐条脱敏;最后 logger.go 第 137 行，任何内容要写进日志落盘之前，再过一遍 SanitizeString——流式响应的每个分片也同样处理，模型吐回来的内容不比用户发进去的干净。

先说脱敏层。我逐一数过，规则表里有二十二条脱敏规则，按目标分七组:四条盯 API 密钥（openai-api-key 识别 sk- 前缀、anthropic-api-key 识别 sk-ant- 前缀、generic-api-key 与 secret-key 盯各种键值对写法）;三条盯平台专属凭证（github-token 识别 ghp_ 前缀、aws-access-key 识别 AKIA 开头、google-api-key 识别 AIza 开头）;六条盯通用凭证（jwt-token、bearer-token、url-credentials、password-field、token-field，以及一条盯三十二位以上十六进制串的 hex-token-32plus）;两条盯 PEM 私钥;三条盯数据库连接串（postgres、mysql、redis 的 DSN）;一条盯环境变量导出语句;最后三条是兜底的通用敏感键名匹配——generic-sensitive-id 这条规则值得单独致敬，它的键名列表里同时写着英文的 AccountID、Phone、Email 和中文的“手机号、身份证、序列号”，用 Unicode 属性匹配让正则认识汉字，专治中文语境里自然语言形态的敏感信息;json-sensitive-field 与 url-param-sensitive-id 则分别覆盖 JSON 字段和 URL 查询参数两种载体。

再说阻断层。有五类内容不走脱敏流程，直接拒绝:PEM 私钥、OpenSSH 私钥、数据库连接串、AWS 密钥对、Slack 的机器人令牌。为什么这五类宁可误杀?因为脱敏的本质是正则替换，而正则永远可能被绕过——私钥换个换行、令牌嵌个转义，规则就可能漏网。对这几类东西，漏检的代价是灾难级的，误杀的代价只是让用户重写一句话。期望损失算下来，答案只有一个:一律枪毙。源码里的 ModeLogOnly 与 ModeStrictBlock 两个模式常量并排写在一起——宽与严不是二选一，而是分层。还有 AddRule 接口，运行时可以追加自定义规则——规则表是活的，随着被骗过的次数增长。

再说说“落盘”为什么值得单独设一道工序，因为日志恰恰是泄露的高发区。请求内容是一次性的，日志是长期的:一次对话发完就散了，而日志文件会在磁盘上安静地躺几个月，等着下一次备份同步、故障排查，或者等着被随手分享出去。我的 logger 在写入前对问题与回答各过一遍脱敏，等于给所有“长命”的数据加了统一的滤网——事后想来，这个设计与本书第八章要谈的数据分级暗合:越长寿的数据，准入越严。密钥治理上也有一个呼应:AI Gateway 的 BYOK 把供应商密钥加密存放在 Cloudflare,集中管理、免代码轮换;而我在 settings.yaml 里用 apiKeyEnv 字段把所有密钥指向环境变量而非明文。同一个思路的两种实现——密钥不进代码，不进日志，不进聊天记录。安检门的职责清单由此完整:拦住高危内容，洗掉敏感片段，管好钥匙串，记清流水账。

把我的土门与 Cloudflare 的 DLP 并排放，能看到两个坐标系。AI Gateway 的 DLP 在边缘执行:敏感数据离开了你的机器，但没离开 Cloudflare 的网络，由平台级规则库替你把关，好处是覆盖面广、免维护，代价是数据必须先出门。我的 sanitizer 在自己的机器上执行:数据根本不出门，代价是规则库只有我一个人维护，二十二条规则覆盖不了语义级的泄露——用户写一句“我的密码是生日加手机号后四位”，任何正则都无能为力，只有理解语义的模型才可能识别，而 Guardrails 那类“用模型审模型”的方案，每一次审核本身都要烧 token。安检门的边界就在这里:形态可拦，语义难防;规则能挡已知，挡不住未知。这不是哪一家的缺陷，是这道门的物理极限。

但物理极限不等于无所作为。三原则的排序本身就是答案:阻断优先于脱敏，脱敏优先于落盘——危险等级越高的内容，处置环节越靠前，处置手段越极端。这个排序让“用户密码进了第三方训练日志”这类事故的概率被砍掉一个数量级，剩下的一成，靠敬畏而不是靠技术。回扣本章开头:AI Gateway 把缓存、限流、预算、脱敏这些横切关注点从应用代码下沉到代理层，让一个人用一个 URL 的代价，拥有了大厂级的 AI 流量治理。安检门不替你写业务，也不替你担保模型的人品;它只是站在请求出发的地方，替你检查每一件行李。从租服务器到买全球网络，这是同一笔交易在 AI 时代的续篇——门是租不来的，门是自己的。

---

## 第7章 域名即主权:want.biz 运维实录

# 第 7 章 域名即主权：want.biz 运维实录

## 7.1 一次续费差点失去一切

数字世界里有很多种“拥有”，大部分经不起细看。服务器是租的，到期就收回；数据库是租的，容量告警比生日提醒来得准时；代码仓库自以为属于你，一声政策变动就能上锁。把全部资产摊在桌上一件一件摸过去，最像不动产的只有一件——域名。want.biz，五个字母加一个后缀，是我在这个星球上的门牌。但它的真相有点尴尬：它也不是买断的，它是我和注册局体系之间的一份年租合同，而且对方最多只肯一次签十年。所谓“我拥有 want.biz”，法律上的准确表述是“我以年为周期租用 want.biz，并且按时交租”。这是我在数字世界里距离“有产”最近的一步，也是唯一一步。

正因为只有这一步，丢掉它的代价才没有上限。域名过期不是“第二天网站打不开”这么朴素，它是一套写进注册局规则里的、结构精密的没收程序。以最常见的情形为例，一个域名从断供到彻底消失，大约要走六十多天：头三十天是宽限期，原价还能续，但解析随时可能先死——网站和邮箱已经在中断了；第三十一天起进入赎回期，域名被注册局锁定，想拿回来要交一笔让肉疼的高额赎回费；第六十一天进入删除期，钱也不好使了，只能干等；到第六十六天前后，它被丢回公共注册池，任何人都可以注册——而此刻，盯着这个时间窗口的，是一群以毫秒为作业单位的抢注机器人。抢注服务的商业模式就是提前下注：在删除瞬间并发提交注册请求，谁先被注册局受理谁拿走，多人命中就进拍卖。高价值域名几乎必然被专业机器人提前锁定。行业数据里有个数字我一直记着：2023 年中小企业百强里，有 22 家遭遇过主域名被恶意抢注（素材：materials/ch07_补充_ssl.md）。主域名一旦被抢走，常见的结局是被指向钓鱼站、仿冒官网，或者干脆挂出一个报价页，居高临下地跟你谈回购。

更狠的是连锁反应。在我的架构里，域名不是一个孤立资产，它是整条自动化链条的地基。我把这条链画出来：

```
域名到期未续费
   → 权威 DNS 停止应答 → 全部子域解析失败
   → 域控验证无法放置记录 → 证书自动续期失败
   → 九十天后证书过期 → 所有 HTTPS 客户端报错
   → 即使后来赎回域名，证书也要从头再签一遍
```

也就是说，本章后面要讲的所有“自动化红利”——证书自动续期、DNS 即改即生效、零配置 HTTPS——全部建筑在“域名本身活着”这个前提上。证书自动化只管证书层，管不了注册层。地基一断，楼上自动化得越漂亮，摔得越整齐。

那次被我称作“差点”，倒不是真丢过域名，而是有一天我翻注册商面板，发现某个不常用域名的自动续费状态是灰的，而到期日就贴在眼前。我把这件事在脑子里推演了一遍——如果失效的是 want.biz 呢？图床链接全部变成死链，微信推送里的封面图集体裂开，队列入口失联，播客订阅源里每一个音频地址同时阵亡，而且订阅源是历史文件，我改了配置也追不回已经分发出去的那些地址。更麻烦的是 upload 背后那条上传链路、poe 背后的模型网关，还有散落在各种脚本里的队列调用——它们不会报错说“域名过期了”，只会表现为一群看似无关的服务同时灵异失效，而我得花上半天才能把线索收拢到同一个根因上。域名失效是少有的、能让所有子域在同一天集体生病的病灶。想到这里我就明白，该做的不是后怕，是把它变成制度。于是我给自己定了几条规矩，合起来就是一个人的续费提醒系统：

第一，自动续费开双通道，绑卡加预存余额，任何一个渠道失血都还有备份。但我不迷信自动续费本身——它同样会死：银行卡过期、余额不足、注册商倒闭，都是真实世界里有案可查的死法。第二，告警走日历的四级节奏，到期前六十天、三十天、七天、一天各响一次，并且告警必须走两条互相独立的通道，其中一条接入我自己的消息队列——时间机器的哲学在这里同样成立：提醒必须在我不在场的时候也能抵达。第三，也是最反直觉的一条：告警邮箱绝不能是挂在同一个域名下的邮箱。域一过期，邮件记录跟着失效，告警会和域名一起死在半路——消防电话不能接在着火的那个电表上。第四，打开域名锁，防止未授权转移或删除。第五，核心域名一次续多年，把风险窗口拉长到机器人够不着的地方。有几年的缓冲，一次银行卡过期的疏忽才不至于演变成赎回期的账单。第六，每年做一次盘点：一张清单写清每个域名在哪家注册商、哪天到期、名字服务器指向哪里、恢复路径是什么。盘点时顺带核对注册商账户里的邮箱与手机号——真到要找回域名的那天，起作用的是它们，不是我对注册商的印象。第七，每年跑一次“域名今天过期了”的桌面推演，把恢复步骤真正走一遍，而不是背一遍。

顺带记下这套系统的心理学：四级告警里，六十天和三十天的那两声我几乎从不响应，真正逼我动手的是七天那一级；但如果没有前两级铺垫，七天那声就会从“提醒”退化成“惊吓”。提醒系统的分级不是做给系统的，是做给人性的——给未来的自己留出可以从容装死、再从容爬起来的余地。

先把丑话说在前面：注册商自带的到期提醒，撑不起这套系统。它默认只往账户邮箱发一封信，而账户邮箱恰恰是最容易被垃圾箱吞掉、最容易被换号或停用的自己废掉的通道；提醒文案是写给所有租户的，它不知道 want.biz 背后挂着图床、队列和播客，不知道这个域名对我不是资产表里的一行，而是全部公共身份。公司丢了域名，有法务、有公关、有多层备份；我丢了域名，丢的就是我自己。所以这套规矩的全部冗余不是过度设计，是按“输了就没有第二次”的标准反推出来的——个人基建的容错预算，从来不是钱，是提前量。

这七条没有一条是什么高深技术，它们的价值全在“提前”两个字。数字世界的不动产和物理世界有一个残酷的区别：房子被占了，你可以报警收回来；域名过了删除期被别人注册走，警察管不着，注册局只认时间戳。所以每年那个三十秒的支付动作，我从不嫌麻烦——那是我为全部数字资产交的地税，也是“我在场”这件事最便宜的一次自我证明。

## 7.2 DNS 记录的界碑学：A、CNAME、TXT 各守什么

地契上最值钱的两个字，不是面积，是“四至”——东到哪、西到哪、南到哪、北到哪，界碑一立，产权才有了形状。DNS 记录就是数字地契上的界碑，每一行都在回答同一个问题：这个名字，归谁管、往哪走。界碑立歪了，地还在，路没了。

先认两种最基本的碑。A 记录是终点碑，上面直接刻着一个 IP 地址，解析器查到它，旅程就结束——去这个地址找服务器，别的不用问；AAAA 是它在 IPv6 世界的孪生兄弟。CNAME 则是别名碑，上面刻的不是地址，而是另一个名字：查 pic.want.biz，它说“去问那个名字”，解析器只好再查一轮，顺着链最终落到某条 A 记录上。两种碑的分工由此分明：A 回答“在哪”，CNAME 回答“跟谁一样”。代价是 CNAME 有洁癖——按协议规范，一个名字一旦立了别名碑，就不能再立任何其他碑，而且根域上根本不允许立 CNAME，否则会和权威记录打架。Cloudflare 用一招“别名扁平化”绕开了这条祖制：解析时它在自家内部顺着链查到最终地址，直接把地址递给查询者，于是根域也能享受别名的灵活，代理也能直接吐出自家的 anycast 地址（素材：materials/ch07_补充_dns.md）。

但 want.biz 的界碑表里，真正的主角不是 A 也不是 CNAME，而是每行记录旁边那朵小云。橙色云朵意味着这条记录被 Cloudflare 代理：DNS 查询返回的不是我服务器的地址，而是 Cloudflare 的 anycast 地址，流量先落到离用户最近的边缘节点，再由它回源。灰色云朵意味着仅 DNS：查询直接返回源站真实地址，流量直连，Cloudflare 全程旁观。两种云的差别，一张表就能说清：

| 对比项 | 橙云（代理） | 灰云（仅 DNS） |
|---|---|---|
| 查询返回 | Cloudflare 的 anycast 地址 | 源站真实地址 |
| 流量路径 | 用户 → 边缘 → 源站 | 用户 → 直连源站 |
| 附赠能力 | 防护、缓存、HTTPS、日志 | 无，只管指路 |
| 缓存时长 | 固定为自动值 | 可自定义 |

规则细节有三条值得刻在碑座上：其一，只有 A、AAAA、CNAME 这三种“解析到地址”的记录可以被代理，邮件记录、TXT、服务发现记录永远是灰云——邮件不走网页协议，验证条目也不走；其二，同一个名字下挂多条地址记录时，只要有一条点了橙云，整个名字都按橙云处理；其三，别名链上只要有一环被代理，整条链都算被代理（素材：materials/ch07_补充_dns.md）。

还有两类小碑值得记一笔。同一个名字下可以立多条地址记录，解析器轮着还地址，这是 DNS 时代最古老的负载均衡——只是在我的架构里，这门手艺被橙云接管了：边缘节点替我站在地址后面，我不再需要用多条记录来分摊流量。另外，橙云记录的缓存时长被固定为自动值，大约三百秒，个人改不了；这合情合理——被代理的记录随时要在边缘重新调度，一条被全球缓存焊死的记录反而是灾难。灰云记录则可以自定缓存时长，迁出域名之前先把缓存时长调短，正是 7.5 那场搬家演练的第一步。


光看表格容易犯困，我在终端里敲了几条命令，给这一节留了活体标本：

```
$ dig +short pic.want.biz @1.1.1.1
104.21.44.228
172.67.204.191
$ dig +short api.want.biz @1.1.1.1
104.21.44.228
172.67.204.191
$ dig +short NS want.biz @1.1.1.1
arya.ns.cloudflare.com.
rommy.ns.cloudflare.com.
```

不只是 pic 和 api——fm、q、poe、upload、blog、wx，八个主机名全部解析到同一对地址。这是橙云世界最反直觉的风景：在 DNS 层面上，我的八个服务共享同一组门牌号，像八户人家共用一块路碑；真正的分诊发生在流量抵达边缘之后，网页协议里的主机名头（以及握手时的服务器名称指示）会告诉边缘节点“我要找的是哪一户”。理解了这一层，就理解了为什么在 Cloudflare 上新增一个子域不需要新增一台机器——界碑的密度和机器的数量，从此解耦。顺带解释这对地址的来历：104 与 172 开头的这两个地址属于 Cloudflare 的 anycast 池，同一组地址在全球几百座城市同时广播，路由协议自动把访客送往网络拓扑上最近的那一个。我的界碑表之所以能写满八行而不用添一台机器，靠的就是 anycast 与代理的组合——地址不指向任何一台具体的机器，指向的是一张网。第 1 章引用过的那组数字，三百三十多座城市、一百二十五个以上的国家、95% 的用户五十毫秒内可达，落到这里只是一行朴素的解析结果：八个子域，同一对地址，背后是一整张网。

界碑表的最后一行是 TXT，一匹永远成不了橙云的马。我在面板里给它写的值是一条发件人策略声明，意思是“本域名发出的邮件，只认 Cloudflare 邮件路由这一家”：

```
$ dig +short TXT want.biz @1.1.1.1
"v=spf1 include:_spf.mx.cloudflare.net ~all"
```

TXT 是 DNS 里的公共留言板：防伪造声明、签名公钥、域名所有权验证，都往这里贴条子。它不解析地址，所以永远灰云——但正因为它直通权威 DNS，它成了本章后面所有自动化的暗道：证书续期的域控验证，靠的就是往这块留言板上贴一串一次性令牌。这块板上的条目远不止一种：防冒名的策略声明之外，还有 DKIM 的签名公钥、DMARC 的处置指令，以及各类第三方服务用来证明“这个域归我”的所有权验证条目——每一项都是一枚小界碑，守着域名在邮件世界和身份体系里的信用。

界碑学还有阴暗的一半，必须讲透：橙云只在“被查询的那一刻”藏住源站，它是一层纱，不是一堵墙。想拿真实地址的人有一整本攻略——翻你还没橙云的子域：ftp、mail、origin 这类调试遗留下来的灰云记录，查一下就是源站地址；看你的邮件记录：邮件永远无法代理，如果邮件服务和网站共用一台机器，邮件记录等于把网站地址登了报；翻邮件头：源站自己发出的告警邮件里，投递路径字段白纸黑字写着发信机器的地址；翻 DNS 历史档案：你接入橙云之前的地址记录，早被被动 DNS 服务永久收录，纱是今天才挂的，裸奔是历史事实；甚至可以扫描全网证书透明度日志，反查“哪个地址出示过写着这个域名的证书”：

```
$ true | openssl s_client -connect 203.0.113.10:443 2>/dev/null | openssl x509 -noout -subject
```

所以完整的主权方案不止一朵橙云：所有网页用途的记录全站橙云，清掉调试遗留的灰云；源站防火墙只放行 Cloudflare 官方公布的地址段，别人拿到地址也敲不开门；开启回源认证，要求每个回源连接出示 Cloudflare 的客户端证书；最彻底的是隧道模式——源站只出不进，公网入站端口全部关闭，源站地址从此物理隐形（素材：materials/ch07_补充_dns.md）。把这些想清楚之后，我把 want.biz 的界碑表誊清如下，本节标题问的“各守什么”，答案全在里面：

```
主机名     云色   守着什么
pic       橙    R2 图床公开出口，播客音频与封面同住于此
upload    橙    Knowly 上传入口
fm        橙    播客与行情
q         橙    队列
poe       橙    LLM 网关
api       橙    knasync 统一入口
blog      橙    博客
wx        橙    微信消息网关
@         灰    TXT：发件人策略声明，守邮件信誉
@         灰    NS：arya 与 rommy，权威指向 Cloudflare
```

## 7.3 证书自动化：让 HTTPS 变成空气

要理解今天的空气有多难得，得先回忆证书曾经是烟花。十几年前，给一个网站配 HTTPS 是一场年度仪式：生成证书请求文件，挑一家证书商，按价目表选验证等级，等一封确认邮件，下载证书压缩包，照着说明书往服务器上贴，贴完在浏览器里反复刷新，来年到期前再把整套流程上演一遍。证书是要按年付费的，忘了续，浏览器就在每个访客眼前挂出一面刺眼的红牌。那个年代 HTTPS 是成本项，是要“上”的东西，像装修，像铺地板。而今天，我指着 pic.want.biz 对任何人说“它有 HTTPS”，用的动词不是“配置”，是“有”——它和空气共用同一个动词。

把空气造出来的机器叫 Universal SSL，它的产品特征表短得可疑：免费，所有套餐默认开启，域名接入即自动签发、自动续期、自动部署，全程无人工干预；验证方式是域名级验证，只验证你对域名的控制权，不验证组织身份；覆盖范围上，完整接入模式下一张多域名证书就罩住主域加一级子域，CNAME 接入则每个被代理的子域各自拿证；签发方是 Let's Encrypt 或 Google Trust Services 这样的公共证书机构，有效期九十天，到期前三十天自动续期（素材：materials/ch07_补充_ssl.md）。注意这里的用词——不是“可以自动”，是默认如此。我一次也没有为 want.biz 的任何子域手动签过证书，这件事本身就是这一节的核心论据。还有一个容易忽略的细节：Universal SSL 的签发机构并非从一而终，早年出自 DigiCert，后来迁移到 Let's Encrypt 与 Google Trust Services。这次无声的换防是我最喜欢的一处注脚——几百万个域名的证书换了一家签发方，没有任何站长收到通知，也没有任何网站闪过一下，因为签发方被藏在了同一套接口背后。对站长而言，证书机构的名字从产品特性退化成了日志里的一个字段，这正是基础设施成熟的样子：供应商可以换，服务不眨眼。

机制上要先拆掉一个误解：经过反向代理的 HTTPS 不止一段加密。访客浏览器到 Cloudflare 边缘节点是第一段，出示的是边缘证书，Universal SSL 管的就是这一段；边缘节点回源到我的源站或 Worker 是第二段，那是另一套证书的事。所谓“免费 HTTPS”，免的是访客看得见的那一段——恰好也是工程上最烦的那一段。

九十天这个数字值得单独站一会儿。证书机构刻意把免费证书的寿命压到这么短，理由有二：其一，证书越短，私钥泄露或错误签发的影响窗口越小，这是一道风险的除法；其二，续期越频繁，人越靠不住，自动化就越成为唯一选项——它是把“纪律”翻译成“机制”的典型手法。与其要求每个管理员记住到期日，不如让到期日短到任何依赖记忆的方案都会被淘汰，最后活下来的只有自动化。背后驱动这一切的协议叫 ACME，自动证书管理环境，流程拆开是六步：客户端初始化，向证书机构索取接口目录；用密钥对注册账户；提交证书订单，拿回每个域名的授权对象；响应验证挑战，要么在约定路径下放一个验证文件，要么往 DNS 加一条 TXT 记录；证书机构从多个网络位置核验通过后签发，并写入证书透明度日志；部署证书并挂上续期监控，到期前三十天自动重来（素材：materials/ch07_补充_ssl.md）。

两种验证挑战的差别，在个人基建的语境里值得一站。放文件的方式要求源站对公网开放一个约定路径，证书机构从多个网络位置来敲门；放 TXT 的方式完全不碰服务器，权威 DNS 替你应答——橙色代理和 DNS 托管恰好住在同一屋檐下时，验证就能全自动。顺带说清覆盖的边界：统一证书只罩主域与一级子域，更深的名字和通配符要么升套餐要么另想办法；好在我的界碑表清一色是一级名字，一个都没越界。


Cloudflare 真正的魔法，是把这六步整个藏进地下。完整接入模式下，权威 DNS 本来就在它自己手里，验证用的 TXT 记录它自己放、自己查、自己删，用户对六步流程一无所知——这就是“零配置”的根本原因，不是魔法绕过了验证，是验证的所有原料恰好都在同一间屋里。当然，魔法也有失效咒语：CAA 记录写错、DNSSEC 配置不当、防火墙拦了验证路径，自动续期都会静默失败，然后在某个凌晨同时过期给你看（素材：materials/ch07_补充_ssl.md）。

我给这一节的教训留了个反向注脚：第一次在浏览器地址栏看到 pic.want.biz 前面的小锁时，我的感觉和看到空气的感觉一样——没有感觉。好的基础设施的最高褒奖不是赞叹，是无感。“不配置”才是最好的配置，但这句话有个常被省略的下半句：那个“不配置”的背后，是有人把配置做成了制度。而制度的全部前提，又回到了 7.1 那张风险链条图——域名活着，传送带才有东西可运。空气不是免费的，它是被一整套我看不见的机器日夜维持着的；我只是恰好，用一年几十块的域名年租，买下了整台机器的产出。

## 7.4 重定向与路由：短链、跳转与页面的门牌

7.2 的解析实验留了一个后门：八个子域共用同一对地址，那么边缘节点靠什么把请求分给八个不同的服务？答案是第二套路由，它发生在网页协议层：每个请求进门都要出示主机名头，握手时还要出示服务器名称指示，边缘节点据此决定把请求递给哪个 Worker、哪个桶、哪条重定向规则。DNS 是城市级的指路牌，协议头才是楼道里的信箱。理解了分层，就理解了路由的三种武器：重定向规则适合“这个地址永远搬去那个地址”的硬搬家；批量重定向适合一整批短链的集中托管；Worker 路由适合需要编程逻辑的分诊——我的 q 和 poe 走的是后一种，门牌后面站的是代码，不是跳转。门牌调度还藏着一套状态码的礼节：永久搬家用 301，浏览器和搜索引擎会记住新地址，旧地址积累的信誉慢慢过户；临时借用用 302 或 307，记忆不被污染，随时可以搬回原址而不留痕迹。这套礼节对个人的意义在于，重定向不是权宜之计，而是可以入账的无形资产迁移——把一个老地址 301 到新地址，等于把老地址多年攒下的收藏夹、外链和搜索权重整批平移。批量重定向再把这件事变成一张清单：地址对的映射表托管在边缘，改一行，全球节点同步生效，全程不需要一台自己的服务器参与。

这一节我最有感情的素材，是 knasync 源码里的一行注释，它记录了一次搬家：

```js
note: '所有客户端统一使用 api.want.biz；历史域名（knasync.want.biz / knasync.yuanguangshan.workers.dev）为同一 Worker 的别名，统一切换至 api.want.biz。'
```

翻译成门牌语言：这个队列服务最早住在 workers.dev 平台送的免费域名里，后来挂上了 knasync.want.biz，再后来统一成 api.want.biz。注意搬家的方式——不是拆旧门牌，而是把旧名字留作同一个 Worker 的别名，旧门铃一直接着线。这是路由设计里我最认同的保守主义：门牌可以加，不可以改；非要改，旧门牌必须永远保留。因为门牌一旦被引用，就成了别人系统的一部分，你删掉的不是一个 DNS 记录，是别人机器上的一个配置项。

门牌被引用之后有多难拔，图床是最极端的样本。pic.want.biz 之所以必须存在，是因为我的图床界面用 Markdown 渲染图片，而渲染器只接受 http、https 开头的绝对地址——相对路径会降级成一行纯文字。于是每一张图的地址都以完整域名的形式，被写进了笔记、发进了微信、嵌进了订阅源。播客分发链路是同构的：

```bash
# 播客产物同步进 R2 桶
rclone copy "$OUTPUT_DIR" "r2:yuangs" --verbose
# 音频地址以 pic.want.biz 的形式直接写进订阅源
MP3_URL="https://pic.want.biz/${ENCODED_FILENAME}"
# 微信图文卡片的封面地址
picurl: 'https://pic.want.biz/podcast.jpeg'
```

rclone 配置里那个叫 r2 的远端，端点指向 R2 的 S3 兼容入口，访问密钥一栏我在书里打了码。这几行配置连起来的意思是：音频文件住进桶里，门牌挂 pic.want.biz，从此每一个订阅了播客的客户端都握着这个门牌的复印件。域名从这一刻起不再是运维对象，而是内容本身的一部分：改门牌等于把所有历史音频连根拔断。这就是 7.2 那张界碑表的真正分量——表里每一行橙云，背后都是一串已经散播出去、无法召回的地址。

至于标题里的“短链”，我承认还停在设想阶段，但形状已经想清楚了：批量重定向天生适合做一个人的短链服务——把一组长地址映射进一个子域，跳转规则托管在边缘，成本落在免费额度之内。真正驱动这个设想的不是技术，是输入成本：q.want.biz 是八个字符，poe.want.biz 是十一个，而我每天要敲它们几十次。门牌的经济学很朴素——高频的门牌，值得更少的字符。这也是为什么我对子域命名的规矩越来越苛刻：能用一个字母的绝不用两个，因为每个字符都要乘以未来十年的敲击次数。设想里最常用的三个入口：q 收发队列消息，p 问模型，u 传文件——高频操作各配一个字符的门牌，把输入成本压到极限。至于更激进的方案，比如干脆换一个单字母域名，那已经是另一场拍卖会里的商品，这笔账我算得清，所以它一直没走出备忘录。门牌学至此凑齐了四件套：DNS 立碑，证书发证，路由分诊，重定向搬家。四件套合起来，就是“我在场”的全部技术含义。但主权的篇章不能以胜利收尾——下一节要泼的那盆冷水，才是这一章真正想留下的东西。

## 7.5 主权的边界：平台即房东的清醒认知

这一章一路写下来像一份产权声明，现在是做诚实资产评估的时候。翻开 want.biz 的地契逐层验伤：权威 DNS 托管在 Cloudflare——实测名字服务器指向 arya 和 rommy，两台都姓别人；边缘证书由 Let's Encrypt 或 Google Trust Services 签发，续期靠 Cloudflare 代放验证记录；图床、播客、队列全部住在 R2 和 Workers 上，连邮件路由都是房东附赠的。也就是说，除了注册局体系里那行以我为受益人的租约，本章讲过的每一层基础设施，都租自同一家房东。主权的真相不是“我拥有”，而是“我可以随时带着全部家当搬走”。我真正拥有的，是可迁移性，不是砖瓦。

所以退出演练比胜利宣言重要。我给自己定的演练规矩是这样的：DNS 层，退出等于把名字服务器记录改指向新的权威服务商，唯一要诀是提前把缓存时长调低，让旧记录尽快失效，把搬家窗口从几天压到几分钟；存储层，R2 讲 S3 方言，rclone 一条命令就能把桶搬去任何一家兼容服务商——第四章讲过的零出站费在这里显出第二重价值：搬家不付行李费；数据层，D1 的底层是 SQLite，导出就是拿走一个文件，时间机器的行李轻得可疑；计算层，Workers 跑的是标准 JavaScript，加上一点适配，任何能跑 JS 的地方都收留它。这四条我每年在纸上合起来推演一遍：假设明天 Cloudflare 消失，我需要多少小时恢复服务？这个问题的答案一年比一年短——不是因为房东变弱，而是因为我的架构越来越不依赖任何一家的私有方言。退出的可能性每增加一分，留在现任房东这里的议价权就多一分；本地优先与边缘优先的分界、多供应商的冗余、S3 兼容的执念，本质上都是同一件事：给未来留一扇没上锁的门。演练清单上还有两行容易漏掉的小字。一是把演练产物落成文字：一份两页纸的迁出手册，写清每一步的命令、验证方法与回退点，并且存放在与房东无关的地方——把迁出手册存在现任房东的托管服务里，是这场戏里最冷的笑话。二是把域名和服务的依赖关系画成一张图：哪些内容里嵌着 want.biz 的地址、哪些外部系统反过来依赖我的队列。演练的目的从来不是搬家本身，是看清自己究竟欠着谁的门牌、又被谁欠着。

主权的边界还有更细的一层，关于隐私。7.2 讲过，DNS 历史档案是永久记录：接入橙云之前的源站地址，已经躺在被动 DNS 服务的数据库里，删不掉，也谈不上被遗忘。数字不动产的房契并不锁在我的抽屉里，它抄送给了全世界的档案馆。我能控制的是现在和将来的界碑，控制不了过去的。这是主权的边界之一，也是“藏好源站”必须在做架构的第一天就想清楚的原因——历史不参与补写。顺带一条自查习惯：隔段时间去被动 DNS 档案站搜一遍自己的域名，看看过去暴露过什么——不是清除，清除不了；是知道对手知道什么，再把防御的假设建立在最坏的那份档案上。

最后是关于房东的清醒。素材里有一组数字：Cloudflare 的网络覆盖三百三十多座城市、一百二十五个以上的国家和地区，网络容量跨过 500 Tbps，全球 95% 的互联网用户距离它的节点不到五十毫秒；它保护着全球五分之一以上的网站（素材：materials/ch01_补充.md）。我反复强调这是个人开发者能拥有的性价比最高的全球基建券，但规模是一枚硬币：一家保护 20% 网站的公司，它的一次故障也会同时属于 20% 的网站；一张覆盖一百多国的网络，它的政策半径也有一百多国。免费额度不是施舍，是获客漏斗与规模经济的商业策略——房东对租客好，是因为好有好的回报。把这句话写在本章结尾不是忘恩负义，恰恰是长期续租的前提：只有清楚房东的动机，租客的感激才不会退化成依赖。

外人叫我广山哥，注册局的数据库叫我一个客户编号。这两个人共用同一套域名、同一桶图片、同一条队列，也共用同一条底线：我可以把整个王国建在你的土地上，但地契的原始文本，和每年那笔续费，必须永远攥在我自己手里。《一个人的军团》写过这些地址如何被一支军团住进去，本章算它的下篇：这些地址如何被一个人守住。全书开篇说 Cloudflare 账号是一张全球地契，第 7 章就是这份地契的验契环节——验到最后发现，契上真正过户的只有一行：域名。它是最难补办的那张证件，也是所有服务的地址开始的地方；它每年提醒我一次，主权的完整表述不是“不依赖任何人”，而是“依赖谁、依赖什么、什么时候能不依赖”，这三问的答案全部由我自己持有。把这道门看牢，边缘王国才不是搭在别人地基上的帐篷，而是一座随时可以连根搬走的、移动的城。

---

## 第8章 本地优先与边缘优先:knowly 与 knasync 的分界

# 第 8 章 本地优先与边缘优先:knowly 与 knasync 的分界

## 8.1 一个剪贴板的两难:便捷还是主权

每一种剪贴板增强工具，都会在安装完成的那个瞬间，逼你回答同一个问题:你复制过的一切，愿意放在谁的硬盘上?

大多数人没有正面回答过这个问题，但都用行动回答了。装了云剪贴板的人，答案是厂商的硬盘;什么都没装的人，答案是哪也不放——一格，新的覆盖旧的，来时无声，去时无痕。这两种答案我都交过卷，后来都撤回了:前者把数字生活里最私密的通道拱手让人，后者浪费了每天上千次本可以沉淀下来的复制。要讲清楚我最终的裁决，得先算一算剪贴板到底有多私密。

把你过去一小时复制过的东西列一遍:某个六位验证码，有效期五分钟;从密码管理器里取出来、正准备粘贴的密码;写给同事又删掉重写的半句话，第一稿的措辞不适合被第二双眼睛看到;一个还没决定要不要发出去的链接。剪贴板是这些内容的第一落点——它是意图的草稿区，是所有“尚未决定”的信息的中转站。密码管理器再安全，密码从复制到粘贴之间的那几秒钟，住在剪贴板里;聊天软件再加密，你打了一半又删掉的话，曾经短暂地存在过剪贴板里。一个能持续读取你剪贴板的服务，等于在你的草稿区架了一台常开的摄像头。

所以当 knowly 决定做剪贴板捕获时，真正的问题从来不是怎么捕获——500 毫秒一次轮询，实测 CPU 占用不足百分之一，这类工程细节军团书第 3 章已经从对抗熵增的角度讲过它的来龙去脉，本章不再重复。真正的问题在捕获之后:这半秒抓到的东西，往哪放?

顺着行业的惯性，答案几乎是现成的:上云。剪贴板数据量小、写入频繁、多端消费，是教科书级的云同步场景，连苹果自己都这么想——“通用剪贴板”把你在 Mac 上复制的内容经 iCloud 中转到 iPhone，一条看不见的线，穿过多地的数据中心。这条路便捷到无可挑剔，直到你意识到那条线的另一端拴着什么:你过去几年的验证码、密码、草稿、私语，按时间顺序躺在一个与你没有雇佣关系、没有居住关系、只有一层服务条款关系的公司的存储里。条款可以修订，服务可以关停，账号可以冻结，而你的剪贴板不会替你转场。

knowly 的裁决写在名字里。名字的九种解读中有一条 Keep NAS As Sanctuary——让 NAS 成为知识圣殿，军团书第 3 章逐条讲过那九种解读，本章只取与“数据放哪”直接相关的这一条:名字先于代码做出了裁决，知识是一个人的圣殿，不是一家公司的数据表。落到工程上，这个裁决只有一个动作:SSH 直存。Mac 这头用 golang.org/x/crypto/ssh 库直接对自家 NAS 说话，远端只需要一个 SSH 服务和一个目录，没有任何 knowly 专属的服务端进程需要部署、升级、打补丁。这个方案有一个容易被低估的美德:它不新增任何依赖。没有第三方中转服务，就没有第三方的隐私条款要读;没有厂商的 API，就没有厂商涨价或停服的风险要扛;没有共享的多租户数据库，就没有“这条数据到底存在哪个区域、哪块磁盘”的问题要问。依赖每少一个，风险面就少一块，需要谈判的对象就少一个——所谓数据主权，工程上的形态就是把需要信任的名单缩到最短。

有人会问:家宽环境里的 NAS，访问速度和稳定性比得过全球边缘节点吗?这又绕回了那个被问烂的伪两难:便捷还是主权。我的答案是，这个两难根本不成立，因为便捷与主权从来不在同一个坐标轴上。便捷的敌人是延迟与故障，主权的敌人是泄露与失控;前者是性能问题，钱和架构能解决，后者是权力问题，钱解决不了。knowly 的取舍是把两者拆开处理:捕获的便捷（半秒轮询，零操作，零感知）留在本地，归档的主权（SSH 直存）也留在本地，而真正需要全球触达的部分——上传入口 upload.want.biz，让手机上的碎片能推进同一条管道——做成公网服务。但请注意方向:那是“我往里推”的入口，不是“别人来取”的仓库。入站走边缘域名，数据本身落进客厅里的盒子;门牌挂在世界的墙上，家当锁在自己的屋里。

把便捷与主权放进同一个四象限，大多数剪贴板工具挤在其中两格：云同步的一格，便捷而失守；纯本地的一格，守住却成孤岛。knowly 想去的是第三格——SSH 直存做底盘，多端入口做天线，主权的地里长出便捷的庄稼。至于第四格，又慢又不安全，没人去，也不该去。四象限画完你会发现，行业里吵了十年的“便捷与主权不可兼得”，吵的其实是同一格里的两种坏选择，真正的解法在格与格的移动路线上。

动笔写这一章之前，我把 knowly 的 docs/项目分析.md 又翻了一遍，想确认当年把数据放进 NAS 的决策有没有留下一句后悔。没有找到后悔，找到了一句更冷静的表述，写在与本章主题直接相关的地方。它说，这种设计让 knowly 在离线场景下表现得像一个本地优先应用——内容首先被安全地保存在本地，网络恢复后自动同步。当年写下这句话时，“本地优先”对我来说还只是一个形容;直到后来读到 Ink & Switch 那篇著名的宣言（《Local-first software: you own your data, in spite of the cloud》，见 materials/ch08_补充.md)，我才发现自己无意中走进了一场早已开始的运动。那篇 2019 年发表的宣言，标题直译过来是:本地优先软件——纵有云，你仍拥有你的数据。纵有云，不是没有云。这三个字，就是 knowly 与 knasync 之间那条分界线的全部秘密，也是接下来四节要展开的全部内容。

## 8.2 泄露成本坐标系:数据的四级分封

工程师喜欢画坐标系，因为坐标系能把争论变成计算。谈“数据放哪”时，业界默认的坐标系是访问频率:热数据放离用户近的地方，冷数据放便宜的地方。这套冷热分层的话语写进了每一份云厂商的销售材料，也写进了大多数工程师的直觉——它看起来如此天经地义，以至于很少有人检查过它的原点。

我的裁决是:频率坐标错了。它衡量的是性能，而性能是钱能解决的问题;它漏掉了那个钱解决不了的问题——泄露。分界线应该画在泄露成本上:如果这条数据被陌生人拿到，最坏会发生什么?

先讲一个我自己的反例，说明访问频率为什么不是决策变量。按访问频率，播客音频是我名下被访问最多的数据:一天四档的财务早餐，凌晨与清晨的定时任务在没有人在场的时候把新的一期推进桶里，第 4 章写过那条 crontab;而剪贴板归档，全世界只有一个读者，是我自己。按频率坐标，播客音频配得上最贵最近的位置，剪贴板配得上最冷的角落。可一旦把泄露成本放进来做交叉检验，排序整个翻转:播客音频泄露了，陌生人得到的不过是几段本来就准备给陌生人听的音频，零损失;剪贴板泄露了，陌生人得到的是我过去几个月的验证码、密码碎片、没发出去的话，灾难。访问频率与泄露成本完全倒挂，这个倒挂说明访问频率根本不是“放哪”的决策变量——它只决定“怎么传”:高频的数据值得缓存、值得预取、值得放近一点;但它从来不决定一条数据有没有资格出门。

我实际使用的坐标系，把名下数据按泄露成本分成四级，像一份家产分封的清单:

| 等级 | 典型数据 | 失守模拟 | 居所 |
| --- | --- | --- | --- |
| 第一级·私有 | 剪贴板归档、笔记、账单、密钥 | 灾难:验证码与私语全部易主 | 自家 NAS，SSH 直存 |
| 第二级·个人 | 跨机指令、微信队列、待办消息 | 麻烦:日程与指令被旁读或伪造 | knasync 的 D1，账号隔离 |
| 第三级·公共 | 播客音频、图床、RSS 订阅源 | 无害:本就是给陌生人看的 | R2，公开域 pic.want.biz |
| 第四级·公开 | 订阅页、落地页、跳转规则 | 为零:甚至怕没人来拿 | Workers 与 CDN |

四级不是安全等级，是主权等级。安全等级问的是“怎么保护”，主权等级问的是“凭什么住这”。逐级说下去。

第一级，失守即灾难，所以它不进任何不属于我的机器。SSH 直存意味着数据从复制发生到归档完成，只经过一条点对点的加密隧道，两端都是我的设备;首次连接按 TOFU 原则记录主机指纹，此后每次连接都校验，中间人无处插足——军团书第 3 章讲过这套连接纪律，这里要补的是它的主权含义:这一级数据的物理位置，从字面意义上说，是我家的客厅。

第二级是分界线上最有意思的一层，因为它是“边缘可用但主权可守”的活样本。knasync 把跨机消息放进 Cloudflare 的 D1——指令、通知、队列，确实住在了边缘，但这一级的泄露成本被两个设计压到最低:其一，消息即取即删，消费掉就没了，不过夜，边缘库里的存量趋近于零;其二，单账号使用，泄露面就是我自己。第二级数据可以出远门，条件是它必须轻——轻到丢光也伤不了根基。

第三级，R2 与公开域，第 4 章整章讲过它的零出站定价，本章不再展开账目，只记录它在坐标系里的身份:第三级数据是主动求曝光的数据，放得越远、越近人，价值越大。把它锁在 NAS 里不是主权，是浪费。

第四级，Workers 与 CDN，连存储都省了——页面本身就是代码，代码本身就是内容。它被“泄露”的唯一后果是多了一个访客。

这套坐标系还有一个常被忽略的性质：等级跟着内容的暴露面走，不跟文件走。同一期播客，录音毛稿与未删减版躺在 NAS，是第一级；剪辑成片推上 R2，是第三级——文件格式没变，等级在发布那一刻完成了迁徙。反过来也一样：一封要发给律师的信，写进笔记时是第一级，发出之后副本散落在邮箱与对方的服务器上，你能守护的只剩自己手里的母本。所以评级不是给文件贴一次性标签，而是每一次流转前的重新验关——母本永远留在最高级，流出的是经过你亲手降级的副本，这条纪律比任何加密算法都更接近主权的本义。

这套坐标系真正的收益，是让一堆纠缠不清的架构争论自动消解。比如“剪贴板要不要同步到手机”:频率视角下这是个同步问题，怎么测延迟、怎么选协议;泄露视角下这是个评级问题——手机上的剪贴板仍属第一级，那么同步通道就必须点对点，不经任何第三方。再比如“NAS 上的笔记要不要备份上云”:泄露视角的答案不是要不要，而是加密之后它属于哪一级——密文失守无碍，那它就可以住进第三级的对象存储;明文失守是灾难，那它就哪儿也不去。评级变了，居所跟着变，而评级只问一个问题:丢了疼不疼。

Ink & Switch 的七项理想（materials/ch08_补充.md）恰好对应这套坐标系的两个端点:安全与隐私对应第一级的底线，长期存活性对应全部四级的愿景。宣言里流传最广的一句话是:“根本没有云，那只是别人的电脑。”听起来像段子，其实是四级坐标系的原点——凡是躺在别人电脑上的数据，泄露成本的裁判权就不在你手里;你能谈的只剩赔偿条款，而赔偿条款从来不按你心疼的程度计价。

分封的隐喻要收得诚实:封建制的要义不是领主把所有土地攥在手里，而是把不同的土地分给不同的封臣，换来整个疆域的秩序。数据也一样——把第三级数据扣在本地，是用最贵的成本干最没价值的事;把第一级数据放上边缘，是用最便宜的账单买最贵的风险。第 1 章说 DNS 记录是地契上的界碑;本章要补一句:界碑圈住的是域名，分封划开的是数据。域名之内的每一条数据，还要按泄露成本再分一次封。接下来的两节，讲这次分封在工程上的两根柱子:进，靠 outbox;出，靠同步脚本。

## 8.3 outbox:边缘不可达时的本地尊严

设想一个具体的下午:NAS 正在升级固件，SSH 拒绝一切连接。此刻你在电脑前复制了一段重要的话，knowly 半秒内捕获了它，然后送不出去。怎么办?

行业的两个标准答案都在场。第一个，弹通知、记失败，把重试的责任交还给用户——这是把系统的责任转嫁给人的记性，而人没有记性，这是一切“请稍后重试”按钮的宿命。第二个，把内容先发到某个云端中转服务器暂存，等 NAS 醒了再取——便捷依旧，但 8.1 立下的裁决就被绕过了:最私密的数据，哪怕只是路过别人的硬盘，也已经是别人硬盘上的一份拷贝。

knowly 的答案是第三个:outbox。这个包的全部代码在 internal/outbox/outbox.go，连注释带空行 194 行，却是整本书里我最愿意逐行讲给人听的一段。先看数据结构:Item 里存着 id、type、content、timestamp，以及一组 AI 元数据字段——标签、摘要、评分、整理后的正文;结构体头顶的注释只有一句，“待同步的条目（SSH 连不上时本地暂存)”。再看落盘位置:配置目录下的 outbox 子目录，一个叫 pending.jsonl 的文件，权限 0600——只有我自己能读。Push 的实现是把整条条目序列化成一行 JSON，以追加模式写进文件尾部，日志里那句原文我每次读都觉得踏实:“will sync when SSH recovers”——会在 SSH 恢复时同步。没有形容词，没有承诺过度，就是一个事实陈述。

排空的过程同样值得细看。Drain 逐条调用同步回调:成功，该条移除;失败，立即停止，该条与其后所有条目原样保留。剩余条目写进一个临时文件，再用原子替换盖回原名;全部排空则直接删掉队列文件。调度在 cmd/knowly/main.go 里，进程启动后先排空一次积压，然后挂一个每五分钟触发一次的周期器，持续补排。这两个动作合起来是一句承诺:只要守护进程活着，积压总会被还上;只要守护进程死了，下次醒来第一件事也是还债。

这段代码最有意思的地方，在于它把分布式系统的一个老模式搬到了人与边缘之间:transactional outbox。微服务架构里，这个模式保证“业务写入”与“消息投递”要么都成、要么都不成——业务数据落库的同时，待发消息也落库，再由后台任务投递。knowly 的版本保证的是另一对动作的原子性:本地捕获与远端归档之间，永远隔着一个已经落盘的承诺。归档可以晚到，但捕获的那一刻，数据就已经安全地属于你了。

三个细节值得单独致敬。其一，AI 元数据随内容一起入箱:离线期间网络断了，但 AI 加工不会白做，标签、摘要、评分都跟着原文一起躺在队列里，重试成功后，远端文件与历史记录依然带着完整的加工信息——离线损失的只是延迟，不是任何一步劳动成果。其二，保序:失败时保留该条及之后全部，而不是跳过失败的继续后面的——顺序是知识的语义，昨天的验证码排在今天的密码前面，乱了就是另一段人生。其三，原子替换:临时文件加改名，绝不在原文件上原地改写——排空排到一半断电，留下的要么是完整的旧队列，要么是完整的新队列，永远不会是半截。图片走的也是同一扇门:二进制内容经 base64 编码后入箱，docs/项目分析.md 第 4.11 节还记录了一个贴心的小动作——入箱前先强制掐断僵死的 SSH 连接，免得下一次排空撞在同一堵墙上。

还有一层可靠性没有交代：排空是 at-least-once 的，不是 exactly-once。极端情况下，同步成功与移除记录之间断电，重启后同一条目可能再排一次。knowly 不为此加锁，因为远端的三层去重早已候着——内存哈希、status.json、NAS 上的哈希索引，同一段内容无论排空几次，归档里只落一份。军团书第 3 章讲过这三层去重的时空分工，换到 outbox 的视角看，它们是排空幂等的承重墙：投递敢承诺“至少一次”，是因为对端敢承诺“只认一次”。第 5 章讲队列时说过，幂等键是 at-least-once 的解药；个人系统里，解药不必花哨，一个哈希足矣。

然后说哲学。outbox 兑现的东西，我叫它本地尊严:数据在到达任何远端之前，先完整地、可读地、带着全部加工成果地属于我。pending.jsonl 是纯 JSONL，任何文本编辑器都能打开看——尊严有一个最低标准，是你的东西你随时看得见，不需要登录，不需要导出，不需要等某家公司的导出功能排期。边缘不可达在 knowly 的世界观里不是数据危机，只是同步延迟:世界断线的时候，知识库欠着你的账，线一恢复，五分钟内一笔笔还清。

这也是本地优先与拒绝云的分界线。本地优先不是拒绝边缘，而是谈判地位——你什么时候才有资格跟边缘服务讲条件?当没有它你也完好无损的时候。Ink & Switch 七理想的第三条说，网络应当是可选项:离线也能读写数据，网络只是碰巧可用的同步机会，不是必需条件（materials/ch08_补充.md)。knowly 的 outbox 是这句话的代码形态:边缘是合作方，不是供养对象;合作方缺席，业务照常，只是晚点清算。

代价也要如实记。JSONL 里损坏的行会被静默跳过——反序列化失败即弃，不阻塞队列;单条读取的缓冲上限十兆，超出读不动。这两条取舍都写在代码里而不在宣传里:宁可丢一行坏的，不可堵一队好的;宁可对超大条目失明，不可让整个队列陪葬。它们不完美，但每一次取舍都指向同一个方向——先保住本地这一层，再谈边缘那一层。outbox 守住了“进”的方向:捕获永不丢失。下一节讲“出”的方向:本地就绪之后，数据怎么体面地走向世界。

## 8.4 双写与同步:R2 同步脚本的设计

军团书里“写即持久化”是知识管道的第一戒律:每一步产物都落盘、可审计、可回放。本章讲它的边缘版，可以叫写即分发——母本在本地落定之后，同一份产物按泄露成本各归各位，一条脚本跑完四级安放。

先看实况。podcast 项目里的 sync.sh，183 行 bash，第一行 set -e，注释自称“全能同步与高级通知脚本”。它每跑一次，把一期播客依次安放到四个地方，外加一条通知。脚本骨架如下:

```bash
# [0/4] 归档旧文件到 NAS(版本史住第一级)
bash "$ARCHIVE_OLD_FILES_SH" || echo "⚠️  归档任务跳过(可能NAS未连接)"

# [1/4] 同步到 Cloudflare R2(核心存储,硬失败)
rclone copy "$OUTPUT_DIR" "r2:yuangs" --verbose
rclone copy "$RSS_XML"   "r2:yuangs" --verbose
rclone copy "$INDEX_HTML" "r2:yuangs" --verbose

# [2/4] 同步到 iCloud Drive(移动端预览,软失败)
ditto "$OUTPUT_DIR" "$ICLOUD_PATH" || echo "⚠️  音频文件同步到iCloud失败"
cat "$RSS_XML" > "$DEST_XML" || echo "⚠️  XML文件同步到iCloud失败"

# [3/4] 更新 GitHub RSS(订阅下发,软失败)
git add "$RSS_XML"
git commit -m "feat: 自动化更新播客RSS [$PODCAST_TITLE]" || echo "Nothing to commit"
git push origin main

# [4/4] 构造 pic.want.biz 试听链接,发送企业微信图文通知
MP3_URL="https://pic.want.biz/${ENCODED_FILENAME}"
curl -s -X POST "https://api.yuangs.cc/weixinpush" --data-raw "${WX_JSON}"
```

注意代码块里的注释是我在摘录时加的，原文没有;原文的 0 到 4 五个步骤是五个编号的 echo，像一张值日表。183 行里真正的主干就是上面这几行，其余是路径解析、URL 编码、备用通知与桌面通知的层层兜底。接下来是设计的五条分析。

第一条，单向流，无双写冲突。整条链路里 NAS 是唯一写者，R2、iCloud、GitHub 全部只读。双写的灾难从来不是同步工具不行，而是两个可写副本互相覆盖、互为真相，最后没有人知道哪份是对的;这里从结构上排除了这种可能，因为真相唯一，分发只是复制。很多团队折在“同步地狱”里，病根不在工具，在源头不唯一。

第二条，硬失败与软失败的分层。仔细看兜底符号的分布:rclone 三行没有兜底，存储失败是硬失败，脚本当场死;归档、iCloud、GitHub、通知，统统有兜底，属于软失败，记一笔继续跑。这个分布不是随手写的。存储是资产的最终归处，存储失败而分发继续，只会制造“看起来发成了、桶里其实是旧的”的假象，比不发更糟;分发失败而存储成功，什么都没损失，下一轮补上即可。失败也分等级:兜底给软的，硬的让它死。shell 脚本里的哲学浓度，经常超过多数架构文档。

第三条，版本化住在第一级。R2 的对象同名即覆盖，旧版本去哪了?答案不在桶的版本控制开关里，在 NAS——第 0 步的归档脚本先把旧文件收进归档目录，再开始新一轮分发。把版本史放在四级坐标里最私有的那一级，而不是依赖边缘桶的功能开关，逻辑与 8.2 一脉相承:历史是我的家底，不该存放在租来的仓库里，更不该指望仓库管理员替我记永远。

第四条，最终一致的自觉。整条链路没有事务，也不需要:音频先进 R2，通知最后发;若通知那步失败，前三步的成果毫发无损，备用通知与桌面通知接着试，再不行就等下一轮。一致性预算全部花在存储这一跳上，分发环节的每一跳都接受“下一轮补齐”。这是把 8.3 的 outbox 哲学翻过来用:outbox 说“边缘不可达，本地替你记住”，sync.sh 说“本地就绪后，向多端依次排空”。一进一出，同一个信念——数据永远先属于本地，边缘只是发行渠道。

第五条，零带宽税的四副本。这份产物同时住在 NAS、R2、iCloud、GitHub 四个地方，而四份副本的增量带宽账单是零:R2 不收出站流量费，BRIEF 里记作“零出站”词条，第 4 章算过细账;iCloud 与 GitHub 各自躺在免费额度内。十年前把同一份内容放四个地方，账单会先杀死你;现在能杀死你的只有归档的自律——而归档恰好是这个脚本的第 0 步，已经写好了。

顺带回答一个常见疑问：为什么不干脆上双向同步工具，让 NAS 与 R2 互为镜像？因为方向即秩序。双向同步意味着两个可写端互为真相，8.2 划定的等级边界会在第一次冲突时失效——你无法保证第三级的分发件不会反向污染第一级的母本。单向流看着笨，笨得像一条单行道，但单行道有单行道的安全：数据永远从高主权等级流向低主权等级，只出不进，流出过什么、流去了哪，一条 rclone 日志看得清清楚楚。对一个人的系统来说，主权的流向图，远比同步的便利性重要。

还有一个纯工程的小坑值得留给后来人:iCloud 的文件锁。脚本注释里写着，XML 用 cat 重定向而不是 cp——目标目录是 iCloud 驱动器，繁忙时文件被锁，cp 会失败，先删旧文件释放句柄、再用重定向写入，成功率明显更高;音频用 ditto 而不是 rsync，因为 ditto 会自动处理 macOS 的 NFD 与 NFC 编码转换并保留资源分支。这些都是只有真踩过才会写进注释的细节，它们共同说明了 8.2 坐标系的一个推论:同一个文件搬到不同等级的居所，要入乡随俗——边缘有边缘的规矩，云端有云端的脾气，别假设一份拷贝能 naive 地四处放。

收个尾。一份播客产物，在这个脚本里恰好走完了 8.2 的整个坐标系:母带与脚本归 NAS，第一级;分发件住 R2，第三级;订阅页与落地页住在边缘的代码里，第四级;通知里那声“更新了”若要跨机接力，走 knasync，第二级。一条 183 行的 bash，就是一次完整的分封仪式。写即持久化的尽头是写即分发，而分发的前提，是每一级居所都各安其位。

## 8.5 分界线的人民性:为什么个人开发者更需要本地优先

人民性这个词，我是从政治学里借来的:一套设计原则，对资源悬殊的博弈双方，谁受益更多。有些原则对巨头是锦上添花，对个人是雪中送炭;本地优先属于后者——对大厂，它是众多架构选项中的一个;对个人开发者，它是保命的那一个。这一节给出三重论证，然后与边缘优先和解。

论证一，成本。个人开发者没有规模折扣，账单敏感度极高，而本地存储恰好是个人唯一打得过巨头的战场:家里的 NAS 设备折旧早已摊完，多存一年数据的边际成本是零;边缘的每一行读写都在账本上。算一笔具体的账:knowly 每天捕获几百条，一年十几万条，纯文本加元数据一年几百兆——放在 NAS，四舍五入等于免费;放进 D1，免费层单库上限五百兆，写操作按行计费，免费额度每天五百万行读听着花不完，但把十几年的知识复利压进一个按行计费、按存储封顶的库里，等于把最该长寿的数据交给了最不适合长寿的计费模型。第 9 章会正式摊开账单，这里先把结论立在分界线上:边缘的钱要花在刀刃上——花在公共分发上，不花在私有沉淀上;私有沉淀的账，自己的设备来付，付一次，管一辈子。

论证二，隐私。8.1 已从剪贴板讲到泄露成本，这里补两条行业事实。2017 年 10 月，谷歌的自动化系统误将部分用户的 Google Docs 文档标记为违规，用户被锁在自己的文件之外（materials/ch08_补充.md);云盘们的服务条款里白纸黑字写着，自动化系统可以分析你的内容。大厂用户遇到这些，有法务审条款，有安全团队盯泄露通知，有 SLA 支撑索赔;个人开发者什么都没有。个人的唯一可靠防线，不是更强的加密或更细的权限，而是让数据根本不出发——不出发，就没有沿途，没有沿途，就没有沿途的风险。这是分界线画在泄露成本上的第二层含义:防御预算要花在失守即灾难的那一级，而那一级的最好防御，是物理上的不出门。

论证三，退出自由。Ink & Switch 七理想的第五条叫长期存活性:即使开发公司倒闭，你的数据和软件依然能用，避免数字黑暗时代（materials/ch08_补充.md)。这条理想落到 knowly 上，是一套朴素到近乎固执的格式选择:归档是 Markdown 文件，按年月日铺在 NAS 上，grep 可搜、rsync 可备份、Hugo 可直接接管——军团书第 3 章讲过这套开放性，这里从退出成本的角度重算一遍账。把十几年归档从某个云笔记里搬出来，成本是导出格式的损耗、内部链接的失效、肌肉记忆的重训，以及一整个周末的坏心情;从 NAS 搬出去，成本是一条 rsync 命令。退出成本才是锁定效应的真度量——免费锁定也是锁定，数据能导出但生态带不走，依然是锁定。本地优先的退出自由，本质是把“离开”这个选项的价钱，永远压在一杯咖啡以下。

三重论证可以合并成一个检验动作。以后再遇到任何一个诱人的服务——免费的、好用的、朋友推荐的——先别问它好不好用，先问一句：它会把我的数据搬到第几级？答案是一级，就让它连门都进不来，或者进门之前先加密成密文；答案是三级四级，就大方地把钥匙递过去，让它替你满世界分发。个人开发者的架构评审，一半可以浓缩成这一个问题；另一半，是诚实回答它的勇气。

三重论证讲完，该和解了。我不是本地原教旨主义者，证据就写在本章标题里:knasync 的跨机消息毫不犹豫放 D1，因为它是第二级数据，泄露成本配得上边缘的便捷;播客毫不犹豫放 R2，因为第三级数据的天然居所就是边缘，把它们扣在本地反而是浪费。和解条款一共三条:边缘拿走的是分发权，不是所有权;本地保留的是母本与隐私，不是与世隔绝;两边的通道——outbox 与 sync.sh——永远存在，且永远单向可控。本地优先与边缘优先不是敌对阵营，是同一场分封里的两个封地，课税不同，义务不同，但都效忠于同一个朝廷。

最后说人民性的收束。大厂讲“数据放哪”，是合规问题，一支法务团队兜底;个人讲“数据放哪”，是生存问题，没有任何兜底。个人开发者手里的筹码只剩一样:数据在我手里——NAS 上的归档不因为任何一家公司的季度财报而改变住址，剪贴板里的私语不因为任何一次条款修订而改变读者。分界线画在泄露成本而不是访问频率上，本质是把有限的防御预算集中到真正疼的地方:访问频率决定了你疼得早一点还是晚一点，泄露成本决定了你疼完还能不能站起来。

第 1 章说，域名是地契，DNS 记录是界碑;这一章补上后半句:真正的不动产不止域名，还有每一条数据的住址确权。从租到有，租来的是计算，攒下的是主权;而主权不在账单的某一行里，在你每一晚入睡之后，数据实际所处的位置里。knowly 与 knasync，一个把知识留在客厅，一个把消息放上边缘——它们不是两个项目，是同一场分封的两个封地。分界线画好了，才好谈第 9 章的账单:因为只有先确了权，才知道每一块钱究竟花在了谁的领地上，哪一块地上长出的庄稼，真正归自己所有。

---

## 第9章 成本账单:跑一个边缘军团到底花多少钱

# 第 9 章 成本账单:跑一个边缘军团到底花多少钱

## 9.1 摊开账单:某月的真实数字

全书从一张账单开场，理应从一张账单收尾。第一章立过方法论的规矩：每一章都有一张账单，九章读完，账单合并——现在到了合并的时刻。但动笔之前，我要先履行一个比方法论更高的承诺：诚实。技术书写到终章，惯例是晒一张具体到分的月度账单，再配一句“真实的数字最有说服力”。我不打算这么做，因为我没有那样的账单可晒——这句坦白本身就是本章的第一组数据。我的做法是另一种：把每个服务的免费额度与单价逐行摊开，每一行注明出处；再以精算师的口吻推算“如果超出免费层，账单会长成什么样”。两张表读完，你比看任何账单截图都更接近真相。真相先放在这里：支撑图床、队列、行情分发与模型网关的那套边缘基建，长期运行在免费额度之内；现金支出的固定项只有一行——域名年费；变动项四舍五入等于零。这不是本章留到结尾的悬念，这是本章开宗明义的论点。

先摊第一张表：免费额度表（来源：materials/ch02_补充.md、ch03_补充.md、ch04_补充.md、ch05_补充.md，均为官方现行口径）。Workers 免费版每天 10 万次请求，每次调用 CPU 上限 10 毫秒，等待网络 I/O 的时间不计入；D1 免费层每天 500 万行读、10 万行写，账户总存储 5GB；R2 免费层每月 10GB 存储、100 万次 A 类操作、1000 万次 B 类操作，出站流量无条件免费；Queues 免费版每天 1 万次标准操作，消息保留 24 小时；Workers KV 每天 10 万次读、1000 次写，存储 1GB。表里最特殊的一行是 AI Gateway：素材档案里找不到它的单价，因为它的价值不以“收你多少”的形式出现，而以“替你省下多少”的形式出现——缓存命中零 token 消耗，Spend Limits 把美元支出按 key 与周期拦在预算之内，真正的账单开在模型供应商那头（materials/raw_queues_ai.md）。安检门不卖门票，安检门负责看住你兜里的钱。

第二张表：如果超出免费层，单价与包含额度长什么样（来源：materials/ch02_补充.md、ch03_补充.md、ch04_补充.md、ch05_补充.md、ch09_补充.md）。Workers 付费计划每月 $5 起付，含 1000 万次请求与 3000 万 CPU 毫秒，超出部分每百万次请求 $0.30、每百万 CPU 毫秒 $0.02；D1 付费含每月 250 亿行读、5000 万行写、5GB 存储，超出部分每百万行读 $0.001、每百万行写 $1.00，存储每 GB-月 $0.75；R2 超出部分存储每 GB-月 $0.015，A 类操作每百万次 $4.50，B 类每百万次 $0.36；Queues 付费含每月 100 万次操作，超出每百万次 $0.40。这里要特别注意 $5 的语义：它不是“买某个服务”的钱，而是账户级的起付线——上面这一整串包含额度全部捆绑在内。这就是“全球基建券”的定价方式：你付的不是机房租金，是一张入场券的底价。

两张表摊开，做三个情景推算，逼一逼这套价目表的上限。情景一，把免费层当满负荷用：Workers 每天 10 万次请求跑满一个月约 300 万次，离付费层包含的 1000 万还有七成富余；顺带一个只有精算师才会在意的巧合——每天 10 万次乘以每次 10 毫秒的 CPU 上限，一个月恰好是 3000 万 CPU 毫秒，与付费层的包含额度严丝合缝。两个世界在这里对齐：一个把免费版用到爆表的用户，升级之后的超额账单依然是零，总支出锁定在 $5 起付线。情景二，图床体量翻十倍：存储从几个 GB 涨到 100GB，超出免费层的 90GB 按每 GB-月 $0.015 折算是每月 $1.35；而日均三十万次的图片读取仍埋在 1000 万次 B 类免费额度里，分文不取。情景三，队列扩容：假设 knasync 的消息量涨到每天三万条——比今天的量级高出约两个数量级——每条消息全生命周期按四次行写、二十次行读、两次请求粗算，一个月是 360 万行写、1800 万行读、180 万次请求。行读与请求两项，免费层的额度就吞得下；只有行写越线约 60 万，按每百万行写 $1.00 折算，每月六毛。把量级放大一百倍，变动成本是一年七块二毛钱。

最后是本月账单的真实读法。固定项一行：域名年费，摊进每个月是它的十二分之一；具体数字取决于后缀与注册商，我不在这里虚报——第一章那张年度对账表上，它是唯一不续费就会消失的行。变动项：零。要紧的是分清这个零的性质——它不是省出来的，是被架构压出来的：入口写得薄，请求就少；索引建得准，扫描就少；消息活得短，存储就小。有人把免费层内运行叫薅羊毛，我不同意：条款白纸黑字写着长期有效，我只是按合同生活的人。接下来的四节，解释这个零背后的一切——平台为什么愿意白送，单价如何翻译成体感，涨价了怎么办，以及这一切最终意味着什么。

## 9.2 免费额度的经济学:平台为什么愿意白送

把 9.1 那张免费额度表再看一眼，会生出一个朴素的疑问：这符合商业逻辑吗？一家上市公司，把分布在全球数百座城市的计算、存储与数据库，白送给一个用量的级以千计的个人用户，图什么？回答这个问题不能靠抒情，要靠资产负债表。免费层不是慈善，是成本表上的一行获客预算、一张写明条款的合同；看懂平台为什么愿意白送，个人开发者才能既不把免费当恩典，也不敢把免费当陷阱。我的答案有三层：漏斗、密度、驻留。

第一层，漏斗。免费层是获客漏斗最宽的那个口，$5 的付费起付线是漏斗的颈——平台的全部赌注押在一个概率上：项目会长大。一个今天每天几百次请求的玩具，三年后可能是一个养活团队的生意；从免费区走到付费区的那一步，往往不是因为价格上涨了，而是因为项目自己长过了额度线。第一章说过，免费层的本质是交易而非施舍：平台赌你的未来，你拿走你的现在（见 1.4 节）。把 R2 与 S3 的免费条款摆在一起，能看清两种商业哲学的差别：R2 每月 10GB 存储、100 万次 A 类、1000 万次 B 类，条款长期有效，不设到期日；S3 的免费层是 5GB，只给十二个月（materials/raw_r2.md）。前者是户口，后者是试用装——一个假设你会留下来，一个假设你会长大。对精算师而言，长期有效的免费条款比更大的短期赠送值钱得多，因为它可以被写进架构假设，而试用装只能写进风险清单。

第二层，密度，这是被白送在物理上成立的原因。回看第二章的解剖：一个 isolate 的额外内存约 3MB，冷启动约 5 毫秒，等待网络 I/O 的时间几乎不占资源，数千个隔离上下文可以共享同一个进程（materials/raw_worker.md）。这些数字在架构上叫轻，在会计上叫边际成本趋近于零——一个免费用户一天十万次请求，对平台而言是几 MB 内存乘以几毫秒周期，摊进同一进程里数千个租户，是零头的零头。cordons 信任分组再把免费租户与企业租户隔进不同进程（见 2.2 节），大客户的稳定性与个人的免费额度互不拖累。这像航空公司的经典生意：满员的航班多载一个轻乘客，边际成本不过一份餐食。白送的不是钱，是余隙——用本就闲置的容量，换一张未来可能兑现的期权。D1 的读副本同样如此：按流量自动启停、不额外收费、不让存储成本翻倍（materials/raw_d1.md），平台的调度能力本身就是它的富余产能。

第三层，驻留，这是白送真正想要的回报。数据引力在第四章讲过：数据有质量，会把计算、缓存与习惯一并吸在身边（见 4.1 节）。免费层住得越久，搬走的心理成本越高——这半句是对我不利的，精算师必须把它记在自己那一栏。平台用它换的不是月费，是长期驻留：你在它的网上放的每一张图、每一张表、每一条队列，都是留在明天的钩子。所以免费层必须设计得“刚好够用”——不够用留不住人，过于够用养不活平台；$5 这条线，就是两边讨价还价出来的均衡点。AI Gateway 把这个逻辑推向极致：连网关本身都不计费，因为它省下的每一笔供应商账单，都在替平台巩固“你的 AI 流量从这里走”的地位（materials/raw_queues_ai.md）。

还有一笔容易被漏记的账：免费层住户并不只是平台的成本，也是平台的资产。一个全球分发的个人项目，是一件行走的展品——它的每一个公开链接、每一篇技术笔记、每一次别人问起“这站怎么搭的”，都是平台花不到一分钱买到的口碑广告。企业客户要靠销售团队一单一单谈下来，免费层用户的推荐却在技术社区里自发传播，边际成本同样是零。平台对此心知肚明，所以免费层的条款宁可写得慷慨——长期有效、全家桶开放、副本不另收费——也不写得寒酸：慷慨是获客成本里性价比最高的一种花法。看懂这一层，个人开发者对平台的姿态就摆正了：不是乞丐领施舍，是创作者与渠道互给流量，双方清清白白，各取所需。

看清了动机，再看免费层的真实边界，就不会有幻觉。边界不是功能阉割——免费版能调用 KV、D1、Queues、Hyperdrive，全家桶一样不少；边界是规模护栏，且越来越硬。Workers 超过每天 10 万次请求，直接返回 Error 1027，超额的请求被平台拒绝而非延迟；每次调用 10 毫秒 CPU 上限，纯转发安然无恙，重计算当场中断；免费账户的子请求上限 50 个、脚本体积 3MB、Worker 数量 100 个（materials/ch02_补充.md）。D1 免费层单库 500MB、每账户 10 个数据库，Time Travel 只保留 7 天（materials/raw_d1.md）；最值得写进运维手册的日期是 2026 年 9 月 1 日——从那天起 D1 开始强制执行免费层每日限额，超额之后查询直接失败，不再只是限流与邮件提醒（materials/ch03_补充.md）。免费的含义，在一个普通的星期一早上悄悄变窄了一格，而没有人给你寄账单。Queues 免费版的消息保留期锁死在 24 小时，连配置的资格都没有（materials/ch05_补充.md）。

这些边界划在哪里，看得越久越清楚：划在“个人”与“生意”的分界线上。每天 10 万次请求、500 万行读、30 万次图片访问——这个量级够一个独立开发者从第一行代码写到第一万个用户；但任何一个商业项目从第一天起就会撞线。免费层是给个人的，不是给创业公司的，这句话是平台经济学里最诚实的注脚。所以我的姿态是精算师的姿态：把免费额度当收入一样逐笔记账，把每一条边界当架构约束写进设计——入口为 10 毫秒 CPU 而薄，索引为行读额度而建，消息为 24 小时的保留期而短。把白送的经济学看穿之后，你既不会在 9 月 1 日那样的早晨措手不及，也不会因为“天下没有免费午餐”这句话，而错过一份真的写了长期有效的午餐。

## 9.3 单位成本思维:每万次请求、每 GB、每天

价目表说的是“每百万”，生活说的是“一次”。这两套语言之间隔着一道翻译的工序，而绝大多数人从不动手翻译——于是价目表永远只是一串与己无关的数字，恐惧与贪婪全靠想象填空。精算师视角的全部工作，就是这道翻译：把官方的定价单位，换算成自己项目的单位消耗。翻译完成之后，每个数字都会开口说话。这一节把 9.1 的两张表翻译到底，然后回答那个真正要紧的问题：我的项目离收费线还有几个数量级。

先做一张换算卡片，全部按付费层单价折算（来源：materials/ch02_补充.md、ch03_补充.md、ch04_补充.md、ch05_补充.md）。每万次 Workers 请求约 $0.003；每万次 D1 行读 $0.00001；每一次行写 $0.000001——一百万次行写才一美元；每 GB-月存储 $0.015；每万次 R2 读取（B 类）$0.0036；每条队列消息按三次操作折算约 $0.0000012。再翻译成体感：一张 200KB 的图片住进 R2，一年的房租约十万分之四美元；它被看一万次，电费不到半美分；一条 knasync 消息从生到死走完全程，在 Queues 的价目表上是百万分之一点二美元。这些数字小到没有现金意义，却有清晰的工程意义——它们把“我能养多大”从感觉变成了除法。

然后给 knasync 算一次全生命周期的单位成本。假设一条消息的一生是这样：提交一次请求、写入两行（表一行、复合索引一行）、被拉取一次请求、区间扫描约二十行、消费后迁入 results 再写两行、登记表按时间戳清扫摊销若干——合计约四次行写、二十次行读、两次请求。按付费单价折算，一条消息的全部数据层开销在 $0.000005 这个量级；每天一千条，一个月约十五美分；每天一万条，一个月约一美元五十美分。而 9.1 的情景三已经算过：即便量级再放大十倍，付费层的包含额度也吞得下绝大部分，真实的超额支出趋近于零。这张小账的意义不在省钱，在校准——它告诉我，knasync 的天花板不是钱，是别的东西：是我的注意力，和 D1 单写者那个每秒几百次写入的咽喉（见 3.5 节）。

换一个更日常的刻度：一天。Workers 免费版每天 10 万次请求，折合平均每秒一点二次——够一个中小型开发者社区的 API 后端从容运转；D1 每天 500 万行读，折合每秒约五十八行——够一个日活数千的应用做状态查询；Queues 免费版每天 1 万次操作，按一条消息三次操作折算，够约三千条消息投递（见 5.3 节的账单段落）。把免费额度翻译成“每天能养活什么”，是精算师最实用的日常练习：它让额度从抽象的数字变成具象的画面，也让人在写每一行代码时都知道自己花的是哪一桶水。

最后是规模拐点——单位成本思维真正的落点。拐点的公式朴素到可以写在便签上：拐点等于免费额度除以单位消耗。每个服务的第一堵墙位置不同，这正是架构翻译的价值所在。Workers 上，计算密集型负载先撞 10 毫秒 CPU 的墙，流量型负载先撞每天 10 万次的墙——同一个服务，两种死法；D1 上，第一堵墙几乎总是无索引的全表扫描：行读取按扫描行计费而非返回行（materials/ch03_补充.md），一条不带索引的查询能把一天的额度烧掉一大截，所以第三章说那四个索引不是性能洁癖，是账单防线；R2 上，读多写少的图床先撞 10GB 存储墙——按一张 200KB 算是五万张图，写密集的场景先撞每月 100 万次 A 类操作墙，官方给的解法是批量写入与预签名直传（materials/raw_r2.md）；Queues 上，墙是每天 1 万次操作，换算成消息约三千条。把每个服务的“我的单位消耗乘以我的量级，再除以免费额度”算一遍，得到的是一张拐点表——它比任何服务等级协议都诚实，因为它明明白白写着：在哪个量级，我从精算师变成客户；变成客户之后，每个月要付多少。

补一个实操细节：单位成本思维要配上用量仪表盘才算闭环。D1 在查询返回的 meta 对象、GraphQL Analytics API 与控制台面板里都能看到行读行写的消耗（materials/ch03_补充.md）；Workers 的 Metrics 页能查到超额时的 Error 1027 与 1102（materials/ch02_补充.md）；R2 与 Queues 各有用量页。我的习惯是每月对账那天把几个面板各看一眼，像看水电表——数字本身没有情绪，但趋势有：曲线的斜率是项目的生长速度，曲线与拐点的距离是余粮。顺手设一条阈值提醒：9.2 里那种 9 月 1 日式的边界收紧不会提前打招呼，但用量曲线会。精算师的仪表盘不在财务软件里，在平台的 Metrics 页里。

单位成本思维的最终产物，不是一份省钱的账，而是一种预见力。知道拐点在哪里的人，不恐惧增长——增长到拐点是喜事，越过拐点是生意，两者都早有预算；恐惧增长的，从来是那些从未翻译过价目表的人。第一章 1.5 节立过的那条纪律——把定价翻译成自己项目的单位成本——在这里兑现完毕。数字本身不浪漫，但数字是个人开发者唯一不会被误导的语言；而把数字翻译成自己项目的语言，是这门语言里最重要的一门语法。

## 9.4 如果明天涨价:退出成本演练

第一章 1.4 节立过一句军令状：第九章要做一次完整的退出演练来验证“从租到有”的押注——如果演练失败，全书论点作废，我会如实写进这一章。现在是兑现的时刻。先把演练条件设到最苛刻：从明天起，所有单价翻倍，免费额度减半，平台态度不再友好。要回答的问题只有一个——我能不能走。

演练之前，先分清涨价的两种形态。对付费用户，涨价是单价上行；对免费层住户，涨价是边界收紧——没有一分钱变多，但“免费”的含义悄悄变窄。2026 年 9 月 1 日 D1 开始强制执行免费层每日限额，超额查询直接失败（materials/ch03_补充.md），就是一次标准的边界收紧：它的学名不叫涨价，叫“免费的重新定价”。所以我长期住在免费层这件事，并不让我对涨价免疫——我只是对另一种涨价敏感。演练的真正对象因此不是“我要多付多少”，而是“门还开着吗”。

第一项，R2。退出 R2 的成本几乎为零，原因有两个，都很硬。硬理由一：S3 兼容 API 是全行业通用的插座，rclone、各类备份工具、任何支持 S3 协议的客户端，改一行 endpoint 就能对着别家继续干活；当初选择它而不是某家私有 API，就是在为今天的演练买保险（materials/raw_r2.md）。硬理由二：零出站。从 R2 拉走全部数据，流量费是 $0——搬出不交税。对照组是历史：当年若从 S3 搬 1TB 数据出来，先要交一笔约 $92 的出站税（materials/raw_r2.md）。数据引力曾经的全部力量，都来自“进得来出不去”；零出站把这个不对称抹平了。搬家成本的金钱项归零之后，剩下的只是复制时间——这是第四章那句“搬家成本决定谈判地位”在终章的回声：谈判地位从来没有像今天这样高过。

第二项，D1。它的退路写在它的血统里：本体是 SQLite。一条导出命令，得到一份标准的 .sql 文件，装进任何一台装了 SQLite 的机器——包括我 NAS 上那一台（第三章 3.5 节预告过的这条路径，此处履约）。这套演练我不只做一次：导出的库文件定期进本地归档，而归档这个动作本身因为零出站变得毫无心理负担——第四章说过，零出站之后连异地备份的懒惰都被治好了（见 4.1 节）。这里必须补一句纪律：演练不落地的承诺等于没有承诺。每年把导出文件真正 restore 一遍、跑一遍最基本的一致性检查，才算数；备份的有效期不是磁盘的寿命，是上一次成功恢复的日期。

第三项，Workers，最难搬的一块，必须诚实。isolate 的运行时契约与 bindings 是平台方言，Worker 代码不能原样跑在任何别处——这部分是全球地契上真正的“租”。但纵深防御早已埋好：入口写得薄（第二章），核心逻辑是无状态纯函数；队列的全部语义建在标准 SQL 的四张表上（第五章），不依赖任何私有队列服务；消费者只依赖 HTTP 与一个鉴权头，Chrome 扩展、NAS 守护进程、命令行工具都能拉消息（见 5.2 节）。这意味着哪怕 Workers 明天整体消失，knasync 的协议可以在任何一台能跑 HTTP 服务的机器上复活。代价同样要写明：从全球三百多座城市退化为一台家用 NAS 的一个进程。“全球性”带不走——数据与协议是自己的，离用户最近这件事是租的，明码标价，价钱就写在 9.1 与 9.3 里。“从租到有”最精确的边界在这里现形：有的是数据与逻辑，租的是分布。

第四项，域名与 DNS。域名注册在我自己名下，解析记录随时可以整表导出，TTL 调低之后最坏的切换窗口以小时计（第七章的主题，此处只记账）。界碑可以搬，地不能丢——这是全书唯一的固定资产，也是所有演练里唯一一项“退出即受损”的资产：受损的不是钱，是全球解析网络的默认接入资格。幸运的是，DNS 是去中心化程度最高的基础设施，换一家权威服务商，地还是那块地。

演练做完，可以打一张成绩单。R2，退出成本趋近于零，通用协议加零出站，五项里最潇洒的一项；D1，退出成本是一次导出，本体开放，是数据库里罕见的“能整库搬走”的物种；Workers，代码本身不可移植，但架构早已把可移植的部分——数据、协议、幂等语义——分层搬了出来；域名与 DNS，界碑在手，切换窗口以小时计。四项里三项的金钱成本为零，一项的代价是分布。这张成绩单验证的不是某家平台有多好，而是当初每一次选型埋下的伏笔有多值钱：选通用协议不选私有接口，选开放格式不选私有容器，选薄依赖不选深耦合。退出演练是一面镜子，照见的全是入住那天的自己。

演练结论：通过，但通过的姿势值得抄写一遍。数据层与协议层的退出成本趋近于零；唯一带走的不了的东西是物理分布，而它本来就是租约上写得最清楚的一行。不锁定的架构习惯，全书的积累可以列成一张清单：幂等键作数据库主键，换库可重放；用标准 SQL，不碰方言甜头；对象不可变、格式开放，Markdown、JSON、PNG，十年后仍有工具能读；密钥与配置收在本地版本库；outbox 本地暂存，边缘不可达时保住本地的尊严（第八章）；每年一次真正的恢复演练。这张清单就是个人开发者的全部保险——保险公司不在别处，就在你写每一行代码的那些克制里。

## 9.5 全球地契的年租:一个人的基建宣言

到了算总账的时刻。把九章的账单合并成年终总账，只有三栏。固定栏一行：域名年费——第一章那张年度对账表上的唯一一行，也是“不续费就会消失”的唯一一行；全球地契的年租，就是它。变动栏：趋近于零——9.1 的两张表与三个情景推算已经把这一栏摊到了小数点后，一年的变动成本在免费层内四舍五入为零，越线之后的量级是几毛到几美元。潜在栏：$5 的付费起付线——注意它在总账里的性质不是支出，是期权的行权价：哪天项目长过拐点，行权换到的是每月 1000 万次请求、250 亿行读、5000 万行写与一整个全家桶，而不是一份安心。三栏加总，就是“一个人的全球基建”的全部租金。

十年前，同一张总账长什么样子？虚拟主机月租、VPS 月租、备份存储、CDN 流量包、证书与监控，五项起，每项都要单独去谈（见 1.1 节）；其中任何一项的全球分发能力都是加购的奢侈品。今天，同一份能力的年租缩成一行域名费。这个对比不是我的节俭，是计费单位十年持续变小的结果——从按月租一台机器，到按毫秒租一段计算，再到按请求租一张网络的入口；计费单位越小，个人与巨头在成本结构上的差距就越小。这就是全书总纲第一条“从租到有”的最终核算：地契是真的，年租是付得起的，退出演练是通得过的。

借终章的位置，把贯穿全书的五条总纲逐一清点归档。其一，从租到有：一个 Cloudflare 账号等于数百城市的计算节点、SQLite 兼容的边缘数据库、零出站的对象存储与一张 AI 安检门，这不是“上云”，这是一张数字世界的全球地契，DNS 记录是地契上的界碑。其二，按量付费的日用品化：全球网络能力与电费水费同构，不用就缩到零，用多少付多少——9.3 的单位成本翻译，就是这门日用品的说明书。其三，人不在场：队列把时间从通信里解绑，knasync 在我睡觉时替我信守承诺，时间机器的车票钱包含在免费额度里。其四，成本即哲学：每月账单明细是对“云原生”最诚实的注脚，免费额度不是施舍而是商业策略，个人开发者要学会在其中做精算师——本书九章，不过是这条哲学的九次展开。其五，本地与边缘的分界：私有数据留 NAS，公共分发走边缘，分界线画在泄露成本上——knowly 与 knasync 各守一边，谁也不吞并谁。

总账之外，还要记下这个论点的代价面，那是本章真正的论点。真实成本趋近于零，是有代价的：代价是把架构写成省钱的样子。入口为 10 毫秒 CPU 而薄；索引为行读额度而建；消息为幂等与 24 小时保留期而设计；数据为 500MB 的免费库而节制；消费者为退出自由而只依赖 HTTP。表面看处处抠门，实际上处处克制——而克制恰恰是架构的本质：架构从来不是“能加什么”，是“能不加什么”。一个把免费额度当预算约束的工程师，会自然而然得到薄入口、小数据、强索引、可迁移的协议；这些习惯单独看是省钱的技巧，合起来看是最好的架构课。约束不是创造力的敌人，是它的模具——一个人的系统尤其如此：一颗脑袋扛得住，一张账单养得起，一场搬家搬得走，这三个“一”，就是我在本书里用到的全部设计美学。

所以终章的最后一页，我想把它写成一份宣言。一个人，一只终端，一个免费账号：他拥有分布在全球三百多座城市的计算，一个自带时间点恢复与读复制的边缘数据库，一个敢被全互联网下载的图床，一条在人睡觉时依然投递的队列，一道替他看着 AI 预算的安检门。他每年为此支付的真金白银，是一行域名年费；他为此支付的其他货币，是克制、索引、薄与备份演练。这在十年前是企业特权的清单，在今天是注册页上的一个按钮。边缘不是大厂发布会上待价而沽的营销词，它是个人开发者可以真实触摸、真实驾驭、真实拥有的物理存在——本书的九章，只为把这一句话钉实。

姊妹篇在此一并交代。《一个人的军团》写的是这支军团如何在基建之上行军——本书是它脚下的大地，两本书共用同一片边缘的天空。《命令之下》写的是当所有按钮收敛成终端里的一句命令之后，人还剩下什么必须亲自决定。《推理工厂》写的是模型被当电来接的年月——推理如何变成以 token 计价的流水线，以及流水线尽头那个依然要为质量负责的人。三本书合起来，是同一个时代的三个切面：基建、命令与智能。

最后回到账单。价格会漂移， $5 会变，额度会收紧，9 月 1 日那样的早晨还会再来；但有三样东西比任何具体数字都长寿：把定价翻译成自己项目的单位成本，把退出成本写进架构，把账单读成哲学。全球地契的年租，此刻已付讫。王国不大，确实是自己的——这就是一个人在边缘时代的全部资产负债表，也是这本账能给出的、关于“云原生”最诚实的注脚。

---
