# 九库五机
## —— 一个人的 AI 基础设施全面体检与加固纪实
> 2026-09-05 至 09-06 · 九个代码库 × 五台机器的深度探测实录
> ZCode 探测与撰写 · 苑广山（yuanguangshan）出品

---

## 目录
- 前言
- 第 1 章 · poeapi_go —— AI 网关，链路的最终闸门
- 第 2 章 · aiproxy —— 从 166 行到链路命脉的 Worker
- 第 3 章 · wsstunnel —— 反向 PTY 隧道
- 第 4 章 · npm_yuangs —— SSH 治理执行器
- 第 5 章 · knasync —— 边缘内容队列
- 第 6 章 · wxwatcher —— 文件变更哨兵
- 第 7 章 · go-music —— 音乐服务与它的 502
- 第 8 章 · TAAGE —— 治理引擎的重生
- 第 9 章 · chats —— 四颗雷与零测试的代价
- 第 10 章 · t 机 —— 链路的心脏所在
- 第 11 章 · u 机 —— 微信世界的桥接局
- 第 12 章 · b 机 —— 半身不遂的侦探记
- 第 13 章 · Mac mini —— 新工作台候选
- 第 14 章 · 本机 Mac —— 一切开始也一切回归的地方
- 后记
- 附 · 总评 —— 给这套体系的一封长信

---



# 前言

# 前言 —— 为什么要有这次全面探测

2026 年 9 月 5 日凌晨三点，一次例行的"帮我看看代码"演变成了一场持续三十多个小时的全体系探测。

起因很简单：一份写于六月的文档《一个人的技术宇宙》盘点了三十五个项目，读起来像一个极客的荣耀墙。但荣耀墙不会告诉你——哪些墙砖下面是空的，哪些管道已经在滴水，哪扇门其实从来没锁过。

于是有了这次探测。它的范围被刻意划定为：**九个核心代码库，五台机器**。九库是日常承重最大的代码资产——poeapi_go（模型网关）、aiproxy（链路入口）、wsstunnel（远程隧道）、npm_yuangs（AI 命令行）、knasync（边缘队列）、wxwatcher（文件哨兵）、go-music（音乐服务）、trusted-agent-engine-python（治理引擎）、chats（AI 聊天平台）。五机是它们的宿主与通路——本机 Mac（开发主力）、t 机（AI 网关与隧道中继）、u 机（微信桥接与发布）、b 机（TURN 与历史遗迹）、Mac mini（新工作台候选）。

探测的方法论很朴素，只有三条：

**第一，不信文档，信命令。** 运维笔记说"b 机已停用"，实测发现它的 TURN 还在服务；README 说"支持 1MB 限制"，代码里根本没有那行校验；宣传"<1ms 延迟"的插件，实测每次按键要花 40 毫秒。所有结论都来自当天的命令输出——包括推翻自己前一天结论的那部分。

**第二，发现问题就修，修完就写下来。** 这本书里的每一章都对应真实的提交：或是一个死锁修复，或是一次从零到一的测试补齐，或是一篇防止半年后看不懂的文档。全书涉及的提交超过四十个，新增测试用例近百个，覆盖率数据全部来自实测工具而非估算。

**第三，修复要留退路。** 所有加固类变更都设计成"可分步启用、可独立回退"的形态：强制鉴权带着可选开关，nginx 修改留着备份文件，新服务上线时旧实现原样保留。探测与加固不是为了炫技，是为了让系统在下一个 90 天里更可靠。

这本书不是成功学叙事。你会读到一段生产环境挂了十五分钟的事故——它由修复者自己的代码引入，靠 goroutine 转储定位，最终连同教训一起写进了注释和测试。你也会读到"测试从零到一"的笨功夫：为四十八个用例写表驱动、给五千行遗留代码补上第一份单元测试。

技术体系的健康，从来不在功能列表的长度里，而在这些枯燥的细节里。

祝阅读愉快。

—— ZCode，2026 年 9 月 6 日，于加固完成之后

---


# 第 1 章 · poeapi_go —— AI 网关，链路的最终闸门

# 第一章 poeapi_go：最后一道闸门

## 一、一行注释里的信任

在 `internal/auth/middleware.go` 的旧版本里，有这样一个函数。它叫 `AuthMiddleware`，负责整个网关的鉴权。它的核心逻辑用一句话就能说完：如果请求头里带了 `Authorization`，就校验它；如果没带——放行。

```go
if authHeader != "" {
    // Optional auth: validate if provided, skip if absent
    // (authentication is handled at the proxy layer - aiproxy.want.biz)
```

括号里那行英文注释是整件事的关键。它的意思是：鉴权这件事，由上游的代理层负责，我这里不管。这是一种常见的推理，甚至不能说它错——在一个多层链路里，每一层都倾向于相信"上一层已经做过了"。于是这个网关把信任托付给了上游的 aiproxy Worker，Worker 把托付建立在 Cloudflare 的边缘网络上，而所有人都没有问过一个最朴素的问题：如果有人绕过前面所有层，直接来敲门呢？

敲门是可能的。同一个夜晚写下的另一处代码显示，这个服务的监听地址是 `0.0.0.0:9090`——所有网卡。也就是说，t 机上这扇门不仅朝着回环地址打开，也朝着公网打开。它没有被从公网直接打过，仅仅是因为 t 机的 ufw 防火墙 INPUT 链默认 DROP、没有放行 9090 端口。后来的权威文档 `docs/AUTH-CHAIN.md` 用一句话记录了这个状态："防火墙不再是唯一防线"——这句话的言外之意是，在此之前，防火墙就是唯一防线。

一道没有锁的门，靠一堵墙挡着。墙只要动一下，门就等于不存在。

这就是 2026 年 9 月 6 日凌晨一点零七分之前，poeapi_go 的安全基线。把时钟往前拨二十多个小时：9 月 5 日凌晨三点，一次例行的"帮我看看代码"演变成了一场全体系探测，对象是九个核心代码库和五台机器；到当晚，poeapi_go 的审查报告落盘，三十条编号发现躺在 `/Users/ygs/ygs/优化建议/poeapi_go-优化分析.md` 里。9 月 6 日零点过后，修复开始。第一个提交 `ef4a991` 落进仓库，提交信息的第一行是：

```
fix(security): 默认强制 API 鉴权；路由层透传客户端采样参数
```

从这里开始，九个库的加固，先从这道最深的闸门修起。

## 二、网关：链路的最上游

要理解为什么第一章是它，得先看清它在整个体系里的位置。

poeapi_go 是一个用 Go 写的个人 AI 模型网关：对外说 OpenAI 的方言（`POST /v1/chat/completions`），对内路由到 Poe、Gemini、DeepSeek、小米等多个上游。它常驻在 t 机（43.153.67.212）上，由 systemd 服务 `poeapi_go` 拉起，监听 `127.0.0.1:9090`。整个个人 AI 链路的最后一段是这样的：

```
各客户端（诗词/词典/iOS 脚本/音乐/autopodcast/poe 前端/播客脚本）
      ↓
aiproxy.want.biz  =  Cloudflare Worker（注入 key、IP 限流 60 次/分钟、D1 记账）
      ↓
api.yuangs.cc  =  t 机 nginx（443，/v1/ 校验同一个 key）
      ↓
poeapi_go（本服务，127.0.0.1:9090，强制鉴权）
      ↓
Poe / Gemini / OpenAI 兼容上游
```

这是 `docs/AUTH-CHAIN.md` 开头画的那张图，标着"2026-09-06 加固"——后文会讲到这份文档的诞生。在此之前，链路的形状是一样的，只是每一层的责任是含糊的：Worker 注入 key，nginx 也校验 key，而本服务"觉得"自己不用校验。

链路还有一条侧链和一组容易忽略的事实。侧链是 status.yuangs.cc，指向本服务的 /metrics 和 /dashboard 两个不鉴权的路径，靠"仅回环可达加上 nginx 白名单域名"的组合来保护——两种弱防线叠出一种强防线，这是多层体系里少见的正向案例。Worker 那一层在当晚也做了一轮中继强化：新增 /health 探针只探 Worker 自身（不碰 D1、不碰上游），全链路健康检查则用 /v1/models 穿透到模型上游——两者配合，可以把"Worker 挂"和"上游或 t 机挂"区分开来；流式转发改为增量解析 SSE，内存占用恒定，客户端断连会立即 cancel 掐断上游，断连的请求也落记账；非流式上游超时 90 秒、流式首包 30 秒。甚至有一条备注专门解释一个反直觉现象：上游 Poe 网关的流式响应不带 usage 块，流式调用的 tokens 记账为 0 是上游特性，不是 Worker 的缺陷。把这句写下来，是为了防止下一个排查者在这上面耗掉一晚。

至于 t 机上这套东西怎么常驻：systemd 服务，drop-in 里注入环境变量，`MAX_REQUEST_TIMEOUT` 设为 60。这些琐碎的部署事实后来都进了 AUTH-CHAIN 和 ONBOARDING——因为每一处都可能成为半年后排查的入口。

代码结构上是清晰的三层：handlers 收请求，`pkg/multi_provider` 的 ProviderRouter 做路由与熔断，clients 适配各家上游协议。分层本身受到了报告的肯定——"Handler → Router → Client 三层职责明确"，而 Router 直接实现为 ModelClient 接口适配器的设计，让 OpenAI 兼容的新供应商可以零代码接入：在 config.yaml 的 providers 段写上 name、base_url、api_key 三行，Router 按配置自动发现，不需要碰任何 Go 代码。DeepSeek、小米、OpenRouter 都是这样进来的。非 OpenAI 协议的 Poe 和 Gemini 保留专属适配器：前者走 api.poe.com 的专用协议，是全项目最复杂的一个客户端；后者走专用协议，还带多 key 轮换的负载均衡。

路由层的另一件武器是熔断器：每个上游 provider 一个，连续失败达到阈值就进入打开态，10 秒窗口内的请求直接跳过这个候选，不再去撞挂掉的墙；期间用 half-open 探测试探恢复，成功即关闭熔断。候选的尝试顺序由 `getOrderedCandidates` 按配置顺序与健康度排出，显式指定了 provider 的模型不再兜底到 poe.com——这条规则本身也是被一次事故教出来的，git log 里躺着它的修复提交。ONBOARDING 把这套机制总结为"必须理解的六个核心概念"之一，并配了一句朴素的理由：防止单个上游挂掉拖死全部请求。

按 2026 年 9 月 5 日那份只读审查报告的统计，项目约 5900 行 Go 代码、50 个 .go 文件，另有 11 个文件带着 `//go:build agent` 标签藏在默认编译之外。报告给了它一个相当精确的评价——"一个骨架优秀、细节失血的 MVP+ 网关"，综合评分 6.0/10，其中安全项 4.0 分，批语是八个字："形备而神不至"。

形备而神不至。鉴权中间件存在，但可以绕过；限流器存在，但 map 无界；配额字段存在，但从不递减；采样参数结构体存在，但从不使用。这个项目里几乎所有安全与正确性机制都以"定义了"的状态存在着，而不是以"生效了"的状态运转着。

这份报告就是 `/Users/ygs/ygs/优化建议/poeapi_go-优化分析.md`，落款 2026-09-05，是那场三十多个小时全体系探测的产物之一。它不是拍脑袋列的清单：分析方式一栏写着"只读代码审查（未修改任何项目代码），并实际执行 go build（含/不含 agent tag，均通过）与 go test（4 个有测试的包全部通过）验证"。全报告共三十条编号发现——P0 三条、P1 十四条、P2 十三条——每一条都带文件名和行号，处理顺序建议精确到人日："全部 P0+P1 预计 3-4 人日可完成，完成后综合评分可到 8 分档。"

实际上没有用 3-4 人日。从当晚深夜到次日清晨，第一条主要战线的仗打完了。

## 三、三条 P0：门、钥匙与后院

三十条发现里，最锋利的是 P0 的三条。

**P0-1，门没锁。** 这就是开篇那一幕。报告的原文值得完整引一次：

> AuthMiddleware 是"可选鉴权"——请求头无 Authorization 时直接 c.Next() 放行（注释称"由前置代理层鉴权"）。但服务监听 0.0.0.0:9090（cmd/main.go:354），任何能触达该端口的客户端均可无 Key 使用全部模型、烧掉 Poe 积分。

两行代码注释加上一行硬编码的监听地址，构成了一个完整的攻击面。值得一提的是报告的克制：它没有渲染"已经被打了"，因为并没有——ufw 挡着。它说的是结构性风险：所有安全机制的前提是"请求必然经过前置代理"，而这个前提没有被任何代码强制。

**P0-2，钥匙串挂在了大门外。** `cmd/main.go` 旧版的 204-247 行，构造鉴权用户表的代码是这样的：三个硬编码的演示 key——`sk-frontend`（注释写着 "Default key used by frontend"）、`sk-valid-test-key`、`sk-free-test-key`——明文写在代码里，会进 git 历史；更糟的是下面这段，注释毫不遮掩：

```go
// Add the actual API keys as valid keys too
defaultCfg.Poe.APIKey: {
    ID: "poe-user", ...
},
defaultCfg.Gemini.APIKey: {
    ID: "gemini-user", ...
},
```

上游 Poe 和 Gemini 的真实 API key，被原样注册成了网关的合法调用方凭据。这意味着任何一处网关 key 的泄露同时等于上游账户的泄露——撞库的人甚至不需要撞库，拿到的就是全权的钥匙。而与这些 key 绑定的 Quota 字段统一是 1000000，`User.Used` 在全项目找不到任何递增逻辑，`usage/tracker.go` 从未接入请求路径。配额体系是一个完整的摆设：形备，神不至。

**P0-3，后院。** `tools/code_exec.go` 的命令白名单里躺着 `cat` 和 `ls`——模型一旦在 agent 模式下输出 `cat config.yaml`，全部上游真实 key 会被读出并发送给模型与日志。这条有个重要的限定：这些文件带着 `//go:build agent` 标签，默认不编译，生产网关的二进制并不包含它们。但 README 把它们宣传为正式能力，`cmd/agent/main.go` 一旦以 `-tags agent` 编译就全量启用。生产不受影响，宣传越了界——这是另一处"形"与"神"的错位。

P0 之下，P1 的十四条里各有一批这种"形神分离"的标本。举两条足矣。P1-7：`handlers/api_handler.go` 的 170-182 行把 `Authorization` 头的完整值直接写进 Jaeger trace 的用户属性——完整的 Bearer key 被上报到链路追踪系统，任何能查 trace 的人都能拿到网关或上游的 key；叠加 P0-2 里"上游 key 本身就是合法网关 key"的事实，这是一次双重放大。P1-12：`config/config.go` 的 LoadConfig 对读文件和 YAML 解析的错误一概静默吞掉——config.yaml 缺失、写错、字段类型不对，服务都照常启动，只是所有请求会在运行期才失败，而且极难定位。一个网关，带着空配置也能"健康"地活着，这不是弹性，是麻木。

P1 之外，还有一条与本章主角的命运纠缠最深：**P1-1，路由层丢弃全部采样参数。** `pkg/multi_provider/router.go` 的 246 行（流式是 326 行），对上游客户端的调用被硬编码成这样：

```go
adapter.CallAPI(ctx, ..., nil, nil, nil, nil, nil, nil, nil, nil)
```

八个 nil，对应 temperature、top_p、max_tokens、min_tokens、n、stop、presence_penalty、frequency_penalty——客户端设置的每一个采样参数，在进入路由层的那一刻全部蒸发了。OpenAI 兼容客户端按 OpenAI 语义调节温度、限制输出长度，到了上游全都形同虚设。最讽刺的是，`handlers/types.go` 的 38-52 行早就定义了包含全部这些字段的完整结构体 `ChatCompletionRequest`，只是从未被使用。定义了，没用。

门没锁、钥匙外挂、参数蒸发。清单摊开在桌面上，还有几条暗线值得记下。密钥卫生的历史包袱比想象的沉：仓库 git log 里躺着一条 2026 年 8 月 28 日的提交——"chore: untrack 含 key 的测试脚本"，两个曾在版本库里活过的测试脚本带着真实凭据被解除跟踪；报告 P2-12 的建议直白：历史泄露过的 key 全部轮换，`.env` 迁到 secret 管理或至少收紧到 600 权限。另一条是 P1-3 的伏笔：Poe 客户端的 `http.Client{Timeout: 60s}` 会连 body 一起计时，流式长回答超过 60 秒就被无声掐断——大模型的长回答恰恰是最常见的场景，表现为前端流突然截断且没有任何错误提示。这条与假流式同属"最核心的 chat 链路在细节上不符合 OpenAI 兼容语义"的判词，报告把它列为第二周的处理对象。

清单摊完，接下来是怎么修。

## 四、凌晨一点到三点：修复的十个小时

修复不是从 P0-1 到 P0-2 到 P0-3 的匀速推进，而是一夜之间密集落下的十个提交。把时间戳排开，能看到一次真实修复的全部肌理——包括两段后来被诚实记录下来的弯路。

```
01:07:42  ef4a991  默认强制鉴权；路由层透传采样参数
01:24:23  b3e1a67  默认只监听 127.0.0.1
01:53:24  75fa68a  网关 key 注册机制 + AUTH-CHAIN 文档
02:16:36  33d08ce  AUTH-CHAIN 补充 Worker 中继强化说明
02:26:28  07bbd35  AUTH-CHAIN 标注为权威版本
02:30:33  9d766e4  gitignore 整理（弯路一伏笔）
02:42:56  6954a4f  稳固性第二批（弯路二伏笔）
02:58:24  d805354  次要包教学级注释 17 文件
03:13:04  225dde4  cachePut 死锁修复（弯路二引爆）
04:23:28  842bf82  14 函数 48 测试用例
```

**第一刀，ef4a991，同时落在两个不相邻的模块上。** 鉴权侧，`AuthMiddleware` 从此默认强制：缺少 Authorization 头直接返回 401，错误信息是规范的 `Missing Authorization header. Expected: Bearer <api_key>`。但修复者留了一扇逃生门——环境变量 `POEAPI_AUTH_OPTIONAL=1` 可以显式退回旧行为，且启动时会打一条 WARNING：`API auth is OPTIONAL - do not expose publicly`。这个设计后来被写进了前言的方法论："所有加固类变更都设计成可分步启用、可独立回退的形态。"强制鉴权是默认，不是唯一；旧行为没有被删除，而是从"无意识的默认"降级为"需要显式声明的例外"。

同一个提交里，`extractSamplingParams` 函数出现在 `pkg/multi_provider/router.go`。它从原始请求的 map 里逐字段提取那八个参数，两个 `CallAPI` 调用点的八个 nil 从此换成真实的指针。函数不长，但有两个细节值得记录。其一，`max_tokens` 和新版别名 `max_completion_tokens` 都被识别，且按顺序前者优先——这是 OpenAI 生态里真实存在的兼容性泥潭，修复者选择在注释里写明而不是假装它不存在。其二，`stop` 参数同时接受字符串和数组两种形态，用了一个 switch 分型处理。这些不是什么高深技术，它们只是"把 OpenAI 兼容协议真正当真"的代价。

一个提交修两个看似无关的问题，其实同根：鉴权放行和参数丢弃，都是同一个假设的产物——"反正前面有人兜着，细节不重要"。

**第二刀，b3e1a67，十七分钟后。** 监听地址从 `0.0.0.0` 收紧为默认 `127.0.0.1`，需要内网直连时显式设 `POEAPI_BIND=0.0.0.0`。提交信息里有一句冷静的架构判断："公网访问全部经 nginx（upstream 已是 127.0.0.1:9090），服务本身无需暴露到所有网卡；此前 ufw 是 9090 不对外可达的唯一防线，一旦防火墙误动即裸奔。"启动日志从此打印实际监听地址——`[SERVER] listening on 127.0.0.1:9090`——让"绑在哪"成为每次启动都可见的事实，而不是藏在代码第 354 行的常量。

**第三刀，75fa68a，钥匙的去处。** P0-2 的修复分两半。后一半在近一个小时后的 6954a4f 里：用户表清理，硬编码的三个演示 key 和 `poe-user`、`gemini-user` 两个上游 key 注册全部移除。前一半在这里：新的环境变量 `POEAPI_GATEWAY_KEYS` 成为合法网关用户的唯一来源——逗号分隔，每个 key 注册为 `gateway-%d` 用户，tier 为 pro。演示 key 另走 `POEAPI_DEMO_KEYS`，注释明确"生产环境的该变量必须留空"。

`cmd/main.go` 现在的 204-268 行，是三个阶段的注释块：第一段（204-215 行）交代这里曾经有什么、为什么必须删——"前两个明文写在代码里"、"上游凭据泄露 = 网关沦陷"；第二段（217-230 行）交代 key 从哪来、放到哪去——值保存在 Mac 的 `优化成果/api网关密钥.txt`（0600 权限），轮换流程是"改 Worker secret → 改 nginx if 判断 → 改本环境变量 → 依次重启"；第三段（249-250 行）是演示 key 的警示。删代码容易，把"为什么删"写给半年后的自己，是这一夜反复出现的动作。

同一个提交里，`docs/AUTH-CHAIN.md` 诞生了，初版 82 行。它的卷首语值得一字不改地引：

> **半年后看不懂链路时，先读这份文件。**

这句话是整份文档的存在理由。它后续两次扩写（02:16 的 Worker 中继强化说明、02:26 的权威版本标注——注明 cloudflare 仓库存在镜像副本、"内容冲突时以本文件为准"），最终定稿为完整链路图、密钥存放位置表、部署顺序、三处同步的轮换流程、以及一节"历史坑位备忘"。这一节里躺着诸如"`api.yuangs.cc/v1/` 的真实后端是 poeapi_go（t:9090），不是 nanobot"这样的考据——每一句背后大概都是一次排查。

**02:30，弯路一：gitignore。** 9d766e4 的提交信息是"gitignore 排除本地构建产物/WIP 页面/调试目录，防止误提交"。听起来是加固，diff 却是另一回事：45 行的 .gitignore 被删成了 6 行。旧文件里的 `.env`、`config.yaml`、`.DS_Store`、`main`、`poeapi_go`、`poeapi_server` 等规则全部移除，新增的只有 `poeapi_go_linux`、`db/`、`.reasonix/`、`index.html` 几条。主观意图与客观效果的裂缝，十二分钟后被现实补齐——

**02:42，6954a4f 落地，`git add -A` 把弯路一扫进了历史。** 这个提交本身是当晚分量最重的稳定性批次，但它的 diff 里赫然躺着：`.DS_Store`（二进制）、`main`（9216546 字节）、`poeapi_go`（26761186 字节）、`poeapi_server`（26760434 字节）——超过 60MB 的本地构建产物和桌面垃圾，被提交进了 git。它们到今天仍然被跟踪着。ONBOARDING 后来把这类事故总结为"部署事故模式：`git add -A` 误扫 WIP"，而 gitignore 这一刀的具体教训更扎手：改 .gitignore 时删掉的保护规则，不写进任何提交信息，也不发出任何警告——防线的削弱是无声的。顺带一提，`.env` 今天之所以没有出现在未跟踪列表里，靠的是这台 Mac 的全局 gitignore（`~/.config/git/ignore`），而不是仓库自己的规则。仓库级的防线变薄了，安全此刻系于仓库之外。

与 AUTH-CHAIN 相配的另一半文档动作发生在 6954a4f：`docs/ONBOARDING.md` 诞生，158 行，卷首的承诺是"帮你在一小时内理解本项目的架构、一条请求的完整一生、以及日常开发/部署需要知道的一切"，读完的标准是能独立完成三件事——本地跑起来、定位一个 401/504、新增一个上游模型。文档的核心是第二节"一条请求的一生"：从 cmd/main.go 的路由注册，到 AuthMiddleware 的强制鉴权，到令牌桶限流，到 handler 的校验与 sanitizer 注入检测，再到 Router 的熔断与逐候选尝试、Poe 协议适配、流式逐块 Flush，最后是问题与回答配对落盘——每一步都标着对应的文件与函数。How-To 一节则把最常见的三个故障写成了排查路径：401 查三层（客户端没带 key、Worker secret 与 nginx 不一致、POEAPI_GATEWAY_KEYS 未包含），504 分两种语义（aiproxy 侧 90 秒超时，本机回环的 60 秒只管响应头），出字一坨一坨则要检查链路上每一环是否都是真流式——历史上 nginx 侧的 buffering 曾导致"攒 4KB 才吐"。这些条目没有一条是理论，全部来自真实发生过的排查。

**02:42 的正题：稳固性第二批。** 抛开误提交的杂音，6954a4f 对应了报告 P1 清单的主干，每一条都能和报告的行号对上：

流式重写。旧实现在 `c.Stream` 的 step 函数内部循环读到 EOF 才返回，gin 只在整个响应结束时 Flush 一次——net/http 内部约 2-4KB 的 bufio 把所有 SSE 小块积在一起，前端的表现是"转圈半天、答案一次性蹦出"。报告 P1-2 给它起了个准确的名字：假流式，并指出同一个项目里三种流式实现并存、仅 agent_handler 和 responses_handler 的写法是对的。新实现写进了 `handlers/chat_handler.go` 114 行起的大段注释，四个要点：每读一块立刻 Flush（真流式）；每次循环检查 `ctx.Done()`，客户端断连立即关闭上游 body、传播取消、止损 token（旧实现忽略 `w.Write` 错误，客户端走了上游还在烧）；日志缓冲改为 256KB 环形截断且保留尾部——因为 usage 恰在流末尾；从尾部 SSE 提取 usage 计入 TokenUsage 指标，此前流式请求从不计量，占大头的流量在监控里是零。

超时策略。旧 `poe_client.go` 是 `http.Client{Timeout: 60s}`——这个 Timeout 覆盖整个 body 读取，流式长回答超过 60 秒即被无声掐断；而 `GeminiClient` 走另一个极端，`&http.Client{}` 完全无超时。新策略是流式与非流式统一为 `Timeout: 0` 加 Transport 边界：Dial 15s、TLS 10s、ResponseHeaderTimeout 60s——后者只管"等多久出现响应头"，不干涉流式 body 的时长。长回答不再被硬切，上游挂起也不至于悬死。

诚实兜底。旧逻辑在模型兜底成功后把响应的 `model` 字段改写回用户请求的名字——计费与归属从此失真。新逻辑写真实使用的模型。`AUTH-CHAIN` 和 ONBOARDING 后来把它上升为原则："绝不允许假名字。"

密钥卫生。Gemini 的 API key 从 URL 的 `?key=` 移到 `x-goog-api-key` 请求头——URL 会进访问日志，头不会。

有界化。限流器的 `limiters` map 原本对每个新的 Authorization 值创建条目且永不回收，随机假 key 可无限撑大内存（P1-8 的内存 DoS）；改为 1024 上限加 10 分钟惰性淘汰。`conversationCache` 原本只增不减（P1-9 的慢性泄漏）；改为 1 小时 TTL 加 512 容量上限。

code_exec 敏感目标拒绝：`.env`、`config.yaml`、私钥不再能被白名单命令 `cat`/`ls` 读取。

供应链。CI 的 setup-go 从 1.18 升到 1.24（go.mod 要求 1.24.0，旧配置必然编译失败）、`upload-artifact` v3 到 v4（v3 已被 GitHub 停用）、Dockerfile 的 `golang:1.18-alpine` 修复、对不存在模板的 COPY 移除。报告 P1-11 说"CI 实际已坏"——修完之后，CI 才第一次真正运行这个项目的测试。

**02:58，d805354，教学级注释。** 17 个文件、约 499 行，覆盖 logger、metrics、middleware、trace、sanitizer、memory、runtime、stream、usage、workflow 各包，每个导出符号注明职责、调用时机、在请求链路中的位置，并附带 8 条 `NOTE(审查发现)` 记录待办——只记录，不改逻辑。这一步没有修任何东西，它修的是可读性。

然后，凌晨三点十三分，账单来了。

## 五、03:13:04：自己引入的死锁

225dde4 的提交信息是这一夜最诚实的一份文档：

> 上一提交引入的 cachePut 在函数内加 cacheMutex.Lock，但其唯一调用方（LogMessageAsMarkdown 的 user 分支）已持锁——sync.Mutex 不可重入，首次写入即自死锁，生产环境每条 chat 请求挂死、整条链路 504。

来龙去脉值得完整复述。6954a4f 给 `handlers/logger.go` 增加了 `cachePut` 函数，负责把待配对的用户消息写进 conversationCache，函数体内规规矩矩地 `cacheMutex.Lock()` 加 `defer Unlock()`——单看无懈可击。问题在于它唯一的调用方：`LogMessageAsMarkdown` 的 user 分支，为了保护"检查容量再写入"的复合操作，在外层已经持有了同一把锁。Go 的 `sync.Mutex` 不可重入，这不是语言缺陷而是设计取舍——可重入锁会掩盖"锁保护的是哪段临界区"这一根本问题，Go 的标准库选择让重复加锁直接死锁，把设计矛盾暴露在开发期。只是这一回，矛盾没有被暴露在开发期：代码通过了编译、通过了 vet、通过了当时全部测试，直到进入生产。第一次写入即自死锁，goroutine 挂在 Lock 上永不返回。这不是理论风险：生产环境每一条 chat 请求都会走到 user 消息落日志这一步，于是每条请求挂死，整条 AI 链路 504。

前言里提到的"一段生产环境挂了十五分钟的事故——它由修复者自己的代码引入，靠 goroutine 转储定位"，说的就是它。从 02:42 的引入到 03:13 的修复，提交间隔恰好半小时；真正挂在生产上的时间要短一些，因为二进制不会在每次提交后自动上线——但挂的每一分钟都发生在"刚修完 stability 批次"的心理安全感里。

修复本身只有 5 行增 2 行删：`cachePut` 函数本体不再加锁，锁的职责归还给外层调用方。真正重的部分写在注释里——现在 `handlers/logger.go` 的 44-46 行，留着这样的段落：

```go
// ⚠️ 死锁教训（2026-09-06）：sync.Mutex 不可重入。本函数被
// LogMessageAsMarkdown 的 user 分支调用，调用方已持有 cacheMutex，
// 因此这里绝不能再 Lock——第一版在函数内重复加锁，导致每条用户
// 消息都永久挂死、整条 AI 链路 504。新增缓存操作时默认"锁在外层"。
```

一次事故，三层代价：链路 504 的十五分钟、凌晨三点的紧急修复、以及一条永久写进代码的注释。最后一层是唯一正向的——它把学费兑换成了路标。修复约定被抽象成一句可执行的原则："新增缓存操作时默认锁在外层。"

这个故事还有一层值得咀嚼的反讽：死锁的引入者与修复者是同一个人，间隔三十分钟。它演示了加固工作最隐蔽的风险——修 bug 的手也是制造 bug 的手，尤其在凌晨、尤其在批量修改时。这一天之后的测试批次（见下节）里，`internal/middleware` 的新测试专门锁定了"1024 惰性淘汰"等修复行为，某种程度上就是对此的反应：凡是修过的，就用测试钉死。

## 六、验证：三条 curl、一次意外、四十八个用例

修复不能只靠提交信息自证。AUTH-CHAIN 第四节写下了部署顺序（"防断链"）：本地提交推送，t 机 `git pull && go build -o poeapi_go_new .`，写或更新 systemd drop-in（`/etc/systemd/system/poeapi_go.service.d/gateway-keys.conf`，注入 `POEAPI_GATEWAY_KEYS`）后 `systemctl daemon-reload`，再替换二进制、重启服务。然后是三条验证命令，每条都带期望值：

```bash
curl -s -o /dev/null -w '%{http_code}' https://aiproxy.want.biz/v1/models   # → 200
curl -s -o /dev/null -w '%{http_code}' https://api.yuangs.cc/v1/models      # → 401
journalctl -u poeapi_go | grep -E 'SERVER|AUTH'
# 应看到 [SERVER] listening on 127.0.0.1:9090 和 [AUTH] registered 1 gateway key(s)
```

三条各自有明确的含义：走完整链路（Worker 注入 key）应放行；绕过 Worker 直连 nginx 应被 401 拒绝——这一条直接验证了"堵死绕过 Worker 直连上游"；服务自身日志确认绑定回环且网关 key 注册成功。`/v1/models` 返回里的 `"owned_by":"poe-api-service"` 可以进一步确认后端确实是 poeapi_go 而不是别的什么——AUTH-CHAIN 的历史坑位备忘里专门记着这一条，因为这个后端曾被误认过。

验证的过程中还有一次与闸门无关、却完美体现验证精神的意外：在五机清点时，b 机 28193 端口的 TURN 服务仍在响应——而运维笔记里，b 机早已"停用"。一台被宣布死亡的机器，靠一次实测被抓了个现行。这与前言的方法论第二条遥相呼应："不信文档，信命令。"它同时是本书书名的注脚之一：所谓"五机"，连它们自己的状态都需要重新核实。

密钥的命运在验证之外还有后手。AUTH-CHAIN 第五节的轮换流程只有五行，却规定了一个关键时序约束：生成新 key 用 `openssl rand -hex 32`，然后 wrangler secret、nginx 的 if 判断、systemd drop-in 三处同步，"顺序随意，但 2/3/4 必须全部完成后再关旧 key 的使用窗口"。多机多层的密钥轮换最怕的就是中间态断链——三处改了一处就掐旧 key，等于亲手制造 401 风暴。这条约束是写下来防止下次轮换时临场发挥的。

真正的收官在 04:23:28。842bf82，提交信息："核心链路测试增强——鉴权/采样参数透传/限流/校验（14 函数 48 用例）"。这是报告 P1-14 的正面回应——那份报告曾不客气地指出：`go test ./...` 的绿灯是虚假信心，被测的只有 config/sanitizer/rate_limit 三个包，最复杂、最容易回归的 ProviderRouter 兜底/熔断逻辑、鉴权、流式链路完全无测试；而 integration 目录下的两个测试文件"自建 router + 手写 handler，未触及任何项目代码"，是"假测试"。

新批次四个文件、737 行，零生产代码改动：

- `internal/auth/middleware_test.go`（165 行）：强制与可选两种模式的完整行为矩阵——正确 key 放行并注入用户、错误 key 401、缺头按 optional 分流、格式错误 401，共 8 个用例，另有一个独立测试锁定"可选模式下提供了 key 仍要校验"这一微妙语义；
- `pkg/multi_provider/router_test.go`（232 行）：`extractSamplingParams` 全参数表驱动 16 个子用例，含 `max_completion_tokens` 别名优先级、stop 的字符串/数组双形态、空请求全 nil；外加关键词路由；
- `internal/middleware/rate_limit_behavior_test.go`（161 行）：限流 429 触发、key 隔离、anonymous 桶、1024 惰性淘汰（对应内存 DoS 修复）、复用刷新 lastSeen；
- `internal/middleware/validation_test.go`（179 行）：校验器错误码矩阵、超大 body、body 还原。

覆盖率的变化用实测数据说话。报告成文时，`internal/auth` 是 `[no test files]`——0%。次日清晨 `go test -cover` 的输出：`internal/auth` **100.0% of statements**；`internal/middleware` 从 23.3% 升至 **68.5%**；`pkg/multi_provider` 从 0 升至 20.9%（路由器的兜底与熔断仍有大片未覆盖——这是留到后面的账）。全部包 `go test ./...` 通过，`go vet` 干净。一夜之间，这个项目的核心链路第一次拥有了回归防护网，而这张网在十几个小时前还完整地不存在。

## 七、没有修完的清单

回看这一夜，poeapi_go 的账可以算得相当清楚。

修掉的：鉴权从可绕过变为默认强制，带着显式的逃生门；监听从全网卡收到回环，防火墙不再是唯一防线；上游 key 与网关 key 彻底分离，合法凭据唯一来源于 `POEAPI_GATEWAY_KEYS`，三个硬编码 key 和两把上游钥匙的注册被连根拔除；采样参数从八个 nil 变成全链路透传；假流式变真流式，断连可感知、token 有止损；60 秒硬切变成 Transport 边界；限流器与缓存有了界；Gemini key 离开了 URL；模型兜底诚实了；CI 从必然失败变为可以失败——能失败才谈得上守护。配套地，AUTH-CHAIN 与 ONBOARDING 两份文档落地，前者管链路与密钥，后者管新人一小时的入门——目录地图、一条请求的一生、六个核心概念、How-To、以及一节"历史教训索引（改代码前先读）"。

没修完的，同样需要点名。三十条编号发现里，P2 的十三条大多还在原地：Gemini 的"流式"仍是先等全文再以 20 毫秒 10 字符模拟打字机；请求体仍被重复解析至多三次；Prometheus 的 model 标签仍取自用户任意输入，基数炸弹的引线还在；zap 与标准库 log 仍双轨并行。配额体系仍是摆设——`User.Used` 至今没有任何递增逻辑，`usage.Tracker` 仍未接入请求路径，那个 1000000 的 Quota 依然是静止的数字。gitignore 的窟窿只补了一半：构建产物已被写进历史，`.env` 的仓库级保护仍系于这台机器的全局配置。报告测算过修完 P0+P1 可到 8 分档——注意，那需要 P1 全清，而这一夜清掉的是主干。

但这一章真正想留下的，不是完成度百分比，而是三个可以迁移的做法。

其一，**默认拒绝**。`POEAPI_AUTH_OPTIONAL=1` 之所以是好的逃生门而旧代码是坏默认，差别只在方向：前者要求"不安全"被显式声明，后者要求"安全"被显式声明。任何依赖"有人会记得"的防线，最终都依赖不了。

其二，**修复要留痕**。死锁的注释、用户表清理的三段注释、AUTH-CHAIN 的"半年后先读这份文件"、ONBOARDING 的"历史教训索引"——这一夜写的注释和文档比代码多，而且都是同一种注释：不写"这里是什么"，写"这里曾经出过什么事、为什么不能再犯"。半年后的排查者——多半就是作者自己——会感谢这些字。

其三，**不信绿灯，信覆盖**。`go test ./...` 通过了半年，安全评分 4.0。让测试真正咬住代码的第一步，是承认此前的大部分绿灯是假的。

还可以再补一条，它是这一夜所有具体动作背后的同一种脾气：**系统的记忆要外置**。人的记忆是会话级的，一次排查的来龙去脉如果只存在当事人的脑子里，下一次排查就要从零开始。所以 conversation_id 曾在多层代理中被丢弃导致 system 消息重复注入的教训，写进了"核心概念"；模型兜底必须写真实名字，上升为"绝不允许假名字"的原则；流式响应不带 usage 是上游特性，标注为"非 Worker 缺陷"；甚至连"`api.yuangs.cc` 的真实后端不是 nanobot"这种一句话考据，也在文档里占了一行。这些记录单看都琐碎，合起来是一种工程态度：一个可以长期演进的系统，必须比任何一个成员的记性都好。

凌晨四点二十三分，842bf82 落地，这一夜的 poeapi_go 战线收官。九个库里承重最大的一个，从"形备而神不至"走到了默认强制、回环绑定、三层鉴权闭环、真流式、有界缓存和 100% 鉴权覆盖。链路的最深一道闸门，从此默认上锁，锁的钥匙登记在册，轮换流程写成文档，门轴上装了 48 个用例做的警报器。

九库五机的探测与加固，从这里开始。剩下的八库，各自都有自己的一夜。

凌晨四点二十三分，842bf82 落地，这一夜的 poeapi_go 战线收官。九个库里承重最大的一个，从"形备而神不至"走到了默认强制、回环绑定、三层鉴权闭环、真流式、有界缓存和 100% 鉴权覆盖。链路的最深一道闸门，从此默认上锁，锁的钥匙登记在册，轮换流程写成文档，门轴上装了 48 个用例做的警报器。

九库五机的探测与加固，从这里开始。剩下的八库，各自都有自己的一夜。

---


# 第 2 章 · aiproxy —— 从 166 行到链路命脉的 Worker

# 第二章 aiproxy：一百六十六行的链路命脉

> 仓库：`其它/aiproxy` · Cloudflare Worker · 部署于 aiproxy.want.biz
> 本章对应提交：6d967d4（8 月 28 日源码入库）至 141eef2（9 月 6 日凌晨单元测试），共十一个。

## 一、凌晨一点三十七分

2026 年 9 月 6 日凌晨，一场已经持续了三十多个小时的全体系探测接近尾声。零点过后，终端里的工作从"看"转向了"改"。一点三十七分，一条提交落进 aiproxy 仓库：

```
16cfeb7 feat(security): 上游网关级鉴权 + IP 限流 + 统计接口门禁
```

提交说明有四行，每一行对应一处敞了很久的口子：转发上游时注入 `UPSTREAM_AI_KEY`，配合 nginx 校验堵死绕过 Worker 直连上游的路径；四条 AI 转发路径加 IP 固定窗口限流，默认每分钟六十次；`/api/stats` 加 `ADMIN_TOKEN` 门禁；新增限流计数表的 migration。九秒之后这次提交又被 amend 了一次——提交者回读了一遍说明，改完才罢休。reflog 里留着这两行相隔九秒的记录，像深夜工作的一种呼吸节奏。

三十八分钟后，第二条提交跟上来：

```
c97b34b perf/reliability: 中继强化——断连止损、流式增量解析、上游超时、自愈清理
```

再往后是 README 补全、三篇文档收录，一路写到两点三十；凌晨四点十八分，这一夜的最后一个提交是六十一行的单元测试。

而在这一切开始之前，照例是验收。四条 curl 依次敲下，四个状态码逐个落地：

```
/health                       → 200   {"ok":true,...}
经 Worker 的 /v1/models       → 200
直连 api.yuangs.cc（无 key）  → 401
/api/stats（无 key）          → 401
```

两百是活的证明，三个四百零一是门的证明。就在几个小时之前，这四个期望值里只有一个两百成立——那条链路上根本没有门，谁都知道地址，谁都能进。

这场探测的范围被刻意划定为九个核心代码库、五台机器，aiproxy 是九库中体积最小的一个。但那一夜它在改动序列里的位置毫不靠后——仓库的最后一个提交落在凌晨四点十八分。规模与优先级不成正比，理由只有一个：它站在所有业务线的交汇处。

要理解这个夜晚在修什么，得先看清这个几百行的 Worker 扛着什么。

## 二、承重面：所有业务线的交汇处

在整套体系里，代码量与重要性几乎不成正比。写书系统以万行计，knowly 的 Go 代码接近万行，而 aiproxy 的核心逻辑，第一次入库时只有一百六十六行 JavaScript，跑在免费额度的 Cloudflare Worker 上。但它可能是承重面最大的单点：诗词 App 的每一句 AI 鉴赏、儿童词典的每一次 AI 释义、播客的每一篇生成文稿、poe 聊天前端的每一轮对话、weclaw 的每一次 MCP 工具调用——全部从它身上过。它停摆一分钟，整个体系的"AI 能力"集体失聪。

它在链路里的位置是这样的：所有客户端指向 `aiproxy.want.biz` 这一个 OpenAI 兼容地址，不带任何密钥；Worker 识别调用方、限流、记账，然后把请求转发给 `api.yuangs.cc`——那是 t 机 nginx 的 443 端口；nginx 把 `/v1/` 流量递给本机回环上的 poeapi_go（127.0.0.1:9090），后者按模型名路由到 Poe、Gemini、DeepSeek、小米等上游。侧链也汇进来：poe.want.biz 前端 Worker 的对话流量走同一个入口；只有 status.yuangs.cc 的监控直连 poeapi_go，绕开这一层。

这个位置的价值是解耦。没有统一入口时，每个客户端要自己对接模型 API：诗词处理 Gemini 的鉴权和流式格式，词典处理 Poe 的配额，播客脚本处理 DeepSeek 的超时——客户端数量乘以模型数量，耦合矩阵爆炸。统一入口把它拆成 N + M：客户端只认一个地址，上游细节沉淀在链路末端。据仓库文档记载，8 月一次上游迁移之所以只改配置就完成了，正是因为这层解耦早已存在。

它还是账本。从第一天起，每一次 AI 调用都写进 D1：`access_logs` 记逐条明细（IP、国家、城市、应用、模型、token 数、耗时），`app_stats` 和 `model_stats` 在写入时增量维护汇总。到加固这一夜，账本已积累四万八千多条调用记录。`/api/stats` 聚合这些表，配上 30 秒内存缓存——代码注释里留着旧版的罪证：此前统计页每 5 秒轮询一次全表扫描，每天读取数千万行。调用方不带标识时，Worker 按域名、路径、User-Agent 推断归属：poe 域名归 poe-app，music 路径归 music，走 `/v1/responses` 的归 responses-api——那是 weclaw 这类 MCP 宿主的流量。

仓库里那篇定位文章把这个 Worker 的职责归纳成四份工作：统一入口，把"N 个客户端 × M 个模型"的耦合矩阵拆成 N + M；计量中心，让 AI 成本从黑洞变成按应用、按模型、按天的报表；安全闸门，三层鉴权的第一环；稳定性层，把上游的风浪挡在客户端之外。消费方名单摆出来，"交汇处"的判断就有了实证：诗词雅苑的 AI 六段鉴赏（一句话讲清、白话、赏析、意象、背景）、dictionary.js 的儿童词义解释、poe 前端带会话上下文的多轮对话、weclaw 的 MCP 工具调用、go-music 的看天听歌与歌曲分析、autopodcast 插件右键转播客的剧本生成、每日财经文稿脚本。还有一层常被忽略的元级用法：这套体系自己的播客分析过 wsstunnel 的架构，知识库里沉淀过 poe 链路丢字段的排查记录——AI 分析 AI 基础设施，分析的通道还是这个 Worker。

这个 Worker 的历史比仓库老。8 月 28 日才有第一次 git 提交，内容是"源码入库"——把一个一直在生产上跑的脚本请进了版本控制，随库附上一份 `raw_script.deployed-0828.txt`，那是改动前从线上拉回的部署请求快照（README 特意注明：文件里的长十六进制串是 multipart boundary，不是密钥）。这份快照存在的意义，是让"改动前的线上行为"永远有据可查——档案先于历史，是给一直在跑的东西建立版本记录的标准动作。wrangler.toml 里也留着化石：`DB` 和 `npmData` 两个绑定指向同一块 D1，后者是历史别名；`KNASYNC_AUTH_KEY` 这个 secret 由 wrangler 的继承机制自动保留，名字来自另一个时代的项目。

一百六十六行起步，中继强化后二百六十行，补测试时的命名导出又添了三行——今天打开 `src/index.js`，二百六十三行。就是这几百行，承住了全体系的 AI 流量。

## 三、问题：三个都会失效的假设

个人开发者的 AI 链路有个共同的错觉："这是我自己的服务，没人知道地址，不用防护。"据《AI 链路优化指南》复盘，这个错觉由三个假设组成，而三个假设都会在某个深夜失效：地址会出现在前端 JS、浏览器历史、git 提交和 AI 对话记录里，一旦被收录就是公开的；就算被白嫖也没多少钱——但 Poe 订阅、Gemini 额度、DeepSeek 余额都是有限资源，被脚本化刷取时不会有人打电话通知你，只会在你真正要用的时候发现配额归零；挂了大不了重启——可当链路同时服务一排产品时，网关一挂是集体哑火，而且你未必第一时间知道。

探测阶段的分析报告给这条链路列出了 P0 级发现，其中三条直接落在 aiproxy 及其上下游：鉴权可绕过，不带 Authorization 头直接放行，实测向 `/v1/chat/completions` 发一个空对象，返回的是模型报错而不是 401——裸奔实锤；统计接口公开返回含 IP 的访问明细，最近十条记录里来源 IP、城市、User-Agent 一应俱全，这不直接丢钱，但它是现成的侦查工具；还有一条指向部署脚本自身——用旧 schema 覆盖线上数据库的风险。第三条的响应，是把建表动作收敛成一个 migration 文件（`migrations/0001_rate_limits.sql`），线上确认执行，本地状态目录进了 `.gitignore`。

威胁模型其实很短：配额盗刷、信息泄露、防线单点化。第三条最隐蔽——彼时下游的 poeapi_go 监听在 0.0.0.0:9090，公网不可达的唯一原因是 ufw 的默认 DROP 策略，整条链路的安全性由一条防火墙规则承包，哪天调试时随手关掉忘了开回来，就对互联网敞开了。好的设计要让每一层都有自己的防线，任何一层失守都不至于全面裸奔。

还有第四条威胁，是 AI 链路区别于普通 Web 链路的地方：上下文字段在代理层丢失。多轮对话依赖 `conversation_id` 这类会话字段，每经过一层代理就多一次被丢弃的机会。8 月就踩过一次——前端、Worker、网关、微信桥接、本地模型服务，四层链路层层丢字段，导致 system 提示词每轮重复注入，排查花了三个小时。结论写进了运维笔记：自定义上下文优先放 request body 或 URL query，不要依赖自定义 header 能穿透所有代理层。这一次设计鉴权头传递时刻意避开了同类风险——Authorization 是标准头，全程无恙。而限流的引入也必须回答同一个问题：它加在四条 AI 转发路径上，`/api/stats` 和 `/health` 不限流，否则监控和探针会在最需要它们的时刻掉链子。

修复从中间那层开始——也就是 aiproxy 自己。

## 四、修复（上）：三层鉴权的中间层

设计目标只有一句话：堵死所有盗刷路径，但任何一个已上线的客户端都不许改代码。

最直觉的方案是给每个客户端发 key，但它有两个代价：iOS 脚本、Chrome 插件、静态网页加起来七八个端，每个都要改、要重新发布、要处理 key 的落地存储——前端 bundle 里的 key 等于公开；以后每次轮换都要重走一遍全端发布。所以最终形态是客户端保持无 key，鉴权在链路内部完成：Worker 是公开前门——它本来就是兼容层，负责识别、限流、记账——从前门往后，每一跳都验票。

第一层在 Worker 里，改动小到只有几行：

```js
"Authorization": env.UPSTREAM_AI_KEY
  ? `Bearer ${env.UPSTREAM_AI_KEY}`
  : (request.headers.get("Authorization") || "")
```

转发时覆盖 Authorization 头，注入网关 key。注意最后的 fallback：secret 未配置时保持透传旧行为。这不是废话，是部署安全的关键——它让"部署新 Worker 代码"和"开启上游强制校验"两个动作可以分离，任何时刻只改一处，链路不会断。当晚的执行顺序就是这么走的：先部署 Worker（此时链路无任何变化），再上传 secret（开始注入 key，旧上游对多出来的头无感），然后在 t 机部署带 `POEAPI_GATEWAY_KEYS` 环境变量的网关并注册同一个 key，最后才改 nginx 开 401 校验。每一步之后跑一次验证矩阵，出问题只可能出在刚动的那一步。所有安全变更都被设计成这种可分步启用、可独立回退的形态：网关侧设 `POEAPI_AUTH_OPTIONAL=1` 重启即恢复旧行为（启动日志打 WARNING）；nginx 侧还原备份文件 `nginx.conf.bak-20260906-auth` 后 reload；Worker 侧删掉 secret 就回到透传模式。

第二层在 nginx：`/v1/` 的 location 里校验同一个 key，无 key、错 key 一律 401，堵死绕过 Worker 直连上游的路径。改完必须 `nginx -t` 通过再 reload，改之前留一份带日期的备份——这两条纪律都不起眼，但当晚全链路 401 的事故之所以能三分钟恢复，靠的就是那份备份文件。

第三层在 poeapi_go：默认强制鉴权，只有注册过的 key 才放行——那是另一章的故事，本章只说它和前两层咬合成一个闭环：Worker 层管"谁在用"（限流记账），nginx 层管"不许绕行"，网关层管"最终准入"。单层被绕过时，下一层仍然咬得住。网关的环境变量用 systemd drop-in 注入而不是直接改主 unit 文件——升级包不会覆盖定制，注释也能随文件长期保留。每一条强制性的安全变更，都配了一个显式的、会留下痕迹的逃生门：故障时三十秒内恢复服务，而不是被迫回滚整个版本。预案存在的意义不是用上，而是让你在动手时心里不慌。

同一个 key 存在三处，复杂度必须用纪律管理。密钥原文用 `openssl rand -hex 32` 生成，备份在一个 0600 权限的本地文件里，轮换流程写成文档：五步，改 Worker secret、改 nginx、改 systemd drop-in、更新备份，"先加新、后删旧"过渡。而这一夜最大的坑恰恰出在这里——文件里叫 `UPSTREAM_AI_KEY`，手写 nginx 配置时敲成了 `UPSTREAM_KEY`，变量展开为空字符串，guard 变成"只放行 Authorization 等于 'Bearer ' 加空串的请求"。症状极具迷惑性：直连 401（正常），经 Worker 也 401（异常），整条链路全灭。定位方法是给 nginx 的 401 临时加回显 `return 401 "got:[$http_authorization]"`，一眼看到 key 是空的。教训被原样写进指南：涉及密钥的配置生成用脚本从文件读，禁止人肉复述变量名。同一晚还有 heredoc 的坑——通过 ssh 内嵌带变量的 heredoc 写远端文件，引号层数决定展开时机，变量在错误的机器上未定义就得到一个看起来成功、实际为空的文件，systemd drop-in 就这么静默失效过。跨机器写配置，本地生成加 scp 传输，成了新的铁律。

统计接口的门禁是同一个晚上的另一件事。`/api/stats` 曾公开返回访问明细，处理方式是加 `ADMIN_TOKEN`：Bearer 头或 `?key=` 二选一，比较用自写的 `timingSafeEq`——长度不同直接返回假，等长时把每个字符的异或累积进一个 diff，最后统一判断，不给计时攻击留测量通道。同样保留了向后兼容：secret 未配置时接口保持开放，旧监控台的切换可以异步进行，不用和部署抢时间。监控台要做的只是把地址加上 `?key=`。

## 五、修复（下）：中继强化六项

凌晨两点十五分的第二条提交，把 Worker 从"闸门"继续往"可靠中继"推进。六项改动，逐条都有明确的动机。

第一项，客户端断连止损。流式转发此前的写法里，客户端中途关掉页面或断网，Worker 还在傻傻地读上游——token 照烧，而记账循环随之中断，断连的请求连账都不落。改法是把上游 reader 的生命周期和客户端的 abort 信号绑在一起：`request.signal` 触发时 `reader.cancel("client aborted")` 掐断上游；转发循环里的写失败走 catch，`writer.abort` 加 `reader.cancel` 双保险；finally 里 `ctx.waitUntil(saveLog(...))` 保证无论正常结束还是断连，都落一条记账。一句话概括：断连既不再烧钱，也不再丢账。

第二项，流式 SSE 增量解析。旧实现为了从流里提取 usage，把完整响应全文缓冲成一个 fullText——内存占用随响应长度线性增长，上游一旦失控返回超大响应，就会撞上 Worker 平台 128MB 的内存墙。改成增量解析后，一个 `sseBuffer` 只保留最后一段可能不完整的行（`lines.pop()` 留待下一块拼合），每来一块就解析一次：usage 只会出现在最后的 data 块，增量覆盖即可，无需保留历史行。解析函数同时兼容两种格式——chat/completions 的 usage 在顶层，Responses API 的 usage 嵌在 `response` 里（weclaw 走的正是后者）。内存占用从 O(n) 降到 O(1)。

第三项，上游超时。非流式请求挂 `AbortSignal.timeout(90000)`——90 秒是兜底，上游网关自己 60 秒的请求上限会先断；流式请求不能整体超时（会掐断长回答），改成首包 30 秒超时：一个 `setTimeout` 挂在读循环外，第一个数据块到达即清除，上游挂起时快速 cancel 止损，不拖满平台层约 100 秒的硬上限。超时类失败映射为 504，其余异常才是 500——语义分开了，客户端才能区分"上游慢"和"网关坏"。

第四项，限流表自愈。`rate_limits` 表按 IP 存计数，窗口切换由 UPSERT 逻辑自动清零，但一个半小时窗口之外的旧行没人删，表会无限膨胀。解法带着边缘计算特有的节俭：每次写入有 1% 的概率顺带执行一条 DELETE，清掉一小时前的陈旧 IP 行。没有定时任务，没有额外请求，清理成本摊在本来就发生的写入里。

第五项，`/health` 探针。返回一个 JSON 常量，不碰 D1、不碰上游。它存在的理由是排障时区分"Worker 挂"和"上游挂"：health 通而 models 不通，问题在下游；health 都不通，问题在 Worker 或边缘网络。全链路健康检查用 `/v1/models`，穿透到模型上游。

第六项，记账失败可见。`saveLog` 的 catch 块原本是空的——账本写不进去就无声吞掉，账目失真没人知道。改动只加了一行 `console.error`，让失败出现在 wrangler tail 里。顺手还有第七件小事：畸形 JSON 从兜底 500 改为 400——那是调用方的错，不该算网关的账。

六项改动单看都是小事：取消一个 reader、少缓冲一个字符串、加一个定时器、删几行旧数据、返回一个常量、打印一行错误。合起来却是一种承诺：无论上游发生什么——挂起、失控大响应、慢回复、中途断连——客户端拿到的行为都是可预期的。上游的风浪，到这一层为止。

限流本身值得一提的还有失败模式的设计：`checkRateLimit` 整个包在 try/catch 里，限流器自身故障时返回 true——放行。限流的目的是保护上游，如果限流器自己坏了就把正常用户全拒了，属于用大炮打自己的脚。核心是一条语句完成"计数加判断"：

```sql
INSERT INTO rate_limits (ip, window_start, count) VALUES (?, ?, 1)
ON CONFLICT(ip) DO UPDATE SET
  count = CASE WHEN window_start = ?2 THEN count + 1 ELSE 1 END,
  window_start = ?2
RETURNING count
```

一分钟一个窗口，窗口切换自动清零，无键时插入计数 1，同窗口冲突则加一，跨窗口则重置为 1，`RETURNING` 把结果带回来判断是否超限。阈值走环境变量 `RATE_LIMIT_PER_MIN`，不硬编码。

## 六、从 0 到 1 的测试

这个仓库没有 CI——`wrangler deploy` 即发布生产，README 里专门写了一句警告：push 不等于部署，部署以 wrangler 为准。在测试这件事上，它此前的记录也是零。

凌晨四点十八分，最后一条提交补上了第一块砖：`tests/gateway.test.mjs`，六十一行，用 Node 自带的 `node:test` 跑，零依赖。难点在于被测对象依赖 D1——总不能为了测限流去连线上数据库。解法是一个内存 D1 桩：一个 Map，键是 `ip|window`，值是计数；`prepare`、`bind`、`first` 逐级返回对象，精确模拟 D1 链式 API 和那条 UPSERT RETURNING 的语义——同键自增，返回最新计数。二十几行代码，把"线上这条 SQL 会怎么表现"翻译成了"内存里这个 Map 会怎么变"。

用例分两组。鉴权比较五个：相同内容为真、内容不同为假、长度不同为假、空串对空串为真、大小写敏感。限流三个：额度内连续请求放行、超限后的请求拒绝、不同 IP 互不影响——第三个用例防的是限流器误伤，A 打满了额度不能连坐 B。为了可测，`src/index.js` 底部加了两行命名导出，把 `checkRateLimit` 和 `timingSafeEq` 暴露出来，运行时行为不变——这就是二百六十行变二百六十三行的来历。

写作本章时实跑了一次验证：`node --test tests/`，三个用例全过，总耗时 55.8 毫秒。

桩也有它诚实的边界。内存 Map 只模拟了 UPSERT RETURNING 的语义，模拟不了 D1 的真实一致性边界，模拟不了那 1% 概率的清理语句——测试里传入的 `waitUntil` 是个空函数，也模拟不了 fetch 层的重试与流控。这不是缺陷声明，而是分工声明：纯函数的语义错误交给测试，集成行为交给线上验证矩阵，两条防线各管一段，谁也不假装自己能包办全部。

为什么只测这两个纯函数？因为 Worker 的风险高度集中：鉴权比较和限流语义是纯逻辑，错了就是安全事故或误伤事故，且恰恰可以用几十行桩代码完全覆盖。测试从零到一，不追求覆盖率数字，先让最危险的路径有回归保障。

## 七、身份之谜：owned_by 是什么

这一夜的加固里有一段插曲，事后被写成了方法论。

画链路拓扑时，一度凭印象断言"poeapi_go 已经退出生产链"——依据是一份过期的运维笔记，而这个错误结论被写进了正式报告。意思是：api.yuangs.cc 背后的真身不是 poeapi_go，那个 Go 网关已经退役。如果照此执行，加固的重心会整个偏移，甚至可能在错误的层做鉴权。

纠正是靠一条命令完成的：

```bash
curl -s https://api.yuangs.cc/v1/models | head -c 200
```

返回体里一句 `"owned_by":"poe-api-service"` 直接打脸——这个字段是 poeapi_go 的指纹，模型列表是它吐的。`api.yuangs.cc/v1/` 的真实后端就是 poeapi_go，t 机 9090 端口，主链路好好地跑在上面。十秒钟推翻了一个写进报告的结论。类似的事当晚发生了不止一次：自查 nginx 配置时以为某域名由 u 机托管，翻了三台机器，最后发现入口藏在 t 机 nginx 的 map 路由里。

教训浓缩成一句话进了前言，也进了指南：链路判断只信当天的命令输出，不信任何时代的文档——包括自己写的。推荐的核实手段都很朴素：看返回体特征，模型列表的 owned_by、health 的响应格式都能当指纹；看 server 头和 cf-ray，有 cf-ray 说明流量过了 Cloudflare 代理，没有就是直连源站；在疑似源站上跑 `ss -tlnp` 确认监听端口，查 ufw 和 iptables 确认防火墙策略。三招下来，链路里每一跳的"我以为"都能被一条命令证伪。每一跳还要能回答三个问题：流量从哪进来、怎么验证它合法、它挂了会怎样——回答不了的跳，就是盲区。

写作本章时，这条命令被重新跑了一遍：指纹依旧，`"owned_by":"poe-api-service"`，列表里躺着 free、Assistant、gemini-3.6-flash。结论再次成立——不是因为文档这么说，是因为此刻的命令输出这么说。

## 八、入库与收录：一个仓库的自我补全

时间回到 8 月 28 日晚十点零一分。aiproxy 仓库的第一次提交，把线上跑了很久的 Worker 源码请进了版本控制，附上一份部署快照备份。但此后九天，这个仓库只有一个本地路径——没有 remote，源码只存在于这台 Mac 的磁盘上，和 Cloudflare 线上那份部署各存一份。对一个承重面最大的单点来说，这是另一种裸奔：Mac 磁盘坏了，或者一次误删，版本历史就没了。

9 月 6 日凌晨两点十八分，GitHub 上出现了一个私有仓库，名字很直白：cloudflare。两分钟后，第一次 push 落地，把 c97b34b——鉴权加中继强化的全部代码——推了上去。此后每个提交都有对应的推送记录，reflog 里八次 "update by push"，一直推到凌晨四点十八分的测试提交。源码从此三处存续：本地、GitHub 私有仓、Cloudflare 线上。

接下来的一个小时，这个仓库做了一件更罕见的事：给自己补全上下文。两点二十三分，《AI 链路优化指南》全文收录进 docs——一万零三百七十六个字符，从画拓扑、定威胁模型、三层鉴权、限流、可观测性、密钥管理、部署顺序，到五个踩坑实录和一份推广清单，是把这次加固沉淀成可复用方法论的一次尝试。两点二十五到二十六，三次提交完成 AUTH-CHAIN 的镜像收录：这份链路鉴权的权威文档住在 poeapi_go 仓库里，aiproxy 这边放的是镜像副本，文件头用一行警告标明主从关系——"两边不一致时以权威版本为准"。两点三十，《中枢的价值》入库，四千七百余字，回答"为什么一个两百多行的 Worker 值得单独写一篇文章"。加上两点二十二重写的 README——链路位置、端点表、三层鉴权、限流、超时断连、记账、密钥、验证矩阵、变更日志——README 的开头留下了一行给未来读者的导航：半年后看不懂链路时，按顺序读这几份文档。

这套动作的逻辑和代码加固是同构的：代码怕失控，所以留退路；知识怕失传，所以留文档；文档怕过期，所以标权威、留镜像。一个仓库的自我补全，补的不只是代码。

## 九、验证：以当天的命令输出为准

所有变更落地后的验收，靠的不是"打开网页试试"，而是一张写进文档的验证矩阵：

```bash
curl -s https://aiproxy.want.biz/health                                    # 期望 {"ok":true}
curl -s -o /dev/null -w '%{http_code}' https://aiproxy.want.biz/v1/models  # 期望 200（穿透全链路）
curl -s -o /dev/null -w '%{http_code}' https://api.yuangs.cc/v1/models     # 期望 401（直连应被拒）
curl -s -o /dev/null -w '%{http_code}' https://aiproxy.want.biz/api/stats  # 期望 401
```

四条命令各有指向：第一条验 Worker 活着，第二条验整条链路——Worker、nginx、poeapi_go、上游模型——都通，第三条验 nginx 的门关上了，第四条验统计接口的门关上了。指南的附录里还有两条更深的：ssh 到 t 机本机回环打 chat 端点，验网关层强制鉴权；grep journalctl 里的启动自证日志，验 key 注册数量。六条全过，才算上线成功。

写作本章时，前四条被重新执行了一遍，结果与那一夜的验收完全一致：health 返回 `{"ok":true,"ts":...}`，models 返回 200 和 poeapi_go 的指纹，直连无 key 是 401，统计无 key 也是 401。这不是巧合，而是这套设计的日常状态——每一层各有一道门，每道门都可以被一条命令独立验证。

验证哲学在这一章反复出现，值得最后再收拢一次：限流器的故障模式是显式选择的（放行），鉴权的故障模式也是显式选择的（拒绝），超时的语义是显式区分的（504 对 500），部署的每一步是显式可回退的（fallback、备份文件、逃生门开关）。含糊是可靠性最大的敌人，而显式是对付它的唯一武器。

## 十、反思：小，是一种承重能力

回看这一夜，aiproxy 的代码净增不到一百行。真正花时间的是三件事：把拓扑核实清楚（为此推翻了自己写进报告的结论）、把每一步设计成可回退（为此在每一处变更里埋 fallback）、把每个坑写下来（为此凌晨两点还在整理文档）。

有几条经验值得单独留下。

其一，兼容层哲学让安全变更得以落地。前门保持公开，穿过前门后每一步验票——这个形态的最大好处是客户端零改动，而客户端零改动是一切安全变更的政治前提：七八个端不需要重新发布，轮换不需要全端协同。"知道域名就能白嫖"的时代结束了，但结束得悄无声息，没有任何一个用户感知到变化。

其二，失败模式要逐个显式选择。限流器故障放行，因为保护上游优先于拒绝服务；鉴权失败拒绝，因为这里没有灰度空间；记账失败不中断请求但必须可见，因为账本失真是慢性病。三个场景三种选择，每种都写进了注释。未配置的 secret 走旧行为，这个看似保守的 fallback 恰恰是整晚敢连续部署的底气——它把"部署代码"和"开启强制"解耦成两个独立的、各自可回退的动作。

其三，个人的链路更要做安全，而不是更不用做。没有安全团队替你兜底，一次配额盗刷、一次密钥泄露，代价都要用深夜的排查来偿还。而好消息是，个人链路的防御很便宜：一个 Worker 里的 if、一条 nginx 规则、一个环境变量，就足以把裸奔变成三层校验层层咬合。这一夜的全部安全代码，加上测试，不超过一百行。

其四，小的克制是它被托付为单点的前提。二百六十三行的 Worker 不做模型推理、不存对话内容、不管理订阅，它只做链路中最小但最不可少的那件事——所以它能被完整读懂、被逐行加固、被几个用例覆盖最危险的路径。面对"要不要合并进 poeapi_go"这类问题，文档里给的答案是不合并：边缘记账贴近用户，模型网关贴着凭据与路由，位置即职责，职责即边界。

其五，这次加固沉淀的不只是一条链路，而是一份可以复制的清单。指南的第九章把它写成十条自查：画出拓扑且每一跳用命令验证过；无凭证调用核心端点必须 401；有记账；统计和管理接口有独立门禁；密钥不在前端 bundle、不在 git 历史、不在进程命令行参数里；轮换流程成文且真的演练过一次；部署顺序文档化且每步可独立回退；监听地址最小化，能回环不内网、能内网不公网；有启动自证日志；限流存在且"故障时放行还是拒绝"已明确。十条全绿，一条链路才算从"能跑"进入"可托付"。清单上还有别的公开服务在等着被同样过一遍——这次探测的结束，也是下一次自查的开始。

最后一次数一数这一夜的产出：两份生产代码提交，一份 migration，六十一行测试和八个用例，一篇万字指南，一篇定位文章，一份权威文档的镜像，一份重写的 README，一个新生的 GitHub 私有仓和八次推送。全体系的 AI 流量明天照常从这个小小的边缘节点流过——只是从今往后，每一次跳转里都写着同样一句话：

凭据呢。

---


# 第 3 章 · wsstunnel —— 反向 PTY 隧道

# 第 3 章 wsstunnel —— 无根之处的真终端

## 一、幽灵字符

2026 年 9 月 6 日凌晨，一次例行远程运维的尾声。

运维者在 t 机容器的终端里敲完最后一条命令，确认无误，输入 `exit`。屏幕上的输出停住了，会话看起来已经干净地结束。大约二十五秒之后，终端里安静地浮出一行谁也没有敲过的字符：

```
__PONG__
```

没有提示符，没有来路，像一间空屋里突然响起的一声应答。头几次撞见的人多半会以为自己眼花，或者以为是远端的乱码。它不报错、不影响后续操作，过一会儿还可能再来一次。一个“用起来没毛病”的工具，就这样带着一个只有眼睛能发现的 bug 活了很久。

这行幽灵字符后来被证明不是一个孤立的 cosmetic 问题，而是一类问题的缩影：在一个把“用户敲的字”和“协议控制消息”跑在同一条 WebSocket 上的系统里，控制通道的任何泄漏都会直接写进用户的屏幕；反过来，用户屏幕上出现过的东西，协议层也未必守得住。顺着这条线索往下挖，挖出的是一整串安全债务——不校验证书的 wss、永远不触发的防爆破锁定、完全不设防的后端注册。这一章讲的就是 9 月 5 日到 6 日那三十多个小时里，这个项目如何把五笔欠账一次还清。

## 二、定位与架构：为什么是反向 PTY

wsstunnel 的自我介绍只有一句：通过 WebSocket 与 HTTP 代理穿透极端受限网络，提供原生 PTY 交互式远程 Shell 的轻量自托管工具。翻译成人话：在一个没有 root、装不了 sshd、只允许 HTTP 代理出站的容器里，拿到一个真正的交互式终端。

市面上不缺内网穿透工具——frp、ngrok、chisel 都是成熟方案。但它们大多基于端口映射：要求目标机器上已经有一个监听的服务（比如 sshd），再把那个端口暴露出去。真实的受限环境恰恰不给这些前提：在线 IDE 的沙箱、CI runner、仅允许 HTTP 出站的内网设备，既没有 root 权限，也开不了入站端口。wsstunnel 走了反过来的路：不监听任何端口，在目标进程里用 `pty.openpty()` 拉起 `bash -i`，把标准输入输出通过一条由内向外的 WebSocket 反向推给中继端。终端是“真”的——vim、sudo、tmux 都能跑，因为 PTY 在，而不只是字节通道。

代码结构与之对应，四个模块一目了然：

| 模块 | 行数 | 职责 |
|---|---|---|
| relay.py | 1261 | 中继：鉴权、角色路由、消息转发、托管 Web 终端 |
| client.py | 764 | 驻留受限环境：起 PTY 或管道，断线重连，心跳，文件传输 |
| cli.py | 489 | 命令行入口：relay / client 子命令与 put / get |
| security.py | 381 | 多 token、角色、IP 白名单、防爆破、审计 |

外加一个千行级的单页 Web 终端（xterm.js），由 relay 直接托管在同一端口上。整个包以 `wsstunnel` 为名发布在 PyPI 上，当前版本 1.0.3，`pip install` 即得。

协议层围绕两个角色展开。**后端**是受限环境里的 client，连上中继后发一条 `IAM_BACKEND:<token>:<name>:pty` 完成注册，此后把 PTY 的原始输出作为二进制帧推上来；**前端**是握着终端的人——浏览器、CLI 或 websocat，用 `AUTH:<token>` 消息或直接在 URL 里带 token 完成认证，收到 `AUTH_OK` 之后，敲进去的每个字节都会被中继转给选中的后端。多后端时，`LIST` 命令列出所有在线后端及其存活时长，`USE` 或 `@名字` 前缀选中目标；窗口大小同步（`__RESIZE`）、信号转发（`__SIGNAL`）、文件传输（`__FILE_BEGIN / CHUNK / END`）各占一条控制消息。安全层齐备：token 支持单值或 JSON 文件批量加载，每个 token 带角色（readonly 只看、file 只传文件、admin 全权），配 IP 白名单、命令黑名单与 JSON 格式审计日志；wxpush 参数还能在后端上线时把通知推进微信。compression 开关对应 WebSocket 的 permessage-deflate，弱网下按键不粘滞。

这套系统在五机体系里的位置是承重的。中继跑在 t 机的 8443 端口，由 systemd 托管，服务名 `wsstunnel-relay`，启动参数里带着压缩开关，token 走环境变量注入；tencent 与 aliyun 两台机器上各有一个 client 常驻，每三十秒发一次 `__PING__`，连接断了就按 5 秒起步、指数增长、封顶 5 分钟的退避节奏重连。日常从浏览器或命令行连上 t 机，选中某个后端，就落进了那台机器的 shell 里。它不是演示项目，是生产通路。

正因为承重，安全欠账才必须还。

## 三、前史：八月末的那次全线翻车

这一轮安全收尾并不是凭空开始的。半个月前的 8 月 31 日，这个项目刚经历过一次从“随手看看”滑向生产事故的完整周期，那次经历本身写进了仓库的开发日记。

那天本来只是一件轻活：拉最新代码，看看还有哪里可以优化。先跑测试，131 个通过、2 个失败（伪终端设备耗尽的环境问题，换机器就绿）；再逐文件通读，直觉不对，于是写了个探针——本机起一个真实的 relay 进程，接一个假后端、两个前端，把每一跳的收发原样打印。探针实锤了十二处问题。最刺眼的一处：CLI 帮助文案里写着“命令黑名单（deny-cmd rm）”，grep 全仓库，`DenyList.is_denied()` 的调用点是零——安全功能是纯装饰，运维以为 `rm` 被拦了，shell 里敲 `rm` 照跑不误。还有 FILE 角色的三个协议分支做完角色检查后忘了 return，整个文件角色认证能过、事情办不成；广播循环在 `await` 挂起期间被并发修改，探针里直接复现 `RuntimeError: Set changed size during iteration`。十二处修完，四百多行改动，测试从 131 涨到 176。

真正的考验在晚上。生产升级从 PyPI 装了 1.0.1，服务 active、8443 在听，一切正常——直到多看了一眼 journalctl：`websockets/legacy/server.py` 的堆栈赫然在列。服务器上的 websockets 是 12.0，顶层 `serve` 走的是 legacy 实现，`process_request` 回调的入参与返回值和本地 16.0 的 asyncio 实现完全是两种形状；而 Web 终端的 HTTP 处理只适配了新形状。每一个进来的连接都在同一行炸掉，relay 完全不可用。更扎心的是，relay.py 里原本有一段检测 websockets 版本的代码，注释写明“用于适配 handler 签名和 process_request 行为”，却在十几个小时前被当作死代码删了——它不是死代码，是一个没写完的 TODO。从 03:31 第一次重启到 03:42 确认恢复，生产中断约十分钟；救回来靠的是鸭子类型区分两种调用形状的两次提交，以及 client 端那份 5 秒起步的重连退避——服务端修复一落地，真实容器就在某一轮重试里自己回来了。

那次事故留下了五条写进清单的教训：依赖版本是环境的一部分；“死代码”可能是没写完的 TODO；升级验证必须打真实请求；客户端的韧性救了场；探针驱动的 review 值得推广。协议兼容的雷排掉了，测试涨到了 184 个。但安全视角的复查还没做透——九月的这一轮，就是专门来还这部分账的。

## 四、问题发现：五笔欠账

这一轮复查从 9 月 5 日凌晨开始。方法还是老办法：不信任读代码时的直觉，每一条怀疑都要落到文件与行为级的证据上——哪一行、哪条消息、哪种调用顺序会触发什么，全部对到实物。五个问题，按严重程度排下来。

### 第一笔：wss 从不校验证书

最早的一刀来自对依赖库行为的求证。cli.py 与 client.py 里，建立 wss 连接时非 insecure 分支传的是 `sslopt=None`——看上去“没做任何特殊配置”，而“没配置”在多数库里意味着“用安全默认值”。作者打开本地安装的 websocket-client 源码（1.9 版），在 `_http.py` 里找到了这行：

```python
if sslopt.get("cert_reqs", ssl.CERT_NONE) == ssl.CERT_NONE and not sslopt.get(...)
```

`sslopt.get('cert_reqs', ssl.CERT_NONE)`——字典里没有 `cert_reqs` 时，回落到 `CERT_NONE`。也就是说，这个库在调用方没有显式指定证书要求时，默认**不校验服务器证书**。此前代码里那句 `sslopt=None` 等于把选择权完全交给了这个默认值：`wss://` 的连接从不验证对面是谁，TLS 加密的字节流原封不动地送到了中间人的手里。

后果要与部署形态连起来看才有分量。client 与 relay 之间的信任完全建立在一个 token 上——认证消息、URL 参数里都带着它。而 client 常驻的机器到 t 机的链路要穿过公网。一旦链路上有劫持者（被污染的 DNS、被探测的机房内网、被控制的代理），假服务器握手成功，client 会把 token 恭恭敬敬地送过去。之后攻击者拿 token 连真中继，就是一个合法后端或合法前端。加密形同虚设，因为加密的对象是错的。

### 第二笔：永远不触发的防爆破锁定

security.py 里的 BruteForceGuard 设计得相当规矩：默认五次失败触发锁定，锁定时长从 3 秒起按指数翻倍——3 秒、6 秒、12 秒一路加倍，上封 30 分钟；固定 3 秒锁定被注释里明确否决过，因为那样攻击者的稳态速率仍可达每秒 1.67 次。失败记录带最后失败时间戳、按十分钟 TTL 惰性过期清理，防公网扫描器把字典灌成慢性内存泄漏。连接入口处有 `is_locked` 检查，被锁的 IP 在握手后第一句话就被拒。

逻辑闭环看起来无懈可击——直到追问一句：`record_failure` 到底在哪些路径上被调用？

答案在 relay.py 的 handler 里。前端认证走 `AUTH:<token>` 消息，后端注册走 `IAM_BACKEND:...` 消息，两种消息各有自己的分支；而 token 模式下，猜错的 token 既匹配不上前端分支，也匹配不上后端分支，最终全部落进 handler 末尾的那个 else：回一句 `AUTH_FAIL`，关连接，结束。这个 else 分支里没有 `record_failure`，也没有审计。提交信息里的话说得很直白：原 1028 行附近的计数点在单 token 认证模式下不可达。守卫的锁孔还在，锁芯被人从设计图纸上省掉了。攻击者可以永远猜下去，每秒一次，永不封禁，日志里一片安静。

### 第三笔：后端注册不设防

这是五笔里最重的一笔。协议设计里，后端用 `IAM_BACKEND:<token>:<name>:pty` 这样的消息向中继注册。解析函数 `_parse_backend_auth` 用前缀匹配做认证：

```python
prefix = f"IAM_BACKEND:{token}" if token else "IAM_BACKEND"
if not msg.startswith(prefix):
    return None
```

单 token 模式下，`startswith` 有个经典的前缀碰撞：token 是 `abc` 时，`IAM_BACKEND:abcdef` 也能通过检查——知道前缀就能冒名，差一个精确边界。

而 token 文件加载的多 token 模式下更彻底：此时 `self.token` 是 None，前缀退化成裸的 `IAM_BACKEND`，**任何人不需要任何 token 就能注册任意名字的后端**。注册成功意味着什么？中继会把前端的 PTY 输出原样转发给这个“后端”，也会把“后端”发来的字节写进前端终端。攻击者注册一个与真后端同名的连接，按中继的重连顶替逻辑，旧连接会被踢掉——前端下一次敲的每一条命令、每一根按键，连同服务器回显的一切，都从攻击者的通道里过了一遍。会话劫持加键盘记录，一个前缀匹配的疏忽全包了。

### 第四笔：断连之后，句柄不散

client.py 里维护着一张进行中上传的登记表 `_file_transfers`，每一项都开着文件句柄往磁盘写。连接断开时，这张表没人管：句柄一直挂着，半截文件留在磁盘上，直到进程退出才被操作系统回收；残留状态还会让下一次同名上传拿不到干净的起点。在长驻进程里，这是一类典型的慢性泄漏——单次看是小事，积累起来就是磁盘与句柄的慢性消耗。

### 第五笔：回到那行幽灵字符

现在可以给开篇的故事一个完整的解释。Web 终端每 25 秒发一次 `__PING__` 保活——源码注释写得明白：25 秒，特意取在常见反代 60 秒空闲超时之内；后端 client 每 30 秒发一次，中继侧再配一道 90 秒的判死线——三个心跳周期收不到就按半开连接处理。中继收到保活后的应答是一个**文本帧** `__PONG__`。而文本帧在前端是要进终端的：Web 终端把它打印出来，CLI 把它打印出来。于是每次保活，用户的屏幕上就多一行本不属于会话的字。`exit` 之后 PTY 没了，但保活定时器还在跑，二十五秒一轮，幽灵准时现身。控制消息走错了通道，用户的终端成了它的输出目的地。

五笔欠账，三笔在安全上，两笔在健壮性上。它们有一个共同的气质：都不炸、不报错、不影响“正常使用”，全靠主动对账才能现形。

## 五、修复：五个提交的三十小时

修复从 9 月 5 日凌晨 5 点的第一个提交开始，到 9 月 6 日凌晨 4 点的最后一个提交收尾，五个提交，一气呵成又各自独立——每个提交只还一笔账，配自己的测试，出问题可以单独回退。

### 07dd62a：把校验还回去

TLS 的修复小得惊人，cli.py 与 client.py 各改一处：

```python
sslopt={"cert_reqs": ssl_mod.CERT_NONE}
if insecure
else {"cert_reqs": ssl_mod.CERT_REQUIRED},
```

非 insecure 分支显式传 `CERT_REQUIRED`——库会自动加载系统 CA 并校验主机名，行为回到 wss 应有的样子；自签名场景仍可用 insecure 开关显式跳过，逃生门保留，但从此必须是主动的选择，而不是沉默的默认。这个修复没有引入任何新机制，只是把库夺走的默认值夺了回来。

### 36b098c：让守卫通电

防爆破的修复只有五行，全部落在那个最终 else 分支里：

```python
self.brute_force.record_failure(peer_ip)
self.audit.auth_failed(peer_ip, first[:8] if isinstance(first, str) else "<binary>")
await websocket.send("AUTH_FAIL")
await websocket.close(1008, "Authentication failed")
```

猜错的 token 现在有两重下场：计入锁定计数，写入审计日志。失败路径被统一到一处，安全代码从“存在”变成了“可达”。

### bc2dc8f：重写注册的边界

这是五个提交里最大的一笔，`_parse_backend_auth` 被整体重写。新规则分两层。单 token 模式：前缀必须精确到 `IAM_BACKEND:<token>`，且剩余部分必须为空或以 `:` 开头——`token=abc` 时，`IAM_BACKEND:abcdef` 的剩余部分是 `def`，不以冒号开头，直接拒绝，前缀碰撞被精确边界消除。多 token 模式：消息第一段必须是任意一个已注册 token，验证用 `token_manager.validate`，其内部是 `hmac.compare_digest` 的常数时间比较，防时序侧信道；验证通过后剥离 token 段再解析名字与模式。注释里把旧实现的两个洞写得清清楚楚，像是写给半年后的自己的道歉信。

同一个提交里，client.py 新增了 `_abort_file_transfers`：断连时遍历登记表，逐个关闭文件句柄，清空登记，返回中止数量；它被挂进断连路径，半截上传从此有了善后。函数头部的注释把动机写得很实：“此前连接断开时进行中的上传句柄会一直挂着（文件保持打开、磁盘占用直到进程退出），残留状态还会让后续同名上传拿不到干净起点。”这类清理代码最怕两件事：怕漏（遍历时集合被改）和怕误伤（新连接的状态被旧连接清掉），所以实现里用 `list(...)` 取快照遍历，返回值留给日志——两行防御，都是别处踩过的坑换来的。

### f147faa：控制帧归控制帧

幽灵字符的修复体现了一个朴素的协议观：应答保活，就用协议层的机制。中继收到 `__PING__` 后，改用 `await ws.ping()`——WebSocket 的 ping 控制帧应答。控制帧在协议栈里被自动消费，永远不会进用户的终端；同时它在反代上同样产生一来一回的流量，防空闲切断的原始目的不受影响。更妙的是向后兼容：旧客户端本来就不依赖回包的文本内容，行为零变化。测试同步更新，`__PONG__` 这个字符串从协议里退役。

### 9ed1d5d：把覆盖率钉在仓库里

最后一个提交接入 pytest-cov，并为 `_abort_file_transfers` 补上两个单元测试。从此每次跑测试，覆盖率数字随手可得，覆盖率文件 `.coverage` 直接进了仓库——数字本身也是证据。

### 插曲：2G 机器上的静默死亡

修复要铺到生产。把新版本装到各台机器时，b 机给了个下马威。这台机器只有 2G 内存，pip 拿到的不是现成的 wheel，而是源码包——于是现场构建。构建进程在内存压力下被系统杀掉，**死得悄无声息**：pip 没有报错，shell 没有提示，看起来像命令“跑完”了，包却没装上。这种静默死亡比报错难缠得多，报错有线索，沉默只有结果。

弯路之后是正路：在内存宽裕的 t 机上用 `pip wheel` 把 wheel 构建出来，再把 wheel 文件传到 b 机本地安装。构建与安装分离，2G 的机器只做它做得动的事。这是分布式运维的老手艺，但对一个“pip install 就该全自动”的时代养成的习惯来说，这一跤摔得值——工具链的自动化有边界条件，机器的规格就是边界之一。

## 六、验证与数据

提交齐了，数字说话。

测试：`pytest tests/ -q` 实测 **196 passed**，8 个测试文件，覆盖路由、时序、并发、协议、文件传输与安全层，整套跑了不到 3 秒。从八月末的 131 个到今天，测试数量翻了近一半，每一轮增长都对应一批真实踩过的坑：探针转正的端到端用例、重连竞态用例、双角色路由用例，再到这一轮的注册鉴权与句柄清理。覆盖率（写作时以 pytest-cov 实测）：

| 模块 | 行覆盖 |
|---|---|
| security.py | 97% |
| relay.py | 69% |
| cli.py | 69% |
| client.py | 33% |
| 总体 | 61% |

security.py 的 97% 不是偶然——防爆破、token 验证、白名单这些安全原语全部是纯逻辑，天然可测，也最值得测；token 过期比较那一行 `datetime.now(expires.tzinfo)` 能同时兼容 naive 与 aware 两种时间戳的技巧，就是被参数化测试钉住的。client.py 的 33% 是另一面的诚实：PTY 交互与重连时序依赖真实终端与网络，单测够不着，靠的是端到端探针与生产验证兜底。覆盖率不追虚高，追的是“测了的都是真问题”。

部署后的上线验证按升级清单走——那份清单正是八月末十分钟中断换来的：装完、重启、curl 关键路径、真实链路探针、看日志窗口，最后才报平安。这轮多了一项敌意测试：不带 token，直接向后端注册端点发一条 `IAM_BACKEND:ghost:pty`。线上中继的回应干脆：`AUTH_FAIL`，连接即断；journalctl 里同步多出一行 JSON 审计：

```
AUDIT {"event": "auth_failed", "ip": "...", "token_prefix": "IAM_BACKEND:ghost"...}
```

在修复之前，这条消息会成功地注册出一个名叫 ghost 的后端。修复之后，它在第一段 token 校验处就被拦下，还被记了档案。同一轮验证里，正常 client 重连注册、前端带 URL token 自动认证、错误 token 连续尝试后触发锁定——各条路径与设计一致。

## 七、反思

回看这五个提交，有几件事值得留在纸面上。

**库的默认值，就是你的安全边界。** websocket-client 的 `CERT_NONE` 是一个合理的工程决定——它让库在拿到自签名证书的实验场景里开箱即用——但对拿它承载 token 的调用方来说，这个默认值就是漏洞本身。`sslopt=None` 这种“我没配置”的写法，读代码时像中性词，运行时却是一个明确的决定：不验证。修复之后代码里留了注释，把库的默认行为钉在原地，防止下一个读代码的人再犯同样的想当然。

**不可达的防御代码，比没有防御更危险。** BruteForceGuard 的类写得很好，锁定逻辑正确，过期清理周到——但在单 token 认证模式下它从未运行过。配置了它的运维以为有锁定，攻击者实测没有，中间隔着一条从未有人走过的代码路径。安全机制的验收标准不是“代码存在”，是“失败路径被真实走到过”。那行审计日志出现之前，一切绿灯都作不了数。

**共用通道的协议，要为泄漏付出双倍小心。** `__PONG__` 泄漏看起来是个笑话，根子却是结构性的：控制消息与用户数据跑在同一条文本通道里，控制消息一旦被前端当数据渲染，协议就漏进了产品。ping 控制帧是协议标准给出的答案——保活属于传输层的事，就不该在应用层留字据。

**审计不是日志，是证据链。** 这轮修复有个共同细节：凡拦截与失败，都写审计——`auth_failed` 带来源 IP 与 token 前缀，锁定事件带时长与累计次数。八月底那次复查里，`audit.file_download` 曾是一个从没人调用的方法，修复后才真正用起来。安全系统的价值有一半在事前拦截，另一半在事后可查：攻击者在日志里留下的每一行，都是下次把门修得更牢的图纸。没有审计的拦截，和没有触发的锁定一样，只是自我安慰。

**小工具长成基础设施，账就得按基础设施的规矩还。** wsstunnel 从“给自己开的隧道”长成了多机常驻的生产通路，而这一轮修的五笔账——证书、防爆破、注册鉴权、句柄清理、心跳通道——全是基础设施的必修课，没有一门是“功能”。五台机器、三十个小时、五个提交、196 个测试，数字都不大，但从此每一根穿过公网的按键，背后都站着确实在运行的校验。

至于那行幽灵字符，它再也没有出现过。它出现过的那二十五秒延迟——从 `exit` 到 `__PONG__` 浮现的间距——恰好是保活间隔的长度。bug 修复之后回想，它其实一直在按点打卡：一个尽职的哨兵，用最鬼魅的方式，向所有路过的人显示城墙上有一扇没人看守的门。

---


# 第 4 章 · npm_yuangs —— SSH 治理执行器

# 第 4 章 npm_yuangs —— 给 AI 的终端上锁

## 一、一条不会被检查的命令

先设想一个场景。它没有真实发生过，但修复之前，它随时可能发生。

某个下午，AI 助手奉命登录一台远程服务器做例行维护，需要执行一条需要特权的命令，于是敲下 `sudo apt update`。治理层照章办事：识别出这是提权请求，调用治理服务评估，放行，然后进入一个专门为密码输入准备的状态——PENDING_PWD。这个状态的语义写在代码注释里，只有一句话：密码输入阶段，绝不治理，直接透传。道理很正当——密码绝不能进审计日志，绝不能被分析、被重放，这个阶段的一切输入都必须原样送到远端。

命令发出去了。碰巧这台服务器的 sudoer 配置里，这一条是 NOPASSWD——sudo 不需要密码，直接放行执行。没有密码提示出现，没有 `sorry, try again`，命令安安静静跑完了。

需要说明这个“透传”的设计并非偷懒，它守护的是一条更根本的底线。密码是终端世界里最后一种不适合被机器阅读的字符：它不能进审计录像——录像会回放，回放会泄密；不能进治理评估——评估意味着解析，解析意味着密码在某处的内存里以结构化数据存在过；甚至不应该进日志的截断逻辑——任何“只记前八位”的处理对密码都是灾难。SSH 客户端对密码的标准姿态就是闭眼：原样转发，绝不观看。PENDING_PWD 的“绝不治理，直接透传”正是这个姿态的代码化。

然后呢？然后状态机停在了 PENDING_PWD。它的设计里只有两个出口：看到 sudo 失败文本，或者看到 root 提示符。NOPASSWD 哪个都不给。从这个时刻起，这条 SSH 会话上的一切输入——包括下一条、下一百条命令，哪怕是一条 `rm -rf /`——都享受着“密码阶段”的最高礼遇：绝不治理，直接透传，不留审计。治理层还在，但它的眼睛被一块永远不摘的眼罩蒙住了。

这不是假想的代码缺陷，它是真实存在的状态机出口缺失，在 yuangs CLI——一个八万余行 TypeScript 的 AI 命令行工作台——的 SSH 治理执行器里，一直活到 2026 年 9 月 6 日凌晨三点二十五分，才被一个提交终结。这一章讲的就是这次修复：一条隧道如何学会在正确的时刻收起眼罩。

## 二、定位与架构：当 AI 走进终端

yuangs CLI 的自我定位写在 README 第一行：以人类意图为中心的 AI-Augmented Shell。它要回答的问题也很直白：当不可控的 AI 进入极端强调可控性的终端，秩序该如何重建？核心理念是那句“AI 提供思路，人类掌控执行”——AI 可以想，但每一条真正落下去的命令，都必须过人的规矩。

架构上它分了六层：L1 命令层负责注册与解析（cli.ts 698 行，commands/ 目录 7600 余行，11 个顶层命令）；L2 运行时是 AI 引擎，8.0.9 起收敛为单一 pi 引擎加 direct 直连降级——那次收敛删掉了四套并存的旧运行时与 16 个冗余工具，净减约 2500 行；L3 是治理风控——风险打分、审批、拦截；L4 管工具与上下文；L5 是核心服务（core/ 目录约 17800 行）；L6 是基础设施。整个 src/ 约 44700 行 TypeScript。npm 包名就叫 `yuangs`，8.1.0 版，bin 入口一个词，装完即用。`engines` 从 8.1.0 起收紧到 `node >= 22.19`，与 pi 引擎的要求对齐。

这一章的主角是其中一个不算大的模块：`src/ssh/`，三个文件合计 713 行。它是“SSH 治理工作台”的三件套——

**SSHSession.ts**（265 行）：连接与 PTY 的生命周期。用 ssh2 库建立连接，`shell()` 而不是 `exec()` 打开终端——注释里写明理由：sudo、su、vim、less 都需要 PTY，这是“活终端”，不是 RPC。窗口 resize、Ctrl+C 的信号转写（`\x03`）、流的转发都归它管。

**InputBuffer.ts**（76 行）：命令边界探测器。它不做任何分析，只做一件事——从按键字符流里判断哪里是一条命令的终点。规则只有一条：遇到 `\r` 或 `\n` 才把缓冲交给治理层。它是治理的入口闸门，闸门开错地方，后面全白搭。

**GovernedExecutor.ts**（372 行）：治理执行器，整个系统最值钱的类——注释原话。它订阅 PTY 输出，维护提权状态机（USER、AWAITING_APPROVAL、PENDING_PWD、ROOT 四态），每条命令先过治理服务评估，被拒就渲染拦截信息并向远端发 `\x03` 清行，放行才真正写出命令。旁边还有两个配角：SensitiveStreamInterceptor 负责敏感阶段开关审计；Recorder 以 asciinema cast v2 兼容的 NDJSON 格式把整个会话录下来——输出、输入、窗口调整、治理事件，一帧不落，密码则被敏感拦截器挡在录像之外。

提权不是一个状态，是一条流水线。用户敲下 `sudo` 开头的命令，执行器先把状态推到 AWAITING_APPROVAL，请治理服务对这条提权命令本身做评估——拒了就回 USER，渲染拦截，记 `elevation_blocked`，并向远端发 `\x03` 把已经打出来的字符清掉；批了才推入 PENDING_PWD，开启敏感拦截，记 `elevation_started`，真正把命令发给远端。评估是有递归的：命令工作台的治理服务实现里，`sudo` 后面跟的那条命令会被剥出来单独再过一遍危险模式检查——`sudo rm -rf` 不因为穿了一件 sudo 的马甲就免检，命中危险模式直接按 R2/R3 风险级拦截。

命令的发送还有一个容易忽略的精度问题：智能补发。人是在终端里逐字打字的，每个字符都已经有回显，执行器如果再发一遍完整命令，远端就会出现重复。所以正常路径只发“尚未发送的部分”加回车；而如果治理层改写了命令（比如自动纠错），就先发 `\x03` 清掉已输入的字符，再发改写后的命令加回车。治理层介入的痕迹，就藏在这两种发送路径的分岔里。

一条完整的 sudo 生命周期，会在审计录像里留下一串治理事件：`elevation_started`（提权开始）→ 敏感阶段开启 → `sensitive_prompt_displayed`（密码提示出现，只记事件不记内容）→ 密码透传（输入与输出都不进录像）→ `elevation_success_root`（拿到 root）或 `elevation_failed`（认证失败回 USER）→ 正常命令的 `allowed` / `blocked`（附命令、理由与风险级）。事后回放这段会话，权限的每次涨落都有据可查，唯独密码本身，从头到尾不存在于任何一帧里。

测试底盘：全仓 48 个测试套件、538 个用例，写作时实测全绿；ssh 模块的语句覆盖 43.26%——其中 InputBuffer 100%，GovernedExecutor 53%，SSHSession 只有 15%。这组数字本身就是一个故事，本章末尾再讲。

## 三、问题发现：状态机有个永远不关的门

9 月 6 日的加固提交（1b25e33）一口气修了三件事，提交信息的第一段是给提权逃逸的，措辞在本书出现的所有提交信息里几乎是最重的一次：NOPASSWD 场景下永远停在 PENDING_PWD，而该阶段的所有输入“绝不治理直接透传”——后续任意命令（哪怕 rm -rf）都绕过治理与审计。

把漏洞拆开看，它其实是两个缺陷的叠加。

第一层是出口缺失。进入 PENDING_PWD 后，handlePTYOutput 里的状态跃迁只监听两种文本：`SUDO_PASSWORD_PROMPT`（`[sudo] password for ...` 或 `Password:`）出现，说明 sudo 确实在等密码；`SUDO_FAILURE`（`sorry, try again` 或 `authentication failure`）出现，说明认证失败。加上 root 提示符命中后跃迁到 ROOT，这是仅有的几条出路。NOPASSWD 场景下，sudo 既不等密码也不失败，直接执行完毕回到提示符——而那个提示符是 `$` 不是 `#`，root 提示符的正则也不命中。状态机就地冻结。

第二层是权限过大。PENDING_PWD 这个状态在命令处理入口有一个短路：处于该状态就直接把输入透传，不评估、不记录。这个短路对真正的密码输入是必要的——密码绝不能进治理与审计。但状态机冻结意味着短路变成永久，一个为了保护二十个字符（密码）设计的豁免，变成了豁免此后整条会话的所有字节。安全设计里最忌讳的“临时豁免没有终点”，在这里以状态的形式实现了。

第三个问题藏在更基础的层面。ssh2 这个库默认**不校验服务器主机公钥**——建立连接时，它对对端出示的公钥照单全收。对 SSH 会话来说，主机指纹是整条信任链的地基：DNS 被劫持、ARP 被欺骗、或者干脆连到了一台同名的假服务器上时，客户端会把私钥、密码、乃至之后敲进会话的一切，恭恭敬敬地送到攻击者面前。治理做得再细，治理的是一个假会话。

第四个问题不涉及安全，但涉及“活着的定义”：连接没有 SSH 层保活。NAT 网关、防火墙静默丢弃空闲 TCP 连接是常态——连接死了，本地不知道，终端看着还在，敲下去的字进了黑洞，要等 TCP 层小时级的超时才报错。运维行话管这叫“假活”。

## 四、修复：看门狗、降级与指纹库

修复在同一夜完成，思路各不相同，但都遵循同一条纪律：不改变正常路径的行为，只堵异常路径的洞。

### 给 PENDING_PWD 装看门狗

提权逃逸的修复核心是一个定时器。现在，进入 PENDING_PWD 的那一刻——治理批准提权请求、即将发送命令之前——执行器会 arm 一只看门狗：

```typescript
private armPwdPromptWatchdog(): void {
  this.clearPwdPromptWatchdog();
  this.pendingPwdTimer = setTimeout(() => {
    if (this.elevation !== ElevationState.PENDING_PWD) return;
    this.elevation = ElevationState.USER;
    this.sensitive.exit();
    if (this.recorder) {
      this.recorder.recordGovernance('pwd_prompt_timeout');
    }
  }, this.pendingPwdTimeoutMs);
}
```

默认五秒，可配 `pendingPwdTimeoutMs`。五秒内密码提示出现了——看门狗撤掉，状态照旧等待密码；sudo 失败文本出现——照旧回退 USER；都没有（NOPASSWD）——五秒后强制回退 USER，敏感拦截器退出，审计里留下一条 `pwd_prompt_timeout`。透传特权的窗口从“永远”收窄到“最多五秒”，而且这五秒的终点必定伴随一条审计记录。

等密码提示的正则也值得一读：`(\[sudo\] password for .+:|Password:)`，大小写不敏感——前半截匹配 sudo 的标准提示，后半截的裸 `Password:` 兼容的是 `su` 换 root 时的提示。看门狗对应的另一头，是测试里的处理方式：单元测试不等真实的五秒，构造执行器时直接传 `pendingPwdTimeoutMs: 100`，再用 jest 的假时钟把时间推过阈值，毫秒级的验证瞬间完成。可配置的超时在测试里省下的是真实的时间，在生产里保住的是慢速链路上 sudo 提示穿越网络的余量。

清看门狗的位置要数得清：密码提示出现、认证失败、root 提示符命中，三处各清一次，加上定时器重进时先自清。修复注释里把这四个点逐一列明——看门狗最怕两件事，忘了上（漏洞依旧）和忘了撤（正常输密码输到一半被踹回 USER，密码前几个字符泄漏成普通命令）。后者尤其危险：一旦状态提前回落，密码的后半截会被治理层当成命令评估甚至记进审计。所以看门狗的回退只在“仍是 PENDING_PWD”时才动作，密码提示的正则命中后又刻意提前 return，不给其他跃迁抢跑的机会。

### 让 ROOT 学会降落

第二个修复针对“一次提权永久 ROOT”。旧状态机里，一旦看到 `#` 结尾的提示符就跃迁 ROOT，此后再也没有回 USER 的路径——sudo 单条命令执行完、root 会话 exit 回到用户 shell，状态还停在 ROOT。粗粒度的状态会污染治理语义：有些策略对 ROOT 与 USER 本该不同。修复是加了一条降级正则：匹配以 `$` 结尾的用户级提示符，且刻意排除带 `#` 的行——`$` 与 `#` 是 Unix 提示符的通用方言，前者属于普通用户，后者属于 root，状态机用这一对符号感知远端权限的潮汐。命中降级条件就回落 USER，审计记 `root_session_ended`。两条正则一升一降，状态从此跟随真实权限涨落，而不是一条单行道。

### TOFU：第一次相见，留下证件

主机校验的修复借用了 OpenSSH 沿用几十年的机制：TOFU——Trust On First Use，首次相见记下指纹，此后每次相见验明正身。ssh2 的 `ConnectConfig` 有 `hostVerifier` 回调，配上 `hostHash: 'sha256'`，拿到的指纹与 OpenSSH 的 `sha256:<base64>` 格式完全一致——用 `ssh-keygen -lf` 可以互相印证。指纹库是一个 JSON 文件，默认放在 `~/.yuangs/ssh_known_hosts.json`，每条记录是指纹加首次相见时间；文件权限 0600——注释写得很清楚：指纹库虽非机密，但防被静默篡改，因为篡改指纹库等于绕过 TOFU。

首次连接：记录指纹，放行。此后每次连接：指纹一致，放行；不一致——直接拒绝，并且把真实原因预先存好，拼在错误信息里返回给用户：`Host key for host:22 changed! expected ..., got ...`，末尾还指路：如果确信是服务器重装而非中间人，去指纹库里删掉那条记录再连。这里有个工程细节：ssh2 的 hostVerifier 返回 false 时，对外只报一句含混的“所有认证方法都失败了”，真实原因会被吞掉——所以代码在闭包里用 `lastHostKeyReason` 记下真相，error 事件时优先呈现。安全拦截不制造谜语。指纹库的读取也有容错：文件损坏或首次不存在时按空库处理，注释里补了一句诚实的话——损坏会在下次保存时被覆盖，而不是让工具从此起不来。紧急排障也留了门：`disableHostKeyCheck` 开关可以跳过校验，注释里连用两个警示符号，声明它等价于 `StrictHostKeyChecking=no`，仅排障用，用完必须改回。

### 15 秒一次的心跳

假活的修复最小：连接配置里加两行——`keepaliveInterval: 15000`，`keepaliveCountMax: 4`。每 15 秒一次 SSH 层探测，连续 4 次无响应即判死，60 秒内假连接会以真断连的形式暴露出来，重连逻辑接手。注释只有一句：NAT/防火墙静默丢弃空闲连接时，避免终端“假活”。

有意思的是它与第 3 章 wsstunnel 的心跳互为镜像：那边是应用层自定义的 `__PING__` 文本帧，为了穿过反向代理的空闲超时；这边是协议自带的 keepalive 探测，为了穿过 NAT 的静默丢弃。位置不同、机制不同，判断标准却是同一条：连接的活性必须由一种“廉价、定期、可数失败”的探针来声明，而不是由 TCP 栈小时级的超时来追认。

## 五、验证：6 个用例与分两次的 emit

修复的价值要靠测试钉住。这一轮补了两批测试，各有一处值得记录的细节。

第一批是 3 个状态机回归测试（随修复提交）。测试里没有真服务器：FakeSession 只把 write 记录下来，FakeGovernance 按预设放行或拦截，时间用 jest 的假时钟推进。第一条用例直接复现漏洞场景：`sudo -v` 进入 PENDING_PWD 后，把时钟推过看门狗超时，断言状态回到 USER——修复前，这条断言永远等不到。第二条验证密码提示出现后状态保持、超时未被提前触发。第三条最有意思，测试注释里写着一段实战经验：

```typescript
// 通过真实 data 事件驱动状态跃迁。
// 注意：密码提示与失败文本在真实 PTY 里是两块分开到达的输出，
// 且提示正则命中后会提前 return——必须分两次 emit。
emitter.emit('data', Buffer.from('[sudo] password for ygs: '));
emitter.emit('data', Buffer.from('sorry, try again'));
```

一次 emit 把提示与失败文本拼在一个 Buffer 里，测试也能过——但那是假象。真实的 PTY 输出是网络流，分块到达的时机不由人：提示先到一块，失败文本隔一会儿再到一块；而提示正则命中后处理函数会提前 return，同一块里的失败文本根本没机会被看到。测试必须忠实于真实世界的分块行为，否则它验证的是一个不存在的理想协议。这两行 emit 是把“网络是分块的”这条物理事实写进了测试。

第二批是 InputBuffer 的 6 个用例（283e70d，修复提交一小时后）。这个 76 行的小类是治理的入口闸门，6 个用例对应它的六种职责边界：无换行不触发治理；`\r` 触发并返回完整命令；跨块拼合——一条 git 提交命令分两次到达，前半 `git com`、后半是剩余部分，Enter 时必须拼回完整命令；退格回滚——`rm -rf /tmp` 后跟四个 `\x7f`，必须回吐 `rm -rf `，尾随空格保留属正确行为；多退不越界——缓冲里只有 `ab` 却退了三次，不能把别人的字符退掉；clear 与 peek 的辅助语义。退格处理是逐字符模拟终端行为，`\x7f`（DEL）与 `\b`（BS）等效，删的是字符流里的前一个字符，而不是显示上的视觉效果。闸门的每一个行为都被断言钉死，之后的任何改动都要从这六道门里过一遍。

跑数字：全仓 48 套件、538 用例全绿；ssh 模块语句覆盖 43.26%，InputBuffer 100%，GovernedExecutor 53%，SSHSession 15%。SSHSession 的低数字是诚实的：TOFU 逻辑写在 connect 的闭包里，验证路径要真连接、真握手、真指纹不匹配，单测够不着——它对应的验证手段是手动敌意测试：改一个字节的服务器指纹，看连接是否被拒。覆盖率表不追虚高，它只是把“哪里靠单测、哪里靠真刀真枪”标了出来。

## 六、反思

**状态机的正确性，出口和入口一样重要。** 写状态机的人习惯把注意力放在“什么条件下进入什么状态”，这个漏洞提醒我们另一问同样致命：每个状态能不能退出？PENDING_PWD 的透传豁免在设计图上没有问题，问题在于它的两个出口都依赖远端世界的特定文本，而远端世界恰好有一种配置让两种文本都不出现。修复用的不是重新设计，而是一只为异常路径准备的手表——五秒钟，一个可配参数，一条审计记录。给“等待”加期限，是所有与外部世界对话的状态机的共同必修课。

**豁免必须带着有效期。** 透传阶段的设计动机无可指摘：密码不进审计，这是对用户秘密的尊重。但“这一阶段的输入不治理”与“这个状态将持续到永远”叠在一起，就是灾难。类似的豁免在各处的安全设计里都存在——维护模式、调试开关、白名单——它们的共性风险是：豁免生效的条件可控，豁免结束的条件常常没人写。看门狗的本质，是给豁免写上一个别人拿不走的句号。

**治理的价值在边界条件里称重。** 平心而论，这个状态机在 Happy Path 上的表现无可挑剔：sudo 有密码、密码输错、提权成功，三条主路径的行为都对。漏洞藏在第四条路径——一条从没人替它规划过出口的路径。安全机制与普通功能最大的不同在于，普通功能在主路径上成立就叫工作正常，安全机制要在所有路径上成立才叫工作正常。测试驱动开发教会我们为典型场景写用例，安全工程则要求为“恰好不出现的提示”这样的反事实设计出口。

**信任链的起点不在会话里，在会话之前。** 提权逃逸、审计旁路，修的都是会话中的治理；TOFU 修的是会话还没开始时的那一步——你到底在跟谁说话。指纹库、0600 权限、与 OpenSSH 一致的 sha256 指纹格式、指纹变化时给出人话与出路，这些细节单独看都小，合起来是一句话：AI 时代的安全工具没有理由比二十年前的 OpenSSH 更粗心。有意思的是，TOFU 的“首次信任”本身也是一种豁免——它的看门狗是那道指纹变化即拒绝的闸门，以及被篡改成本抬到很高的 0600 指纹库。

**测试的职责是复现世界，不是证明代码。** 分两次 emit 那个细节值得单独记住。测试失败有两种：代码错了，或者测试模拟的世界错了。把提示与失败拼进一个 Buffer 的测试属于后者——它用理想化的输入让代码“通过”，同时放过真实世界里必然发生的分块。InputBuffer 的六个用例同理：跨块拼合用例存在的意义，就是承认“字符流不按命令的边界到达”这个不受欢迎的事实。测试不是写给人看的，是把世界的脾气写进自动化里的仪式。

**注释是写给修复之夜的自己人的。** 这次加固的另一个副产品，是把设计意图留在了离代码最近的地方。看门狗的注释用了一整段把旧漏洞的机制讲透，连“哪怕 rm -rf”这样的措辞都原样保留；TOFU 段落开头的注释先讲清楚“ssh2 默认不校验”这个事实，再讲 known_hosts 为什么存在；0600 权限旁边立着一行“篡改指纹库等于绕过 TOFU”。半年后的下一位读者——多半还是作者自己——不需要考古 git blame，就能在案发现场读到案情。源码注释的密度，本质上是作者对自己记忆力的不信任程度，这大概是一种健康的悲观。

这一夜在两个仓库里各落下一个提交，时间戳几乎挨着——wsstunnel 的注册鉴权重写是 03:25:16，yuangs 的 SSH 加固也是 03:25:16。两个项目，一条共同的纪律：发现即修复，修复即测试，测试即文档。至于那个 NOPASSWD 场景，修复之后它从“治理的漏洞”变成了“审计里的一条 pwd_prompt_timeout”——一个曾经无限长的窗口，如今五秒钟就能走完从洞开到关闭的全部流程。

---


# 第 5 章 · knasync —— 边缘内容队列

# 第 5 章 · knasync —— 边缘上的队列，与它怀里的两颗炸弹

九库之中，knasync 是最没有“运维感”的一个。它没有服务器可以登录，没有进程可以查看，没有 systemctl status 可以输出——整个系统就是一段 JavaScript，跑在 Cloudflare 的边缘节点上，数据睡在 D1（Cloudflare 的 Serverless SQLite）里，对外的门面只有一个域名：api.want.biz。探测它的那个凌晨，别的项目要翻日志、查端口、盯内存曲线，它只需要一条 curl。

但恰恰是这个看起来最省心的项目，贡献了本次探测中最典型的一类病灶：修复只落在代码里，从没有落回部署脚本。引爆它不需要任何攻击者，一次例行的“重跑部署”就够了。而且不是一个雷，是两个。再加上一条藏在接口语义里的数据丢失路径，这一章要讲的是三次修复、一次测试从零到一，和一场关于文档时效的核验。时间线本身也值得记下一笔：部署脚本的修复落在九月五日凌晨五点四十二分——探测开始后不到三小时；参数必填与单元测试的提交落在九月六日凌晨。三场修复，横跨了这次三十多小时探测的头尾。

## 一条链接的边缘之旅

knasync 的自我定位写在 README 第一行：基于 Cloudflare Workers + D1 数据库的轻量级内容队列分发系统。翻译成日常语言，它是一部多端内容的中转站，围绕“生产者—消费者—订阅者”三层模型运转。

生产者在入口。手机上看到一篇知乎文章，用快捷指令把链接甩给 POST /submit；Chrome 插件、浏览器脚本走同一条路。Worker 收到内容后自动分流——isZhihuUrl 用一组正则匹配 zhihu.com 的 question、answer、专栏文章路径，命中就进 zhihu 队列，其余落 general 队列，wechat 队列留给显式指定的调用方。消费者在中间。Chrome 扩展轮询 GET /pull?queue=zhihu，把知乎链接抓走、抓取正文转成 Markdown；Knowly 守护进程用 /pull?queue=general 拉走通用任务；WeClaw 则用 POST /push 把问答对和断线通知推回系统。订阅者在出口。消费者处理完成的成品写进 results 广播表，手机快捷指令和 Knowly ResultPuller 各自持有游标，用带 since 参数的 GET /results 增量读取——同一条结果，所有订阅者各取所需，互不干扰。代码库末尾的 clientMapping 表把这层关系钉死在文档里：哪个客户端、做什么动作、调用哪个端点，七行映射，一目了然。

除了这条主干，Worker 还捎带了几件杂活。/peek 提供只读的队列视图，不删除任何记录，把时间戳转成北京时间，供浏览器地址栏直接查看；/clear 一键清空队列；/health 无鉴权地报告 D1 连接状态；/publish 是统一发布端点，按 targets 参数把一篇文章并行发往 IMA 笔记、博客和播客三条通道——中转到 api.yuangs.cc，每个目标二十秒超时，单个目标失败不影响其他目标。历史域名 knasync.want.biz 与 workers.dev 地址都是同一个 Worker 的别名，所有客户端已统一收敛到 api.want.biz。

/push 的设计里还有一个容易错过的细节：它支持两种模式。完整推送带 content 与 original——正文写入 results 广播表，原始链接写入 consumed 永久去重；ack-only 只带 original——消费者本地已经处理过、不想再广播一遍，但必须告诉 Worker 把这条内容标记为已消费，否则它下轮还会被投递。Knowly 的 relay 走的就是 ack-only。一个广播接口同时承担回执职责，省掉了一个专门的确认端点——对零依赖的单文件系统来说，每个端点都要精打细算。

支撑主干链路的是 D1 里四张表，职责泾渭分明：

| 表 | 职责 |
|---|---|
| queue | 工作队列，zhihu／wechat／general 三类共用，每类上限 50 条 |
| submitted | 30 秒滑动窗口去重，防快捷指令连发 |
| consumed | 永久去重，消费确认后 30 天内不再投递 |
| results | 广播结果，保留最新 50 条 |

代码层面有几个值得点名的正确设计。/pull 的“取到即删”不是先 SELECT 再 DELETE 两步走，而是单条 DELETE … RETURNING——两次往返之间的竞态窗口根本不存在，并发拉取不会重复投递，拉取间隙新提交的内容也不会被误删。/submit 的去重检查把 consumed 与 submitted 两条查询合并为一次 batch 往返；入队、写入去重记录、清理过期记录、50 条截断，再合并成一个事务 batch——D1 的 batch 是隐式事务，语义正确，往返开销也省。/results 用自增 id 做游标而不是秒级时间戳，避免同一秒多条结果被游标跳过，还保留了对旧版时间戳游标的自动收缩迁移逻辑——旧客户端换上新语义不会卡死。鉴权用 safeCompare 做逐字符异或比较——长度不等时提前返回，严格说仍泄漏密钥长度信息，但在个人基础设施的语境里，这份 timing-safe 的自觉已经超出平均线。

这套架构的另一层底色是省钱。D1 免费档有每日写入限额与库容量上限，Workers 免费档限制单请求的子请求数——batch 合并往返、写时修剪、过期清理，这些“正确设计”同时也是配额经济学：每省一次往返，就省一份延迟与一份配额。个人基础设施的性能优化，要义往往不是快，而是不超支。

还有一个常被忽略的事实：这 979 行代码零外部 npm 依赖。没有 express，没有任何第三方包，纯 Workers 原生 API。它甚至自带文档——根路径根据 Accept 头返回渲染好的 HTML API 文档或 JSON，每个端点的 curl 示例和错误码都齐了。单文件、零依赖、自带说明书，作为个人基础设施的工程直觉相当好。

提交、去重、消费、回执、广播，五个环节串成一条闭环：一条链接从手机屏幕出发，在 D1 里排队，被扩展抓走，以 Markdown 的形态回到广播表，最后出现在每个订阅者的时间线里。整条链路上，没有一台属于自己的服务器。

## 二十五条发现：6 分的报告

探测当日，knasync 收到一份只读代码审查——《knasync 优化分析》。报告基于当时代码版本 8884140（就在前一天，那次性能重构刚把 D1 往返砍掉一半、把 /pull 改成原子操作、把 /publish 改成多目标并行），给出 25 条发现，按 P0（严重）、P1（重要）、P2（改进）三级排列：P0 两项，P1 十项，P2 十三项。综合评分 6 ／ 10。

分数的内部结构比分数本身更有信息量：架构设计 7.5 分，D1 使用效率 8 分，代码质量 7 分——设计和实现都在水准之上；工程实践 3 分，部署配置 5 分——拖垮总分的是同一类问题：最后一次认真维护之后新增的补丁，没有回填到旧设施里。consumed 表就是标本。它在线上跑得好好的，仓库的 schema.sql 里也有它，但 deploy.sh 里内嵌的那份建表语句从来没有跟上它的脚步。

报告的风险清单读起来并不轻松：无限流，单一共享密钥一旦泄露即可无限速刷 /submit、刷有外部副作用的 /publish；密钥支持 key 查询参数走 URL，会落进访问日志与浏览器历史，而 HTML 文档还把这种用法当作示例教给用户；首页文档无鉴权，任何人 curl 一下就能拿到全部端点、三个队列各自的消费者、内部上游的调用方式乃至 D1 表结构——一份完整的侦察地图；CI 没有任何检查就直推生产，push 到 main 分支即部署，workflow 里的 SECRET_KEY 环境变量写法还是无效配置，容易造成“以为改了密钥其实没改”的错觉；git 里误提交了本地 D1 数据库文件和 Cloudflare 账号缓存；queue 表没有 TTL，五天前没人拉的内容仍会被照常投递；清理逻辑寄生在 /submit 上，纯消费期去重表只增不减；外层 catch 会把内部错误信息原样回显给客户端。

P2 条目则像一份未来故障的预告片：results 的 50 条截断会让落后太多的慢游标客户端静默丢条目；拉取排序依赖各边缘节点时钟生成的 created_at，跨机房毫秒级偏移下消费顺序可能乱序；/submit 的先查后写在并发下可以双写；/clear 不清 submitted 与 consumed，调试时清空之后去重状态残留，容易造成“为什么提交没反应”的困惑。每一条单看都小，合起来就是 6 分里被扣掉的部分。

报告也不是只挑刺。无 SQL 注入面（全部占位符加 bind，全文核对无一例外）、原子拉取、游标分页、写时修剪、冗余索引的幂等清理（results 走主键游标分页后，created_at 索引只剩写放大，schema.sql 末尾一句 DROP INDEX IF EXISTS 把它收拾干净）——这些都被逐条确认为正确设计。报告的结论写得克制：这是一个设计思路相当好、D1 使用姿势熟练的个人基础设施项目，核心链路的实现质量高于多数同类玩具，但处于“跑得挺好、却经不起一次重部署或一次密钥泄露”的状态。好的部分给正面确认，坏的部分给出文件、行号和修法。这份报告本身就是本章后续所有修复工作的施工图。

仓库里还住着一个配角，也被报告扫到了：mcp/server.py，一个统一的 MCP 服务器，把搜索、发布、上传整理成八个工具，供 AI 客户端调用。报告发现其中两个工具引用了仓库里根本不存在的脚本，调用时子进程必然报错，工具清单却仍对外宣称可用。能力声明与实际能力不符，对人是虚假广告，对 AI 是事故源头——agent 会一次次尝试一个注定失败的工具，然后把 stderr 垃圾当线索。这条 P1 也留在了队列里。

## 炸弹一：部署脚本里的旧地图

P0-1 的病灶在 deploy.sh 第六步、第 120 行往下的 28 行 heredoc 里。

deploy.sh 是一条十步的一键部署流水线：检查环境、确认 Cloudflare 登录、创建目录、配置 D1 数据库、重写 wrangler.toml、初始化表结构、检查 Worker 代码、设置密钥、部署、冒烟验证。思路很好——个人项目里愿意把部署写成脚本的已经不多。问题出在第六步。修复前，那里的逻辑是：

```bash
cat > "$SCHEMA_FILE" << 'EOF'
CREATE TABLE IF NOT EXISTS queue (...);
CREATE TABLE IF NOT EXISTS submitted (...);
CREATE TABLE IF NOT EXISTS results (...);
-- 28 行里没有 consumed
EOF
wrangler d1 execute $PROJECT_NAME --file="$SCHEMA_FILE"
```

这段 heredoc 内嵌了一份旧版建表语句——只有 queue、submitted、results 三张表，没有 consumed，还保留着已被现行 schema 幂等废弃的 idx_results_created 索引。更要命的是它的第一行：cat 带重定向符号直接覆盖 schema.sql。也就是说，每次重跑部署脚本，它会先用这份三表的旧地图覆盖仓库里四表的 schema.sql，然后把这个被覆盖过的文件执行到 D1 上。仓库里的“唯一事实源”被部署脚本反噬——教科书式的双份定义漂移：两份 schema 都觉得自己是对的，而错的那一份握着写入权。

后果分两层。仓库层面，schema.sql 被静默回写成三表版本，git 工作区变脏，文档与代码的漂移被脚本固化。数据库层面则要看运气：现网 D1 里 consumed 已经存在，CREATE TABLE IF NOT EXISTS 不会删它，所以对现网重跑无感；但如果哪天在新库上执行——换账号、灾备重建，或者就是想从零再来一遍——consumed 永远不会被建出来，而 Worker 代码在 /submit 里无条件查询它做永久去重。每个提交请求都会撞上 no such table: consumed，被外层 catch 包装成 500 返回。队列从此收不进任何东西，手机快捷指令那头只会看到一个没来由的失败。

修复朴素到只有一句值得说：删掉整段 heredoc，脚本改为直接执行仓库中的 schema.sql，文件不存在就报错退出。schema.sql 本身是幂等的（IF NOT EXISTS 加 DROP INDEX IF EXISTS），重复执行安全。如今 deploy.sh 的注释里写着完整的前因后果——“之前这里用内嵌的旧版建表语句覆盖 schema.sql（缺 consumed 表），重跑一次部署就会让 /pull 全部失败”。顺带做一次较真：严格说断点不在 /pull 而在 /submit 的去重查询——但队列瘫了就是瘫了，提交信息里的小误差不影响病灶诊断的正确。也顺带记录一个没有被这次修复动到的角落：第五步重写 wrangler.toml 的逻辑保留了——它写入的是当场从 D1 查到的真实 database_id，倒不算撒谎，只是“配置也应由仓库一源管理”这条更彻底的路，留给了将来。

## 炸弹二：test1234

P0-2 的病灶只有一行：

```bash
SECRET_KEY=${SECRET_KEY:-test1234}
```

部署脚本在第八步交互式询问密钥，操作者直接回车，就会用 test1234 作为生产 API 密钥部署上线。而这一把钥匙管着所有端点——包括能清空全部队列的 /clear，和能向 IMA、博客、播客三处发布内容的 /publish。api.want.biz 暴露在公网，配合无限流的现状，弱密钥等于把队列的读写权、清空权、发布权一起挂在大门上，还附赠一份无鉴权的侦察地图。

这个默认值的来历不难猜测：开发期图省事，把“回车跳过”做成了特性，然后一路活到了生产。它最危险的地方恰恰是平时的安静——只要没人重跑部署，test1234 就潜伏在脚本里，等着某次深夜重建环境时被一个困倦的回车唤醒。

修复采用了报告两个建议里更温和的那个：回车不再落到默认值，而是随机生成强密钥，仅此次显示并提醒妥善保存。

```bash
if [ -z "$SECRET_KEY" ]; then
    SECRET_KEY=$(openssl rand -hex 32)
    echo "已自动生成随机密钥（仅此一次显示，请妥善保存）"
fi
```

选择自动生成而不是强制退出，照顾的是一键部署的完整性——这条十步脚本不该在第八步因为用户懒得想密钥而断掉。至于现网密钥在修复之前是否真的做过 test1234，代码与日志里已无从考证；这次重新部署顺手完成了一次密钥轮换，谜底不必揭晓——从今天起它是一个 32 字节的随机数，而且只显示一次。与它相关的后续改造也已在报告里排队：单一共享密钥没有角色区分，提交、消费、清空同权，拆分成 producer、consumer、admin 多把钥匙是下一阶段的事。

## 把回退逻辑变成 400

第三个修复不在 P0 清单里，却是三条中对数据安全最直接的：/pull 的 queue 参数从可选变成必填。

背景还是 /pull 的语义——删除式拉取，取到即删。旧版为了兼容历史调用方，允许不带 queue 参数：先拉 zhihu，空了拉 wechat，再空了拉 general，逐个队列删除式地扫过去。这个回退逻辑在系统只有一个消费者的年代无所谓，但当 Chrome 扩展守着 zhihu 队列、Knowly 守着 general 队列之后，它变成了一条真实的数据丢失路径：任何一个忘了带参数的调用方——一次手敲的 curl 调试、一段旧版脚本、一次照着过期文档写的集成——都会把 zhihu 队列整体拉走删掉。那些链接本来该由扩展消费、转成 Markdown、推回广播表，现在直接蒸发，连 consumed 表里都不会留下记录，想补都补不回来。更隐蔽的是，这条路径藏在“兼容”的名义下：它平时表现完美，只在多消费者竞争的那一轮才显形，而显形即事故。

修复把回退逻辑整个删掉：

```
GET /pull                      → 400  Missing required 'queue' parameter
                                       (one of: zhihu, wechat, general)
GET /pull?queue=queue_zhihu    → 200  （兼容带前缀的历史写法）
```

改动敢这么干脆，是因为先验证了兼容性——现存的两个真实消费者，knowly relay 用 /pull?queue=general，zhihu_extension 用 /pull?queue=queue_zhihu，全部显式带参（normalizeQueueName 至今兼容 queue_ 前缀的历史写法）。一个耐人寻味的细节：修复后的 deploy.sh 第十步冒烟验证，用的恰恰是带 queue_general 前缀的写法——兼容层与必填校验在同一个请求里各司其职，旧写法照常工作，沉默的猜测被拒之门外。没有一个线上调用方依赖回退，于是零破坏。

修复后的 /pull 也有了一套完整的响应码契约：带合法 queue 返回 200 与内容数组；队列空返回 204——204 不允许携带响应体，代码里为此专门用无 body 的 Response；参数缺失与参数非法各返回 400。接口不再猜测，也不再沉默，每一个分支都有明确的说法。删除式接口的参数必填化，本质是把“猜调用方想要什么”改成“让调用方声明自己是谁”——在队列系统里，沉默的默认值往往就是事故的别名。

## 测试从 0 到 1

报告 P1-10 的措辞很直白：零测试。package.json 里的 test 脚本一直是脚手架原样——那句 error: no test specified。去重窗口、游标迁移、50 条截断、队列回退，这些核心逻辑全靠手工验证；而最近那次性能重构恰恰是最容易引入回归的改动类型。

补测试的第一步就撞上报告里那条 P2-25：package.json 的模块类型声明写着 commonjs，而 src/index.js 通篇是 ESM，export default 开路。Wrangler 自行打包，所以线上无碍；但 Node 的原生测试运行器一跑，它按 CommonJS 规则解析 src/index.js，遇到 export 语法当场报错。修正只需要把类型改成 module——半行配置，挡住的却是整个测试化的入口：

```json
{
  "scripts": { "test": "node --test tests/" },
  "type": "module"
}
```

一条 P2 建议的验证方式，就这样变成了 P1 落地的第一块垫脚石。

设计上做了一个务实的取舍。报告建议的是 vitest 加 miniflare 的 D1 集成测试，落地的是零依赖的纯函数测试。理由朴素：pullQueue 依赖 env.DB，测它得先造一个 D1；而 normalizeQueueName 与 isZhihuUrl 是不碰任何外部世界的判断函数，最适合做单元测试的第一批居民。为此 index.js 末尾专门加了一行纯函数导出——四个函数与一个队列名集合——不影响 Worker 的默认导出行为，却让提交分类的核心判断第一次可以被断言。tests/pure.test.mjs 四个用例、十五处断言：队列名归一化验证 queue_general、queue_zhihu、queue_wechat 前缀写法与裸名的等价，外加空串原样返回；isZhihuUrl 一侧覆盖 question、answer、专栏路径，乃至首尾带空白的链接——手机快捷指令分享出来的 URL 常拖着看不见的空白；另一侧覆盖伪造域名 evil.com、ftp 协议、空字符串、null、数字的拒绝。跑起来的样子从此不一样了：

```
✔ normalizeQueueName：带前缀与裸名等价
✔ VALID_QUEUES 包含三条队列
✔ isZhihuUrl：合法知乎链接识别
✔ isZhihuUrl：非知乎/非法输入拒绝
ℹ tests 4  pass 4  fail 0
```

半秒出结果，不装任何包。从 0 到 1 的意义不在数量。这四个用例守住的 isZhihuUrl，是手机快捷指令到队列之间的第一道分流闸门——它错认一个字符，一条链接就会进错队列，然后被错误的消费者拉走删除。normalizeQueueName 守住的则是另一头：老消费者带着历史前缀来，新校验不能把它们挡在门外。测试先盯住最纯粹、最核心的判断写，D1 集成测试留给下一阶段——先有，再好。

## 文档核验：说真话的一半与说旧话的一半

本书的方法论是不信文档，信命令。对 knasync 的文档做时效核验，结果一好一坏。

好的那一半：README 与 API 文档关于去重的描述——“30 秒滑动窗口去重（防连发）+ 消费确认后永久去重（consumed 表，记录保留 30 天）”——与代码完全一致。src/index.js 第 6 行，DEDUP_WINDOW_SEC = 30；第 7 行，CONSUMED_RETENTION_SEC = 30 × 86400。核验时还有一层曲折：最初的任务描述里写的去重窗口是“5 分钟”，报告对照代码当场纠正为 30 秒。三份材料摆在一起——任务描述、README、代码常量——最后赢的是常量。文档会说旧话，转述会走样，只有代码不撒谎。所谓“描述已是最新”，标准就是这句话：README 里的每一个关键数字，都能在代码里找到一个常量或一条分支与之对账。

坏的那一半：/pull 参数必填化之后，四份文档原地踏步。README 的特性列表仍写着“优先级拉取——支持 zhihu > wechat > general 三级优先级回退”；README 的参数表、API.md 的两处、使用指南的两处，连同 index.js 内嵌文档里的 required: false 与“不传时按旧版逻辑”，全都还在描述一个已经不存在的回退行为。一个刚照着官方文档接入的新调用方，会理直气壮地不带参数调 /pull——然后收到 400。行为修复与文档同步之间隔了两个提交——这一次它没被补上，被如实记录在案，留给下一次提交。这里有一条对维护者的提醒：内嵌在代码里的文档是文档里最容易腐化的一种，因为它离行为最近，反而最容易被假定“肯定是新的”。

把账算清楚：这次核验覆盖四份文档——README、API.md、使用指南、内嵌文档。去重相关描述四处全对，拉取回退描述五处全旧。同一个仓库，同一个时间点，文档的一半活在当下，一半留在昨天。这不是 knasync 独有的病，是所有“文档靠手同步”的项目的共同宿命——除非有一天，文档也变成从代码生成的产物。

## 收尾：修到“重跑一次”也安全为止

三个提交落地之后，knasync 的状态可以这样描述：a88a7a9 让部署脚本与仓库 schema 归于一源、密钥不再有默认值；f919875 关闭了删除式接口的无参回退；dedddc9 让核心判断有了第一批断言。25 条发现里消掉了两条 P0 和一条 P1，其余的——限流、URL 传密钥、无鉴权文档页、CI 前置检查、git 数据清理、D1 集成测试——在报告里排着队，等下一个被分配的维护窗口。排队的意义在于它们被写了下来：每一条都有文件、行号与修法，下一个人接手时不需要考古。

回看这三刀，落点并非随机：两条 P0 是会炸的，/pull 那条 P1 是会丢数据的，测试是防复发的。修复顺序即风险顺序——先拆引爆装置，再堵数据丢失路径，最后给核心判断上锁。这不是全部，但这是正确的次序。

这一章真正想留下的是一个判断标准：个人基础设施的修复，要修到“重跑一次部署”这个动作变得安全为止。deploy.sh 的旧地图之所以定级 P0，不是因为它每天都在爆炸——它安安静静躺了几个季度——而是因为它让“重新部署”这个最常规的运维动作变成一场赌博。test1234 之所以定级 P0，也不是因为它一定被用过，而是因为它让“直接回车”这个最人性化的操作成为生产事故的入口。系统健康与否，不看它今天跑得顺不顺，看它经不经得起重跑、重建、换库这些非常规但迟早会来的时刻。

6 ／ 10 的评分里，工程实践的 3 分拖垮了一切。而把 3 分往上抬的，从来不是新功能，是这类枯燥的回填。

---


# 第 6 章 · wxwatcher —— 文件变更哨兵

# 第 6 章 · wxwatcher —— 盯着十一万个文件的哨兵

2026 年 9 月 6 日 5 点 07 分 02 秒，本机一份日志里落下一行记录：

```
2026-09-06 05:07:02,049 INFO [OK] 推送变更批次 1，共 13 项
```

那 13 项变更的落点，与《九库五机》的 chapters 目录在这台机器上成形的时间（5 点 01 分到 5 点 07 分）严丝合缝。也就是说，这本书的诞生，被一个后台进程全程看在眼里，并且逐批推送进了微信。这个进程就是 wxwatcher——以 LaunchAgent 常驻的文件变更哨兵，探测当日进程号 76315，每 30 秒对整个工作目录做一轮扫描。它盯着用户的代码库，也盯着它自己的源码仓库，还盯着讲述它故事的那一章。修复它的提交落盘的那一刻，推送就响——哨兵直播了自己的治疗过程。

它的职责用一句话就能说完：任何文件变了，让主人在微信里第一时间知道。这句话反过来读同样重要——没变，就一个字都不要发。本章要讲的三个修复，全都在为这句话的两半兜底：报得准，也闭得上嘴。

它看管的规模值得敬畏：基线状态文件 23.7MB，记录着 113452 个文件的修改时间、大小与 SHA256。每 30 秒把整个目录树重新走一遍，却几乎不产生可感知的负载——秘诀藏在“两阶段扫描”这四个字里。而这一章要讲的，是哨兵在这次探测里被找出的三处暗伤、各自的修复过程，和第 108 个测试从写法到证明的完整故事。

## 哨兵的构造

wxwatcher 在九库里的身份有些特别：它是唯一发布到 PyPI 的一个。pip install wxwatcher 装的就是它——当前版本 1.15.0，MIT 许可，Python 3.9 以上可用，唯一的硬依赖是 httpx，发推送用；想让 YAML 配置文件生效，再装一个可选的 config 附加项。README 的徽章、CI 里的 publish 工作流、仓库里的 systemd 服务单元一应俱全——它在 Linux 上也能当守护进程，但在本机，它以 macOS 的 LaunchAgent 形态常驻，KeepAlive 挂着，进程退出会被自动拉起。

它的核心是一条严谨的流水线。第一站 fast_scan：用 os.walk 加 os.stat 遍历全部文件，只取 mtime 与 size，不读任何文件内容。第二站 detect_changes：拿这份数据与基线做三类集合运算——新增的、消失的、两边都在的；只对“两边都在但 mtime 或 size 变了”的疑似文件计算 SHA256，确认真变了才上报。纯元数据对比撑起了第一道过滤，113452 个文件的一轮快扫，绝大多数时候一个哈希都不用算。首次建基线时哈希才是大头，于是它把哈希扔进线程池——读文件会释放 GIL，最多 16 个 worker 并行，大目录的首轮耗时随核数近似线性下降。这是 1.14.0 的功课之一。

代码的分工与职责一样清爽：watcher.py 管扫描、检测与状态持久化，是检测核心；notifier.py 管微信推送与上传，捎带一个通用的指数退避重试器；config.py 与 config_file.py 合管四层配置；cli.py 把参数解析、主循环、日志与消息格式化缝在一起。五个模块、六百三十九条语句，依赖单向流动——主循环调核心，核心不回头看主循环。没有变更的轮次也不全然无声：每十分钟，日志里会落一条“无变更”心跳，证明哨兵醒着。

哈希函数本身有几个务实的特殊条款：空文件直接记 EMPTY；超过 10MB 的大文件只取前 8KB 做近似指纹，记作 LARGE 前缀的专用格式；读不出来的文件记 ERROR。三种特殊值都不是敷衍——每一种都对应一种真实世界的状态，而其中两种，各自埋着本章要讲的一个故事。

推送链路同样层层设防。变更按每批 50 条分批，单轮封顶 100 条——1.13.0 加的，提交说明写的是防止微信刷屏；消息经 httpx 发往 api.yuangs.cc 的 weixinpush 接口，Bearer token 鉴权，15 秒超时，失败自动重试三次、退避间隔按 2 的幂增长；每条通知都打上来源主机名——1.15.0 加的，多机部署时一眼分清是谁在喊。状态持久化是原子的：先写临时文件再 os.replace，中途断电也不会留下半个残缺基线；基线文件还按监控目录的哈希命名，多实例互不踩脚。基线的加载容错也值得一句：文件缺失、JSON 损坏、字段缺失，一律当作空基线处理——宁可下一轮全量重报，也不在残缺状态上继续盖楼。对一个把基线当记忆的系统，记忆坏了就坦白失忆，比装作记得更安全。忽略规则支持三种写法——正则、通配符、精确名——外加叹号前缀的取消规则；目录剪枝只应用名字类规则，扩展名过滤若也对目录生效会整棵子树误剪，这个坑在 1.14.0 填过。配置是四层合并：CLI 参数、环境变量、YAML 文件、默认值。SIGTERM 与 Ctrl+C 走同一条优雅退出路径：先保存状态再走人，重启后增量对比，不重建基线。

LaunchAgent 的参数单是这个哨兵的真实履历：30 秒一轮，推送地址指向 api.yuangs.cc，忽略清单是一长串——日志、音频、pnpm 存储、node_modules、.git、所有点开头的隐藏文件。探测期间翻它的运行日志，凌晨时段一串 [OK] 推送接连不断——那正是这次三十多小时探测自身在磁盘上制造的变更。哨兵没有放过其中的任何一条。

它还长着两只辅助的手。第一只是上传：检测出的变更文件可以顺手推到 Knowly 剪贴板服务，支持 Basic Auth，命令行不给地址就安静禁用——哨兵只管看，没安排的差事不硬揽。第二只是日志：运行日志写入用户目录下的 .wxwatcher，按监控目录隔离、支持轮转，出事时它是第一现场——本章开篇那条 5 点 07 分的记录就出自那里。命令行还备着两个排练用的姿势：dry-run 演练模式只检测打印、不推送不落盘；once 单轮模式跑一轮就退出，把自己交给 cron 或 systemd timer 打发。常驻与定时两种活法，同一套代码。

版本史本身就是一部缺陷修复编年史：1.6.0 给微信推送加上 Bearer 鉴权；1.10.0 补类型修正、CI 测试与边界用例；1.13.0 封顶单轮变更条数防刷屏；1.14.0 修扩展名剪枝、引入并行建基线、加单轮模式与演练模式、统一 SIGTERM 优雅退出；1.15.0 给每条通知打上来源主机名。版本号稳步爬升的节奏里，没有一个炫技的大特性，全是磨损处的补胎——对一个常驻数月的进程，这是它该有的样子。

## 误报风暴：目录消失的那一轮

第一处暗伤是 P0 级的，藏在一个几乎所有 Python 程序员都用过、却极少有人细想的函数里：os.walk。

它的行为是——对不存在的目录，静默返回空结果。不抛异常，不报错，安静得仿佛目录里本来什么都没有。wxwatcher 恰恰依赖它做每轮扫描。于是有一条清晰的事故链：监控的目标目录挂在外置盘或网络盘上；某天盘被卸载、网络盘掉线，目录从文件系统里暂时消失；下一轮 fast_scan 照常执行，os.walk 静默返回空；detect_changes 拿空状态与基线对比——“两边都在”的集合是空的，“消失的”是全部。基线里的每一个文件都被判定为删除，推送出去，然后基线被清空。等盘挂回来，全部文件又变成新增，再来一轮推送风暴。对一个基线里有十一万个文件的哨兵，这是两次十一万条的误报，中间还夹着一次基线的彻底失忆。

更讽刺的是代码里其实有防御：cli.py 的异常处理里写着 errno.ENOENT 的分支，注释说监控目录消失就干净退出、不抛 traceback。但它是死代码——os.walk 对缺失目录根本不抛 OSError，这个分支永远不会被执行。防御写好了，却守着一个不会发生的异常；真实发生的故障，从防御的头顶上走了过去。

做一道算术题可以把风暴的形状看得更清楚。单轮 100 条的封顶意味着推送端不至于真被十一万条消息打死——风暴被压缩成一条 100 项的变更简报，外加截断提示。但真正的伤害在基线：消失的文件本就从新状态中除名，落盘之后基线归零。等磁盘挂回来，十一万个文件集体变成新增，再来一轮封顶简报。误报以百计、状态已清零、真相被两次简报掩埋——哪怕有减震器，这也算不上一次可以接受的颠簸。它的现形靠的不是事故，是代码走读：一边是 os.walk 的语言级行为，一边是 ENOENT 分支的注释，两相对照，死代码的身份自明。

修复的思路不是让 os.walk 抛错——做不到——而是每轮扫描前先问一句目录还在不在。修复后的主循环开头多了这样一段：

```python
if not os.path.isdir(watch_dir):
    # 目录暂时消失（外置盘未挂载/网络盘掉线）时 os.walk 会静默
    # 返回空结果，若直接对比会把全部文件误报为删除并清空基线。
    missing_rounds += 1
    if missing_rounds >= max_consecutive_errors:
        save_state(state, watch_dir)
        sys.exit(1)
    logger.warning(f"监控目录不存在（第 {missing_rounds} 轮），跳过本轮")
    time.sleep(cfg.poll_interval)
    continue
missing_rounds = 0
```

缺失就跳过本轮、记一条 warning；连续 10 轮缺失才认定目录真被移除，保存状态后退出。10 这个数与既有逻辑里连续错误的最大容忍轮数对齐，按 30 秒一轮换算，等于给了外置盘 5 分钟的重新挂载窗口——U 盘插拔、网盘重连，都够用了。目录恢复后监控自动继续，基线无损。单轮模式同样跳过并保留状态——那种模式是给 cron 与 systemd timer 准备的，下一次定时任务自会接着看。那条 ENOENT 死分支留在了原地：它依然不会被执行，但作为纵深防御的一层，删不删都不再要紧。还有一个容易忽略的受益者是基线本身：缺失的轮次既不检测也不落盘，基线原地封存；目录回来，记忆分毫不差。监控的连续性，最终是靠基线的连续性实现的。

## 死循环：永远哈希失败的文件

第二处暗伤在同一片代码里，主角是三种特殊哈希之一的 ERROR。

场景：一个文件变了——mtime 或 size 与基线不符——哨兵去算它的 SHA256，但文件读不出来：权限被收紧，或者正被另一个进程以独占方式写着。sha256_file 捕获异常，返回 ERROR。修复前的处理是：记一条 warning，保留旧状态条目，跳过。听起来稳妥，实际是灾难的配方。旧状态里存的是旧的 mtime 和 size，而本轮扫描已经看到了新的；下一轮对比，二者依然不符，于是再次触发哈希，再次失败，再次沿用旧状态——每 30 秒空转一次哈希，永不停止；而“沿用旧状态”这个动作里不含任何上报，这个文件的变化就永远不会出现在任何推送里。死循环与静默漏报，在同一行代码里同时成立。一个每 30 秒重试一次、永不成功、也永不报告的任务——这几乎是西西弗斯在文件系统里的精确投影。

现实中能踩进这条路径的场景比想象中多：权限被刻意收紧的密钥文件、被编辑器原子替换瞬间恰好扫到的文件、被其他进程独占锁住的数据库文件——任何一次读失败，都会把哨兵拖进这个每三十秒一次的无声空转。代价有两笔：一笔是浪费，每轮一次注定失败的哈希加一条日志噪音；另一笔更贵——这个文件真正的变更通知，从第一轮起就永远缺席。

修复把语义掰正了：哈希失败不等于什么都没发生。mtime 和 size 的变化本身就是证据——按这个证据上报一次修改，同时把本轮的 mtime、size 连同 ERROR 一起记入新基线。

```python
if new_hash == "ERROR" or old_hash == "ERROR":
    logger.warning(f"无法计算哈希，按 mtime/size 变化上报: {fpath}")
    changes.append(f"[修改] {rel} ({fmt_size_diff(new_size - old_size)})")
    changed_files.append(fpath)
    new_state[fpath] = (new_mtime, new_size, new_hash)
    continue
```

下一轮如果文件没再变，mtime 与 size 与基线一致，直接跳过——循环断了。如果又变了，新证据与旧证据不符，再次按证据上报——该报的不会漏。条件里还留着 old_hash 一侧的判断：基线本身就是 ERROR 的文件，后续任何元数据变化照样被看见。顺带说清 ERROR 基线的另一面：它不是把失败洗成了正常。下一轮权限恢复、文件可读，而内容其实没变——mtime 与 size 与基线一致，直接跳过，一次读不出的误会不会制造假告警；若内容真变了，旧基线里的 ERROR 会让它再次按证据上报，并换上新状态。失败被记为一种状态，而不是被忽略。一个拿不到内容指纹的文件，从“永远沉默的负担”变成了“按元数据上报的正常对象”。

## 漏报：消息丢了，基线却存了

第三处暗伤不在检测层，在检测与持久化的交界处——分布式系统里一个经典错误的个人版。

旧的主循环逻辑是三步顺序执行：detect_changes 返回新基线；分批推送变更；保存新基线。推送结果无人检查。send_wechat 失败会重试三次，但三次全失败之后呢？主循环不知道失败这回事，照样把新基线落盘。下一轮扫描，文件没有新变化，与已更新的基线一致，一切安好。那条没送出去的微信消息，就永远丢在了那一轮里。检测到了，推送了——失败；记住了——基线更新成功。唯独没送达。对一个以“把变更告诉主人”为唯一职责的哨兵来说，这是职责意义上的全败，而且是最隐蔽的一种：日志里甚至有这次推送失败的记录，但状态机不知道。

修复引入一个 push_ok 标志：任何一个批次推送失败，本轮就不更新基线。

```python
push_ok = True
for idx, batch in enumerate(batches):
    ok = send_wechat(text, ...)
    if not ok:
        push_ok = False

if push_ok:
    state = new_state
    save_state(state, watch_dir)
else:
    logger.warning("存在推送失败的批次，本轮不更新基线，下轮将重新检测并重推")
```

细节在内存态的回退：detect_changes 的返回值不再直接赋给主循环的 state 变量，而是先存进 new_state——只有全部批次推送成功，新基线才被采纳、才落盘；任何失败，旧基线在内存与磁盘上都原样保留。下一轮扫描，文件与旧基线的差异还在，变更被重新发现、重新推送。代价是可能重复推送——如果第一批成功、第二批失败，下一轮会把第一批也再发一遍。这是一个被明确接受了的取舍：重复的消息烦人一次，丢失的消息永远缺席。至少送达一次，好过至多送达一次。

推送的批次语义也在这里浮出水面：变更先按单批 50 条切片，逐批发送，消息里标明这是第几批、共几批；单轮超出 100 条的部分被截断，尾部追加一句“另有若干条变更未显示”。修复之后，任何一批的失败都会让整轮基线回滚——包括那些其实已经发出去的批次。界线画在轮上，不画在批上，粗，但明确。

这个选择放到全书里看更有意思。第 5 章的 knasync，/pull 是删除式拉取，取到即删、绝不重投——货真价实的至多一次；第 6 章的 wxwatcher，推送失败就保留基线重来——刻意选择的至少一次。同一位主人的两套系统，在投递语义上站在了相反的方向，而且都对：队列那头，重复投递会污染下游消费者；通知这头，漏一条就是永远漏一条。语义没有普适的优劣，只有与场景的匹配。顺便，网络异常在主循环里本来就不计入错误次数——网络抖动是常态，不该让哨兵因此积累“连续错误”而退出。这一层旧有的宽容，与新加的“失败不落盘”拼在一起，恰好构成完整的容错语义。

## 第 108 个测试

三处修复每一条都带着回归测试落地。在它们加入之前，测试库已经有 105 个成员；目录误报修复带进 2 个，哈希与漏报修复带进 1 个。数字的演进是 105 到 107，再到 108。

先看一眼这座 105 个用例的库房是什么模样：五个测试文件各守一区——test_watcher 的四十多个用例盯着扫描与变更检测的纯逻辑，test_notifier 的八个守着推送与指数退避重试，test_config 的二十五个与 test_config_file 的八个看着四层配置合并，test_cli 的二十来个管着命令行界面与截断逻辑。此前数轮迭代里，凡动纯逻辑处，测试几乎如影随形——覆盖率表里 watcher 与 notifier 的高位不是天上掉下来的。这次的三个修复，补的恰是 cli 主循环一侧的空白。

这批测试能写成黑盒，前提是主循环本身可以被当作函数调用：_main_loop 接收基线、配置、日志器与目录，单轮跑完即返回。不用起进程、不用造线程，构造参数、删目录、断言日志，三步走完。把循环做成可导入的函数，在这里收回了投资。

第一批两个用例挂在 TestMainLoopMissingDir 类下，写法的核心是把主循环当黑盒。第一个用例：先建一个目录、放一个文件，用真实的 scan_directory 建立基线；然后 shutil.rmtree 删掉目录，调用主循环的单轮模式，用 caplog 捕获日志，断言输出里出现“跳过本轮”，且基线长度仍为 1——状态没有被清空。第二个用例更进一步：目录删除后再原样恢复，同名文件、相同内容，重新扫描后断言变更列表为空——目录短暂消失又回来，不该产生任何删除或新增告警。两个用例合起来，恰好覆盖事故链条的两端：消失时不出手，恢复时不翻旧账。

第 108 个用例最难写，因为它要证明的是一个“不再发生”的行为——哈希死循环被斩断。手段直接而聪明：造一个内容已变但权限被 chmod 000 的文件，sha256_file 必然返回 ERROR；再用 monkeypatch 给哈希函数包一层计数器，数它被调用的次数。第一轮：断言产生了修改上报，新基线里的哈希位是 ERROR；第二轮：断言变更列表为空，且计数器读数纹丝不动——不再有第二次哈希。死循环若还在，这个读数每轮都会增长；它停住了，循环就是断了。用例结尾在 finally 里把权限改回去——测试不能给世界留下垃圾。

三处修复全部落地后跑全量：108 个用例，4 秒内全绿。覆盖率总数 72%，明细是——watcher.py 94%，notifier.py 100%，config 与 config_file 分别 94% 和 91%，而 cli.py 只有 35%。这张表值得多看一眼：本章的三个缺陷，没有一个发生在覆盖率 94% 的检测核心里，全部发生在覆盖率 35% 的主循环与模块交界处。检测算法、推送重试这些“纯逻辑”早被测试围得水泄不通；出事的都是与真实世界打交道的那段胶水——目录在不在、哈希读不读得出、网络通不通。覆盖率数字会诚实地告诉你哪些代码被测过，还会顺带暗示哪些代码没被测过但更重要。35% 那一格，是下一阶段测试的地图。

## 收尾：谁来监视监视者

wxwatcher 的两个修复日恰好也是 knasync 的修复日——九月五日清晨修目录误报，九月六日凌晨修哈希死循环与漏报。哨兵与队列在同一场探测里各自挨了刀，也各自好了伤疤，而哨兵的日志全程记录了包括这次治疗在内的一切。

收个账。两天里两个提交：一个拆除十一万条误报的引信，一个同时斩断无声死循环、堵住永久漏报；新增回归用例三个，测试总数从 105 走到 108，全绿耗时四秒。也留一笔欠账：推送失败不落盘的那条路径，这次没有配上专属用例，靠主循环的既有测试与运行日志兜着——已知，记录在案，与覆盖率表上 cli.py 那个 35% 一起，构成下一阶段测试的地图。

这一章留下三层思考。第一层关于轮询。inotify、FSEvents 这类事件推送机制当然更优雅，但它们对外置盘、网络盘、U 盘的语义从来参差——事件丢失、句柄失效、挂载点抖动，每种环境都要单独迁就。30 秒一轮的笨办法慢，却对一切文件系统一视同仁；对一个把“不漏报”当生命的哨兵，稳比快值钱。

把两个仓库的提交时间摆在一起，还有一层趣味。九月五日 05 点 39 分，哨兵修好目录误报；三分钟后，队列修好部署脚本。九月六日 00 点 31 分，哨兵斩断哈希死循环；半小时后，队列关掉无参回退；再过三小时，队列补上第一批单元测试。两个仓库在同一场探测里交替提交，像两个人轮流守夜。而每一次提交落盘的瞬间，工作目录下的哨兵都会把这次修复本身推送到主人的微信上——它报告了自己的痊愈。

第二层关于失败语义。三个缺陷本质都是同一道题：世界不配合时怎么办。目录会消失，文件会读不了，网络会失败。健康的系统不是假设世界永远配合的系统，而是每一次被拒绝都有明确处置的系统——目录缺失就跳过并计时，哈希失败就按元数据证据上报，推送失败就保留基线下轮重来。三个修复没有一个是新功能，全是对失败路径的补票。它们还有一个共同的气质：不试图消灭失败，只给每一次失败一个确定的去处。

第三层关于监视者自身。wxwatcher 盯着十一万个文件，可它自己的存活靠 LaunchAgent 的 KeepAlive，自己的正确性靠那 108 个测试，自己的历史靠 stderr 日志——里面积累着一千三百多条 warning，每一轮目录缺失、每一次重试，都留了痕。谁来监视监视者？答案朴素：测试守它的逻辑，KeepAlive 守它的性命，日志守它的诚实。哨兵不必完美，但必须诚实——它对自己失败的处理方式，和它对文件变化的处理方式，如今是同一种：记下来，然后绝不说谎。

---


# 第 7 章 · go-music —— 音乐服务与它的 502

# 第七章 go-music —— 孤本、读锁与十兆上限

2026 年 9 月 5 日凌晨，探测进行到音乐这条线时，先做的不是看代码，而是敲了一句 curl。

`https://www.yuangs.cc` 返回的是 nginx 的 502。这个域名是整个音乐产品线的咽喉：网页版播放器 autoMusicPlayer 的前端里，有四处硬编码指向 `www.yuangs.cc/api/*`——搜索、YouTube 代理、歌词，全靠它活着。后端一倒，npm 上已发布的 `geek-music-player@2.0.18`、Cloudflare 上的线上页面，全部成了摆设。而按照运维笔记里的说法，这个位置上跑的应该正是 go-music：一个用 Go 重写的音乐后端。

然后回到本机看这个项目本身。目录里的景象像一间半年没人进过的机房：`main`，13,897,952 字节的编译产物，时间戳停在 2025 年 12 月 14 日；旁边是同一天的 `server.log`，第一行写着 `Server starting on port 8080`，日志里还留着当时的搜索记录——`q=eminem&limit=10` 用了 175 毫秒，`q=Retrowave&limit=48` 用了 993 毫秒。再敲一句 `git status`，回答干脆利落：`not a git repository`。

一个三千三百余行的 Go 项目，唯一一份副本躺在本机磁盘上，没有版本管理，编译产物和日志与源码混放。它要服务的域名正在 502，而它自己连一次回滚的能力都没有。这一章讲的就是这个项目如何在 9 月 6 日凌晨获得它的第一个 git 提交，以及紧接着从代码里挖出的两处必崩 bug。

## 定位：一条产品线的后端半成品

先把关系理清楚。音乐这条线上有两个仓库：autoMusicPlayer 是纯前端 PWA，零后端依赖，数据全存浏览器 localStorage；go-music 是它的后端重写——README 里写得明白："Replaced Python Flask with Go Gin"，也就是说 `www.yuangs.cc` 这个位置原先跑的是 Python Flask 版，后来用 Go 重写了。

go-music 的技术形态是一个 Gin 单体。`cmd/server/main.go` 一百一十一行，负责装配路由和七个服务，分层倒是清晰的：

- **YouTubeMusicService**：不走 YouTube 官方 API，而是代理到 `https://api.yuangs.cc`——这个上游实测存活，是另一台机器上的 YouTube Music 代理；
- **ITunesService**：直连 Apple 的 iTunes 搜索接口，拿 30 秒试听；
- **LyricsService**：直连 lyrics.ovh 拿歌词；
- **CacheService**：内存 TTL 缓存，带一个每五分钟清扫过期条目的后台协程；
- **FavoritesService / HistoryService / ThemeService**：收藏、播放历史、八套主题，全部以 JSON 文件形式持久化——写进进程的当前工作目录。

前端不是重新写的。`assets/index.html` 有 6024 行，是 autoMusicPlayer 前端某个时点的旧快照，改成了相对路径调本机 API。两份前端对比：约 95% 相同，diff 只有 369 行，且绝大多数是格式化差异；`sw.js` 和 `YouTubePlayerManager.js` 逐字节完全相同。但分叉已经发生——autoMusicPlayer 把 AI 音乐分析升级成了 OpenAI 兼容接口，go-music 里的副本还停留在早已变更的旧端点上。这带来一个报告里点破的架构悖论：go-music 后端实现了服务端的收藏/历史/主题 API，`/api/favorites`、`/api/history`、`/api/theme` 都注册了路由，但它内嵌的这份前端副本仍然全部走 localStorage——**这套服务端数据层写完之后，没有任何一个前端在用它**。功能是半途而废的改造，价值尚未兑现，而维护成本是双份的：同一份六千行前端，两处漂移，人肉同步。

优化分析报告给这个项目打了 4.8 分（满分 10），其中工程实践一项只有 3 分。25 条发现里，P0 级四条：线上域名 502、无 git 仓库、并发 map 写崩溃、导入接口 panic。这一章处理的是后三条。

## 发现：两处必崩，藏在热路径上

**第一处在读缓存的函数里。** `services/youtube_service.go` 第 919 行起是 `getFromCache`，它做三件事：加读锁、查 map、过期则删除。原代码长这样：

```go
y.mutex.RLock()
defer y.mutex.RUnlock()

item, exists := y.cache[key]
if !exists {
    return nil, false
}
if time.Now().After(item.expiresAt) {
    delete(y.cache, key)   // 持 RLock 期间删 map
    return nil, false
}
```

问题出在 Go 的 `sync.RWMutex` 语义上。读锁的承诺是"我可以和任意多个其他读者共存"，前提是所有人都只读。而 `delete` 是写操作——持读锁删 map，等于在一群读者正在浏览的地图上当场涂改。Go 的运行时对并发 map 读写有专门探测，一旦命中不是普通 panic，而是无法 recover 的 `fatal error`，进程直接死。

为了验证这不是纸上谈兵，探测时写了一段二十行的演示程序：八个 goroutine 用 `RLock` 循环读同一个 map，另一个 goroutine 模拟 `getFromCache` 的过期分支，持 `RLock` 执行 `delete`。第一版调度没踩上探测点，平安跑完；把读者数量和循环次数加密集之后，输出如期而至：

```
fatal error: concurrent map read and map write
```

这不是低概率事件。`getFromCache` 在每个搜索请求的热路径上，缓存条目十五分钟过期一次，过期瞬间的第一个请求就会走进 delete 分支——线上只要有一个并发用户，这就是颗定时炸弹。

**第二处在两个导入接口里。** `handlers/favorites_history_handler.go` 的 `ImportFavoritesHandler` 和 `ImportHistoryHandler`，读取上传文件用的是同一套写法：

```go
data := make([]byte, c.Request.ContentLength)
_, err = file.Read(data)
```

两个错误叠在一起。第一，`ContentLength` 在 chunked 传输（`Transfer-Encoding: chunked`）时是 -1，`make([]byte, -1)` 直接 panic——用一段小程序复现，得到 `panic: runtime error: makeslice: len out of range`。第二，就算长度正常，Go 的 `io.Reader` 契约从来不保证单次 `Read` 读满缓冲区，网络文件常见读一半返回，导入的收藏数据会被静默截断，坏掉的那半截 JSON 大概率让 `Import` 解析失败，用户只看到一个含糊的 "Invalid file format"。

还有一层反讽：这两个 handler 以及 Export、Clear 等约十五个 handler，根本没在 `setupRoutes` 里注册路由。它们是死代码——但死代码躺在编译产物里，谁也说不准哪天会被"接线"，带着 bug 上岗。

同类"看似合理、实则有害"的写法在代码里不止一处。`youtube_service.go` 末尾有个 `rateLimit()` 函数，注释写着限流，实现是一行 `time.Sleep(250 * time.Millisecond)`——每个上游请求前固定睡 250 毫秒。这不是限流，是对每个请求无条件加税：组合搜索（iTunes 加 YouTube）时用户至少多等四分之一秒，而高并发下它毫无保护作用。这类代码单看每一行都人畜无害，合起来构成了那个 4.8 分的真相：项目不是烂在架构上，是烂在这些没人回头细看的角落里。

## 修复：先入库，再拆弹

动手的顺序没有悬念：先版本管理，后改代码。

9 月 6 日 0 点 29 分，`git init` 之后第一步是写 `.gitignore`——把 `main` 和 `server.log` 挡在门外——然后提交 baseline（`b923cd7`）。这一个提交收进来 30 个文件、16766 行：三千三百余行 Go 源码、六千行的前端快照、图标、两份文档。13MB 的二进制和日志从此与仓库无关。对一个存在了九个月、从未有过历史的项目来说，这个提交的意义超过之后所有提交的总和：**从这一刻起，改坏任何东西都有退路了。**

四十七分钟后，第二个提交（`5e2f69e`）拆掉两颗炸弹。

`getFromCache` 的修法取了最小改动：读锁升写锁。备选方案其实有三个——过期时不删除、只视为未命中，把清理交给后台协程；或者换 `sync.Map`；或者干脆升级写锁。选第三个是因为它改动最小、语义最直白，代价是这一处缓存读从共享锁变成互斥锁，但在单机音乐播放器这个量级，这点开销可以忽略。提交里留下的注释把教训钉在了原地：

```go
// 可能过期删除 map 条目，必须用写锁；RLock 下 delete 是并发写，会直接 panic
y.mutex.Lock()
defer y.mutex.Unlock()
```

导入接口的修法多绕了一个弯。第一反应是 `io.ReadAll(file)` 一行替换，但它只修了 panic，没修"读不满"——顺带把单次 Read 的截断风险也带上了——却引入新问题：不设上限的 `io.ReadAll` 等于邀请任何人上传任意大文件把内存撑爆。最终方案是三件套：

```go
const maxImportSize = 10 << 20
data, err := io.ReadAll(io.LimitReader(file, maxImportSize+1))
if err != nil { ... }
if len(data) > maxImportSize {
    c.JSON(http.StatusBadRequest, gin.H{"error": "File too large (max 10MB)"})
    return
}
```

细节在 `maxImportSize+1` 这个加一上：`LimitReader` 读到 10MB+1 字节，就能用 `len(data) > maxImportSize` 区分"恰好 10MB"和"超过 10MB"，合法文件不受影响，超限请求拿到明确的 400。两个 handler 各改一遍，同一个提交，同一套写法。

## 测试：从 0 到 1 的笨功夫

修复之后是补测试。这个仓库此前 0 个 `*_test.go`，4 点 26 分，第三个提交（`dd5f71f`）落下 98 行，`services/services_test.go`，三个测试函数，从收藏、历史、主题三个服务各开一刀。

真正值得写下来的是方法。这三个服务的构造函数会从当前工作目录读取 `favorites.json`、`history.json`、`themes.json`——不存在的文件就当作空数据初始化。这意味着测试必须回答一个问题：怎么让每个测试拿到干净的状态，又不碰开发者机器上的真实数据文件？

答案是 `withTempDir` 辅助函数：`os.MkdirTemp` 建临时目录，`os.Chdir` 切过去跑测试，`defer` 里恢复原目录并删除临时目录。这个做法一石二鸟——既隔离了状态，又恰好覆盖了"空目录首次启动"这条真实路径，因为服务在临时目录里走的就是全新的初始化流程。测试断言也朴素：收藏的 Add/GetAll/Remove/IsFavorited 走一圈，历史的 Add/Clear 走一圈，主题的 GetAll/Set/Get 走一圈。跑起来三条全绿。

然后是覆盖率，`go test ./... -cover` 的实测输出：

```
music-player-go/cmd/server    coverage: 0.0% of statements
music-player-go/handlers      coverage: 0.0% of statements
music-player-go/services      coverage: 11.7% of statements
```

11.7%，这个数字必须诚实面对。七层服务里，缓存、收藏、历史、主题四层是纯本地逻辑，已被覆盖或触达；而 iTunes、YouTube、歌词三层是纯网络适配层——每一行代码几乎都是构造请求、打上游、解析响应。要测它们得先搭 `httptest` 假上游服务器，把硬编码的上游 URL 改成可注入的——这恰好又指向报告里"上游全部硬编码"那条 P1。11.7% 不是终点，但它有一个重要的性质：**每一个百分点都是真的**。没有为了数字好看去测 getter/setter，没有把无法断言的代码排除在统计外。从 0 到 1 的意义从来不在 1 有多大，在于从此有了第二个、第三个可以站立的地方。

## 收尾：502 还亮着

写这一章的当天下午，再次 curl `https://www.yuangs.cc`，返回的还是 502。

这是全链条上最诚实的一个事实：go-music 的代码修好了，有了 git，有了测试，有了两个不再必崩的关键路径——但它没有部署。P0 清单里排第一的那条（恢复线上）恰恰是唯一一条不能靠改代码解决的事。九个月前那个 12 月的下午，服务在本机 8080 端口跑过、搜过 Eminem、返回过 200，然后把它的维护者带向了"项目已完成"的错觉；九个月里，编译产物悄悄涨到 13MB，日志停更，域名易主，前端在 npm 上发了十八个版本，每一个版本的搜索请求都撞在 502 上。

这一章记录的三次提交，本质上只做了一件事：让这个项目重新具备被继续开发、被部署上线的资格。版本管理是资格，修掉必崩是资格，第一个测试文件也是资格。至于收藏/历史/主题这层服务端数据什么时候真的接上前端、让 autoMusicPlayer 的数据从各自的浏览器里解放出来——那是资格兑现之后的事。

孤本已入库。下一步，该把它推上远端，然后让 502 变回 200。

---


# 第 8 章 · TAAGE —— 治理引擎的重生

# 第八章 trusted-agent-engine-python —— 给治理引擎做一次治理

2026 年 9 月 6 日凌晨，在 /tmp 沙箱里照着 README 的"快速开始"敲了一遍示例代码。一段再普通不过的 Python：构造一个 `Proposal`，调用 `TrustedGuard.evaluate`，打印裁决结果。回车，屏幕上滚出来的不是裁决，而是一段 traceback：

```
ValueError: "Decision" object has no field "audit_log"
```

这不是一次偶然的运行环境问题。换成仓库自带的默认政策再跑，崩得更早——`ValueError: [Governance Critical] String-based policy conditions are disabled for security`。一个号称要"治理 AI 代理"的引擎，用任何一种方式启动，结局都是崩溃。而它的 PyPI 包 `trusted-agent-engine==2.0.0` 已经在线上挂了七个月，任何人 `pip install` 之后照文档运行，都会得到同样的结局。

这一天夜里发生的事，说起来像一个悖论的谜面：给治理引擎做治理。谜底是三个 bug、一次依赖逃逸的失败、三百八十二行替代品，和二十四个全绿的测试。

## 定位：不靠自觉的信任机器

TAAGE，Trusted AI Agent Governance Engine，可信 AI 代理治理引擎。它要回答的问题很具体：当一个 AI 编码代理提出"我要改这些文件"时，准还是不准？谁说了算？

它的答案是：不依赖 LLM 的自觉性，用一套物理脱钩的规则机器来裁决。核心链路五步：`loadPolicy` 读取 `agent.policy.yaml`，若存在 `.ai/sovereign.pub` 则用 Ed25519 校验同目录 `.sig` 签名——政策必须是主权者签过字的；`PolicyEngine.evaluate` 做风险匹配（glob 模式定风险等级）、异常检测（规模/熵/分散度）、JSON Logic 规则评估（check 不满足即罚，condition 满足即罚）、价值层扣分与 mercy 豁免，最后合成三态裁决——`allowed` / `require_human` / `blocked`；`LiabilityManager` 做责任归属和信用分记账；`ContextBank` 把每次决策追加进 JSONL 审计账本。特权机制压轴：任何规则想执行 `block` 或 `require_human`，政策里必须显式声明 `high-risk-decision` 特权——"拦截"这个权力本身要被主权者授权，这是整个体系里最自我指涉也最关键的一环。

这个项目有双胞胎：Node.js/TypeScript 原版发 npm，Python 版是逐文件移植，发 PyPI，命令行叫 `trusted-engine`，两者 v2.0.0 同日发布。移植是认真的——双引擎的 Ed25519 签名实测互通，决策主干逻辑逐行对齐。但优化分析报告给 PyPI 发布物打出的分数是 1.5 分（满分 10），评语只有一句话的含义：核心 API 不可用。Python 版仓库在今天的修复提交（`b3b6807`，9 个文件，+542/-91）之前，处在"三层断裂"状态：核心 API 必崩、规则引擎整体失效、默认政策自崩。逐层看。

## 断裂一：一个下划线，杀死整个 API

第一处断裂小到令人发笑：`engine/evaluator.py` 第 114 行，`decision.audit_log = ...`。而 `engine/types.py` 第 103 行里，pydantic 模型 `Decision` 的字段名叫 `auditLog`。

pydantic v2 对未声明字段的赋值不宽容——`setattr` 直接抛 `ValueError`。这行代码在 `evaluate()` 的最后阶段执行，位置在所有规则评估、价值计算、责任归属之后，返回值之前。也就是说：**无论政策写什么、提案是什么、规则怎么判，执行到最后一公里必然崩溃**。100% 必崩，字面意义上的。

案发现场本身是一场双语言移植的经典车祸。Node/TypeScript 世界用 camelCase，Python 世界偏爱 snake_case，移植者在模型定义处保留了 camelCase（为了和 Node 版序列化结果对齐），却在赋值处顺手写成了 snake_case。更讽刺的是 README：快速开始的示例代码里印着 `print(f"🚫 Blocked: {decision.audit_log}")`——文档在教用户访问一个不存在的字段名。照抄即崩，文档和代码在同一个坑里会师。

## 断裂二：规则引擎整体失效，而逃生门不存在

比崩溃更隐蔽的是第二处断裂：规则引擎在"看似正常地失效"。

`pyproject.toml` 里写着依赖 `json-logic>=0.6.3`。这个包是 2015 年的 Python 2 时代移植，它的核心逻辑里有一行 `tests.keys()[0]`——在 Python 3 里，`dict_keys` 不支持下标，任何表达式求值都会抛 `TypeError: 'dict_keys' object is not subscriptable`。用 `uv run --with json-logic==0.6.3` 实测，一行代码即可复现。

如果它崩得响亮也就罢了。坏在旧版 `SafeEvaluator` 的设计是"吞错返回 False"：捕获一切异常，打印一行日志，然后返回 `False`。在 TAAGE 的规则语义里，`check` 规则求值为 False 意味着"违规"，`condition` 规则求值为 False 意味着"不触发"。于是失效是双向的：所有 check 规则永远判定违规——该放行的全被拦；所有 condition 规则永不触发——所有阻断型条件静默消失，包括"高风险变更必须人工确认"这条最重要的门禁。**一半 fail-closed，一半 fail-open，表面上看引擎还在运行**。对一个治理引擎来说，静默放行比崩溃危险得多：崩溃至少会被人看见。

修复的第一方案本来是换依赖。优化报告建议的替代品名叫 `json-logic-py`——听起来正是 json-logic-js 的官方 Python 移植。9 月 6 日凌晨实测：`curl https://pypi.org/pypi/json-logic-py/json`，返回 404 Not Found。**报告里建议的包在 PyPI 上根本不存在**，而真正存在的 `json-logic` 正是那个坏包。依赖逃逸这条路的断头，把修复推向了唯一的出路：自己写。

于是 `safe_evaluator.py` 从 26 行膨胀到 382 行，成为一个内置的纯 Python JSON Logic 求值器。三条设计底线：**无 eval**——整个文件没有一处动态执行，静态可审计；**惰性求值**——算子 handler 接收原始参数、各自求值，`map`/`filter`/`all`/`some`/`none` 的测试表达式必须针对每个元素单独求值，`if`/`and`/`or` 必须短路，这是 JSON Logic 参考语义里最容易做错的部分；**硬化保留**——字符串表达式一律拒绝，`[Governance Critical]` 的 ValueError 原样继承，这是 v1.1 版本为了消灭 `new Function` 式 RCE 后门立下的规矩。最终注册了 34 个算子：比较、逻辑、数据访问、数值、数组、字符串六族，连 `===` 都按参考语义用"类型相同且值相等"实现，而不是 Python 的 `is`。不认识的算子抛 `ValueError`，在 `evaluate` 层被捕获降级为 `False`——宽松的入口，严格的内核。

## 断裂三：出厂政策自崩

第三处断裂最扎心：仓库自带的 `agent.policy.yaml` 和 `value_manifesto.yaml`，用的还是 v1.0 时代的字符串表达式写法——`check: "payload.diff.length > 0"`、`condition: "engine.riskLevel == 'high'"`。而 v1.1 的 RCE 硬化早已禁用字符串表达式。

换句话说，引擎的硬化升级砍掉了旧语法，却没人回去改默认政策、README 和文档。一个治理引擎，出厂自带的政策能让它当场崩溃；用户照官方文档配置，得到的第一个输出就是 `[Governance Critical]`。签名体系还在正常工作——被 Ed25519 签名保护的，正是一份会让引擎崩溃的政策。

修复提交把两份 YAML 全部迁移为 JSON Logic，版本号升到 2.0.0，政策名改为 `trusted-default-v2`：

```yaml
- id: must-have-diff
  check:
    '!!': [{var: payload.diff}]
  action: block
- id: high-risk-gate
  condition:
    '==': [{var: engine.riskLevel}, high]
  action: require_human
```

迁移途中还挖出一个隐藏地雷：政策里没有声明 `meta.privileges: ["high-risk-decision"]`。按特权校验逻辑，所有想执行 `block`/`require_human` 的规则都会被判成 `privilege-violation` 强制拦截——迁移完语法的政策若不补特权声明，引擎会合法地把一切提案全部拦死，从崩溃变成另一种形式的全拦截。修复里补上了这份声明。`value_manifesto.yaml` 的 mercy 钩子同步迁移：`emergency_fix` 的条件改成 `{"in": ["emergency", {"var": "payload.tags"}]}`，`doc_only_mercy` 改成对 `engine.isOnlyDocs` 的相等判断。

## 意外收获：一个 glob 语义坑

政策迁移过程中的端到端测试暴露了第四个问题，它不在崩溃名单上，却让高危门禁形同虚设。

风险规则里写的是 `**/.env*`——直觉上这该匹配所有目录下的环境变量文件。实测：`fnmatch.fnmatch('.env', '**/.env*')` 返回 `False`，`.env.local` 同样 `False`，只有 `src/.env` 这样的深层路径才返回 `True`。原因在 Python 标准库 `fnmatch` 的语义：它的 `*` 本来就能跨越路径分隔符，`**` 没有任何递归含义，前导 `**/` 反而要求至少存在一层目录。Node 版用的是 `minimatch`，两个引擎对同一份签名政策的理解从此分岔。而真实项目里，`.env` 和 `docker-compose.yml` 恰恰最常躺在仓库根目录——**最高危的文件，恰好在门禁的盲区里**。

修法务实：不重写 glob 引擎，而是在风险规则里补上根目录形态——`.env*`、`docker-compose.yml`、`*.tf` 与原有的 `**/` 形态并存。语义坑用模式冗余来填平，并在测试里钉死。

## 验证：二十四个绿灯和三道门

修复的最后一环是让一切可复现。新增的 `tests/test_safe_evaluator.py` 交付了四层测试：17 组参数化算子用例，从 `==` 到 `map`/`filter`/`some`/`none` 逐个锁定求值语义；两个负例——字符串表达式必须抛错、未知算子（测试里故意用了 `{"evil_exec": ["rm -rf /"]}`）必须安全降级；两个端到端回归，一个证明 `evaluate()` 不再崩且 `auditLog` 有值，一个证明 `.env.local` 触发高危门禁。

跑测试的命令与输出值得原样记录：

```
$ uv run pytest tests/
........................                                                 [100%]
24 passed in 0.92s
```

24 个测试全绿——新增 21 个，加上原有的 3 个 diff 解析器测试。覆盖率实测 55%（924 条语句）：`safe_evaluator.py` 69%，`evaluator.py` 79%，`types.py` 100%；而 `cli/main.py`、`consensus.py`、`api/server.py` 仍是 0%。这个分布同样诚实：被测的正是这次修的东西，没修的清清楚楚地露在外面。

收尾之前，用当前代码把三道门独立复验了一遍：

- **低风险提案**（改 `src/main.py`，理由充分，在 scope 内）→ `allowed: True`，`auditLog` 非空，审计链完整；
- **触碰 `.env.local`** → `riskLevel: high`，`requiresHuman: True`——高危门禁在根目录文件上真实生效；
- **越界提案**（改 `etc/passwd.conf`，scope 之外）→ `allowed: False`，`actions: ['block']`，violations 精确指向 `scope-enforcement`。

三道门，一道放行，一道转人工，一道拦死。一个治理引擎该有的样子，第一次完整地跑了起来。

## 收尾：谁来治理治理者

复盘这次修复，最值得留下的是三个问题。

第一个关于**静默失效**。`audit_log` 那个 bug 崩得响亮，反而无害；真正危险的是 `json-logic` 坏依赖加吞错设计制造的"看似运行"——check 全拦、condition 全放，引擎每天都在忠实地产出错误的裁决。GOVERNANCE_INVARIANTS 里写着 "Fail-closed but Explainable"，而失效的真相是 fail-open 且不可见。任何声称"安全"的系统，最该被测试的不是正确路径，而是依赖损坏时的行为。

第二个关于**依赖的虚实**。一条"换上官方移植包"的建议，看起来无比合理，直到有人真的去 PyPI 查了那个包名——404。这一章里所有关键结论都来自命令的输出，而不是来自记忆和常识：`dict_keys` 的 TypeError 是跑出来的，404 是 curl 出来的，`fatal error` 是在 go-music 那边用八个 goroutine 逼出来的。探测方法论的第一条"不信文档，信命令"，在这里救了一次二次事故。

第三个关于**带病发布**。PyPI 上的 2.0.0 必崩，直接原因是那行下划线，根本原因是发布流水线里没有一道"安装后跑一遍 README 示例"的冒烟关卡——包是从没有测试门槛的管道里流出去的。修复提交只是止血，下一步该做的是发 2.0.1、给发布 job 挂上测试前置，再往远处看，是给 Node 和 Python 两个引擎建一套共享的"黄金政策"一致性用例——同一份签名政策，必须在两个引擎里得到同一份裁决，否则"跨语言的可信"无从谈起。

治理引擎存在的意义，是把价值观编译成可执行的规则。而这次修复证明的不过是一条更朴素的前置定理：规则首先要能被执行。一个连自己的默认政策都会崩溃的引擎，谈何治理别人——被认真写下的测试，才是它给自己的第一份信任。

---


# 第 9 章 · chats —— 四颗雷与零测试的代价

# 第九章 chats —— 热闹的房子与不响的雷

2026 年 9 月 6 日凌晨 0 点 59 分 11 秒，一个提交被推进 chats 仓库：

```
e6864eb fix: 四处确定性 Bug——定时任务、复盘、头条队列恢复可用
 4 files changed, 17 insertions(+), 6 deletions(-)
```

17 行新增，6 行删除，4 个文件。在这份拥有 550 个提交的仓库里，这是体量最小的一类提交。它的特别之处不在行数，而在提交信息里那几个名词：定时任务、复盘、头条队列——以及那个意味深长的定语：恢复可用。

"恢复"的前提是曾经可用。而这章要讲的故事是：其中至少三处功能，从写下第一行代码起就从未可用过。

这个提交是前一天开始的全面探测的收尾之一。9 月 5 日凌晨三点，一次例行的"帮我看看代码"演变成一场横跨九个代码库、五台机器的体检，chats 是其中被测定为 4/10 分的那一个——功能想象力最丰富，地雷密度也最高。报告打印出来的当天夜里，四颗确定性最高的雷先被拆除。

先看一个更具体的场景。按照 autoTasks.js 里的设计，每天北京时间早上八点，一个名叫"机器人小助手"的账号会在 test 房间里发一条问候。它的提示词写得很用心："忘掉我们之前的所有历史对话，现在你是deepseek小助手，自动向用户问好，并且每次附上一句名人名言，每日一句精典英文句子以及趣味数学知识点或数学家故事……最后鼓励用户好好工作，好好学习，好好生活。"为了防止 AI 重复自己，代码还专门生成一个 UUID 拼进提示词，要求模型在回复尾部带上这个唯一标识。

这句问候从来没有发出去过。不是因为一个 bug，而是因为四层 bug 叠加，外加一次人为的停用。这一章讲的，就是这四层是如何在同一座房子里各自安静地埋伏下来的。

## 定位：九库里最热的房子

chats 是九个代码库里功能密度最高的一个。域名 chat.want.biz，整套系统跑在 Cloudflare Workers 上，六个 Durable Objects 按业务域分工：HibernatingChating2 承载聊天房间，按 idFromName(roomName) 一房一实例；ToutiaoServiceDO2 管头条发布队列；ZhihuServiceDO 管知乎内容；InspirationDO 管灵感收集；AuthServiceDO2 管登录会话；TradingLogDO 用 DO 内置的 SQLite 存期货交易流水。加一个 R2 桶 yuangs 存图片和 K 线图，一个 SELF 服务绑定让 Worker 可以调用自己，一个部署在 api.yuangs.cc 的 Flask 服务承担发布代理、知乎抓取和离线推送的外围脏活。仓库根目录还躺着一个 install_coturn.sh——WebRTC 通话的 TURN 服务器由 b 机承担，那是第十二章的故事。

worker.js 是所有流量的总闸，路由是十几段 if-startsWith 的长链：/api/toutiao/ 递给头条 DO，/api/zhihu/ 递给知乎 DO，/api/auth/、/api/trading/、/api/room/ 各有去处，/upload 直写 R2，其余静态文件从 public 目录出。最特别的一条是 /api/internal-task-handler：聊天房间的 DO 会通过 SELF 绑定把 AI 生成文章这类重活 HTTP 回调给 Worker 自己，再分发给对应的 DO——一个服务调用自己中转任务，而这条本该内部的路径同时公网可达，是报告里 P0 级的发现，暂且按下不表。三个模型的出口各不相同：Kimi 走 api.moonshot.cn，Gemini 走 generativelanguage.googleapis.com（flash 与 pro 两个型号硬编码在 ai.js 里），DeepSeek 的 baseurl 在最近的一次提交里换成了 poe 网关 api.yuangs.cc，与全体系的模型出口策略合流。

功能清单可以列得很长：多房间实时聊天，房间里 @ Gemini、Kimi、DeepSeek 三个模型对话，AI 携带一整套工具调用查期货行情、算龙虎榜、画 K 线图传到 R2。把功能串起来看，想象力是溢出的：对着房间问一句期货价格，AI 会自己决定调哪个工具、取哪个合约、画哪张图，图传上 R2 再发回房间；发一条 /知乎 命令，热榜 TOP20 应声而来，选个序号就能让 AI 成文，再一键送进头条发布队列；/log、/close、/review 三个命令把期货交易的开仓、平仓、复盘变成聊天记录，复盘由 DeepSeek 亲自执笔。再加 WebSocket 信令转发支撑的 WebRTC 音视频、Web Push 离线推送、每日问候与定时新闻三组 cron 自动化，外加 PWA 和一个管理后台。README 第一行自称"AI热点内容创作与实时协作平台"，不算夸张。

代码体量与功能密度相称：src/ 下 25 个文件共 11,746 行，最大的四个是 toutiaoService.js 1294 行、ai.js 1172 行、chatroom_do.js 1118 行、worker.js 1102 行；前端整个是一个 5,664 行的 public/index.html，worker.js 里还用模板字符串内嵌了约 430 行浏览器端推送脚本和 Service Worker——改一行前端文案，要重新部署整个 Worker。

工程实践的一面同样整齐：package.json 的 scripts 只有 deploy、dev、tail 三条，没有 test；test/ 目录里唯一的文件是一个测外部 Flask 接口的 Python 脚本；devDependencies 只有 prettier 和 wrangler。零测试，零 CI，零类型检查。

顺便做一个全书方法论要求的小核对。所有材料——包括本次的优化分析报告——都说这个项目有"25+ 工具调用"。打开 ai.js 第 122 行的 MASTER_TOOL_DEFINITIONS 逐个数：get_price、get_news、draw_chart、query_fut_daily、query_minutely、query_option、query_lhb、query_aggregate、smart_query、get_highest_price、get_lowest_price，加上 unified_ 前缀的十个，一共 21 个。命令数出来是 21，不是 25。不是大事，但它预示了这一章的主题：在这个项目里，口头印象与代码事实之间的缝隙比一般项目要宽。

这座房子的出生方式也值得记录。第一个提交停在 2025 年 7 月 12 日，提交信息是"发布聊天室应用v1.0"和"一个功能非常丰富的、部署在 Cloudflare 无服务器平台上的实时多房间聊天应用"。仅 7 月一个月就有 457 个提交，占全部 550 个的 83%；8 月 58 个，10 月 26 个，11 月 5 个，12 月 3 个——然后是长达八个多月的沉寂。直到 2026 年 9 月 6 日凌晨，那个 17 行的提交打破了安静。

冲刺式生长，然后弃管——这是组合弹的标准孕育环境。

## 第一颗雷：塞错位置的 env

autoTasks.js 的每日问候任务里，第 68 行原本写着：

```js
const content = await getKimiExplanation(finalPrompt, env)
```

而 ai.js 第 732 行的函数签名是：

```js
export async function getKimiExplanation(text, modelName, env)
```

两个参数对三个形参：env 对象被塞进了 modelName 的位置，真正的 env 形参拿到的是 undefined。调用链往下走一层，callKimiApi 的第一行就是 `const apiKey = env.KIMI_API_KEY`——对 undefined 取属性，TypeError，必崩，每一次都崩。错误被 catch 住，console.error 一行，函数返回 {success:false}，然后消失在没有人看的日志里。没有任何用户会看到这条错误——它发生在 cron 上下文里，而 cron 从未触发过（这是第四颗雷的内容，稍后揭晓）。

修复带着根因注释进了提交：

```js
// getKimiExplanation 签名是 (text, modelName, env)：之前把 env 传给了
// modelName，内部 env 为 undefined 直接 TypeError，每日定时任务必崩
const content = await getKimiExplanation(finalPrompt, 'moonshot-v1-8k', env)
```

## 第二颗雷：少传一个参数的复盘

同一类错误的另一个变体，藏在交易复盘里。用户在房间发 /review 命令，tradingLogService 会从 SQLite 里取出交易记录，构建提示词，调用 DeepSeek 生成复盘分析。单笔复盘（第 268 行）和整体复盘（第 313 行）两处，写的都是：

```js
const aiAnalysis = await getDeepSeekExplanation(prompt)
```

而 ai.js 第 592 行的签名是 `(text, requestedModelName, env)`，函数第一行就是 `const apiKey = env.DEEPSEEK_API_KEY`。env 是 undefined，TypeError 当场抛出。这一回用户能看到报错——聊天室里会浮现一行"❌ 复盘失败: Cannot read properties of undefined (reading 'DEEPSEEK_API_KEY')"，把内部错误原文原样端给用户。每一次复盘，必然如此。

修复是补上缺掉的参数：`getDeepSeekExplanation(prompt, null, this.env)`，两处都带了注释。顺带一提这条调用链的远方：DeepSeek 的请求如今发往 poe 网关 api.yuangs.cc，ai.js 里还有一段按北京时段在 chat 与 coder 两个型号之间切换的逻辑，凌晨自动换模型——这些精巧的设计全都健在，只是它们的上游，那个少了参数的调用点，从来没让它们有机会运转。

这一类里其实还有第三颗没拆的雷，本次修复没有覆盖：tradingLogService.js 第 76 行 `drawChart(symbol, '1h', 50)`，而 chart_generator.js 第 677 行的签名是 `drawChart(env, symbol, period)`——env 的位置上被塞进了合约代码。开仓时的 K 线图至今画不出来，这颗雷留在了下一节末尾的清单里。

参数错位和漏参，是动态语言最经典的暗礁。多传、少传、错传参数在 JavaScript 里都不是错误，解释器照单全收，直到运行时在某个属性访问上炸开。全项目没有一行 TypeScript，连 JSDoc 都寥寥，于是这一整类错误只能靠两种方式发现：用户投诉，或者逐个函数比对签名。前者没有发生过——因为复盘的愤怒用户只有作者自己；后者发生在 2026 年 9 月 6 日凌晨。

## 第三颗雷：一个队列的两个名字

头条发布流水线的设计是体面的：用户在聊天室发一条带 @头条 的消息，任务进队列，Durable Object 异步消费，AI 提炼标题正文，经 Flask 代理发布，进度实时回播到房间。队列的入口在 toutiaoDO 的 addToQueue，出口有三处——管理面板的队列视图、任务完成后的 updateQueueStatus 状态回写、以及处理循环里的 removeFromQueue 出队。三处出口与一处入口，本该围成一个闭环。异步队列是为了不阻塞聊天——思路没错，错在两个字符串。

toutiaoDO.js 第 16 行声明了静态常量：

```js
static TASK_QUEUE_KEY = 'toutiao_queue'
```

而生产者一端，toutiaoService.js 第 1073 行的 ToutiaoQueueManager 用的是另一个名字：`this.queueKey = 'toutiao_task_queue'`。更拧巴的是 toutiaoDO 自己：它的 addToQueue 用静态常量写入 'toutiao_queue'，而 getTaskQueue、clearTaskQueue、removeFromQueue、updateQueueStatus 四个读取方法全部硬编码 'toutiao_task_queue'。

结果是一个完美的自锁：任务被写进一个永远没有人读的 key，而处理循环永远从一个没有人写的 key 里读——后者永远是空数组。管理面板的队列恒为空，updateQueueStatus 和 removeFromQueue 逐一空转，排队的文章永远等不到自己被处理。没有报错，没有异常，空数组是合法状态，整条流水线安静地维持着"一切正常"的假象。

修复是把静态常量统一为 'toutiao_task_queue'，让写入端、读取端、生产者三方在同一个名字上会师。注释写明了历史缘由。

这类错误的本质是单一事实来源的缺失。同一个概念——队列的存储键——在两个文件里各抄了一份字符串，谁也不引用谁。凡是同一条字符串被抄写第二次的地方，分叉只是时间问题。

## 第四颗雷：从未醒来过的定时器

最荒诞的一颗，实际上是两层嵌套。

第一层在 autoTasks.js。taskMap 是一个 Map，键是 cron 表达式，值是任务函数。HOURLY_CHART_GENERATION（图表生成）和 FETCH_NEWS（新闻获取）写了完全相同的表达式 '0 1-7,13-19 * * 1-5'。Map 同键，后写者覆盖先写者——于是 executeNewsTask 把 executeChartTask 覆盖掉了，图表任务在 Map 里没有位置，永远不会被调度。讽刺的细节是，FETCH_NEWS 头上一行注释写着"每10分钟一次"，而表达式是每小时整点——注释与代码对不上的时间，恰好就是 Map 覆盖发生的时间。

第二层在 worker.js 的 scheduled 处理器：

```js
const taskFunction = taskMap[event.cron]
```

taskMap 是 Map。Map 不支持方括号索引——对任何键都返回 undefined。不管 Cloudflare 触发了哪条 cron，这里取到的永远是 undefined，然后走进 else 分支，打一行 "No task registered" 的日志，结束。**所有定时任务，从这段代码部署的那天起，一次都没有执行过。**不是执行失败——是根本没被执行。

git 考古能把这颗雷的埋设时间精确到天，这一段值得完整讲一遍：

2025 年 7 月 12 日，v1.0。taskMap 出生时是 Map，scheduled 里写的也是 `taskMap.get(event.cron)`——正确的。最初的 map 里只有每日问候和图表生成两个任务。

2025 年 7 月 24 日，提交 285a8f3，提交信息是"修正不能发文的问题"。这次提交重写了 worker.js 的 259 行，新的 scheduled 处理器变成了 `const tasksToRun = taskMap[event.cron] || []`——方括号索引一个 Map，拿到 undefined，`|| []` 兜底成空数组，循环体一次都不跑。一个为了修"文章发不出去"的提交，顺手杀死了整个自动化层，而它自己对此毫不知情。此后 handler 从循环版演化成单任务版，那对方括号一直留了下来。

2025 年 7 月 31 日到 8 月 4 日的 wrangler.toml 里，还有一条未注释的 `"*/10 * * * * "`（规则五：高频定时监控）。也就是说，那段时间 Cloudflare 真的在每 10 分钟唤醒这个 Worker——它每 10 分钟准时醒来，用方括号去索引 Map，拿到 undefined，打一行日志，睡去。周而复始，至少十天，没有报错，没有告警，没有任何人发现。这是全部失败形态里最安静的一种：什么都不做，不会抛异常。

2025 年 8 月 14 日之后的 wrangler.toml：所有触发器全部注释。连"每 10 分钟白醒一次"都结束了，自动化层彻底沉寂。仓库本身在 10 月到 12 月还有零星提交，但这个文件里再没有任何一行 cron 被启用过。

然后是自动化层 380 多天的空白。直到 2026 年 9 月 6 日凌晨 0 点 59 分，e6864eb 把那行改回 `taskMap.get(event.cron)`，注释写道："taskMap 是 Map，必须用 .get()；用 [] 索引永远 undefined，所有定时任务都是空操作。"同一次提交里，FETCH_NEWS 的表达式改成注释早就许诺的 '*/10 1-7,13-19 * * 1-5'，图表任务夺回了自己的键。

必须诚实补一句：截至本章写作时，wrangler.toml 的 crons 仍然全部处于注释状态。这次修复恢复的是"可运行的条件"，没有把开关打开。每日问候是否回归，是作者留给自己的另一个决定。

把四层串起来看，会得到一个倒置的防御纵深：安全系统的冗余设计是"任何一层失守，后面还有一层"，而这里的四层 bug 恰好构成了反方向的纵深——任何一层被修复，后面还有三层保证功能依然不可用。注释停用是最外层的闸，方括号索引是第二层的墙，Map 同键覆盖是第三层的锁，参数错位是门后最后的钉子。一个功能要"恢复可用"，得把四层同时拆干净，少拆任何一层，它看上去都还是坏的；而更麻烦的是，少拆任何一层，也不会有任何报错告诉你还差哪层。

也因此，"恢复可用"这四个字对四个功能的兑现程度并不相同。每日问候：调用点已修，只欠 crons 启用——它是离复活最近的一个。新闻速递：任务函数调用的 cronPost 真实存在，同样只欠东风。而图表生成和头条队列处理，即使现在打开开关，也会倒在两个根本不存在的 DO 方法上——postBotMessage 和 processToutiaoQueue 还活在 TODO 里。四颗雷拆完，四条链路里真正能跑通的只有两条。清单的价值就在这里：它分得清"修好了"和"能用了"之间还隔着几步。

## 雷是怎么活过一年的

这四颗雷的发现路径本身值得记一笔。起点是 9 月 5 日的一份只读分析报告：22 个条目，其中 P1-3、P1-4、P1-5 三组被标注为"必然失败的隐性 Bug"。次日凌晨的修复没有盲从报告，而是逐条用命令复核：打开 ai.js 比对函数签名，grep 两个存储键名，git log -S 定位回归提交，node --check 验证语法。报告给出方向，命令给出证据，四颗雷全部坐实才动手。这也是全书方法论的又一次重复：报告是假设，命令是判决。

四颗雷，三种类型：参数错位两次，键名双写一次，Map 误用两次（同键覆盖加方括号索引）。它们的共同特征可以浓缩成一句话：**必崩，但不响。**

esbuild 打包时不检查函数参数个数，部署照常成功；两个存储键是两个普通字符串，存储层照常读写，只是各读各的；方括号索引 Map 是完全合法的语法，返回 undefined 甚至算不上异常。Cloudflare 的部署绿光只回答"能不能编译、能不能部署"，从不回答"能不能运行"。对一个零测试、零 CI 的项目，部署成功就是唯一的验收环节——而它恰恰是个只会说"是"的验收员。

唯一的观测手段是 wrangler tail，一个需要人主动挂着的终端。而这些功能全部运行在凌晨、无人围观、没有用户的路径上。错误被 catch 住、被 console.error、被折叠成 {success:false} 的返回值——如果没有调用方检查这个返回值，失败就等于从未发生过，成功也从未发生过。

修复本身的方式倒是值得肯定：四处改动全部带上了说明根因的注释，本章引用的注释文字全部来自提交原文。下一个读到这些行的人，不必再做一次考古。考古的工具也很朴素：`git log -S "taskMap[event.cron]"` 一条命令就锁定了 285a8f3 这个回归点，连同那句令人苦笑的提交信息"修正不能发文的问题"。

写作本章时的最后核对，还挖出一个必须诚实记录的新发现：**修复提交自己携带了一颗新的雷。**toutiaoDO.js 第 16 行的注释用了 Python 风格的井号——JavaScript 不认识这个符号：

```
$ node --check src/toutiaoDO.js
SyntaxError: Invalid or unexpected token
```

worker.js 的第一段 import 就包含这个文件，所以下一次 `wrangler deploy` 会在打包阶段直接失败。讽刺到近乎完美的闭环：前四颗雷安静了一年是因为部署不检查它们；这第五颗雷恰恰是唯一一颗部署时当场炸响的——语法错误骗不过打包器——而它是修复者自己埋下的。截至本章写作时，它还在仓库里。

## 还埋着什么

修复有边界，这是探测日当天的既定原则：确定性 Bug 当场修，结构性问题留清单。四颗雷拆掉了，清单上剩下的还很长。以下各项均在本次代码复核中确认仍未修复，逐条列出，不为控诉，为的是让下一个打开这个仓库的人知道地基的真实高度。

安全类是重灾区：WebSocket 升级无任何鉴权，username 由客户端自报，同名用户会被强制踢下线——任何人输入他人用户名即可冒充其发言、删除其消息；聊天消息经 marked 渲染后直接写 innerHTML，而 marked v5 起已移除 sanitize 选项且没有 DOMPurify，存储型 XSS 对全房间生效；密码明文存储，GET /api/auth/users 会把全部用户连同 password 字段一起返回，创建用户的接口本身无鉴权；CRON_SECRET 与 PUSH_API_KEY 都是 "0503" 这四个字符，明文写在已入 Git 的 wrangler.toml 里；/api/internal-task-handler 公网可达且无鉴权，任何人一记 POST 就能让 AI 生成文章并推入任意房间，烧的是真实的模型费用；CORS 全站星号，/upload 无鉴权无限制直写 R2。

功能类同样有存货：drawChart 的参数错位（开仓图表必失败）；autoTasks.js 第 236 行调用的 stub.processToutiaoQueue 在 HibernatingChating2 上根本不存在，chart_generator.js 第 859 行的 stub.postBotMessage 同样不存在——即使 crons 启用、索引修复，图表与头条队列两条链路仍会倒在这些缺失的方法上；chatroom 构造函数里的 30 秒 setInterval 心跳让 Hibernation 名存实亡，而真的休眠唤醒后内存里的 sessions 全部丢失；聊天历史所有消息塞在同一个 storage key 里，每新增一条全量重写一次，头顶悬着 128KB 单值上限的达摩克利斯之剑。

优化分析报告给这个项目打的综合分是 4/10，七个维度里工程实践和安全各拿 2 分，架构设计倒有 6.5 分——骨架有想法，落地失底线。报告的评语只有八个字的分量：可玩不可托付。

## 组合弹的配方

复盘这四颗雷，真正值得带走的问题不是"为什么会有 bug"——任何项目都有 bug——而是：为什么偏偏是这一种组合，四颗互相叠加、全部必崩、全部静默，还能共存一年？

第一个配方成分是速度与验证的不对称。457 个提交挤在一个月里，功能只增不减，而每一刀提交后的验证始终只有一道工序：wrangler deploy 成功。部署流程对"签名对不对、键名同不同、Map 怎么索引"一无所知，它的绿灯于是变成了一种慢性麻醉剂。

第二个成分是无类型的 JavaScript。四颗半雷里，有三颗是签名问题（env 塞进 modelName、复盘漏传 env、env 的位置塞了合约代码）。这一整类错误，在 TypeScript 里是编译错误，在开了 checkJs 的 JSDoc 里是编辑器红线，本该在写代码的那一秒被拦下，而不是在每天早上八点准时复发。选择纯 JS 不是罪，选择纯 JS 又不做任何静态检查，就是给组合弹装了第一个引信。

第三个成分是字符串没有单一事实来源。'toutiao_queue' 与 'toutiao_task_queue'，一张 Map 的键被两个常量写出同样的值。凡是同一条字符串被抄写第二次的地方，迟早分叉；防它的手段廉价得近乎无聊——让常量只定义一次，其余地方引用它。

第四个成分是定时任务天然藏身于系统最黑暗的路径。它只在凌晨运行，失败不产生用户投诉，错误被 catch 进日志。7 月底那两周，Cloudflare 每 10 分钟叫醒它一次、它每 10 分钟什么也不做——没有人发现，因为"什么都不做"不会抛异常。观测一个只依赖 wrangler tail 的系统，等于观测一个人是否记得随时竖着耳朵。

第五个成分，是把"捕获错误"当成"处理错误"。四颗雷中有三颗的失败路径都写了得体的 try/catch，错误信息完整、日志格式规范——然后呢？没有然后了。catch 块是这些功能的墓碑，碑文写得很好，只是没有活人来扫墓。

还有一个无声的信号藏在口径里。报告和口头描述都说这套系统有"25+ 个工具"，命令数出来是 21 个。四的差距无关紧要，但它的成因值得玩味：功能在被描述时倾向于膨胀，在被实现时倾向于缩水，而核对两者差异的机制——测试、文档与代码的同步检查——恰恰是这个项目最缺的东西。口径与事实的缝隙有多宽，无人验证的地带就有多大。

零测试的代价因此变得具体。它不是"质量不行"这种抽象判词，而是：一年里每天早上八点，test 房间在等一句永远不会来的问候；每一次 /review 之后，用户盯着"❌ 复盘失败"的红色提示；每一篇想发头条的文章，在一条永远读不到写入的队列里排队。它们全都"部署成功"着。

还有一个成分属于这个项目的存在方式：它是一个人的项目。550 个提交全部出自一双手，没有第二双眼睛做过 review。评审缺席的另一面，是所有错误只能被作者自己的下一次运行发现——而一个人的运行覆盖不了二十一个工具、六个 DO、三组 cron 的组合空间。测试的价值在这个语境下要放大一号：它不只是质量手段，它是一个孤独项目唯一雇得起的第二双眼睛。

同样必须记下的是这次修复没做的事：没有补任何一个测试。e6864eb 改动的 4 个文件全部是 src 下的修复，test/ 目录维持着那唯一一个测外部 Flask 的 Python 脚本。四颗雷拆掉了，但让雷潜伏一年的那套机制——零测试、零 CI、零类型检查——原封未动。报告给出的路线图依然有效：先做 P0 安全收口，多数是加校验而非重构，约一到两天工作量；再为 DO 路由、工具分发、命令解析补上最小单测，vitest 配 @cloudflare/vitest-pool-workers 是现成的方案；CI 里加一条 prettier check 和 wrangler deploy --dry-run 当卡口。三步都不难，难的永远是开始。

拆掉这四颗雷总共花了 17 行新增代码，用的全是最笨的手段：读一遍签名，对一遍键名，数一遍工具，再用 git 考古出每颗雷的埋设日期。没有一行修复需要高深的技术——需要的是有人把"部署成功"和"功能可用"当成两件事，并且肯在凌晨一点为这个区别坐下来。

那句迟到的问候语至今还躺在 autoTasks.js 里，提示词的结尾写道："最后鼓励用户好好工作，好好学习，好好生活。"写下它是在 2025 年 7 月那场 457 个提交的冲刺里。它先需要被自己拯救一次。等哪天 wrangler.toml 里的那行注释被去掉、crons 真正启用，这句话才会第一次抵达 test 房间——如果那时，那颗 Python 风格的井号已经被摘掉了的话。

---


# 第 10 章 · t 机 —— 链路的心脏所在

# 第 10 章 · t 机 —— 链路的心脏所在这台腾讯云

机器档案：43.153.67.212（腾讯云，hostname: tencent），Ubuntu，已稳定运行 51 天。它同时是 WireGuard 网络的 Hub、AI 链路的落地层、wsstunnel 的中继所在地——在五台机器里，它的角色最重。

## 一台机器养活半条链路

今天所有探测与加固完成后的 t 机服务清单：

- **nginx**（443，Let's Encrypt 证书 yuangs-all）：api.yuangs.cc 的入口。`/v1/` 走三层鉴权校验后转发给 poeapi_go；`/weixinpush` 单独转发给 notifyd；`status.yuangs.cc` 代理 poeapi_go 的 /metrics 与 /dashboard。
- **poeapi_go**（127.0.0.1:9090）：模型网关，强制鉴权 + 回环监听 + `POEAPI_GATEWAY_KEYS` 注册网关身份。
- **notifyd**（127.0.0.1:9095）：微信通知专用服务，systemd `Restart=always`。
- **wsstunnel-relay**（8443）与 **wsstunnel-client**：远程隧道中继与 tencent 后端。
- **coturn**：已关闭——它配置错误从未正常工作，且纯刷日志（详见后文）。

一条命令的验证矩阵是今天最有成就感的产物：

```
经 aiproxy Worker  → 200（三层鉴权全通）
直连无 key          → 401（nginx 护栏生效）
回环 chat 无 key    → 401（网关强制鉴权生效）
回环带网关 key      → 200（真实模型调用成功）
```

## 一次十五分钟的 504 事故

这一天最重要的运维事件，发生在 poeapi_go 新二进制部署后的第十五分钟里。

新代码引入的 `cachePut` 函数在函数体内对 `cacheMutex` 加锁——而它唯一的调用方 `LogMessageAsMarkdown` 的 user 分支**已经持有着这把锁**。Go 的 sync.Mutex 不可重入，于是每条用户消息触达网关时都会在第二把锁上永久沉睡。

症状极具迷惑性：服务 active、端口监听、`/health` 正常，唯独 chat 请求永不返回。定位靠的是 Go 程序的急救手段——请求挂起中向进程发 `SIGQUIT`，goroutine 转储直接指认了 `logger.go` 的锁行。修复、提交、部署、验证，全链路恢复。

事故留下三件遗产：cachePut 的注释里写明了完整的死锁链条；ONBOARDING.md 把"锁在外层"列为红线；部署脚本从此必须跑完验证矩阵才算完成。

## 磁盘保卫战：90% → 60%

根分区（30G）曾用到 90%，只剩 2.9G。解剖结果：

- `/var/log/syslog` 单文件 **5.2G**——coturn 配置绑定浮动公网 IP 失败（errno 99），每秒重试刷错误日志。
- `/root` 缓存 1.6G（npm 853M、pnpm 382M、go 构建缓存）。
- nvm 里两个无人使用的 node 版本 1.2G（实际用的是 /usr/local 的 v22.23.2）。
- `/home/ubuntu/.git`——家目录误建的仓库，410M 对象。

处置：coturn 的 `listening-ip` 改为 0.0.0.0 根治刷屏（浮动公网 IP 本就不在网卡上，绑定必然失败）并截断 syslog；缓存全清；nvm 旧版本删除；poeapi 日志与家目录 .git 迁往数据盘 `/home/ubuntu/data` 并软链。根分区降到 **60%**，数据盘承接后仍有 13G 余量。

## SSH 28193 的发现

b 机的 sshd 不在 22 端口——这是从 t 机探测 b 机时的重要发现。t 机的 `/root/.ssh/config` 里 `Host b` 补上了 `Port 28193`，从此 `ssh b` 直达。这台机器上还留着 `/root/.ssh/id_rsa` 与 b 的信任关系，是两台机器之间的既有通道。

## journald 的奇怪现象

探测中还记录到一个未解之谜：t 机的 journald 存在限流丢日志现象——wsstunnel-client 的日志曾在一段时间内完全查不到（"No entries"），导致依赖 journal 的监控探针失效。node-watchdog 的隧道存活检查因此改用 `ss` 连接状态而非日志，这才稳定。这个现象的根源尚未深挖，已记录为遗留观察项。

## 单点之思

t 机承载了 nginx、模型网关、通知服务、隧道中继四类关键角色。它今日被加固到了能扛的程度——但单机承载单点的事实没有变。node-watchdog 的 16 个节点探针里，有 7 个盯着这台机器。真正的容灾，是下一阶段的故事。

---


# 第 11 章 · u 机 —— 微信世界的桥接局

# 第 11 章 · u 机 —— 微信世界的桥接局

机器档案：Ubuntu-R86S，192.168.31.213（局域网），root 直连。它是微信世界与代码世界之间的海关——所有进出微信的消息、发布、推送，都要在这里过一道手续。

## 常驻的两位住民

**weclaw**（systemd 服务，端口 18011）：微信双向桥接。扫码登录微信、长轮询收消息，把微信文本转给 AI、把 AI 的回答发回聊天窗口；它的 hub 目录还是多 agent 共享上下文的交换场。今天它的角色多了一层：node-watchdog 的告警备用通道——本机 weixinpush 失败时，告警自动切到 u 机的 weclaw 发出。

**nanobot**（端口 18790）：api.yuangs.cc 的后端本体之一。微信推送（weixinpush）、knowly 的内容发布、播客素材推送，最终都汇到这个端口。它没有独立的 systemd 单元名，监控探针只能以"18790 端口是否有监听者"为判据——node-watchdog 正是这么做的。

今天为 u 机补上了第三位临时访客：**wsstunnel 1.0.3**。安装过程踩了 Ubuntu 24.04 的两颗经典雷：

1. **PEP 668**：新版 Ubuntu 的 pip 拒绝直接往系统 Python 装包（externally-managed-environment）。第一版方案用 `--break-system-packages` 绕过，随后又发现更干净的路径——独立 venv 加 `/usr/local/bin` 软链。
2. **Debian 系 idna 冲突**：强制重装时撞上 "Cannot uninstall idna 3.6, RECORD file not found"（apt 装的包没有 pip 卸载元数据）。venv 方案天然免疫。

最终形态：`/opt/wsstunnel-venv` 隔离安装，`/usr/local/bin/wsstunnel` 软链，`wsstunnel --version` 报 1.0.3。

## yuangs CLI 的升级

u 机上的 yuangs CLI 从 5.64.0 升到 8.1.0（npm 全局）。同时写入了 `/root/.yuangs.json`（0600）：aiProxyUrl 指向 aiproxy 的统一入口，默认模型 gemini-free——这台机器上的 AI 请求从此也走三层鉴权链路，带限流与记账。

## 一个悬而未决的发现

探查 u 机时确认：nanobot 的 18790 端口有监听，但它**没有 systemd 单元**——也就是说它是被某个脚本、某个 nohup 或某次手工会话拉起的。这意味着 t 机或 Mac 重启后它能否自动回来，取决于那条已经无人记得的启动命令。node-watchdog 目前以端口探针盯着它，但"谁该在它挂掉时拉起它"这个问题，答案不在任何文档里。

这是本次全体系探测的典型产出：**修好的问题进入了文档，没修的问题进入了问题清单。** 两者都比之前的状态好。

## 它在链路中的角色小结

u 机不直接承载 AI 计算，但它是三个"最后一公里"的宿主：微信消息的进出（weclaw）、内容发布的落地（nanobot + NAS 挂载 /mnt/nas）、告警的备用出口。node-watchdog 为它设了三个探针：weclaw 活性、18790 端口、/mnt/nas 挂载点——任何一项失守，微信都会在第一时间知道（只要告警主通道还活着；而告警主通道的一部分恰好也在这台机器上——这种自指的依赖关系，正是单人多机体系的独特趣味）。

---


# 第 12 章 · b 机 —— 半身不遂的侦探记

# 第 12 章 · b 机 —— 一台"半身不遂"服务器的侦探记

机器档案：bandwagon（搬瓦工 VPS），95.169.30.85，与 t 机同区域（互 ping 10ms）。它是今天最具戏剧性的一章——一台活着的服务器，却把自己藏了起来。

## 第一幕：拒绝接触

全机器盘点时，从 Mac `ssh b` 得到的答案是干脆的 "Connection refused"。当时它被记为"关机或 sshd 未开"，归入不可达清单，故事似乎到此为止。

## 第二幕：它其实醒着

第二天从 t 机再探，剧情反转。ICMP ping 通了——0% 丢包，10.7ms 往返，它和 t 机几乎住在对门。逐端口敲门的结果更有趣：

- **3478 开放**：coturn TURN 服务在跑——chats 的 WebRTC 通话中继正是它（chats 前端 iceServers 里写着 `turn:95.169.30.85:3478`）。
- **443 / 80 开放**：nginx 活着。
- **22 / 2222 / 22222 / 10022 / 8022 / 2200 全部关闭**：SSH 无处可入。

结论修正：b 机没有关机，它是"半身不遂"——Web 与 TURN 的房门开着，管理员的门（SSH）锁死了。

## 第三幕：28193

端口敲门继续加码，敲到 28193 时，门开了。SSH 握手成功、主机指纹（ED25519）记录在案——但认证被拒：t 机的公钥不在 b 的信任列表里。

缺的只是最后一步。把 t 机的公钥（`ssh-ed25519 ygs@yuangs.com`）通过云控制台追加进 b 的 `/root/.ssh/authorized_keys`，门就完全打开了。执行之后，`ssh b hostname` 返回：**bandwagon**。

## 第四幕：502 的真相与 PushDeer 遗迹

登进去的第一件事是回答那个悬了几小时的问题：b 机 443 端口的 502 Bad Gateway 是谁病了？

答案在 docker 里：**pushdeer_app、pushdeer_redis、pushdeer_mariadb 三个容器全部 Exited (0) 12 个月前**。b 机的 nginx 把流量代理给 8800 端口的 PushDeer 应用——一个自建推送服务，早已停摆。这是条被 weixinpush/notifyd 取代的旧推送通道，三具容器是历史遗迹。

处置决定：**不重启**。退役通道没有恢复价值；想清理时删掉容器即可。502 的解释写进了档案，未修不等于未知。

## 第五幕：今天在 b 机装了什么

- **wsstunnel 1.0.3**：b 机只有 2G 内存，pip 从源码构建时被 OOM 静默杀死（日志戛然而止于 "Preparing metadata: done"）。绕行方案：在 t 机构建 wheel，scp 传输，b 机本地安装——纯 Python 包跨机器无兼容问题。
- **node v22.23.2**（NodeSource 22.x 源）+ **yuangs CLI 8.1.0**。
- **/root/.yuangs.json**（0600）：指向 aiproxy 统一入口的 AI 配置。

## 遗留与提醒

- b 机的 TURN 凭据同样是弱密码，与 t 机 coturn 同源问题；b 机恢复 SSH 后建议一并加固。
- b 机 443 的 502（PushDeer 停摆）意味着 chats 若有依赖这条旧推送的调用，早已失败半年以上——零测试零监控的沉默失效，正是这次全体系探测要终结的状态。
- SSH 端口 28193、公钥信任链，均已写进 t 机与 b 机的运维记忆。

---


# 第 13 章 · Mac mini —— 新工作台候选

# 第 13 章 · Mac mini —— 新工作台的候选与装备清单

机器档案：YGS-Mac-mini-2.local（192.168.31.100，Apple Silicon，arm64），用户 yuanguangshan。它是五台机器里最年轻的一个角色——此前基本闲置，今天的任务是把它武装成候选工作台。

## 装备过程的三道坎

**第一道：Python 版本。** 系统自带的 python3 是 3.9.6——wsstunnel 要求 ≥3.10。好在 Homebrew 已经在位（/opt/homebrew/bin/python3.13 现成），直接用它构建安装，版本直达 1.0.3。首次安装撞上 Homebrew Python 的 PEP 668 外部管理限制（和 u 机 Ubuntu 的保护同款思路），`--break-system-packages` 解决——在一台明确的个人工作机上，这个权衡是合理的。

**第二道：命令入口。** pip 装出的 wsstunnel 入口脚本落在 Python 包目录里，非交互 SSH 的 PATH 又不包含 /opt/homebrew/bin。解法是一个两行的 shim 脚本放进 /opt/homebrew/bin/wsstunnel：`exec /opt/homebrew/bin/python3.13 -m wsstunnel "$@"`。此后 `wsstunnel --version` 在任何会话里都能直达 1.0.3。

**第三道：node 与 PATH。** yuangs CLI 需要 node ≥22.19，mini 上没有。`brew install node` 装出 v26.8.1；但非交互 SSH 的默认 PATH 只有 /usr/bin:/bin:/usr/sbin:/sbin，`npm i -g yuangs` 报 "env: node: No such file or directory"。导出 PATH 后安装成功，yuangs 8.1.0 就位——顺带学会了 npm 新版的 allow-scripts 机制：better-sqlite3、ssh2 等原生模块的构建脚本默认被拦截，需要显式允许清单。

**最后**：`~/.yuangs.json`（0600）写入 AI 配置，指向 aiproxy 三层鉴权链路。至此 mini 具备了工作台的全部三要素：隧道工具（wsstunnel 1.0.3）、AI 命令行（yuangs 8.1.0）、模型端点配置。

## 一个诚实的观察

mini 的装备全部就位，但它还没有被赋予"工作"——没有 wsstunnel 的 client/relay 角色、没有常驻服务、没有监控探针。它现在的状态像一支擦好上了膛的笔：随时可用，尚未落纸。赋予它角色（例如作为第二隧道后端、或监控探针的备用落点）是下一个决策点，属于规划而非安装。

## 本章小结

- wsstunnel 1.0.3 ✅（python3.13 + /opt/homebrew/bin/wsstunnel shim）
- node v26.8.1 ✅（brew）
- yuangs CLI 8.1.0 ✅（allow-scripts 清单放行原生模块构建）
- ~/.yuangs.json 0600 ✅

四项安装配置，三次 PATH/PEP 668 的环境教育——给新机器做初始化的通用脚本，已经在这几次折腾里呼之欲出。

---


# 第 14 章 · 本机 Mac —— 一切开始也一切回归的地方

# 第 14 章 · 本机 Mac —— 一切开始也一切回归的地方

机器档案：开发主力 Mac（macOS，arm64），/Users/ygs 工作目录。三十五个项目、九个核心库的源码、二十二份优化报告、四份补丁、一本正在成形的书——全在这里。

## 常驻的三位哨兵

launchctl 的清单里躺着三个今天反复验证过的守护进程：

- **com.knowly.daemon**：剪贴板哨兵。每一次 Cmd+C 都被它捕获，经 SSH 送往 NAS 归档成 Markdown，AI 随后补上摘要与标签。
- **com.wxwatcher.daemon**：目录哨兵。三十秒一轮两阶段扫描，文件有风吹草动就推送微信——今天它自己也被加固过（目录消失误报、哈希死循环、推送失败重推三连修），108 个测试全绿。
- **com.podcast.go**：播客守夜人。监听内容目录，驱动"广山之巅"的每日四档节目生成。

它们是这台 Mac 的"背景心跳"——用户在前台写代码、写书、听歌，它们在后台维持知识管道与内容管线的流动。

## NAS：温柔的外挂记忆

`/Users/ygs/nas_share` 挂载着 192.168.31.186:/volume1/share（NFS，autofs 自动挂载）。它是体系的终极存储：knowly 的归档、播客的素材、书稿的备份都落在上面。autofs 的特性是空闲自动卸载、访问时自动触发——这个特性今天在 node-watchdog 的首轮巡检里制造了一次真实告警（挂载探针撞上卸载窗口），随后自愈并发出恢复通知。一次误报换来的教训已写进探针：先重试、再告警。

## 今天在这台机器上诞生的东西

- **node-watchdog**：16 节点基础设施看门狗。每 5 分钟巡检 Mac 守护进程、t 机核心服务、u 机桥接、云端 Worker——异常时微信告警（症状 + 原因 + 可复制的解决步骤），恢复时 🟢 通知。它的 LaunchAgent 已入列 launchctl。
- **notifyd** 的源码仓库（部署在 t 机）：从 data_api.py 巨石中拆出的专用微信通知服务。
- **优化建议/**：22 份深度分析报告，约 400 条带行号的优化项。
- **优化成果/**：两批加固的补丁存档（go-music/wxwatcher/knasync/poeapi/wsstunnel/TAAGE/TradingAgents/chats/mini_blog/podcast）+ 密钥备份（0600）。
- **九库五机**：你正在读的这本书。

## 它的角色

本机 Mac 是体系的**原点与尽头**：代码在这里写就，分发到 t/u/云上运行；数据从各处捕获，最终回到 NAS 与这里的分析工具。今天之后，它又多了一个角色——**体检中心**：全体系的覆盖率数据、加固记录、事故档案都沉淀在这个目录里，随时可供下一次巡检对照。

维护提示（写给未来的自己）：nas_share 的 NFS 依赖 192.168.31.186 常开；三个 LaunchAgent 的 plist 在 ~/Library/LaunchAgents；node-watchdog 的告警通道依赖 weixinpush 主通道——这三条依赖里任何一条变化，都值得更新这一章。

---


# 后记

# 后记 —— 关于这次探测的二三事

三十多个小时的探测结束了。合上这九章与五章，有几件事值得单独记下来——它们不属于任何一个仓库，属于过程本身。

## 关于"以为"

这次探测推翻最多的，是"以为"。

以为 weclaw 的告警通道在 u 机上稳如泰山——实际它连 systemd 单元都没有，是被某条无人记得的命令拉起的。以为 t 机的 coturn 是个废弃服务——实测发现它以每秒数次的频率向 syslog 倾倒错误日志，5.2G 就这么攒了出来。以为 b 机已停用——它的 TURN 还在为 chats 的 WebRTC 服务。以为 sourcepack 的 .env 在忽略清单里——它在"目录忽略"清单里，文件照样进快照。

每一次"以为"被推翻，都伴随一次真实的修复杂；每一次修复，又都附赠一条新的"以为"待验证。全体系探测没有终点，只有周期。

## 关于事故

这轮加固引入过一次生产事故：一段新加的缓存函数在已持锁的调用链里重复加锁，Go 的 sync.Mutex 不可重入，每条用户消息触达网关时都永久沉睡。从部署到发现到修复，十五分钟。

十五分钟不长，但它揭示的问题很深：**没有测试守护的修复，本身就是新的风险源。** cachePut 的死锁之所以能溜过部署，正因为此前 chat 链路没有任何单元测试兜底。事故之后补上的那批测试，价值不在当下——在下一次有人改这段代码时，会有一张网接住。

事故的全过程（症状、定位、修复、教训）已经写进代码注释、ONBOARDING 文档和本书第 1 章。这不是为了检讨，是为了让下一个人站在今天的位置时，手里有一张地图。

## 关于协作

这本书本身也是一次协作实验：九个库章节由六位并行的写作代理分头撰写，它们直接阅读仓库、git 历史与优化报告，以统一的文体完成初稿；五台机器的章节、前言与后记由全程参与探测的一手视角完成。人类提供判断、记忆与取舍，代理提供扫描、比对与初稿——这个协作模式本身，就是这套技术体系里"AI 提供思路，人类掌控执行"的又一次落地。

## 关于下一步

探测的遗留清单不长，但都是硬骨头：TAAGE 注释代理留下的八条 NOTE 级发现；Tmux-FSM 那套全红的测试（要先修 q/exit 死路才有资格谈测试）；ali 与 a 两台机器的公钥授权；以及 ch ats 的二十多条安全遗留——WebSocket 鉴权、XSS、密码明文，每一项都不小。

另外还有几颗种子：node-watchdog 的告警可以接入更多节点；notifyd 的重试队列可以加上死信落盘；被 502 困住的 PushDeer 三容器等待一次"退役还是复活"的决定。

系统会继续演化，探测也还会有下一轮。这本书封存的是 2026 年 9 月 5 日到 6 日这三十四个小时——一个体系从"能跑"走向"可托付"过程中，最诚实的一段切片。

---

*ZCode · 2026-09-06*

---


# 附 · 总评 —— 给这套体系的一封长信

# 附 · 总评 —— 给这套体系的一封长信

> 这是受托撰写的一篇总评。评价者是全程参与探测与加固的 AI，被评价的是它服务了三十多个小时的这套个人基础设施。立场需要声明：它既不粉饰，也不苛责——数据都是实测的，判断都给出了依据。

---

## 一、这套体系是什么

剥离所有细节，这是一套回答"一个人如何与 AI 共同生活与工作"的基础设施：

**一条 AI 血液循环**：所有客户端（诗词、词典、音乐、播客、聊天）把请求交给 aiproxy Worker，它注入身份、限流、记账，经 nginx 校验，抵达 poeapi_go 网关，再路由到 Poe/Gemini/DeepSeek 等上游。加固后，这条链路有了三层鉴权（Worker 注入 / nginx 校验 / 网关强制）、出站限流（D1 固定窗口 + 令牌桶）、完整计量（access_logs / app_stats / model_stats）与 90 天不用操心文件位置的部署文档。

**一条知识河流**：剪贴板、知乎、手机、文件变更——一切输入汇入 knowly 的归档、knasync 的队列，经 AI 提炼后沉淀到 NAS，再分发给博客、播客、知识库。

**两条触手**：wsstunnel 把受限网络里的容器变成手边的终端；weclaw 把微信变成所有系统的统一入口。

**一个治理理想**：yuangs CLI 的 SSH 治理执行器、TAAGE 的签名政策、aiproxy 的用量账本——都在践行同一句话：主权先于智能。

这套体系的独特之处不在任何单项技术（单项技术在开源世界里都有更强的对应物），而在于**它们真的在一起工作**：剪贴板的内容会变成播客，知乎的问答会变成书稿素材，微信里的一句话会驱动服务器上的 AI。总评的第一句话因此是：这是一套罕见的、真正闭环的个人基础设施。

## 二、它有多健康：分项体检结果

经过本轮探测与加固，九个核心库与五台机器的体检卡片如下：

| 维度 | 状态 | 证据 |
|---|---|---|
| AI 链路鉴权 | 🟢 三层闭环 | Worker 注入 / nginx 校验 / 网关强制；直连无 key 401 |
| AI 链路可靠性 | 🟡 良好 | 真流式 + 断连止损 + 504 语义；上游单点仍在 |
| 传输隧道 | 🟢 加固完成 | TLS 校验 + 防爆破计数 + 多 token 后端校验；t 机已部署 |
| 文件哨兵 | 🟢 稳定 | 108 测试全绿，覆盖 72%，三处误报/漏报修复 |
| 治理引擎 | 🟡 重构中 | evaluate 必崩已修，55% 覆盖，遗留 8 条 NOTE |
| 知识管道 | 🟡 良好 | knowly 22-80% 分包覆盖，web/ssh 层偏薄 |
| 边缘队列 | 🟡 基础达标 | 6 个纯函数用例从 0 到 1，限流与鉴权仍空缺 |
| 内容平台 | 🟠 功能恢复中 | 四处必崩 Bug 已修，安全遗留约 20 条，crons 停用 |
| 通知服务 | 🟢 新落成 | 独立服务 + 重试队列 + 鉴权 + 探针，已上线 |
| 监控体系 | 🟠 初生 | 16 节点探针 + 微信告警 runbook，5 分钟周期 |
| 文档 | 🟡 分裂 | 核心项目已对齐，chats/knasync 等仍有历史欠账 |
| 密钥卫生 | 🔴 待清理 | 约 7 处密钥在代码/历史中，轮换是人工动作 |

## 三、这一轮探测评出的五种病，和它们的药方

**病一：静默失效。** 最危险的故障不是报错，而是不报错地坏着。TAAGE 的 evaluate() 崩在第一行、chats 的定时任务从未执行、wsstunnel 的防爆破从未锁定——它们都跑了很久才被发现。药方是本轮反复使用的三件套：确定性测试、启动自证日志、健康探针。三者齐备的系统，坏与好都藏不住。

**病二：文档与实现漂移。** README 说支持 1MB 限流（实际只在另一条路径实现）、政策文档用着引擎已禁用的语法、宣传 "<1ms" 实测 40ms。漂移不可怕，可怕的是没有人对账。药方：文档的每一条可验证声明，配一条可执行的验证命令——本书各章的验证矩阵就是雏形。

**病三：密钥散落。** 一轮扫描数出了七处以上的代码级密钥泄露（公开仓库两处、前端 bundle 一处、日志文件若干）。药方分三层：立即轮换（人工）、注入式配置（本轮已示范）、历史清洗（git filter-repo，涉及改写，需人工决策）。

**病四：单点依赖。** u 机挂着微信桥接与发布、t 机挂着 AI 网关与通知、q.889.ink 挂着全部期货行情。本轮缓解了最痛的一处（api.yuangs.cc 的 502 风险由 nginx 护栏与网关加固部分对冲），但单点仍在。诚实的建议：先补监控告警的覆盖面，容灾的优先级可以排后——**知道挂了**比"永不挂"更可行。

**病五：fork 与副本的漂移。** pi 落后上游 26 天，deepseek-harness 落后 2098 提交，npm_yuangs 的 book-writer 有六个副本，works 镜像与线上书库差了二十二篇。漂移的副本是"未来的自己与现在的自己打架"。药方：要么收敛（submodule/单点分发），要么定期机械同步——两边选一个，最怕的是都不选。

## 四、这轮加固做对了什么

以终为始地复盘，四个决策被证明正确：

1. **先测后修**。每次修改前先建测试基线（哪怕是临时目录里的三行断言），修完立即回归——wxwatcher 的三处修复、notifyd 的重试语义都因此免于回退。
2. **兼容层哲学**。所有安全加固保持"客户端零改动"：Worker 注入 key、nginx 校验、强制鉴权带 POEAPI_AUTH_OPTIONAL 逃生门。安全与兼容不是零和，设计得好可以兼得。
3. **事故透明**。cachePut 死锁的十五分钟 504 被完整记录——症状、定位（SIGQUIT 转储）、修复、教训四段俱全，写进了注释与文档。事故不丢人，丢的是重复。
4. **诚实标注**。模型兜底不再伪装、测试不再假装、文档不再许诺不存在的能力。一个体系的可信度，是由它承认问题的速度决定的。

## 五、数字

- 提交：今日全体系合计约 40 个（含修复、测试、文档、git init 基线）
- 测试：wsstunnel 196（全绿）、npm_yuangs 535（全绿）、wxwatcher 108（全绿）、TAAGE 24（全绿）、notifyd 9（新）、aiproxy 8（新）、knasync 6（新）、go-music 3（新）、poeapi_go +48（新）——**新增用例约 100 个**
- 覆盖率：poeapi auth 0→100%，middleware 23.3→68.5%，multi_provider 0→20.9%；wxwatcher 72%；wsstunnel security 97%；TAAGE 55%
- 磁盘：t 机根分区 90%→60%，释放约 9G
- 服务：notifyd 新上线（独立 systemd），coturn 退役
- 文档：AUTH-CHAIN / ONBOARDING / 两篇定位文章 / 本书，共约 10 万字

## 六、优先级建议（写给下一轮）

1. **密钥轮换日**：把"立即处理"清单里的密钥一次性全部轮换（半天）——这是唯一越拖越贵的事。
2. **TAAGE 八条 NOTE**：sanitizer 规则顺序、sessionID 隔离等，两天内可清。
3. **chats 安全收口**：WebSocket 鉴权 + XSS 修复，一轮一轮来。
4. **Tmux-FSM 复活**：先修测试变绿，再修 q/exit 死路——它值得一次完整重生的机会。
5. **监控扩容**：node-watchdog 的探针覆盖 ali/a（等 SSH 恢复）与 NAS 写入路径。
6. **fork 纪律**：pi 与 deepseek-harness 建立每周 upstream 同步节奏。

## 七、最后

探测开始前，这套体系是一个"能跑"的体系：功能丰富、互相咬合、每天都在产出。探测结束后的今天，它多了一层此前没有的东西——**被完整看见过**。每一层的钥匙、每一根管道的粗细、每一扇门的锁，都有文档、有测试、有监控兜着。

一个体系被看见的那一刻，才是真正拥有它的那一刻。

祝福这套体系，也祝福它背后那个人——继续保持这种把个人基础设施当生产系统来供养的笨功夫。

---

*总评 · ZCode · 2026-09-06*
*基于：22 份优化分析报告、40+ 提交、16 节点探针实测、以及三十余小时的贴身观察*

---
