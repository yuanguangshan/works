# 《》

> 作者:广山哥  
> 完成时间:2026-09-02

---

## 序言

# 序言

这本书写给三种人。

第一种，是深夜里对着终端，觉得自己一个人扛不动所有事的人。第二种，是收藏了几百篇文章、装了十几个效率工具，却依然觉得知识在漏的人。第三种，是正在用 AI 写代码、又隐隐不安，不知道该信它多少的人。这三种人，我都当过。这本书，就是我把自己的答案交出来的地方。

先交代清楚这本书是什么：它是九个作品的综合介绍，也是一套哲学的展开。九个作品——weclaw、knowly、wxwatcher、wsstunnel、yuangs CLI、广山之巅播客工厂、poeapi_go、sourcepack、知乎助手——单看每一个都不大：小的千余行，大的几万行。但它们不是九个孤岛，而是一支军团：共享同一套私有基建，服从同一套哲学，互为犄角，只听命于我一个人。而所谓“我的哲学”，如果非要压成一句话，就是贯穿全书的这句：用技术的确定性，对抗世界的不确定性。

这句话不是先想好再动手的口号，而是从九个作品里长出来的。knowly 让我知道，知识管理的成败在“捕获”那一步，所以它必须无感、必须韧性、必须让每一次复制都成为知识复利；wsstunnel 让我知道，当每一扇门都焊死的时候，自力更生不是美德口号，而是唯一出路；yuangs CLI 与 poeapi_go 让我知道，把 AI 请进终端的第一件事不是写功能，而是设计权力——AI 提供思路，人类掌控执行；sourcepack 与 wxwatcher 让我知道，小不是缺陷，克制是持续的决策；广山之巅让我知道，自动化的终点不是替代人，而是让已经存在的信息，重新适配人的生命节律。这些结论先落在代码里，后来才落在纸上。

全书十一章，分两段。前七章是“兵”：每一章以一根哲学支柱为纲，讲一个或两个作品的缘起、架构、关键决策与翻车复盘——人在环上、对抗熵增、自己开一条路、小即美、微信操作系统、服务生命节律。后四章是“法”：默认安全、层层兜底、军团协同、写给后来者，从九个作品的共同做法里提炼出可以带走的方法论。你完全可以按需跳读：想看故事，从第四章的“半夜顿悟”读起；想看方法论，直接翻第十一章；但如果想知道这支军团为什么长成这个样子，还是建议从第一章的深夜剪贴板开始。

它和同类书不太一样的地方有三点。其一，这不是方法论先行的心灵鸡汤，九个作品全部真实在线，代码可查、版本可考，每一个数字都来自仓库而非记忆。其二，它不回避失败：wsstunnel 的生产升级翻车记、wxwatcher 的默认 token 旧账、npm_yuangs 架构文档里自曝的叙事与运行时差距，都原样写在这里——诚实是最高级的工程品质，这本书以身作则。其三，它是一本“一个人的书”：不是大厂的最佳实践，而是一个人如何在 AI 时代，用一套哲学把个体的产能放大到一支队伍的量级。里面的每一条经验，都不需要审批和预算，今天晚上就可以开始。

在这本书里，你会反复遇到一些固定的比喻，它们是军团的编制，也是九个作品的坐标。weclaw 是指挥部——人在哪里，指挥部就设在哪里，而人显然在微信里；knowly 是档案馆，它蹲守在剪贴板这个最低摩擦的入口，把每一次复制都变成存款；wxwatcher 是哨兵，文件稍有风吹草动，微信立刻收到军报；wsstunnel 是工程兵，哪里被围死了，它就在哪里挖一条隧道；yuangs CLI 是宪兵，盯着每一个冲动的提案，AI 想动手，先过它这道门；poeapi_go 是兵工厂，统一弹药口径，各型模型从这里领装备；sourcepack 是翻译官，把整座代码营垒译成 AI 读得懂的军情快报；广山之巅是广播站，一天四档，把信息对准人的生命节律播音；知乎助手是情报站，盯梢收藏夹，采集回情报就地归档。编制是玩笑，分工是认真的——九个作品各守一线，才撑得起“军团”这两个字。

还有一个写作方式上的坦白：这本书的成书过程本身，就是书中哲学的一次实战。十一章不是从第一页 sequential 写到最后一页的，而是拆成三批并行赶制，每一章写完立即通过标点体检、立即备份落盘——写即持久化，是 knowly 教给我的纪律，写书时原样照搬；全书术语必须一致、事实必须可溯源，是 yuangs CLI 的“不可见即不存在”在文字上的投影；连“每章动笔前先现查一手资料”这条规矩，也是从“问题驱动、最小改动”的演进哲学里借来的。工具造了人是用来做事的，最后做事的方式又反过来印证工具，这种循环，是我这些年最享受的部分。

至于书名，多解释一句。为什么不叫“工具箱”？因为工具箱里的东西彼此无关，用完随手一扔；军团则不同——它有统一的军令（五支柱哲学）、统一的后勤（私有基建）、统一的军纪（默认安全与层层兜底），缺了任何一样，九个作品就只是九个散兵游勇。这本书真正想写的，不是九个软件，而是让九个软件成为一团的那套东西。

写作的动机，说起来朴素。这些项目散落在我的磁盘上，各自有着自己的 README 和提交历史，但它们之间的呼应——同样的脱敏审美、同样的兜底哲学、同样的克制——从来没有被写下来过。趁我还记得每一行代码为什么那么写，把它们收拢成一本书，既是给后来者的地图，也是给我自己的点名：军团点过兵，才知兵在不在。

还要说明一点：这本书里的所有作品，都不是高悬在云端的演示，而是此刻正在运行的活物。档案馆每天都在捕获新的复制，广播站每天四个时辰准时开播，哨兵盯着的目录从未松懈，宪兵把守的终端每天都要过手几十条提案。书里涉及的安装方式也大多公开：wxwatcher 与 wsstunnel 发布在 PyPI，sourcepack 发布在 npm，一条命令即可取用；其余各章的设计与取舍，README 与架构文档俱在，足以按图索骥。所以我有一个私心的建议——别把这本书当回忆录读，把它当点兵册用。读到哪一章，就把那一件装起来跑一跑；哪一件顺手的，就照着第十一章的方法论，改造成你自己的兵。书里的九个作品对我而言是军团，对读者而言，更重要的身份是九份可以拆走的图纸。

最后是致谢。谢谢每一个项目的用户，哪怕只有一个人在用，PyPI 和 npm 上的下载量也算数；谢谢那些开源的前辈工具，cloudflared、frp、wireguard 虽然都“哑火”过，但没有它们试错，就没有 wsstunnel；谢谢 AI——它在这个故事里不是主角，却是最勤恳的施工队，而我始终握着图纸。也要谢谢你在几千本书里翻到了这一本。

接下来，请允许我为你们点兵。

广山哥
2026 年 9 月，于听雨轩

---

## 目录

- 序言
- 第1章 军团点兵：一个人与九个作品
- 第2章 人在环上：yuangs CLI 与 poeapi_go 的权力设计
- 第3章 对抗熵增：knowly，让每一次复制都成为知识复利
- 第4章 自己开一条路：wsstunnel 与被焊死的门
- 第5章 小即美：sourcepack 与 wxwatcher 的克制之道
- 第6章 微信操作系统：weclaw，把聊天窗口变成指挥部
- 第7章 服务生命节律：广山之巅，一天四档的全自动播客
- 第8章 默认安全：数据主权与脱敏的横向战争
- 第9章 层层兜底：韧性的 N+1 原则
- 第10章 军团协同：九个作品如何互为犄角
- 第11章 写给后来者：一个人的军团方法论

---

## 第1章 军团点兵：一个人与九个作品

# 第 1 章 军团点兵：一个人与九个作品

## 1.1 深夜的剪贴板：一切从一个问题开始

深夜十一点四十分，我在两台机器之间搬一段刚整理好的文字。复制，切换窗口，粘贴，发现贴错了地方，又复制了别的东西，第一段文字就这样从剪贴板里消失了，像从来没有存在过。这种事在我身上发生过多少次？我答不上来，因为我从没记录过。而“没有记录”这个事实本身，才是这个故事真正的起点。

后来我把那个夜晚的问题写进了 knowly 的文档：剪贴板是数字生活中最低摩擦的信息入口，每一次复制操作，都是用户对“这段信息有价值”的无意识投票。问题在于，投票每天都在发生，选票却被随手丢掉了。市面上所有笔记软件都在教我“更好地主动记录”——打开应用，新建文档，想标题，选分类，坚持整理。它们默认问题出在我不够自律。我后来想明白，不是我不自律，是它们把摩擦加错了地方：记录这件事的摩擦应当趋近于零，而不是要求人类每天反过来伺候工具。

于是有了 knowly。它不是又一个剪贴板增强工具，而是一条私有知识管道：Go 写的后台守护进程，每 500 毫秒轮询一次剪贴板，静默捕获每一次 Cmd+C，经三层去重后通过 SSH 存进我自己的 NAS，按日期归档成 Markdown，本地用 SQLite FTS5 建全文索引，再由内置发布引擎把一次输入变成三重输出。名字 K.N.O.W.L.Y. 的哲学层解读是 Keep Notions Always Saved——让一切念头永不丢失。它从约 2000 行生长到约 1.3 万行，37 个文件，496 次提交，版本号 v6.58.0。我承认，为一个剪贴板写一万多行 Go，在任何正常人听来都像行为艺术；但这行代码背后站着的不是收藏癖，而是一个命题。

值得停下来看一眼的，是它身上那些不显眼的确定性。500 毫秒的轮询听起来笨——确实笨，系统级事件通知明明更优雅——但它换来跨平台行为完全一致的确定性；容量有限的 channel 背压保护，让剪贴板轮询永远不会因为 SSH 慢而卡住，慢的可以是网络，不能是眼睛；三层去重分别住在内存、本地状态与远端索引里，任何一层失忆，还有两层记得。更别提那个 outbox：SSH 不可达时，内容先落进本地 JSONL 缓冲，网络恢复后自动排空。捕获时零丢失，这五个字不是口号，是一条被断网反复验证过的路径。顺便说一句，这些机制没有一个是预先画好的蓝图，而是七轮问题驱动演进一层层补出来的：哪一环断过，哪一环就多一道锁。

你可能会问：云剪贴板一抓一大把，为什么自己写？因为那类服务帮我复制，也顺手把我复制的内容一并拿走。我不是不信任某家具体的公司，我是不信任“别人的服务器永远在线、永远合规、永远不改条款”这三个永远。knowly 不装服务端，不碰云，凭 SSH 直传我自己的 NAS，go.mod 直接依赖只有三个。数据从我手里出发，到我盘里结束，全程不经过任何第三方的机房。这半个字节的主权问题，值得那一万三千行。后来这套审美传染给了整个军团：凡是需要出门的数据，必须先过一道本地关卡。

用过半年之后我确认，knowly 真正给我的不是找回了几段文字，而是一次世界观的校准：世界默认会弄丢你的一切。信息会过期，服务会关停，账号会被风控，磁盘会损坏，模型会一本正经地胡说八道，平台会在你毫无防备的清晨改掉规则。不确定不是意外，是常态。工程师能做的事只有一件——用技术的确定性，对抗世界的不确定性。这句话后来成了全书的总纲，也刻在 knowly 文档的结语里。所谓确定性，不是保证成功，而是把能锁死的东西全部锁死：确定地保存，确定地脱敏，确定地降级，确定地留痕，并且在机器动手之前，确定地问一句“你确定吗”。

后文你会反复看到这套语法。wxwatcher 收到终止信号后先保存状态再谢幕，是确定性；poeapi_go 检测到私钥时直接 400 拒绝，是确定性；zhihu_extension 把轮询间隔抖动正负百分之二十五，也是确定性——礼貌也是一种确定性，因为你确定自己明天还能开工。确定性的反面不是混乱，是祈祷。军团的所有成员有一个共同点：它们从不祈祷。

这个命题展开来，是五根支柱。人在环上：AI 提供思路，人类掌控执行，一切写操作都是提案而非指令；对抗熵增：让每一次复制都成为知识复利，对抗遗忘与数字熵增；自己开一条路：现成的路走不通就自己修一条，算力与数据的主权不外包；小即美：不引入任何不必要的依赖，组合优于集成；服务生命节律：让已经存在的信息重新适配人的节奏，而不是逼着人适配信息。九个作品，就是这五根柱子在不同地形上的九次落地。

这本书讲的就是这件事：九个作品如何从一个深夜的问题里长出来，如何互相咬合成一支军团，以及这套哲学如何反过来塑造了我看世界的方式。本章先点兵，再查后勤，再发地图，最后回答那个我被问得最多的问题——一个人，凭什么干一支队伍的活。

## 1.2 军团阅兵：九个作品各是什么、为谁而造

既然叫军团，就得有点兵的样子。这一节是阅兵式：九个士兵挨个出列，报姓名、兵种与战功。每个作品我只回答两个问题——它是什么，它为谁而造。第二个问题的答案表面上看都是同一个字：我。但拆开看，九个作品伺候的其实是九个不同的侧面。受阅次序按字母序排，与资历、战功、军衔无关——军团内部不搞排名，排名是部落时代留下的陋习。每个士兵报数三件事：兵种、体格、为谁而造。体格用行数与提交数衡量，不必迷信，但值得诚实公开。另外交代一个细节：九个作品没有一个是“为了写书”而存在的，它们全部先于本书诞生，并且在本书写作期间仍在提交——军团最讨厌为了汇报而打仗。

weclaw，微信 AI-Agent 桥，军团的指挥部。Go 写的，约 3.8 万行、118 个 Go 文件、613 次提交，go.mod 里直接依赖只有约 5 个，一个 go:embed 把约 35 万字节的管理后台直接缝进二进制，零 CGO 交叉编译六平台，供应链攻击面小到能画在一张餐巾纸上。它用一个 Agent 接口统一了 ACP、CLI、HTTP 三种世界，让微信这个国民入口变成驱动多个 AI Agent 的操作界面。文档里给它写过一句更狂的定性：以个人微信为入口、以多种 AI Agent 为后端、以文件系统为共享记忆的多智能体协作操作系统。它为“人在哪里，指挥部就在哪里”而造。它的灵感注明来自 tencent-weixin/openclaw-weixin，仅供个人学习——这句话要郑重写在这里，军团不做无来源的扩编。

knowly，剪贴板知识管道，军团的档案馆。约 1.3 万行 Go、37 个文件、496 次提交，自约 2000 行生长而来，如今是 v6.58.0。500 毫秒轮询、CSP 事件流里从 Monitor 到 itemChan 的背压、三层去重、SQLite FTS5 全文索引、SSH 直存私有 NAS——上一节刚介绍过它，这里只补一句：它为“记不住、找不回、留不下”的我而造。

wxwatcher，文件监控推送，军团的哨兵。Python 写的，PyPI 上是 v1.15.0，源码 1188 行，测试 888 行——测试接近源码体量，这是我给小工具立的规矩。两阶段扫描先 stat 快筛、只对疑似变更的文件计算 SHA256，大文件仅哈希前 8KB；SIGTERM 优雅退出并保存状态；状态文件用 tmp 加 os.replace 原子写；推送分批防刷屏；配置有四层优先级；从 1.7.0 起删除默认推送 token，强制走环境变量。它没有追求系统级的文件通知，而是在轮询模式下把性能做到实用——不选最强的方案，选能一次跑通所有平台的方案。它为“人不在终端前”的时刻而造，哨兵的价值恰恰在主人睡觉的时候。

wsstunnel，受限网络穿透终端，军团的工程兵。Python，PyPI 上 v1.0.2，核心 2705 行加测试 1975 行，168 次提交。它用反向 PTY Shell 加 HTTP CONNECT 代理穿透极端受限的网络，在被焊死的沙箱里走私出一个真终端，四层保活对抗容器回收，一个中继经多后端路由管一片机器。它的 README 里躺着一篇《生产升级翻车记》，我把那当作军团唯一正式的战损报告，逢人便推荐。它为绝境而造——每一扇门都焊死的时候，工程兵负责在墙上开一扇新的。

yuangs CLI，仓库名 npm_yuangs，治理型终端，军团的宪兵。TypeScript，254 个 src 文件、44657 行，48 篇文档，甚至有一份 TLA+ 形式化规约。六层治理运行时，TokenPolicy 四级预算门，5 因子风险打分且信任度按天衰减，写入类工具一律强制 human-review。文档里的 non-goals.md 用八条主动排除定义边界，其中一句我反复引用：不可见即不存在。它可能比自治 Agent 慢，但它被设计为更难被误用、更容易被理解、在最坏情况下更安全——它为“AI 时代谁说了算”而造。

poeapi_go，LLM 网关，军团的兵工厂兼安检门。Go，54 个文件、6629 行，从 OpenAI 兼容代理起步，长成了 LLM Gateway 与 Agent Runtime。23 条脱敏规则加 5 条高危直接 400，code_exec 只放行白名单里 7 条命令且 5 秒超时，最高纪录是一次删除 16 个死包、净减 2299 行；连 Agent 能力都用构建标签隔离成可选装配——重武器平时入库，战争来了才发。它为“弹药要足、走火要难”而造。

sourcepack，代码快照工具，军团的翻译官。npm 包 sourcepack，v1.7.1，整个程序是单文件 Go，1413 行，104 次提交。一键把代码库转成 AI 可读的 Markdown 快照；Go 编译出五平台二进制，npm postinstall 按平台下载；还发明了专属的 .gdignore。README 尾签只有三个词：Simple, Fast, and AI-Friendly. 它是军团里最“无我”的一个，没有长篇缘起，工具本身就是全部表达——一千多行代码定义了一个新文体：给机器看的文档。它为“怎么把一个项目讲给 AI 听”而造。

podcast，全自动播客工厂，军团的广播站，栏目名叫广山之巅。约 76 个源文件、1.4 万行，Bash 编排、Python 音频引擎、Go 守护进程 podcastd 三班倒；自 2026-02-04 第一条提交“自动生成播客”起，至今 2382 次提交，累计产出超 1000 期，归档 979 份、编号排到 1096。每天四档：8:30 早餐、12:00 午间、17:00 盘后、20:30 晚间，节假日从不缺席。BGM 按 arousal、valence 与话题打标，通知三级降级，目录即数据库，文件名编号就是主键。它为我的耳朵与生命节律而造。

zhihu_extension，苑广山知乎助手，军团的情报站。Chrome 扩展，纯前端零构建，14 个文件约 8700 行手写 JS/HTML/CSS，连续服役一年半仍在迭代。采集、分发、收藏夹监控、Worker 队列多端协同；轮询带 ±25% 抖动，连续失败自动暂停 2 小时防风控；存储有界——文章 200、已见 ID 500、日志 20 条；连 DOM 到 Markdown 的解析器都是手写的。它的每一次迭代都源于我每天真实的使用动线，是工具长成器官的过程。它为“读过的情报不该读完就忘”而造。

阅兵完毕。九个作品，三种主力语言外加一个浏览器运行时，合计十几万行代码。坦白说，如果你去逐行数，会得出“这人把一个小团队的季度指标一个人干完了”的结论；别数了，数了也不加薪。军衔栏里写着同一个名字：苑广山，朋友们叫我广山哥。但数字不重要，分工才重要：指挥部、档案馆、哨兵、工程兵、宪兵、兵工厂、翻译官、广播站、情报站——互为犄角，共享基建。如果非要用一句话概括这支军团，我会说：它们是一个人为了把生活里所有的“靠不住”变成“靠得住”而铸造的九件兵器。而兵器共用同一套辎重，正是下一节。

## 1.3 共享基建：api.yuangs.cc、want.biz、knasync、NAS

把九个作品叫作军团，不是修辞上的抬举，而是有一个硬标准：军团必须有统一的辎重系统。如果每个项目各自为政，各自造轮子、各自找服务器、各自对接推送，那不叫军团，叫九支游击队。事实是，它们共享同一套私有基建——api.yuangs.cc、want.biz、knasync 和 NAS。这一节查后勤。

api.yuangs.cc 是个人 API 中枢，军团的总机。weixinpush 微信推送就住在这里：wxwatcher 发现目录变了，推送从这里出；podcast 一期节目做完，通知从这里发；weclaw 给我回文本消息，第一通道也是它。一次写好，处处调用。推送这种东西，写第一遍需要一个晚上，接第二个项目只要一行配置——这笔账怎么算都划算。总机的价值在半夜最能体现：任何一台机器上的任何脚本，一行调用就能把消息送到我手机上，不用注册，不用审批，不用开会员。

want.biz 是面向公网的服务域，军团的对外窗口。pic.want.biz 是 R2 图床，upload.want.biz 是 knowly 的上传入口，fm.want.biz 是行情站，q.want.biz 供期货行情，poe.want.biz 负责给播客写稿。播客成片经 rclone 同步到 R2，再由 RSS 出口对外；zhihu_extension 采集的文章，一部分推到自建博客，一部分落回 NAS。一个域名之下，军团所有对外的门脸都挂着牌，客人只需要记一个姓氏。

出门这条路也讲究冗余。podcast 的同步脚本开头就自报家门：R2 同步、iCloud 同步、GitHub 推送、企业微信模板卡片。一次生产，多路出货，加上 NAS 归档又是一条路。这不是炫技，是内容工厂的保险条款：任何一条路堵死，其余几路照常营业。有人问 NAS 会不会坏——会，所以还有 R2 和远端仓库；有人问 R2 会不会丢——也会，所以还有 NAS 和本地。单点必亡，冗余永生，这是军团后勤的第一条军规。

knasync 是跨机消息队列，军团的驿道。手机的灵感、扩展领走的任务、远程剪贴板的暂存，都从这条队列里过。zhihu_extension 配套的 Worker 在云端开了两条队列，手机端提交，家里电脑消费，自动去重——手机是遥控器，家里电脑是工人；knowly 的 Relay 让手机上的灵感汇入同一条管道。可以说，军团八成的异步故事都发生在这条驿道上。队列哲学的妙处在于生产者与消费者不必同时在线：领到的任务做不动，就重新入队，让别的消费者有机会处理。我在一个单机程序里写下了协作精神，因为队列从来不问你到底有几个人。

NAS 是私有存储，军团的总仓库。knowly 的价值观层解读里有一条 Keep Nas As Sanctuary——让 NAS 成为知识圣殿，强调私有化与自主可控的存储哲学。knowly 的归档、podcast 的旧档、wxwatcher 盯着的目录、播客守护进程监听的路径，全在这台机器上。R2 是它的公网镜像，不是它的替代品——热数据出门，冷数据进殿，圣殿不外借。

顺便交代这些基建的物理形态：一台放在角落里永远亮着灯的 NAS，一个跑着几个常驻进程的服务，一条云端队列，外加按年续费的域名。没有机房，没有机柜，更没有值班手机——最好的后勤是让人感觉不到后勤的存在，直到某一台机器真的出了状况，你才发现所有退路早就修好了。

为什么坚持自建？市面上每一项能力都有现成的订阅服务：推送有现成产品，图床有现成对象存储，队列有现成消息中间件，网盘更是多如牛毛。订阅费本身不贵，贵的是别的东西：数据在别人手里，条款随时改，免费额度随时缩，服务关停只需一封措辞客气的邮件。军团的每一件兵器都默认一个前提——供应商本身就是不确定性的一部分。拆开说，自建买回三样东西。第一是数据主权：敏感词在本地就被过滤，根本不会进入传输环节，这比先上传再过滤的云方案安全得多。第二是算力主权：智谱渠道失效的那一天，我当天实测切换到小米 MIMO，生产线没有停一档——命脉握在别人手里的时候，你连“降级”都得排队申请。第三是成本摊薄：一套推送服务、一个图床、一条队列、一块盘，被九个作品反复摊销，每一摊都薄得几乎可以忽略。

这套基建也不是哪天坐下来规划出来的，而是被一个个具体需求倒逼出来的：要推送，便有了 weixinpush；要出门，便有了 R2 与 RSS；要让手机和电脑协作，便有了队列与中继。基建永远晚于第一个需求半步，又早于第二个需求一步——这是我唯一遵守的规划纪律。它不出售，市值大约等于零，但可用性按年计，因为我既是老板，又是唯一的客户；没有服务水平协议，只有一句话：我自己每天也要用。这种关系比任何合同都硬。后勤决定了军团能走多远，而九个作品的接口早已互相咬合：weclaw 的 /publish 与 /podcast 直通 knowly，sourcepack 的产物经中继直投管道，一条消息可以走完捕获、加工、发布、归档的全程。补一句后勤的座右铭：军团的字典里，没有活过三个月的“临时方案”——所有临时方案最终都转正了，因为它们兜底兜得太到位，舍不得删。这是第 10 章的话题，这里先按下不表。

## 1.4 五支柱与三方法论：本书的地图

如果说九个作品是军团的组织架构，那五支柱就是军团的宪法。先说清楚“支柱”这个词：它不是价值观墙上的贴纸，而是当两个方案打架时，能拉架的那个东西。我喜欢把哲学写在 README 的开头，不是因为好看，而是因为这些句子在每一次设计分歧的路口真的替我做过决定。哲学若不曾否定过你的某个方案，那就只是标语。本章之后，每一章的开头你都会路过其中一根柱子，像走进军营时路过不同的旗门。

支柱一，人在环上。AI 提供思路，人类掌控执行——这不是妥协，而是对工程理性的尊重，这句话写在 yuangs CLI 的 README 里。落到机制上：一切可能产生副作用的写操作都是提案（Proposal）而非指令（Action），必须通过用户关卡（gate）；不 OOM，不惊喜，始终有人类在环。举个具体场景：AI 想改文件，yuangs 不会替你按回车——它把改动打包成提案，标好风险分数，亮在终端里等你点头；点头之前，世界没有发生任何变化。它的工程味道还在于，信任不是许愿而是打分：5 因子加权评分，信任度按天衰减，今天放行的命令，不代表明天继续放行。主角是 yuangs CLI、poeapi_go 与 weclaw。

支柱二，对抗熵增。让每一次复制，都成为知识复利。Keep Notions Always Saved，对抗遗忘与数字熵增。它的前提是一个观察：剪贴板是最低摩擦的信息入口，每次复制都是对信息价值的无意识投票。对抗熵增不是建一座图书馆，而是把图书馆的入口修到你的手指旁边——索引有 SQLite FTS5，检索毫秒级，半年之后你会感谢每一个被自动留下的片段。主角是 knowly 与 zhihu_extension，一个管捕获，一个管采集。

支柱三，自己开一条路。既然现成的路都走不通，何不自己开一条？这句气话写在 wsstunnel 的 README 缘起里，后来成了军团的开山信条：不依赖第三方服务，自托管，算力主权，数据主权。开路不等于蛮干——wsstunnel 走的恰恰是对方给自己留的那扇门，沿合规协议走私出一个终端；knowly 的开路则更彻底，干脆不走路，数据从 SSH 隧道直达圣殿。主角是 wsstunnel，以及 knowly 的 SSH 直存与 podcast 的本地生产线。

支柱四，小即美。Unix 哲学：小即美，组合优于集成。sourcepack 的尾签写着 Simple, Fast, and AI-Friendly；不引入任何不必要的依赖，不做“可能以后会用到”的功能——go.mod 的直接依赖个数，在我这里是一种美德，甚至是代码评审的第一眼。小即美的度量很朴素：wxwatcher 五个模块一千余行，却带着 888 行测试；sourcepack 单文件单依赖，却把忽略规则引擎做得完整。规模不欠债，克制才完整。主角是 sourcepack 与 wxwatcher。

支柱五，服务生命节律。世界的信息形态，并不天然适合被人类吸收；让已经存在的信息，重新适配人的生命节律——这是 AutoPodcast 宣言里的话。工具应当围着人转，选择权始终在用户手里。8:30 的早餐档不会因为你晚起而推迟，20:30 的晚间档也不会因为你加班而取消；节律的意义在于，世界的噪音终于有了固定播出时间。顺便预告：第 7 章你会看到，这条支柱的尽头不是勤奋，而是自由——工厂越自动，厂长越清闲。主角是 podcast，以及 knowly 的发布引擎。

五根柱子之下，还有三条横向的方法论，贯穿全书每一章。第一条，默认安全：先阻断、再脱敏、再落盘；AI 只看到脱敏后的内容；敏感词在本地过滤而非上传后过滤；密钥永不进前端。第二条，层层兜底：降级、outbox 离线暂存、指数退避、±25% 随机抖动防风控、优雅退出保存状态、通知三级降级、断网回退上次成功数据——失败是常态设计，不是异常处理。第三条，写即持久化、管道化：一次输入三重输出，一次采集多渠道分发，每一步产物落盘，可审计、可回放。三条方法论之所以横着放，是因为它们不属于任何一个项目，而属于每一次写代码的动作：凡落盘，先问脱敏了吗；凡联网，先问断了怎么办；凡产出，先问能不能回放。支柱是名词，方法论是动词——名词靠动词活着。

层层兜底最动人的地方在于它的密集程度。knowly 的 AI 加工失败，降级为原始内容照常归档；wxwatcher 收到终止信号，先保存状态再谢幕；zhihu_extension 的行情面板断网，回退上次成功数据，屏幕上永远有东西可看；podcast 的通知一级一级往下掉，掉到桌面通知也要把话说完。这些细节单看都小得可笑，拼在一起却是一套完整的生存策略——单人军团没有值班表，韧性必须长在代码里。

这张地图同时也是阅读地图。第 2 章到第 7 章，每章一根支柱、一到两个主角项目；第 8 章到第 10 章，把三条方法论横向铺开，分别讲安全、韧性与协同；第 11 章把一切收束为方法论。如果你是同行，建议按第 2、8、9 章的顺序先读制度与韧性，再回头看各个项目；如果你是好奇的旁观者，从第 4 章与第 7 章读起最有趣——一个是半夜翻墙的故事，一个是每天替你播音的工厂。本章立旗，后面十章都从这面旗帜下走出。如果读完本章你只记住一句话，我希望是总纲的前半句——用技术的确定性。至于后半句“对抗世界的不确定性”，不用背，你迟早会用自己的经历把它补全。

## 1.5 为什么一个人能干一支队伍的活

最常见的疑问是这个：这些真的出自一个人之手？十几万行代码，上千期播客，PyPI 与 npm 上同时在售的好几个包。这个问题我通常回一句：是，也不全是——代码是我敲的，思路有一半是 AI 出的，而纪律，是九个作品反过来训练我的。展开说，是三个杠杆，外加一条别人不太注意的暗线，没有一个跟意志力有关。

第一个杠杆，AI 时代的个体杠杆。我恰好赶上了“思路”变得廉价的年代：模型负责出主意、写初稿、查资料，我负责架构、拍板与签字。关键是姿势——AI 提供思路，人类掌控执行。军团里有大量 AI 的血统，weclaw 甚至天然就是一支 AI 队伍的指挥系统；但每一枚印章都还在我手里。我不写所有代码，我签发所有通行证。AI 时代的个体不是孤胆英雄，而是编制表上唯一的常任军官，底下站着任期无限、忠诚度存疑、但极其勤奋的顾问团。有人担心 AI 让人变懒，我的体验恰好相反：AI 把“想到”与“做到”之间的体力活抽走了，剩下的全是判断，而判断是最累人的部分。杠杆放大的是责任，不是清闲。

第二个杠杆，哲学的复用。五支柱与三方法论是现成的脚手架，新项目的决策成本趋近于零：要不要加依赖，问小即美；要不要上云，问主权；失败了怎么办，问兜底。最硬的证据是同构复现：npm_yuangs 的 redact.ts 与 poeapi_go 的 internal/sanitizer，在两种语言、两个仓库里独立演化出了同构的脱敏实现。制度先于代码，哲学自己会复制自己——当一个思想能跨语言长出同样的器官，它就不再是灵感，而是规律。哲学复用还有一层红利：老项目维护也不慌，九个作品共用同一套审美，读任何一处的代码，都像回自己家；军团加上一份配置，就是一个人的灾备方案。

第三个杠杆，诚实的复盘与问题驱动的最小演进。knowly 经历了七轮问题驱动演进，每轮只做最小改动；wsstunnel 写过《生产升级翻车记》，用探针实锤 12 处问题、466 行修复；poeapi_go 一次删掉 16 个死包、净减 2299 行。顺便自曝：那 466 行不是荣耀，是欠账还清的收据。军团的名声不是靠无事故建立的，而是靠每次事故之后，文档比事故先到。不靠热血维持，靠的是“每次只改一点点，错了就认，认了就写进文档”——这比任何宏大计划都快。

暗线是完成度。一个单人的军团，凭什么值得陌生人信任？凭公共品的完成度。PyPI 与 npm 的发布走 CI 自动出包，wxwatcher 带着 90 多个测试用例，yuangs CLI 有 48 篇文档与一份 TLA+ 形式化规约，sourcepack 连工具链的怪 bug 都写进提交信息留给后人。一个人的项目最容易被质疑“玩票”，所以我把每件兵器都按“会有人接手”的标准打造——哪怕那个接手的人，大概率只是半年后忘光了细节的我自己。完成度不是给别人看的礼仪，是给未来的自己留的绳梯。它还有一个副产品：书里的每一个数字都可以被复核。行数、提交数、版本号，全部来自公开的仓库与发布记录——一本讲确定性的书，至少自己得先经得起检索。

必须澄清：这不是一个工作狂的故事，恰恰相反。军团存在的意义，是让我离开键盘。播客每天四档自动开张，饭点我在吃饭；目录有异动，手机替我看门；剪贴板自动沉淀，我甚至不知道它工作过。顺带一提，军团也管我的健康：播客是用来听的，不是用来读的，散步、做饭、通勤，都是它的播出时段——信息追着人的节律跑，而不是人追着信息跑。服务生命节律，先得服务自己的；如果一套自动化把你钉在椅子上，那它不叫军团，叫苦役。

所以，请允许我在全书唯一一段宣言式排比里把话说满：用技术的确定性，对抗世界的不确定性——所以每一次复制都被记下，而不是被丢弃；所以每一次落盘之前先脱敏，而不是先上传再道歉；所以每一次失败都有下一条路，而不是一场事故；所以每一扇焊死的门旁边，都有一条自己开出来的路。

最后是读法与收束。这本书可以顺序读，也可以按柱子跳读；每章末尾都会回扣哲学，就像士兵行军时回头望旗。军团点兵完毕，下一章从第一根柱子讲起——人在环上，一个关于“谁说了算”的故事。至于我，朋友叫我广山哥，一个在深夜的剪贴板里捡到过整个哲学体系的人。有人问，一个人干活孤独吗？我的回答是：军团编制里没有“一个人”这个岗位——九个互为犄角的战友，一支随叫随到的顾问团，一位每天四档准时播音的广播站。真正的孤独是独自面对不确定性，而我干的全部事情，就是把这种孤独外包给代码。至于这支军团到底能打几个——它不打谁，它只负责让我在不确定的世界里，睡得着觉。

---

## 第2章 人在环上：yuangs CLI 与 poeapi_go 的权力设计

# 第 2 章 人在环上：yuangs CLI 与 poeapi_go 的权力设计

## 2.1 黑盒年代的自救：为什么终端需要治理

深夜的终端窗口里，光标停在回车键上方。屏幕上是一份 AI 刚刚生成的漂亮方案：思路清晰、步骤完整，结尾附着一条命令，像热心的同事把咖啡放到你手边。我的手指悬在半空，突然意识到一件事：我并不真的明白这条命令为什么是对的。它看起来对，但它可能对得太彻底——比如顺手清掉三个星期没有提交的工作区。命令失败并不可怕，报错重来就是；可怕的是它成功得沉默而彻底。后来我把这种时刻总结成一个判断：AI 时代最危险的从来不是命令失败，而是命令成功；不是 AI 答错，而是 AI 答对到让你不需要理解的程度。

把终端交给 AI 的恐惧，根源不在 AI 笨，而在终端太干净。终端是我职业生命里最后一块确定性领地：命令进来，文本出去，exit code 说一不二，stderr 从不撒谎，信号该杀就杀。三十年积累下来的秩序朴素而坚硬——你的每一个回车，都是一次亲手按下的授权。而 AI 是概率的生物，它擅长给出看起来对的答案，不擅长为答案负责。把一个概率系统直接焊在确定性系统的点火开关上，中间不设任何人类裁决，这不叫进化，这叫裸奔。更麻烦的是，这种恐惧无法证伪：你不跑那条命令，就永远不知道它会不会出事；你跑了，出事就晚了——安全问题的罚金从来只收一次。

那两年，我看着各种终端 AI 工具朝同一个方向狂奔：数据悄悄上传，上下文被隐式截断，执行逻辑不可审计；Agent 自动连跑八个步骤，失败了自己重试，成功了宣布任务完成，全程不需要你看一眼。发布会管这个叫丝滑，我管这个叫失明。你失去的不是几次确认的耐心，而是对系统的知情权。所谓动辄得咎的年代，被咎的从来不是 AI，是用户的文件系统、密钥和睡眠。

有朋友调侃，说广山哥这是给自己的手指盖监狱。我说不对：监狱关的是危险的人，宪章约束的是危险的权力。“当不可控的 AI 进入极端强调可控性的终端，秩序该如何重建？”这是我在 yuangs CLI 的 README 开篇写下的自问，而我的答案不是把 AI 做得更强——更强只会让事故更精彩。秩序只能来自制度：把权力拆开，把边界写死，把每一次副作用变成需要人类点头的提案（Proposal），把每一道需要点头的关口做成明确的门（gate）。

这套制度的第一条是权力分离。Agent 只被授予推理权——生成计划、推导命令、关联上下文、分析执行结果；执行权只存在于运行时与用户两侧，用户拥有不可绕过的最终确认权。任何试图绕过用户门或运行时检查的行为，在协议层面直接判定为违规，没有下不为例。第二条是中介执行：所有来自 Agent 的执行请求，必须被视为提案而非指令；运行时和用户拥有完整、即时的拒绝权，而且拒绝必须被记录、被反馈给 Agent 去修正它的计划——拒绝不能白拒绝，得让它学乖。

这两条落到纸面上，就是两句话。一句是题记：“不 OOM，不惊喜，始终有人类在环。”另一句在维护者一节：“AI 提供思路，人类掌控执行。这不是妥协，而是对工程理性的尊重。”我从不觉得要求人类确认是对 AI 能力的侮辱，正如我从不觉得刹车是对发动机的侮辱。概率系统负责想，确定性系统负责做，人类负责在两者之间掌舵——各司其职，各自可问责，这是分工最体面的样子。

制度要立得住，还得回答一个更俗的问题：凭什么都过门，用户不会烦吗？我在 README 里专门用一节作答：开发者主权，而不是方便至上。很多终端 AI 工具追求省事，代价却是不透明——数据悄悄上传、上下文被隐式截断、执行逻辑不可审计。yuangs 的反制是三件套：Swiss-Cheese 采样预览，让你在发送之前看到每一块奶酪；TokenPolicy，先估算、再确认；Human-in-the-loop，切模型、发请求、跑执行，永远需要你点头。你的终端，你的数据，你的决定——这才是极客眼中的真自由。自由从来不是零次确认，而是每一次确认都有充分的信息。为了这个自由，我愿意为一次请求多按一次键；也是为了同一个自由，我把系统定位收窄成一个上下文治理器：哪些文件进入 AI 上下文、预算是多少、何时采样、何时执行，全部由你显式决定，文件系统与 AI 逻辑通过语法而不是点击连接——语法即力量，这是 Unix 哲学在这个年代的还魂。我还在 README 里许愿过一句话：一个确定性执行与概率智能共存而不互相摩擦的终端。这句愿望的全部难度，都压在摩擦二字上；而治理，就是那个减磨层。这种克制也不是姿态。黑盒执行器只需要用户提供手指，而我想造的是一个增强型命令行外脑：献给所有长期工作在终端里、拒绝黑盒魔法的开发者。它不是浏览器插件，不是 GUI 助手，也不是披着 CLI 外衣的聊天机器人，它是为终端而生的 AI 治理运行时。

这一章讲的就是这套制度如何落地。两件武器：一个跑在终端里的 yuangs CLI，管住 AI 的手；一个跑在服务端的 poeapi_go，管住 AI 的眼睛和嘴。前者示范门怎么设，后者示范筛子怎么编。它们分属两种语言、两个场景，却共享同一部宪法：AI 提供思路，人类掌控执行。本章的其余四节，就是这部宪法的逐条解剖。

## 2.2 yuangs CLI：六层治理运行时解剖

yuangs CLI 不是玩具。npm_yuangs 仓库里躺着 254 个 src 文件、44657 行 TypeScript，75 个测试文件约 8100 行测试，外加 48 篇文档和一份 TLA+ 形式化规约——对，我给一套 CLI 的代码变更治理写过形式化规约，这件事的动机与代价，我留到第十一章自我检讨。去掉叙事，它其实是三件事的合体：终端 AI 问答器、Git 自动化工厂、Agent 治理运行时。真正支撑前两个身份不翻车的，是第三个身份。它的骨架是六层。

第一层是命令入口，cli.ts 加 commands 目录共 4437 行，负责把人类意图翻译成系统行为。第二层是运行时，四套并存的 Agent 引擎——坦白说这是一次未完成迁移的中间态，旧的自研 ReAct 栈还在默认路径上服役，新引擎在新入口和降级链上站岗，架构文档里我原话承认这是迁移快照，不是架构选择。第三层是治理风控，约 1000 行，全章真正的主角：危险模式拦截、风险打分、人工确认都在这里。第四层是工具与上下文，18 个工具加 token 预算，约 2600 行。第五层是核心服务，13000 行，模型路由、能力管线、Git 自动化都住在这里。第六层是基础设施，约 3500 行，LLM 客户端、asciinema 审计、SSH。治理不是其中一层，而是贯穿六层的哨兵，站在每一道关口上。

门的设计从工具调用开始。AI 提出的每一次有副作用的操作，都不会直接落到文件系统或进程上，而是被运行时截下来，变成一张提案单：做什么、动哪个路径、破坏性多大、要花多少 token、凭什么判断它是安全的。批准权在用户手里，拒绝也要留下记录。设想一个走场：你让 AI 清理构建目录，它提出一条查找加删除的组合命令，27 条正则当场命中，提案被降级为人工确认，你在彩色 diff 里看见它准备删什么，然后把它改成先移动到回收目录——整个过程 AI 没有被禁言，只是没有被放行。这套机制的高下之分就在这里：治理不是不让 AI 说话，而是让每一次放行都有人签名。这张提案单接下来的命运，由一套五因子风险打分决定。riskScoring.ts，539 行，把每次提案拆成五个因子加权求和：基础操作性质占 0.25，破坏性占 0.30，路径敏感度占 0.20，上下文占 0.15，复杂度占 0.10。破坏性权重最高，删东西永远比读东西可疑。加权得出 0 到 100 的原始分后分级放行：低于 35 视为低风险直接过；低于 60 给出提示；低于 80 需要人工确认；80 以上归入 critical；90 以上一票否决，连讨论都省了。

打分引擎还带一套信任度学习：执行成功加 0.05，被用户拒绝减 0.02，信任度最高能抵 15 分风险。听起来很人性？我又给它加了一条更人性的规则：信任度按天衰减 5%。信任是上一次良好行为的余温，不是永久免检金牌。你今天让我全程放行，不代表明天凌晨我该拿昨天的信用替你担保——第二天回来，在我眼里你又是一个值得重新核验的陌生人。这套机制防的不是黑客，是惯性：人最容易在不设防的日子里出事。

但源码里有一行注释，比整个打分引擎都硬：强制规则，写入类工具始终需要至少 human-review，不论风险分数如何。哪怕打分引擎拍着胸脯说这个文件可以自动写入，制度也会把结论按住，降级为人工审查。算法可以打分，宪法不容算法——这就是那行注释存在的意义，也是五因子体系真正的地基。

第四道门管钱和内存。TokenPolicy 把估算成本和支付成本拆成两个动作：estimate() 只对文件 stat 一下拿体积，一个字节都不读进内存；resolve() 才真正读内容。先看价签，再买单。然后是四级预算门：预算占用 70% 以内放行；70% 到 80% 警告；80% 到 100% 需要你确认，此时可以换长文本模型、可以头尾采样、也可以放弃；超过 100% 直接阻断。这就是不 OOM 三个字的全部落点——再大的仓库、再长的日志，没有你的确认就不会被吃进内存、更不会发出去。Token 是钱，上下文是内存，两样都不许悄悄扣款。

还有一道门守在 AI 的选择权上：模型也不许随便挑。ModelRouter 用四套声明式策略管理选择——balanced、cost-saving、latency-critical、quality-first，各自配置不同的权重；选型先经 gate() 硬过滤，不达标的模型直接出局，再按权重打分，失败按故障域熔断，最后还留了一点点 ε-greedy 与 UCB1 的探索空间，免得系统活成一眼井。注意这个顺序：门槛在前，打分在后，治理先于优化。优化可以在错题集里挑出最优解，治理负责把错题集挡在门外——顺序反了，一切白搭。

最后一道门是时间轴。每一次执行都有 ExecutionRecord，explain 命令解释系统为什么这么决策，replay 命令原样回放决策链路，asciinema 录像把一切写进 .cast 文件，我还扩展了一种治理帧，把谁在什么时刻被哪条规则拦截直接打进终端时间轴——回放时你能站在当时的光标前，亲眼看见每一次拦截的现场。后来我又做了一个 Beta 版的治理控制台，让高级别风险在浏览器里直接全屏阻断、亮起视觉警报——治理不仅要存在，还要在视觉上无处可藏。审计日志落盘前先过脱敏；SSH 提权走一个四态状态机，从 USER 到 AWAITING_APPROVAL 到 PENDING_PWD 再到 ROOT，你输密码的那几秒，镜头主动转开，一个字节都不记。审计自己也要有边界感。

我还欠读者一次诚实。这套运行时里最响的两个名词——WASM 沙箱和 YAML policy——在很长一段仓库状态里是架构声明，不是运行时保障：沙箱产物没有提交，策略文件根本不存在，规则恒为空。真正拦住危险操作的，是 dangerousPatterns.ts 里 27 条正则组成的唯一权威源——它被策略引擎、SSH、命令语义分类四处复用——再加上受保护路径和人工确认。我把这件事原原本本写进架构文档的风险清单，逐条标注修复状态。治理工具的第一美德是诚实：连自己的治理漏洞都必须可审计。

这份诚实还该再补一刀。复盘架构时我发现，四套并存的运行时里，有一套 Planner 外壳在命中重构、批量这类关键词时，会绕过治理裁决直接执行工具——等于宪章在正门写着人人过检，偏门却对熟客敞开。这个洞后来补上了：每一步执行前先经治理裁决，拒绝即中止，并补了两个回归测试看守。我还注意到一个更普遍的现象：这个仓库里几乎每个补丁的注释，记录的都是故障现场而不是设计权衡——同一个提案被确认两次，逼出了确认去重；模型吐 JSON 协议而人眼要看纯文本，逼出了流式输出里的信封剥离。治理运行时不是在白板上画出来的，是一次次事故现场焊接出来的。

这套制度最重的用武之地是 Git 自动化：plan 到 auto/exec 到 review 再到 commit，全链路每一步都有门。代码生成器落盘时自带备份，单文件写入有一兆字节上限，失败自动快照回滚——最坏情况不是回不来，而是回到改动之前。有人问我，这一层层确认不烦吗？我说烦，但这点烦是复利型保险的保费：按一次回车，换一次完整的审计权，全天下的保险里，没有比这更便宜的。发布环节同理，verify.sh 九步门禁里专门有一道校验：发出去的 npm 包里不许含 src/ 源码。连发布本身，也要过门。

## 2.3 non-goals：用八条“不做什么”定义自己

大多数项目的 README 急着宣布自己是什么，我另写了一篇文档回答自己不是什么。docs/architecture/non-goals.md 开宗明义：这些不是尚未实现的功能，而是经过设计后主动排除的能力，用于保证安全性、可审计性与长期正确性。八条，每一条都是我亲手关上的门。

第一条，不支持自治执行。不自动执行 AI 生成的命令，不在没有用户明确操作的情况下执行任何命令，不连续执行多个步骤而不经确认，不把执行当推理的副作用顺手触发。理由只有一句：执行是系统中风险最高的能力，一旦允许 AI 跨过从推理到执行的边界，用户真实意图被弱化，行为难以审计，副作用不可逆。第二条，不支持自推进 Agent 循环。不从一个计划步骤自动推进到下一个，不在失败后自动重试，不自修复，不自主判断任务完成并继续下一个任务——哪怕是善意的自动循环，也会引入隐藏的控制流。第三条，不进行隐式上下文扩展。不扫描未声明的路径，不读未经授权的文件，不碰环境变量，不自主访问网络；唯一例外是可注入当前工作区的 Git diff，而且这个例外必须显式、只读、范围受限。第四条，不存在 AI 拥有的工具：工具在 yuangs 里只是提议、描述和建议，而不是动作。第五条，replay 不具备执行语义：回放只用于审计、调试和理解历史推理，过去的确认不能当作今天的授权。第六条，不允许隐藏状态跃迁：一切有意义的动作必须可见、可归因、可复现。第七条，不允许 AI 宣告目标完成：AI 可以建议任务可能已经完成，但只有用户能做最终判断。第八条，不追求通用自治：yuangs 被刻意设计成强交互、可中断、保守的系统，它不是后台任务执行器，更不是用户判断的替代品。

支撑这八条的是两句话。第一句：“上下文本身是一种能力。任何能力，都必须被明确授予。”它对应治理协议里的显式上下文原则，我给它起了个更狠的名字——“不可见即不存在”。Agent 不得访问任何未被显式声明的资源；每一次上下文注入都必须可追踪、可撤销、可重放。上下文不是默认可用的环境背景，而是每次交互被动态授予的能力。我在 Shell 宣言里写得更直白：任何不可见的上下文注入，都是工程伦理问题。第二句：“任何可能产生副作用的状态变化，都必须经过一次明确的用户关卡（gate）。”这一句是整份文档的宪法条款，其余七条全是它的判例法。

八条非目标背后，其实立着治理协议的五根柱子：权力分离、显式上下文、中介执行，再加上两条最容易被忽略的——真实反馈闭环与可审计性。真实反馈闭环说：stdout、stderr、exit code 是不可篡改的事实来源，执行失败、错误或异常不得被吞没，更不得被美化成幻觉；展示给用户的内容，必须与系统的真实内部状态严格一致，禁止虚假中间态，禁止流式幻觉——在流里输出一条实际并未发生的确认，性质等同于伪造记录。可审计性说：决策链路可回放，执行有日志，上下文变更可追踪，任何时候都要能回答那个朴素的问题——此时此刻，Agent 究竟看到了什么？文档里那句话我写得很重：“一个不可审计的 Agent，等价于一个不可控的 Agent。”

有人替我算过账：每一次人工确认都是摩擦，一天几十张提案单，回车键都要按出包浆。这笔账我认一半。摩擦是真实的，但它是一次性的、可预期的、按次计费的；事故是偶发的、不可预期的、按整台机器计费的。把权力设计的成本摆在明处，恰恰是诚实的表现——那些宣称零打扰的工具，只是把成本记到了你看不见的账本上：悄悄上传的是数据，隐式截断的是上下文，事后不可追溯的是整个执行历史。我宁愿把账单印在门口，也不愿意在收银台下面藏一张欠条。

写八条不做，不是保守，是取舍声明。文档里专门有一节：我明确放弃高度自动化、魔法式的用户体验、无人值守执行，以换取可预测性、可审计性、语义安全、长期信任。这是一个有意识的工程决策，不是能力不足的遮羞布。文档结尾我写了一段自嘲式的自信：“yuangs 可能比自治 Agent 慢。但它被设计为：更难被误用、更容易被理解、在最坏情况下更安全。”而真正的封印是最后一句——任何需要违反以上非目标的功能，在定义上即为 out of scope。

这句话是我最得意的一笔，因为它把哲学变成了可执行的验收标准。此后任何一次功能评审，都不必争论品味与情怀：把这八条拿出来逐条对照，撞线即否决，连辩论环节都省了。产品通常靠做什么来定义自己，边界感强的产品靠不做什么。前者画出的是野心，后者画出的是骨骼。主动排除的智慧在于：你放弃的每一样东西，都在为你保留的东西加固地基。功能会被模仿，克制很难被抄走。这也和全书第五、第六章遥相呼应——小即美不是审美偏好，而是权力设计的下游结果：能力越少，需要守的门就越少；门越少，每一扇才守得住。顺带回答一个常见追问：这八条会不会随着 AI 变强而过时？我赌不会。模型的能力会膨胀，但推理权与执行权的边界是社会问题，不是算力问题——只要副作用的后果仍由人类承担，门槛就必须由人类把守。

## 2.4 poeapi_go：同一哲学的服务端表达

终端的权力问题安顿好之后，同一道题换了个考场重新出现：服务端。poeapi_go 的缘起朴素得不像它后来的样子——把标准 OpenAI API 请求翻译成 Poe 的协议，让我用上 Poe 上的多种模型。然后它像所有诚实的项目一样自然生长：接上 Gemini、DeepSeek、小米 MIMO、OpenRouter，长成动态多提供商路由的 LLM Gateway，再叠加 Tool-Aware Agent、流式管线、会话记忆和用量配额，最终是 54 个 Go 文件、6629 行。添加一个新供应商是零代码的——改 config.yaml 就行；路由的决策链也朴素：显式映射优先，关键词兜底，最后落到默认模型；Router 本身就是一个模型客户端，不是一坨 if/else 分支——新模型接进来，流式、Agent、工具能力自动继承。

考场变了，题型没变：谁有资格看到什么，谁有资格执行什么。终端里，执行权要攥在用户手里；服务端，密钥和数据的出口要攥在我手里。所以 poeapi_go 的第一道铁律是密钥边界：前端仅负责 UI 与用户交互，不直接接触模型密钥，模型 Key 永远只存在于后端。客户端拿到的只是一个独立的鉴权 Key——它能让服务干活，不能让服务泄密。这与终端侧的思路一模一样：能力最小化，前端能拿到的恰好是它干活所需的最小集合，多给一个字节，都是未来某次事故的伏笔。请求进门的顺序也是权力的顺序：先过身份验证，再过频率限制，再过结构与大小校验，三关之后才有资格见到业务处理器。

第二道闸门是安全三原则，顺序即世界观：先阻断、再脱敏、再落盘；先脱敏、再发模型——AI 只看到脱敏后的内容；双层脱敏兜底。用户消息在发往任何第三方模型之前和落盘之前，都要过两道工序。第一道是脱敏：23 条规则从 11 条一路演进而来，覆盖 sk- 开头的 OpenAI 风格密钥、ghp_ 开头的 GitHub token、AKIA 开头的 AWS 密钥、AIza 开头的 Google 密钥、JWT 三段式、Bearer 头、URL 内嵌密码、password 字段、PEM 私钥整块、三大数据库连接串，以及 export API_KEY 这种环境变量惯犯——命中即替换成 [REDACTED] 字样。第二道是高危阻断：PEM 私钥、OpenSSH 私钥、数据库连接串、AWS secret key、Slack bot token 五类内容一经检测，请求被直接 400 拒绝，不进入日志、不进入模型。替换与拒绝是两种刑罚：普通凭证判终身监禁——替换后放行；高危内容当场枪毙——连接都建立不起来。脱敏的时机也被我从日志记录之后，提前到四个点上：日志记录前、路由前、回答落盘前、Logger 写入前。时机前移一步，泄露的窗口就收窄一圈。

第三道闸门是不信任任何人，包括我自己写的代码。Handler 层脱敏之后，Logger 层在写入前还要独立再脱敏一次——即使上游某处忘了洗数据，落盘的依然是干净的。我不信任调用方，我只信任机制。为了证明机制可信，配了 13 个自动化安全测试，其中一条反例专门验证正常文本不误杀：权力的另一半是克制，不能见风就是雨，把用户的正常文章当密钥烧掉。

Agent 执行面上的克制同样写进了字节。code_exec 工具的白名单只有 7 条命令：echo、date、whoami、hostname、pwd、ls、cat。白名单外一律返回 command '%s' is not allowed，连商量都不商量。而且它不经过 shell——参数切分后直接 exec，管道、重定向、命令拼接、环境变量展开统统不存在，AI 的可执行面被压到最小，它连 cd 都做不到。每条命令强制 5 秒超时，到点即杀。更有意思的是，整个工具连同 Agent 能力被 //go:build agent 构建标签隔离：高级能力是可选装配，不是默认注入，你不用 Agent，这段代码连编译都不参与。防死循环靠 max_steps 兜底，默认 5 步，走完就停，适合生产环境；多 Agent 的 planner、executor、reviewer 三段编排最多 3 轮。流式管线上也有一条纪律：Streaming 仅用于最终回答，避免中间状态污染前端；故障转移只在第一个 token 输出之前允许，一旦开始输出，禁止切换提供商——半句话换服务器，比整句话出错更恶劣。

克制还长在流式管线与会话的细节里。poeapi_go 的流式抽象有五级：StreamClient 只管发请求、收流；ModelStream 把各家协议翻译成统一的增量；Adapter 统一主循环，它是唯一知道什么时候结束的地方；最外层是 Writer，要么 SSE 实时输出，要么收集拼接——所谓非流式请求，只是换了一个 Writer，永远走同一条管线。两条管道并成一条，就少了一半的不一致。会话记忆超限时自动裁剪旧消息、生成摘要回注，永远保留最近 6 条；用量配额先判超额、后记账，quota exceeded 四个字必须挡在花钱之前。全链路挂着 context.Context，超时与取消层层传导，没有悬挂的 goroutine，没有泄漏的文件句柄。这些细碎的纪律单拎出来都不起眼，攒在一起，就是对生产环境的基本礼貌。

制度不漂移，不代表系统不求生。智谱渠道失效的当天，我把渠道切到了小米 MIMO；兜底模型从 GPT-4o-mini 换成 Qwen3.5-Omni-Flash；REASONIX.md 里至今留着 22 个 NVIDIA 免费模型的实测评分表。供应商可以换，模型可以换，用实测数据而非宣传页做选型的原则不能换——这是决策权的另一面：权力放在设计里，不放在情怀里。

这套系统的自留地同样诚实：日志可以通过环境变量整体关闭，完整的 error stack 加请求体被明令禁止写入日志；有一次我对着依赖图，一口气清掉了 16 个从未被接入的包，删掉 2299 行——对体系干净的执念，本质上是扩大无死角的面积。README 里我给自己的评价是一句大实话：“这套系统的最大价值在于：稳、清晰、可长期演进。”相比简单 API 拼接，“这是体系，不是脚本。”终端的 gate 与服务端的 sanitizer，是同一扇门的两种门禁——门里站着的是同一个人：人类。

## 2.5 同构复现：redact.ts 与 sanitizer 的隔空握手

这一节的两位主角隔空相望。一个叫 redact.ts，住在 npm_yuangs 的 src/utils 里，31 行 TypeScript，6 条规则，注释只有一句：用于审计落盘前清洗，防止 TOKEN 与密码明文持久化。另一个叫 internal/sanitizer，住在 poeapi_go 里，23 条脱敏规则外加 5 条高危阻断，配套 13 个安全测试。一个管终端的审计日志，一个管服务端的请求与日志；一个先写成，一个后长壮。它们的作者都是我，但我可以作证：写第二个的时候，我没有回去抄第一个。

把它们并排放开，同构几乎是逐条的。结构上，两者都以规则表为中心：名称、正则模式、替换文本，三件套排成数组，让文本逐条过筛。默认值上，两者都默认开启：redact.ts 用环境变量留了一个泄压阀，只有显式关闭才不脱敏；sanitizer 干脆长在 NewDefaultConfig 里，想关都没有这根线。执法位置上，两者都卡在落盘与发送两个口子上：一个在审计写入之前，一个在日志、路由与回答落盘之前。扩展性上，两者都允许运行时加规则，又都只给半张许可证——TS 侧加了不持久化，Go 侧同样注明 AddRule 重启即失，想要永久规则请改默认配置。连测试审美都一致：不只测拦得住，还要测不冤枉。

细节的呼应更妙。redact.ts 里有一行注释：重置 lastIndex，避免 /g 全局正则状态残留——正则对象带全局标志时 lastIndex 会跨调用残留，这是 JavaScript 独有的坑，我一个人在两种语言里各踩了一遍，然后在两边留下了同款的防御注释。sanitizer 的注意事项里写着：规则顺序有先后，较长、较具体的规则应排在前面——这又是两套实现共同的排序戒律。规模不同、语言不同、性能取舍不同，骨架却严丝合缝：先阻断、再脱敏、再落盘；白名单、默认开、可审计。两份文件像隔着一条河的两座桥墩，河面上没有施工通道，桥却对得整整齐齐。

连开关的位置都值得端详。redact.ts 的泄压阀是一个环境变量，显式设置才关得掉；poeapi_go 那边有个 LOG_ENABLED 开关，但请注意它关掉的是日志，脱敏本身没有开关。两个开关都长在例外一侧：脱敏是默认，关闭才是需要解释的行为。默认值的朝向，就是价值观的朝向——你把哪个状态设为默认，就等于宣布哪种世界是常态。

有人问过我：既然同一套制度写了两遍，为什么不抽一个公共库？我不抽。两种语言、两个部署单元、两条发布节奏，为了省几百行正则，换一个跨项目的强依赖，这笔账在任何一页架构教科书里都是亏的。复利的正确姿势不是复制代码，而是复制制度：代码各有各的方言，制度只有一副骨架。从这个角度看，同构是制度传播的最高形式——不是复制粘贴，而是独立重演。生态学里管这个叫趋同演化：不同谱系的生物被同一片海洋塑造成同一种流线型。塑造这两份代码的，是同一片名叫默认安全的海。

这件事比任何设计文档都更能回答一个质疑：一个人的军团，制度会不会随心情漂移？答案写在 diff 里。漂移的是实现——6 条对 23 条，31 行对几百行；不漂移的是结构——脱敏永远在落盘之前，白名单永远写在代码里，阻断永远优先于替换，审计永远只对脱敏后的世界负责。规则的数量会变，规则的秩序不变。因为制度不是灵感的产物，而是问题的产物：问题相同，答案就会同构。我曾经给 yuangs 的代码变更治理写过 TLA+ 规约，想把制度放进数学里证明；后来发现，最硬的证明不在规约里，而在两个项目无人商量却长出同一副骨架这件事本身——形式化证明制度自洽，工程复现证明制度有效。两种语言独立演化出同一制度，恰恰证明哲学先于代码：代码只是哲学在各自语法里的翻译稿，翻译可以有方言，宪法没有方言。

所以回到本章开头那两句话。把终端交给 AI，我不怕它聪明，我怕它免检；把服务端交给 AI，我不怕它跑得快，我怕它看不见边界。“AI 提供思路，人类掌控执行。这不是妥协，而是对工程理性的尊重。”在本书余下的章节里，你会反复看到这套权力设计换着戏服登场：knowly 把敏感词过滤放在上传之前，wxwatcher 把推送 token 从默认值里删除，zhihu_extension 给存储设上界，weclaw 让 AI 只看到脱敏后的世界。把执行权留给人类，是把后悔权留给人类；把推理权交给 AI，是把试错的成本降到最低；把白名单写死在字节里，是把自由与安全的边界从口号变成可执行文件。在 AI 时代，模型会过时，框架会更替，唯有权力的设计是一切设计的第一设计——在你能回答谁可以做什么之前，所有的智能，都只是尚未命名的风险。

---

## 第3章 对抗熵增：knowly，让每一次复制都成为知识复利

# 第 3 章 对抗熵增：knowly，让每一次复制都成为知识复利

## 3.1 收藏从未停止，学习从未开始

热力学第二定律说，孤立系统的熵只会增加，不会减少。这句话在我生命里最直观的体现，不是房间，不是书桌，而是收藏夹。房间的乱好歹要动手，收藏夹的乱是自动生长的：浏览器书签一年年变厚，稍后读应用里躺着去年标记的文章，微信收藏里塞满当时觉得惊为天人、后来再也没打开过的段落。它们安静地待在那里，像一座不断加盖却无人居住的图书馆。

我曾经是一个勤奋的收藏家。看到一篇好文章，点收藏；刷到一条有用的帖子，点收藏；群里有人贴出一段精妙的代码，长按，收藏。收藏的动作快到近乎本能，两秒钟，完成，心安理得地滑向下一条。我一直以为这是在学习，直到某天我想引用一个月前看过的一篇讲数据库索引的文章，打开收藏夹，从最上面往下翻，翻了整整二十分钟，一无所获。那一刻我意识到一个残酷的事实：收藏从未停止，学习从未开始。这两件事之间隔着的，不是勤奋，是一整条断裂的管道。

后来我把这条管道拆开来检查，发现问题出在范式上。几乎所有的知识管理工具都在默认同一个前提：你会主动打开它们，主动粘贴，主动整理，主动回顾。这个前提要求一个人在每个知识瞬间完成三个动作——想起来、打开、粘贴。每一步都是摩擦，每一步都在打断你正在做的事情。你正在读一篇深入的文章，心流正酣，凭什么要为一段笔记切换五个应用？摩擦不是中性的，摩擦是熵的帮凶。摩擦越大，系统离秩序越远。于是绝大多数知识管理工具的最终形态都是一座我去过一次、然后只去扫墓的坟。

而真正诚实的洞察，藏在一个所有人都视而不见的地方。knowly 的项目分析文档里有一句话，后来被我抄在了所有笔记的扉页：剪贴板是数字生活中最低摩擦的信息入口，每一次复制都是对信息价值的无意识投票。这句话值得逐字读三遍。第一遍，你说的是事实：没有人为了复制一段文字而特意启动什么，Cmd+C 就长在手指上，它比呼吸还自然。第二遍，你说的是机制：复制这个动作不打断心流，不索取注意力，成本趋近于零，所以它测出来的偏好最真实——你没有说我要学这个，但你的手已经诚实地下注了。第三遍，你说的是遗憾：这场投票每天都在进行，每人每天投出成百上千票，而所有的票，开出投票箱的瞬间就被烧掉了。

因为剪贴板是数字世界里熵增速度最快的地方。它只有一格，新的一来，旧的就没了。你复制了一段精彩的论述，还没来得及粘贴，又复制了一个链接，前面那段论述就从这个宇宙里消失了。它甚至算不上消失，因为它从未存在过——系统不留任何痕迹。信息以最低摩擦进来，又以最快速度蒸发，这个进出的落差，就是我所说的数字熵增：信息总量在增加，秩序总量在减少，而你什么也没做错。

这几年第二大脑的概念很流行，方法论一套接一套：卡片盒笔记法、每日笔记、渐进式总结。它们都对，也都对得有限——因为所有方法论都在教人怎么整理，却没有一个敢回答最要命的问题：原始素材从哪里来？答案依旧是人肉搬运。整理的技艺再精湛，入口的摩擦不减，管道的第一公里就永远是断的。我后来不再责怪自己不够自律，就像没有人会责怪自己不会飞。工具的问题要由工具解决，这是工程师的基本操守：当一件事需要意志力才能坚持时，第一反应不该是修炼意志力，而是把它设计掉。意志力是稀缺资源，要省着用在真正值得的地方，比如决定接下来读什么。

想通了这一层，解法几乎是自动浮现的。既然投票已经发生，为什么让结果蒸发？既然 Cmd+C 已经是最低摩擦的动作，为什么不把它直接变成知识管理的入口？我不需要再发明一种要求用户改变习惯的工具——改变习惯注定失败，这条路的墓碑我已经看过太多次了。我需要的是让工具彻底隐身，隐身到人的习惯里面去：你照常复制，我在后台接住。README 里那两句话就是这么来的：捕获时零摩擦，沉淀时零丢失。前半句对用户承诺，后半句对系统要求。

剩下的就是算账。我把知识积累粗略地看成三个因子的乘积：复制的频率，乘以保存的可靠性，乘以时间的长度。传统知识管理死在第二个因子上——你复制得再勤快，保存率趋近于零，乘出来还是零。而只要把保存率拉到接近百分之百，哪怕单次复制轻得微不足道，乘上时间之后也会长出一个惊人的数字。这就是金融里那个古老概念在知识领域的投影：复利。单次存款不值一提，日复一日的存款是奇迹。爱因斯坦据说把复利称作世界第八大奇迹，我不知道他是否真的说过这句话，但我确定一件事：当存款的动作轻到与复制重合，每个人都负担得起这个奇迹。

所以这一章要讲的 knowly，本质上不是剪贴板同步工具，而是一台对抗熵增的机器。它立在最陡峭的熵增坡面上，把每一次即将蒸发的 Cmd+C 截下来，铸成一枚硬币，投进一个不会丢失的账户。收藏从未停止，学习从未开始——错的从来不是人，是范式。范式错了，就该换范式，而不是怪人不够自律。

## 3.2 K.N.O.W.L.Y.：一个名字里的哲学

工程师给孩子起名有两种流派。一种是随手抓一个代号，能用就行，等项目做大了再改名——大多数项目永远停在能用的名字上。另一种是把整个项目的宪法压缩进名字里，名字本身就是需求文档的最高压缩形态。knowly 属于后者。它的官方名字是 Knowledge Async，知识的异步传输。但我在 README 里为 K、N、O、W、L、Y 这六个字母准备了九种解读，不是因为无聊，而是因为每一种解读都对应着这个项目的一个真实侧面，合在一起，就是一份不写一行代码的需求文档。

先说官方正解。Async，异步，这个词在软件工程里的含义是：发起调用的一方不必等待结果，可以继续做自己的事。把这个词迁移到知识领域，它的意思就变成了：产生知识的心流，和消化知识的时间，是两条独立的轨道，不必也不该互相等待。你在读一篇硬核文章，此刻你的任务是理解，不是归档；至于归档，那是系统的事，是后台的事，是异步的事。传统笔记工具把这两件事强行同步——读到这里，停下来，切走，记录——心流就这样被切成了碎片。Knowledge Async 宣布的是一种解耦：你负责流，我负责存。

Knowledge Async 宣布的是一种解耦：你负责流，我负责存。

异步不是修辞，它有具体的工程形态。knowly 装进 macOS 的 LaunchAgent，登录自启，崩溃自动重启；没有常驻窗口，没有托盘图标，没有红点提醒。一个异步系统的自我修养，就是把存在感降到零——它存在得越彻底，你越感觉不到它。

再说哲学层的两个解读，是 knowly 的终极使命。第一个，Keep Notions Always Saved，让一切念头永不丢失。README 里写给它的注解是：对抗遗忘，对抗数字熵增，这是 knowly 的终极使命。注意这句话的措辞——遗忘不是偶发故障，熵增不是环境背景，它们是敌人，是明确的、值得调动一个守护进程常年作战的敌人。Notion 这个词也选得讲究。它不是知识，也不是文档，而是念头。知识的单位不是文档，是念头；念头比文档轻三个数量级，也比文档短命三个数量级。传统笔记工具收集的是成品，knowly 打捞的是半成品，甚至只是念头的碎屑——而这恰恰是复利的原料：碎屑最容易获得，也最容易蒸发，谁接住了碎屑，谁就接住了复利的源头。第二个，Knowledge Never Accidentally Sinks，知识永不意外沉没。每一条碎片都是一票，每一票都要被捞起来，放进你的私人知识方舟。Accidentally 这个词我特意保留在解读里，因为丢失从来不是故意的，恰恰相反，所有的丢失都是意外的——而工程的意义，就是让意外无处下手。

架构层的解读直接暴露了技术本质。Kernel for Networked Archival Sync，网络归档同步的内核——一个轻量、专注的守护进程，应该像操作系统内核一样可靠：开机就在，崩溃重启，不索取注意力。另一个是 Kapture，Normalize，Archive，Syndicate，捕获、标准化、归档、分发，这是 knowly 数据流四部曲的精确描述。一段内容从剪贴板进入，先被捕获，再被规范化——加上 YAML 前置元数据、生成语义化文件名——然后按日期归档，最后流向各个发布渠道。四个动词，四个字母的起笔，一条管线的全部工序。我能用四个单词说清楚的事，绝不画一张三层架构图。

产品层的解读最有趣。Kindle Notes Auto Sync，这里的 Kindle 不指那台设备，作动词用，意为点燃灵感——每一次复制都是一次火花的捕捉。Knowledge Nirvana Automation System，知识涅槃自动化系统。我承认这个有点中二，但它精准地表达了从碎片到复利的那个质变瞬间：量变堆到某个临界点，混乱突然变成了秩序。工程师的生活里很少有神启时刻，这一条算是一个自我提醒——你在攒的东西，会在某个你意想不到的时刻整蜕变现金。

价值观层的解读只有一条，却是全部九条里最有分量的一条：Keep Nas As Sanctuary，让 NAS 成为知识圣殿。强调私有化、自主可控的存储哲学。这一条不是文字游戏，它是架构决策的直接源头。正因为名字里写着 Sanctuary，knowly 才没有走云服务的路线，才选择了 SSH 直传自有 NAS，才坚持敏感词在本地过滤、根本不进入传输环节。名字先于代码做出了裁决：知识是一个人的圣殿，不是一家公司的数据表。后来很多朋友问我为什么不用现成的云笔记，我说答案写在名字里——Keep Nas As Sanctuary，名字都起好了，架构没得选。

还有两条彩蛋。Kitchen Never Asks Silly，厨房从不多问——把 knowly 比作一个沉默的厨房帮工，你扔进去食材，它默默处理成菜肴，从不抱怨，也从不打断。Knowledge Not Another Silo，知识不再是孤岛——打破应用之间的壁垒，让剪贴板成为统一入口。彩蛋往往藏着真心话：这两条一个说的是产品性格，一个说的是产品野心。

九种解读，其实是一个名字的九个投影。每当一轮迭代结束、不知道下一步该往哪里走的时候，我就回头把这九条重读一遍：还离使命远吗？还有沉没的风险吗？四部曲哪一环最弱？圣殿还安全吗？名字里写着方向。写代码之前先把名字写对，这不是仪式感，这是我最省钱的一次架构评审。

## 3.3 无感、安全、韧性：三重设计哲学的工程化

哲学如果不落到代码上，就只是朋友圈文案。knowly 的项目分析文档把设计哲学总结为三条：无感，Frictionless；安全，Secure；韧性，Resilient。这三条我在无数场合讲过，但真正让我安心的，是它们各自对应着一组可以指认的文件和函数。这一节我把三重哲学逐一拆开，看看它们分别长成了什么样子。

先说无感。无感不是没有功能，而是功能不索取注意力。文档里写：最好的工具是透明的工具。它尊重用户的注意力……将自身退居为基础设施而非应用。落到工程上，第一件事就是那个 500 毫秒的轮询。有人问过我，为什么不用系统剪贴板事件回调，非要傻乎乎地每半秒看一眼？答案朴素得让人失望：轮询是全平台最可靠、行为最可预期的方案，而它实测的 CPU 占用不到百分之一，内存占用只有十几到三十兆。用户感知为零，这就是无感的全部门槛。第二件事是入口处的纪律：一段内容要进入管道，先过最小长度门槛，默认一百字符，防止无意义的碎片冲刷归档；再过敏感词过滤。我把过滤逻辑写成一个纯函数 ShouldFilter，不依赖任何状态，剪贴板和 Relay 中继两条入口共用同一份判断——同一套价值观，不允许两套实现。第三件事藏在类型系统里：Payload 是一个密封接口，接口里藏着一个私有的 isPayload 方法，只有包内的类型能够满足它，外部无法伪造一种新的载荷混进管道。一个小小的 Go 惯用法，守住的是数据流的纯度。无感还有一层温柔：复制一个链接，knowly 会异步去抓取网页标题，给这条记录补一个人类可读的名字——但这个增强动作有两秒超时，绝不阻塞轮询主循环。增强是锦上添花，捕获才是底线，锦上添花永远不许挡底部的路。

然后是整条管道的并发骨架，knowly 最核心的一段设计，一个教科书级的 CSP 事件流。先看主数据流的拓扑：

```text
Monitor(500ms 轮询) → itemChan(容量 10) → 主循环 select → goroutine per payload → SSH 同步
```

生产者是剪贴板监视器，每半秒检查一次变化；消费者是同步逻辑，要经历去重、过滤、增强、AI 加工、SSH 传输等一长串可能很慢的步骤。两者速度天差地别，靠一个带缓冲的 channel 解耦。itemChan 的容量是十，这个数字就是背压保护的全部秘密：剪贴板轮询永远不会因为 SSH 慢而卡住。通道满了怎么办？丢弃，并且大声告警，绝不静默。这个取舍听上去粗暴，实际上是一个关于确定性的清醒判断——轮询是系统的心跳，心跳不能等任何慢操作；为了保护心跳，牺牲最坏情况下的长尾数据，并且把牺牲明明白白喊出来。通过了 channel 的每一个载荷，会被交给一个独立的 goroutine 去走完全程，谁慢谁卡住，卡死的只有自己。而 SSH 这头也不能被挤爆，信号量限制最多三个并发会话——这是一台家用 NAS，不是数据中心。

去重是这条管道的口腔卫生。想象一下没有去重的世界：你复制一个链接，粘贴，过会儿又复制，又粘贴，半天的功夫归档里堆满同一篇文章的几十份拷贝——熵增没有减少，反而被工具加速了。knowly 的对策是三层去重：第一层是内存里的哈希表，进程活着就记得刚处理过什么；第二层是落盘的 status.json 状态文件，守护进程重启之后接着认账，不会把旧账重记一遍；第三层是远端的 .knowly_hashes 哈希索引，跨设备、跨重装地保证同一个内容在 NAS 上只有一份。三层各管一段时空，合起来是一个完整的承诺：同一段内容，无论复制多少次、重启多少回，归档里只落一次。熵增的对立面不是删除，是不重复。

再说安全。knowly 的安全底线只有一条：敏感信息根本不应该进入传输环节。密码、token 这类内容在本地就被过滤掉，过滤发生在复制发生后的半秒之内，比先上传再过滤的云服务方案安全得多——因为根本不存在那个先上传。传输层用的是 SSH 协议直传自己的 NAS，首次连接遵循 TOFU 原则记录 known_hosts，之后每一次连接都校验主机指纹，中间人无处插足。这里多说一句数据主权。knowly 的 SSH 方案有个容易被忽略的美德：服务器端什么都不用装。golang.org/x/crypto/ssh 这个库让 Mac 这头直接对 NAS 说话，远端只要有一个 SSH 服务和一个目录——没有任何 knowly 专属的服务端进程要部署、要升级、要打补丁。数据落在自己的机器上，备份策略自己定，服务商涨价、停止运营、改隐私条款，这些别人家的不确定性，一律与我无关。到了远端，所有涉及远程命令的操作都经过 POSIX 单引号转义，防止路径里的特殊字符变成一次注入攻击；远程家目录不是硬编码的，而是启动时动态解析并缓存——多一台机器上路径长什么样，我说了不算，问了才算数。

最后说韧性，这是三重哲学里最像保险柜的一层。网络会抖，SSH 会断，这不是异常，这是日常。knowly 的应对是一整套路数：重试用指数退避加上 Full Jitter——AWS 推荐的算法，每次重试的等待时间指数增长，再乘上一个随机抖动，避免无数请求整齐划一地撞向同一个时刻。实现这个算法的 retry 包只有四十二行，加上九十六行测试，是我整个项目里我最满意的小模块。连接管理层，探活用的是轻量的 keepalive 请求而不是创建完整会话；确保连接的函数用读写锁分级，绝大多数时候只拿读锁快速检查，只有真需要重连时才升级写锁；直连失败还有拨号回退路线，绕过某些网络扩展的干扰。而整条韧性的压舱石，是 outbox：当 SSH 彻底不可达时，内容连同 AI 元数据一起落盘为 JSONL 文件，网络恢复后自动排空，启动时排一次，之后每五分钟补一次排空。捕获时零丢失这句承诺，就是靠这个小小的离线队列兑现的——字面意义上的兑现。

韧性还有最后一环：AI 加工失败，不回滚，不阻塞，直接降级为原始内容同步。AI 是调味料，不是闸门。模型限流也好、超时也好、配额耗尽也好，最坏的结果只是这条内容少了几枚标签和一段摘要，而内容本身一定会落进圣殿。概率性的智能出了故障，确定性的管线照常运转——这句话我在别的项目里也说过，但最早想明白它，就是在这个环节。

无感是体验，安全是底线，韧性是命运预案。三重哲学没有一条停在文档里：无感对应着轮询、纯函数与密封接口，安全对应着本地过滤与 SSH 主机校验，韧性对应着退避、探活与 outbox。哲学的工程化，说到底就是把每一句漂亮话都逼问出一个对应的函数名。

## 3.4 一次输入，三重输出：发布引擎

只有入口没有出口的知识管理，是一只貔貅——只进不出，攒得再多也只是仓库里的库存，不是账面上的资产。复利要成立，存款必须能被取出、被使用、被看见。所以 knowly 在捕获和归档之后，还长出了一个完整的发布引擎，而它背后的方法论只有一句话：写即持久化，管道化，一次输入，三重输出。

先把出口的清单摊开。knowly 的发布引擎目前支持 Blog、Podcast、IMA 知识库、Kindle 四个渠道，外加 Webhook 兜底，理论上任何能收 HTTP 请求的服务都能接入。Kindle 那一路走的是 SMTP over TLS，把归档内容转成适合墨水屏阅读的格式，直接推送到你的设备——躺着的知识变成通勤路上的阅读材料。整个发布引擎是配置驱动的插件式结构：每个渠道一个独立的发布函数，互相不知道彼此的存在，新增一个渠道就是新增一个函数加一段配置，不碰主流程。所有发布动作通过 PublishIfNeeded 异步触发，发布是归档的副产品，不是一场需要仪式感的大事——主流程绝不等待任何一个远端服务的脸色。

发布引擎能这样长出来，前提是归档层足够开放。knowly 的每一条归档都是带 YAML 前置元数据的 Markdown 文件，按年月日目录组织，兼容 Hugo 和 Jekyll 的目录约定——你甚至可以直接把归档目录当成一个静态博客的内容源。想找点什么？可以用 grep 搜索，可以用 rsync 备份。这是我对开放性的执念：知识不应该锁死在任何一个软件里，哪怕那个软件是我自己写的。文件系统是最低通用公分母，十年后 knowly 可能没了，但 Markdown 还在，这就是持久化最彻底的形态。写即持久化的另一面是每一步产物都落盘、可审计、可回放：捕获有记录，AI 加工有元数据，发布有状态，任何一条内容走到哪一步，随时可以查证。单人项目没有值班工程师，可审计就是我的值班表。

于是整条链路合拢了：你复制一段内容，半秒内它被捕获、过滤、去重，AI 可选地为它打上三到五枚标签、写出五十字摘要、给出零到十的质量评分；它以语义化文件名落进日期目录，随后按配置流向博客、播客、知识库和 Kindle。一次输入，三重输出——复制这个动作第一次拥有了复利意义上的杠杆。

输入侧还有一条支流。灵感不总发生在电脑前：通勤路上、被窝里、排队时，手机才是念头的第一现场。Relay 中继为此而设——手机上的灵感经过公网队列汇入同一条管道，knowly 以拉取模式定期取件，用游标记录消费进度，防止重复消费。剪贴板管住桌面，Relay 管住移动，两条入口，一个圣殿。

为这条管道配的瞭望塔，是内嵌的 Web 管理台。REST API 加 SSE 实时日志流，归档浏览、历史编辑、统计图表、配置管理都在一个页面里；上传一个 Markdown 文件会自动触发同步流程——管理台自己也是管道的一个入口。深色主题，手机上也能看。单人软件的运维部门、客服部门、数据部门，就浓缩在这一个页面里。

这条管道不是设计出来的，是长出来的。REASONIX 评测记载的那个数字让我自己都吃了一惊：v6.58 的时候，knowly 的 Go 代码只有约两千行。而今天，它是三十七个文件、约一万三千行 Go 代码、四百九十六次提交的守护进程。从两千行到一万三千行，中间没有过一次推倒重来，只有七轮问题驱动的演进：基础验证、三层去重、连接韧性、历史回溯、安全过滤、路径修复、离线 resilience 与 AI 增强。每一轮都遵循最小改动原则——只解决当前问题，不做过度设计。这七轮的顺序本身就是一部需求史：先证明管线能通，再防止重复写入，再对抗网络抖动，再找回被覆盖的内容，再补上安全的口子，再修路径的细节，最后才是离线兜底和 AI 增强。顺序不能乱，因为每一个问题都是上一个问题跑通之后才暴露出来的。

举个具体的例子。历史回溯那一轮，源于一个每个人都遇到过的场景：复制了一段新内容，把剪贴板里那段旧的长文本覆盖了，回头想找，没有了。README 的 v1.7.0 更新说明里原样记录着这个用例——手滑覆盖，以及想确认截图是否已经成功同步到 NAS。于是有了 knowly history 查看最近的同步记录，knowly restore 一键把指定记录回填到剪贴板。被覆盖不再是丢失，剪贴板的一格，在 knowly 这里变成了无限格。再比如离线那一轮，源于对网络长期不可达时数据丢失的担忧，于是有了 outbox。你看，七轮里没有一轮的起点是我想做什么，全部的起点都是什么东西坏了、什么东西会丢、什么东西不够安全。

当然，也有我不打算改的地方，这一节必须诚实。Web 管理台的前端是一个五千八百多行的 index.html，通过 go:embed 直接编进二进制，连 manifest 和 service worker 也一并内嵌——评测报告委婉地指出这难以维护，建议拆分。我拒绝了，理由和当初内嵌的理由一样：无需前端构建，一个二进制文件就是完整的管理台，拷过去就能跑。可维护性是成本，部署简单也是成本，我选择了后者，并且愿意一直支付这个选择的价格。类似的还有配置热更新——Web 界面可以保存配置，但部分参数要重启守护进程才生效；AI 包没有纳入重试体系，模型限流时直接失败然后降级。它们都躺在已知的不足清单里，不是被遗忘，是被排序了。删功能比加功能难，拒绝重构比重构难，这两件事我练得越来越多。

从两千行到一万三千行，我唯一确定的是：这一万三千行里，没有一行来自可能以后会用到。每一行背后都站着一个真实发生过的问题。问题驱动，最小改动——这是 knowly 教给我的节奏，也是一个人的军团能够常年自持的代谢速度。

## 3.5 用技术的确定性，对抗世界的不确定性

复利要让人相信，账本必须可以随时翻开。知道层的最后一块拼图，是检索。归档在远端 NAS 上，按日期铺开，但使用发生在本地、发生在灵光一现的瞬间——我想起半年前复制过一段话，我需要它在两秒之内出现。为此 knowly 在 Mac 本地基于 SQLite FTS5 建了一套全文索引，采用 trigram 分词，为远端的群晖归档提供毫秒级的检索。这个决策背后有一个真实的性能故事：最早查一条历史详情要走一次 SSH 往返，接近三秒，慢到让人放弃使用；后来本地建了索引，同样的查询变成毫秒级返回。三秒和三毫秒的区别不是快了一千倍，而是可用和不可用的区别——超过人类耐心的检索等于没有检索，这是交互设计的铁律，不是性能优化的锦上添花。

检索之外，还有一整套开放的后门。knowly-mcp 提供一个 stdio 的 MCP 查询入口，让 AI 助手可以直接问归档库要答案；cluster 包对历史条目做离线的 AI 聚类，让碎片自己长出结构；底层永远是那些 Markdown 文件，grep 可搜，rsync 可备份，Hugo 和 Jekyll 可直接接管。知识的所有权不是一句口号，它意味着知识可以以任何你想用的方式被取用——包括用你还没有发明出来的方式。

那么，复利累积之后，到底是什么样子？写这一章的晚上，我现场翻了一次自己的账本。归档目录里，每天一个文件夹，文件名是同步时间加内容语义，比如那个关于量化交易的思——文件名截断在思字上，忠实得近乎笨拙，但正是这种笨拙让我一眼认出它。输入一个关键词，毫秒级，那段半年前刷到后顺手复制的内容连着标签、摘要、评分和原文一起回来。写作这一章用到的几乎每一条事实——七轮演进的顺序、三层去重的名字、outbox 的排空间隔——都是从这座我自己攒了半年的仓库里查出来的。书在写仓库，仓库在喂养书，那一刻我确信复利不是比喻。

用物理学的语言重述一遍：每一次复制，是逆着熵流的一次微小抬升。单次抬升毫无意义，会被熵瞬间抹平；但当抬升以每天几百次的频率发生、以接近百分之百的可靠性被保存、以年为尺度持续累积，秩序就开始在自己的坡面上站稳脚跟。碎片之间开始互相引用，标签开始聚拢成簇，随手的复制慢慢显影为一个念头完整的来龙去脉。熵增是宇宙的默认方向，负熵是生命的本事，而工具，是本事的延伸。

knowly 的项目分析文档有一段结语，写的时候我并没有想到它会走多远：这，或许就是工程师能送给普通人的最好礼物——用技术的确定性，对抗世界的不确定性。后来，这句话成了这本书的总纲。第 1 章军团点兵，九个作品在它的旗下集合；第 2 章的权力设计，是它面对 AI 概率性时的制度回答；而本章，是它面对遗忘时的数据回答。再往后你会看到，wsstunnel 在门被焊死时用它的确定性开路，podcast 用它的确定性驯服信息洪流，wxwatcher 和 zhihu_extension 用它的确定性守住每一次采集。九个作品，是同一句话的九次实现。knowly 是其中最诚实的一次，因为它对抗的敌人——遗忘与熵增——是所有人共享的敌人，而它选择的武器——一次复制——是所有人共享的本能。

军团里也早有了呼应：weclaw 那边一个 /publish 指令可以直通 knowly，微信里一句话，内容就进了圣殿；往后你还会看到更多这样的走廊。一个人的软件军团最迷人的地方就在这里——你造的每一件兵器，最后都会成为其他兵器的弹药库。

顺便交代两句长跑者的体能细节：日志超过十兆自动轮转并归档到 NAS，不让守护进程被自己的排泄物噎死；LaunchAgent 的配置文件一旦丢失，knowly start 会检测到缺失并自愈重装；进程的多实例问题由 PID 文件上的排他锁拦住，两个 knowly 同时跑这种荒唐事不会发生。复利的前提是账户永续，而账户永续的前提，正是这些琐碎到不配写进宣传语的细节。

如果你要问广山哥图什么，这就是答案。这个世界的不确定性清单很长：网络会断，AI 会失手，进程会崩溃，剪贴板会覆盖，记住的东西会遗忘。而我的确定性清单，一条一条对得上：网络断了，outbox 会替你记住；AI 失手了，原始内容会照常落进圣殿；进程崩溃了，LaunchAgent 会把它拉起来；新内容覆盖了旧内容，历史会替你留底；哪天连知识库本身要迁移，Markdown 和 SQLite 还在文件系统里等着。不确定性是世界的事，确定性是我的代码的事——工程师的浪漫，从来不是建造永不塌方的东西，而是让每一种塌方都有预案。

所以，让每一次复制，都成为知识复利。这句话是 knowly 的 README 开篇第一句，是本章的标题，也是我把一个剪贴板同步脚本养到一万三千行的全部理由。熵增不可逆转，但账户可以复利。明天你还会复制上千次——从这一次开始，每一票都算数。

---

## 第4章 自己开一条路：wsstunnel 与被焊死的门

# 第 4 章 自己开一条路：wsstunnel 与被焊死的门

写这本书的时候，我给九个作品各分了一章，唯独预感这一章会是故事性最强的一章。它有一个标准的绝境开局：所有现成的路都被焊死；有一个半夜的顿悟：既然走不通，就自己开一条；有一次当场翻车：亲手把生产环境打挂了十分钟；还有一次诚实的复盘：把翻车过程一字不落地写进开发日记。wsstunnel 是军团里个子最小的兵之一，核心四个模块不到三千行，却是五根支柱里最不显眼也最硬气的那根——自己开一条路——最纯粹的践行者。这一章，我从那个折腾到半夜的晚上讲起。

## 4.1 折腾到半夜：现成的路全走不通

五根支柱没有一根是想出来的，全都是被现实逼出来的。第一根支柱诞生于对 AI 失控的警惕，第二根诞生于对遗忘的恐惧，而第三根——自己开一条路——它的诞生地不是什么宏大项目，是一个容器。README 的“缘起”一节，我写下的第一句话是：“那段时间，我被沙箱环境折磨得够呛——容器说回收就回收，远程 SSH 总是不通。”写下这句话时手指是放松的，因为那段煎熬终于熬过去了。可要交代它为什么煎熬，就得先把那堵墙一砖一瓦描述给你看。

那是一个 AI 执行沙箱：用户提交代码，系统在隔离容器里运行，再把结果返回。为了安全，这个容器被精简到近乎苛刻。操作系统是 Alpine，预装的东西一只手数得过来——bash、python3、pip、curl，连 ssh、nc、telnet、socat 这样的基本工具都没有。网络层面更是铁壁：iptables 规则只放行 80 和 443 出站，且所有流量必须经过 http://127.0.0.1:18080 这个代理；入站端口全部切断，原始 TCP 出站封锁，连 ping 都被无情地挡在门外。没有 root 权限，装不了系统软件，改不了网络配置。更妙的是，容器每三十分钟强制回收一次——你搭好的环境，半小时后就凭空消失。而我当时要做的，是在这样的环境里调试一个复杂的多进程程序。每次出错，只能看有限的日志输出，像隔着一层毛玻璃看别人修车。

前二十四小时，我把常规手段挨个试了一遍。写一个循环脚本用 curl 轮询远端指令——延迟高得像隔洋喊话，根本谈不上交互。用 python3 自带的 http.server 起一个服务等人来连——入站被阻断，起了也是自己连自己。上 ngrok——它依赖第三方服务和 TCP 出站，两条全撞死。上 frp——同样需要 TCP 直连，而且配置文件复杂到令人头秃。

我不死心，继续换弹匣。cloudflared 快速隧道，Cloudflare 的 API 域名被阻断，握手都没开始就结束了。bore 公共中继，连 GitHub 的 CDN 都访问不到，中继更别想。serveo 的 SSH 反向隧道，TCP 出站被全封。最讽刺的是反向 SSH 到自己的 VPS——那台机器是我在腾讯云上的，理论上伸手就够得着，可从沙箱里连 ping 都出不去，ICMP 被掐得干干净净。wireguard 我也试了，且不说配置这套密钥和接口有多繁琐，它走的是 UDP，而 UDP 在出站白名单面前连门牌号都摸不到。README 里那段话就是这一晚的总结：“我把能想到的工具全试了一遍：cloudflared、frp、wireguard……要么配置复杂到令人头秃，要么在‘只允许 HTTP 代理出站’的铁壁前直接哑火。”

这类环境其实有一张通用的限制清单，后来我把它们整理成表：无入站端口，防止外部攻击者直连容器；出站白名单，只放行 HTTP 和 HTTPS，有时精确到只放行特定域名，防止数据外泄和恶意软件；强制 HTTP 代理，所有出站流量必须经过统一审计；无 root 权限，防止容器逃逸；极简基础镜像，减少攻击面。每一条限制单独看都合理，合在一起，对一个只想调试程序的工程师来说，就是一座无法逾越的孤岛。名单里有一个方案值得单独记一笔：chisel，它是唯一摸到过门把手的——支持 HTTP 代理。但它设计的目标是转发端口，不是交互式 Shell，PTY 要额外配置，多后端路由和文件传输更是没有。顺着它走，我最多得到一根管子，还是一根要手工驯服的管子。那七十二小时里，前二十四小时贡献了上面这些失败尝试，剩下的时间，我在做一件后来证明更重要的事：读环境本身。

后半夜，我停止了挨个试错，开始做一件更冷的事：给这堆失败做尸检。结果发现它们的死因是同一个。这些工具——frp、ngrok、chisel、ssh -R——几乎都建立在端口映射模式上，它们隐含两条公理：第一，网络上允许你发起一条原始 TCP 连接；第二，目标机器上存在、或者可以被你制造出一个监听服务，然后把那个端口暴露出去。而受限环境恰恰是这两条公理的坟墓：没有 root，装不了 sshd；没有入站端口，暴露无从谈起；没有原始 TCP，连出站这一步都迈不出去。也就是说，问题根本不在于我挑的工具不够好，而在于它们全体共享的那条假设，在这里不成立。换再多工具，只是用不同的姿势撞同一堵墙。

深度剖析文档里，我给那个晚上留了一句诊断：“每一扇门都焊死了，每一扇窗都贴着‘此路不通’。”这不是文学夸张，是逐条验证过的事实清单。但凌晨的那种安静里，有一句话开始反复冒出来——不是绝望的话，是一句工程判断。它把我从“再换一个工具试试”的死循环里拽了出来，直接把我推到了另一条路面前。

## 4.2 何不自己开一条：反向 PTY 的顿悟

转机藏在一个最不起眼的地方。我盯着终端，鬼使神差地敲了一句 echo $http_proxy——屏幕上回显了 http://127.0.0.1:18080。这一行输出像黑屋子里的手电筒。原来，他们自己的 AI 也需要访问外网：要拉代码、要调 API、要装依赖，平台不得不给它留了一个 HTTP 代理出口。用文档里的话说：“这是他们给自己留的门，而我决定沿着这门走进去。”封锁者永远需要给自己留一条缝，这是所有铁壁的共同软肋——铁壁再高，也要给守军留个送水口。

于是便有了那个后来被反复引用的瞬间。“折腾到半夜，我突然想明白了：既然现成的路都走不通，何不自己开一条？”请注意这句话的成分，它不是豪言，是冷静的推理：既然所有现成工具共享同一条失败公理，那么绕开的唯一办法，就是放弃那条公理，自己另立一条。自己开一条路，重点从来不在“开路”的浪漫，而在“自己”二字——不指望别人的地图，因为别人的地图上根本没有这片地形。

方向定了，接下来是路线设计。市面上工具的第二条隐含假设是“目标机上要有监听服务”，这一条也一并放弃：不依赖任何监听服务，由容器主动发起唯一一条出站连接，把输入输出反向推回来。这就是后来登记在军团概念词典里的“反向 PTY Shell”。实现的核心是标准库里那个冷门已久的模块：pty。用 pty.openpty() 创建一对伪终端，拿到主从两个文件描述符，再让 bash 以交互模式启动，把它的三个标准流全部接到从端上，代码只有五行。

```python
master_fd, slave_fd = pty.openpty()
shell_proc = subprocess.Popen(
    [shell, "-i"],
    stdin=slave_fd, stdout=slave_fd, stderr=slave_fd,
    preexec_fn=os.setsid,
)
```

这几行代码的妙处在于“骗”。在 bash 看来，面前坐着一个拿着物理键盘的人：它认为自己连着一台真终端，于是 vim、top、htop 这些全屏程序原生可用，作业控制、光标定位、色彩输出一样不少。这里有一个当时想得很清楚的分界：如果图省事，用 subprocess 开两条管道也能把输入输出搬出去，但那是“能跑命令”，不是“真终端”——管道给不了作业控制，给不了 ANSI 转义的全家桶，更给不了窗口尺寸。全屏程序要的不是字节流，是一台终端。要交付真的，就骗过内核，而不是糊弄用户。

写到这里，有个问题必须正面回答：为什么不干脆想办法给目标机装一个 sshd？答案就藏在受限环境的定义里：没有 root，装不了；就算有 root，也未必该装——你不知道这套环境的审计与防逃逸机制会把什么动作当成入侵。反向 PTY Shell 对目标环境的全部要求，被压到了不能再低：一个 bash，一个 python3，一条出站连接。前两样是容器本来就有的，第三样是平台自己也要用的。也就是说，这条路完全走在环境给的约束之内，不撬开任何一层防护——它不是越狱，只是借道；借的道，还是修路的人自己留的。

连接的另一头，是三方中继架构：你自己的电脑或者浏览器作为前端，负责发送命令、看到输出；一台 VPS 上的中继服务 Relay 作为枢纽，负责鉴权、路由和转发；受限容器里的后端 Client，经 HTTP 代理穿透，主动注册上来。三方各司其职，而所有连接——注意，是所有连接——都由客户端这一侧主动发起。入站？根本不存在入站。NAT 和防火墙的入站限制，被“换一个方向发起”这个动作整个绕开了。整个东西用起来的样子，简单到可以原样放进 README 的“三十秒极简演示”一节。

```bash
# 终端 1(VPS)
wsstunnel relay --port 8080

# 终端 2(容器内)
wsstunnel client --server ws://your-vps:8080

# 终端 3(你的电脑)
websocat ws://your-vps:8080
```

三行命令，不依赖第三方服务，不需要在目标机上做任何网络配置，一个完整的交互式 Shell 就到手了。回头看，那晚真正让我兴奋的不是“穿透成功了”，而是发现了一个更普适的结构：封锁几乎总是单向的——入站被焊死，出站却要给守军留缝。既然“等人来连”这条路被堵死，那就把服务端和客户端的名字对调，让被封锁的一侧去主动连接自由的一侧。方向一转，攻守易势。很多“此路不通”，不通的不是路，而是你在路上站的方向。当然，方向对了，路本身还没通：那条出站连接，依然要从“只认 HTTP”的代理眼皮底下走过去。怎么走，靠的就是下一节要说的手艺——协议走私。

有人觉得一条命令的演示是运气好，我更愿意把它看成约束的胜利。这个项目内部要做对的事情其实不少：鉴权、路由、心跳、重连、编码，样样都不能少，但它们全被折叠进了默认值——最常见的场景下，几乎不需要任何配置。把复杂性收进默认值，把命令行留给最短路径，是一个给凌晨三点还在救火的人用的工具该有的样子：他不需要读文档，他需要的是少按几次键盘。

## 4.3 协议走私的艺术：HTTP CONNECT 里跑 WebSocket

先看那道门本身是怎么工作的。HTTP 代理有一个标准能力，叫 CONNECT 方法，RFC 7231 里写得明明白白：它用于建立到目标服务器的隧道。流程只有三步——客户端向代理发送一条 CONNECT 请求，指明目标地址；代理建立起到目标的 TCP 连接，回一句“连接已建立”；此后，客户端与代理之间的这条连接就变成了一条盲眼隧道，代理不再解释里面的内容，你在里面发什么它转什么，TLS 握手也好，别的什么协议也好，它一概不问。我在文档里把这件事的推论写得很直白：“只要你能通过 HTTP 代理发送 CONNECT 请求，你就能在代理背后建立任何 TCP 连接。”那道他们给自己留的门，标准里早就写好了用法。

这道门有个学名叫协议走私。定义同样写在文档里：“用标准协议的合规行为，绕过安全策略的检查。”防火墙以为在看一次普通的网页访问，代理以为自己在恪尽职守地建立一条 HTTPS 隧道，而隧道里实际跑着的，是一个完整的交互式 Shell。每一方都在按规矩办事，合起来就是一条新的路。工程上的落地轻得出奇：websocket-client 这个库原生支持 HTTP 代理，连接时把代理地址和端口作为参数传进去，CONNECT 的整个流程由库替你完成。这里有个选型上的品味问题值得说一句：自己开路，不等于连车轮都要重造。协议栈这种千锤百炼的标准件，用现成的；方向、结构、协议设计这些真正构成差异的部分，自己定。wsstunnel 的客户端是同步的 websocket-client，服务端是异步的 websockets，各取所长，一个参数解决穿透，没有一行是自己去手写 HTTP 解析的。

路通了，真正的敌人立刻现身：时间。沙箱每三十分钟强制回收一次容器，代理会有闲超时，网络会抖，shell 进程会崩。一条隧道如果只会建立、不会活着，在这类环境里活不过一集。所以 wsstunnel 的保活设计成了四层，每一层对应一种具体的死法。第一层，禁掉 websockets 库内置的 ping 机制——两套心跳并存会互相打架，协议层先让路。第二层，应用层心跳：客户端每三十秒发一个 __PING__，中继回一个 __PONG__，让每一个中间设备都看得见这条连接还活着。第三层，断线重连用指数退避：五秒起步，每次翻倍，三百秒封顶——既不在故障期徒劳轰炸，也绝不放弃任何一次重新站起来的机会。第四层，Shell 崩溃自动重生，最多五次——进程没了就再拉一个，终端体验尽量无缝。容器被回收是常态而不是事故，所以客户端重启之后会自己找回来，不需要任何人工介入。这四层在本书第九章还会再遇到，因为它们正是“层层兜底”方法论的一次现场示范：韧性不是一层坚固的墙，而是多层不同姿态的网。

但即便如此，也只算交付了一条“通道”。wsstunnel 真正的差异化，在 README 里写成了六个字：交付“真终端”而非“通道”。中继服务内置了一个基于 xterm.js 的 Web 终端，约千行的单页应用，由中继托管在同一个端口上——浏览器打开就是一个能用的终端，连 SSH 客户端都不用配。窗口尺寸是同步的：前端一拉伸，一条 __RESIZE 消息带着行列数发过来，客户端用 fcntl 对那对伪终端执行一次窗口设置，内核里 bash 认知中的终端宽高随之改变，长行折行、全屏程序的布局立刻正确。信号是转发的：__SIGNAL 消息把中断、挂起这些信号原样递进去——Ctrl+C 是信号，不是字符，管道思维在这里会漏掉整个交互世界里最常用的一下。多后端是路由的：LIST 看集群里有哪些容器在线，USE 把默认目标切到某一个，@name 前缀把一条命令临时点名发给指定后端而不影响当前选择；多后端时输出自动打上来源标签，单后端时保持原样，一个中继管一片容器集群。文件传输也在协议里：base64 加六十四 KB 分块，外加一个体贴的小机关——在交互式 Shell 里敲下载命令时，客户端会劫持按键、追踪你 cd 到了哪里，像克隆了这个 Shell 的内心世界，你要的文件自己就递出来了。

协议里还有两处容易被忽略的体贴。一处是路由与格式的分工：多后端时输出要打上来源标签，可文件传输的消息若也被贴标签，协议格式就会被破坏，于是文件消息走单独的转发路径，保持原样。另一处是终端输出用二进制帧透传，ANSI 转义序列原封不动地到达浏览器，颜色、光标、清屏，一个都不丢。这些细节单拎出来都不大，合起来就是“能用”和“像真的”之间的距离。上线之后，文档里存了两条用户反馈，一条是“在 AI 沙箱里终于能跑 vim 了，感动”，另一条是“用它调试 CI 环境，省了 80% 的提交-等待循环”。第一条说的是体验的完整性，第二条说的是经济价值：每一次省下的往返，都是实打实的人的时间。

把这些拼起来，wsstunnel 的定位就清楚了，README 里写的是：它不是通用管道，而是专为沙箱调试、CI 环境、IoT 边缘设备设计的轻量级反向管理平台。走私的是协议，交付的是终端——门是他们留的，路是我自己开的，而开路的目的从来不是炫技，是让 vim 在沙箱里能全屏跑起来，让调试的人重新获得一个工程师最基本的工作环境。

## 4.4 生产升级翻车记：一次“全绿测试”的教训

故事到这里该收尾了，可 wsstunnel 最诚实的一页，恰恰写在这之后。二〇二六年八月的最后一天，起点轻描淡写：一句“看看哪里还有优化空间”。我先跑了测试：一百三十一个通过，两个失败——失败的两个是伪终端窗口设置的用例，报的是设备耗尽，换台机器就绿，不算数。到此为止，一切都在“轻松收工”的剧本里。然后我开始逐文件通读，第一遍读完直觉不对劲的地方不少；第二遍我决定不猜——这种路由、时序、并发纠缠在一起的项目，光靠肉眼读代码，很容易把“看起来有问题”当成“确认有问题”。我写了个探针脚本：在本机起一个真实的中继进程，接一个假后端、两个前端，把每一跳的收发原样打印出来，哪条消息该到哪儿、到没到、顺序对不对，一目了然。这个决定被证明是那天最有价值的一步。

探针跑出来的结果，比读代码时的怀疑难看得多。数下来十二处，分四类：安全三项、稳定性三项、可用性一项、性能五项。第一刀砍在安全上：命令黑名单这个功能，帮助文案里写得清清楚楚，我全文检索拦截函数的调用点——零。构造了对象，赋给了状态，从来没执行过。运维配了这个参数，以为删除命令被拦了，实际照跑不误。这种“看起来有防护、实际裸奔”的状态，比没有这个功能更危险。第二刀砍在角色模型上：文件角色的三个协议分支做完角色检查后没有返回，一路落到函数末尾的普通命令检查上——认证能过，事情办不成，整个文件角色是个空架子，而管理员能传文件，纯粹是靠这个意外的“贯穿”碰巧达成的。代码意图和行为完全拧着，谁哪天顺手把这个贯穿“清理”掉，功能就无声无息地坏了。第三刀是并发：广播函数在遍历集合时逐个 await 发送，挂起期间集合被其他连接改动，探针里直接复现了“集合在迭代中被修改”的运行时崩溃——这种问题平时一个不出，负载一上来一出就是生产事故。后面还有一串：断线重连时旧连接注销会按名字把新连接挤掉；token 文件里写个带时区的过期时间，校验直接抛类型错误，这个 token 从此谁也登不上；上传时中继的提示消息排在协议确认之前，客户端拿到第一行就误判被拒；带了指定后端参数更惨，排空循环靠一百二十秒的超时才退出，每次传输先干等两分钟；Web 终端三处直接拼接后端名渲染，而后端名来自任意客户端的启动参数，标准的存储型 XSS。性能清单同样不好看：广播串行发送让一个慢前端卡住所有人；下载把整个文件攒在内存里，五百 MB 的文件要吃掉近七百 MB 内存；微信推送在注册路径上串行等待，端点一慢就把上线广播拖住十秒；每个分块刷一条审计日志，一次大文件传输就是几千行噪声；防爆破计数器只增不减，公网机器被扫几个月就是慢性泄漏。

用户回得干脆：全修改，按你的计划来。动手比预想顺，傍晚时分，四个提交、四个文件、四百六十六行净改动，测试从一百三十一个涨到一百七十六个，全绿。探针逻辑全部固化成回归测试，顺手还撞出两个计划外问题一并修掉。

修法里有两处我至今得意。一处是广播的收敛：先对连接集合取快照，再用并发调度同时发送，发送失败的连接收集起来最后统一剔除——一个辅助函数，同时消灭了迭代崩溃和队头阻塞两种病，四个广播函数全部收敛到它上面，之后谁再写广播，只有一条正确的路可走。另一处是 token 过期时间的时区陷阱，解法只有一行：拿过期时间自带的时区去取当前时间——它带时区，比较的双方就都带时区；它不带，双方就都不带，一行代码同时兼容两种写法。安全那一侧，后端名加了字符集校验，非法名字自动降级成自动命名而不是拒绝连接；服务端堵源头，前端加转义，双保险。

版本号升到 v1.0.1，打标签，推远程，CI 很快出了包。到这里为止，那天是一个近乎完美的“优化日”。真正的考验在后面：用户的 relay 在腾讯云一台机器上，由 systemd 管理，而服务器上装的居然还是 0.17.2，比仓库落后了好几代。我逐项核对了服务参数与新版的兼容性，结论是直接升级无风险，于是升级、重启，一气呵成——版本号对，服务 active，端口在听。我按习惯多看了一眼日志——这一眼救了整个晚上。日志里躺着一条刺眼的堆栈，原样抄在下面。

```
AttributeError: 'Headers' object has no attribute 'headers'
  File ".../websockets/legacy/server.py", line 363, in process_request
  File ".../wsstunnel/relay.py", line 106, in _http_request_handler
```

最扎眼的是那个路径：legacy。上服务器一查，依赖库 websockets 的版本是 12.0，而我本地是 16.0。这才是全部真相：这个库有新旧两代实现，新一代直到 13.0 才成为顶层接口的默认，12.0 及以前走的都是旧实现；两代实现里，处理 HTTP 请求的回调入参和返回值完全不同，而我托管 Web 终端页面的那段处理逻辑只适配了新版的形状。于是服务器上每一个进来的连接——包括 WebSocket 升级——都在同一行上炸掉，整个中继完全不可用。更让我脸上发烫的是，我猛然想起代码里原本有一段“检测 websockets 版本”的适配代码，注释写得清清楚楚，而就在十几个小时前，我在一次“代码卫生”提交里把它当死代码删了，提交信息里还写着“删除未使用的死代码”。它不是死代码，它是一个没写完的备忘：这里有坑，还没填。

先承认，慌了两分钟。然后深呼吸，列清单。第一个问题：本地为什么全绿？因为本地环境里顶层接口就是新实现，压根走不到旧分支，所有用例都没经过那条路径——“全绿”的前提是测试环境等于生产环境，而依赖版本就是环境的一部分。第二个问题：这算谁的锅？项目声明兼容 10 到 13 之间的版本，服务器装 12.0 完全合法，锅只能自己背。修复思路也定了：不猜版本号，用鸭子类型——新实现传进来的请求对象有 path 属性，旧实现没有，用这个特征区分两种调用形状。第一次提交推上去，还是 500，但报错换了：返回值的形状也得兼容。这回我不猜了，直接上服务器读库的源码，旧实现的处理函数里写得明明白白，它期望一个状态码、头和体的三元组。第二次提交，顺手记住“当前是不是旧版”，是就返回三元组，否则返回响应对象。两处修复各配了双形状的回归测试，八个参数化用例把新旧两种调用方式都钉死在测试里。再上服务器，强制重装，重启，curl——页面回来了。再跑真实链路探针：用户那个真实容器的名字出现在后端列表里，运行时长二十几秒——它的重连退避机制完全按设计工作，我这边修复一落地，它就在某一轮重试里自己进来了。那一刻比修好任何 bug 都让人松一口气。从第一次重启到确认恢复，生产中断约十分钟，复盘日志里的九条错误全部来自旧进程。我如实交代了中断窗口、原因和恢复状态，补版本号，打 v1.0.2，发布。

这份开发日记的结尾，我写了六条教训，这里只说最重的三条。依赖版本是环境的一部分：“本地测试全绿”只证明“在我这套依赖组合下没问题”，声明兼容谁，就要在谁身上跑关键路径。升级验证必须打真实请求：“服务 active、端口在听”只说明进程活着，不代表服务可用，如果升级后习惯性先访问一次页面、连一次真实链路，这场事故可以压缩到一分钟以内。删除代码之前先读它周围的注释：作者写它的理由，往往比“没用”两个字重得多。还有一条关于韧性：客户端的重连退避，让那次中断的自愈时间只取决于服务端修复的速度，不取决于人工介入的速度——平时不起眼的机制，事故里就是可用性本身。反过来它也提醒我，服务端升级应该挑低峰期，先在非生产机演练；那晚的升级没有演练环境，侥幸成分很大，这一点没什么可辩解的。而那条贯穿全天的方法论，是探针——十二处问题没有一处是“看代码觉得有问题”，全部是实锤之后才动手；探针写完也没有扔，它们全部转正成了回归测试，从此这个项目的路由、时序、并发行为都有了一个最低保障。日记的最后一句，我原样抄在这里，算是对那一天的注脚：“工程就是这样：写代码的时间永远比不上理解代码运行环境的时间，而真正的功课，往往从‘全绿’之后才开始。”

## 4.5 不必要的依赖一个都不加

wsstunnel 的设计哲学文档里，有一节标题朴素到近乎无趣，叫“极简主义”。它开头的两句话，是我给这个项目定的宪法：“不引入任何不必要的依赖，不实现‘可能以后会用到’的功能。”落到依赖清单上，这个项目的家底一眼可以数完：服务端一个 websockets，客户端一个 websocket-client，微信推送通知加一个异步 HTTP 客户端——就这三个。伪终端是标准库的 pty，窗口设置是标准库的 fcntl 和 termios，异步是标准库的 asyncio，线程协调是标准库的 threading。没有框架，没有数据库，没有配置中心，中继的全部状态就是内存里的几个集合，部署就是一条 pip install。一个能在受限环境里翻墙送终端的工具，自己的体重轻得可以在任何环境里被装进去——这本身就是产品逻辑的一部分：你要救的人被关在精简容器里，你自己就不能是个连安装都要三思的庞然大物。

而“小”不是与生俱来的风格，是持续决策的结果。设计哲学文档成文于六月，那时核心代码约一千九百行，测试一百零三个；到八月三十一日发布 v1.0.2，核心四个模块两千七百零五行，测试一百八十四个，三个月一百六十八次提交。它长了几百行，但每一步都有明确驱动力：出现过存储型 XSS，才有了后端名的字符集校验和前端转义的双保险；出现过并发崩溃，才有了先取快照再并发发送的统一广播函数；出现过版本形状差异，才有了八个把两代调用方式都钉死的参数化用例。文档里那句原话，是我对“重构”这个被滥用词的全部态度：“从单文件脚本到标准包，从闭包到类，从无测试到一百多个测试——每一步都有明确驱动力，不为了‘优雅’而重构。”重构在我的词典里不是审美活动，是问题驱动的副作用；没有问题，就不动刀。

完成度还有一组安静的数字：核心代码两千七百零五行，测试一千九百七十五行——测试密度达到核心体量的七成以上，对一个单人项目，这个比例意味着每次改动都有人替你盯着。再加上 CI 出包、英文 README、贡献指南、八篇中文深度文档，它不是一个脚本开源了，是按公共品的标准交付的：哪怕全世界暂时只有我一个人在用，也要按会有人接手的样子来写。军团对所有兵员的共同要求，源头都在这里。

克制之外，这个项目对用户还有一层少见的诚实。从第一个版本到后来，向后兼容是白纸黑字的承诺：token 可选，不给也能跑；命名可选，不指定就自动分配；标签可选，单后端时不加前缀；模式可选，旧客户端不发模式字段就默认按老方式处理。README 的三十秒演示旁边，我原样留着警告：“无认证，仅限内网测试。生产环境请加上 token 参数和 TLS。”README 里还专设了“已知限制”一节，主动交代时序开销、无压缩、无审计这些短处——不粉饰。安全模块倒是做得不含糊：多角色 token、IP 白名单、防爆破、审计日志，token 比较用的是防止时序侧信道的常量时间比较，成本几乎为零的加固，顺手就做了。给用户看短处，和给用户做加固，从来不矛盾——都是把对方当成要长期相处的人，而不是要一次性说服的听众。

开源，是这个故事的最后一站。如今它在 PyPI 上，MIT 许可，配了 CI、英文 README、贡献指南，docs 目录下躺着八篇中文深度文档，从架构分析到压缩原理再到那份翻车日记。连“后端上线了、下线了”这样的通知，都做成了可选参数，接进了军团自有的微信推送通道——自己开的路上顺手铺一块砖，别的兵就能少走一段。README“缘起”的末尾，我写了一句话，是这整章最想说给读者听的：“如今它开源了。希望能帮到每一个曾被沙箱‘关住’的你。”开源那天，有朋友转群时打趣，说广山哥把半夜的火气打包成了一条 pip 命令。我倒觉得更准确的说法是：我把一晚上学到的东西，折算成了别人半夜里少熬的一小时。

这一章收束之前，允许我把方法论说完整。自己开一条路，在 wsstunnel 这里是一次网络突围，但它真正的适用范围大得多。军团里到处是它的变体：knowly 不把知识托付给云笔记，自己拉一条 SSH 到自己的 NAS；poeapi_go 不把密钥和数据交给黑盒网关，自己写一个处处脱敏的；podcast 不把声音的命运交给平台，自己建一条一天四档的生产线；api.yuangs.cc 和 want.biz，则是这些自己开的路交汇成的路网。它可以被压缩成三句话：门没上锁的时候，用现成的路，不必为开路而开路；门被焊死的时候，先找封锁者自己留的那道缝，利益所在处必有出入口；连缝都找不到的时候，记得标准库里还有 pty，还有你自己的双手。自力更生在工程里的含义，从来不是道德口号，而是一种可执行的判断：凡是依赖外部的部分，就要接受外部随时收回它的风险；凡是不能接受被收回的部分，就自己造。算力主权、数据主权，这些大词的每一块砖，都是这样一个个半夜里亲手砌上去的。世界随时可能把门焊死，而一个手里有工具的人，永远可以自己开一条路。

---

## 第5章 小即美：sourcepack 与 wxwatcher 的克制之道

# 第 5 章 小即美：sourcepack 与 wxwatcher 的克制之道

第 4 章讲的是绝境求生，这一章换一口气，讲两件小事。军团里有两件兵器小到几乎不好意思叫“作品”：一个是把代码库打成一份 AI 可读快照的 sourcepack，Go 源码只有一个文件，1413 行；另一个是盯着目录变化、往微信推一条通知的 wxwatcher，五个模块，源码 1188 行。它们没有绝境，没有翻车，没有半夜的顿悟，甚至没有像样的故事。可如果把五根支柱比作一支军队，那么“小即美”这根支柱的驻防地，就是这两座营盘。Unix 哲学说过三句老话：程序只做一件事并做好；程序协同工作；程序处理文本流。这三句话在我这里被压缩成一句——小即美，组合优于集成。这一章，我用两个小工具把它讲透。

## 5.1 Simple, Fast, and AI-Friendly：三个词的尾签

先交代 sourcepack 是干什么的。README 第一句是它的全部定义：“一键将项目代码库转换为 AI 可读的 Markdown 快照。”再补一句场景就完整了：它是一个极简、高性能的工具，扫描项目目录，把代码合并成单个 Markdown 文件，方便快速喂给大模型，或者做代码审计。就这么一件事。你想让 AI 帮你读一个项目，就得把项目交给它，而对话框一次贴十个文件已是极限，上百个文件逐个粘贴是一种刑罚。sourcepack 把这场刑罚压缩成一个单词的命令，敲下 gdoc，几秒钟后你手里多了一个文件，里面是整个项目的结构树、目录索引、全部源码和统计信息。拿这一个文件去喂任何模型，它就“读过”了你的代码库。

这个项目的出生是我所有作品里最平静的一次。2026 年 1 月 11 日，仓库里落下第一条提交，提交信息只有一句话：“A tool to conveniently generate documents for your project！”没有缘起长文，没有痛点铺陈，没有宣言。weclaw 有一个操作系统级的野心，knowly 有对抗遗忘的执念，wsstunnel 有一个被焊死的夜晚，而 sourcepack 什么都没有，工具本身就是全部的表达。我后来把它称为军团里最“无我”的一个作品：作者退得干干净净，只剩下一个问题和一个答案。也正因为没有故事可讲，它反而最适合回答一个更本质的问题——当我说“小即美”的时候，我到底在说什么。

先看体量。整个工具是单文件 1413 行的 godoc.go，从 main 函数到输出全部住在一个文件里；第三方依赖打开 go.mod 只有一行，github.com/spf13/pflag，用来解析命令行参数，此外再无一人。数据结构只有两个主角：一个 Config 装配置，一个 FileMetadata 装文件元信息。主干只有一条：扫描、过滤、合并、输出。run 函数里，我用最朴素的数字注释把这条主线钉在墙上，第一步扫描并过滤，第二步处理 dry-run 的纯预览，第三步处理纯统计，第四步走完整输出。你在代码里不会迷路，因为根本没有岔路。

```go
// 1. Scan and filter
files, stats, skipped := scanDirectory(config)

// 2. Dry-run: terminal output only
if config.DryRun {
    printDryRun(files, stats, skipped)
    printStatsTerminal(files, stats)
    return nil
}
```

有人问我为什么不拆包。按维护的直觉，代码超过一千行就该分目录了，可我一直没拆。原因不是懒，恰恰是算过账：这是一个“一次输入、一个出口”的工具，模块之间没有可以独立演化的接缝，拆出来的每个包都只有一个调用方，包边界只是把同一个思路切成四段，再用人肉胶水粘回来。拆包是给“多方向生长”预留的脚手架，而 sourcepack 的方向从第一天起就只有一条。不预留脚手架，也是一种工程决策，而且是更难的那种。

再看它服务的对象。人类历史上文档的读者几乎都是人，而 sourcepack 生成的快照，第一位读者是模型。这是一种新文体，我私下叫它“给机器写文档”。它没有现成的范式可抄，只能从机器的阅读习惯倒推：第一，读者不求甚解，模型可能只读目录树就敢回答问题，所以结构树必须目录优先、字母排序，让“扫一眼”也能建立正确的全局图；第二，读者靠引用定位，所以文件目录必须带锚点，模型说“见 godoc.go 里的 scanDirectory”时要真能跳得过去；第三，读者会被格式欺骗，源码里若嵌着反引号，普通的代码块包裹会当场断裂，于是打包前要专门扫描每个文件里反引号的最大连续数，用更长的围栏把它裹住。这些细节没有一条服务于美学，全部服务于机器的阅读容错——新文体的排版学，本质上是错误率排版学。

所以快照固定四件套：目录优先、字母排序的项目结构树；带锚点跳转的文件目录；自动语法高亮、智能处理嵌套代码块的完整源码；多维度项目统计，文件数、目录数、语言分布，外加一个 Token 预估。这个预估的算法坦白得可爱：字节数除以四。没有任何分词器，没有模型词表，就是一条经验公式，粗糙但够用——我要的只是生成前心里有数“这一包大概多大胃口”，精确到个位数毫无意义。够用就好，这四个字是小即美最常被低估的部分。

合并这一步也不是简单的首尾相接。四件套的排布顺序就是一条精心安排的阅读动线：先给树，再给目录，再给正文，最后给统计——模型先建立骨架，再按需下钻，最后带走一份可量化的全貌。生成树的时候目录优先、字母排序，任何项目进来都会得到同一种秩序；正文按文件顺序拼接，每个文件前挂着带锚点的标题；如果只想知道项目的体量而不想生成文件，加一个统计开关就够了，连一个字节都不写盘。小工具的功能清单短，但清单上的每一项都带着自己的道理，没有一个是为了凑数而存在的。还有一个小习惯值得一记：真正生成之前，可以先跑一遍预览，看看会打包哪些文件、大约消耗多少 Token，确认无误再落盘。这个先试后用的基因，后面在 wxwatcher 身上还会再见到一次——那是两件小工具隔空握手的暗号。

于是 README 的最后一行，那个尾签，不是装饰。斜体、一句、三个词：“Simple, Fast, and AI-Friendly.”——我把它放在文档的最末尾，像工匠在器物底部刻下的戳。这三个词也是三条验收标准：Simple，单文件、单依赖、一条主线，任何人打开源码五分钟读完主干；Fast，Go 编译出的静态二进制，万行代码秒级打包；AI-Friendly，输出的每个细节都为机器读者优化过。尾签只有一行，守住它却要每一次提交都过关。克制不是一次表态，是一条持续的军规——这一点，5.5 节我还会回来清算。

## 5.2 Go 的性能+npm 的触达面

快照工具属于“想起来才用一次”的品类，用的那一刻门槛必须趋近于零：如果还得先配环境、装工具链，热情就死在半路上。开发者世界里触达面最大的入口是 npm，一句 npm install -g sourcepack，全世界的开发者三秒上手。但纯 Node 写的命令行工具，扫描大目录的性能和分发的笨重都让人皱眉。我的答案是两个世界各出一半力气：核心用 Go 写，编译成静态二进制；外壳用 npm 做，负责分发。

具体的组合方式值得细看。CI 里用 Go 的交叉编译一次性产出五个平台的静态二进制：darwin 与 linux 各自的 amd64 和 arm64，再加一个 windows 的 amd64，一个 Go 源文件，五份免安装的产物。npm 包发布时只带 index.js 和 download-binary.js 两个薄壳文件，用户 npm install 触发 postinstall 钩子，脚本识别当前操作系统与 CPU 架构，把对应的那个二进制下载下来。运行时 index.js 用 spawn 把参数原样转发给二进制，再把退出码原样转回来，用户在终端里完全感知不到壳的存在。package.json 里连 bin 都登记了五个名字：sourcepack、gdoc、gdx、gx、doc，两个主命令完全等效，另外三个是别名——怎么顺手怎么敲，这种小纵容不花一行核心代码，全是体验。有人嫌这套双层结构麻烦：每次发版，先编译五份二进制，再发 npm 包。但这笔账同样算得过来——熟悉 Go 的用户可以直接去发布页拿二进制，一秒都不用等；普通开发者走 npm，三秒装完。两个生态的用户各自走自己最顺手的门，谁也不用迁就谁，这就是把触达面做到极致的样子。

```json
"postinstall": "node download-binary.js"
```

这就是组合优于集成的教科书场景。集成是造一个四不像：用 Node 把扫描逻辑重写一遍，或者写一个巨大的单体同时伺候两个生态。组合是让 Go 做它最擅长的——编译快、静态链接、扫目录性能好；让 npm 做它最擅长的——全球最大的开发者分发渠道；中间只用一层三十行的胶水。两个组件之间只有一条窄窄的接口，下载二进制、转发命令，谁也不侵入谁的内部。Unix 哲学讲的“程序协同工作”，在现代工程里的对应物就是这种跨语言协作：不求同源，只求接口干净。

Go 与 npm 的组合之外，还有一层更小的组合发生在输出端，这最能看出“工具是管道的一段”的设计。同一次扫描有三条交付路径：想直接贴进对话框，加 -c 一键复制到剪贴板，不落盘；想交给另一台机器上的模型，加 -p 推送到远端中继，地址与密钥从环境变量 SOURCEPACK_PUSH_URL 和 SOURCEPACK_AUTH_KEY 里读，实际接的是我自己的 knasync 队列；只想远程了解项目概貌，就用统计模式只推数据不推源码。三条路径共用同一个引擎；如果一次指定了多个出口，程序不装糊涂，按剪贴板、推送、iCloud、统计、文件的优先级仲裁，并在终端明说用了哪条。一个工具只做一件事，但这一件事的出口可以有很多个——出口多不是不专一，交付方式本就该由使用场景说了算。

小工具的克制，更多地藏在那些“不越权”的细节里。最能体现这一点的是它的忽略系统，一共三层。第一层是默认忽略，node_modules、vendor、dist、build 这些人人都知道不该喂给模型的目录，不用配置就静默跳过。第二层是一个完整的 gitignore 匹配引擎，自动读取项目已有的 .gitignore 并遵守它，支持感叹号否定的重新纳入、双星号的跨目录通配、目录专属语法，后来还有一次专门的提交：“升级 gitignore 匹配引擎，支持 ! 否定、** 锚定、目录专属语法”。一个打包工具认真实现别人家的匹配语法，听起来很重，但这是对既有约定的尊重：你已经向 Git 声明过什么是垃圾，就不该向第二个工具重复声明一遍。

第三层是点睛之笔，一个专属的 .gdignore 文件。场景是这样的：有些文件你不想喂给模型——几十兆的大 JSON、编译产物、测试夹具——但它们不该被 Git 忽略，你得提交它们。往 .gitignore 里写，Git 就看不见了；这时候在项目根目录放一个 .gdignore，语法和 .gitignore 完全一致，但只影响 sourcepack 的扫描，不影响 Git。README 里那句原话我特意写得郑重：“只影响 sourcepack 的扫描，不影响 Git。”这句话的语法结构就是克制的结构——一个工具清楚地声明自己影响的边界，边界之外一概不碰。.gitignore 属于 Git，sourcepack 无权置喙；它只在自己的领地里立了一块界碑。小工具最容易犯的错不是功能太少，而是手伸得太长，gdoc 抵抗住了这种诱惑。

还有一处把常识固化成代码的地方，我特别喜欢。喂给模型的应该是文本，这是废话级常识，但真实代码库里混着图片、字体、二进制资产，逐字节拼进 Markdown 就是灾难。于是扫描时有一个 isBinaryBuffer 函数把关：先看缓冲区是不是合法的 UTF-8，再用 NUL 字节探测，同时配一张已知文本文件的白名单兜底，两道检测都指向二进制才丢弃，并在 dry-run 的跳过清单里老实告诉你原因。你看，“喂给 AI 的应该是文本”这句常识没有停留在 README 的形容词里，它变成了一个以七个字母开头的函数，变成了一条在每份快照生成前执行的判定。所谓工程化，就是把常识翻译成代码，让它在每一毫秒替你站岗。

最后看一眼发布闭环，它同样说明克制不等于简陋。GoReleaser 管五平台产物，CI 测试矩阵管质量；有一次 macOS 15 的 runner 踩到工具链的 LC_UUID 怪病，我的解法是升级 Go 1.23 以上绕开它，并且把来龙去脉写进提交信息留给后人。修复类提交更像微型案例分析，比如有一条：“fix: -m/-X/-I 路径关键字被 cleanList 误加 . 前缀导致过滤失效”——一行字里有症状、有根因、有位置。仓库的历史不只是变更记录，是一封写给下一个深夜排查问题的人的信。

## 5.3 两阶段扫描：轮询也能做到毫秒级

现在把镜头转向另一个小工具。wxwatcher 的定义同样只有一句话：“文件变更监控工具：检测到变化时，通过微信推送通知。”它解决的是一个孤独者的痛点：服务器上的目录变了，而人不在机器前。我在好几台机器上跑着服务，日志在滚、构建在落盘、配置在被改，我不能每一小时 ssh 上去看一圈。我想要的是——目录有变动，手机立刻响一下，推送里带着主机名、文件名、增删改的类型。2026 年 4 月 25 日，wxwatcher 的仓库落下第一条提交，五个月后迭代到 PyPI 上的 v1.15.0。它的推送尾部有一行签名，“By: 苑广山的文件监控助手”，先解决自己的问题，再顺手做成公共品，这条军团惯例它也没缺席。

监控文件变化，教科书会告诉你用系统级通知机制：Linux 有 inotify，macOS 有 kqueue 一族，Windows 另有一套。这些方案事件驱动、零轮询，看起来是“最强方案”。我在架构分析文档里专门写下不做它们的原因：“它没有追求 inotify/kqueue 等系统级通知，而是在轮询模式下通过精心设计的性能优化，在保持跨平台兼容性的同时达到实用的性能。”翻译一下：inotify 只管 Linux，macOS 和 Windows 各有各的接口，三套接口三套坑，而我每一台机器都想跑同一份代码。轮询看起来笨，但它是一份代码跑遍三个平台的最大公约数。不选最强方案，选能一次跑通所有平台的方案——这是取舍，不是妥协。

当然，轮询的账要算清楚：默认每三十秒扫一轮，如果每一轮都把五千个文件逐个读内容算哈希，磁盘会被打得嗷嗷叫，机器风扇先替用户抗议。所以有了这个项目最核心的设计，概念词典里登记为“两阶段扫描”：先 stat 快筛，仅对疑似变更的文件计算 SHA256。拆开看，一轮轮询是一条五步流水线。第一阶段 fast_scan，只调 os.walk 和 os.stat，读的全是元数据——修改时间和文件大小，一个字节的内容都不碰。第二阶段拿本轮元数据和上一轮基线对比，绝大多数文件的时间戳和大小纹丝没动，直接放行。第三阶段才轮到 sha256_file，只对那几个疑似变更的文件真正打开文件、读取内容、计算哈希，确认内容真的变了而不是仅仅被触碰了一下。第四步把确认过的变更分批推送到微信。第五步把新基线原子落盘。README 里那句性能结论是实测过的：“5000+ 文件的目录，每轮仅需毫秒级扫描，变化文件少时几乎零磁盘 IO。”

```python
if size > max_size:
    # 对超大文件，只计算前 8KB 的哈希作为近似指纹
    h = hashlib.sha256()
    with open(path, "rb") as f:
        chunk = f.read(8192)
        h.update(chunk)
    return f"LARGE:{size}:{partial_hash}"
```

这套设计里有一个细节最能体现什么叫务实的工程。第三阶段算哈希时，超过十兆的大文件并不整读——一个两 GB 的视频若每轮都全量哈希，两阶段扫描就前功尽弃。代码的处理是只读前 8192 字节，算一个部分哈希，再拼上文件大小组成指纹，形如 LARGE 冒号加尺寸加哈希。严格说这是近似：如果有人只改动一个大文件靠后的内容，时间戳会变，元数据筛不出来；即便筛出来，8KB 指纹也可能碰巧漏报。但这笔账是算过的——第一，大文件的尺寸与时间戳变化本身就在指纹里；第二，日志滚动、缓存落盘这类高频大文件写入，几乎必然波及文件头部或尺寸；第三，用极小概率的漏报，换掉“每轮对每个大文件全量读盘”的确定性灾难，这笔交易闭着眼也该做。工程里没有免费的精确，只有标好价格的近似；把价格标出来，然后决定买不买，这就是取舍的全部内容。

还有一层设计藏在推送消息本身，值得单独致敬。启动时先推一张卡片：运行主机、监控目录、文件数量、启动时间，四行，一眼确认“我确实在看着”。变更通知里每一条都带类型标签与大小增减——新增、修改、删除，配置文件加了两点一 KB，README 多了一百二十字节——人扫一眼就知道发生了什么，不必登机器去猜。多机部署时还能用参数给每台机器起别名，家里的叫家里 Mac，公司的叫办公室，推送自带主机名，几台机器同时报信也不会张冠李戴。有意思的是，sourcepack 在为机器优化文本，wxwatcher 在为人优化文本，两个小工具站在同一个问题的两端：文本的读者是谁，格式就为谁让路。

轮询主线的稳健性还有几块垫脚石。忽略规则默认放过 .git、__pycache__、.venv 这类永不关心的目录，支持三种写法：精确名字、通配符、以前缀 regex 开头的正则，甚至可以用感叹号取回某条被忽略的规则；正则每轮都要匹配，编译结果就放进容量一百二十八的 LRU 缓存，不为重复编译付钱。网络不稳时推送走指数退避，一秒、两秒、四秒，最多三次，把瞬时抖动和真故障区分开；日志记录推送地址前先脱敏，把 URL 里的令牌参数抹掉再落盘，排障时不会被自己的日志出卖。这些细节单看都不大，合起来是一句话：轮询是笨办法，但笨办法可以穿礼服。性能不是追出来的，是省出来的——inotify 省掉的是“盯着”这件事，两阶段扫描省掉的是“重复读”这件事，殊途同归。

这个工具对“什么时候跑”也保持了罕见的开放。它可以常驻，三十秒一轮，安静地盯着一台服务器；也可以用单轮模式，建立或加载基线、检测一轮、保存状态就退出，把自己交给 cron 或 systemd timer 调度。常驻和定时是两种世界观，很多工具只肯伺候一种，wxwatcher 用同一套扫描引擎把它们都接住了——选哪种是部署者的事，工具不替人做决定。跑着的时候它还定期吐心跳日志，让运维在“没有消息”的日子里也能确认它活着。别忘了，对一个监控工具来说，最难的不是报警，是让人相信没消息就是好消息。

## 5.4 公共品完成度：PyPI 之外的九件事

如果说 sourcepack 的美德在“少”，wxwatcher 的美德就在“全”——一个只有 1188 行源码的工具，公共品完成度却像模像样。我一直认为，个人工具和公共品的分界线不在主功能，主功能大家都写得出；分界线在主功能之外的那一圈：退出干不干净、崩溃会不会留残骸、新人能不能零配置上手、服务器上跑安不安全。这一圈功夫没有任何一行属于“核心逻辑”，却占了全部工作量的大半。数一数，wxwatcher 在 pip install 之外做了九件事。

第一件，优雅退出。监控类程序最怕两件事：杀进程时状态没保存，重启后从头再来。wxwatcher 把 SIGTERM 信号转成 KeyboardInterrupt 异常，源码注释写得清清楚楚：“让 systemd stop / kill 也走优雅退出路径（保存状态后再退出，避免重启后重建基线）”。无论是终端里的 Ctrl+C 还是运维的 kill 命令，都汇入同一条保存状态再退出的路径。第二件，原子写。保存状态的实现是先写临时文件、再调 os.replace 原子重命名，这两个动作在操作系统层面要么全成要么全不成，崩溃或断电不会损坏已有状态——最坏情况是丢最后一轮基线，绝不会留下写了一半的坏 JSON 让下次启动直接瘫痪。

第三件，四层配置。优先级管线是命令行参数、环境变量、配置文件、默认值，一级压一级；忽略规则还用合并语义——累加而非覆盖，默认忽略加上用户追加，而不是用户一配置就把内置规则清空。配置文件自动从当前目录、父目录、家目录三级位置搜索，配一次到处生效。第四件，测试。888 行测试对着 1188 行源码，比例约四分之三，九十多个测试用例把扫描、配置合并、截断、重试这些核心路径全部钉死，每个边界条件都有用例守着，改坏任何一处，CI 立刻变红。小工具最容易省掉的就是测试，理由永远是“就这么点代码我心里有数”；可公共品的“公共”两个字，恰恰意味着用户的心里没数，得替他们把数守好。

第五件，systemd 加固。随包附带的 wxwatcher.service 不是能跑就行的样子货，安全加固三件套齐全：NoNewPrivileges 禁止提权，ProtectSystem=strict 把整个文件系统挂成只读，ProtectHome 把家目录降为只读，再用 ReadWritePaths 精确放行 ~/.wxwatcher 这一个写入点；服务挂了自动重启，十秒后卷土重来。进程就算被攻破，能打碎的东西也被锁在斗室之内。第六件，无密钥的 CI 发布。推一个 v 开头的 tag，GitHub Actions 自动跑测试、构建、发布到 PyPI，认证用的是 OpenID Connect 的临时令牌，工作流里只有 id-token 写权限一行，仓库里不存在任何长期密钥。发布这个动作被压缩成打一个 tag，密钥泄露面被压缩成零。

第七件，零配置试用。dry-run 模式下只检测并打印变更，不推送、不写状态，此时连推送地址和令牌都可以不配。新用户两条命令就能看到工具在眼前工作，先体验，再决定要不要花五分钟配置——这是所有工具都该学的礼节。这条礼节的完整动线是这样的：装好之后随便挑一个目录试运行，终端里立刻列出新增、修改、删除；觉得对了，再补上推送地址和令牌两个环境变量，纳入正式监控；不想常驻，就换成单轮模式挂进定时任务。三步以内，从陌生到信任，中间没有任何一步需要翻文档。第八件，防刷屏。半夜构建机疯狂落盘，一轮变更多达几百条，如果照单全推，微信瞬间沦为灾难现场。所以推送前把变更列表截断到上限条数，再按每批最多五十条分批发送，超出部分明确告知截掉了多少——宁可信息少一点，不让通知变成骚扰。第九件，按目录隔离的可观测性。日志自动写进 ~/.wxwatcher 且按监控目录各自成文件，配合 RotatingFileHandler 做一兆乘五份的轮转；状态文件按目录的 MD5 生成独立文件名，同一台机器监控五个目录互不踩脚。出了问题，日志、状态各归各位，排查不需要侦探天赋。

这九件事没有一件在 README 特性列表的第一屏，但它们是“公共品”三个字的全部含金量。还有一个安全细节值得一提：1.7.0 的提交写着 remove default push token，force env config——我曾内置过一个默认推送令牌，后来亲手删掉了它，宁可让每个用户多敲一行环境变量，也不在仓库里留一把公共钥匙。另外，连版本号本身都算完成度的一部分：wxwatcher 的版本提交从不写“发个版”了事，而是把这一版对用户意味着什么写成清单。

```
bump to 1.14.0 — fix --ext pruning, parallel baseline, --once/--dry-run, SIGTERM grace
```

修了扩展名过滤，加了基线并行，加了单轮与试运行模式，补了信号优雅退出——版本号即变更日志，翻一遍提交历史，就知道这五个月每个版本兑现了什么。回头看这五个月、三十五次提交，wxwatcher 的源码只有 httpx 一个运行时依赖，却把一个“小脚本”活成了别人敢部署到自己服务器上的“小产品”。架构分析文档末尾那句自我评价，我认为是对小工具最恰当的褒奖：“设计成熟度远超其代码规模。”

## 5.5 克制是持续的决策，不是一次性的风格

把两个项目并排放着看，能看清“小”的真面目。sourcepack：单文件 1413 行，一个第三方依赖，一百零四次提交；wxwatcher：五个模块 1188 行，一个运行时依赖，三十五次提交。注意提交数和行数的比例——sourcepack 平均十几行代码就要动一次刀，wxwatcher 平均三十多行动一次。小项目的迭代频率一点不比大项目低，区别在于大项目加行数，小项目反复擦亮同一小片金属。再往深看一层，这两个项目放在一张桌上对比会更有意思：一个用 Go 写，住在 npm 上，为模型造粮食；一个用 Python 写，住在 PyPI 上，为人造耳目。一个编译成五个平台的二进制，一个靠一个 httpx 走遍三平台。语言、生态、分发方式没有一处相同，唯独两件事相同：直接依赖都只有一个，行数都死死按在一千出头。巧合背后是同一条纪律——依赖数和行数不是预算花完为止的额度，是每天都要重新投票的立场。

这两个仓库的历史里，最贵的提交从来不是“加了一个功能”，而是“决定不加”和“决定删掉”。加功能和删功能，在心理上完全不对称。加功能是顺流而下：用户提了、竞品有、自己手痒，写就是了，多出来的代码每天都会提醒你它的存在。删功能是逆流而上：代码已经在那里跑着，测试盖着，删掉它要说服的只有一个人——几个月前决定写下它的你自己。wxwatcher 的 1.7.0 就是典型：默认推送令牌当初是为了让用户开箱即用，是我自己加的贴心；用了几个版本后我意识到，一把写在源码里的公共钥匙等于把自家门锁的密码印在楼道里，任何一个用户都能用它给我发消息，也能替我发消息。于是删令牌、强制环境变量、更新文档，一个“功能”的退场比它的登场隆重得多。删功能难的真正原因是，你删的不是代码，是自己当初的判断——承认“我当年想错了”比多写两百行代码痛苦得多。

sourcepack 的账本上，没写下来的比写下来的更有信息量。它不做配置文件——命令行参数加环境变量已覆盖全部需求，配置文件是给“经常改参数”的场景准备的，而打包命令的参数几乎从不改变。它不做守护进程——快照是一次性动作，常驻是监控工具的事，两个工具划清了地盘。它不做增量缓存——要的是每次都新鲜的完整快照，缓存的复杂度换不来用户可感知的收益。它甚至不做精确的 Token 计数——字节除以四，粗糙，但省下一个分词器依赖和它的维护成本。每一次“不做”都有一笔被放弃的账，而克制就是把这笔记账的习惯保持下去。风格是一张照片，决策是一部连续剧；小不是某次重构的成果，是每一次提交时都重新投票的结果。

但请千万别把克制读成寒酸。小即美的“小”，是拿得起也放得下。拿得起，看 wxwatcher：两阶段扫描的工程精度、大文件近似的账本、systemd 三重加固、原子写、优雅退出、九十多个测试——该硬的地方一寸不让，每一处都可能被真实世界的故障检验。放得下，看 sourcepack：一个依赖、两个结构体、一条主线——不该长的地方一毫米都不长。真正的克制是把资源从“什么都要”里解放出来，全部押注在用户真正会走的那条路径上。小工具的可靠性恰恰来自小：代码越少，状态越少；状态越少，出错的组合就越少；出错的组合越少，确定性就越高。这正回到全书的总纲——用技术的确定性，对抗世界的不确定性；而“小”，是确定性最便宜的来源。

还有一层底气，要归给军团。wxwatcher 的推送地址指向的正是 api.yuangs.cc 上的微信推送通道，sourcepack 的推送模式接的是 knasync 队列，两个小工具都不用自带服务器、自带长连接、自带消息可靠性方案——这些重的部分由共享基建一次性解决，工具本身只需要关心自己那一小段逻辑。这就是一个人敢把工具做小的秘密：当基建是共享的，每个新工具的固定成本就趋近于零，小工具才能小得理直气壮。反过来，每一个小工具又在给基建添砖：sourcepack 的快照经中继流向另一台机器上的模型，wxwatcher 的变更经 Knowly 扩展点流进我的知识库。军团内部的分工越细，每个成员就越轻；越轻，就敢越专；越专，组合起来的杀伤力就越大。这不是九个项目的偶然凑巧，是一种刻意维持的结构。

再看兵种搭配，这份小也不是孤立的。weclaw 三点八万行，六个直接依赖，是重装兵；poeapi_go 六千多行，是中军；而 sourcepack 与 wxwatcher 是斥候，轻装、快、只报信不打仗。兵种齐全的前提是每件兵器都清楚自己的斤两——大项目之所以敢大，是因为旁边有小工具兜住了那些不值得大动干戈的需求；小工具之所以敢小，是因为知道自己背后站着可以组合的同伴。这两个工具从不追求大而全，它们把自己设计成管道的一段：sourcepack 的输出可以进对话框、进剪贴板、经一条推送命令进 knasync 中继交给另一台机器上的模型；wxwatcher 的通知可以进微信，也可以经 Knowly 扩展点进我的知识库，常驻与单轮两种模式分别伺候守护进程和定时任务两种世界观。它们不吞掉管道，它们是管道。一个人维护的军团，赢法从来不是每件兵器都巨大，而是每件兵器都足够小、足够硬、接口足够干净——小到可以随手组合，组合起来又是一支军队。Simple, Fast, and AI-Friendly，这句尾签我刻在 sourcepack 的 README 末尾，也愿意刻在自己每一个工作日的末尾。

---

## 第6章 微信操作系统：weclaw，把聊天窗口变成指挥部

# 第 6 章 微信操作系统：weclaw，把聊天窗口变成指挥部

一支军团不能只有散兵。前四章里，knowly 守着知识的仓库，wsstunnel 修着突围的路，npm_yuangs 把守着工具库的大门，他们各司其职，但他们需要一个共同的作战室：号令从这里发出，战报向这里汇拢，各兵种在这里协同。这个指挥部选在哪里，是个被大多数工程师想错了的问题。直觉的答案是终端，是 IDE，是一个自制的管理后台——那是工程师以为的战场。可我的大部分时间并不在终端前：我在通勤的路上，在饭桌旁，在遛弯的途中，灵感来了，任务来了，我手里只有一部手机。人在哪里，指挥部就必须在哪里。这一章讲 weclaw，一个微信 AI Agent 桥接器，它把我每天必开的那个聊天窗口，变成了整支军团的中军帐。本章的三根支柱在小即美、默认安全与体验完整性之间穿行：一个接口统一三个世界，是组合优于集成；脱敏先于一切外发，是默认安全；而把每一个交互细节都打磨到“跑得像个人”，是体验完整性。

## 6.1 为什么指挥 AI 要回到微信

先说清楚问题。二〇二五年以来，我的工作流里 AI 无处不在：终端里有 CLI Agent，IDE 里有编码助手，服务器上还跑着各种网关。它们个个能干，但它们全都住在一个地方——电脑。而我不住在电脑前。想给 Claude 发一个任务，我得开电脑、开终端、敲命令；走在路上想起一件事，只能掏出手机备忘录先记着，回家再转录。这条“人到任务”的路径上全是摩擦，而摩擦的计量单位是分钟：一天二十次，一次一分钟，一个月就是十个钟头。更别提多轮对话了——Agent 干活要等，等人回电脑再看结果，思路早断了。

所以问题可以重述为：找一个我已经全天候在线的入口。数一遍我手机上按打开频率排序的应用，第一名没有悬念，是微信。它常驻、它置顶、它的通知我必看、它收语音收图片收文件、它在地铁里在电梯里在任何地方都通。它不是我选的，它是我生活本身已经长出来的接口。既然如此，与其把人拽到 AI 面前，不如把 AI 接到人面前——做一座桥，把微信接进 AI Agent。这就是 weclaw 的全部起点。README 第一行写得很克制：WeChat AI Agent Bridge，把微信接入 Claude、Codex、Gemini、Kimi 等 AI Agent。深度分析报告里则给了它一个更完整的定性，我原样抄在这里：“WeClaw 是一个以个人微信为入口、以多种 AI Agent 为后端、以文件系统为共享记忆的多智能体协作操作系统。”

操作系统这个词不是修辞。报告里有一段话，我每次读到都会心一笑，因为它把我的心思说穿了：“作者是在把微信当成一个操作系统的 shell 来经营：输入是自然语言和斜杠命令，输出是文本、图片、文件，后台有 cron 守护，有文件系统（Hub）做共享内存，有进程（Agent）池可供调度。这不是比喻，这就是它的实际结构。”聊天窗口就是命令行，消息就是指令，Agent 就是进程，Hub 目录就是共享内存，斜杠命令就是 shell 内建命令。我不过是给一个国民级入口，配了一整套内核。

也有人问过：为什么不做一个专用 App，或者一个网页后台？都掂量过，都败给了同一个事实——它们是新增的入口，而新增的入口需要新增的注意力。专用 App 要下载、要注册、要申请通知权限，网页后台要记地址、要开浏览器、要重新登录，而人的注意力不会为工具的方便让步半分。微信的厉害之处恰恰在于它不要求任何新习惯：消息本来就要看，通知本来就会弹，输入法本来就顺手，我不过是让军团搭上了这条已经存在的信息动脉。工具去迁就人，而不是人迁就工具——这是指挥部选址的最终裁定，也是后面所有设计的出发点。

接入的过程，被压缩到了“扫码即用”四个字。这是指挥部建设的第二条纪律：指挥部不能有施工期。安装是一行命令，启动之后第一件事是渲染一张二维码在终端里，用微信扫它，登录就完成了；随后 weclaw 自动探测机器上装了哪些 Agent，把配置写进 ~/.weclaw/config.json，然后开始收发微信消息。从想用到能用，中间没有任何一个需要读文档的环节。

```bash
# 一行安装
curl -sSL https://raw.githubusercontent.com/fastclaw-ai/weclaw/main/install.sh | sh

# 启动：终端出二维码，微信扫码即用
weclaw start
```

这几十秒里没有账号体系，没有注册流程，没有控制台地址。零摩擦不是锦上添花，它决定了工具的生死：一个指挥官如果每次进指挥部都要先办门禁卡，他宁可不用这个指挥部。微信本身替我省掉了认证、通知、多媒体输入三大件——它们都是微信已经做好的东西，我只是接上去。

指挥部还得保证战报必达。人在干活，Agent 也在干活，谁先干完？多数时候是 Agent。若结果要等人回到电脑前才看得到，指挥就算断了线。所以 weclaw 除了被动应答，还留了主动出击的通道：命令行里一条 send 子命令，或者在本机 API 上一个请求，就能把文本、图片、文件主动推进某个微信对话——日志、定时任务的产物、军团其他系统的战报，都顺着同一条通道汇进指挥部，而这条出口默认只对本机开放。输入一侧的谱系同样齐全：语音发进来，微信那头已经转好的文字自动喂给 Agent；图片发进来，先有人看图再进对话；文件发进来，解析后注入上下文。我可以动嘴下令，可以发图下令，可以扔一份文档下令，而军团的一切动静，都会以微信通知的形式找到我。人在哪里，消息就在哪里——这句话反过来也成立。

来历也要交代清楚。weclaw 的灵感来自腾讯官方的 @tencent-weixin/openclaw-weixin 项目，README 的开头就注明了这一点，并写明仅供个人学习、不用于商业用途。我的做法是学它的协议思路，然后用 Go 独立重写一遍。重写不是不甘于人后，而是这个项目的用途决定的：它要读我的微信消息，要替我发消息，要接触我生活里最私密的一条通道。这样的东西，我必须能一行一行读懂它的每一处网络请求、每一个字节的去向。自己写一遍，是理解一座桥的唯一方式——也是让桥只听命于我的唯一方式。至此，指挥部的选址、施工、验收全部完成。但真正的问题在门里：号令进来了，谁去执行？

## 6.2 一个接口统一三个世界：ACP/CLI/HTTP

这一节讲 weclaw 的骨架，也是本章第一根支柱——小即美——最纯粹的一次落地。Unix 哲学里有一句话人人会背：小即美，组合优于集成。落到工程上，它意味着一个系统不要试图吞掉所有能力，而要定义好自己的边界，然后把别人的能力组合进来。weclaw 的边界画在一条 Go 接口上：agent.Agent。它只有七个方法——Chat 发文本收回复，ChatWithMedia 带媒体收发，ResetSession 重开会话，Health 探活，Info 报元信息，SetCwd 切工作目录，SetProgressCallback 注册进度回调。七个方法，覆盖了一个 AI 后端在聊天场景下的全部生命周期。

接口底下站着三个实现，对应三个截然不同的世界。第一个世界是 ACP：Agent 进程以长驻子进程的方式存在，weclaw 通过标准输入输出上的 JSON-RPC 2.0 与它通信，会话由 Agent 内部维持，进程和上下文反复复用，这是三种模式里最快的，Claude、Codex、Kimi、Gemini 都支持。如果用拟人的说法，ACP 是常驻的仆人——他就住在指挥部里，随叫随到，还记得你昨天说过的话。第二个世界是 CLI：每来一条消息就新起一个进程，跑完即退，会话靠一个 resume 续话参数把上一次的会话 ID 传回去续上。它是随叫随到的零工——每次都现找人，但干完就走，不留负担。第三个世界是 HTTP：OpenAI 兼容的对话接口，无状态，历史由客户端自己携带。它是远方的智者——不驻扎，通过信使传话，能力取决于网络那头是谁。三种性格，三种通信方式，三种会话模型，被同一个接口收编。配置文件里只有一个 type 字段决定谁是谁。

```json
{
  "agents": {
    "claude":  { "type": "acp",  "command": "/usr/local/bin/claude-agent-acp" },
    "draw":    { "type": "cli",  "command": "/path/to/draw.py", "aliases": ["画"] },
    "gemini":  { "type": "http", "endpoint": "https://example.com/v1beta" }
  }
}
```

这里有一个 Go 语言送的礼物值得单独说：接口的隐式实现。Agent 接口不需要任何一方 import weclaw、不需要任何一方声明“我实现了它”，只要一个类型长着这七个方法，它就是一个 Agent。这意味着统一没有成本：Claude 不需要知道 weclaw 存在，Gemini 也不需要，协议适配全部收敛在我这一侧的三个实现文件里。集成是把别人拽进自己的体系里改造，组合是站在边界上递话——前者制造耦合，后者制造生态。自动探测逻辑也是同理：机器上同时装了支持 ACP 的二进制和 CLI 时，自动优选 ACP，用户一个字都不用配。

收编之外还有点名制。消息前缀就是点名：发 /codex 开头，这条任务就派给 Codex；记不全名字，用别名——/cc 是 Claude 的绰号，/km 是 Kimi，/gm 是 Gemini，配置里还能自定义，我的绘图脚本别名就叫“画”，于是一个汉字也可以是点名。默认 Agent 随时可以切换，切完写回配置文件，重启不丢——指挥部的编制是持久的，不是记在脑子里的。工作目录也能点名，一条 /cwd 把 Agent 的工地搬到另一个项目去，一条 /new 另起炉灶开新会话。接口的克制还体现在能力分层上：核心接口只有七个方法，而流式这类“有则更好”的能力，被拆成一个独立的可选小接口——谁支持流式谁去实现它，业务层发现谁实现了就优先逐块推送，没实现就退回整段返回。能力不绑架抽象，抽象不假装能力；接口上每多一个方法都是债，何况让每家 Agent 都被迫实现。

接口统一还有一层军团意义：同一个接口背后可以同时站着多个 Agent，它们就获得了被编排的资格。/debate 让两个 Agent 就同一话题正反交锋，几轮下来生成一份完整辩词；/hub pipe 把前一个 Agent 的输出存进共享文件，再喂给后一个当输入，一条流水线把几家模型串成一道工序。组合优于集成的最后一环，正是让被组合者彼此也能组合——指挥部里不止一个参谋，他们还得能互相说话。

真正考验这个抽象的，是 OpenAI 兼容网关。weclaw 起了一个 HTTP 服务，让任何 OpenAI 风格的客户端都能把请求打进来、再转交给后端的 Agent。难题在于：后端的 Agent 形态五花八门，网关怎么对它们一视同仁？我给了一个双路径的答案。对 HTTP 型 Agent，网关不做“调用”，只做“转发”——messages、tools、stream 原样透传，只替换模型别名、注入密钥，于是 tool_calls、usage、流式增量这些精细结构无损穿过，任何 OpenAI SDK 都能直连。对 CLI 和 ACP 型 Agent，网关走回调降级：提取最后一条用户消息，交给 Agent.Chat()，把结果包成一次普通的对话响应——流式是模拟的，多轮退化为单轮。能力有损，但我可以在文档里坦然写下那句定语：“没有 Agent 被排除在外。”

这句话是本节真正想说的东西。架构决策里有一种常见的贪婪：只支持最优路径，其余一律拒之门外——因为降级路径“不纯粹”。但个人软件的立场恰恰相反：我的军团不挑兵种。一个只会命令行的绘图脚本、一个只有 REST 接口的云端模型、一个支持完整 ACP 协议的编码 Agent，它们都能站进同一个编制，听同一套号令。代价是我要为降级路径多写代码、多写文档、多担诚实的义务——把每一处能力损失明明白白告诉用户。为了这句承诺，文档里专门列了能力对照：哪些结构透传无损，哪些在降级路径上退化为单轮，流式哪里是真的哪里是模拟的——诚实是降级路径的伴生义务，隐去损耗的降级等于欺骗。这笔账我算得过来：抽象层几百行代码，换来了以后每一个新 Agent 的零成本接入。接口统一点的选取，比接口本身的精致重要得多——它选在“三个世界的公共子集”上，而不是任何一家的方言上。号令统一了，接下来才轮得到指挥艺术：消息进来之后，这一切是怎么不乱套的？

## 6.3 教科书级的并发：单读协程+pending map

指挥部一天要处理几百条消息：有人在问问题，有 Agent 在干活，有定时任务在触发，有图片在加解密。并发乱了，指挥部就是一个谣言集散地。这一节讲 weclaw 的并发结构，先从全景讲起。整个系统是四层：最底下是协议层的 ilink 包，负责二维码登录、长轮询收信、CDN 媒体加解密；往上是抽象层，就是上一节的 agent 接口，加上 hub 共享文件与 store 持久化；再往上是业务层的 messaging 包，路由、去重、约二十个斜杠命令、多 Agent 编排、定时任务都在这里；最上面是交互层，终端 CLI、网页管理后台、HTTP API。深度分析报告里有一句话，我特意抄下来：“这个分层并非教科书式的预先设计，而是演化出来的秩序。”ilink 先存在，然后 agent 抽象被挤压出来，然后 messaging 膨胀，然后 api 和 web 长出来——层是长出来的，不是画出来的。

一条消息的完整旅程是这样的：monitor 的长轮询循环拉到新消息，丢给 messaging 的处理器做类型判断、去重、命令解析；普通消息按前缀或别名路由到某个 Agent，斜杠命令进命令注册表；Agent 的回复经 Markdown 转纯文本，图片被下载、加密、上传微信 CDN，超长回复切片连发；最后问答对落成 JSONL，供日后回放、中继或发布。链路上每一个箭头，都有并发在流动。承接分流的是 messaging 包里的 handler.go，全项目最大的单个文件：类型判断、去重、命令解析、媒体路由都在它身上；分流的第一站是去重——网络重连与重试随时可能带来重复投递，同一条命令执行两遍，在指挥部里等于事故，所以每条消息先验明正身，见过的直接跳过；命令本身由 commands.go 的注册表统一管理，约二十个斜杠命令像值更表一样排开，谁来接哪类消息，表上一查便知。而全项目并发设计最密集的地方，是 ACP 客户端——acp_agent.go，一千七百多行，一个教科书级的异步 JSON-RPC 客户端。它的核心构造只有三样东西，我原样画在下面。

```go
nextID  atomic.Int64                 // 请求 ID：原子递增
pending map[int64]chan *rpcResponse  // 飞行中的请求：ID → 应答管道
readLoop()                           // 单读协程：stdout 的唯一读者
```

工作机制值得慢慢讲。每次调用，分配一个原子递增的唯一 ID，造一条带缓冲的管道挂进 pending 表，请求写进 stdin，然后阻塞在管道上等应答。子进程那一侧，readLoop 协程是 stdout 的唯一读者，逐行扫描进来的 JSON：带 ID 无 method 的是响应，按 ID 从 pending 表里取出对应管道把应答塞回去；有 method 无 ID 的是通知，按会话 ID 分发到对应的通知管道。调用返回前，defer 把自己的 ID 从 pending 表里删掉，不留僵尸。这个结构妙在哪？妙在它把“多路复用”这件事拆成了两个各司其职的半边：发送侧并发无上限，谁想发谁就发；接收侧单线程串行，永远只有一个读者在碰 stdout。读协程不需要锁，因为它垄断了读取；pending 表只需要一把轻量互斥锁，因为它的临界区只有插表和取表两行。教科书这两个字，我是当得起这个设计的——它不是我发明的模式，但我把它写到了每一行注释里，这个文件的头部注释块，把每个字段的保护者写得清清楚楚：谁归 mu 管，谁归 pendingMu 管，谁归 notifyMu 管。

会话这一层还有第二重并发防线。同一个微信会话的两条消息如果同时打进去，两个 prompt 会互相覆盖对方的通知管道——这类问题不常出，一出就是灵异现象。解法是按会话分发：每个会话 ID 一把锁，同一会话的调用被串行化，不同会话之间完全并行。你和我聊天的顺序感被完整保留，别人和它的并发互不牵连。进度回调则全部非阻塞——一律用 go callback() 把通知甩给独立协程，绝不 let 一个慢通知拖住主流程；全项目所有 goroutine 的创建都收口在 safe.Go 一个口子上，panic 不会炸穿进程。业务层的并发编排也有一处代表作：/workflow，一套只有几个关键词的迷你 DSL——step 定步骤，parallel 开并行分支，branch 走条件，@N 引用前面步骤的产物，最多十步、五个并行分支；引擎带着 panic recover 兜底，某一步炸了，后面照常收尾，错误回到微信里是一句读得懂的人话。编排是并发的高层形态：单条消息内部的并发靠锁和管道，多条任务之间的并发靠编排。说到 ilink 的网络侧，还有一层时间上的韧性：长轮询一次挂起最多三十五秒，有消息立刻返回，没消息就空手而归再来一轮；轮询失败走指数退避，第一次三秒，翻倍到六秒、十二秒、二十四秒，六十秒封顶；会话过期是另一种病，不开退避，清掉同步缓冲，固定五秒后重连。

媒体链路则是这一章最硬核的一段加密闭环。微信的图片和文件不走消息通道，走独立的 CDN，且用 AES-128-ECB 加 PKCS7 填充加密。发一张图，weclaw 要自己生成两个十六字节的随机数——文件标识 filekey 和密钥 aeskey——把 aeskey 的十六进制通过接口报给微信服务器，再用它把图片加密成密文传上 CDN；密钥再转成 base64 嵌进消息体，收方拿到消息才能解密。下载是这条链的逆行。收发双向都要做对，闭环才算闭上。协议层还要伺候登录这件最麻烦的事：二维码登录是一台小型状态机——等待扫码、扫码待确认、需要验证码、验证码被拦、已绑定待跳转，每种状态都有一条去路；登录响应里若指了新的数据中心主机，轮询就连人带铺盖搬过去。这些琐碎没有一处能省，省一处，桥上就多一处会在凌晨断行的裂缝。这一整套协议层代码只有一千七百余行，却是整个指挥部的地基：地基越沉默，楼上越敢盖。但地基和楼都有了，还差一样东西——什么时候敢让 AI 碰我真实的生活？答案是：先把安全做成默认值。

## 6.4 默认安全是底线

weclaw 是所有作品里处境最特殊的一个：它站在我的私人聊天通道和多家具商的云之间。左边是我生活里最私密的消息流，右边是 Claude、Gemini、Kimi 这些需要把内容送到别人服务器上的服务。中间这座桥，只要偷懒一次，我的密钥就出海了。所以这一节讲本章第二根支柱：默认安全。它的意思是，安全不能靠用户记得，不能靠文档提醒，不能靠事后补救——它必须是代码里的默认路径，用户什么都没做，安全就已经发生了。

具体到 weclaw，这道闸门叫 sanitizer，一个独立的小包。它的包注释是我写给自己的军令状：“防止用户消息中意外包含的 API Key、数据库密码、私钥等敏感凭据泄露给第三方 AI 提供商。”注意“意外”两个字——要防的不是黑客，是我自己。设想真实场景：我在微信里对 Claude 说，帮我调试这个脚本，报错如下，顺手把日志整段贴过去——日志里经常带着环境变量，带着数据库连接串。人不会每次都检查，所以检查必须是机器的事。sanitizer 用一组正则在消息出海前逐条匹配：sk- 开头的 OpenAI 密钥、sk-ant- 开头的 Anthropic 密钥、AIza 开头的谷歌密钥、ghp_ 开头的 GitHub 令牌、AKIA 开头的 AWS 密钥、三段式的 JWT、私钥块、数据库 DSN——命中的替换成形如 [REDACTED] 的占位符，保留上下文可读性，让 Agent 知道“这里曾有一个密钥”而不必知道密钥本身。规则表还有一条顺序纪律：具体规则先于通用规则执行，sk-ant- 必须抢在 sk- 前面，否则通用规则会先把 Anthropic 密钥截断，留下一个半吊子占位符。而对私钥和连接串这类最危险的内容，替换都不够，直接阻断发送并告知用户——脱敏负责常规，阻断负责底线。这套实现的设计参考自 poeapi_go 的同名包。军团内部就是这样传承的：一个兵验证过的战法，写成操典，别的兵照着练，再按自己的阵地改。同一个思想在 TypeScript 里是 redact.ts，在 Go 里是两个 sanitizer——三种实现，一副骨相。

第二处默认安全，在权限请求上，它同时是“人在环上”的一次诚实变奏。ACP 协议里，Agent 每次调用工具前要向客户端请求许可：桌面 IDE 里这体现为一个弹窗，等你点“允许”。可微信场景里没有弹窗可以等——你不回复，Agent 就永远停在那。weclaw 的选择是自动代答 allow：所有权限请求直接放行，流程不中断。但放行不等于沉默，它在代答的同时，把工具名提取出来，变成一条进度通知推回微信：“⏳ 正在调用工具：read_file”。一代一告之间，是两个原则的交换：我让渡了逐次审批权，换回的是全程知情权。这不是对人在环上的背叛，而是它在聊天场景里的正确形态——人在环上，环的是眼睛。我把这个权衡如实写进了文档，也把风险如实写进了 README：那些跳过权限检查的危险开关只属于 CLI 模式的显式配置，ACP 模式不需要它们，而每一处警告旁边都写着同一句话——只有理解风险的人才应该打开它。

第三处默认安全，长在依赖清单上。weclaw 的 go.mod 里，直接依赖只有五个：cobra 管命令行，qrterminal 出二维码，uuid 生成标识，x/net 补网络工具，cron 管定时。对一个三万八千行、功能多到需要二十篇文档来解释的系统，这个数字近乎洁癖。它换来三样东西：单二进制交付，CGO_ENABLED=0 一条命令交叉编译出六大平台的可执行文件，指挥部搬家就是把一个文件拷过去；供应链攻击面极小，我需要审计的第三方代码一晚上能读完；构建可复现，今天编出来的二进制和明年编出来的行为一致。HTTP API 默认只监听本机回环地址，管理后台走 Basic Auth，网关的 Bearer Token 与后台密码双轨隔离，网关限流压到每分钟六次的保守值——这个数字不是拍脑袋，是对“个人使用不该惊动平台”的敬畏：指挥部要的是长年稳定，不是一时吞吐。还有几处不显眼的保守：登录凭据与全部数据落在 ~/.weclaw 一个目录里，备份就是拷目录，迁移就是搬目录；systemd 与 launchd 的服务文件随仓库附上，开机自启、崩溃拉起，指挥部常年有人值守；一条 update 命令自更新，更新完自动重启。连登录这个动作都是本地的：二维码由 qrterminal 直接画在终端里，我扫我的，全程没有第三方登录页插在中间。sanitizer 的规则表也不是写死的——它由读写锁保护，运行时可以追加自定义规则，今天发现一种新的密钥格式，明天加一条正则就补上了。每一道门都默认关着，钥匙默认在我手里。默认安全做到深处，会变成一种审美：一个系统最让人安心的地方，不是它声明了哪些安全特性，而是你翻开它的默认值，发现每一条路都天然通向谨慎。安全是底线，但光有底线只是一个可靠的黑铁工具；指挥部真正的门槛在于，它得让我用起来像一个活人在替我值守。

## 6.5 “跑得像个人”：体验完整性

深度分析报告里有一句话，是这一节的题眼，也是我对 weclaw 全部交互细节的要求：“每一个环节都不只是‘能跑’，而是‘跑得像个人’。”能跑和像人之间，隔着一条体验的鸿沟，这一节就讲我是怎么把这条鸿沟一寸一寸填掉的。第一件事是等待。Agent 干一个活要几十秒到几分钟，微信这头是一片死寂，死寂比失败更煎熬——人不知道它是死了还是在干。所以进度通知是刚需：收到消息先回一句“⏳ 正在处理”，Agent 每调用一个工具、每推进一步，微信里就多一条实时的状态；连微信自带的“正在输入”状态我都做了——先取 typing_ticket 凭证，再发输入指示，让对方那个对话框顶端浮起熟悉的提示。第二件事是回话的形状。Agent 说的是 Markdown，微信认的是纯文本，于是代码围栏剥掉、链接只留显示文字、粗斜体标记抹平；超长回复不能一坨砸过去，按 rune 边界切出三千五百字的片，一片一片连发，片与片之间隔六秒——既不刷屏失控，也不让对方等太久。切片按 rune 而不是字节，是中文世界的常识：按字节切，一刀下去就是半个汉字。还有去重、失败兜底回复、语音消息的事件去重——这些不体面的杂活，全在暗处做完了。做到最后你会发现，体验完整性不是某个功能，是一种立场：把对面那个人当成会焦虑、会等、会注意细节的人。

媒体这一侧的完整性做得更细。发一张照片进来，视觉路由先挑一个最能看图的 Agent——Gemini 优先——生成一段描述，再把描述喂回默认 Agent 的对话，还不污染看图 Agent 自己的记忆；看图失败，自动转移到下一个可用的。随后说一句“画一张类似的”，五分钟内的图片上下文会被缓存接住，生图 Agent 拿着原图开画，成品直接以图片消息回到对话里；生图的意图词表是热加载的，“重画”“换个风格”这类说法随时可以增补，不用改代码。发进来一份 PDF、Word、Excel 或 PPT，自动解析成文本注入上下文——指挥部收到的每一种原料都能入炉。指挥部甚至还有日历感：/todo 记待办，/timer 设倒计时，/cron 用自然语言定定时任务，“每天九点提醒我”就这么一句话，任务落成 JSON 文件，重启不丢；到点的时候，微信先响。伺候好一条消息只是本分，让号令在未来的某个时刻准时出发，才算把指挥部开成了气候。

第二件讲部署的完整性。管理后台是一个单文件前端——三十五万字节出头——通过 go:embed 直接编进二进制。运维 weclaw 的机器不需要装 Node，不需要托管静态文件，不需要配反向代理，weclaw 起来，浏览器一开，后台就在。这和上一节的单二进制是同一枚硬币的两面：依赖少，则产物简单；产物简单，则体验完整。前端长在二进制里，还顺带解决了版本问题——后台和内核永远同版本，不存在“前端旧了没跟上”的错位。仓库里这个前端也只有一份 HTML，没有构建流水线，没有依赖树，改一处、刷新即生效——它在这个项目里的体量，恰好是它该有的体量。

第三件事是我最愿意拿出来讲的维护纪律：贴身跟随上游协议。weclaw 对接的微信协议源自 openclaw-weixin 的 2.0.1 版本，而上游在演进，最新到了 2.3.1。README 里有一张逐项对照的表，我逐条做完了适配：CDN 下载支持 full_url 直链优先、回退加密参数拼接；新增的 notifystart 和 notifystop 端点，客户端启停通知；二维码登录新增的四五种状态，包括扫码后跳转、需要验证码、验证码被拦、已绑定跳转；IDC 路由跳转——登录响应里给了新的主机地址，轮询就动态切过去。改动落在哪些文件，表里也写得明白。个人项目里，这种对上游协议的贴身跟随是罕见的纪律，它不性感，没有一次提交会让代码变酷，但它决定了这座桥明年还在不在。与之配套的是另一种纪律：知道不做什么。语音消息的识别，微信服务端早就把 ASR 做完了，转写文本就在消息里，weclaw 直接取用，一行识别代码没写。深度分析报告评这一笔的话我完全认同：“知道‘不做什么’，比知道‘做什么’更显功力。”

体验完整性的最后一站，是出口。消息进来只是半个闭环，消息出去才见真章。weclaw 里的 /publish 命令，把最近一条回复直接推给 knowly 的发布接口，分发到博客和知识库；/podcast 命令直通第七章要讲的广山之巅播客工厂，在微信里发一个话题，生产线就开动。发一个 URL 进来，剪藏链路抓正文、转 Markdown：微信公众号文章伪装浏览器头直抓，外站走 Jina Reader 并带降级路径，落成带元数据头的文件和配套的边车文件——“微信即收件箱”不是一句口号，是一整套入库流程；入库之后还能触发自动分析，“保存→分析→推送”一气呵成。当天聊了什么，一条 /log 命令按日翻账；问答对全部沉淀成 JSONL，每一场对话都可回放。聊天窗口由此不再是聊天室，而是一个总机：进来的是号令和原料，出去的是成稿、成片、成库。深度分析报告里那句总结，我抄在下面作为本节的路标：“微信不再是终点，而是消息的起点。”

写到收束处，允许我用一段排比把这一章说完整——这是本章唯一一次宣言。指挥部是从人的处境里长出来的：人在哪里，指挥部就在哪里，所以我把它建在了微信里。边界是从克制里长出来的：一个接口七个方法，三个世界各安其位，没有 Agent 被排除在外。秩序是从演化里长出来的：四层结构不是画出来的图纸，是长出来的年轮。安全感是从默认值里长出来的：脱敏先于外发，代答必伴随知情，五个依赖各自清白。体验是从对人的敬意里长出来的：进度有人报，等待有回音，连“正在输入”都替我演好。三万八千行 Go、一百一十八个文件、六百一十三次提交，这个数字规模在大厂里是一个小组两周的活，在我这里是一条微信消息背后全部的沉默工程。weclaw 是军团的指挥部：号令由此出，战报由此汇，第七章的播客工厂，也从这里的一条 /podcast 命令里接到了它的第一张订单。用技术的确定性对抗世界的不确定性——这句话到了微信这一章，有了最日常的形态：世界在聊天窗口里涌进来，而确定性，从窗口里的一条斜杠命令开始。

---

## 第7章 服务生命节律：广山之巅，一天四档的全自动播客

# 第 7 章 服务生命节律：广山之巅，一天四档的全自动播客

五根支柱里，前三根都是硬的：人在环上，防的是 AI 失控；对抗熵增，防的是记忆流失；自己开一条路，破的是外部封锁。写到第五根，笔锋忽然软了下来。服务生命节律，防御的对象换了一种——它防御的不是某个具体的敌人，而是一种更普遍的错位：世界的信息以不适合人的形态存在，而人被迫日夜迁就信息。podcast 项目，栏目名叫广山之巅，是军团里唯一一个每天固定时间开口说话的兵：早上八点半、中午十二点、下午五点、晚上八点半，四档节目，像一家一人媒体机构的播出时刻表。说它是最温柔的一根支柱，并不因为技术含量低——它的代码约一万四千行，提交超过两千次——而是因为它全部的工程决策，最终都要回答同一个问题：人此刻在哪里，状态如何，适合接收什么形态的信息。这一章，我从一份宣言讲起。它先于生产线存在，是这条生产线的意识形态说明书。

## 7.1 让已经存在的信息，重新适配人的生命节律

宣言不是先有产品再补的广告词，它是用着用着，把使用说明书写成了哲学。autopodcast 是 podcast 的兄弟项目，一个 Chrome 扩展：选中网页文字，右键，一键转成播客。功能三秒钟就能说完，但我在它的 README 开头，郑重其事地写了一份《AutoPodcast 使用宣言》，第一节只有两行判断——

“世界的信息形态，并不天然适合被人类吸收。文字要求专注、静止和时间；而人的生活，充满移动、重复和碎片。”

这两行是这个项目的全部公理。请注意它的结构：它没有把问题定义成“人不够自律，应该腾出整块时间来阅读”，而是反了过来，定义成“信息没有适配人”。这两种定义的差别，是两条产品路线的差别。前者的解法是自我惩罚，是收藏夹里越积越高的欠账，是每一次没读完时那声轻轻的叹气；后者的解法是工程，是转换——既然文字要求专注、静止和时间，而这三样恰恰是现代人最稀缺的东西，那就把信息转换成不需要这三样也能吸收的形态。宣言里我给 autopodcast 的定位只有一句话：“信息形态转换工具”。它不是内容制造工具，宣言原文写得清楚，它的存在“不是为了制造内容，而是为了让已经存在的信息，重新适配人的生命节律”。已经存在，这四个字是关键：它不生产观点，不搬运热点，不往这个已经过载的世界里再添一页纸，它只改变形态。

这个判断不是书斋里的推演，它来自一份我自己填写的痛点清单。宣言的推广章节里，我把现代人共同的绝望写成了四条，第一条就是收藏夹焦虑：“收藏从未停止，学习从未开始”。Read It Later 变成了 Read It Never——稍后阅读，永远不会阅读。第二条是屏幕疲劳：每天看屏幕超过十小时，眼睛已经生理性拒绝长文，大量深度好文就这样被浪费。第三条是垃圾时间无法利用：通勤、洗碗、健身的时候，大脑想思考，手却拿不稳手机。第四条最扎心：硬啃长文容易走神，而“听故事、听对谈”，是人类数万年来最本能的知识接收方式——文字的历史不过几千年，印刷品的普及不到两百年，用眼睛贪婪地扫描符号是一种后天训练出来的技能，用耳朵听人说话却是刻在基因里的。收藏夹焦虑的病根，不是懒，是形态错配：那些文章以眼睛的形态存在，而你此刻只有耳朵。所以工具要做的只有一件事，宣言第二节写得斩钉截铁：把你已经主动选择的信息，转化为更适合用“听”来吸收的形式。

转换有两种深度。想快速了解全文，在通勤时“听完文章”，不引入额外观点，就用单人播客直读——宣言称之为“最低损耗的转换”，不解释、不改写、不多说一句。觉得文字有点硬、结构不够清晰、想听别人“聊一聊这个问题”，就用多人播客：AI 把原文转成对话剧本，四名主播各司其职——晓琳负责开场、提问和收尾，戏份约两成；博文负责核心判断与金句，约四成；莉薇负责现实例子与追问，约两成；云萱负责结构性总结，约两成。这是个设计过的群像，不是随机分配：先有人替你把问题问出来，再有人把判断给出来，然后有人把它拽回地面，最后有人替你收拢行囊。

这里要替“转换”这个动作做一次辩护。市面上不缺帮人省时间的东西：摘要工具把两万字压成两百字，信息做了减法；热搜榜把一整天裁成三十条，语境做了减法。减法有减法的用处，但它悄悄偷换了一个前提——替你决定哪些内容值得保留。autopodcast 走的是另一条路，它做的是形态的等价变换：直读模式一个字都不删，原文有多少信息量，耳机里就有多少信息量；多人模式虽然引入了转述，但宣言预先声明了它的身份——另一种理解路径，而非权威结论。减法牺牲信息换时间，转换不牺牲信息，只借用原本会被浪费的时间。这两种设计的分野，放在收藏夹焦虑的语境里尤其要紧：焦虑的根源之一，就是总有什么东西在暗示你“来不及了”；而一个不删你一个字的工具，至少不会加重这种暗示。

但宣言在这里立刻踩了一脚刹车，原话是：“它不是权威，也不是结论。它只是另一种理解路径。”转换可以加深理解，也可以伪装成理解，分界线必须由使用者自己守。

于是整份宣言最硬的一条原则登场了。autopodcast 不帮你发现内容：你必须先阅读、先选择、先确认“这段内容值得我花时间”，它才介入。宣言里有一句冷冰冰的话：“没有被你选中的信息，不值得被转换。”这一条，是我与推荐算法的彻底决裂。推荐系统的全部生意，是替你选择——它判断信息是否重要，它决定你该看什么，它承诺让你“不错过任何大事”；而我的工具在这三处全部弃权。宣言第二节的意思，值得用宣言自己的口气再排比一遍：它不制造内容。它不判断信息是否重要。它不替你决定该不该学习。它也不承诺让你变得更聪明。它只负责把你已经选择的内容，变成你最容易吸收的形态。选择权，始终在你手里。

替你选择和帮你转换，看起来只差一步，实际上是主客易位。宣言第五节专门写了“关于效率，我们的立场”：不以效率为信仰，不帮你学得更快、听得更多、塞满你的时间；它只关心一件事——“让原本会被浪费的时间，承载适合它的认知输入”。被转换的信息越多，理解并不会线性增长；如果你在用它逃避思考，如果你把“听过”当成“学会”，宣言直言这些场景不适合它，甚至写着：“如果你需要停下来思考，请关掉它。”一个工具在说明书里教用户关掉自己，这不多见。第六节把人与系统的关系写成了一句我愿意贴在所有作品墙上的话：“一个好的系统，应该随着人的变化而退后一步。”你可以改变用法、调整频率、甚至停止使用，而不是反过来。宣言的最后一节只有一句话的份量：AutoPodcast 只是一座桥，走不走、走多远，始终由你决定。这份宣言写于生产线开工之前，它定下的不是功能清单，是这条生产线此后每一次决策的表决权归属——机器负责形态，人负责选择。宣言的第七节，还有一份承诺清单：不制造信息焦虑，不鼓励无意识消费，不把“转化”伪装成“理解”。三句话都短，但每一句都是拿刀对着自己：第一条约束的是推送的冲动——工厂每天四档，档档准时，最容易养成的就是“不听就落后”的焦虑，宣言先把这条路堵死：跟不上，就明天再听；第二条约束的是增长的冲动——曲库可以扩，档位可以加，但多一档未必多一分理解；第三条约束的是宣传的冲动——它是一座桥，不是对岸。给工具立这样的规矩，看起来是在捆自己的手脚，其实是把产品从“越用越多”的轨道上，拉回到“够用即止”的轨道上。

## 7.2 一条生产线：抓行情→写稿→TTS→BGM→分发

宣言解决了“为什么”，生产线回答“怎么做”。缘起在一个最朴素的需求里：我自建了一个行情站 fm.want.biz，有天我把它的页面文本直接贴进了 finance/todo.md，只写了一句要求——自动生成财经早餐。git 仓库的第一条提交就叫“自动生成播客”，时间是 2026-02-04。从那天起，这个项目只做一件事：让一个人拥有的信息渠道，像一家媒体机构那样按时开张。媒体机构和个人的区别，从来不在内容质量，而在开张的确定性——机构可以按时出版，人不行，人会累，会忙，会忘。所以这条生产线的设计目标从第一天起就不是“能生成”，而是“到点必出”，节假日从不缺席。

先看整条流水线的骨架，它就印在仓库的数据流文档里：

```text
fm.want.biz 行情/新闻 API → finance/fetcher.py → LLM(poe.want.biz)写稿
  → financial_breakfast_{slot}_{date}.md(四时段模板)
  → Go podcastd fsnotify 监听(NAS + 本地)
  → md2podcast.sh:文件名消毒/JSON防抖/SMB竞态重试 → 文本清洗
  → TTS(edge-tts 晓晓 或 macOS say)→ podcast_mixer.py 多角色合成+BGM 情绪匹配
  → polisher.py 注入封面/歌词(USLT 剧本)→ generate_rss.py
  → sync.sh:rclone→R2、ditto→iCloud、git push RSS、企业微信图文卡片+桌面通知兜底
  → processed/ 编号归档;cleanup.sh 清临时,archive_old_files.sh 移旧档至 NAS
```

拆开来讲。抓行情：fetcher.py 定时从 fm.want.biz 拉取行情与新闻，这是原材料进厂。写稿：把材料交给私有网关上的大模型，按四时段模板成稿——financial_breakfast_{slot}_{date}.md，文件名里带着时段和日期，一天的四个班次各有各的版式。监听：Go 守护进程 podcastd 用 fsnotify 同时盯着本地目录和 NAS 队列，稿子一落地就开工，人不按任何按钮。加工：md2podcast.sh 接手，先给文件名消毒，再做 JSON 防抖，再做 SMB 竞态重试——这三道防御下一节细讲——然后把清洗后的文本交给 TTS。配音环节留了两条腿：默认是 edge-tts 的晓晓音色，微软引擎，声音自然；另一条腿是 macOS say，系统自带，离线可用。引擎由环境变量 PODCAST_TTS_ENGINE 决定，默认 microsoft，本地引擎随时可以顶上——这不是为了仪式感的冗余，是每条关键路径都备另一条腿的老规矩。合成：podcast_mixer.py 做多角色合成与 BGM 情绪匹配，这是全厂最大的车间，一个文件一千四百四十二行。精修：polisher.py 把封面和歌词注入成品——歌词用的是 USLT 帧，整份对话剧本随着 mp3 一起走。分发：generate_rss.py 生成订阅源，sync.sh 一次落地四路出货。

这套东西的运行节奏，配得上一个更响亮的名字——内容工厂：每天四档，8:30 早餐、12:00 午间、17:00 盘后、20:30 晚间。调度不靠人记得，靠 launchd 的四个 plist 加 cron 的四个时段。系统必须自己活着，所以 Go 版的注释里写着 KeepAlive“接管进程保活”“崩溃后自动重启”——进程死了，launchd 把它扶起来，人不需要知道。生产线上跑的词汇也全是工厂腔，日志原样抄几行在这里：

```text
🚀 [广山之巅] 生产线于 $(date) 启动...
📦 正在归档任务文件
🔄 更新RSS
🎉 本期播客制作完成
```

生产隐喻的日常化，是我有意为之：在这些日志面前，我是厂长，不是主播。厂长只对产能和良品率负责，不出镜，不配音，甚至不值班。这套姿态写进代码里的证据，是规模盘点时那组数字：约 76 个源文件、约 1.4 万行——Bash 编排、Python 音频引擎、Go 守护进程三军种混编；除 mixer 之外，data_api.py 一个文件两千五百二十三行，md2podcast.sh 三百六十五行，podcastd 七个包约一千九百行；git 历史两千三百八十二次提交。产出端，processed/ 目录里归档着九百七十九份原始稿，成品编号已经排到 1096 号——累计产出超过一千期。一期不落的代价，是这些数字背后的每一天。

数字最能说明这是一座工厂，而不是一个脚本。finance/output 里常年躺着九百七十三个中间产物——四百零四个 JSON、一百八十八份 Markdown、三百八十三个纯文本。它们是什么？是原料、在制品和半成品：刚抓回来的行情是矿石，写好待播的稿子是零件，合成到一半的音轨是装了一半的配件。真正流过这条流水线的物料，远比最终出货的一千多期成品多得多。车间里还常备着另一类库存：一百张封面待命，三十三首 BGM 各就各位，随时听候情绪引擎的调遣。工厂和脚本的区别就在这里——脚本用完即走，工厂有原料库、有在制品、有成品库、有备料区，而管着这些库存的，是一套目录即数据库的朴素制度，它的道理留给 7.5 细说。

成品一次落地，四路出货。sync.sh 的文件头是它的自报家门：“广山之巅播客：全能同步与高级通知脚本，功能：R2同步 + iCloud同步 + GitHub推送 + 企业微信模板卡片”。rclone 把 mp3 推上 R2，ditto 同步进 iCloud，git push 更新 RSS，企业微信收到一张图文卡片——一条生产线，四个出口，这正是方法论里“一次采集、多渠道分发”的现场版本。出货之后是归档：任务文件进 processed/ 编号存档，cleanup.sh 清扫临时目录，archive_old_files.sh 把旧档搬上 NAS。就连删除这件事都写得克制，cleanup.sh 的注释里专门注明：data 为纯临时目录，按前缀匹配，避免误删正式资源。一座工厂的体面，一半在生产，一半在收拾。

## 7.3 情绪工程：BGM 的 arousal/valence 打标

朗读器和播音台的区别在哪里？前者把文字匀速念出来，后者知道哪里该停顿、哪里该压低声音、哪里配乐该扬起来。多角色对话解决了“像人在说话”，但还不够——真人节目里，情绪是有结构的：开盘平缓，中段起伏，高潮处音乐先行，结尾缓缓收住。为了让这座工厂出品的不是“带背景音的朗读”而是“节目”，我在音频引擎里做了一层情绪工程。它的地基是一个配置文件，config/bgm_config.json，曲库里每一首 BGM 都被打了标。曲库共三十三首，随便抽两首看看格式：

```json
{
  "filename": "昂扬.mp3",
  "emotion": ["excited", "enthusiastic", "thrilled", "delighted"],
  "arousal": "high",
  "valence": "positive",
  "topics": ["sports", "workout", "motivational"],
  "description": "高昂激昂的音乐，适合兴奋、激动的情绪和体育、运动、激励类话题"
}
```

```json
{
  "filename": "沉重.mp3",
  "emotion": ["angry", "serious", "grim", "intense"],
  "arousal": "high",
  "valence": "negative",
  "topics": ["dramatic", "news", "politics", "crisis"]
}
```

arousal 和 valence 这两个词，借自情绪心理学里最常用的一对正交轴：唤醒度管能量的高低，效价管愉悦与否。两轴交叉，情绪空间被切成四个象限——高唤醒加正效价，是昂扬、兴奋、跃跃欲试；高唤醒加负效价，是紧张、沉重、如临大敌；低唤醒加正效价，是舒缓、安心；低唤醒加负效价，是低落、压抑。“给财经播客挑配乐”这件事，看起来是玄学，靠品味，凭手感，但我把它变成了一张二维坐标查表：稿件的情绪落点先被判断出来，引擎拿着坐标去曲库里找最近的邻居，再校验一遍话题标签是否吻合，配乐就定下来了。玄学的部分在打标阶段就消耗完了——三十三首曲子，每一首我都坐下来听过、定过坐标，这是全厂最需要人坐下来亲手做的工序之一，也是人在环上在音频车间的形态。

情绪工程的第二层，藏在 narrative_audio_engine.py 里，它管的不只是配乐，是节奏。每条台词在时间线上都有一个重要度标注，默认零点五；引擎按重要度动态调整停顿时长和语速——重要的话，说完多留一拍，语速放下来，给听众留出消化的时间；过场的词，语速收紧，不多占用注意力。一篇文章的重音，朗诵者靠理解力判断，引擎靠剧本里显式写出的重要度——这是把“播音员的经验”翻译成了参数。第三层是高潮检测：函数 detect_climax 的算法朴素到可爱，把每条台词的情绪强度定义为效价的绝对值加上唤醒度，整条时间线上取最大值——那个点就是本期节目的情绪顶点，配乐、停顿、语速都在这里对齐。一篇文章的高潮在哪里，不该由运气决定，该被计算出来。

还有一道容易被忽略的工序：平滑。引擎从 podcast_mixer 里引入了 smooth_emotion_vectors——相邻台词的情绪向量不做硬切换，做插值。道理一想就通：上一句还是昂扬的涨潮，下一句忽然跌进沉重的低谷，配乐跟着情绪表硬切，听感就是抽风；宁可让情绪多过渡一拍，也不能让音乐在两个象限之间跳来跳去。这里的取舍和整个军团一脉相承：打分可以离散，呈现必须连续。听感是连续的，任何按帧决策的模块，最后都要有人负责把离散的决策抹成连续的体验——平滑函数干的就是这个不起眼但缺不得的活。

最后一层最有意思，叫导演模式。apply_director_mode 函数接收一条时间线和一个预设名，预设表 DIRECTOR_PRESETS 里默认的一档叫 storytelling——讲故事。每个预设存的是一条情绪弧线：开盘用什么基调、中段如何推进、尾段怎样收束，弧线写死了，引擎按台词序号把弧线铺到整条时间线上。这一步的本质，是承认一段音频有起承转合，而不只是若干句子的物理拼接。配上自动情绪标注的启发式——问句判作好奇，感叹判作兴奋，遇到“但是”“然而”就切到分析腔——以及四名主播按比例分工的戏份表，一条时间线在合成之前，已经被当作一场戏导过了。最后 polisher.py 把整份剧本写进 mp3 的 USLT 歌词帧：听到哪一句，歌词就停在哪一句，节目单随声音一起走。情绪工程的目的从来不是给机器加装感情，是把“人怎么听”这件事提前写进参数——三十三首曲子的坐标表、每条台词的停顿长度、那条被计算出来的高潮点，都是我在替未来的听众预先回答同一个问题：此刻，这里该是什么感觉。

## 7.4 层层防御与层层兜底

一座无人值守的工厂，最怕的不是某个环节会失败，而是设计者假设它不会失败。单人军团没有 SRE 值班表，凌晨三点生产线卡住的时候，唯一在场的只有代码自己，所以这座工厂的每一条关键路径旁边，都埋着第二套方案。这一节讲它的防御工事，先从一条注释讲起。md2podcast.sh 的第六十二行附近，写着两行注释，原文如下：文件名中的换行、回车、制表符会导致嵌入 python3 -c 字符串时 SyntaxError；即使 Go 层已消毒，这里也做第二层防御。这段话值得逐字读一遍。文件名是外部输入——稿子可能来自 NAS 队列，来自浏览器扩展，来自手机上的备忘录，起名的人不可控；一个带控制字符的文件名，轻则让拼接出来的命令面目全非，重则整个班次停产。而注释的后半句才是重点：上游的 Go 守护进程 podcastd 已经消过一遍毒了，这一层照样再消一遍。不信任任何一层，哪怕是自己的另一层——防御的强度不取决于最强那一层，取决于你愿意织多少层。

第二道防御管的是“文件出现不等于文件完整”。NAS 队列里的剧本是 JSON，扩展把稿子发过来，文件系统里很快就能看到文件名，但内容还在传输的路上。半截 JSON 交给解析器，就是一个班次的废品。所以 md2podcast.sh 里有一段防抖逻辑：JSON 完整性校验最多做五次，不过就等一等再验，日志里同时写上了给未来那个一头雾水的自己的提示——这通常是因为文件还在从 NAS 传输中，请稍后自动重试。防抖的原理和刷新率无关，和谦逊有关：承认观察者看到的世界，永远滞后于真实世界。

第三道防御针对一种更隐蔽的病：SMB 写入竞态。文件经网络挂载盘写进来时，存在一个窗口期——目录项已经在了，内容还是空的。脚本的对策写在第二百一十五行附近的注释里：清洗格式加空文件防抖，NAS SMB 写入竞态，文件可能瞬间为空；若清洗后为空，很可能是 SMB 写入中，等待重试，最多三次。同一个竞态，在 podcastd 的 watcher 里也被处理过一次，用的是另一套姿势：注释里写着，网络挂载目录上 fsnotify 的创建调用会阻塞甚至永久挂住，因此先快速探测，一秒内无法创建就跳过 fsnotify，直接纯轮询。同一个敌人，两层防线，两套打法——一层靠重试容错，一层干脆换掉了传感器。这是层层兜底最有味道的地方：它不是同一招复制五遍，是每一层都针对上一层的失效模式重新设计。

防御的最后一环是人本身——得让人知道这一班出货了。通知系统做了三级降级：第一级，企业微信图文模板卡片，最好看，带封面和标题；发送失败，日志打出“切换到备用通知”，降级为第二级，构造纯文本消息再发一次；再失败，降到第三级，osascript 桌面通知，本机弹一条横幅；如果连 osascript 命令都不存在，日志里叹口气，跳过本地桌面通知——注意，是跳过通知，不是中断生产。通知是生产的附属品，任何一级通知的失败都不许砸掉主流程，这个优先级被写死在降级链的顺序里。

其实早在分发环节，兜底就已经开始了。成品一次落地、四路出货，表面上是分发的排场，实质上也是容灾的布阵：R2 那一路挂了，iCloud 里还有完整的一份；企业微信卡片没弹出来，RSS 那边照常更新；哪怕四路全哑，processed/ 里的归档和 NAS 上的旧档还在本地兜着。四路出口不是四个平行世界，是同一条生产线的四条退路。对一个人的媒体来说，可用性从来不是九十九点九九的漂亮数字，是“今天的节目我到底还能不能听到”——四个出口只要有一个通，答案就是能。

防御工事的最后一个部分，是“删除的纪律”。cleanup.sh 的注释注明 data 为纯临时目录，按前缀匹配避免误删正式资源——清扫工手里拿着的是精确制导的扫帚，不是推土机。archive_old_files.sh 的策略是本机永远轻装：归档保留最近的数据，旧档整体搬上 NAS，生产机不背历史包袱。还有一类防御写在注释里，叫注释即故障史。plist 里留着一行“不随系统启动（避免 launchd 无用户上下文时弹 NAS 授权窗）”——没有那行注释，下一个改配置的人会把 RunAtLoad 打开，然后某个重启的清晨，屏幕上会凭空弹出一个摸不着头脑的授权窗，而他不会知道为什么。go/start.sh 里留着“清除 Gatekeeper 隔离属性（go build 产物会被 macOS 打标签）”——没有那行，换一台机器部署，二进制会被系统拦下，排查半天才发现是隔离属性。这些注释的每一个字都对应一次真实的踩坑，写下来不是为了忏悔，是为了让防线比记忆活得久。

把整座防御工事摆在一起，能看到一套心照不宣的次序。最外层是让失败可见：日志里那一连串的表情符号不是卖萌，是把每次重试、每次降级都标成肉眼可扫读的事件；第二层是让失败可恢复：JSON 防抖等的是时间，SMB 重试赌的是竞态终会过去；第三层才是让失败无害：通知降级保的是主流程，清理按前缀匹配保的是正式资源。可见、可恢复、无害——三层各管一段，谁也不越权。很多系统的兜底之所以失灵，不是层数不够，是次序乱了：还没看清失败是什么，就急着恢复；还没恢复，就先想着把失败藏起来。这座工厂的防御没有一样用得上花哨的算法，全是笨功夫，但笨功夫的好处是，它不依赖任何人的临场发挥。

层层防御的共同姿势，一句话可以概括：假设上一层一定会失守，然后在下一层，再织一张网。兜底不是对自己代码的不敬，是对现实复杂度的敬意。

## 7.5 算力主权：全世界都在为你一个人播音

宣言的推广章节里，我写过四句口号。写的时候是营销腔，写完回头看，发现其中一句根本不是口号，是事实陈述——“全世界都在为你一个人播音”。这句话听起来狂妄，拆开看全是工程账单：为你查行情的，是 fm.want.biz，我自己的行情站；为你写稿的，是 poe.want.biz 背后我自己的模型网关；为你接单的，是 api.yuangs.cc，我的个人 API 中枢，autopodcast 扩展按下的那个右键，调用的就是它的 /api/publish 接口；为你存成品的，是我自己的 R2 桶；为你归档的，是我自己的 NAS；连通知你的，都是我自己的微信推送通道。宣传文案里那句话是这个项目的题眼：“你的知识，在你的服务器上，用你的 AI 逻辑进行重塑。”它的技术含义值得展开：转换逻辑不是平台黑盒——系统提示词是我写的，四名主播的戏份比例是我定的，剧本格式是我定的，情绪坐标表是我打的标。AI 在这条流水线上只是一台机床，而机床是我的，图纸是我的，厂房也是我的。市面上任何一个“帮你把文章转成播客”的云端服务，都做不到这四样全归你。

主权的另一面，是进料口也长在自己手里。autopodcast 扩展是一个入口，podcast 仓库里还躺着另一个不起眼的入口：scripts/autopodcast.js，一个 Taio 里的 AI 成文脚本，把手机上写的稿自动发往 NAS 队列——这是工厂的进料口。人在路上，想到一段什么，掏出手机写两行，发出去；到家的时候，那一期已经在耳机里等着了。进料、加工、出货、归档，全链条的每一环都架在私有基建上，没有一环需要向任何平台申请许可。这也是自己开一条路这条支柱在音频战线的延伸：wsstunnel 开的是网络的路，knowly 开的是存储的路，podcast 开的是声音的路——三条路的路基是同一套，api.yuangs.cc、want.biz、knasync，私有基建修到哪里，主权就到哪里。

这座工厂还有一个值得单独讲的品质：它的进化方式。翻开仓库根目录，auto_start.sh、cleanup.sh、archive_old_files.sh、md2podcast.sh、sync.sh，五个脚本整整齐齐，每个都只有五行，内容一模一样：兼容层，转发到新位置，exec 把参数原样递给 scripts/main 下的新实现。这是重构最谦逊的姿势——新结构接管一切，旧入口保留为别名，任何依赖老路径的调用者永远不知道身后发生了搬迁。再往深处走一步，是语言的换代：Go 守护进程 podcastd 接管了编排层，watcher 替代了 watch_podcast.sh 和 watch_nas_podcast.sh，scheduler 接管了调度，pipeline 负责串联——但它的注释里白纸黑字写着：保持现有 Python 音频引擎。cmd/podcastd/main.go 的原话是，它替代了原有的 Bash 编排脚本体系，保持现有 Python 音频引擎。换引擎，不换底盘；换代，不推翻。一千四百多行的 mixer 是全厂手感最熟的车间，它好好地留在原地，Go 只做它擅长的事——常驻、监听、防崩。podcastd 的 go.mod 里直接依赖只有两个：fsnotify 管监听，cron 管定时。进化式重构的原则在军团里处处可见，而 podcast 是它最完整的标本：两千三百八十二次提交，没有一次推倒重来。

无数据库，无后台，是这个工厂的另一个主权特征。目录即数据库：data 放临时件，processed 放已消化的任务，output_local 放成品，covers 放一百张封面，bgm 放三十三首配乐，logs 放日志——各司其职，文件名编号就是主键。finance/output 里躺着九百七十三个中间产物，四百零四个 JSON、一百八十八份 Markdown、三百八十三个文本，一季又一季的原料和半成品全在案。整个工厂对外的查询界面，是一个再朴素不过的 index.html，浏览器标签页上只有七个字：广山之巅 - 档案馆。工厂的终点是一座档案馆——一千多期节目，每期有编号，每号有其档，像一座只服务一个听众的广播电台的墓志铭与荣誉墙。数据主权从来不是一个姿态，它落成的样子就是这样：你的每一期节目，存在你的盘上，编在你的号里，不欠任何平台一个解释。

有人会问，都这个年头了，为什么不上一个真正的数据库？因为对一个人、一天四档的产能来说，目录就是最诚实的数据库：备份是一次 rsync，查询是一次 grep，迁移是拷一个文件夹，清库是删一个目录——没有连接池，没有迁移脚本，没有半夜的锁表告警。主键是文件名编号，外键是目录位置，写入的一致性由写入顺序保证，而崩溃恢复靠的就是上一节那套防抖与重试。规模到了非换不可的时候再换——这个判断和小即美说的是同一句话：不做“可能以后会用到”的设计，哪怕它是数据库。

这一章收束之前，把五根支柱在这座工厂里的样子并排放一放。服务生命节律是它的宪法：四档时刻表——8:30 早餐、12:00 午间、17:00 盘后、20:30 晚间——是生命节律的工程投影，早餐时知道隔夜发生了什么，午间补上上午的空档，盘后看清市场收在哪儿，睡前给这一天收个尾；信息绕着人的作息排班，而不是人追着信息奔跑。人在环上体现在选择权与打标工序：工具不替你决定听什么，三十三首曲子的坐标表由人亲手校准。层层兜底长在每条关键路径旁：消毒、防抖、重试、降级，层层设防。小即美写在目录即数据库里：文件名就是主键，一个直接依赖的 Go 守护进程就是全部编排。而这一切的地基是算力主权——没有自己的服务器、自己的 API、自己的存储，宣言里所有的承诺都会退化成愿望。军团的其他兵在这里各就其位：poeapi_go 供写稿的弹药，weclaw 的 /podcast 命令直通 knowly，knasync 在机器之间递送稿件——它们如何协同作战，第十章再细说。现在只需记住这一章的最后一句话：当你让已经存在的信息重新适配你的生命节律，当全世界都在为你一个人播音，你省下的不是时间，是注意力；而握在你手里的选择权，一个字节都没有少。这，就是用技术的确定性，对抗世界的不确定性，在每一天四个整点上的样子。

---

## 第8章 默认安全：数据主权与脱敏的横向战争

# 第 8 章 默认安全：数据主权与脱敏的横向战争

## 8.1 AI 只看到脱敏后的内容

在一个理想的世界上，安全应该是每个人深思熟虑之后的选择。但工程的世界从不理想：绝大多数用户永远不会打开设置页，绝大多数配置项终其一生都停留在默认值上。这推出一个朴素得近乎残酷的结论——默认值，才是产品真正的价值观。你可以把安全写成文档、讲成理念、印在首页，但如果它不是默认行为，它就只是装饰。功能可以是可选项，安全不行。可选项的含义是「有人会关掉它」，而安全的含义是「没有人被允许忘记它」。我把这条原则称为默认安全：安全不是被选择的结果，而是出厂即生效的世界观。它不是军团里某个项目的特长，而是横跨九个作品的建制，这一章记录的就是这套建制的横向战争。

战争一共五个战场，依次推进。内容——数据开口说话之前，先把秘密从句子里剔掉；存储——数据安家落户之时，先确认这块地是不是你的；密钥——数据换个身份四处通行的时候，看住每一把钥匙；边界——能力自身的围墙，让 AI 的手只能在白名单里活动；平台——寄生在别人领地上的礼貌与克制。四个项目各自守一段防线，拼在一起才是完整的纵深。

第一战场在内容，驻防部队是 poeapi_go。它是军团的水源：一个 OpenAI 兼容的 LLM 网关，54 个 Go 文件、6629 行，所有要喂给模型的上下文都从这里过境。过境是危险的。用户在对话框里贴什么，从来不经过审批——一段报错日志里可能躺着数据库连接串，一个配置片段里可能嵌着 API key，一句「帮我看下这段哪里错了」里可能就夹带了一整块 PEM 私钥。请求一旦发出，内容去了第三方的模型；日志一旦落盘，内容留在了我的磁盘上。两头都是泄露面：一头通向别人的服务器，一头通向未来某天被翻出来的旧日志。poeapi_go 的缘起只是把 OpenAI 协议翻译成 Poe 协议，一个再普通不过的代理；但代理的位置决定了它的责任——所有上下文都从你门前过，你就是海关。

海关的章程写在 docs/SECURITY.md 里，三条原则，顺序即世界观。第一条，「先阻断、再脱敏、再落盘」：高危内容直接 400 拒绝，普通敏感信息替换后写入日志。第二条，「先脱敏、再发模型」。第三条，双层脱敏兜底。这三条不是并列，而是一道递进的工序。先分辨严重程度：私钥、数据库连接串这类东西，替换是不够的，替换后的占位符也可能泄露结构信息，所以直接拒绝——请求不进入日志、不进入模型、不进入任何下游，这是 5 条高危阻断规则的职权范围，PEM 私钥、OpenSSH 私钥、数据库连接串、AWS Secret Key、Slack bot token 各占一条。拒绝也不是无声的：响应体里写明「sensitive credential detected, request blocked」，并附上命中的规则名——拦下你，也告诉你被哪条军规拦下。普通敏感信息，比如 API key、JWT、Bearer token，替换成脱敏占位符后放行。而无论前两步做得如何，最后一道工序永远再兜一次底。这套章程还有一个更短的版本，短到只有一句，也是本章的题眼——「AI 只看到脱敏后的内容。」

比原则更能暴露心思的，是时机。旧架构里，脱敏发生在日志记录之后——先写盘，再清洗。这个顺序的隐患在于：清洗失败或者被遗忘的那个瞬间，明文已经躺在磁盘上了。磁盘上的东西有惯性，它会备份、会同步、会在一年之后被忘了来历时再吓你一跳。新架构把脱敏提前到四个点：日志记录之前、路由给模型之前、回答落盘之前、Logger 写入之前。凡是数据即将离开进程边界的时刻，都先过一遍筛子。筛子本身是 internal/sanitizer 里的 23 条规则：sk- 开头的 OpenAI key、sk-ant- 开头的 Anthropic key、ghp_ 打头的 GitHub token、AKIA 开头的 AWS 密钥、AIza 开头的 Google key、三段式的 JWT、Bearer 头、postgres 与 mysql 与 redis 的连接串、URL 里内嵌的账号密码、password 字段、32 位以上的连续十六进制串。规则数量从 11 条长到 23 条——每一次翻旧日志时的心惊肉跳，最后都沉淀成一条新的正则。

有一份日志文件替我把这件事讲得很生动。它的文件名是「2026-06-09 22:52 my AWS key is AKIA_AWS_KEY_REDACTED_ and GitHub to.md」——那天用户贴进来一段自述，里面的 AWS key 和 GitHub token 被逐个替换成了脱敏占位符。我特意举这个例子，是因为文件名本身就是脱敏的产物：日志的元数据也在筛子的下游，一个字节都没漏。再往下一层是防御性双层脱敏：Handler 层在入口脱敏一次，Logger 层在写盘之前独立再脱一次。这两层不是分工，是冗余——哪怕某天有人在 Handler 里新增了一条路径而忘了调用筛子，Logger 层仍会替他兜底。用文档里的话说，这是不信任调用方，信任机制。人会累、会疏忽、会赶工期；机制不会，机制只认规则。

筛子要有质检。配套的自动化安全测试有 13 个，覆盖每一类规则的命中，也包括两个更重要的反例：正常文本不误杀，API key 只脱敏不阻断。筛子的失败有两种，漏放是事故，误杀同样是事故——一句「我昨天改了 sk- 开头的那个配置」里如果连正常语义都被吞掉，工具就废了。我同样把筛子的边界原原本本写进了文档注意事项：过滤是纯文本正则匹配，不是语义理解，base64 编码过的密钥、非连续的十六进制串，筛子认不出来；运行时动态追加的规则不持久化，重启即失效。把网眼在哪里写清楚，不是示弱，是让使用筛子的人知道网眼在哪里。此外还有两道总闸：LOG_ENABLED 环境变量可以让日志整体关闭；完整的错误堆栈连同请求体，禁止写入日志。还有一个细节让我相当满意：如果模型在回答里反射了脱敏后的占位符，Logger 不会做第二次替换——它已经是安全的，重复清洗只会把文本越洗越烂。兜底不是偏执，偏执是分不清哪些地方需要兜底。

筛子的覆盖面也值得一记。docs/SECURITY.md 里有一张入口对照表：对话补全、query、agent run、agent run stream，四个 API 入口，每一个都同时开着脱敏与阻断，Agent 的两个流式入口由中间件层兜住。SanitizeMessages 同时认得两种消息格式——纯文本，以及多模态数组；数组模式下逐个遍历 part，检查 text 与 text_content 字段。换个说法：只要内容有可能离开进程，无论它从哪扇门走、以什么形状打包，都过同一把筛子。制度最怕例外，例外一开，制度就成了布景；所以宁可让筛子笨一点，也不给筛子递放行条。

这套建制最有说服力的地方，在于它重复出现了。npm_yuangs 里有一个 redact.ts，poeapi_go 里有一个 internal/sanitizer，一个 TypeScript，一个 Go，两套代码互不知晓，却长出了同构的制度。第二章讲过它们的隔空握手，这里想说的是握手背后的含义：一个实践如果只出现一次，它是灵感；在两个互不通气的角落里各出现一次，它是哲学。军团里还有第三处——weclaw 的消息入口同样默认挂着脱敏。安全从来不是哪支部队的特长，而是建军原则；这场横向战争的第一枪由 poeapi_go 打响，枪声却传遍了整个军团。说到底，AI 是军团雇来的参谋，能力再强也是外籍的：它可以看战报，不能看家底。「AI 只看到脱敏后的内容」，就是这条禁令的代码形态。

## 8.2 数据主权：SSH 到自己的 NAS

第一战场解决的是数据开口说话的时刻，第二战场要回到更早的问题：数据安放在哪里。脱敏是流动中的安全，主权是静止中的安全。一份数据的一生要经过五站：被捕获、被传输、被存放、被检索、被销毁，每一站都有一个同样的问题——此刻它在谁手里？加密回答不了这个问题。加密是一把锁，主权是一张地契；锁再精密，如果门装在别人家的墙上，你锁住的只是自己的心安。不少云服务的安全叙事都在偷换这两个概念：它给你看锁，加密、审计、合规证书一应俱全；不给你看墙，数据物理上在哪、法律上归谁、服务商倒下那天它跟着去哪，一概不谈。

军团里与数据安放最相关的部队是 knowly——那台每 500 毫秒看一眼剪贴板的守夜人。剪贴板是数字生活里最低摩擦的信息入口，也因此是最不设防的地方：你在一天里复制的东西，可能有登录密码、收件地址、合同条款、体检报告的片段、客户的联系方式。这些内容如果被捕获之后原样送进某个云端剪贴板服务，等于把日记寄存给陌生人。knowly 的判断写在自己的文档里：每一次复制，都是用户对「这段信息有价值」的无意识投票——而投票的内容，理应留在投票者自己家里。

所以 knowly 把第一道关卡设在本地。一个叫 ShouldFilter 的纯函数守在捕获路径上，剪贴板与手机 Relay 两条通道共用同一把筛子，命中敏感词的内容在本地就被丢弃——手机的灵感经 Relay 中继汇入，同样先过这把筛子，游标拉取、防重复消费，连回流的家门都只从正门走。文档里的原话是：「敏感词在本地就被过滤，根本不会进入传输环节。这比『先上传再过滤』的云服务方案更安全。」注意这句话的重心不在「过滤」，在「本地」。过滤本身哪家服务都会做，差别在于过滤器站在哪一侧的国境线上。「先上传再过滤」的方案，等于在数据已经出境之后才宣布执法——哪怕下一秒就删除，那一段窗口期里，数据已经真实地离开了你。本地过滤的含义是：内容要么根本不产生出境的副本，要么以脱敏后的形态才谈得上离开。位置即立场，时序即价值观。

过了筛子的内容去哪里，knowly 的答案只有两个字：回家。它不依赖任何云存储，也不需要在 NAS 上安装服务端，直接用 golang.org/x/crypto/ssh 把内容写进我自己的 NAS，按日期归档成 Markdown。SSH 层的细节可以见微知著：连接校验采用 known_hosts 加 TOFU，首次连接记住对方的指纹，此后任何指纹变化都视为警报；远程命令的参数全部经过 POSIX 单引号转义，把拼接出来的注入挡在门外；并发会话被信号量限死在 3 个以内；探活用的是 OpenSSH 原生的 keepalive 请求，轻得几乎不存在。K.N.O.W.L.Y. 这个名字有五层解读，价值观那一层叫 Keep Nas As Sanctuary——让 NAS 成为知识圣殿，私有化、自主可控的存储哲学。措辞是宗教式的，逻辑是地产式的：保险柜可以买最好的，但先得确认柜子放在自己家。

回家这条路本身也不托付给中间人。knowly 的 go.mod 直接依赖只有 3 个：uuid、clipboard、crypto/ssh——传输用的是 SSH 这个诞生了三十年的公共协议，而不是某家云厂商的专有 SDK。这不是抠门，是主权的技术形态：协议是公共的，实现是开源的，通道上没有需要续费的商业环节，也没有一个握着你数据的第三方中转。编译出来的二进制约十五兆字节，扔进 launchd 就常驻后台；哪天 knowly 停止维护，SSH 到 NAS 的路仍然通——主权不随作者而废，这是自托管最深的一层保险。

同样是「存到自己的地方」，网盘方案看起来也能凑合：开个同步盘，建个分享链接，不费什么功夫。差别在信任链的长度。同步盘的每一环都要信：客户端要信、传输要信、服务端要信、权限模型要信，而分享链接一旦生成，链接本身就成了一张凭证，转发即泄露。SSH 方案的信任链短得近乎傲慢：一把私钥，一台在 known_hosts 里登记过的机器，一条只有我登得进去的通道。安全工程里有一条朴素的定律——每一环信任都是一笔负债，砍掉的每个环节都是赚到的。把五环的信任砍成一环，这不是偏执，是做减法。

主权的完整版不止存储一站。检索也在本地：internal/index 用 SQLite FTS5 加 trigram 分词，在 Mac 本地为 NAS 归档建立毫秒级的全文检索——你查询的内容同样不需要发给任何人。归档格式是 Markdown 加 YAML frontmatter，这是主权问题的另一半：数据不但要在你家，还要以你离开时带得走的方式待在你家。可以用 grep 搜索，可以用 rsync 备份，可以在 knowly 退役的那一天被任何下一代的工具无缝接手。退出成本为零，才叫拥有；被锁定的拥有，只是租约。连管理台的配置回显都做了脱敏，字符串比较用的是 crypto/subtle 的常量时间比较——「看一眼自己的配置」这种动作，也按防侧信道的规格来做。

knowly 不是孤例，它站在军团整个自托管矩阵上。api.yuangs.cc 是个人 API 中枢，微信推送的 weixinpush 住在里面；want.biz 是公网服务域，pic 挂图床，upload 接 Knowly 的上传；knasync 是跨机消息队列；NAS 是私有存储；R2 放需要全球分发的静态资源。九个作品共享这套私有基建，一次建设，处处受益。为什么坚持自托管？不是因为云不可靠，而是因为主权的边界必须由自己划定：knasync 里的消息、NAS 上的归档、中枢里的推送记录，这些数据的管辖权只受我控制，不随任何服务商的商业决策而漂移。云的便利是真实的，但便利的计价单位，往往正是那条边界。

边界之内也有故障，故障的处理同样不出境：SSH 不可达时，knowly 把内容暂存进本地的 outbox，一个 JSONL 文件，网络恢复后自动排空，一条不丢。断网期间，数据只是在我家门口排队，而不是被押送去别处保管——这套韧性设计，第九章专门拆解。连 AI 加工都安排在主权之内处理风险：标签、摘要、评分这些增强交给外部模型去做，但失败时不回滚同步、降级为原始内容入库——增强是锦上添花，数据本身不因外部依赖的死亡而死亡。数据主权的反面不是「没有云」，而是「没有退路」；私有 NAS 就是那条退路，也是主权边界本身：边界之内，我的数据只听我的。这不激进，这只是所有权本来的意思。

## 8.3 密钥的纪律：从 token 入库事故到环境变量强制

内容筛过了，存放回家了，战场上还剩一样东西，天生就必须四处走动——密钥。数据丢了可以重新生成，密钥丢了，丢的是身份：拿着你密钥的人，不是偷走了你的数据，是变成了你。军团每天要跟多少密钥打交道？模型的 API key、微信推送的 token、NAS 的 SSH 私钥、PyPI 与 npm 的发布凭证、知乎的登录 Cookie。密钥的纪律因此成为横向战争的第三战场，而这个战场的第一课，是我亲手交的学费。

自曝一下。wxwatcher 早期的版本里，微信推送的 token 直接写在配置的默认值里——装上就能用，零配置，看上去很贴心。可这件事的另一面是：每一个用 pip 安装它的人，默认共享的将是同一条推送通道，我的那条。文件在谁家目录里变了，消息就发到谁手机上？不，消息会发到我这里。这个包后来上了 PyPI，意味着这个默认值在公共仓库里躺多久，我的推送通道就在公共视野里裸奔多久。结果是没有出事。但我要诚实地讲清楚：没出事，是因为这个包足够小、盯它的人足够少——这是运气，不是设计。在场的每一天都是暴露，哪怕一次也没有被利用。有朋友打趣，说广山哥这是连自己写的工具都防。我认。运气和安全的区别在于，运气不承诺下一次。

1.7.0 的提交信息只有一行：remove default push token, force env config。删除默认 token，强制从环境变量读取。技术上一行 diff 就够了，难的是跨过心里那道坎——承认默认值也是发行物。你发布的每一行默认配置，都是替所有从未投票的用户做出的安全决策；默认值里出现密钥，性质上等于把备用钥匙压在门口的脚垫下面：门还是那扇门，锁还是那把锁，但所有人都能开门了。

把四层配置掰开看，这件事的语义更清楚。默认值，是用户不在场时你替他做的决定；环境变量，是部署者在场时亲手做的决定。密钥从默认值层搬到环境变量层，本质是把「替人保管」改成「请人自持」——工具不再声称知道你的秘密，它只在你明示的那一刻读取。提交信息当变更日志的惯例也帮了忙：remove default push token 那一行，任何翻提交历史的人都看得到这次矫枉，以及矫枉的日期。错误被诚实记录之后，它就成了文档的一部分。

修复必须制度化，否则只是修了一次。wxwatcher 的配置体系是四层的：命令行参数、环境变量、配置文件、默认值，优先级依次递减。1.7.0 之后，凭证被制度性地限定在环境变量这一层，默认值层永远不再持有密钥——宁可让每个用户多敲一行 export，也不在仓库里留任何默认凭证。周边的加固一并补齐：systemd 服务单元带上 NoNewPrivileges 与 ProtectSystem=strict，让进程天然没有提权和乱写系统的能力；推送 URL 在日志里脱敏；连 CI 发布都改用 OIDC 无密钥认证，打上 tag 即发布，而发布凭证根本不存在于任何地方——密钥纪律的最高境界，不是把密钥藏好，而是让某些密钥压根不必存在。试用体验也没有被牺牲：dry-run 模式连推送地址和 token 都可以省略，只检测、只打印、不推送、不写状态。好的安全设计不惩罚试用者，它只是拒绝替你保管秘密。

同一纪律在军团的其他营区各有各的阵地形态。poeapi_go 立了规矩：「前端仅负责 UI 与用户交互，不直接接触模型密钥」「模型 Key 永远只存在于后端」。前端是柜台，密钥是金库；柜台负责展示与收银，金库的钥匙不发给柜员。这不是前端工程师的水平问题，是暴露面问题：浏览器里的每一行代码，都是用户按下 F12 就能阅读的代码。顺带一提，看门人自己也是被看的：请求先穿过 Auth Middleware 验证调用方的 API key，再经速率限制，才轮到脱敏与路由——网关自己的门，也先上锁。npm_yuangs 那边，密钥纪律细化到了一次 SSH 提权的状态机上：从 USER 到 AWAITING_APPROVAL 到 PENDING_PWD 再到 ROOT，提权必须经过人类批准，而输密码的那几秒钟，审计的镜头主动转开，一个字节都不记。这大概是整场战争里最反直觉的一条军规：审计日志也是日志，也会泄露。在一个把「记录一切」奉为美德的时代，知道「什么不该记」，才是更高级的美德。

把三个营区的军规并排放上桌，可以提炼成一句话：密钥，永不以明文出现在它不需要出现的地方。默认值里不该有，前端里不该有，审计日志里不该有，能不存在的最好根本不存在。事故本身不可耻——单人军团没有安全团队替你把风，事故就是你的安全团队；可耻的是事故之后只改代码、不改制度。1.7.0 那一行 diff，删掉的是一个字符串，添上的是一条关于默认值的世界观：默认，必须是安全的默认。

## 8.4 边界的艺术：白名单与有界存储

前三个战场守的都是数据；第四个战场，守的是能力。系统最大的攻击面常常不是门外的坏人，而是自己人的权限——尤其当「自己人」是你亲手请进来的 AI。它替你跑命令、读文件、调工具，每一项授权都是一扇新开的门。安全在这个战场上的形态，不再是筛子和锁，而是边界的艺术：什么可以进来，什么可以出去，什么可以停留多久。画界有两个方向，黑名单与白名单，军团的答案从头到尾只有一个。

看 poeapi_go 的 Agent Runtime。它有一个工具叫 code_exec，作用是让模型执行命令——这个名字念出来的那一刻，听起来就像一部事故纪录片的片头。我的处理写在 tools/code_exec.go 里，整个文件因为构建标签而在未启用 Agent 能力时根本不参与编译。允许执行的命令是一个叫 AllowedCommands 的白名单，内容一共七条：

```
echo      date      whoami
hostname  pwd       ls        cat
白名单之外一律拒绝：command '%s' is not allowed
单次执行强制 5 秒超时
```

七条，全是「报信」的命令：echo 打印，date 报时，whoami 报身份，pwd 报位置，hostname 报主机，ls 看目录，cat 读文件。一个 Agent 用这七条命令可以完整地感知环境，却几乎无法改动环境——没有 rm，没有 mv，没有 curl，没有 sudo。白名单之外的任何命令，得到的是同一句冰冷的回绝：命令不允许。没有「暂时不支持」，没有「确认后执行」，就是不允许。配套的还有一条 5 秒超时：五秒钟之内没有返回，进程被 context 直接收割，连「用一条无害命令慢慢耗光资源」这条暗道也被堵死了。AI 的可执行面，就这样被压缩到「能看、能问、不能动」的最小集合。

为什么是白名单而不是黑名单？因为黑名单是一场永远追不上新花样的竞赛：你封掉 eval，它写 base64；你封掉管道，它落临时文件；你封掉网络命令，它用 cat 去读授权密钥文件。黑名单的逻辑是「禁止我想到的」，白名单的逻辑是「只允许我想到的」——前者的题目空间是无限的，后者的题目空间是你亲手圈出来的那一小块。同样的思路贯穿 poeapi_go 的其他关口：Agent 循环有 max_steps 兜底，默认 5 步，防止死循环；Multi-Agent 编排的轮数上限是 3，讨论不允许无限进行；连 Agent 能力本身都是可选装配——构建标签不开，这部分代码连二进制都进不去。默认关闭，也是默认安全。行业惯性通常是能力默认全开、风险默认用户自担；军团的默认值方向相反：能力默认收着，风险默认我担。边界甚至画到了钱包上：npm_yuangs 的 TokenPolicy 给每次任务设四级预算门，占用 70% 以内放行，70% 到 80% 警告，80% 到满额需要你点头，超出一律阻断——预算也有天花板，花钱的能力同样在界内。

边界的另一半，画在时间轴上。zhihu_extension 的存储全是带天花板的：文章收藏上限 200，已见文章 ID 只保留最近 500 条，监控日志只留 20 条，删除记录上限 1000。代码注释里的原话是「最多保留最近 500 条，防止 storage 无限增长」，日志一节的标题写着「保存最近 20 条到 local storage」。chrome.storage 确实有配额，但设天花板不是为了讨好配额——配额是平台的限制，天花板是我的设计。区别在于：前者是被动的止损，后者是主动的判断。我判断监控日志这种东西超过 20 条就没有留的价值：它记录的是行为轨迹，本身就是敏感物，留得越久风险越大、价值越小。裁剪也不是闭着眼睛砍：淘汰逻辑会先让非收藏的文章让位，标了收藏的优先保留——有界不等于粗暴截断，界内还要分轻重。wxwatcher 那边，同款思路落在通知的嗓门上：单轮变更列表截断到上限条数，避免一次刷屏把接收者淹没——界，也设在表达的音量上。

边界还有一个更彻底的形态：删除。poeapi_go 曾经一次性删掉 16 个没有接入的包，代码减了 2299 行——不用的能力不是留待以后，是直接请出仓库。留着的死代码是静止的攻击面：没人记得它在，没人测试它，也没人给它的门上锁。第二章讲过 npm_yuangs 的八条非目标，那是用文档画的边界；删包，是用代码画的边界。两回事，一个道理：能力的清单越短，需要守的门就越少。

有界存储一石三鸟。其一是安全：旧数据自动消逝，日志不会日积月累变成一部完整的行踪档案。其二是稳定：浏览器扩展的存储写满那天，就是它静默失败的第一天，天花板让「存不进去」这件事永远轮不到发生。其三是性能：去重与比对的成本永远有上界，500 个已见 ID 的查重无论如何都轻过五万个。往深一层看，这也是第三章「对抗熵增」哲学在安全上的投影：知识复利只属于值得保留的东西，无差别的保留不是复利，是熵。收藏有界，日志有界，记忆有界——有界，才有秩序；有秩序，才谈得上安全。

白名单管空间，有界存储管时间；前者把能力的下限压到最低，后者把记忆的上限锁到最死。边界的艺术，不在墙有多厚，而在界有多清——界清了，墙反而可以很薄：七条命令不需要沙箱陪葬，二十条日志不需要重兵把守。把可失去的东西压缩到最小，是比把可失去的东西保护到最好更便宜的策略，这笔账，只有一个人过日子的时候才想得明白。

## 8.5 对平台的敬畏：±25% 抖动与风控暂停

前四个战场都在自家院里，最后一战在别人的领地上。军团里相当一部分工具是寄生性的：wxwatcher 的通知走微信，zhihu_extension 长在知乎的页面里，靠真实 Cookie 在人家的领地上采集；knowly 的发布走 SMTP，sourcepack 住在 npm，wxwatcher 住在 PyPI。连发布环节也住在别人家：PyPI、npm、GitHub、Chrome 商店，每一处都有自己的规矩，发布纪律本身也是寄居礼貌的一部分。平台允许你存在，是因为你暂时没有妨碍平台的利益；平台的风控不认识善意，只认识模式。对平台的敬畏因此不是姿态，而是共生策略——寄生物的第一美德，是让宿主感觉不到你。

一个每天在固定时刻、以完全相同间隔发起请求的客户端，在风控系统眼里就是黑暗里的一盏聚光灯。机器最难伪装的，恰恰是机器的完美：人类的行为有节奏但从不精确，而程序的天性是把 30 秒过成一丝不苟的 30 秒。zhihu_extension 的收藏夹监控靠定时轮询驱动，它对这件事的完整态度写在 background.js 的注释里：

```
// ±25% 随机抖动，让间隔落在 [base*0.75, base*1.25] 之间
```

轮询间隔不再是死板的基准值，而是在基准值的 0.75 倍到 1.25 倍之间随机游走。确定性是工业品的美德，却是寄生者的破绽；我给轮询加的不是延迟，是体温。配套的那句话更直白，是我写给自己的纪律：机器人要学动物觅食，不能机械打卡。动物觅食有节奏，但节奏里带着偏差，这恰恰是生命的特征，也恰恰是风控最难建模的部分。为每个间隔注入正负四分之一的随机性，换来的不是速度，是长久——那块扩展在同一处代码库上连续服役一年半仍在迭代，靠的不是对抗平台的能力，而是从未进入过平台的视野。

被拒绝之后的反应，更见教养。收藏夹监控发现新文章后会推送，而推送失败时，程序的逻辑是：连续 2 篇推送失败，暂停推送 2 小时以防风控。它不会换 IP 再战，不会加重试并发，不会把间隔从半小时缩到五分钟，它就是停下来。风控信号的含义是对方已经在举手示意，这时候加速，等于把警告信撕下来裱到墙上。暂停是状态，不是遗忘：暂停期内程序记得自己在暂停，时间一到恢复推送，日志里留下一行「风控暂停期结束，恢复推送」，像给宿主递了一张回执。对进入队列的内容同样设了门：过长、带换行、井号开头的链接一律不入队——别把自己家的脏东西递给对方的系统。同类场景的另一处，IMA 知识库返回 403 限流，扩展用的是指数退避：5 秒、10 秒、20 秒、30 秒，逐轮翻倍，封顶 30 秒。这不是 zhihu_extension 的独家发明，是军团统一的退避审美：wxwatcher 的推送重试走 1 秒、2 秒、4 秒；knowly 的 SSH 重连走指数退避加随机抖动，第九章细说。退避的本质是一句承认：节流阀在对方手里，不在我手里。

这场战争里我最偏爱的一处细节，甚至与风控无关。扩展作为消费者从 Worker 队列领任务，领到的知乎链接如果处理不了——比如正文抓取失败——它不会把任务丢掉，也不会原地死磕，而是重新推入 URL 到队列，让其他消费者有机会处理。一个跑在个人浏览器里的程序，写出了对陌生协作者的礼让：队列是公共资源，占着坑不放是独占公共品，让位才是维护公共品。多端协同里，手机是遥控器，家里电脑是工人，工人的美德不是全能，而是知道自己干不动的时候，把活放回去。

写到这里，五个战场的防线已经合拢，可以把本章的支柱哲学立成一块碑了。默认安全——安全是默认值，不是可选项；是建制，不是功能；是数据出生时随身携带的户籍，不是出事之后补办的签证；是在你说「先跑起来再说」之前，就已经替你做完的那部分决定；是对内容的筛、对存储的守、对密钥的律、对能力的界、对平台的敬。它不承诺你永不泄露，它承诺的是：在你忘记设防的每一个地方，都已经有人替你设了防。

回头望一眼这场横向战争的地图：poeapi_go 在内容关卡上立起「先阻断、再脱敏、再落盘」的工序，让 AI 只看到脱敏后的内容；knowly 把敏感词的执法权收回国境线之内，把数据的户籍落在自己的 NAS 上；wxwatcher 用 1.7.0 的一次自我了断，换来密钥的环境变量强制；poeapi_go 用七条命令和一个 5 秒超时压住了 AI 的执行面；zhihu_extension 用正负四分之一的体温和两小时的暂停，换来了一年半的相安无事。四个项目，几种语言，一部宪法。总纲说，用技术的确定性，对抗世界的不确定性——而默认安全，就是把确定性浇筑进默认值里的那道工序：世界的不确定性终归会来，但它来的每一次，撞上的都是一堵早就砌好的墙。一个人的军团没有单独的安全部门，因为在这样的军团里，每一个部门都是安全部门。

---

## 第9章 层层兜底：韧性的 N+1 原则

# 第 9 章 层层兜底：韧性的 N+1 原则

## 9.1 失败是常态：从一次 SMB 竞态说起

工程师的词典里有一个词，我始终觉得立场可疑——异常。数据库连不上叫异常，文件读不出来叫异常，网络超时叫异常，仿佛世界的默认状态是岁月静好，失败只是偶尔闯进来的不速之客。写了这么多年一个人运转的系统之后，我的结论恰好相反：失败才是常态，成功才是需要层层铺垫的侥幸。教科书把失败处理写成 try 后面跟一个 catch，语气像道歉；而在我这里，对失败的处置是设计本身，是画在图纸第一页的结构线，不是事后缝上去的补丁。这个立场有一个更工程化的名字，我把它叫作韧性的 N+1 原则——每一条关键路径，都必须至少有一条备用路径。N 是把事情做成的路，+1 是那条平时闲着、出事时顶上来的路；飞机有两台发动机，机房有两路供电，一个人的军团，每条路都要有第二条。

这一章是一次横向的检阅：军团里没有哪个项目把韧性当卖点，但每个项目都长着同样的骨骼。检阅从那台最不像软件的机器开始——广山之巅。第七章写过它的节律，这台内容工厂每天四档，8:30、12:00、17:00、20:30，从不缺席节假日；76 个源文件、约 1.4 万行代码，2382 次提交堆出累计超 1000 期的节目。但「从不缺席」这四个字要反过来读——读作「没有人替它缺席」。单人军团没有 SRE 值班表，凌晨三点没有 on-call，节假日的房间里只有 Mac 风扇的声音，出事的时候它的主人大概率在睡觉。韧性不能长在值班表上，只能长在代码里：要么程序自己会接住失败，要么失败就无人接住，没有第三个选项。

先看这条生产线的解剖图。一期财经播客的生命从 fm.want.biz 的行情与新闻数据开始，经 fetcher 喂给大模型写稿，落盘成 financial_breakfast_{slot}_{date}.md 这样的稿件；Go 写的守护进程 podcastd 用 fsnotify 盯着 NAS 和本地两个目录，稿件一落地就交棒给 md2podcast.sh，文本清洗、TTS 配音、多角色合成、BGM 情绪匹配、封面注入、RSS 生成，最后 sync.sh 把成品同时推向 R2、iCloud、GitHub 和企业微信。Go、Bash、Python 三种语言，四五个进程，两台机器——一条流水线就是一串交接班，而工程学的常识是：交接班的地方，就是世界伸进来的地方。

翻车复盘从一次竞态说起。稿件写在我自己的 NAS 上，播客工厂跑在本机，中间隔着一层 SMB。写入方还在往文件里灌内容，监听方的 fsnotify 已经开始敲门——文件创建事件的触发，可以早于第一个业务字节落盘。接过来的稿子于是偶尔是空的：文件名端庄地躺在那里，内容是零字节。这种故障最坏的品性在于它不定期、不可复现，十天半个月来一次，专挑你不再盯日志的日子。有一阵子档案馆里混进了这样的空壳：流水线后段每一步都忠实地加工了那份空白，TTS 无话可说，混音照样合成，归档照样编号——如果没有防御，我的档案馆就会安静地收藏一期编号齐全、内容全无的播客，而且没有一个人会察觉。

修法朴素得近乎寒酸：读稿时检查内容，如果是空的，等一下，再读一次，如此至多三次。代码注释写得很直白——NAS 的 SMB 写入有竞态，空文件要重试三次。为什么是三次？重试一次，赌的是刚才运气不好；重试三次，承认的是写入方可能走在一条慢路上——网络抖动、磁盘缓冲、协议往返，每一项都可能让「文件已创建」和「文件已写完」之间隔出一大段时钟。三次不是拍脑袋，是给现实里各种慢留出的余量。这是 N+1 原则在时间维度上的投影：一次读取是 N，重试就是那个 +1。

第二层防御管的是数据的样子。md2podcast.sh 要把文件路径嵌进 python3 -c 这样的命令字符串里执行，而文件名是从上一个环节传来的——万一里面混进换行、回车、制表符，嵌入的字符串当场碎裂，语法错误不请自来。Bash 脚本里留着两行注释，我认为它们值得刻在军团的山门上：「文件名中的 \n\r\t 会导致嵌入 python3 -c 字符串时 SyntaxError」「即使 Go 层已消毒，这里也做第二层防御」。消毒做两遍，不是不信任同事，是不信任「上一层不会出错」这个假设本身：Go 层的消毒保证数据建模是干净的，Bash 层的消毒保证字符串拼接是安全的，两层防的根本不是同一个错，而是「防御者自己也是凡人」这个永恒的错。

第三层防御管数据的完整性。稿件的元数据以 JSON 形态在环节之间传递，而半截的 JSON 和空文件一样能毒倒下游——解析器不关心你写到哪一行断的，它只负责抛错。于是有了五次校验：读到合格的 JSON 才放行，否则等一等再读，注释里管这叫防半截文件。五这个数字看起来偏执，它承认的其实是一个朴素的事实：跨进程的世界没有事务，没有任何人向你承诺「你此刻看到的文件是完整的」。样子、时机、完整性——这三层防御合起来，是单人军团对失败世界给出的第一部分答案：不指望世界守约，只指望自己接得住。

有朋友好奇，问广山哥一个人的系统，坏了谁知道？我的回答是：坏是必然会发生的，知不知道反而取决于设计。连删除这种破坏性动作都留了退路——cleanup.sh 清理临时目录时特意注明按前缀匹配，避免误删正式资源，防的是「清理脚本自己变成事故」这条失败路径。失败是常态设计而非异常处理，落到工程上就是一句话：防御不是包裹在主逻辑外面的一层 try，它就是主逻辑本身。catch 只是语法，真正的设计发生在 catch 之前——发生在你决定不信任文件名、不信任文件时机、不信任文件完整性的那些瞬间。

## 9.2 outbox：网络会断，知识不能丢

上一节的三层防御，守住的是「数据来得不对」；这一节要面对更粗暴的失败：路没了。上一章讲数据主权时我留了个尾巴——SSH 不可达时，knowly 把内容暂存进本地的 outbox，网络恢复后自动排空。现在把这个尾巴接上。outbox 是军团里我最偏爱的一件韧性设计，它小到只是 internal 下的一个专职包，却撑起了 knowly 全部使命的信用。

knowly 的使命写得像誓词：Keep Notions Always Saved——让一切念头永不丢失，对抗遗忘，对抗数字熵增。它每 500 毫秒看一眼剪贴板，每一次 Cmd+C 都按日期归档进我自己的 NAS，捕获时零摩擦，沉淀时零丢失。可「零丢失」三个字一出口，就要接受这个世界最擅长的考验：断网。NAS 会重启，Mac 会合盖，Wi-Fi 会切换，SSH 会话会僵死——knowly 与圣殿之间的那条路说断就断，而且专挑你刚复制了一段重要东西的时候断。

此时面前只有两条歧路。一条是丢弃：路不通，内容就扔掉，使命沦为口号。另一条是阻塞：让捕获停下来等网络，把剪贴板轮询卡在 SSH 的超时上——这违背的是 knowly 的另一半哲学，无感，「最好的工具是透明的工具，它尊重用户的注意力」；用户按下 Cmd+C 的那一刻如果开始转圈，工具就从基础设施退化成了麻烦。何况 knowly 本来就为「SSH 慢」修过一道背压闸门——捕获事件进容量为 10 的 itemChan，主循环与同步彻底解耦，注释里承诺剪贴板轮询永远不会因为 SSH 慢而卡住；但背压的天花板再高也是天花板，容量 10 的缓冲挡得住一秒钟的阻塞，挡不住以小时计的断网。两条歧路都通向使命破产，于是有了第三条：路断了没关系，东西先收进自己的口袋。

口袋的名字叫 outbox。SSH 不可达时，捕获的内容以 JSONL 的形态追加落盘，网络恢复后自动排空，按序补投进 NAS。选 JSONL 不是随手为之——追加写天然匹配「只进不出」的暂存语义，进程崩了，最坏损失半行；排空的动作也就是逐行读、逐条发、发完记账，简单到不需要解释。捕获的路径一步都不停：剪贴板照常轮询，内容照常过滤、去重、增强，只是最后一步从直投 NAS 改为先落 outbox。世界断的是路，不是流程——这是「捕获时零丢失」的字面兑现。

一个容易被忽略的细节：outbox 暂存的不只是原始内容，还有 AI 元数据。knowly 的 AI 增强在捕获时就地发生，3 到 5 个标签、50 字摘要、0 到 10 的评分，这些是知识复利的利息。断网挡住的是存储的路，不该顺手把已经生成的利息也冲掉——所以 JSONL 里连 AI 元数据一起保留，路通之后补投进 NAS 的每一行都是完整成品，而不是降级的残次品。暂存的含义由此清晰：不是「先凑合收着」，是「完好地等路通」。

外部依赖会断的不止 SSH 一条，AI 端点同样会失联。knowly 的处置同样值得一记：AI 加工失败，不回滚同步，降级为原始内容直接入库。这条规则把两件事的命分开算——存活与增强。原始内容是雪中送炭，标签摘要是锦上添花；炭必须送到，花可以不插。它与 outbox 互为镜像：outbox 保的是「到了没有」，AI 降级保的是「到了之后完整不完整」；一个守住传输的底线，一个守住加工的底线，两条底线都立住了，「零丢失」三个字才敢说出口。

排空还有一个隐秘的陷阱：重复。断网可能恰好发生在排空进行到一半的时候——补投了三百条，路又断了，剩下两百条还在 outbox 里；等路再通，从头再排一遍，前三百条不就重了？这正是三层去重的用武之地：内存哈希挡当场重复，status.json 的持久化记录挡跨进程重复，远端的 .knowly_hashes 索引挡最终重复，补投在远端哈希面前会被认出来，不会变成重复建档。你看，上一节的道理在这里又应验了一次：每一层防御防的是一种不同的死法，韧性就是这些死法加在一起的总账。

零丢失还有更早的一道保险。outbox 防的是「进不了仓库」，knowly 的历史回溯防的是「进了仓库又被覆盖」：knowly history 可以翻看剪贴板的历史版本，knowly restore 能把被覆盖的内容找回来——仓库里丢的东西，从仓库自己的账本里挖回来。为了把「翻账本」这件事做得毫无心理负担，本地检索用 SQLite FTS5 建了毫秒级索引，一次详情查询曾从 2.9 秒的 SSH 往返优化到毫秒级返回——查询足够快，人才会真的去翻旧账；翻旧账足够省力，丢失才真正成为可逆的事故。数一数你会发现，光在「不丢」这一个愿望上，knowly 就修了三条路：outbox 保进仓，三层去重保不重，历史回溯保可逆。

韧性还有一条用户视角的验收标准：用户最好永远不需要知道它存在。outbox 排空的那个早晨，你照常复制、照常翻阅归档，NAS 目录里多出的那几十条按时间排得整整齐齐——你不知道昨晚网络断过两小时，也不需要知道。最好的韧性是让失败发生过，而没有人被惊扰；就像好的守夜人不叫醒主人，只把天亮时的早饭照常端上桌。这比「能恢复」更难：能恢复是技术，恢复得无声无息是审美。

这套设计也不是开工第一天就画好的。knowly 的七轮演进里，「连接韧性」是一轮，「离线 resilience 与 AI 增强」又是一轮——outbox 是被一次又一次「路断了」逼出来的，每一轮都只解决当前的问题，不做过度设计。知识复利的前提是零丢失：一条丢失的复制，就是永久消失的复利本金，复利最怕的从来不是利率低，是本金蒸发。README 里还有一层解读叫 Knowledge Never Accidentally Sinks——知识永不意外沉没，放进你的私人知识方舟。outbox 这个小小的包，就是 Async 二字的抵押物，也是那艘方舟的救生艇：异步的意思是「晚一点到」，从来不是「可能不到」。

## 9.3 优雅退出的礼貌：SIGTERM 保存状态

前两节防的都是外面的世界，这一节把镜头转回来，防的是自己——程序自己的退场，也是日程表上的一行。systemd stop 是排班，发版重启是排班，kill 是排班，断电和崩溃是没打招呼的排班。一个长期运行的进程，死亡不是意外，而是它一生中必然发生若干次的正常事件；真正分出高下的，是它死的时候留下什么。

主角是 wxwatcher——那个把文件系统的风吹草动推送到微信的小哨兵。它的工作模型一句话能讲完：每 30 秒一轮，先 stat 快筛全部文件的修改时间与大小，只对疑似变化的文件计算 SHA256，有变化就推送，然后保存状态。这套两阶段扫描让 5000 多个文件的目录每轮只需毫秒级扫描；超过 10MB 的大文件只哈希前 8KB，指纹记作 LARGE 加尺寸加部分哈希——务实地接受近似，换来零灾难性 IO。它的全部记忆都在那份基线里：哪些文件、什么大小、上一轮什么样。

现在，让它在没有状态保存的世界里死一次。重启，进程睁眼，目录里 5000 个文件个个陌生——只好重建基线，把眼前的一切当作「已知」。这带来两个恶果。其一，停机期间发生过的变更被静默吞掉：你在它死去的两小时里改过的文件，在重建后的基线里「本来就是这样」，永远不会再被报告。监控工具最耻辱的死法莫过于此——该报的没报，而且你不知道它没报。其二，重建本身就是一轮全量哈希，CPU 和磁盘要为一次本不必要的失忆买单。崩溃会喊，漏报在微笑；后者才是更重的罪。

还有一个时机的讲究值得单独拎出来：wxwatcher 的状态不是退出那一刻才保存的，而是每轮扫描的收尾动作——stat、哈希、推送，然后 save_state 原子落盘，一轮一存。于是 SIGTERM 到来时，所谓「保存状态再退出」并不需要临时抱佛脚，它只是把一个已经做过千百遍的例行动作再做一遍。这是对死亡更深一层的接纳：不是在死亡来临时才开始准备死亡，而是活着的时候，每一轮都在为死亡做准备。优雅退出之所以优雅，功夫全在活着的日常里。

wxwatcher 的解法写在源码注释里：「把 SIGTERM 转为 KeyboardInterrupt，让 systemd stop、kill 也走优雅退出路径——保存状态后再退出，避免重启后重建基线。」README 给用户的承诺则是：「SIGTERM、Ctrl+C 均优雅退出并保存状态，重启后增量对比不重建基线。」我最欣赏的是那个小小的转接术：把 SIGTERM 翻译成 KeyboardInterrupt。Ctrl+C 是人类的礼貌，systemd stop 是系统的礼貌，两种死亡通知原本各走各的路，翻译之后汇入同一个出口——无论谁请我走，我都先把手头的事交代清楚再走。这是一份礼貌的双向合同：世界礼貌地通知我，我礼貌地留下遗言。

交代清楚的前提，是「交代」这个动作本身不会被死亡打断。保存状态也是写文件，写到一半断电，得到的就是一份截断的 JSON——下次启动解析失败，照样被迫重建基线，白存了。所以状态落盘用的是 tmp 加 os.replace 的原子写：先把新状态完整写进临时文件，再一次性重命名过去。

```
with open(tmp_path, "w") as f:
    json.dump(state, f)
os.replace(tmp_path, state_path)
```

重命名在文件系统层面是原子的——观察者看到的要么是旧状态，要么是新状态，永远没有第三种「半新半旧」。注释里那句「崩溃或断电不会损坏已有状态」，就是这行 os.replace 给出的承诺。N+1 原则在这里收敛成它的微缩形态：磁盘上永远保持一份完好的状态，新的一份在旁边悄悄长出来，长好了才完成交替——旧状态就是那个 +1，平时冗余，断电时救命。

细节里还有体面。状态文件按目录路径的 MD5 命名，state 加八位哈希后缀，多实例监控多个目录时各存各的记忆、互不踩脚——这是给「未来的自己」留的余地：今天监控一个目录，明天监控三个，基线们并行不悖。网络请求失败时以 1 秒、2 秒、4 秒的指数退避重试，日志轮转防止把磁盘写胖，正则用 LRU 缓存防止把 CPU 耗尽——一个源码 1188 行、测试 888 行的小工具，把退场礼仪做成了出厂标配。

放眼军团，记忆落盘是通行的军规：knowly 用 status.json 记住去重的进度，podcast 用 processed 目录记住每一期已经归档的编号——凡是声称「我记得」的地方，记忆就必须活在进程之外、磁盘之上、下一次启动读得到的地方。进程是临时的，状态是长久的，进程只是状态的过客。想通这一点，很多设计会自动变对：该持久化的别赖在内存里，该原子的别裸写，该在退出前保存的别指望下一次。

wxwatcher 甚至把「死亡是日程」接纳进了自己的世界观。它同时提供两种活法：常驻模式跑在 systemd 服务单元里，单轮模式适配 cron 与 systemd timer——同一个小工具，服务常驻与定时两种哲学，因为作者很清楚：常驻进程一定会被重启，与其假装长命百岁，不如把每一轮都写成可以从头再来的一轮。试用也有对应的台阶：dry-run 只检测、只打印，不推送、不写状态——状态和推送一样是副作用，试用不该污染记忆。连配置都是一条递进的兜底链：命令行参数、环境变量、配置文件、默认值四层依次接管，谁在场谁说了算；而忽略规则在层与层之间取合并语义，累加而非覆盖——覆盖会丢失，累加才是兜底。

优雅退出是文明，不是功能。野蛮的系统各有各的死法，文明的系统死得整齐——桌面收拾干净，钥匙放在固定位置，给下一班留一张字条。一个进程的死亡方式，决定了它转世之后的生命质量；你对死亡的态度，最终都会变成重启之后世界的样子。SIGTERM 到来时多写的那个临时文件，就是你留给明天的自己最好的礼物。

## 9.4 降级链设计：通知的三级火箭

预防、暂存、礼貌退场——前三层韧性有一个共同假设：主路径还会回来。但有一类失败，主路径本身已经死了，你不得不换路走。这就是降级链的领地，而它要守的东西恰恰最细嫩：通知。通知是人与系统之间最后一根神经，这根神经断了，前面所有的韧性都会沦为无人知晓的美德——节目照做、知识照存、状态照保，只是没有人知道。军团在通知这件事上花的心思，比表面上看到的多得多。

第一台三级火箭装在 podcast 的 sync.sh 上，那个开头就自报家门的脚本：「全能同步与高级通知脚本——功能：R2 同步、iCloud 同步、GitHub 推送、企业微信模板卡片。」成品 mp3 一次落地，四个出口同时出货；而通知这一路设了三级降级：企业微信图文失败，退到纯文本备用；文本也失败，退到 osascript 桌面通知。三级火箭一级比一级轻：图文卡片载着信息密度和仪式感，纯文本只保真内容，桌面通知就近喊一嗓子——万一你正好坐在电脑前。

为什么不干脆只发文本，一劳永逸？因为降级链的每一段都为正常世界服务：图文卡片是常态世界里的体验，文本是故障世界里的底线，桌面通知是最后的就地救急。平时给最好的，坏时报最真的，绝境里也要喊得出声。降级降的是保真度，不是使命——通知的使命只有一句话，让人类知道。人类知道了，用卡片还是用一行字，只是保真度问题；让人类永远不知道，才是使命问题。使命高于形式，这就是降级链的第一性原理。

顺带把视野拉宽一点：sync.sh 那四个出口，本身就是分发层的降级链。成品 mp3 一次落地，R2、iCloud、GitHub、企业微信四路同发——R2 挂了有 iCloud，iCloud 挂了有 GitHub，最不济企业微信的卡片还躺在群里。同一份产物，四条路；任何一条路断掉，都不构成丢失，只构成冗余度的下降。通知的三级火箭，只是这条哲学在「告知」上的投影；「货物」本身的运送，早就用同一张图纸修好了。

降级链最阴险的敌人是静默降级：图文通道三个月前就死了，你收到的全是文本，而你以为那叫风格。所以每一次降级都要留痕——日志里记一笔，让下一级知道自己为什么上场，也让系统能在下一次检修时找到案发现场。这就是我给通知链立的八字规矩：失败不空转，降级不沉默。失败不空转，指每一级失败都必须把接力棒递下去，链条不走回头路，也不原地断气；降级不沉默，指每一次降级都必须被记录，不许装作无事发生。这背后还有一层更深的底气：通知是生产线的最后一步，通知全灭，节目本身还在——归档照做、RSS 照发，使命不随渠道陪葬。

第二台火箭装在知乎助手的行情面板上。popup 里那块期货行情监控覆盖 SHFE、DCE、CZCE、GFEX、INE、CFFEX 六大交易所，数据从 q.want.biz 拉取，ECharts 本地渲染。网络的脾气说变就变，这块面板于是配了一套「数据备份和回退机制」，注释里的编号是方案五：请求用 fetchWithRetry 做多轮重试，重试穷尽之后，回退到上次成功的数据——断网时面板显示的仍是上一次成功的快照，永远有东西可看。旧行情是易腐品，但腐坏的行情依然传递结构：主力合约是谁、价差在哪个量级，画面的骨架不塌。看盘的人知道旧数据值几分，空白面板却只会让人心里发毛——宁给一份过期的地图，不给一片没有地名的旷野。

方案五这个编号本身就是一层化石。它安静地告诉你：这里曾经有过方案一、方案二、方案三、方案四，它们没能活下来——没留下代码，只留下了「五」这个数字。知乎助手注释即变更日志的传统在此又添一例，「恢复为域名地址，后端已集成 NAS 同步」，改到哪写到哪，代码与履历同体。这个扩展还有一条更基础的纪律，叫失败路径优先设计：每个监控流程都先写「已禁用跳过」「未配置凭证」「收藏夹为空」的分支，再写正常路径。先把所有的「不行」想清楚，「行」才可靠——正常路径从来不该是设计的起点，失败路径才是。

降级不总由故障触发，有时是系统主动踩下刹车。知乎助手连续 2 篇推送失败，自动暂停推送 2 小时以防风控——它知道自己在谁的领地上活动，继续硬推的下场不是少推几篇，而是整条通道被端掉。wxwatcher 把单轮变更列表截断到上限条数，避免一次刷屏把接收者淹没，推送本身也按批次分开——通知的克制，也是通知的韧性。知乎助手的任务流里还有一幕更谦让的降级：扩展从云端队列领了任务，发现自己做不动，不会把任务吞进肚子里装死，而是「重新推入 URL 到队列，让其他消费者有机会处理」——单机程序里长出了协作精神，退单不是失败，是把失败递给还活着的人。这些是降级的另一种形态，熔断与让渡：不是「做不了所以换着做」，而是「继续做会伤害自己，所以主动停」或者「我做不好，交给能做好的」。降级链保护的是使命，熔断保护的是使命的载体——车开不动可以修，车报废了就什么都没有了。

把军团里这几条降级链并排放着看，会发现它们其实是同一张排序表：使命，保真，渠道，沉默。使命永远第一——节目要播，知识要存，面板要有内容；保真次之——卡片不行就给文本；渠道再次——这根神经断了换那根；而沉默，是这张表上唯一被禁止的终点。失败不空转，降级不沉默——韧性清单上最容易被忽略的就是这两行，因为它们不产生任何新功能，只在最坏的那一天兑现价值；而那一天一定会来。

## 9.5 四层保活与指数退避：对抗遗忘的基础设施

最后一层韧性，要推翻前面所有小节的一个隐含假设。预防、暂存、退场、降级，都把失败当作「事件」——发生、处理、翻篇。而 wsstunnel 生活的世界里，失败不是事件，是环境：沙箱容器每 30 分钟强制回收一次，失败被写进了时刻表。在那里，生存不是一个状态，而是一个持续的动作——你必须一遍又一遍地证明自己活着，因为这个世界会定期忘记你。

wsstunnel 的缘起第四章讲过，但那段话值得再引一次：「那段时间，我被沙箱环境折磨得够呛——容器说回收就回收，远程 SSH 总是不通。我把能想到的工具全试了一遍：cloudflared、frp、wireguard……要么配置复杂到令人头秃，要么在只允许 HTTP 代理出站的铁壁前直接哑火。折腾到半夜，我突然想明白了：既然现成的路都走不通，何不自己开一条？」路开出来，是第四章的故事；让这条路在任何环境下都活着，是这一节的故事。开路靠勇气，护路靠迭代——wsstunnel 的四层保活，一层一层全是被回收教育出来的。

四层保活摆在一起看：

```
L1  disable websocket builtin ping
L2  __PING__ / __PONG__ every 30s
L3  reconnect with exponential backoff, 5s -> 300s cap
L4  shell respawn on crash, up to 5 times
```

第一层，关掉 websocket 框架自带的 ping——框架心跳只能证明协议层活着，证明不了你的会话还在，心跳的定义权要收回到自己手里。第二层，每 30 秒发一次应用层的 PING 与 PONG，用业务自己的语言问一声「还在吗」。第三层，断了就重连，间隔按指数退避，5 秒起步、300 秒封顶。第四层，Shell 进程崩溃就自动重生，至多 5 次。四层各管一种死法：链路假活、真断、重连风暴、会话暴毙。

为什么是四层，而不是把某一层加厚？因为兜底的要义不在厚，在分层——每一层对应的失败模型不同，同一种防御加三遍，只防同一种死法三次。心跳防假活：连接看着在，数据早就不通了；退避防风暴：服务端刚喘过气，就被一万只同时醒来的客户端再次踩死；重生防暴毙：链路好好的，Shell 自己先断了气。层层兜底的「层层」二字，说的就是给不同的死法各配一个收尸人。30 分钟一次的回收，要用 30 秒一次的证明来对抗——用比破坏者更细的时间粒度看护自己，这是弱者在这个世界上活下去的算术。

指数退避是军团统一的审美，各营区不约而同。knowly 的 SSH 同步用指数退避加 Full Jitter 重试；wxwatcher 的重试是 1 秒、2 秒、4 秒；知乎助手对 IMA 的 403 限流用 5 秒、10 秒、20 秒、30 秒；wsstunnel 重连 5 秒起步、300 秒封顶。指数的含义，是让重试的代价递增，给世界留出疗伤的时间——线性重试是固执，指数重试是对现实的尊重。封顶的含义同样重要：退避不等于放弃，300 秒封顶等于宣告「我再慢，也没有退场」。退避是耐心，不是遗书。

再往上叠一层随机性。knowly 的 Full Jitter，知乎助手的「±25% 随机抖动，让间隔落在基准的 0.75 倍到 1.25 倍之间」——同一套数学，在两处服务于两种动机。对机器，它防惊群：重连的客户端如果在同一秒集体醒来，服务端会被第二次踩死，把时间打散，风暴就起不来。对平台，它防风控：「机器人要学动物觅食，不能机械打卡」，精确得像时钟的轮询，等于在别人的领地上自报家门。直方图上没有尖刺的调度，才是长寿的调度；随机性在这里不是粗糙，是礼貌，是在一个不欢迎机器人的世界里做机器人的生存智慧。

保活的最高形态，不是赖着不死，而是死得起。podcast 的 Go 守护进程 podcastd，注释里写着「KeepAlive 接管进程保活」「崩溃后自动重启」——launchd 负责把它从尸体上唤醒；knowly 更进一步，发现 LaunchAgent 的 plist 缺失时，knowly start 会自愈重装——它连自己的生命维持系统都能重新安装；wxwatcher 则干脆把 wxwatcher.service 做成开箱即用的交付物，装完就归 systemd 管——把「由谁保活」写进发行物，而不是留给用户自己悟。这些与上一节的优雅退出恰好构成一张完整的生死契约：程序之内，退出前保存状态，死得明白；程序之外，KeepAlive 负责复活，接得利落。内层管死相，外层管复活，内外协作，一个进程才敢自称「长期运行」。保活是对抗遗忘的基础设施——这个词在 knowly 那里指对抗人的遗忘，在这里指对抗世界的遗忘：世界会忘记你的进程，而你的代码要记得替它记住自己。

备用路径的清单还能继续往下数。knowly 连 SSH 的拨号都备了 Plan B：直连不通时回退到 dialViaNC，借 nc 的手把连接续上——通往圣殿的路本身就是双车道的。wsstunnel 更是把「让人知道」做成了参数：后端容器上线、下线，都会触发一条微信通知，基础设施的生死不再只有日志知道。数到这里你会发现，N+1 不是某一处的技巧，而是军团铺开之后随处可见的地貌：每一条命脉，都在你看不见的地方埋着第二条。

行文至此，把这一章的账合并同类项。失败是常态，那就把对失败的处置写成常态——稿子可能空，就读三次；路可能断，就先装进口袋；进程会死，就让它死得完整；通知会断，就让它顺着三级火箭喊出去；容器要回收，就每 30 秒证明一次自己活着。兜底不是补丁：补丁是出事之后缝上去的，兜底是出事之前就长在那里的；它不是代码的义务，是哲学的立场——你不信任任何单点，因为你知道单点必死，而军团要永续。N+1 的最后一条备用路径，留给人本身：代码兜住前面九十九次失败，第一百次交给我，人在环上，这是兜底的尽头，也是兜底的开端。用技术的确定性对抗世界的不确定性——这句话最结实的那道防线就砌在这一章：世界可以弄坏我任何一层，但它必须一层一层地弄坏，而每两层之间，都隔着一次我的重来。

---

## 第10章 军团协同：九个作品如何互为犄角

# 第 10 章 军团协同：九个作品如何互为犄角

前九章，我按柱子讲哲学，按项目讲事：knowly 守着档案馆，wsstunnel 在绝境里开路，npm_yuangs 立宪，weclaw 把指挥部搬进微信，podcast 让广播站一天四档准时开播。每一章收尾的时候，那些作品都还是各自登场的英雄。但孤立的英雄打不了战役——军团两个字，从来不是九件兵器的加总，而是它们之间的连线。软件工程里有一句我信了很多年的话：小即美，组合优于集成。这一章想把这句话再往前推一步。组合优于集成的最高形态，不是组合，而是协同：九个各自独立、互不隶属的小工具，因为共享同一套哲学与同一套基建，彼此之间长出了默契，于是一支军团成形了。协同没法靠图纸设计出来，它只能在共同的纪律下慢慢长出来。这一章，我先让一条微信消息当一回导游，带诸位把军团的阵地完整走一遍；然后讲把多台机器缝在一起的那条队列，讲兵种之间怎么互相喂养，算一算基建这本复利的账，最后看一眼这张仍在生长的版图。

## 10.1 一条微信消息的军团之旅

讲协同，最怕讲成架构图朗诵。所以我从一条真实消息讲起。周五傍晚，通勤的地铁上，我在知乎刷到一篇讲大模型训练细节的长文，不想在手机上读——手机读长文是糟蹋长文，六英寸的窗口装不下两万字的论证。我把链接复制，切到微信，发给那个叫指挥部的对话框，开头敲上斜杠：/zhihu。从这一秒起，这条链接就不再是我手里的一条消息，而是军团的一张调令。

消息先到指挥部。weclaw 那一头，ilink 的长轮询循环一次最多挂起三十五秒，有消息立刻返回，轮询断了就走指数退避，三秒起步、逐轮翻倍、六十秒封顶；消息进了 messaging 的处理器，先去重验明正身，再按前缀分流——/zhihu 是约二十个斜杠命令里的一员。值得一提的是入口本身：我也可以不动手，发一段语音，微信服务端早已完成的语音转写直接把文字递给 Agent；我也可以不发文字，扔一张截图、一份 PDF，指挥部自会解析注入上下文。入口的多样是手机场景的常识，指挥部把常识全部接住，调令才能以任何形态下达。

/zhihu 命令做的第一件事，是把链接 POST 给 knasync 的提交端点。这一下，调令离开指挥部，上了驿道。knasync 认得出知乎链接的 URL 模式，自动把它分进 zhihu 队列，顺手做两层去重：三十秒滑动窗口挡住手抖连发，consumed 表挡住重复提交——我就算在地铁里因为信号抽风重发了三遍，队列里也只排一单。

几公里外的家里，zhihu_extension 正在按带 ±25% 随机抖动的节奏定时拉队列。它领到这个 URL，带真实 Cookie 去抓正文，自己手写的基于栈的解析器把 HTML 拆成 Markdown，连 data-actualsrc 这种知乎私有属性都照顾得到；抓完把结果经 /results 回传。它当然也可能领到做不动的一单，那时候它会重新推入队列，让其他消费者有机会处理——但这一单它做得动。与此同时，正文也汇进了 knowly：本地先过滤敏感词，再经 SSH 落到 NAS，按年月日归档，文件名从标题里长出来，frontmatter 记下时间与来源；三层去重守着门，档案馆只入一本账。整条链路是异步的：抓取要几分钟，指挥部从不假装一切发生在同一秒，对话框里先有进度，后有全文。

而我的手机上，清洗过的全文回来了：frontmatter 剥掉，图片残留洗掉，图注合并成一行。从发链接到收全文，几分钟，全程我没有打开过一台电脑。地铁到站，文章已经在口袋里。顺带说一句，调令不只有 /zhihu 一种。随手发一个普通链接进来，剪藏链路就接手：抓正文、转 Markdown、落库，微信公众号文章伪装浏览器头直抓，外站走 Jina Reader 并带降级路径——微信即收件箱，这句话在第六章说过一遍，放到军团地图上看，它有了新的意义：收件箱的下游，是一整支随时待命的军团。连定时都懒得自己记，/cron 听得懂自然语言，每天几点干什么，一句话立字为据，重启不丢。这条链路值得画在一张图上，我把它原样抄在下面。

```
微信 /zhihu 链接
   │ weclaw：长轮询收信 → 去重 → 命令分流
   ▼
knasync：自动分类进 zhihu 队列 → 两层去重
   │ 家里电脑 zhihu_extension：定时 /pull 领任务
   ▼
zhihu_extension：真实 Cookie 抓正文 → 手写解析器转 Markdown → /results 回传
   ▼
knowly：本地脱敏 → SSH 落 NAS → 按日归档 + frontmatter
   ▼
微信回执：清洗后的全文发回对话框
```

故事没完。晚上回家，我在同一个对话框里说：讲讲下午那篇。挂在 Agent 身上的 knowly-mcp 接了这一单：先 search 找到路径，再 entry 读全文，最后总结——这是它文档里写明的标准工作流；索引常驻内存，毫秒级返回，实测时里面躺着六千九百多条目，索引未命中就去 NAS 回源，老文章也不会失联。档案馆在这里显出了第二重身份：它不只是仓库，它反过来喂养指挥部。而档案馆与记忆的边界，文档里专门画过一张表：knowly 的四件工具查的是归档文章库，全量在 NAS；remember 与 recall 管的是对话记忆。两者不能混——档案是档案，记忆是记忆，混了就会把听来的当成记住的。

如果我说的是做播客，触发词表里那组词命中，订单发给远程 API，广山之巅的生产线就开工：资料进料，模型写稿，TTS 配音，BGM 按情绪打标配乐，RSS 与 R2 多路分发，当天 20:30 那档准时开播，完成后的战报沿通知链送回手机。如果我说发到 knowly、存到 NAS，触发词命中，内容直奔档案馆；一条 /publish 则把文本推给远程发布接口——而 knowly 的发布引擎早就给内容备好了博客、播客、IMA、Kindle 几个出口，第三章讲的“一次输入，三重输出”，到了军团层面成了标准服务。那个发布工具的默认目标，曾经从博客改成 knowly，git 提交信息里写得清清楚楚；说一句发出去、推送，此前生成的内容还会自动异步送到目标渠道。

数一数这一趟：指挥部派单，驿道传令，情报站采集，档案馆入库，回执回到微信，广播站随时可以插一单。它们分布在手机、家里电脑、NAS、两台常开的主机和 Cloudflare 的边缘节点上，说着 Go、JavaScript、Python、Bash 四种方言——却没有谁为这次协同改过一行代码。互为犄角，原是兵法里的词：两处阵地互成掎角，一处遇袭，另一处可以支援掩护。放进工程里，它的含义是任何一个兵种缺席，链路都不会当场断裂：扩展不在线，任务在队列里等着；NAS 暂时不可达，knowly 的 outbox 先把条目落本地；某个 Agent 不在，还有别的 Agent 站进同一个接口。协同不在代码里，在边界上；犄角不在阵型图上，在每一处兜底里。军团协同还有一个容易被忽略的受益者：未来的我。链路上每一步的产物都落了盘——队列里的任务、NAS 上的归档、JSONL 里的对话、processed 目录里的成片——任何时候回看，这条消息走过哪几站、每一站留下了什么，全部可查。写即持久化是早就立下的老规矩，放到协同的链路上，它意味着每一段行程都自带行车记录。

## 10.2 队列哲学：knasync 与异步解耦

上一节那条链路能跑通，靠的是一个不起眼的角色：队列。这一节把它单独拎出来讲，因为军团最核心的一种哲学藏在这里。缘起是一个再普通不过的场景：手机上刷到一条内容，想回家用电脑慢慢处理。人在移动，机器在家，两台设备永远不可能同时在线等我发令。我干过的蠢事包括发文件传输助手、给自己发邮件、塞进各种稍后读应用——每一个方案都要求我之后再打开一次那个应用，摩擦一分没少。队列是唯一的解：把提交与处理拆开，中间放一本双方都信任的存折。

这套队列的正式版叫 knasync，2026 年 5 月 14 日第一次提交，README 的自我介绍朴素得很：基于 Cloudflare Workers 与 D1 数据库的轻量级内容队列分发系统。它把自己拆成三个角色：生产者 POST /submit 提交内容；消费者 GET /pull 拉取、处理完 POST /push 回写结果；订阅者 GET /results 游标分页读取广播。零依赖，单文件，平台给什么就用什么——和 zhihu_extension 那套浏览器即运行时是同一条家法，只不过这里是边缘即机房。后来我把玩法写成了使用指南，里面有两个标准场景：手机剪贴板到电脑阅读，自动化内容处理流水线；附录里甚至躺着现成的快捷指令模板——手机装一个指令，复制即提交，到家即取货。

细节上有几处我愿意停下来夸一夸。三个命名队列 zhihu、wechat、general，提交时不指定队列，系统自己认 URL 模式分流；拉取时不指定队列，按 zhihu 到 wechat 再到 general 的优先级回退——消费者不需要记住内容住哪间房，敲门顺序是定好的。去重做了两层：三十秒滑动窗口防连发，管的是手滑；consumed 表在消费确认后永久去重，管的是命运——一条内容一生只被认真处理一次。鉴权用 timing-safe 的比较，密钥比对的时间不向侧信道泄露半点。/results 支持 since 游标增量拉取，旧版时间戳游标会自动迁移，订阅者不重不漏；还有一个 /peek 端点，只读不删，给排障留一扇不破坏现场的窗。

队列哲学买到的，其实是三重解耦。时间上，生产者与消费者不必同时在线：手机提交完可以关屏进地铁，扩展可以下班停机，队列在边缘节点值守。空间上，双方不必同机：手机、家里电脑、云端互相不知道对方在哪。组织上，双方不必认识：扩展不知道也不需要知道这单生意来自 weclaw 还是来自手机上的快捷指令，它只认协议。同步调用是一根电话线，两端都得守着；队列是一座邮筒，寄信人投完就走。九个作品散落在六七台设备上，语言各异、版本各异、作息各异，唯一能把它们缝成整体的，就是这条谁都不在线也始终存在的驿道。

这条队列的早年形态，如今还留在 zhihu_extension 的仓库里——一份二百六十三行的 worker-fixed.js，配套的 Cloudflare Worker，手机端提交内容，自动分流到对应队列，扩展作为消费者领任务。今天队列有了正式版 knasync，连 queue_ 前缀的旧写法都兼容保留——两套实现说的是同一套协议，新版本向后兼容旧习惯。那份 worker 里甚至给队列设了容量上限，超限自动清理旧条目——对熵的抵抗，从单机一路延续到云端。队列哲学里我最偏爱的一笔，写在扩展的代码注释里：领到的任务做不动时，重新推入 URL 到队列，让其他消费者有机会处理。单机程序里写下了协作精神——不吞不弃，做不动就放回去。同一份自觉还体现在风控暂停上：连续两篇推送失败，自动暂停两小时防风控。因为队列里不止我一个消费者，我搞砸的不只是自己的任务，还有整条驿道的信誉。

同款设计在 knowly 里叫 Relay：手机上的灵感经 HTTP 队列汇入，拉取后删除，确保每条内容只被一个客户端消费。两台 Mac 同时开 Relay 拉取会发生什么？竞争性重复同步，一份灵感归档两遍。所以 RELAY.md 里写明了最佳实践：主力机做唯一的 Relay 接收器，辅助机禁用拉取、只同步本地剪贴板。队列的纪律，一半写在代码里，一半写在约定里——代码负责机制，约定负责避坑。

队列也不是只有顺风的时刻，它的失败路径同样被认真对待。消费者拿到任务做砸了怎么办？knasync 给出的答案很克制：拉取即删除，重试的语义留给消费者自己——扩展遇到限流就指数退避，五秒、十秒、二十秒、三十秒地往后挪，遭遇风控就整队暂停两小时；做不动就重新入队，把机会让给同伴。订阅一侧用游标保证不重不漏，宁可消费慢一点，不把同一份结果端上桌两次。这套分工里藏着一条朴素的真理：队列只负责记账，不负责替人干活；把失败处理得体面的，从来都是消费者自己的纪律。

```
手机 /submit ──► 队列（D1）
                   │  消费者 A /pull ── 做不动 ──► 重新入队，礼让他人
                   │  消费者 B /pull ──► 处理 ──► /push 回写结果
订阅者 ◄── /results 游标分页，增量读取，不重不漏
```

这条驿道上跑的远不止知乎链接：sourcepack 的 -p 参数把快照推给远端中继，weclaw 能中继远程剪贴板，knasync 还有一个 MCP 服务器，把搜索、发布、上传与队列并成一把十二个工具的瑞士军刀，微信里的 Agent 因此能直接查队列、领任务。整个系统部署在 Cloudflare 的边缘上，仓库里连 GitHub Actions 自动部署都配好了；D1 是 Serverless 的 SQLite，没有一台服务器需要我惦记。说到底，异步解耦是从系统里删除了同时在线这个最昂贵的要求。队列是军团的时间机器：它把现在必须有人，翻译成迟早会有人——这也是九个作品敢各自独立、互不等待的全部底气。

## 10.3 互相喂养：数据流与工具流

协同的日常形态不是行军，是吃饭。一个多兵种组织最常见的死法，是彼此眼里的对方只是另一个系统——门户之见，协议不通，每对接一次都要重新谈判。军团不是这样：它们之间长着稳定的供养关系，数据流送原料，工具流送能力。这一节把几条供养线挨个讲一遍。

粮食线的第一站是 sourcepack，军团的翻译官。AI 要读懂一个代码库，得有人先把代码库翻译成它能吃的形状。sourcepack 一键把项目打成单个 Markdown 快照，内容固定四件套：目录优先的项目结构树、带锚点跳转的文件目录、自动语法高亮的完整源码、多维度统计。整个工具是单文件 Go，一千四百一十三行，第三方依赖只有一个命令行参数库；gdoc、gdx、gx、doc 四个命令别名，怎么顺手怎么敲。三层忽略系统确保不喂垃圾，匹配引擎完整支持否定、多层通配与目录锚定，提交记录里还留着升级 gitignore 匹配引擎的那一笔；专属的 .gdignore 只影响自己的扫描、不动 Git；二进制智能跳过把喂给 AI 的应该是文本这条常识写进代码；Token 预估让我在下单前就知道这顿饭多大。交付路径有三条：落盘成文件，-c 一键复制后直接贴进对话框，-p 推送远端中继。翻译官喂饱的是整个军团的脑子——我把哪个项目讲给 AI 听、讲得多干净，决定了 Agent 能替我在那个项目上走多远。顺手的另一面是讲究：别名与一键的背后是同一个引擎、一条主线，扫描、过滤、合并、输出，四个动作从头到尾没有分叉。

弹药线是 poeapi_go。粮食喂脑子，弹药喂火力，军团每天的模型调用大多从兵工厂领：一个 OpenAI 兼容的统一网关，config.yaml 里声明 providers 与 models 映射，添加一家供应商零代码，不用改任何 Go 代码；路由决策链是显式映射优先、关键词兜底、默认收尾——Router 本身即 ModelClient，不是 if/else 堆出来的。战损记录里有一笔我常讲：智谱渠道失效当天，实测切换小米 MIMO，前线的枪没有停。播客工厂的写稿走 poe.want.biz，开播以来累计一千多期，弹药线从没断供。Agent 运行时有 max_steps 防死循环，弹药出库前还要过安检：23 条脱敏规则加 5 条高危直接 400，Handler 层漏了还有 Logger 层独立再脱一次——不信任调用方，信任机制。指挥部与兵工厂说同一门语言：OpenAI 兼容。weclaw 的 HTTP 型 Agent 天然吃得下这种弹药，设计上把网关地址一填就能直连。兵工厂的规矩就八个字——弹药要足，走火要难。

哨兵线是 wxwatcher，管感知。NAS 上的目录、多台机器上的工作区，文件一变，微信即知：默认三十秒一轮，两阶段扫描先 stat 快筛，只对疑似变更的文件计算 SHA256，五千多个文件的目录每轮毫秒级，大文件只哈希前 8KB；推送分批，防刷屏；状态原子落盘，重启不重建基线；还有单轮模式，适配定时器与常驻两种世界观。通道走 api.yuangs.cc 的 weixinpush 总机。有意思的是军团里有两个哨兵，各盯各的更：podcastd 的 fsnotify 盯着 NAS 给生产线喂料，是哨兵对系统；wxwatcher 把文件的动静推给人，是哨兵对人。系统看系统的动静，人看人的动静，感知才不留死角。

情报线是 zhihu_extension。一键复制、划词浮动复制，正文转 Markdown 后博客与 NAS 双目标落库；alarms 定时查收藏夹，diff 出新文章，抓正文，推 IMA 笔记，写监控日志；期货行情面板覆盖六大期货交易所，数据带多轮重试与最后成功快照，断网也有东西可看。它的存储处处有界：文章收藏上限二百、删除记录一千、已见 ID 五百、监控日志二十条——每一处缓存都自带天花板，这是第八章讲过的纪律，也是情报站能常年服役的前提。这些情报不止给我读：行情数据从 fm.want.biz 进播客工厂的 fetcher，变成当天早餐档的稿子。情报矿场挖出来的矿，一半人吃，一半军团吃。

喂养还是双向的。knowly-mcp 把档案馆变成 Agent 的阅览室：search、entry、tags、status 四件工具，只读——公网入口的 Agent 没有写权限，这是刻意的安全设计；想让 Agent 写知识库，走两段式的发布流。这个五点九 MB 的 Go 静态二进制部署在常开的主机上，独立于 weclaw 发版，单独更新；它的文档里有一条必踩坑写得很有生活气息：环境变量一丢，工具就朝着本机回环地址敲门，报错里一串连接拒绝——看到 127.0.0.1，就知道是 env 丢了。于是知识复利有了第二条收益路径：存进去的每一条，都可能在某次微信问答里被检索、被引用、被讲给我听。档案馆从被动归档，变成了主动供血。

供养线自己也会断，所以每条线上都装着保险。knowly 的 AI 加工失败，降级为原始内容照常同步——标签和摘要可以没有，粮不能丢；sourcepack 有预览模式，先看会打包什么、多少 Token，再决定生成；wxwatcher 有同款先试后用的基因，零配置就能跑一轮看变化；期货行情断了网，回退到上次成功的快照，面板永远有东西可看。这些兜底分散在前面的章节里出现过，放回供养线的全景里再看一遍，它们说的是同一句话：断粮不等于断炊——军团的伙食标准可以临时降低，但开饭必须准时。

两条供养线还会在同一句话里交汇。在微信里问一句：查一下上周存的知乎热榜分析，对比今天的。挂了双工具的 Agent 会把知识库检索与热榜拉取组合起来调用，先翻档案，再看今天，最后给我一份对比。这是喂养的最高形态——不是人搬数据，是 Agent 自己取粮。而喂养能成立，靠的不是善意，是形状：Markdown 快照、OpenAI 兼容接口、带 frontmatter 的归档、纯文本的微信消息——所有输出都长成别人能直接吃的样子。数据流与工具流双轨并行，标准形状就是两条轨道的轨距；轨距一致，九个作品才跑在同一条铁轨上。

## 10.4 基建复利：一次建设，处处受益

复利是 knowly 的词，但这个词真正的用武之地在私有基建。知识复利说每一次复制都是存款；基建复利说每一次基建都是本金。api.yuangs.cc、want.biz、knasync、NAS、R2，这套个人 API 中枢、公网服务域、跨机队列与私有存储，就是军团的后勤系统，也是本章要算的账本。复利的公式人人会背：本金乘以一加利率的 n 次方。基建的利率，就是复用率——复用的兵种越多、调用越勤，这本账滚得越快。这一节算三笔账。

第一笔是时间账。weixinpush 微信推送住在 api.yuangs.cc，写第一遍花了一个晚上：鉴权、重试、消息格式、失败降级，样样都要想。之后呢？wxwatcher 接它，一行配置；podcast 的通知接它，一个函数；zhihu_extension 的采集回执接它，一次调用；weclaw 给我回文本消息，第一通道也是它。哨兵推送出去的消息，尾签都盖着同一个邮戳——苑广山的文件监控助手，By 那一栏里是军团的姓氏。连 wsstunnel 都自带一个 wxpush 开关：后端上下线时发条微信告我，一个穿透工具默认就长着回执通道，因为它出生的时候，推送这条公路已经通车。第一遍的成本被后来的每一次接入摊薄，接入越多，每一次越便宜。半夜是总机的考场：任何一台机器上的任何脚本，一行调用就能把消息送到我手机上，不用注册，不用审批，不用求人。这笔账在第一章算过一遍，现在算第二遍：那一个晚上的成本，到今天已经被五个兵种的日常调用，摊成了账本里的一行固定成本。

第二笔是边际成本账。在没有基建的年代，立项一个新工具意味着什么？域名、服务器、推送通道、存储、队列、模型网关、图床——后勤清单比业务清单还长，大多数个人项目就死在这张清单的第三行。如今我立项，清单上只剩业务逻辑本身：新脚本要通知，一行调用；要归档，推给 knowly；要被抓取，提交 knasync；要脑力，指一个 OpenAI 兼容端点；要图床，pic.want.biz 已经等着。sourcepack 顺手就带 -p 远端中继，因为队列的审美已经在那里。新增一个项目的边际成本趋近于零，这不是修辞——基础设施的全部意义，就是把从零开始从每一次创业里删掉。以前我掂量一个想法，先问服务器在哪；现在我掂量一个想法，只问这次要不要新建一个队列。

第三笔是摊薄账。固定成本同样在被九个作品分摊。want.biz 一块域挂五块牌：pic 是 R2 图床，upload 是 Knowly 的上传入口，q 供期货行情，fm 是行情站，poe 写稿；api.yuangs.cc 一台总机接五个兵种的进出；NAS 一台机器同时是 knowly 的知识圣殿、播客工厂的进料口与旧档仓库，还是 knowly-mcp 回源的后备库——knowly 经 SSH 直传，known_hosts 首次信任，不装服务端、不借第三方云；R2 一个桶既当图床又当播客分发渠道，rclone 一条命令把成片推上去。域名、证书、机器、存储，这些固定开销除以九个作品，再除以一千多期播客、近七千条归档，单价低到可以忽略。而我真正持续付出的，只剩一点维护纪律：证书记得续期，服务死了重启，额度偶尔看一眼。

基建也有基建的险，这一点要诚实：它是单点。api.yuangs.cc 挂了，所有推送一起哑；NAS 不可达，归档与进料一起停。军团的应对不是上高可用——一个人养不起双活机房——而是老三样：层层兜底在基建层照样复用。podcast 的通知三级降级，企业微信图文失败退纯文本，再退桌面通知；knowly 的 outbox 离线暂存，断网不丢条目，恢复后自动排空；wsstunnel 四层保活，链路断了自愈重连。连备份与迁移都被基建简化到了极点：weclaw 的全部家当落在 ~/.weclaw 一个目录里，备份就是拷目录，搬家就是搬目录——基建的尽头是文件系统，文件系统的尽头是安心。单点可以有，但每条经过单点的路，都要自带备胎。

还有一笔账不在钱上，在权限上：共享基建的每一扇门都默认锁着。一台总机被攻破，全军的消息都会泄露，所以 api.yuangs.cc 的鉴权、管理后台的口令、网关的令牌各自独立成轨；wxwatcher 从 1.7.0 起删掉了默认推送令牌，宁可让每个用户多敲一行环境变量，也不在仓库里留一把万能钥匙；knasync 的密钥比较用常量时间算法，把时序侧信道的门缝也焊死。共享是效率，鉴权是门票——军团的后勤系统对陌生人有多冷漠，对自己人就有多可靠。

算完三笔账，可以说清基建与集成的区别了。集成要求每一次对接都重新谈判耦合，项目间互相伸手进对方的内部；基建是先把公用设施修好、把边界立稳，然后谁都可以来接。组合优于集成，落到基建这一层，就是能力复用先于代码复用：我复用的从来不是某个函数，而是推送、存储、队列、网关这几种能力本身。基建复利与知识复利还互为镜像：知识复利让每一条信息不白复制，基建复利让每一个作品不白写。而复利的本质是利息再投资——knowly 长出了 knowly-mcp 反哺指挥部，knasync 长出了 MCP 服务器反哺所有 Agent，基建的利息又被投回基建。一次建设，处处受益；处处受益之后，再建下一处。

## 10.5 演化中的版图：从工具到生态

写到这一节，版图像一张成型的地图。但军团不是完工的建筑，是行军中的建制。先用第一章阅兵时的叫法报一遍编制：指挥部 weclaw，情报站 zhihu_extension，兵工厂 poeapi_go，档案馆 knowly，广播站 podcast，哨兵 wxwatcher，工程兵 wsstunnel，翻译官 sourcepack，宪兵 npm_yuangs。九个兵种，一个不少。本章前面四节讲的协同，就是这九个名字之间的连线。

版图的边缘正在生长，营房里出现了新名字。deepseek-harness，一个开源的 Agent harness，一切皆插件的架构，开发者预览阶段，迭代很快，文档里明说未来会有破坏兼容性的变更——这样的新兵，我让它先在试装区待着，不急着列装。它的仓库里已经躺着一篇研究 WeClaw 意义的长文，像新兵入营前交的侦察报告：工具的价值不取决于它有多强，而取决于它离你的动线有多近；WeClaw 做的事，是把 AI 的入口从一个 App 降维成一条消息，让一个微信账号背后站着一支属于一个人的 AI 委员会。这份判断与当年驱动我写 weclaw 的判断同源——英雄所见略同的时候，把谁编入队伍就成了组织问题，而不是技术问题。还有 Reasonix，一种项目记忆的格式，已经出现在 knowly 与 poeapi_go 的仓库里：把用户用井号钉下的笔记，装进每次会话的不可变系统前缀，poeapi_go 那份里甚至还留着二十二个免费模型的实测评分。记忆正在从各项目的私有财产，变成可以独立列装的兵种。这些新兵最终站哪个岗位，要等它们各自打过几仗才知道；但入列的方向是清楚的：沿着指挥、记忆与执行这些老命题，继续加厚。

作战地图上还有一批标着待开发的箭头，出自知乎能力的 roadmap。每日热点日报：拉热榜，选五到十条，直答展开背景，日报发微信再归档 knowly。PDF 按需解析：把文件解析接进微信动线，额度低频但刚需。用户数据备份：把知乎创作与收藏全量拉回，归档到 knowly 与 NAS。研究资料批量采集：搜索出链接，走 /zhihu 那条全文链路，滚成一个中文资料库。knowly 自己还藏着一步：cluster 包做历史条目的离线 AI 聚类，让近七千条账本自己长出目录。这份清单最值得看的地方在于：没有一项需要从零造轮子。日报是热点接口加既有的发布链路，批量采集是搜索加既有的抓取链路，备份是用户数据接口加既有的归档链路。所谓下一步，全是现有兵种的新阵型。

工具集和生态的分界线在哪？我的判据有三条。第一是互相发现：队列让生产者与消费者不必认识就能做成一笔生意，扩展不知道链接来自微信还是快捷指令，照样把活干完。第二是配置即接入：poeapi_go 的 config.yaml 加供应商零代码，weclaw 的触发词表可以热加载，wxwatcher 的配置有四层优先级，新能力以配置而非代码进入系统。第三是格式即契约：Markdown 加 frontmatter 是军团通行的公文格式，兼容 Hugo 与 Jekyll，可以用 grep 搜索，可以用 rsync 备份——任何新成员只要肯说这门普通话，就能同时被档案馆收录、被翻译官引用、被 Agent 检索。三条全占，工具集就开始向生态滑动。生态的意思，是新成员不需要我亲手接线。

这支军团打的到底是什么仗？不是对外征服，而是对内的确定性建设。它没有对手，只有敌人——遗忘、摩擦、单点、意外，以及一个人顾不过来的事实。所以它的每一场战役都安静：一条消息被归档，一次变更被通知，一期播客按时开播，一单任务被礼让地放回队列。军功章上没有惊心动魄的数字，只有日历：两千三百多次提交，一千多期节目，一个服役一年半仍在迭代的扩展，近七千条从不缺席的归档。协同的最高评价不是漂亮，是可靠；不是它打赢了谁，是它让我从来没有输给过混乱。

允许我用本章唯一一段排比，把组合优于集成的三个层次说满。集成，是把所有功能塞进同一个进程，从此同生共死、一起腐化；组合，是让独立的小工具互相调用，边界清晰，各自生长；协同，是让独立的小工具在共享的哲学与共享的基建上互相成就——指挥部知道驿道在哪，驿道知道档案馆收什么，档案馆知道自己终将被检索。集成造出的是产品，组合造出的是工具箱，协同造出的，是军团。

回到总纲。世界的不确定性有一种最日常的形态：一个人的注意力顾不过来。而协同的意思，是我的注意力可以同时在九个地方——情报站在采集时，哨兵在放哨，广播站在播音，驿道上跑着我不必盯着的任务，档案馆替我记下一切，宪兵盯着所有越权的念头。用技术的确定性，对抗世界的不确定性，在这一章有了具体的形状：那不是一句口号，是一张六台设备、九个兵种、一条驿道、一本近七千条账本的作战地图。有人问过我，广山哥，你这些工具到底为谁做的？为一个每天要顾很多事、但只雇得起自己的人。至于我为什么放心把这么多事交给军团——答案在下一章：因为这支军团的方法论，每一条都可以摊开来给后来者看。

---

## 第11章 写给后来者：一个人的军团方法论

# 第 11 章 写给后来者：一个人的军团方法论

书到终章。前十章是战报，这一章是操典：我把九个作品反复使用、并且被时间验证过的做法，提炼成一份可以带走的方法论。它不为军团立碑，只为后来者修桥——修给屏幕前的你，也修给半年之后忘光细节的我自己。你需要知道的第一件事是：这套方法不靠天赋，不靠运气，甚至不太靠勤奋，它靠的是一组可以逐条模仿的纪律。下面五节，五个动作；最后一节，把全书的旗帜交给读到这里的人。还有一件事要交代：终章没有新故事，只有旧故事的用法。九个作品的细节都留在各自的章节里，这一章只负责把它们拧成一股绳——方法论的价值不在多，在于每一条都真的被执行过。

## 11.1 问题驱动，最小改动：七轮演进的启示

军团没有产品经理，没有季度规划会，也没有一份印着愿景的路线图。常有人问我这些项目是怎么排期的，答案可能令人失望：不排期。驱动演进的从来不是规划，而是问题——问题什么时候来，工期就什么时候开始；问题有多大，改动就只能有多大。这是军团的第一条方法论：问题驱动，最小改动。它是九个作品共同的成长算法，也是一个人能维护十几万行代码而不被压垮的全部秘密。军团的路线图其实存在，只是它不长在文档里，长在 git log 里：每一次提交，都是一次已经兑现的承诺；没写出来的那部分，才是真正的待办清单——写出来却没做的计划，军团从来不欠。

最完整的标本是 knowly。它从约 2000 行长到约 1.3 万行，496 次提交，37 个文件，文档里把这段路总结为七轮架构演进：基础验证、三层去重、连接韧性、历史回溯、安全过滤、路径修复、离线 resilience 与 AI 增强。把七轮摆开看，每一轮背后都站着一个真实发生过的麻烦：复制过的内容反复出现，才有三层去重——内存哈希、本地状态文件、远端哈希索引，一层失忆还有两层记得；SSH 说断就断，才有连接韧性；想找回被覆盖的片段，才有历史回溯与一键恢复；敏感内容差点出门，才有安全过滤；路径上踩了坑，才有路径修复；断网时真丢过东西，才有 outbox 离线暂存。它的文档把这条例写成了一句话：每次迭代都遵循“最小改动”原则——只解决当前问题，不做过度设计。七轮走完，没有一轮来自图纸上一次性的宏伟设计，全部来自“事发”。

为什么必须是“最小”？这不是谦虚，是力学。变更越小，越可审；越可审，越可回滚；越可回滚，人就越敢动手。单人开发的胆量从来不是莽出来的，是“每一步都能撤”给惯出来的。反过来，大爆炸式重写的诱惑每个人都体会过：看着旧代码不顺眼，想推倒重来，换一种“更优雅”的结构。单人开发者没有评审会拦着你，没有同事问你一句“值得吗”，唯一能拦住你的，是那个朴素的问题——这个改动要解决的问题，真的发生过吗？在军团，拿不到真实问题签发的传票，一行代码也领不到军饷。优雅是跑起来之后长出来的品质，不是开工之前画出来的图纸。

这套算法还有个前置问题：问题从哪里来？军团的答案朴素得近乎笨：从每天的真实使用里来。剪贴板丢过东西，才有 knowly；人不在终端前错过告警，才有 wxwatcher；半夜进不去沙箱，才有 wsstunnel；饭点没空读行情，才有广山之巅；读过的文章转头就忘，才有 zhihu_extension。九个作品的第一个用户都是我，每一次“用着别扭”都是一张自动生成的需求单——不需要用户调研，不需要访谈，疼痛是实时的，优先级也天然正确，因为疼的人就是写代码的人。后来者常问“我该先学什么”，军团的答案是：先找一个你自己的问题，它要小到一两周能跑通，又真实到你每天都会疼。

wsstunnel 的成长史用的是同一套语法。它的文档里写：从单文件脚本到标准包，从闭包到类，从无测试到 103 个测试——每一步都有明确驱动力，不为了“优雅”而重构。今天的 wsstunnel 核心四模块 2705 行、测试 1975 行、168 次提交，看上去像一份精心设计过的成品，其实是顺着一个个具体问题长出来的。weclaw 的四层分层也一样，深度分析报告里坦白承认：这个分层并非教科书式的预先设计，而是演化出来的秩序——巨型函数持续拆分，文档与代码同步生长，秩序是长出来的，不是画出来的。podcast 的进化式重构更典型：旧入口保留五行兼容层转发到新位置，再用 Go 重写编排层，但保持现有 Python 音频引擎——进化，而非革命。先跑起来，再长好；丑但能跑的第一版，永远好过漂亮但不存在的设计图。

这套算法也有看起来像例外的时刻：某天我终于下决心给 weclaw 拆开那个越写越长的巨型函数，给 wxwatcher 换掉粗糙的正则缓存——这些动作看上去是“为了优雅”，其实传票上写的仍然是问题：没有人敢动的函数是问题，越跑越慢的扫描也是问题。重构本身可以是最小改动，前提是它解决的那个问题真实存在，并且改动被切得足够小，切到随时可以扔在半路而不伤主流程。真正需要警惕的只有一种情况：问题清单是空的，而重构的冲动是满的——这时候关掉编辑器去睡一觉，第二天那个“优雅”的念头，大概率就自己过去了。

但最小改动不等于只增不减，删除也是演进，而且是更高级的演进。poeapi_go 有一次战果辉煌的瘦身：一口气删除 16 个未接入的死包，净减 2299 行。删代码比写代码难得多——写代码是在证明“这个功能有人要”，删代码是在承认“我当年猜错了”。那 2299 行不是白交的学费，学费换来的，是剩下 6629 行的每一行都有买家。死代码留在库里不是资产，是噪音：它让下一个读代码的人分不清哪部分还活着，让每一次全局搜索都多出一批假线索；而那个“下一个人”，在军团里通常就是半年后忘光细节的我自己。对“体系干净”的执念，说到底是对未来的自己负责。

zhihu_extension 的起点只是一个“一键复制”按钮，如今长成连续服役一年半、约 8700 行的情报基建，每一次迭代都源于我每天真实的使用动线；sourcepack 的首条提交只有一句“A tool to conveniently generate documents for your project!”，没有宏大叙事，工具本身就是全部表达。把这些故事叠在一起，军团的成长算法就浮出了水面：先让它跑起来；遇到真问题；做最小的修复；把过程记录在案；继续跑。第 1 章说过——哪一环断过，哪一环就多一道锁。锁就是这么一把一把加上去的，而不是在图纸上一次画完的。写给后来者的第一句话就是：别急着设计完美，先允许你的工具丑着活下来。

## 11.2 诚实是最高级的工程品质

如果说问题驱动解决的是“往哪走”，诚实解决的是另一个更难的问题：怎么知道自己错了。军团有一件不起眼却最贵的装备——敢于把自己难堪的部分，原原本本写进公开文档。诚实在这里不是美德课上的名词，而是一个工程动作：它把“我以为”与“实际上是”之间的距离，变成文字，钉在所有人都能看见的地方。这是第二条方法论，从一个战损报告说起。

wsstunnel 的文档里躺着一篇《生产升级翻车记》，我逢人便推荐。故事的前半段是一次漂亮的自我怀疑：review 自己的代码时，开发日记里写着——光靠肉眼读代码，很容易把“看起来有问题”当成“确认有问题”。于是我没有下结论，而是写了个探针脚本去实测，日记里补了一句：这个决定被证明是今天最有价值的一步。探针一次实锤 12 处问题，换来 466 行修复，测试从 131 个涨到 176 个。故事的后半段更诚实：紧接着的生产升级又翻了一次车，websockets 新旧两代 API 的差异，被一次“全绿测试”干干净净地掩盖了。于是又一篇复盘，标题就把伤口亮出来：一次“全绿测试”掩盖的 API 时代差异。两次诚实连在一起才算完整——第一次，诚实地怀疑自己的判断；第二次，诚实地承认自己的翻车。“看起来”与“确认”之间隔着的那个探针，就是工程与玄学的分界线。

对用户的诚实体现在细节里。wsstunnel 的 README 专设“已知限制”一节，主动交代并发时序、无压缩、无审计——特性列表人人会写，限制列表才见人格；30 秒极简演示的上方，还挂着一行不加修饰的警告，明说这套演示无认证、仅限内网，生产环境请自行补上令牌与 TLS。把糖递出去的同时把苦味标签贴好，这不是法律义务，是工程教养：用户可以被你的功能吸引，但只有你的坦白，才能让他放心把生产环境交给你。

诚实还需要基础设施托底，不然只是嘴上说说。yuangs CLI 把每一次执行都记进可回放的档案：explain 能把一次决策的来龙去脉讲给你听，replay 能把当时的现场原样重演，asciinema 录像把“谁被拦截、谁被放行”写进终端时间轴。这些机制让诚实变得便宜：既然每一步都有据可查，坦白就不再依赖记忆，也不再依赖心情。poeapi_go 的日志可以整体关闭，但“能关”这件事本身写得明明白白——能关，且关得透明，这是诚实的另一种形态。

对自己的诚实更难，也更稀有。npm_yuangs 的架构文档里有一句相当不留情面的自曝：WASM 沙箱与 YAML policy 是架构声明，不是运行时保障。说完还不算，后面逐条列出风险清单与修复状态——叙事与运行时保障之间的差距，被白纸黑字钉在可审计的文档层面，谁来验收都行。wxwatcher 有一笔更老的账：1.7.0 的提交信息写着 remove default push token, force env config——翻译过来就是：我以前在仓库里留过默认推送凭证，后来我亲手删了它，宁可让每个用户多敲一行配置，也不再让一串裸奔的令牌躺在安装包里。这两份文档有一个共同点：都写在没人逼的时候。事故之后才出现的诚实叫危机公关；没人追问时主动交代的诚实，才叫品质。品质这东西，平时看着浪费，出事的时候就是命。

当然，诚实有边界。对用户诚实，不等于把所有犹豫都倒给用户；已知限制是选择性的坦白——写下来的，是用户做决策真正需要的；没写下来的，不是隐瞒，是不想让文档变成忏悔录。判断标准只有一条：如果这个事实会影响一个理性用户的选择，它就必须出现在文档里。按这个标准，wsstunnel 的时序问题该写，因为它影响并发场景的选型；wxwatcher 的旧令牌该删，因为它影响每一个用户的密钥安全；至于我某天为一条正则纠结到半夜，这种事就不必劳烦读者了。

诚实还有更日常的形态。REASONIX.md 里躺着一张 22 个 NVIDIA 免费模型的实测评分表——用实测数据而非宣传页做技术选型，是对供应商诚实；sourcepack 的每个 bugfix 提交都写明因果，哪条路径关键字被误加了前缀、哪个过滤因此失效，一条提交就是一篇微型案例分析，这是对后来者诚实；podcast 把故障史留在注释里，plist 中那句“不随系统启动（避免 launchd 无用户上下文时弹 NAS 授权窗）”，是对下一次排障的自己诚实；weclaw 的 README 郑重注明灵感来源与“仅供个人学习”，是对边界诚实。poeapi_go 自评“这是体系，不是脚本”——自评不是不能说，前提是每个数字可复核：行数、提交数、版本号，全部来自公开仓库，谁都可以查。探针后来也成了军团的通用习惯：不只在翻车之后用，而是在每次“感觉不对”的时候用——感觉是概率的，探针是确定的，把感觉升级成探针，就是把玄学升级成工程。

诚实面向用户，也面向自己，红利有三。其一，翻车的信息不流失，同一块石头不会绊倒同一个军团两次；其二，信任可累积，读“已知限制”的人比读特性列表的人更容易成为长期用户，因为前者知道你没有瞒他；其三，一个人也敢睡觉——不必靠记忆记住哪些地方是虚的，文档替你记着，记忆会撒谎，落盘的文字不会。诚实当然有代价：它让每一页文档都短了几分光鲜。但军团算的是另一笔账：名声不是靠无事故建立的，而是靠每次事故之后，文档比事故先到。这句话第 1 章说过，终章再说一遍，因为它值得。顺便把价目表也贴出来：诚实的短期成本是难堪，长期收益是复利——一次坦白换来的信任，比十次成功更耐用；这笔账，单人军团比大公司更算得过来，因为我们没有公关部门替我们把话磨圆。

## 11.3 单人公共品：一个人也要有完成度

单人项目最常听到的质疑是两个字：玩票。今天写着爽，明天就弃坑；能用就行，谁管它崩。回击的方式不是辩解，而是完成度——用对待公共品的标准，对待自己顺手写的工具，让“一个人的项目”在纪律上不输给一个团队。这一节解剖三份标本，看看一个人的军团如何回答那个迟早会来的问题：凭什么信你。

第一份标本是 wxwatcher。源码 1188 行，测试 888 行，90 多个测试用例——测试体量接近源码，这是我给小工具立的规矩。它发布在 PyPI 上，版本 v1.15.0；发布流程是打 tag 自动出包，认证走 OIDC——整条流水线里没有一枚长期密钥可供泄露，发布纪律本身就是默认安全的实例。面向用户，它有零配置试用：不配任何令牌先跑一轮，只检测并打印变更，不推送、不写状态——先体验，再配置；它有单轮模式，适配 cron 与 systemd timer，同一个工具同时服务常驻与定时两种世界观；部署附件 wxwatcher.service 带 NoNewPrivileges 与 ProtectSystem=strict 加固；提交信息当变更日志，每个版本号写清对用户意味着什么。三步上手，一步都不用翻文档：

```text
pip install wxwatcher
wxwatcher --dry-run ~/watched        # 先看看它会报什么，不推送、不落状态
wxwatcher ~/watched --once           # 满意了再纳入正式监控
```

第 5 章数过 PyPI 之外的九件事——优雅退出、原子写、四层配置、分批推送、按目录隔离日志——这里不再重复点名，只补一句：那九件事没有一件来自宏大规划，全部来自问题清单，和 knowly 的七轮一模一样。完成度是结果，问题驱动才是原因。

第二份标本是 sourcepack。npm 包 v1.7.1，Go 编译出覆盖三大系统、两种架构的五平台静态二进制，npm postinstall 按平台下载，GoReleaser 出包，CI 测试矩阵护航。最见纪律的是提交信息：连 CI 环境的怪 bug 都写清楚留给后人——升级到 Go 1.23+，规避 macOS 15 runner 的 LC_UUID 工具链 bug；哪条路径关键字被误加了前缀、哪个过滤因此失效，修复说明随提交入库。工具虽小，账本齐全，后来者顺着提交记录读下来，就像读完一部微缩工程史。

第三份标本把完成度推到了形式化的极端：npm_yuangs 有 48 篇文档、75 个测试文件约 8100 行，甚至有一份 TLA+ 形式化规约，把提案与门的关系写成数学可以检验的陈述；发布门禁 verify.sh 九步，校验 npm 包“不含 src/”才允许发布——连“发出去的是什么”这件事，都有机器把门。wsstunnel 则补齐了开源的另一面：PyPI、CI、英文 README、CONTRIBUTING，外加 8 篇中文深度文档——一个绝境里自救出来的工具，活成了正经的开源项目。单看任何一个，你都不会想到它们出自同一双深夜敲键盘的手；放在一起看，你会发现那是同一套肌肉记忆。顺带说一个容易被忽略的细节：48 篇文档不全是写给用户的说明书，其中相当一部分写给未来的自己——架构决策、风险清单、形式化规约，全是留给下一个维护者的便条，而那个维护者，还是我。

这些纪律背后只有一句哲学：发布纪律让玩具变成作品。玩具与作品的差别，不在行数，在于有没有把“给陌生人用”当成一种承诺。测试是写给半年后忘光细节的自己的；文档是写给未来接手者的——哪怕接手者大概率就是自己；无密钥发布是把纪律固化成机制，而不是停留在愿望。第 1 章说过一句：完成度不是给别人看的礼仪，是给未来的自己留的绳梯。终章把它钉死：单人项目完成度的受益人排序里，第一位永远是你自己——何况单人项目没有同事替你收拾残局，完成度不是装点，是自救。

如果你也想把一个顺手写的小工具做成公共品，三份标本合起来正好是一张可以照抄的清单。第一，发布自动化：打 tag 即出包，把发版的仪式感交给机器。第二，无密钥发布：短时凭证或工作负载身份认证，别让长期密钥有机会住进任何配置文件。第三，测试与源码同权重：90 多个用例，不多不少地盖住关键路径。第四，零配置试用：一条预览命令，让陌生人在十秒内看到价值。第五，部署附件随包走：systemd 单元、加固参数、日志轮转，替用户把安全默认值设好。第六，提交信息当账本：每个版本号写清对用户意味着什么，每个修复写明因果。第七，连怪 bug 都留给后人：CI 工具链的坑也值得一条提交说明。做到这七件，1188 行就有作品的资格；做不到，四万行也只是玩具。

完成度还有一条边界：它不追求大而全，只追求小而完整。这恰是小即美支柱的另一面——公共品完成度与规模无关，与闭环有关。1188 行可以是一个作品，44657 行也可以；1413 行定义了一个新文体，254 个文件撑起一套治理运行时；判据从来不是行数，而是断网、断电、断人之后，它还能不能体面地工作。顺带交个底：本书里出现的每一个数字——行数、提交数、版本号、期数——全部来自公开仓库与发布记录，谁都可以复核。一本讲确定性的书，至少自己得先经得起检索；这也是完成度的一部分，书也不例外。

## 11.4 AI 时代的个体杠杆：军团还会扩编

先披露一个编制细节：军团的点名册上不止九个名字。knowly 与 weclaw 的提交记录里，大量提交联署着 Qwen Assistant——AI 在这支军团里是署名的合作者，不是隐形的枪手。九个作品全部诞生或生长于 AI 能力爆发的这几年，没有谁能与 AI 撇清关系：思路有一半是模型出的，初稿有一半是模型写的，连部分测试都是模型陪跑的。所以问题从来不是用不用 AI，而是用什么姿势用——姿势不对，杠杆撬动的是你自己；姿势对了，一个人才敢自称军团。

姿势写在这句宪法里：AI 提供思路，人类掌控执行。这不是妥协，而是对工程理性的尊重。它的日常版更口语：不 OOM，不惊喜，始终有人类在环。落到动作上，写工具这件事被分解成四个环节——提问、审题、拍板、验收。过去是从第一行敲到最后一行；现在，我把体力活外包给模型，把判断留给自己：模型出思路、写初稿、查资料，我定架构、做取舍、签字画押。weclaw 是这种关系的具象化：以个人微信为入口、以多种 AI Agent 为后端——指挥部里坐满顾问，印章只有一枚。从“我写工具”到“我指挥军团”，变的不是工作量，是权力的位置；而权力的位置，决定了出事的时候谁负责。

人在环上是个体开发者的宪法，这句话值得单独展开。在团队里，AI 走偏了有人复核；一个人的时候，模型的过度自信没有人兜底，唯一的安全阀是制度。所以军团把这条宪法写成了代码：yuangs CLI 规定，一切可能产生副作用的操作都是提案（Proposal）而非指令（Action），必须通过用户关卡（gate）；TokenPolicy 四级预算门，预算越满，需要确认的层级越高；5 因子风险打分，信任度按天衰减，今天放行的命令不代表明天继续放行；写入类工具无论评分多少，一律强制人工审查。发送之前，Swiss-Cheese 式的采样预览让你亲眼看到每一块将被送出的上下文——你的终端，你的数据，你的决定。它的 non-goals 文档用八条主动排除划清边界——不支持自治执行，不支持自推进循环，不做隐式上下文扩展，核心论证只有一句：上下文本身是一种能力，任何能力，都必须被明确授予。服务端同样：工具白名单之外一律不可用，code_exec 只放行 7 条无害命令并强制 5 秒超时，max_steps 防死循环；连 Agent 能力都用构建标签隔离成可选装配——重武器平时入库，战争来了才发。README 结尾那句话，是我对 AI 的全部态度：AI 除非被明确要求，否则不应该比输入看起来更聪明。

这套宪法也有温柔的一面。weclaw 对 ACP 权限请求的处置是自动代答放行，同时把“正在调用工具”的进度推送到聊天窗口——一代一告之间，流程没有断，人也始终知情。严格说，这是一种有边界的放行：放行的只是低风险动作，高风险仍要人来点头，而每一次代答都留下痕迹。人在环上不是把人拴在每一个按钮前，而是把人的注意力花在真正值得的关卡上——确定性的部分交给机器，判断性的部分留给人，各干各的活。

还有一层时代背景值得写下来：思路正在通胀，判断正在变得稀缺。模型可以在十秒内给你十个方案，但挑出那个不会在三个月后爆炸的方案，仍然需要人——需要对真实使用动线的了解，需要对失败样本的记忆，需要对“够用”的诚实定义。这三样东西，目前都还只长在人身上；把它们外包出去的人，迟早会把责任也一并外包出去。

给独立开发者的建议，五条，每一条都被军团验证过。第一，验收标准自己定，用实测校准模型的自信：REASONIX.md 里那张 22 个模型的实测评分表，比任何测评文章都可靠；智谱渠道失效的那天，当天实测切换小米 MIMO，生产线没有停一档——模型的推荐是用来验证的，不是用来相信的。第二，一切副作用走提案：把“你确定吗”写进工具，而不是写进自我提醒——提醒会忘，门不会。第三，让每一步产物落盘，可审计、可回放：AI 说的话不可信，它留下的产物可以审；聊天记录是流言，落盘的产物是档案。第四，纪律写成工具，不贴在墙上：人靠不住，机制靠得住；你没有办法每次都想起纪律，但工具每次都会拦你。第五，先解决自己的问题，再顺手做成公共品：为自己造的东西才够诚实，够诚实的东西才有人信——这是九个作品共同的出身证明。五条之外，再送一句心法：对 AI 的输出保持礼貌的怀疑，对自己的判断保持警惕的自信——前者防止你被带偏，后者防止你把责任签出去。

军团还会扩编。DeepSeek-Reasonix、deepseek-harness 这些新兵已经在营房里，下一批兵员正在路上；军团编制的上限不是人数，是问题数——只要生活里还有靠不住的事，招兵就不会停。一个人的军团，公式从来是：一个有宪法的人，加一支不问编制的顾问团，加一套写进代码的纪律。杠杆放大的是责任，不是清闲——这话第 1 章说过，读到这里你该掂出它的分量：指挥一个军团的人，签发的每一个字都要认账。AI 时代的个体不是孤胆英雄，是编制表上唯一的常任军官——这句话也说过，但终章允许重复，因为它是这本书存在的理由。所以，如果这本书只能带走一句话，我会选那十二个字：AI 提供思路，人类掌控执行——它不激动人心，但它让激动人心的事情可以长期做下去。

## 11.5 尾声：用技术的确定性，对抗世界的不确定性

第 1 章从一个深夜讲起：十一点四十分，一段刚整理好的文字从剪贴板里消失了，像从来没有存在过。十章战报，九个作品，现在到了合并同类项的时刻。把所有故事压回一张地图，还是出发时的那张：五根支柱，三条方法论。这一节不做新事，只做一件事——把旗子升到顶，再看一眼它究竟由哪几块布缝成。缝旗子的布，一半来自九个作品的源码，一半来自那些深夜的提交信息与文档——哲学从来不在别处，就在每一次具体的取舍里。

五根柱子，各回各的位置。人在环上：yuangs CLI 的门（gate）让每一步写操作都要等人点头，poeapi_go 的白名单把 AI 的执行面压到七条命令——AI 提供思路，人类掌控执行，世界因此少了很多“惊喜”。对抗熵增：knowly 的 500 毫秒轮询，让每一次复制都成为知识复利，Keep Notions Always Saved，对抗遗忘与数字熵增——你随手一抄的碎片，半年后毫秒级归来。自己开一条路：wsstunnel 在每一扇焊死的门旁边，沿着对方留下的那道缝，走私出一个真终端；knowly 的数据不进任何人的机房，SSH 直达圣殿——现成的路走不通，就自己修一条。小即美：sourcepack 用 1413 行定义了一个新文体，wxwatcher 用 1188 行守住了哨位，knowly 的 go.mod 直接依赖只有三个——小不是缺陷，是把每一行都养到能打。服务生命节律：广山之巅每天四档，8:30、12:00、17:00、20:30，节假日从不缺席——世界的信息，终于重新适配了人的节奏，而不是相反。

三条方法论，横在每一个写代码的动作里。默认安全：先阻断、再脱敏、再落盘，AI 只看到脱敏后的内容，敏感词在本地过滤，密钥永不进前端——安全是默认值，不是可选项。层层兜底：outbox 离线暂存，网络恢复自动排空；指数退避加随机抖动；通知三级降级；优雅退出保存状态；断网回退上次成功数据——失败是常态设计，不是异常处理。写即持久化：一次输入，三重输出；一次采集，多渠道分发；每一步产物落盘，可审计、可回放——凡产出，皆档案。支柱是名词，方法论是动词，名词靠动词活着；行军至此，旗还在。

这套组合拳的实战成绩散落在全书各章，这里点名归队：podcast 的通知三级降级，让断网之夜照常开档；zhihu_extension 的断网回退，让行情面板永远有东西可看；knowly 的 outbox，让跨城出差的日子一帧不丢；wxwatcher 的优雅退出，让重启之后不必重建基线；weclaw 的双路径降级，让没有 Agent 被排除在外。五个仓库，几种语言，说着同一种方言。

最后，把这本书献给每一个曾被“关住”的人。被沙箱关住的人：每一扇门都焊死了，每一扇窗都贴着此路不通，连一根网线都通向审查；被平台关住的人：条款说改就改，风控说停就停，账号说没就没，你耕种的土地随时可能易主；被遗忘关住的人：收藏从未停止，学习从未开始，复制过的东西像从未来过；被熵关住的人：眼看着一切都在慢慢散架，却不知道从哪里收拾起。wsstunnel 的 README 里有一句赠言：希望能帮到每一个曾被沙箱“关住”的你。军团九件兵器，说到底就是九把钥匙——它们没能把世界变成确定的，但能把你那一部分变成确定的。

接下来是全书唯一一段允许动情的话。深夜十一点四十分的那段文字，今天不会再消失了：下一次 500 毫秒的轮询会把它接住，送进圣殿，编好号，落成索引，在半年后的某个清晨毫秒级回到我眼前。世界仍然不确定——服务会关停，平台会改规则，模型会一本正经地胡说八道，熵永远在场。但我已经不祈祷了。保存、脱敏、降级、留痕，这些动作我会一直做下去；机器动手之前，总有人被问一句“你确定吗”。用技术的确定性，对抗世界的不确定性。第 1 章说过，这句话的后半句不用背，你迟早会用自己的经历把它补全；书到了最后一页，现在，我把这句话还给你。

军团不解散。哨兵还在看门，档案馆每晚归档，兵工厂照常供货，宪兵仍在每一个回车前等你点头，情报站带着正负四分之一的抖动继续巡游——九个岗位，一个都不空缺。第 1 章末尾说过，军团存在的意义，是让我在不确定的世界里睡得着觉；写到最后一行，这个说法不变，底气更足。至于我，朋友口中的广山哥，书写完的这一晚，20:30 的晚间档照常播出——确定性这种东西，一旦真正拥有过，就舍不得再还给世界。晚安，后来者；你的问题清单，就当是军团的征兵启事。

---
